# Cold cubic diagonal executable acceptance

This directory is generated by:

```bash
python3 tools/export_hard_cubic_householder.py \
  --task QBE-HARD-CUBIC-DIAGONAL-HIER-COLD-001 --n 2
```

The finite Qiskit unitary follows the same ancilla-major controlled rational
Householder construction as the Lean root
`CubicDiagonalOracle.cubicDiagonalHouseholderExactBEContract_complete`.
`qiskit/acceptance.json` records the clean-block, unitarity, Qiskit `Operator`,
transpiled-QASM `Operator`, and OpenQASM3 parser gates.  This validates the
declared `n=2` export; Lean proves the symbolic family.
