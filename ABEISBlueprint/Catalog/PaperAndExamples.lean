import QuantumBlockEncoding
import Verso
import VersoManual
import VersoBlueprint

open Verso.Genre
open Verso.Genre.Manual
open Informal

set_option linter.hashCommand false
set_option linter.style.longLine false
set_option verso.blueprint.externalCode.strictResolve true

#doc (Manual) "Declaration catalog: PaperAndExamples" =>
%%%
file := "catalog-paper-and-examples"
%%%

This chapter is generated from the Lean source. Every node denotes one explicit public
declaration, and every Lean link is checked during the Blueprint build. Definitions appear
in source order before later results whenever the source module does so.

Reader orientation: Paper-facing backend models and concrete Robin-boundary example artifacts. Each card separates an accessible
reading cue from formal status, the source docstring, and the authoritative Lean panel.
The standalone Library Explorer adds full-text search and filters across every chapter.

# QuantumBlockEncoding/Examples/RobinHeat.lean

12 explicit public declarations, in source order.

:::definition "QuantumBlockEncoding.Examples.RobinHeat.fourthOrderSecondDerivative" (lean := "QuantumBlockEncoding.Examples.RobinHeat.fourthOrderSecondDerivative")
*Plain-English reading.* This definition gives the library's named construction or computation for “fourth order second derivative”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Examples/RobinHeat.lean:18](../../../../library/modules/examples-robinheat/#decl-quantumblockencoding-examples-robinheat-fourthordersecondderivative). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Examples.RobinHeat.centralBulkEntries" (lean := "QuantumBlockEncoding.Examples.RobinHeat.centralBulkEntries")
*Plain-English reading.* This definition gives the library's named construction or computation for “central bulk entries”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Examples/RobinHeat.lean:24](../../../../library/modules/examples-robinheat/#decl-quantumblockencoding-examples-robinheat-centralbulkentries). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Examples.RobinHeat.A1dx" (lean := "QuantumBlockEncoding.Examples.RobinHeat.A1dx")
*Plain-English reading.* This definition gives the library's named construction or computation for “a 1 dx”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Examples/RobinHeat.lean:33](../../../../library/modules/examples-robinheat/#decl-quantumblockencoding-examples-robinheat-a1dx). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Examples.RobinHeat.B1dx" (lean := "QuantumBlockEncoding.Examples.RobinHeat.B1dx")
*Plain-English reading.* This definition gives the library's named construction or computation for “b 1 dx”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Examples/RobinHeat.lean:34](../../../../library/modules/examples-robinheat/#decl-quantumblockencoding-examples-robinheat-b1dx). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Examples.RobinHeat.leftBoundaryRow0" (lean := "QuantumBlockEncoding.Examples.RobinHeat.leftBoundaryRow0")
*Plain-English reading.* This definition gives the library's named construction or computation for “left boundary row 0”. First row after eliminating the left Robin ghost points.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* First row after eliminating the left Robin ghost points.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Examples/RobinHeat.lean:37](../../../../library/modules/examples-robinheat/#decl-quantumblockencoding-examples-robinheat-leftboundaryrow0). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Examples.RobinHeat.leftBoundaryRow1" (lean := "QuantumBlockEncoding.Examples.RobinHeat.leftBoundaryRow1")
*Plain-English reading.* This definition gives the library's named construction or computation for “left boundary row 1”. Second row after eliminating the left Robin ghost points.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Second row after eliminating the left Robin ghost points.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Examples/RobinHeat.lean:45](../../../../library/modules/examples-robinheat/#decl-quantumblockencoding-examples-robinheat-leftboundaryrow1). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Examples.RobinHeat.rightBoundaryRowNm2" (lean := "QuantumBlockEncoding.Examples.RobinHeat.rightBoundaryRowNm2")
*Plain-English reading.* This definition gives the library's named construction or computation for “right boundary row nm 2”. Penultimate row after eliminating the right Robin ghost points.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Penultimate row after eliminating the right Robin ghost points.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Examples/RobinHeat.lean:54](../../../../library/modules/examples-robinheat/#decl-quantumblockencoding-examples-robinheat-rightboundaryrownm2). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Examples.RobinHeat.rightBoundaryRowNm1" (lean := "QuantumBlockEncoding.Examples.RobinHeat.rightBoundaryRowNm1")
*Plain-English reading.* This definition gives the library's named construction or computation for “right boundary row nm 1”. Last row after eliminating the right Robin ghost points.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Last row after eliminating the right Robin ghost points.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Examples/RobinHeat.lean:63](../../../../library/modules/examples-robinheat/#decl-quantumblockencoding-examples-robinheat-rightboundaryrownm1). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Examples.RobinHeat.robinWindow" (lean := "QuantumBlockEncoding.Examples.RobinHeat.robinWindow")
*Plain-English reading.* This definition gives the library's named construction or computation for “robin window”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Examples/RobinHeat.lean:70](../../../../library/modules/examples-robinheat/#decl-quantumblockencoding-examples-robinheat-robinwindow). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Examples.RobinHeat.oneTermParameters" (lean := "QuantumBlockEncoding.Examples.RobinHeat.oneTermParameters")
*Plain-English reading.* This definition gives the library's named construction or computation for “one term parameters”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Examples/RobinHeat.lean:74](../../../../library/modules/examples-robinheat/#decl-quantumblockencoding-examples-robinheat-onetermparameters). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Examples.RobinHeat.fourthOrderStencilWidth" (lean := "QuantumBlockEncoding.Examples.RobinHeat.fourthOrderStencilWidth")
*Plain-English reading.* Lean checks the proposition indexed as “fourth order stencil width”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Examples/RobinHeat.lean:80](../../../../library/modules/examples-robinheat/#decl-quantumblockencoding-examples-robinheat-fourthorderstencilwidth). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Examples.RobinHeat.robinHeatAncillas" (lean := "QuantumBlockEncoding.Examples.RobinHeat.robinHeatAncillas")
*Plain-English reading.* Lean checks the proposition indexed as “robin heat ancillas”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Examples/RobinHeat.lean:83](../../../../library/modules/examples-robinheat/#decl-quantumblockencoding-examples-robinheat-robinheatancillas). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/GHL2025.lean

403 explicit public declarations, in source order.

:::definition "QuantumBlockEncoding.GHL2025.OneTermRobinParameters" (lean := "QuantumBlockEncoding.GHL2025.OneTermRobinParameters")
*Plain-English reading.* This record groups the data and proof fields needed for “one term robin parameters”. A proposition-valued field is a requirement until a constructor supplies it.

*Formal status.* Data contract in the default import surface; proposition-valued fields are obligations, not automatically established facts.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* structure.

Source: [QuantumBlockEncoding/GHL2025.lean:18](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-onetermrobinparameters). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.isBulkRow" (lean := "QuantumBlockEncoding.GHL2025.isBulkRow")
*Plain-English reading.* This definition gives the library's named construction or computation for “is bulk row”. Classical specification of the indicator oracle U\_indic(K1,K2).

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Classical specification of the indicator oracle U\_indic(K1,K2). Returns 'true' when row index 'i' is in the bulk region \[K1, K2\], meaning U\_indic maps |i⟩|0⟩ → |i⟩|1⟩. Returns 'false' for boundary rows (0 ≤ i < K1 or K2 < i), meaning U\_indic maps |i⟩|0⟩ → |i⟩|0⟩. main.tex:1060-1065 -

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:32](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-isbulkrow). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.isBoundaryRow" (lean := "QuantumBlockEncoding.GHL2025.isBoundaryRow")
*Plain-English reading.* This definition gives the library's named construction or computation for “is boundary row”. Complement of isBulkRow: returns true for boundary rows (j < K1 or K2 < j).

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Complement of isBulkRow: returns true for boundary rows (j < K1 or K2 < j). The paper's boundary set is \{0,...,K1-1\} union \{K2+1,...,2^n-1\}. main.tex:1113, 1035-1038 -

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:39](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-isboundaryrow). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.RobinRegisterPartition" (lean := "QuantumBlockEncoding.GHL2025.RobinRegisterPartition")
*Plain-English reading.* This record groups the data and proof fields needed for “robin register partition”. A proposition-valued field is a requirement until a constructor supplies it. Detailed register partition matching the wavefunction ket labels in Eq.

*Formal status.* Data contract in the default import surface; proposition-valued fields are obligations, not automatically established facts.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Detailed register partition matching the wavefunction ket labels in Eq. ROBIN clarified (main.tex:1113-1117). Each field is the qubit count for one register in the circuit. Total signal qubits = m\_f + 1 + ceil(log2 kappa) + 4 (indicator + ancilla + 1), plus n system qubits. Pure ancillas appear in two groups totaling 2n. figure:1\_term\_ROBIN caption -

*Declaration kind.* structure.

Source: [QuantumBlockEncoding/GHL2025.lean:49](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-robinregisterpartition). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.RobinRegisterPartition.totalQubits" (lean := "QuantumBlockEncoding.GHL2025.RobinRegisterPartition.totalQubits")
*Plain-English reading.* This definition gives the library's named construction or computation for “total qubits”. Total qubits used by the register partition (all registers summed).

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Total qubits used by the register partition (all registers summed).

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:69](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-robinregisterpartition-totalqubits). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.defaultRobinRegisterPartition" (lean := "QuantumBlockEncoding.GHL2025.defaultRobinRegisterPartition")
*Plain-English reading.* This definition gives the library's named construction or computation for “default robin register partition”. Default register partition from concrete parameters.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Default register partition from concrete parameters. figure:1\_term\_ROBIN -

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:76](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-defaultrobinregisterpartition). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.RobinRegisterPartition.totalPureAncillas" (lean := "QuantumBlockEncoding.GHL2025.RobinRegisterPartition.totalPureAncillas")
*Plain-English reading.* This definition gives the library's named construction or computation for “total pure ancillas”. Pure ancilla qubits visible in the Eq.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Pure ancilla qubits visible in the Eq. ROBIN register partition: '(n - ceil(log2 kappa)) + 1' from the O\_D^BS register plus the trailing ancilla. This is intentionally narrower than the theorem's full '2n' pure-ancilla budget. The theorem-level count in 'oneTermRobinLayout' and 'oneTermRobinResourceExpr' also includes internal workspace required by the banded sparse-access and oracle subcircuits. Keep this distinction explicit to avoid treating the ket-level register partition as the full resource proof. figure:1\_term\_ROBIN caption, main.tex:1149, main.tex:1131-1136

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:96](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-robinregisterpartition-totalpureancillas). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.oneTermRobinResourceExpr" (lean := "QuantumBlockEncoding.GHL2025.oneTermRobinResourceExpr")
*Plain-English reading.* This definition gives the library's named construction or computation for “one term robin resource expr”. Theorem 1-term Robin resource shape: 'O(sum\_g Q\_g n log n + kappa n)' gates and '2n' pure ancillas.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Theorem 1-term Robin resource shape: 'O(sum\_g Q\_g n log n + kappa n)' gates and '2n' pure ancillas.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:103](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-onetermrobinresourceexpr). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.deviatingIndices" (lean := "QuantumBlockEncoding.GHL2025.deviatingIndices")
*Plain-English reading.* This definition gives the library's named construction or computation for “deviating indices”. Number of deviating (boundary) indices: K1 + 2^n - K2.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Number of deviating (boundary) indices: K1 + 2^n - K2. The paper notes this is O(1) as it depends on the finite-difference accuracy order. main.tex:1092-1095 -

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:112](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-deviatingindices). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.oneTermRobinPreciseResourceExpr" (lean := "QuantumBlockEncoding.GHL2025.oneTermRobinPreciseResourceExpr")
*Plain-English reading.* This definition gives the library's named construction or computation for “one term robin precise resource expr”. Precise gate cost formula from the text (main.tex:1088-1089), before absorbing the O(1) boundary deviation count into the Theorem's simplified formula.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Precise gate cost formula from the text (main.tex:1088-1089), before absorbing the O(1) boundary deviation count into the Theorem's simplified formula. 'O(sum\_g Q\_g n log n + kappa \* (K1 + 2^n - K2) \* n)' gates. The term 'K1 + 2^n - K2' is the number of deviating rows, which is O(1). main.tex:1088-1089 -

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:121](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-onetermrobinpreciseresourceexpr). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.deviatingIndices_example" (lean := "QuantumBlockEncoding.GHL2025.deviatingIndices_example")
*Plain-English reading.* Lean checks the proposition indexed as “deviating indices example”; the hypotheses and conclusion in the code panel fix its exact scope. deviatingIndices computes K1 + gridSize - K2, the number of boundary rows.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* deviatingIndices computes K1 + gridSize - K2, the number of boundary rows. For the fourth-order stencil with K1=2, K2=gridSize(n)-3, this gives 2+3=5. main.tex:1092-1095 -

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:130](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-deviatingindices-example). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.oneTermRobinResource" (lean := "QuantumBlockEncoding.GHL2025.oneTermRobinResource")
*Plain-English reading.* This definition gives the library's named construction or computation for “one term robin resource”. Numeric resource useful for concrete search runs with fixed parameters.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Numeric resource useful for concrete search runs with fixed parameters.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:134](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-onetermrobinresource). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.oneTermRobin_pureAncilla" (lean := "QuantumBlockEncoding.GHL2025.oneTermRobin_pureAncilla")
*Plain-English reading.* Lean checks the proposition indexed as “one term robin pure ancilla”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:140](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-onetermrobin-pureancilla). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.oneTermRobinLayout" (lean := "QuantumBlockEncoding.GHL2025.oneTermRobinLayout")
*Plain-English reading.* This definition gives the library's named construction or computation for “one term robin layout”. Register layout for the one-term Robin block encoding.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Register layout for the one-term Robin block encoding. Signal qubits = ⌈log₂ n⌉ + ⌈log₂ G\_f⌉ + ⌈log₂ κ⌉ + 4 match the paper's Theorem (main.tex:1098-1109). System qubits address 'n' grid points; pure ancillas are workspace.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:149](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-onetermrobinlayout). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.oneTermRobinCircuit" (lean := "QuantumBlockEncoding.GHL2025.oneTermRobinCircuit")
*Plain-English reading.* This definition gives the library's named construction or computation for “one term robin circuit”. Placeholder circuit for the one-term Robin block encoding.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Placeholder circuit for the one-term Robin block encoding. Gate order matches Fig. 1\_term\_ROBIN (main.tex:1125-1163): 1. U\_indic sets bulk/boundary indicator ancilla. 2. O\_DT^S encodes D^T amplitudes (bulk) via sparse-amplitude oracle. 3. Ry\_boundary applies controlled rotations for boundary entries. 4. O\_D^BS is the banded-sparse-access oracle for D. 5. O\_f encodes f(x\_j) via amplitude oracle. 6. SWAP between two n-qubit registers. 7. (O\_D^BS)^† uncomputes the sparse-access register. Oracle names match 'defaultRobinCircuitSkeleton' field values. figure:1\_term\_ROBIN -

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:166](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-onetermrobincircuit). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.oneTermRobinTheoremFacingFig4Circuit" (lean := "QuantumBlockEncoding.GHL2025.oneTermRobinTheoremFacingFig4Circuit")
*Plain-English reading.* This definition gives the library's named construction or computation for “one term robin theorem facing fig 4 circuit”. Theorem-facing Fig.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Theorem-facing Fig. 1-term Robin transcript. This label list is the source-facing circuit map used by the conversion window. It deliberately differs from the active seven-gate backend list: the backend matrix product still uses 'oneTermRobinCircuit', while this transcript keeps the sparse-register preparation sides, the explicit 'U\_indic^dagger' cleanup slot, and the pre-SWAP 'O\_DT^BS' label visible for paper audit. figure:1\_term\_ROBIN, eq:arbitrary sparcity -

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:185](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-onetermrobintheoremfacingfig4circuit). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.oneTermRobinTheoremFacingFig4Circuit_gateList" (lean := "QuantumBlockEncoding.GHL2025.oneTermRobinTheoremFacingFig4Circuit_gateList")
*Plain-English reading.* Lean checks the proposition indexed as “one term robin theorem facing fig 4 circuit gate list”; the hypotheses and conclusion in the code panel fix its exact scope. The theorem-facing transcript exposes the source-correction slots explicitly.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The theorem-facing transcript exposes the source-correction slots explicitly. This theorem is a transcript guard only. It does not replace 'oneTermRobinCircuit', does not change 'oneTermRobinGateMatrixPlaceholders', and does not promote any oracle correctness or unitarity flag.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:205](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-onetermrobintheoremfacingfig4circuit-gatelist). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.oneTermRobinActiveBackendCircuit_gateList" (lean := "QuantumBlockEncoding.GHL2025.oneTermRobinActiveBackendCircuit_gateList")
*Plain-English reading.* Lean checks the proposition indexed as “one term robin active backend circuit gate list”; the hypotheses and conclusion in the code panel fix its exact scope. The active backend circuit remains the seven-gate product currently used by the finite matrix semantics.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The active backend circuit remains the seven-gate product currently used by the finite matrix semantics. Theorem-facing proof maps must not call this list the full Fig. 1-term Robin transcript, because it omits both 'H\_W^(kappa)' sides and the explicit 'U\_indic^dagger' source slot.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:227](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-onetermrobinactivebackendcircuit-gatelist). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.oneTermRobinNormalizer" (lean := "QuantumBlockEncoding.GHL2025.oneTermRobinNormalizer")
*Plain-English reading.* This definition gives the library's named construction or computation for “one term robin normalizer”. Symbolic normalizer α = N\_D · N\_f · κ for the one-term Robin construction.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Symbolic normalizer α = N\_D · N\_f · κ for the one-term Robin construction.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:239](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-onetermrobinnormalizer). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.oneTermRobinSpec" (lean := "QuantumBlockEncoding.GHL2025.oneTermRobinSpec")
*Plain-English reading.* This definition gives the library's named construction or computation for “one term robin spec”. Block-encoding spec for the one-term Robin derivative operator.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Block-encoding spec for the one-term Robin derivative operator. Takes the target matrix as a parameter so the spec is reusable across different stencil choices and boundary data without creating import cycles. Normalizer: symbolic 'N\_D · N\_f · κ'. Error: zero (exact encoding, no approximation yet).

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:249](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-onetermrobinspec). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.oneTermRobinSpec_ancilla" (lean := "QuantumBlockEncoding.GHL2025.oneTermRobinSpec_ancilla")
*Plain-English reading.* Lean checks the proposition indexed as “one term robin spec ancilla”; the hypotheses and conclusion in the code panel fix its exact scope. The spec's pure ancilla matches the resource formula.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The spec's pure ancilla matches the resource formula.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:260](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-onetermrobinspec-ancilla). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.oneTermRobinSpec_circuitCost" (lean := "QuantumBlockEncoding.GHL2025.oneTermRobinSpec_circuitCost")
*Plain-English reading.* Lean checks the proposition indexed as “one term robin spec circuit cost”; the hypotheses and conclusion in the code panel fix its exact scope. The spec's circuit local cost: the SWAP placeholder costs 3 CNOTs and each unexpanded oracle call is counted as one unresolved call in the candidate score.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The spec's circuit local cost: the SWAP placeholder costs 3 CNOTs and each unexpanded oracle call is counted as one unresolved call in the candidate score. figure:1\_term\_ROBIN

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:269](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-onetermrobinspec-circuitcost). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.oneTermRobinNormalizer_eval" (lean := "QuantumBlockEncoding.GHL2025.oneTermRobinNormalizer_eval")
*Plain-English reading.* Lean checks the proposition indexed as “one term robin normalizer eval”; the hypotheses and conclusion in the code panel fix its exact scope. Evaluating the symbolic normalizer 'N\_D · N\_f · κ' under an environment gives the product of the three symbol values.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Evaluating the symbolic normalizer 'N\_D · N\_f · κ' under an environment gives the product of the three symbol values.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:274](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-onetermrobinnormalizer-eval). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.oneTermRobinClaim" (lean := "QuantumBlockEncoding.GHL2025.oneTermRobinClaim")
*Plain-English reading.* This definition gives the library's named construction or computation for “one term robin claim”. The paper's one-term Robin block-encoding construction claim.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The paper's one-term Robin block-encoding construction claim.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:279](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-onetermrobinclaim). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.oneDimHamiltonianResourceExpr" (lean := "QuantumBlockEncoding.GHL2025.oneDimHamiltonianResourceExpr")
*Plain-English reading.* This definition gives the library's named construction or computation for “one dim hamiltonian resource expr”. One-dimensional Hamiltonian block-encoding resource shape.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* One-dimensional Hamiltonian block-encoding resource shape.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:288](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-onedimhamiltonianresourceexpr). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.oneDimHamiltonianClaim" (lean := "QuantumBlockEncoding.GHL2025.oneDimHamiltonianClaim")
*Plain-English reading.* This definition gives the library's named construction or computation for “one dim hamiltonian claim”. The paper's 1D Hamiltonian block-encoding construction claim.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The paper's 1D Hamiltonian block-encoding construction claim.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:296](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-onedimhamiltonianclaim). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.multiDimHamiltonianResourceExpr" (lean := "QuantumBlockEncoding.GHL2025.multiDimHamiltonianResourceExpr")
*Plain-English reading.* This definition gives the library's named construction or computation for “multi dim hamiltonian resource expr”. Multidimensional Hamiltonian block-encoding resource shape.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Multidimensional Hamiltonian block-encoding resource shape.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:305](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-multidimhamiltonianresourceexpr). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.multiDimHamiltonianClaim" (lean := "QuantumBlockEncoding.GHL2025.multiDimHamiltonianClaim")
*Plain-English reading.* This definition gives the library's named construction or computation for “multi dim hamiltonian claim”. The paper's multidimensional Hamiltonian block-encoding construction claim.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The paper's multidimensional Hamiltonian block-encoding construction claim.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:313](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-multidimhamiltonianclaim). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.ObligationRecord" (lean := "QuantumBlockEncoding.GHL2025.ObligationRecord")
*Plain-English reading.* This record groups the data and proof fields needed for “obligation record”. A proposition-valued field is a requirement until a constructor supplies it. A proof obligation tracked by description and paper source anchor.

*Formal status.* Data contract in the default import surface; proposition-valued fields are obligations, not automatically established facts.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* A proof obligation tracked by description and paper source anchor. 'proved' is 'Bool' (not 'Prop') so that unproved obligations are honest data, not mathematically false claims. main.tex -

*Declaration kind.* structure.

Source: [QuantumBlockEncoding/GHL2025.lean:324](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-obligationrecord). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.RobinCircuitSkeleton" (lean := "QuantumBlockEncoding.GHL2025.RobinCircuitSkeleton")
*Plain-English reading.* This record groups the data and proof fields needed for “robin circuit skeleton”. A proposition-valued field is a requirement until a constructor supplies it. Circuit skeleton matching Fig.

*Formal status.* Data contract in the default import surface; proposition-valued fields are obligations, not automatically established facts.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Circuit skeleton matching Fig. 1\_term\_ROBIN (main.tex:1137-1167). Each field corresponds to a labeled box or operation in the figure. All oracles are recorded as symbolic names; their implementation is delegated to separate oracle-contract structures. figure:1\_term\_ROBIN -

*Declaration kind.* structure.

Source: [QuantumBlockEncoding/GHL2025.lean:334](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-robincircuitskeleton). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.RobinGamma1" (lean := "QuantumBlockEncoding.GHL2025.RobinGamma1")
*Plain-English reading.* This record groups the data and proof fields needed for “robin gamma 1”. A proposition-valued field is a requirement until a constructor supplies it. Eq.

*Formal status.* Data contract in the default import surface; proposition-valued fields are obligations, not automatically established facts.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Eq. ROBIN clarified, gamma\_1 component (main.tex:1113). State after U\_indic sets the indicator ancilla. The boundary and bulk summation terms have different normalizers: boundary: 1/(N\_D · sqrt(kappa)), indicator |0> bulk: 1/sqrt(kappa), indicator |1> main.tex:1113 -

*Declaration kind.* structure.

Source: [QuantumBlockEncoding/GHL2025.lean:373](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-robingamma1). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.RobinGamma2" (lean := "QuantumBlockEncoding.GHL2025.RobinGamma2")
*Plain-English reading.* This record groups the data and proof fields needed for “robin gamma 2”. A proposition-valued field is a requirement until a constructor supplies it. Eq.

*Formal status.* Data contract in the default import surface; proposition-valued fields are obligations, not automatically established facts.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Eq. ROBIN clarified, gamma\_2 component (main.tex:1115). State after sparse-amplitude oracle encodes D^T values. main.tex:1115 -

*Declaration kind.* structure.

Source: [QuantumBlockEncoding/GHL2025.lean:396](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-robingamma2). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.RobinGamma3" (lean := "QuantumBlockEncoding.GHL2025.RobinGamma3")
*Plain-English reading.* This record groups the data and proof fields needed for “robin gamma 3”. A proposition-valued field is a requirement until a constructor supplies it. Eq.

*Formal status.* Data contract in the default import surface; proposition-valued fields are obligations, not automatically established facts.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Eq. ROBIN clarified, gamma\_3 component (main.tex:1117). State after O\_f encodes f(x\_j) values. main.tex:1117 -

*Declaration kind.* structure.

Source: [QuantumBlockEncoding/GHL2025.lean:413](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-robingamma3). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.RobinWavefunctionDecomposition" (lean := "QuantumBlockEncoding.GHL2025.RobinWavefunctionDecomposition")
*Plain-English reading.* This record groups the data and proof fields needed for “robin wavefunction decomposition”. A proposition-valued field is a requirement until a constructor supplies it. Bundle of the three intermediate wavefunction states from Eq.

*Formal status.* Data contract in the default import surface; proposition-valued fields are obligations, not automatically established facts.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Bundle of the three intermediate wavefunction states from Eq. ROBIN clarified. Captures the full circuit state evolution from input through U\_indic, O\_DT^S, and O\_f. main.tex:1113-1117, figure:1\_term\_ROBIN -

*Declaration kind.* structure.

Source: [QuantumBlockEncoding/GHL2025.lean:433](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-robinwavefunctiondecomposition). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.defaultRobinWavefunctionDecomposition" (lean := "QuantumBlockEncoding.GHL2025.defaultRobinWavefunctionDecomposition")
*Plain-English reading.* This definition gives the library's named construction or computation for “default robin wavefunction decomposition”. Default wavefunction decomposition from concrete parameters.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Default wavefunction decomposition from concrete parameters. figure:1\_term\_ROBIN, main.tex:1113-1117 -

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:452](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-defaultrobinwavefunctiondecomposition). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.RobinProofObligations" (lean := "QuantumBlockEncoding.GHL2025.RobinProofObligations")
*Plain-English reading.* This record groups the data and proof fields needed for “robin proof obligations”. A proposition-valued field is a requirement until a constructor supplies it. Bundle of proof obligations for the one-term Robin block encoding.

*Formal status.* Data contract in the default import surface; proposition-valued fields are obligations, not automatically established facts.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Bundle of proof obligations for the one-term Robin block encoding. Each obligation references a specific claim in the paper and tracks whether it has been formally proved. None are proved in the current version. Guseynov-Huang-Liu 2025, one-term Robin theorem, arXiv:2506.20478. -

*Declaration kind.* structure.

Source: [QuantumBlockEncoding/GHL2025.lean:488](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-robinproofobligations). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.defaultRobinCircuitSkeleton" (lean := "QuantumBlockEncoding.GHL2025.defaultRobinCircuitSkeleton")
*Plain-English reading.* This definition gives the library's named construction or computation for “default robin circuit skeleton”. Default circuit skeleton for the one-term Robin construction, with oracle names matching the paper's notation.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Default circuit skeleton for the one-term Robin construction, with oracle names matching the paper's notation. figure:1\_term\_ROBIN -

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:553](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-defaultrobincircuitskeleton). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.BandedSparseAccessPaperContract" (lean := "QuantumBlockEncoding.GHL2025.BandedSparseAccessPaperContract")
*Plain-English reading.* This record groups the data and proof fields needed for “banded sparse access paper contract”. A proposition-valued field is a requirement until a constructor supplies it. Paper-level source contract for the banded sparse-access oracle in Lemma 1.

*Formal status.* Data contract in the default import surface; proposition-valued fields are obligations, not automatically established facts.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Paper-level source contract for the banded sparse-access oracle in Lemma 1. The input register is the padded sparse-index register '|0>^(n-l)|s>^l' followed by the row register '|i>^n'; the output is '|r\_si>^n|i>^n'. This record is intentionally separate from the current 'bandedSparseAccessMatrix' helper, which overwrites the system register with a Robin column map and therefore does not yet implement this paper contract. Guseynov-Huang-Liu 2025, Lemma 1, arXiv:2506.20478.

*Declaration kind.* structure.

Source: [QuantumBlockEncoding/GHL2025.lean:577](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspapercontract). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.defaultBandedSparseAccessPaperContract" (lean := "QuantumBlockEncoding.GHL2025.defaultBandedSparseAccessPaperContract")
*Plain-English reading.* This definition gives the library's named construction or computation for “default banded sparse access paper contract”. Default Lemma 1 register contract for the one-term Robin parameters.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Default Lemma 1 register contract for the one-term Robin parameters. The 'widthCompatible' obligation stays explicit because the current parameter type does not enforce 'clog2 kappa <= n'; faithful proofs should discharge that side condition or specialize to a parameter family where it is available.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:602](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-defaultbandedsparseaccesspapercontract). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.DerivativeOracleContract" (lean := "QuantumBlockEncoding.GHL2025.DerivativeOracleContract")
*Plain-English reading.* This record groups the data and proof fields needed for “derivative oracle contract”. A proposition-valued field is a requirement until a constructor supplies it. Contract for the derivative oracle O\_D: sparse-access oracle for the banded stencil matrix.

*Formal status.* Data contract in the default import surface; proposition-valued fields are obligations, not automatically established facts.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Contract for the derivative oracle O\_D: sparse-access oracle for the banded stencil matrix. Records stencil metadata, bandwidth, and a correctness obligation. main.tex:784-801 -

*Declaration kind.* structure.

Source: [QuantumBlockEncoding/GHL2025.lean:650](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-derivativeoraclecontract). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.FunctionOracleContract" (lean := "QuantumBlockEncoding.GHL2025.FunctionOracleContract")
*Plain-English reading.* This record groups the data and proof fields needed for “function oracle contract”. A proposition-valued field is a requirement until a constructor supplies it. Contract for the function oracle O\_f: amplitude oracle encoding f(x) on the grid.

*Formal status.* Data contract in the default import surface; proposition-valued fields are obligations, not automatically established facts.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Contract for the function oracle O\_f: amplitude oracle encoding f(x) on the grid. Records the piece count, normalization bound, and a correctness obligation. main.tex:870-910

*Declaration kind.* structure.

Source: [QuantumBlockEncoding/GHL2025.lean:661](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-functionoraclecontract). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.derivativeOracleResource" (lean := "QuantumBlockEncoding.GHL2025.derivativeOracleResource")
*Plain-English reading.* This definition gives the library's named construction or computation for “derivative oracle resource”. Resource for the derivative oracle O\_D using the banded sparse-access formula from Lemma 1 of Guseynov-Huang-Liu 2025.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Resource for the derivative oracle O\_D using the banded sparse-access formula from Lemma 1 of Guseynov-Huang-Liu 2025. The half-bandwidth parameter is 'stencil.leftRadius' (assumes a symmetric stencil where leftRadius = rightRadius).

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:671](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-derivativeoracleresource). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.derivativeOracleResource_pureAncilla" (lean := "QuantumBlockEncoding.GHL2025.derivativeOracleResource_pureAncilla")
*Plain-English reading.* Lean checks the proposition indexed as “derivative oracle resource pure ancilla”; the hypotheses and conclusion in the code panel fix its exact scope. The derivative oracle's pure ancilla count is n - 1 (from Lemma 1).

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The derivative oracle's pure ancilla count is n - 1 (from Lemma 1).

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:675](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-derivativeoracleresource-pureancilla). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.OneTermRobinTheoremData" (lean := "QuantumBlockEncoding.GHL2025.OneTermRobinTheoremData")
*Plain-English reading.* This record groups the data and proof fields needed for “one term robin theorem data”. A proposition-valued field is a requirement until a constructor supplies it. Typed theorem data for Theorem one-term block-encoding (main.tex:1098-1109).

*Formal status.* Data contract in the default import surface; proposition-valued fields are obligations, not automatically established facts.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Typed theorem data for Theorem one-term block-encoding (main.tex:1098-1109). Captures the exact block-encoding tuple (α, m, a) from the paper: α = N\_D · N\_f · κ (normalizer) m = ⌈log₂ n⌉ + ⌈log₂ G\_f⌉ + ⌈log₂ κ⌉ + 4 (signal ancilla qubits) a = 0 (zero approximation error) along with the gate-count and pure-ancilla resource claims.

*Declaration kind.* structure.

Source: [QuantumBlockEncoding/GHL2025.lean:684](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-onetermrobintheoremdata). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.defaultOneTermRobinTheoremData" (lean := "QuantumBlockEncoding.GHL2025.defaultOneTermRobinTheoremData")
*Plain-English reading.* This definition gives the library's named construction or computation for “default one term robin theorem data”. Default theorem data instance from concrete parameters.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Default theorem data instance from concrete parameters. main.tex:1098-1109 -

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:700](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-defaultonetermrobintheoremdata). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.RobinBoundaryRotationAngle" (lean := "QuantumBlockEncoding.GHL2025.RobinBoundaryRotationAngle")
*Plain-English reading.* This record groups the data and proof fields needed for “robin boundary rotation angle”. A proposition-valued field is a requirement until a constructor supplies it. A controlled R\_y rotation angle for a single boundary row entry.

*Formal status.* Data contract in the default import surface; proposition-valued fields are obligations, not automatically established facts.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* A controlled R\_y rotation angle for a single boundary row entry. The paper (Eq. angles for Ry, main.tex:1081-1083) defines: theta\_j^s = arccos(D\_j^(s) / N\_D) for sparse index s in \{0,...,kappa-1\} and boundary row j. main.tex:1081-1083 -

*Declaration kind.* structure.

Source: [QuantumBlockEncoding/GHL2025.lean:714](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-robinboundaryrotationangle). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.RobinBoundaryRotationSet" (lean := "QuantumBlockEncoding.GHL2025.RobinBoundaryRotationSet")
*Plain-English reading.* This record groups the data and proof fields needed for “robin boundary rotation set”. A proposition-valued field is a requirement until a constructor supplies it. The set of all boundary-controlled rotation angles for a given Robin construction.

*Formal status.* Data contract in the default import surface; proposition-valued fields are obligations, not automatically established facts.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The set of all boundary-controlled rotation angles for a given Robin construction. For each boundary row j and sparse index s, there is one angle theta\_j^s. Total count = kappa \* (K1 + gridSize - K2) = kappa \* deviatingIndices. main.tex:1081-1083, 1088-1089 -

*Declaration kind.* structure.

Source: [QuantumBlockEncoding/GHL2025.lean:732](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-robinboundaryrotationset). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.RobinBoundaryRotationSet.expectedCount" (lean := "QuantumBlockEncoding.GHL2025.RobinBoundaryRotationSet.expectedCount")
*Plain-English reading.* This definition gives the library's named construction or computation for “expected count”. Number of boundary rows = K1 + gridSize - K2.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Number of boundary rows = K1 + gridSize - K2. Each boundary row has kappa rotation angles (one per sparse index). main.tex:1092-1095 -

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:752](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-robinboundaryrotationset-expectedcount). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.importedClaims" (lean := "QuantumBlockEncoding.GHL2025.importedClaims")
*Plain-English reading.* This definition gives the library's named construction or computation for “imported claims”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:755](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-importedclaims). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.oneTermRobinTotalQubits" (lean := "QuantumBlockEncoding.GHL2025.oneTermRobinTotalQubits")
*Plain-English reading.* This definition gives the library's named construction or computation for “one term robin total qubits”. Total number of qubits in the one-term Robin circuit.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Total number of qubits in the one-term Robin circuit. Uses the register partition total: sum of all register widths. main.tex:1098-1109 -

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:770](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-onetermrobintotalqubits). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.effectiveRobinSignalQubits" (lean := "QuantumBlockEncoding.GHL2025.effectiveRobinSignalQubits")
*Plain-English reading.* This definition gives the library's named construction or computation for “effective robin signal qubits”. Effective signal qubits: total circuit qubits minus the system register width.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Effective signal qubits: total circuit qubits minus the system register width. This is the number of non-system qubits in the register partition. main.tex:1098-1109 -

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:777](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-effectiverobinsignalqubits). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.defaultOneTermRobinTheoremData_signalQubits_eq_layout" (lean := "QuantumBlockEncoding.GHL2025.defaultOneTermRobinTheoremData_signalQubits_eq_layout")
*Plain-English reading.* Lean checks the proposition indexed as “default one term robin theorem data signal qubits eq layout”; the hypotheses and conclusion in the code panel fix its exact scope. The theorem tuple uses the paper's signal-qubit count.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The theorem tuple uses the paper's signal-qubit count. This is the block-encoding parameter 'ceil(log2 n) + ceil(log2 G\_f) + ceil(log2 kappa) + 4', not the number of all non-system wires in the concrete circuit register partition. Guseynov-Huang-Liu 2025, Theorem one-term block-encoding, arXiv:2506.20478.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:789](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-defaultonetermrobintheoremdata-signalqubits-eq-layout). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.defaultOneTermRobinTheoremData_pureAncillas_eq_layout" (lean := "QuantumBlockEncoding.GHL2025.defaultOneTermRobinTheoremData_pureAncillas_eq_layout")
*Plain-English reading.* Lean checks the proposition indexed as “default one term robin theorem data pure ancillas eq layout”; the hypotheses and conclusion in the code panel fix its exact scope. The theorem tuple and the reusable layout record carry the same '2n' pure-ancilla resource count.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The theorem tuple and the reusable layout record carry the same '2n' pure-ancilla resource count.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:798](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-defaultonetermrobintheoremdata-pureancillas-eq-layout). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.defaultOneTermRobinTheoremData_pureAncillas_eq_resource" (lean := "QuantumBlockEncoding.GHL2025.defaultOneTermRobinTheoremData_pureAncillas_eq_resource")
*Plain-English reading.* Lean checks the proposition indexed as “default one term robin theorem data pure ancillas eq resource”; the hypotheses and conclusion in the code panel fix its exact scope. The theorem tuple and concrete resource record carry the same '2n' pure-ancilla count.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The theorem tuple and concrete resource record carry the same '2n' pure-ancilla count.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:807](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-defaultonetermrobintheoremdata-pureancillas-eq-resource). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.effectiveRobinSignalQubits_eq_layout_signal_plus_visibleWorkspace" (lean := "QuantumBlockEncoding.GHL2025.effectiveRobinSignalQubits_eq_layout_signal_plus_visibleWorkspace")
*Plain-English reading.* Lean checks the proposition indexed as “effective robin signal qubits eq layout signal plus visible workspace”; the hypotheses and conclusion in the code panel fix its exact scope. The concrete block projection has to project all non-system wires.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The concrete block projection has to project all non-system wires. Compared with the theorem-level signal parameter, the circuit-level projection also includes the visible padded 'O\_D^BS' pure-register qubits and the trailing one-qubit ancilla in the register partition. This is an arithmetic bridge between the theorem tuple and the matrix backend, not a block-correctness proof.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:821](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-effectiverobinsignalqubits-eq-layout-signal-plus-visibleworkspace). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.effectiveRobinSignalQubits_eq_theoremData_signal_plus_visibleWorkspace" (lean := "QuantumBlockEncoding.GHL2025.effectiveRobinSignalQubits_eq_theoremData_signal_plus_visibleWorkspace")
*Plain-English reading.* Lean checks the proposition indexed as “effective robin signal qubits eq theorem data signal plus visible workspace”; the hypotheses and conclusion in the code panel fix its exact scope. Same projection bridge, stated directly against the theorem-data tuple.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Same projection bridge, stated directly against the theorem-data tuple. This pins the matrix backend's projection dimension to the paper theorem's signal parameter plus the visible padded 'O\_D^BS' workspace and the one-qubit ancilla. It is still only a layout bridge, not an ancilla-cleanup or block correctness proof.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:839](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-effectiverobinsignalqubits-eq-theoremdata-signal-plus-visibleworkspace). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.robinIndicatorBitPosition" (lean := "QuantumBlockEncoding.GHL2025.robinIndicatorBitPosition")
*Plain-English reading.* This definition gives the library's named construction or computation for “robin indicator bit position”. Bit position of the indicator qubit in the compound register.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Bit position of the indicator qubit in the compound register. = ancillaQubit + systemQubits + odPureAncillaQubits + sparseIndexQubits = 1 + n + (n - clog2 κ) + clog2 κ = 1 + 2n main.tex:1113 -

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:852](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-robinindicatorbitposition). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.robinSparseColumnMap" (lean := "QuantumBlockEncoding.GHL2025.robinSparseColumnMap")
*Plain-English reading.* This definition gives the library's named construction or computation for “robin sparse column map”. Column mapping for the banded sparse access oracle O\_D^BS.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Column mapping for the banded sparse access oracle O\_D^BS. Returns the column index for sparse index s in row i of the Robin derivative matrix. For bulk rows (K1 ≤ i ≤ K2): 5 entries, col(s,i) = i - 2 + s for s < 5. For left boundary: - Row 0 (3 entries): col(s,0) = s for s < 3 - Row 1 (4 entries): col(s,1) = s for s < 4 For right boundary (N = gridSize n): - Row N-2 (4 entries): col(s,N-2) = N-4+s for s < 4 - Row N-1 (3 entries): col(s,N-1) = N-3+s for s < 3 For unused sparse indices (s ≥ entry count): returns i (identity on system register). main.tex:784-801 -

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:869](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-robinsparsecolumnmap). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.oneTermRobinGlobalSparseOffset" (lean := "QuantumBlockEncoding.GHL2025.oneTermRobinGlobalSparseOffset")
*Plain-English reading.* This definition gives the library's named construction or computation for “one term robin global sparse offset”. Global sparse-slot offset table for the one-term Robin 'κ = 7' construction.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Global sparse-slot offset table for the one-term Robin 'κ = 7' construction. The first five slots keep the existing fourth-order stencil order '\{-2,-1,0,1,2\}'. The final two slots record the boundary-effect diagonals '\{-3,3\}' required by the source audit. The active Lemma 1 address uses this global slot table; row-dependent branch deletion remains only a rejected-model helper. Guseynov-Huang-Liu 2025, Lemma 'Diagonal sparsity', Lemma 'Banded-sparse-access-oracle', and Remark 'sparsity maximum', arXiv:2506.20478.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:897](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-onetermrobinglobalsparseoffset). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.oneTermRobinGlobalSparseAddress" (lean := "QuantumBlockEncoding.GHL2025.oneTermRobinGlobalSparseAddress")
*Plain-English reading.* This definition gives the library's named construction or computation for “one term robin global sparse address”. Global sparse-access address 'r\_\{si\}=r\_\{s0\}+i mod 2^n'.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Global sparse-access address 'r\_\{si\}=r\_\{s0\}+i mod 2^n'. This is the active paper address for 'O\_D^BS'; it does not remove zero boundary-amplitude slots from the sparse register.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:915](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-onetermrobinglobalsparseaddress). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.oneTermRobinGlobalSparseAddress_lt_gridSize" (lean := "QuantumBlockEncoding.GHL2025.oneTermRobinGlobalSparseAddress_lt_gridSize")
*Plain-English reading.* Lean checks the proposition indexed as “one term robin global sparse address lt grid size”; the hypotheses and conclusion in the code panel fix its exact scope. The global sparse-slot address is always an 'n'-bit row address.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The global sparse-slot address is always an 'n'-bit row address.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:919](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-onetermrobinglobalsparseaddress-lt-gridsize). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.oneTermRobinGlobalSparseInverseSlot" (lean := "QuantumBlockEncoding.GHL2025.oneTermRobinGlobalSparseInverseSlot")
*Plain-English reading.* This definition gives the library's named construction or computation for “one term robin global sparse inverse slot”. Inverse sparse slot used by the post-SWAP cleanup candidate for the global offset table.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Inverse sparse slot used by the post-SWAP cleanup candidate for the global offset table. This is only an executable preimage witness helper. It does not assert inverse uniqueness or promote the dagger-cleanup obligation.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:934](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-onetermrobinglobalsparseinverseslot). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.oneTermRobinGlobalSparseInverseSlot_lt_eight" (lean := "QuantumBlockEncoding.GHL2025.oneTermRobinGlobalSparseInverseSlot_lt_eight")
*Plain-English reading.* Lean checks the proposition indexed as “one term robin global sparse inverse slot lt eight”; the hypotheses and conclusion in the code panel fix its exact scope. The global inverse-slot helper fits in the three-bit sparse register.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The global inverse-slot helper fits in the three-bit sparse register.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:946](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-onetermrobinglobalsparseinverseslot-lt-eight). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.oneTermRobinGlobalSparseInverseSlot_lt_seven" (lean := "QuantumBlockEncoding.GHL2025.oneTermRobinGlobalSparseInverseSlot_lt_seven")
*Plain-English reading.* Lean checks the proposition indexed as “one term robin global sparse inverse slot lt seven”; the hypotheses and conclusion in the code panel fix its exact scope. The inverse sparse-slot helper stays in the active seven-slot table.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The inverse sparse-slot helper stays in the active seven-slot table.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:952](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-onetermrobinglobalsparseinverseslot-lt-seven). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.oneTermRobinGlobalSparseInverseSlot_involutive_of_lt_seven" (lean := "QuantumBlockEncoding.GHL2025.oneTermRobinGlobalSparseInverseSlot_involutive_of_lt_seven")
*Plain-English reading.* Lean checks the proposition indexed as “one term robin global sparse inverse slot involutive of lt seven”; the hypotheses and conclusion in the code panel fix its exact scope. The inverse sparse-slot helper is an involution on the active 'κ = 7' slot set.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The inverse sparse-slot helper is an involution on the active 'κ = 7' slot set. This is a finite-table proof block for the global-source preimage route; it does not prove uniqueness of the full 'O\_D^BS' matrix image.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:967](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-onetermrobinglobalsparseinverseslot-involutive-of-lt-seven). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.oneTermRobinGlobalSparseInverseSlot_injective_of_lt_seven" (lean := "QuantumBlockEncoding.GHL2025.oneTermRobinGlobalSparseInverseSlot_injective_of_lt_seven")
*Plain-English reading.* Lean checks the proposition indexed as “one term robin global sparse inverse slot injective of lt seven”; the hypotheses and conclusion in the code panel fix its exact scope. The inverse sparse-slot helper is injective on the active 'κ = 7' slot set.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The inverse sparse-slot helper is injective on the active 'κ = 7' slot set. This feeds the later clean-preimage uniqueness proof for the corrected global slot model, but it intentionally leaves the semantic cleanup and unitarity obligations false.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:985](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-onetermrobinglobalsparseinverseslot-injective-of-lt-seven). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.oneTermRobinGlobalSparseAddress_inverseSlot_address_eq" (lean := "QuantumBlockEncoding.GHL2025.oneTermRobinGlobalSparseAddress_inverseSlot_address_eq")
*Plain-English reading.* Lean checks the proposition indexed as “one term robin global sparse address inverse slot address eq”; the hypotheses and conclusion in the code panel fix its exact scope. Global sparse-address roundtrip for the supplied inverse-slot helper.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Global sparse-address roundtrip for the supplied inverse-slot helper. For an 'n >= 3' grid and any encoded sparse value below '8', addressing by a slot and then by the inverse slot returns the original row modulo '2^n'. This is the arithmetic block needed by the conditional post-SWAP preimage candidate; it is not an injectivity or cleanup proof.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:1009](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-onetermrobinglobalsparseaddress-inverseslot-address-eq). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.oneTermRobinGlobalSparseOffset_lt_gridSize_of_lt_seven" (lean := "QuantumBlockEncoding.GHL2025.oneTermRobinGlobalSparseOffset_lt_gridSize_of_lt_seven")
*Plain-English reading.* Lean checks the proposition indexed as “one term robin global sparse offset lt grid size of lt seven”; the hypotheses and conclusion in the code panel fix its exact scope. Every active global sparse-slot offset is an 'n'-bit address when '3 ≤ n'.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Every active global sparse-slot offset is an 'n'-bit address when '3 ≤ n'.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:1131](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-onetermrobinglobalsparseoffset-lt-gridsize-of-lt-seven). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.oneTermRobinGlobalSparseAddress_comp_eq_mod_offset_sum" (lean := "QuantumBlockEncoding.GHL2025.oneTermRobinGlobalSparseAddress_comp_eq_mod_offset_sum")
*Plain-English reading.* Lean checks the proposition indexed as “one term robin global sparse address comp eq mod offset sum”; the hypotheses and conclusion in the code panel fix its exact scope. Composing two global sparse-slot addresses is addition by the sum of their global offsets modulo the grid size.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Composing two global sparse-slot addresses is addition by the sum of their global offsets modulo the grid size.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:1149](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-onetermrobinglobalsparseaddress-comp-eq-mod-offset-sum). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.oneTermRobinGlobalSparseOffset_sum_mod_eq_zero_unique_of_lt_seven" (lean := "QuantumBlockEncoding.GHL2025.oneTermRobinGlobalSparseOffset_sum_mod_eq_zero_unique_of_lt_seven")
*Plain-English reading.* Lean checks the proposition indexed as “one term robin global sparse offset sum mod eq zero unique of lt seven”; the hypotheses and conclusion in the code panel fix its exact scope. If two active global sparse-slot offsets sum to zero modulo the grid, the first slot is the reverse slot of the second.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* If two active global sparse-slot offsets sum to zero modulo the grid, the first slot is the reverse slot of the second. This is only a finite arithmetic block for the corrected 'O\_D^BS' address route; it does not prove dagger cleanup, unitarity, or block extraction.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:1183](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-onetermrobinglobalsparseoffset-sum-mod-eq-zero-unique-of-lt-seven). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.oneTermRobinGlobalSparseAddress_inverseSlot_unique_of_lt_seven" (lean := "QuantumBlockEncoding.GHL2025.oneTermRobinGlobalSparseAddress_inverseSlot_unique_of_lt_seven")
*Plain-English reading.* Lean checks the proposition indexed as “one term robin global sparse address inverse slot unique of lt seven”; the hypotheses and conclusion in the code panel fix its exact scope. Uniqueness of the reverse sparse slot for the corrected global-slot address.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Uniqueness of the reverse sparse slot for the corrected global-slot address. For active one-term Robin slots 's,t < 7', if applying slot 't' after slot 's' returns every in-range row 'i', then 't' must be the table inverse of 's'. This feeds the future clean-preimage uniqueness route while leaving all semantic 'O\_D^BS' proof flags false.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:1228](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-onetermrobinglobalsparseaddress-inverseslot-unique-of-lt-seven). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.oneTermRobinGlobalSparseAddress_same_row_injective_of_lt_seven" (lean := "QuantumBlockEncoding.GHL2025.oneTermRobinGlobalSparseAddress_same_row_injective_of_lt_seven")
*Plain-English reading.* Lean checks the proposition indexed as “one term robin global sparse address same row injective of lt seven”; the hypotheses and conclusion in the code panel fix its exact scope. For a fixed in-range row, the corrected seven-slot global address table is injective in the sparse slot.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* For a fixed in-range row, the corrected seven-slot global address table is injective in the sparse slot. The proof reuses the reverse-slot uniqueness block rather than repeating the finite offset table. It is still only an address-level arithmetic lemma; it does not promote any 'O\_D^BS' semantic flag.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:1261](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-onetermrobinglobalsparseaddress-same-row-injective-of-lt-seven). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.robinSparseColumnBranchValid" (lean := "QuantumBlockEncoding.GHL2025.robinSparseColumnBranchValid")
*Plain-English reading.* This definition gives the library's named construction or computation for “robin sparse column branch valid”. Row-dependent sparse-branch domain for the executable one-term Robin stencil.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Row-dependent sparse-branch domain for the executable one-term Robin stencil. This predicate is a source-contract correction candidate for Lemma 1 'O\_D^BS': it marks exactly the sparse indices that correspond to nonzero stencil entries in the same five row regions used by 'robinSparseColumnMap'. The active matrix is not changed by this predicate; unused branches still remain a separate unitary-extension obligation. Guseynov-Huang-Liu 2025, Lemma 1, arXiv:2506.20478.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:1295](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-robinsparsecolumnbranchvalid). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.robinSparseColumnBranchValid_boundaryUnused_n3" (lean := "QuantumBlockEncoding.GHL2025.robinSparseColumnBranchValid_boundaryUnused_n3")
*Plain-English reading.* Lean checks the proposition indexed as “robin sparse column branch valid boundary unused n 3”; the hypotheses and conclusion in the code panel fix its exact scope. The proposed valid-branch predicate separates the boundary unused branch that caused the recorded 'n = 3' collision, while the current executable map still sends both branches to the same address.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The proposed valid-branch predicate separates the boundary unused branch that caused the recorded 'n = 3' collision, while the current executable map still sends both branches to the same address.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:1314](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-robinsparsecolumnbranchvalid-boundaryunused-n3). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.robinSparseColumnMap_lt_gridSize_of_row_lt" (lean := "QuantumBlockEncoding.GHL2025.robinSparseColumnMap_lt_gridSize_of_row_lt")
*Plain-English reading.* Lean checks the proposition indexed as “robin sparse column map lt grid size of row lt”; the hypotheses and conclusion in the code panel fix its exact scope. Proof-DAG block for the Lemma 1 address-range route.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Proof-DAG block for the Lemma 1 address-range route. For the fourth-order Robin stencil, if the input row is an 'n'-bit value and 'n >= 2', then the executable one-term column map also returns an 'n'-bit value. The paper-level contract still records 'addressRange.proved := false' because the parameter-family side condition is not yet part of 'OneTermRobinParameters'.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:1329](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-robinsparsecolumnmap-lt-gridsize-of-row-lt). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.robinSparseReverseColumnIndex" (lean := "QuantumBlockEncoding.GHL2025.robinSparseReverseColumnIndex")
*Plain-English reading.* This definition gives the library's named construction or computation for “robin sparse reverse column index”. Candidate reverse sparse index for the one-term Robin stencil.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Candidate reverse sparse index for the one-term Robin stencil. Given a target row 'target' and a post-SWAP row 'row', this returns the sparse index that would make 'row' address 'target' in the executable fourth-order Robin column map. It is only a reverse-index candidate: the checked roundtrip and cleanup obligations remain separate proof blocks. Guseynov-Huang-Liu 2025, Lemma 1, arXiv:2506.20478.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:1386](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-robinsparsereversecolumnindex). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.robinSparseColumnMap_zero" (lean := "QuantumBlockEncoding.GHL2025.robinSparseColumnMap_zero")
*Plain-English reading.* Lean checks the proposition indexed as “robin sparse column map zero”; the hypotheses and conclusion in the code panel fix its exact scope. Normal form for the leftmost row of the executable Robin sparse map.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Normal form for the leftmost row of the executable Robin sparse map.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:1396](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-robinsparsecolumnmap-zero). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.robinSparseColumnMap_one" (lean := "QuantumBlockEncoding.GHL2025.robinSparseColumnMap_one")
*Plain-English reading.* Lean checks the proposition indexed as “robin sparse column map one”; the hypotheses and conclusion in the code panel fix its exact scope. Normal form for the second row of the executable Robin sparse map.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Normal form for the second row of the executable Robin sparse map.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:1401](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-robinsparsecolumnmap-one). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.robinSparseColumnMap_bulk" (lean := "QuantumBlockEncoding.GHL2025.robinSparseColumnMap_bulk")
*Plain-English reading.* Lean checks the proposition indexed as “robin sparse column map bulk”; the hypotheses and conclusion in the code panel fix its exact scope. Normal form for a bulk row of the executable Robin sparse map.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Normal form for a bulk row of the executable Robin sparse map.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:1406](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-robinsparsecolumnmap-bulk). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.robinSparseColumnMap_rightBoundaryPrev" (lean := "QuantumBlockEncoding.GHL2025.robinSparseColumnMap_rightBoundaryPrev")
*Plain-English reading.* Lean checks the proposition indexed as “robin sparse column map right boundary prev”; the hypotheses and conclusion in the code panel fix its exact scope. Normal form for the penultimate row of the executable Robin sparse map.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Normal form for the penultimate row of the executable Robin sparse map.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:1412](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-robinsparsecolumnmap-rightboundaryprev). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.robinSparseColumnMap_rightBoundaryLast" (lean := "QuantumBlockEncoding.GHL2025.robinSparseColumnMap_rightBoundaryLast")
*Plain-English reading.* Lean checks the proposition indexed as “robin sparse column map right boundary last”; the hypotheses and conclusion in the code panel fix its exact scope. Normal form for the last row of the executable Robin sparse map.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Normal form for the last row of the executable Robin sparse map.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:1427](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-robinsparsecolumnmap-rightboundarylast). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.robinSparseReverseColumnIndex_zero" (lean := "QuantumBlockEncoding.GHL2025.robinSparseReverseColumnIndex_zero")
*Plain-English reading.* Lean checks the proposition indexed as “robin sparse reverse column index zero”; the hypotheses and conclusion in the code panel fix its exact scope. Reverse-index normal form for row zero.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Reverse-index normal form for row zero.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:1443](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-robinsparsereversecolumnindex-zero). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.robinSparseReverseColumnIndex_one" (lean := "QuantumBlockEncoding.GHL2025.robinSparseReverseColumnIndex_one")
*Plain-English reading.* Lean checks the proposition indexed as “robin sparse reverse column index one”; the hypotheses and conclusion in the code panel fix its exact scope. Reverse-index normal form for row one.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Reverse-index normal form for row one.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:1448](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-robinsparsereversecolumnindex-one). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.robinSparseReverseColumnIndex_bulk" (lean := "QuantumBlockEncoding.GHL2025.robinSparseReverseColumnIndex_bulk")
*Plain-English reading.* Lean checks the proposition indexed as “robin sparse reverse column index bulk”; the hypotheses and conclusion in the code panel fix its exact scope. Reverse-index normal form for a bulk row.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Reverse-index normal form for a bulk row.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:1453](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-robinsparsereversecolumnindex-bulk). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.robinSparseReverseColumnIndex_rightBoundaryPrev" (lean := "QuantumBlockEncoding.GHL2025.robinSparseReverseColumnIndex_rightBoundaryPrev")
*Plain-English reading.* Lean checks the proposition indexed as “robin sparse reverse column index right boundary prev”; the hypotheses and conclusion in the code panel fix its exact scope. Reverse-index normal form for the penultimate row.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Reverse-index normal form for the penultimate row.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:1461](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-robinsparsereversecolumnindex-rightboundaryprev). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.robinSparseReverseColumnIndex_rightBoundaryLast" (lean := "QuantumBlockEncoding.GHL2025.robinSparseReverseColumnIndex_rightBoundaryLast")
*Plain-English reading.* Lean checks the proposition indexed as “robin sparse reverse column index right boundary last”; the hypotheses and conclusion in the code panel fix its exact scope. Reverse-index normal form for the last row.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Reverse-index normal form for the last row.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:1477](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-robinsparsereversecolumnindex-rightboundarylast). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.robinSparseReverseColumnRoundtrip_of_lt_eight" (lean := "QuantumBlockEncoding.GHL2025.robinSparseReverseColumnRoundtrip_of_lt_eight")
*Plain-English reading.* Lean checks the proposition indexed as “robin sparse reverse column roundtrip of lt eight”; the hypotheses and conclusion in the code panel fix its exact scope. The reverse sparse-index candidate is a left inverse for the executable one-term Robin column map on the three-bit sparse-index range used by the current one-term parameter family.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The reverse sparse-index candidate is a left inverse for the executable one-term Robin column map on the three-bit sparse-index range used by the current one-term parameter family. This is only the arithmetic roundtrip needed by the O\_D^BS post-SWAP cleanup route. It does not prove uniqueness of the preimage, dagger cleanup, unitarity, or block correctness.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:1502](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-robinsparsereversecolumnroundtrip-of-lt-eight). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.robinSparseReverseColumnIndex_lt_eight_of_columnMap" (lean := "QuantumBlockEncoding.GHL2025.robinSparseReverseColumnIndex_lt_eight_of_columnMap")
*Plain-English reading.* Lean checks the proposition indexed as “robin sparse reverse column index lt eight of column map”; the hypotheses and conclusion in the code panel fix its exact scope. The reverse-index candidate stays inside the three-bit sparse register for columns produced by the executable one-term Robin map.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The reverse-index candidate stays inside the three-bit sparse register for columns produced by the executable one-term Robin map. This is paired with 'robinSparseReverseColumnRoundtrip\_of\_lt\_eight'; it is a local arithmetic block for the post-SWAP preimage route, not a uniqueness or dagger-cleanup proof.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:1715](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-robinsparsereversecolumnindex-lt-eight-of-columnmap). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.robinSparseReverseColumnRoundtripCheck" (lean := "QuantumBlockEncoding.GHL2025.robinSparseReverseColumnRoundtripCheck")
*Plain-English reading.* This definition gives the library's named construction or computation for “robin sparse reverse column roundtrip check”. Executable finite audit for the reverse-index candidate.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Executable finite audit for the reverse-index candidate. For each sparse-index value below 'sparseBound' and each row of the 'n'-qubit grid, this checks that forward addressing followed by 'robinSparseReverseColumnIndex' returns to the original row. A true result is local evidence for the inverse-on-range route; it is not a semantic cleanup proof for 'O\_D^BS'.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:1875](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-robinsparsereversecolumnroundtripcheck). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.BandedSparseAccessPaperRegisters" (lean := "QuantumBlockEncoding.GHL2025.BandedSparseAccessPaperRegisters")
*Plain-English reading.* This record groups the data and proof fields needed for “banded sparse access paper registers”. A proposition-valued field is a requirement until a constructor supplies it. Register values used by the faithful Lemma 1 'O\_D^BS' contract.

*Formal status.* Data contract in the default import surface; proposition-valued fields are obligations, not automatically established facts.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Register values used by the faithful Lemma 1 'O\_D^BS' contract. The current compound-index convention stores the row register in bits '\[1, 1+n)' and the paper's padded sparse-address register in bits '\[1+n, 1+2n)'. Inside that address block, the low 'n - clog2 kappa' bits are the padded-zero workspace and the remaining bits encode the sparse index 's'. Guseynov-Huang-Liu 2025, Lemma 1, arXiv:2506.20478.

*Declaration kind.* structure.

Source: [QuantumBlockEncoding/GHL2025.lean:1890](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspaperregisters). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperRegisters" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperRegisters")
*Plain-English reading.* This definition gives the library's named construction or computation for “banded sparse access paper registers”. Extract the Lemma 1 padded sparse-address and row registers from a compound basis index.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Extract the Lemma 1 padded sparse-address and row registers from a compound basis index. This is a source-contract skeleton only; it does not alter the interim 'bandedSparseAccessMatrix' helper and does not prove unitarity.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:1902](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspaperregisters). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperRegisters_row_lt_gridSize" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperRegisters_row_lt_gridSize")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper registers row lt grid size”; the hypotheses and conclusion in the code panel fix its exact scope. The row field extracted for Lemma 1 is always an 'n'-bit row value.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The row field extracted for Lemma 1 is always an 'n'-bit row value.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:1919](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspaperregisters-row-lt-gridsize). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperRegisters_sparseIndexValue_eq" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperRegisters_sparseIndexValue_eq")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper registers sparse index value eq”; the hypotheses and conclusion in the code panel fix its exact scope. The sparse-index field is the high sparse slice of the full O\_D register.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The sparse-index field is the high sparse slice of the full O\_D register.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:1928](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspaperregisters-sparseindexvalue-eq). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperRegisters_paddedZeroValue_eq" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperRegisters_paddedZeroValue_eq")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper registers padded zero value eq”; the hypotheses and conclusion in the code panel fix its exact scope. The padded-zero field is the low padded slice of the full O\_D register.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The padded-zero field is the low padded slice of the full O\_D register.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:1936](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspaperregisters-paddedzerovalue-eq). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperRegisters_sparseIndex_lt" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperRegisters_sparseIndex_lt")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper registers sparse index lt”; the hypotheses and conclusion in the code panel fix its exact scope. The extracted sparse-index field always fits in its declared bit width.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The extracted sparse-index field always fits in its declared bit width.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:1944](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspaperregisters-sparseindex-lt). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperRegisters_odRegisterValue_lt" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperRegisters_odRegisterValue_lt")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper registers od register value lt”; the hypotheses and conclusion in the code panel fix its exact scope. The extracted O\_D register value always fits in its declared 'n'-bit block.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The extracted O\_D register value always fits in its declared 'n'-bit block.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:1955](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspaperregisters-odregistervalue-lt). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.bandedSparseAccessRowDependentPaperAddress" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessRowDependentPaperAddress")
*Plain-English reading.* This definition gives the library's named construction or computation for “banded sparse access row dependent paper address”. Rejected row-dependent paper-address helper.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Rejected row-dependent paper-address helper. This is the old active address model: it used 'robinSparseColumnMap', which deletes boundary zero-amplitude sparse slots by folding them back to the row. It is retained only for regression tests and proof-attempt memory.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:1971](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccessrowdependentpaperaddress). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperAddress" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperAddress")
*Plain-English reading.* This definition gives the library's named construction or computation for “banded sparse access paper address”. Paper address value 'r\_si' for the one-term Robin sparse-access oracle.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Paper address value 'r\_si' for the one-term Robin sparse-access oracle. The active address follows the global sparse-slot formula 'r\_si = r\_s0 + i mod 2^n'. Boundary or zero-amplitude slots remain present in the sparse register; the coefficient layer supplies zero values where needed.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:1983](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspaperaddress). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperAddressInRange" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperAddressInRange")
*Plain-English reading.* This definition gives the library's named construction or computation for “banded sparse access paper address in range”. Executable check that the paper address 'r\_si' fits in the n-bit address register.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Executable check that the paper address 'r\_si' fits in the n-bit address register.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:1988](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspaperaddressinrange). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperAddressInRange_iff" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperAddressInRange_iff")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper address in range iff”; the hypotheses and conclusion in the code panel fix its exact scope. Boolean form of the executable 'O\_D^BS' address-range check.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Boolean form of the executable 'O\_D^BS' address-range check.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:1992](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspaperaddressinrange-iff). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperAddress_lt_gridSize_of_two_le" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperAddress_lt_gridSize_of_two_le")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper address lt grid size of two le”; the hypotheses and conclusion in the code panel fix its exact scope. The executable paper address is in range for the fourth-order grid regime '2 <= n'.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The executable paper address is in range for the fourth-order grid regime '2 <= n'. This is a reusable arithmetic block; it does not promote the paper-level semantic obligation.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:2004](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspaperaddress-lt-gridsize-of-two-le). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperAddressInRange_eq_true_of_two_le" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperAddressInRange_eq_true_of_two_le")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper address in range eq true of two le”; the hypotheses and conclusion in the code panel fix its exact scope. The executable address-range Boolean evaluates to true for the fourth-order grid regime '2 <= n'.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The executable address-range Boolean evaluates to true for the fourth-order grid regime '2 <= n'. The contract flag remains false until the paper parameter family records this side condition.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:2017](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspaperaddressinrange-eq-true-of-two-le). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperImage" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperImage")
*Plain-English reading.* This definition gives the library's named construction or computation for “banded sparse access paper image”. Executable Lemma 1 image skeleton for 'O\_D^BS'.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Executable Lemma 1 image skeleton for 'O\_D^BS'. It preserves the row/system register and replaces the padded sparse-address register by 'r\_si'. Correctness, unitarity, and dagger cleanup remain recorded in 'defaultBandedSparseAccessPaperContract p' with 'proved := false'.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:2031](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspaperimage). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.bandedSparseAccessRowDependentPaperImage" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessRowDependentPaperImage")
*Plain-English reading.* This definition gives the library's named construction or computation for “banded sparse access row dependent paper image”. Rejected row-dependent image helper corresponding to the old active address.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Rejected row-dependent image helper corresponding to the old active address. This has the same register splice as 'bandedSparseAccessPaperImage', but writes the row-dependent helper address. It is not the active paper image.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:2047](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccessrowdependentpaperimage). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperRegisterValue_eq_mod" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperRegisterValue_eq_mod")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper register value eq mod”; the hypotheses and conclusion in the code panel fix its exact scope. Bit-slice extraction as arithmetic division followed by an 'n'-bit remainder.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Bit-slice extraction as arithmetic division followed by an 'n'-bit remainder. This keeps later register-splice proofs in ordinary arithmetic form.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:2062](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspaperregistervalue-eq-mod). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperHighWidth_le_totalQubits" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperHighWidth_le_totalQubits")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper high width le total qubits”; the hypotheses and conclusion in the code panel fix its exact scope. The O\_D^BS address block ends before the full one-term Robin basis width.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The O\_D^BS address block ends before the full one-term Robin basis width.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:2076](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspaperhighwidth-le-totalqubits). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperImage_lowBlock_lt_highBase_of_address_lt" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperImage_lowBlock_lt_highBase_of_address_lt")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper image low block lt high base of address lt”; the hypotheses and conclusion in the code panel fix its exact scope. The low block of the paper image fits below the high-tail boundary whenever the written O\_D^BS address is an n-bit value.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The low block of the paper image fits below the high-tail boundary whenever the written O\_D^BS address is an n-bit value.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:2087](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspaperimage-lowblock-lt-highbase-of-address-lt). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperImage_mod_lowBase" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperImage_mod_lowBase")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper image mod low base”; the hypotheses and conclusion in the code panel fix its exact scope. The paper image preserves the low ancilla-and-row block modulo its width.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The paper image preserves the low ancilla-and-row block modulo its width.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:2133](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspaperimage-mod-lowbase). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperImage_div_lowBase_mod_eq" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperImage_div_lowBase_mod_eq")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper image div low base mod eq”; the hypotheses and conclusion in the code panel fix its exact scope. After shifting past the low block, the paper image exposes the written address modulo the n-bit O\_D^BS register.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* After shifting past the low block, the paper image exposes the written address modulo the n-bit O\_D^BS register.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:2172](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspaperimage-div-lowbase-mod-eq). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperImage_lt_qubitDim_of_address_lt" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperImage_lt_qubitDim_of_address_lt")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper image lt qubit dim of address lt”; the hypotheses and conclusion in the code panel fix its exact scope. The executable paper image remains inside the full finite basis when the input column is in range and the written O\_D^BS address is n-bit.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The executable paper image remains inside the full finite basis when the input column is in range and the written O\_D^BS address is n-bit.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:2219](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspaperimage-lt-qubitdim-of-address-lt). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperImageFin" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperImageFin")
*Plain-English reading.* This definition gives the library's named construction or computation for “banded sparse access paper image fin”. Finite-basis index for the executable Lemma 1 'O\_D^BS' paper image.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Finite-basis index for the executable Lemma 1 'O\_D^BS' paper image. This constructor is available only when the source column is already in the full finite basis and the written 'O\_D^BS' address is n-bit. It is a bridge from the arithmetic image function to matrix entries, not a unitarity proof.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:2268](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspaperimagefin). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperImageFin_val" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperImageFin_val")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper image fin val”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:2276](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspaperimagefin-val). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperImage_rowValue_eq" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperImage_rowValue_eq")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper image row value eq”; the hypotheses and conclusion in the code panel fix its exact scope. Register extraction from the paper image preserves the row register.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Register extraction from the paper image preserves the row register.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:2284](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspaperimage-rowvalue-eq). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperImage_odRegisterValue_eq" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperImage_odRegisterValue_eq")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper image od register value eq”; the hypotheses and conclusion in the code panel fix its exact scope. Register extraction from the paper image reports the written O\_D^BS address.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Register extraction from the paper image reports the written O\_D^BS address.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:2303](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspaperimage-odregistervalue-eq). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperHighTail" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperHighTail")
*Plain-English reading.* This definition gives the library's named construction or computation for “banded sparse access paper high tail”. High signal/workspace bits above the n-bit 'O\_D^BS' address register.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* High signal/workspace bits above the n-bit 'O\_D^BS' address register.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:2315](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspaperhightail). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperImage_highTail_eq_of_address_lt" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperImage_highTail_eq_of_address_lt")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper image high tail eq of address lt”; the hypotheses and conclusion in the code panel fix its exact scope. The arithmetic register-splice form of 'bandedSparseAccessPaperImage' preserves all bits above the 'O\_D^BS' address register when the written address is n-bit.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The arithmetic register-splice form of 'bandedSparseAccessPaperImage' preserves all bits above the 'O\_D^BS' address register when the written address is n-bit. This is a proof-DAG block for Lemma 1 register safety. It does not promote the paper-level 'noSpill' obligation because the parameter-family side conditions are still tracked by 'defaultBandedSparseAccessPaperContract'.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:2326](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspaperimage-hightail-eq-of-address-lt). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperImageNoSpill" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperImageNoSpill")
*Plain-English reading.* This definition gives the library's named construction or computation for “banded sparse access paper image no spill”. Executable check that the paper-image skeleton does not write past the n-bit 'O\_D^BS' address register into the indicator or 'm\_f' bits above it.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Executable check that the paper-image skeleton does not write past the n-bit 'O\_D^BS' address register into the indicator or 'm\_f' bits above it.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:2361](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspaperimagenospill). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperImageNoSpill_iff" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperImageNoSpill_iff")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper image no spill iff”; the hypotheses and conclusion in the code panel fix its exact scope. Boolean form of the executable high-tail no-spill check.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Boolean form of the executable high-tail no-spill check. The high-tail theorem above discharges this Boolean under an n-bit written address, while the paper-level semantic obligation remains a separate flag.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:2371](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspaperimagenospill-iff). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperImageNoSpill_eq_true_of_address_lt" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperImageNoSpill_eq_true_of_address_lt")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper image no spill eq true of address lt”; the hypotheses and conclusion in the code panel fix its exact scope. The no-spill Boolean follows from the executable n-bit address bound.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The no-spill Boolean follows from the executable n-bit address bound.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:2380](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspaperimagenospill-eq-true-of-address-lt). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperImageNoSpill_eq_true_of_two_le" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperImageNoSpill_eq_true_of_two_le")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper image no spill eq true of two le”; the hypotheses and conclusion in the code panel fix its exact scope. The no-spill Boolean is true in the fourth-order grid regime '2 <= n', reusing the address-range proof-DAG block.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The no-spill Boolean is true in the fourth-order grid regime '2 <= n', reusing the address-range proof-DAG block. Semantic obligation flags remain false.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:2391](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspaperimagenospill-eq-true-of-two-le). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperCleanInput" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperCleanInput")
*Plain-English reading.* This definition gives the library's named construction or computation for “banded sparse access paper clean input”. Clean-domain predicate for the Lemma 1 'O\_D^BS' source equation.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Clean-domain predicate for the Lemma 1 'O\_D^BS' source equation. The paper specifies columns whose padded zero register is '|0>^(n-l)'. Columns outside this domain still need a separate unitary-completion proof; the current paper-image matrix is only a Phase 1 skeleton for that full-space extension.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:2406](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspapercleaninput). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperSparseIndexInKappa" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperSparseIndexInKappa")
*Plain-English reading.* This definition gives the library's named construction or computation for “banded sparse access paper sparse index in kappa”. Faithful sparse-slot range for the Lemma 1 'O\_D^BS' source equation.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Faithful sparse-slot range for the Lemma 1 'O\_D^BS' source equation. The paper source domain keeps the global slot 's' whenever 's < kappa'. Whether a boundary coefficient is zero is handled by the amplitude layer, not by deleting the sparse-register slot from the index oracle.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:2416](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspapersparseindexinkappa). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperGlobalSlotSource" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperGlobalSlotSource")
*Plain-English reading.* This definition gives the library's named construction or computation for “banded sparse access paper global slot source”. Faithful clean source domain for the active global-slot 'O\_D^BS' address.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Faithful clean source domain for the active global-slot 'O\_D^BS' address. This predicate is the padded clean input from Lemma 1 together with the global slot range 's < kappa'. It supersedes the row-dependent nonzero-branch classifier as the active source-domain contract for 'bandedSparseAccessPaperImage'.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:2427](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspaperglobalslotsource). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperGlobalSlotSource_cleanInput_eq_true" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperGlobalSlotSource_cleanInput_eq_true")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper global slot source clean input eq true”; the hypotheses and conclusion in the code panel fix its exact scope. A faithful global-slot source column is clean in the padded O\_D register.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* A faithful global-slot source column is clean in the padded O\_D register.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:2433](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspaperglobalslotsource-cleaninput-eq-true). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperGlobalSlotSource_sparseIndex_lt_kappa" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperGlobalSlotSource_sparseIndex_lt_kappa")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper global slot source sparse index lt kappa”; the hypotheses and conclusion in the code panel fix its exact scope. A faithful global-slot source column has sparse index below 'kappa'.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* A faithful global-slot source column has sparse index below 'kappa'.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:2442](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspaperglobalslotsource-sparseindex-lt-kappa). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperGlobalSlotSource_inverseSlot_injective" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperGlobalSlotSource_inverseSlot_injective")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper global slot source inverse slot injective”; the hypotheses and conclusion in the code panel fix its exact scope. Global-source wrapper for inverse-slot injectivity.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Global-source wrapper for inverse-slot injectivity. For the one-term Robin 'κ = 7' family, two active global-source columns with the same reverse sparse slot have the same extracted sparse slot. This is a local source-domain block for the later unique-preimage proof; it does not promote inverse-on-range, cleanup, or unitarity obligations.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:2461](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspaperglobalslotsource-inverseslot-injective). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperAddress_same_row_injective_of_globalSlotSource" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperAddress_same_row_injective_of_globalSlotSource")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper address same row injective of global slot source”; the hypotheses and conclusion in the code panel fix its exact scope. Same-row injectivity of the active paper address on the global-slot source domain.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Same-row injectivity of the active paper address on the global-slot source domain. This lifts the seven-slot address lemma through the Lemma 1 register extractor: if two active global-source columns have the same row and the same corrected paper address, then their sparse slots are equal. It does not assert matrix unitarity or dagger cleanup.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:2493](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspaperaddress-same-row-injective-of-globalslotsource). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperValidSparseBranch" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperValidSparseBranch")
*Plain-English reading.* This definition gives the library's named construction or computation for “banded sparse access paper valid sparse branch”. Candidate row-dependent sparse-branch domain for a basis column of Lemma 1.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Candidate row-dependent sparse-branch domain for a basis column of Lemma 1. This is deliberately separate from 'bandedSparseAccessPaperCleanInput'. The paper clean-input condition only checks the padded zero register, while this candidate also excludes row-boundary sparse indices that do not correspond to nonzero stencil entries. It is now a rejected-model audit helper; the active paper source domain is 'bandedSparseAccessPaperGlobalSlotSource'.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:2541](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspapervalidsparsebranch). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperValidCleanSource" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperValidCleanSource")
*Plain-English reading.* This definition gives the library's named construction or computation for “banded sparse access paper valid clean source”. Candidate corrected clean source domain for Lemma 1: padded-zero input plus a row-dependent valid sparse branch.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Candidate corrected clean source domain for Lemma 1: padded-zero input plus a row-dependent valid sparse branch. This is a rejected-model contract-audit predicate only. Use 'bandedSparseAccessPaperGlobalSlotSource' for the active global-slot source contract.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:2552](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspapervalidcleansource). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperValidCleanSource_cleanInput_eq_true" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperValidCleanSource_cleanInput_eq_true")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper valid clean source clean input eq true”; the hypotheses and conclusion in the code panel fix its exact scope. The corrected source-domain candidate implies the original clean input.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The corrected source-domain candidate implies the original clean input.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:2558](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspapervalidcleansource-cleaninput-eq-true). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperValidCleanSource_validSparseBranch_eq_true" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperValidCleanSource_validSparseBranch_eq_true")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper valid clean source valid sparse branch eq true”; the hypotheses and conclusion in the code panel fix its exact scope. The corrected source-domain candidate implies a valid sparse branch.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The corrected source-domain candidate implies a valid sparse branch.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:2567](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspapervalidcleansource-validsparsebranch-eq-true). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperValidCleanSource_separates_boundaryCollision_n3" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperValidCleanSource_separates_boundaryCollision_n3")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper valid clean source separates boundary collision n 3”; the hypotheses and conclusion in the code panel fix its exact scope. The row-dependent valid-source audit excludes the concrete unused sparse branch from the recorded 'n = 3', 'kappa = 7' rejected-model collision.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The row-dependent valid-source audit excludes the concrete unused sparse branch from the recorded 'n = 3', 'kappa = 7' rejected-model collision. The active global-slot paper image now separates the same two clean columns.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:2580](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspapervalidcleansource-separates-boundarycollision-n3). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperUnusedSparseBranch" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperUnusedSparseBranch")
*Plain-English reading.* This definition gives the library's named construction or computation for “banded sparse access paper unused sparse branch”. Classifier for clean padded-register columns whose sparse branch is invalid for the row-dependent Robin stencil.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Classifier for clean padded-register columns whose sparse branch is invalid for the row-dependent Robin stencil. This is the source-domain side of the unused-branch extension obligation. The active O\_D^BS matrices are not changed by this predicate.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:2601](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspaperunusedsparsebranch). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperUnusedSparseBranch_cleanInput_eq_true" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperUnusedSparseBranch_cleanInput_eq_true")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper unused sparse branch clean input eq true”; the hypotheses and conclusion in the code panel fix its exact scope. An unused sparse branch is still in the padded clean-input domain.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* An unused sparse branch is still in the padded clean-input domain.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:2607](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspaperunusedsparsebranch-cleaninput-eq-true). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperUnusedSparseBranch_validSparseBranch_eq_false" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperUnusedSparseBranch_validSparseBranch_eq_false")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper unused sparse branch valid sparse branch eq false”; the hypotheses and conclusion in the code panel fix its exact scope. An unused sparse branch is outside the row-dependent valid-branch classifier.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* An unused sparse branch is outside the row-dependent valid-branch classifier.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:2616](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspaperunusedsparsebranch-validsparsebranch-eq-false). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperCleanDomainSplit_iff" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperCleanDomainSplit_iff")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper clean domain split iff”; the hypotheses and conclusion in the code panel fix its exact scope. The executable clean padded-input domain splits into valid sparse branches and clean unused sparse branches.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The executable clean padded-input domain splits into valid sparse branches and clean unused sparse branches. This is only the local Boolean classifier split for the source-contract audit. It does not choose an image for unused branches and does not promote the semantic 'cleanDomainSplit' obligation in the full-domain wrapper.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:2632](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspapercleandomainsplit-iff). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperCleanDomainSplit_disjoint" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperCleanDomainSplit_disjoint")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper clean domain split disjoint”; the hypotheses and conclusion in the code panel fix its exact scope. The two branches in 'bandedSparseAccessPaperCleanDomainSplit\_iff' are disjoint.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The two branches in 'bandedSparseAccessPaperCleanDomainSplit\_iff' are disjoint. This is a classifier fact only; injectivity and unitary extension for the eventual image rule remain separate false obligations.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:2657](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspapercleandomainsplit-disjoint). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.BandedSparseAccessUnusedBranchImageRuleContract" (lean := "QuantumBlockEncoding.GHL2025.BandedSparseAccessUnusedBranchImageRuleContract")
*Plain-English reading.* This record groups the data and proof fields needed for “banded sparse access unused branch image rule contract”. A proposition-valued field is a requirement until a constructor supplies it. Interface for the missing reversible image rule on clean unused sparse branches.

*Formal status.* Data contract in the default import surface; proposition-valued fields are obligations, not automatically established facts.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Interface for the missing reversible image rule on clean unused sparse branches. No paper-backed formula has been selected yet, so 'proposedImageIndex' is 'none' and every semantic claim remains an explicit false obligation. The active 'bandedSparseAccessPaperImage' skeleton is not changed by this record.

*Declaration kind.* structure.

Source: [QuantumBlockEncoding/GHL2025.lean:2680](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccessunusedbranchimagerulecontract). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.bandedSparseAccessUnusedBranchImageRuleContract" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessUnusedBranchImageRuleContract")
*Plain-English reading.* This definition gives the library's named construction or computation for “banded sparse access unused branch image rule contract”. Default image-rule interface for one unused-branch source column.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Default image-rule interface for one unused-branch source column. The missing reversible image is intentionally represented by 'none'; later faithful work must replace this with a paper-compatible extension before any injectivity, dagger-cleanup, or unitarity proof is attempted.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:2702](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccessunusedbranchimagerulecontract). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessUnusedBranchImageRuleContract_flags_false" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessUnusedBranchImageRuleContract_flags_false")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access unused branch image rule contract flags false”; the hypotheses and conclusion in the code panel fix its exact scope. The unused-branch image-rule interface is obligation-only in Phase 1.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The unused-branch image-rule interface is obligation-only in Phase 1.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:2735](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccessunusedbranchimagerulecontract-flags-false). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessUnusedBranchImageRuleContract_of_unusedBranch" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessUnusedBranchImageRuleContract_of_unusedBranch")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access unused branch image rule contract of unused branch”; the hypotheses and conclusion in the code panel fix its exact scope. Classifier bridge for the unused-branch image-rule interface.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Classifier bridge for the unused-branch image-rule interface. For a clean invalid sparse branch, Lean records the branch classification and keeps the image-rule target unspecified with false proof fields.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:2750](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccessunusedbranchimagerulecontract-of-unusedbranch). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.BandedSparseAccessUnusedBranchExtensionContract" (lean := "QuantumBlockEncoding.GHL2025.BandedSparseAccessUnusedBranchExtensionContract")
*Plain-English reading.* This record groups the data and proof fields needed for “banded sparse access unused branch extension contract”. A proposition-valued field is a requirement until a constructor supplies it. Contract slot for a faithful reversible extension on unused sparse branches.

*Formal status.* Data contract in the default import surface; proposition-valued fields are obligations, not automatically established facts.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Contract slot for a faithful reversible extension on unused sparse branches. GHL2025 keeps zero-amplitude sparse branches inside the kappa-wide register. The current active image skeleton can collide on such branches, so Phase 1 records the missing extension as obligations instead of proving injectivity or unitarity for the colliding skeleton.

*Declaration kind.* structure.

Source: [QuantumBlockEncoding/GHL2025.lean:2775](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccessunusedbranchextensioncontract). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.bandedSparseAccessUnusedBranchExtensionContract" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessUnusedBranchExtensionContract")
*Plain-English reading.* This definition gives the library's named construction or computation for “banded sparse access unused branch extension contract”. Default unused-branch extension contract for one O\_D^BS basis column.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Default unused-branch extension contract for one O\_D^BS basis column. All semantic fields remain false. The record exists so later work can state the reversible completion separately from the paper image on valid branches.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:2797](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccessunusedbranchextensioncontract). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessUnusedBranchExtensionContract_flags_false" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessUnusedBranchExtensionContract_flags_false")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access unused branch extension contract flags false”; the hypotheses and conclusion in the code panel fix its exact scope. The unused-branch extension contract is obligation-only in Phase 1.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The unused-branch extension contract is obligation-only in Phase 1.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:2837](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccessunusedbranchextensioncontract-flags-false). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessUnusedBranchExtensionContract_boundaryCollision_n3" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessUnusedBranchExtensionContract_boundaryCollision_n3")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access unused branch extension contract boundary collision n 3”; the hypotheses and conclusion in the code panel fix its exact scope. The unused-branch contract classifies the recorded row-dependent boundary collision without promoting any O\_D^BS semantic proof flag.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The unused-branch contract classifies the recorded row-dependent boundary collision without promoting any O\_D^BS semantic proof flag. The active global-slot image no longer has this concrete collision; the extension fields remain false because this packet does not prove full clean-domain injectivity, dagger cleanup, or unitarity.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:2856](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccessunusedbranchextensioncontract-boundarycollision-n3). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessUnusedBranchExtensionContract_of_unusedBranch" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessUnusedBranchExtensionContract_of_unusedBranch")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access unused branch extension contract of unused branch”; the hypotheses and conclusion in the code panel fix its exact scope. Package the unused-branch classifier with the reversible-extension obligations.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Package the unused-branch classifier with the reversible-extension obligations. This is a contract bridge only: it exposes that an unused clean branch is in the clean padded-input domain, is outside the row-dependent valid sparse-branch classifier, and still has only false extension proof fields.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:2875](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccessunusedbranchextensioncontract-of-unusedbranch). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.BandedSparseAccessFullCleanDomainExtensionContract" (lean := "QuantumBlockEncoding.GHL2025.BandedSparseAccessFullCleanDomainExtensionContract")
*Plain-English reading.* This record groups the data and proof fields needed for “banded sparse access full clean domain extension contract”. A proposition-valued field is a requirement until a constructor supplies it. Paper-level wrapper for the full clean-domain extension obligation of 'O\_D^BS'.

*Formal status.* Data contract in the default import surface; proposition-valued fields are obligations, not automatically established facts.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Paper-level wrapper for the full clean-domain extension obligation of 'O\_D^BS'. The paper clean domain contains every padded-zero source '|0>^(n-l)|s>^l|i>^n', including zero-amplitude sparse branches. QBE currently has only the active Lemma 1 image on valid row-dependent branches and a per-column interface for clean unused branches. This record lifts those pieces into one contract without choosing a reversible unused-branch image and without changing the active forward or dagger matrices.

*Declaration kind.* structure.

Source: [QuantumBlockEncoding/GHL2025.lean:2906](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccessfullcleandomainextensioncontract). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.bandedSparseAccessFullCleanDomainExtensionContract" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessFullCleanDomainExtensionContract")
*Plain-English reading.* This definition gives the library's named construction or computation for “banded sparse access full clean domain extension contract”. Default full clean-domain extension contract for Lemma 1 'O\_D^BS'.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Default full clean-domain extension contract for Lemma 1 'O\_D^BS'. All semantic fields are false obligations. The nested per-column image-rule contract still has 'proposedImageIndex = none', so this declaration only records the missing proof interface for later source-domain reconciliation.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:2932](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccessfullcleandomainextensioncontract). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessFullCleanDomainExtensionContract_flags_false" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessFullCleanDomainExtensionContract_flags_false")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access full clean domain extension contract flags false”; the hypotheses and conclusion in the code panel fix its exact scope. The full clean-domain wrapper is obligation-only in Phase 1.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The full clean-domain wrapper is obligation-only in Phase 1.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:2986](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccessfullcleandomainextensioncontract-flags-false). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessFullCleanDomainExtensionContract_of_unusedBranch" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessFullCleanDomainExtensionContract_of_unusedBranch")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access full clean domain extension contract of unused branch”; the hypotheses and conclusion in the code panel fix its exact scope. The full clean-domain wrapper reuses the existing per-column unused-branch classifier bridge and keeps every extension proof flag false.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The full clean-domain wrapper reuses the existing per-column unused-branch classifier bridge and keeps every extension proof flag false.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:3006](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccessfullcleandomainextensioncontract-of-unusedbranch). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessFullCleanDomainExtensionContract_localCleanDomainSplit" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessFullCleanDomainExtensionContract_localCleanDomainSplit")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access full clean domain extension contract local clean domain split”; the hypotheses and conclusion in the code panel fix its exact scope. Wrapper-facing form of the local clean-domain split audit.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Wrapper-facing form of the local clean-domain split audit. The classifier split is Lean-proved, while the full semantic wrapper still keeps 'cleanDomainSplit.proved = false' because no unused-branch image rule, injectivity proof, or unitary extension has been supplied.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:3032](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccessfullcleandomainextensioncontract-localcleandomainsplit). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.BandedSparseAccessUnusedZeroBranchSourceDecision" (lean := "QuantumBlockEncoding.GHL2025.BandedSparseAccessUnusedZeroBranchSourceDecision")
*Plain-English reading.* This record groups the data and proof fields needed for “banded sparse access unused zero branch source decision”. A proposition-valued field is a requirement until a constructor supplies it. Lean-facing source decision for unused zero-amplitude 'O\_D^BS' branches.

*Formal status.* Data contract in the default import surface; proposition-valued fields are obligations, not automatically established facts.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Lean-facing source decision for unused zero-amplitude 'O\_D^BS' branches. Cycle 14 records that no paper-backed image formula and no accepted external reversible-extension theorem currently supplies the missing image rule for clean unused sparse branches. This is a blocking dependency record, not a new oracle construction.

*Declaration kind.* structure.

Source: [QuantumBlockEncoding/GHL2025.lean:3049](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccessunusedzerobranchsourcedecision). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.bandedSparseAccessUnusedZeroBranchSourceDecision" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessUnusedZeroBranchSourceDecision")
*Plain-English reading.* This definition gives the library's named construction or computation for “banded sparse access unused zero branch source decision”. Default cycle-14 source decision for unused zero-amplitude sparse branches.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Default cycle-14 source decision for unused zero-amplitude sparse branches. The false Boolean fields deliberately prevent lower proof work from treating the current colliding active image as a permutation or unitary extension.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:3064](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccessunusedzerobranchsourcedecision). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessUnusedZeroBranchSourceDecision_flags_false" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessUnusedZeroBranchSourceDecision_flags_false")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access unused zero branch source decision flags false”; the hypotheses and conclusion in the code panel fix its exact scope. The cycle-14 source decision is a blocking obligation, not a proof ticket.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The cycle-14 source decision is a blocking obligation, not a proof ticket.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:3078](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccessunusedzerobranchsourcedecision-flags-false). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessUnusedZeroBranchSourceDecision_keepsFullDomainFlagsFalse" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessUnusedZeroBranchSourceDecision_keepsFullDomainFlagsFalse")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access unused zero branch source decision keeps full domain flags false”; the hypotheses and conclusion in the code panel fix its exact scope. The source decision keeps the full clean-domain wrapper in obligation mode.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source decision keeps the full clean-domain wrapper in obligation mode. This ties the cited-results dependency to the existing wrapper fields without changing any active matrix or promoting any semantic proof flag.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:3091](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccessunusedzerobranchsourcedecision-keepsfulldomainflagsfalse). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessUnusedZeroBranchSourceDecision_keepsImageRuleUnspecified" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessUnusedZeroBranchSourceDecision_keepsImageRuleUnspecified")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access unused zero branch source decision keeps image rule unspecified”; the hypotheses and conclusion in the code panel fix its exact scope. The blocking source decision keeps every unused-branch image slot unspecified.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The blocking source decision keeps every unused-branch image slot unspecified. This is a guard for later lower packets: disabling proof search also means the per-column image-rule contract and the full-domain wrapper still expose 'proposedImageIndex = none' with false image-rule obligations.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:3112](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccessunusedzerobranchsourcedecision-keepsimageruleunspecified). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessUnusedZeroBranchSourceDecision_keepsPaperContractFlagsFalse" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessUnusedZeroBranchSourceDecision_keepsPaperContractFlagsFalse")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access unused zero branch source decision keeps paper contract flags false”; the hypotheses and conclusion in the code panel fix its exact scope. The blocking source decision also keeps the paper-level O\_D^BS contract obligations false.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The blocking source decision also keeps the paper-level O\_D^BS contract obligations false. This is separate from the full clean-domain wrapper: it pins the original Lemma 1 contract fields that a later proof packet would otherwise try to close.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:3134](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccessunusedzerobranchsourcedecision-keepspapercontractflagsfalse). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.BandedSparseAccessRobinZeroInclusionSourceContract" (lean := "QuantumBlockEncoding.GHL2025.BandedSparseAccessRobinZeroInclusionSourceContract")
*Plain-English reading.* This record groups the data and proof fields needed for “banded sparse access robin zero inclusion source contract”. A proposition-valued field is a requirement until a constructor supplies it. Source transcript for the Robin zero-inclusion sentence near Theorem 1.

*Formal status.* Data contract in the default import surface; proposition-valued fields are obligations, not automatically established facts.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Source transcript for the Robin zero-inclusion sentence near Theorem 1. The paper states that zeros may be included in the sparse enumeration and then uses the range 's = 0, ..., kappa - 1' in Eq. ROBIN clarified. This source fact explains why the row-dependent nonzero-stencil classifier is not a faithful full domain restriction. It still does not choose an injective image for clean unused zero-amplitude branches.

*Declaration kind.* structure.

Source: [QuantumBlockEncoding/GHL2025.lean:3152](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccessrobinzeroinclusionsourcecontract). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.bandedSparseAccessRobinZeroInclusionSourceContract" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessRobinZeroInclusionSourceContract")
*Plain-English reading.* This definition gives the library's named construction or computation for “banded sparse access robin zero inclusion source contract”. Default transcript of the GHL2025 Robin zero-inclusion source text.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Default transcript of the GHL2025 Robin zero-inclusion source text. The source records that zero-amplitude sparse branches remain in the kappa-wide sparse register. The missing image rule and any reversible extension theorem are intentionally absent, so this declaration does not unblock O\_D^BS proof search.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:3176](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccessrobinzeroinclusionsourcecontract). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessRobinZeroInclusionSourceContract_blocks_unusedZeroBranch" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessRobinZeroInclusionSourceContract_blocks_unusedZeroBranch")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access robin zero inclusion source contract blocks unused zero branch”; the hypotheses and conclusion in the code panel fix its exact scope. The Robin zero-inclusion source transcript keeps the unused-branch route blocked.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The Robin zero-inclusion source transcript keeps the unused-branch route blocked. This guard records the exact source-backed inclusion of zero-amplitude sparse branches while preserving the absence of a branch image rule or reversible extension theorem.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:3211](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccessrobinzeroinclusionsourcecontract-blocks-unusedzerobranch). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessRobinZeroInclusionSourceContract_keepsImageRuleUnspecified" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessRobinZeroInclusionSourceContract_keepsImageRuleUnspecified")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access robin zero inclusion source contract keeps image rule unspecified”; the hypotheses and conclusion in the code panel fix its exact scope. The zero-inclusion transcript does not fill the per-column image-rule slot.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The zero-inclusion transcript does not fill the per-column image-rule slot. For every one-term parameter choice and source column, the direct image-rule contract remains unspecified while lower proof search is disabled.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:3239](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccessrobinzeroinclusionsourcecontract-keepsimageruleunspecified). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.BandedSparseAccessPriorPDESourceContract" (lean := "QuantumBlockEncoding.GHL2025.BandedSparseAccessPriorPDESourceContract")
*Plain-English reading.* This record groups the data and proof fields needed for “banded sparse access prior pde source contract”. A proposition-valued field is a requirement until a constructor supplies it. Source contract imported from the prior PDE block-encoding paper.

*Formal status.* Data contract in the default import surface; proposition-valued fields are obligations, not automatically established facts.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Source contract imported from the prior PDE block-encoding paper. The prior paper supplies the same padded-register equation and an appendix decomposition into a first-row index unitary and modular addition. This record is intentionally only a source transcript: it does not provide a Robin-specific image rule for clean unused zero-amplitude sparse branches.

*Declaration kind.* structure.

Source: [QuantumBlockEncoding/GHL2025.lean:3264](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspriorpdesourcecontract). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.bandedSparseAccessPriorPDESourceContract" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPriorPDESourceContract")
*Plain-English reading.* This definition gives the library's named construction or computation for “banded sparse access prior pde source contract”. Default transcript of arXiv:2405.12855v3 Definition 6, Lemma 1, and the appendix construction for 'O\_A^BS'.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Default transcript of arXiv:2405.12855v3 Definition 6, Lemma 1, and the appendix construction for 'O\_A^BS'. The field 'robinUnusedBranchImageRule = none' records the audit result: this source supports the imported sparse-access primitive, but not the missing Robin unused-branch image formula.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:3285](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspriorpdesourcecontract). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.robinBandedSparseAccessCitationChain" (lean := "QuantumBlockEncoding.GHL2025.robinBandedSparseAccessCitationChain")
*Plain-English reading.* This definition gives the library's named construction or computation for “robin banded sparse access citation chain”. The explicit citation chain for the displayed Robin sparse-address equation.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The explicit citation chain for the displayed Robin sparse-address equation. Lemma 2 of arXiv:2506.20478 invokes Lemma 1 of arXiv:2405.12855; older transcript fields above retain their historical labels for drift auditing.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:3308](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-robinbandedsparseaccesscitationchain). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.robinBandedSparseAccessCitationChain_eq" (lean := "QuantumBlockEncoding.GHL2025.robinBandedSparseAccessCitationChain_eq")
*Plain-English reading.* Lean checks the proposition indexed as “robin banded sparse access citation chain eq”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:3313](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-robinbandedsparseaccesscitationchain-eq). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPriorPDESourceContract_blocks_unusedZeroBranch" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPriorPDESourceContract_blocks_unusedZeroBranch")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access prior pde source contract blocks unused zero branch”; the hypotheses and conclusion in the code panel fix its exact scope. The prior PDE source does not unblock the QBE unused-zero-branch extension.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The prior PDE source does not unblock the QBE unused-zero-branch extension. This is the compiled guard for the source audit: the cited theorem is recorded, but lower proof search for Robin unused-branch injectivity, cleanup, and unitarity remains disabled.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:3326](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspriorpdesourcecontract-blocks-unusedzerobranch). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPriorPDESourceContract_oracleEquation" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPriorPDESourceContract_oracleEquation")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access prior pde source contract oracle equation”; the hypotheses and conclusion in the code panel fix its exact scope. The prior PDE source contract records the exact sparse-access equation.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The prior PDE source contract records the exact sparse-access equation.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:3335](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspriorpdesourcecontract-oracleequation). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPriorPDESourceContract_resource_unproved" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPriorPDESourceContract_resource_unproved")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access prior pde source contract resource unproved”; the hypotheses and conclusion in the code panel fix its exact scope. The prior PDE resource claim remains an external obligation in QBE.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The prior PDE resource claim remains an external obligation in QBE.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:3340](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspriorpdesourcecontract-resource-unproved). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperCleanInput_iff" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperCleanInput_iff")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper clean input iff”; the hypotheses and conclusion in the code panel fix its exact scope. Boolean form of the Lemma 1 clean-input domain.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Boolean form of the Lemma 1 clean-input domain. The executable predicate is exactly the statement that the padded part of the 'O\_D^BS' sparse-address register is zero. This only classifies columns; it does not prove the clean-input source equation or a unitary extension.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:3350](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspapercleaninput-iff). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.BandedSparseAccessPaperColumnContract" (lean := "QuantumBlockEncoding.GHL2025.BandedSparseAccessPaperColumnContract")
*Plain-English reading.* This record groups the data and proof fields needed for “banded sparse access paper column contract”. A proposition-valued field is a requirement until a constructor supplies it. Per-column audit record for the executable Lemma 1 paper image.

*Formal status.* Data contract in the default import surface; proposition-valued fields are obligations, not automatically established facts.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Per-column audit record for the executable Lemma 1 paper image. This records the source-domain flag, the image index, and the two register properties expected from the paper equation. The Boolean fields are executable checks for the current skeleton; they are not promoted to theorem-level correctness. The obligation fields keep the clean-domain and full-unitary extension gaps explicit.

*Declaration kind.* structure.

Source: [QuantumBlockEncoding/GHL2025.lean:3366](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspapercolumncontract). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperColumnContract" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperColumnContract")
*Plain-English reading.* This definition gives the library's named construction or computation for “banded sparse access paper column contract”. Default per-column contract for the 'O\_D^BS' paper image skeleton.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Default per-column contract for the 'O\_D^BS' paper image skeleton.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:3385](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspapercolumncontract). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperColumnContract_inputRegisters_eq" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperColumnContract_inputRegisters_eq")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper column contract input registers eq”; the hypotheses and conclusion in the code panel fix its exact scope. The per-column contract uses the shared Lemma 1 register extractor.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The per-column contract uses the shared Lemma 1 register extractor.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:3409](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspapercolumncontract-inputregisters-eq). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperColumnContract_cleanInput_eq" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperColumnContract_cleanInput_eq")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper column contract clean input eq”; the hypotheses and conclusion in the code panel fix its exact scope. The per-column clean-domain flag is the executable padded-zero predicate.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The per-column clean-domain flag is the executable padded-zero predicate.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:3415](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspapercolumncontract-cleaninput-eq). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperColumnContract_cleanInput_iff" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperColumnContract_cleanInput_iff")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper column contract clean input iff”; the hypotheses and conclusion in the code panel fix its exact scope. The per-column clean-domain flag is true exactly on Lemma 1 clean columns.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The per-column clean-domain flag is true exactly on Lemma 1 clean columns. Columns with a nonzero padded register are still covered only by the explicit unitary-extension obligation.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:3426](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspapercolumncontract-cleaninput-iff). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperColumnContract_unitaryExtension_proved_eq_false" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperColumnContract_unitaryExtension_proved_eq_false")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper column contract unitary extension proved eq false”; the hypotheses and conclusion in the code panel fix its exact scope. The per-column audit keeps the full-space unitary extension as an open obligation for every column, including non-clean padded-register inputs.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The per-column audit keeps the full-space unitary extension as an open obligation for every column, including non-clean padded-register inputs.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:3437](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspapercolumncontract-unitaryextension-proved-eq-false). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperColumnContract_imageIndex_eq" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperColumnContract_imageIndex_eq")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper column contract image index eq”; the hypotheses and conclusion in the code panel fix its exact scope. The per-column contract records the same image index as the paper-image skeleton.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The per-column contract records the same image index as the paper-image skeleton.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:3442](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspapercolumncontract-imageindex-eq). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperColumnContract_addressInRange_eq" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperColumnContract_addressInRange_eq")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper column contract address in range eq”; the hypotheses and conclusion in the code panel fix its exact scope. The per-column contract records the executable n-bit address range check.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The per-column contract records the executable n-bit address range check.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:3448](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspapercolumncontract-addressinrange-eq). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperColumnContract_imageNoSpill_eq" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperColumnContract_imageNoSpill_eq")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper column contract image no spill eq”; the hypotheses and conclusion in the code panel fix its exact scope. The per-column contract records the executable high-bit no-spill check.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The per-column contract records the executable high-bit no-spill check.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:3454](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspapercolumncontract-imagenospill-eq). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperColumnContract_rowPreserved_eq_true" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperColumnContract_rowPreserved_eq_true")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper column contract row preserved eq true”; the hypotheses and conclusion in the code panel fix its exact scope. The per-column audit records that the paper image preserves the row register.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The per-column audit records that the paper image preserves the row register. This is an executable register-safety fact for the Phase 1 skeleton; it does not promote the paper-level 'forwardCorrect' obligation.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:3465](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspapercolumncontract-rowpreserved-eq-true). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperColumnContract_addressWritten_eq_true_of_address_lt" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperColumnContract_addressWritten_eq_true_of_address_lt")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper column contract address written eq true of address lt”; the hypotheses and conclusion in the code panel fix its exact scope. The per-column audit records that the paper image writes the O\_D register to the computed address whenever that address is an n-bit value.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The per-column audit records that the paper image writes the O\_D register to the computed address whenever that address is an n-bit value.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:3475](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspapercolumncontract-addresswritten-eq-true-of-address-lt). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperColumnContract_addressInRange_eq_true_of_address_lt" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperColumnContract_addressInRange_eq_true_of_address_lt")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper column contract address in range eq true of address lt”; the hypotheses and conclusion in the code panel fix its exact scope. The per-column address-range audit Boolean follows from the address bound.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The per-column address-range audit Boolean follows from the address bound.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:3483](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspapercolumncontract-addressinrange-eq-true-of-address-lt). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperColumnContract_imageNoSpill_eq_true_of_address_lt" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperColumnContract_imageNoSpill_eq_true_of_address_lt")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper column contract image no spill eq true of address lt”; the hypotheses and conclusion in the code panel fix its exact scope. The per-column no-spill audit Boolean follows from the address bound.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The per-column no-spill audit Boolean follows from the address bound.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:3491](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspapercolumncontract-imagenospill-eq-true-of-address-lt). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperColumnContract_registerSafety_of_address_lt" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperColumnContract_registerSafety_of_address_lt")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper column contract register safety of address lt”; the hypotheses and conclusion in the code panel fix its exact scope. Reusable per-column register-safety package for the active Lemma 1 image skeleton.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Reusable per-column register-safety package for the active Lemma 1 image skeleton. The package is deliberately conditional on the existing n-bit address hypothesis, so it does not hide the paper parameter-family obligation.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:3503](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspapercolumncontract-registersafety-of-address-lt). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperMatrix" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperMatrix")
*Plain-English reading.* This definition gives the library's named construction or computation for “banded sparse access paper matrix”. Matrix entries for the faithful Lemma 1 'O\_D^BS' paper-image skeleton.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Matrix entries for the faithful Lemma 1 'O\_D^BS' paper-image skeleton. The column 'j' has a candidate '1' entry at 'bandedSparseAccessPaperImage p j.val', which replaces the padded sparse-address register by 'r\_si' and preserves the row register. This declaration is the active 'oneTermRobinGate\_O\_D\_BS' matrix, but it does not prove that the image is in range, injective, unitary, or cleaned up by the dagger.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:3525](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspapermatrix). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperMatrix_eq_image" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperMatrix_eq_image")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper matrix eq image”; the hypotheses and conclusion in the code panel fix its exact scope. The paper-image matrix entry is governed by 'bandedSparseAccessPaperImage'.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The paper-image matrix entry is governed by 'bandedSparseAccessPaperImage'.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:3531](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspapermatrix-eq-image). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperMatrix_imageFin_eq_one" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperMatrix_imageFin_eq_one")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper matrix image fin eq one”; the hypotheses and conclusion in the code panel fix its exact scope. Forward paper-image matrix entry at the finite image column.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Forward paper-image matrix entry at the finite image column. The hypotheses are the same explicit range hypotheses used to construct 'bandedSparseAccessPaperImageFin'. This theorem does not assert that the image function is injective or that the matrix is unitary.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:3544](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspapermatrix-imagefin-eq-one). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperDaggerMatrix" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperDaggerMatrix")
*Plain-English reading.* This definition gives the library's named construction or computation for “banded sparse access paper dagger matrix”. Transpose-style matrix for the faithful Lemma 1 'O\_D^BS' paper-image skeleton.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Transpose-style matrix for the faithful Lemma 1 'O\_D^BS' paper-image skeleton. The entry 'M†\[i,j\]' is '1' exactly when column index 'j' is the forward paper image of row index 'i'. This is only the matrix-level transpose of the current executable image skeleton; the inverse, unitarity, and post-SWAP cleanup claims remain tracked by 'defaultBandedSparseAccessPaperContract p' with 'proved := false'.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:3561](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspaperdaggermatrix). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperDaggerMatrix_eq_image" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperDaggerMatrix_eq_image")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper dagger matrix eq image”; the hypotheses and conclusion in the code panel fix its exact scope. The paper-image dagger matrix is the transpose-style matrix for the image skeleton.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The paper-image dagger matrix is the transpose-style matrix for the image skeleton.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:3567](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspaperdaggermatrix-eq-image). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperDaggerMatrix_imageFin_eq_one" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperDaggerMatrix_imageFin_eq_one")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper dagger matrix image fin eq one”; the hypotheses and conclusion in the code panel fix its exact scope. Transpose-style paper-image matrix entry paired with the finite forward image.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Transpose-style paper-image matrix entry paired with the finite forward image. This is the entry relation needed before an inverse-on-range proof. It does not prove that the transpose-style matrix cleans the ancillas after SWAP.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:3579](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspaperdaggermatrix-imagefin-eq-one). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.robinSparseAmplitudeValue" (lean := "QuantumBlockEncoding.GHL2025.robinSparseAmplitudeValue")
*Plain-English reading.* This definition gives the library's named construction or computation for “robin sparse amplitude value”. Sparse amplitude value: the s-th nonzero stencil coefficient of row i in the Robin derivative matrix, returned as a Coeff value.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Sparse amplitude value: the s-th nonzero stencil coefficient of row i in the Robin derivative matrix, returned as a Coeff value. This is the data layer that both O\_DT^S (sparse amplitude oracle, Lemma 3) and Ry\_boundary (boundary-controlled rotations) need. The column index corresponding to each (s, i) pair is given by 'robinSparseColumnMap'. For the fourth-order central second-derivative stencil: - Bulk rows (K1 ≤ i ≤ K2): 5 entries at offsets \{-2,-1,0,1,2\} - Left boundary row 0: 3 entries with Robin correction (A1\*dx term) - Left boundary row 1: 4 entries with Robin correction (A1\*dx term) - Right boundary row N-2: 4 entries with Robin correction (B1\*dx term) - Right boundary row N-1: 3 entries with Robin correction (B1\*dx term) - Unused sparse indices (s ≥ entry count): Coeff.rat 0 main.tex:822-849, 1081-1083, 1113-1117 -

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:3604](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-robinsparseamplitudevalue). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.robinGlobalSparseAmplitudeValue" (lean := "QuantumBlockEncoding.GHL2025.robinGlobalSparseAmplitudeValue")
*Plain-English reading.* This definition gives the library's named construction or computation for “robin global sparse amplitude value”. Global sparse-slot coefficient source for the one-term Robin table.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Global sparse-slot coefficient source for the one-term Robin table. Unlike 'robinSparseAmplitudeValue', the sparse index is interpreted through the active global slot table used by 'oneTermRobinGlobalSparseAddress'. Thus slot '2' is the zero-offset diagonal slot in every row. Boundary slots that are present in the global sparse register but absent from the Robin row carry coefficient '0'; the slot itself is not deleted. Guseynov-Huang-Liu 2025, Lemma 'Diagonal sparsity', Lemma 'Banded-sparse-access-oracle', the zero-inclusion paragraph before Theorem '1 term robin', and Eq. 'ROBIN clarified', arXiv:2506.20478.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:3661](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-robinglobalsparseamplitudevalue). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.robinGlobalSparseAmplitudeValue_boundarySlot2_row0_n3" (lean := "QuantumBlockEncoding.GHL2025.robinGlobalSparseAmplitudeValue_boundarySlot2_row0_n3")
*Plain-English reading.* Lean checks the proposition indexed as “robin global sparse amplitude value boundary slot 2 row 0 n 3”; the hypotheses and conclusion in the code panel fix its exact scope. Focused boundary regression: global slot '2' is the row-'0' diagonal.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Focused boundary regression: global slot '2' is the row-'0' diagonal.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:3707](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-robinglobalsparseamplitudevalue-boundaryslot2-row0-n3). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.robinGlobalSparseAmplitudeValue_boundarySlot2_differs_rowLocal_n3" (lean := "QuantumBlockEncoding.GHL2025.robinGlobalSparseAmplitudeValue_boundarySlot2_differs_rowLocal_n3")
*Plain-English reading.* Lean checks the proposition indexed as “robin global sparse amplitude value boundary slot 2 differs row local n 3”; the hypotheses and conclusion in the code panel fix its exact scope. The focused global slot is not the old row-local sparse entry.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The focused global slot is not the old row-local sparse entry. This records the contract drift found by the gamma3 boundary packet without promoting any analytic normalizer or block-encoding flag.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:3719](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-robinglobalsparseamplitudevalue-boundaryslot2-differs-rowlocal-n3). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.DerivativeNormalizerNDContract" (lean := "QuantumBlockEncoding.GHL2025.DerivativeNormalizerNDContract")
*Plain-English reading.* This record groups the data and proof fields needed for “derivative normalizer nd contract”. A proposition-valued field is a requirement until a constructor supplies it. Shared Phase-1 contract for every paper route that uses the normalized derivative coefficient 'D\_j^(s) / N\_D'.

*Formal status.* Data contract in the default import surface; proposition-valued fields are obligations, not automatically established facts.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Shared Phase-1 contract for every paper route that uses the normalized derivative coefficient 'D\_j^(s) / N\_D'. Both Lemma 3 'O\_DT^S' and the boundary 'R\_y' angle formulas use the same global sparse-slot coefficient source and the same normalizer symbol 'N\_D'. This record keeps the common analytic gaps in one Lean object: nonzero normalizer, division semantics, coefficient bound, absolute-square semantics, square-root complement, arccos semantics, and two-by-two unitarity. It is a contract only; every obligation is false in Phase 1. Guseynov-Huang-Liu 2025, Lemma 3, Eq. (20), Fig. 1-term Robin, and boundary rotation equations, arXiv:2506.20478.

*Declaration kind.* structure.

Source: [QuantumBlockEncoding/GHL2025.lean:3737](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-derivativenormalizerndcontract). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.derivativeNormalizerNDContract" (lean := "QuantumBlockEncoding.GHL2025.derivativeNormalizerNDContract")
*Plain-English reading.* This definition gives the library's named construction or computation for “derivative normalizer nd contract”. Default shared 'N\_D' normalizer contract for one Robin coefficient.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Default shared 'N\_D' normalizer contract for one Robin coefficient. The normalized coefficient is represented by multiplying the sparse derivative coefficient by the formal symbol 'N\_D\_inv'. This is not a proof that 'N\_D' is nonzero or that a division operation has been interpreted.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:3761](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-derivativenormalizerndcontract). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.derivativeNormalizerNDContract_coefficient" (lean := "QuantumBlockEncoding.GHL2025.derivativeNormalizerNDContract_coefficient")
*Plain-English reading.* Lean checks the proposition indexed as “derivative normalizer nd contract coefficient”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:3808](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-derivativenormalizerndcontract-coefficient). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.derivativeNormalizerNDContract_normalizerND" (lean := "QuantumBlockEncoding.GHL2025.derivativeNormalizerNDContract_normalizerND")
*Plain-English reading.* Lean checks the proposition indexed as “derivative normalizer nd contract normalizer nd”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:3813](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-derivativenormalizerndcontract-normalizernd). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.derivativeNormalizerNDContract_normalizedCoefficient" (lean := "QuantumBlockEncoding.GHL2025.derivativeNormalizerNDContract_normalizedCoefficient")
*Plain-English reading.* Lean checks the proposition indexed as “derivative normalizer nd contract normalized coefficient”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:3818](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-derivativenormalizerndcontract-normalizedcoefficient). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.DerivativeNormalizerNDSourceBound" (lean := "QuantumBlockEncoding.GHL2025.DerivativeNormalizerNDSourceBound")
*Plain-English reading.* This record groups the data and proof fields needed for “derivative normalizer nd source bound”. A proposition-valued field is a requirement until a constructor supplies it. Phase-1 source/bound view for the shared 'N\_D' normalizer contract.

*Formal status.* Data contract in the default import surface; proposition-valued fields are obligations, not automatically established facts.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Phase-1 source/bound view for the shared 'N\_D' normalizer contract. This does not prove the analytic inequality. It only packages the exact coefficient source and the paper normalizer symbol used by the future bound obligation, so 'O\_DT^S' and 'Ry\_boundary' can point to the same fixed interface.

*Declaration kind.* structure.

Source: [QuantumBlockEncoding/GHL2025.lean:3831](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-derivativenormalizerndsourcebound). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.derivativeNormalizerNDSourceBound" (lean := "QuantumBlockEncoding.GHL2025.derivativeNormalizerNDSourceBound")
*Plain-English reading.* This definition gives the library's named construction or computation for “derivative normalizer nd source bound”. Default source/bound interface for the paper statement '|D\_j^(s)| <= N\_D'.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Default source/bound interface for the paper statement '|D\_j^(s)| <= N\_D'. The coefficient and obligation are reused from 'derivativeNormalizerNDContract'; the obligation remains false until the coefficient semantics and analytic normalizer bound are formalized.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:3849](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-derivativenormalizerndsourcebound). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.derivativeNormalizerNDSourceBound_sourceCoefficient" (lean := "QuantumBlockEncoding.GHL2025.derivativeNormalizerNDSourceBound_sourceCoefficient")
*Plain-English reading.* Lean checks the proposition indexed as “derivative normalizer nd source bound source coefficient”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:3863](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-derivativenormalizerndsourcebound-sourcecoefficient). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.derivativeNormalizerNDSourceBound_normalizerND" (lean := "QuantumBlockEncoding.GHL2025.derivativeNormalizerNDSourceBound_normalizerND")
*Plain-English reading.* Lean checks the proposition indexed as “derivative normalizer nd source bound normalizer nd”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:3868](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-derivativenormalizerndsourcebound-normalizernd). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.derivativeNormalizerNDSourceBound_boundFormula" (lean := "QuantumBlockEncoding.GHL2025.derivativeNormalizerNDSourceBound_boundFormula")
*Plain-English reading.* Lean checks the proposition indexed as “derivative normalizer nd source bound bound formula”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:3873](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-derivativenormalizerndsourcebound-boundformula). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.derivativeNormalizerNDSourceBound_coefficientBound" (lean := "QuantumBlockEncoding.GHL2025.derivativeNormalizerNDSourceBound_coefficientBound")
*Plain-English reading.* Lean checks the proposition indexed as “derivative normalizer nd source bound coefficient bound”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:3878](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-derivativenormalizerndsourcebound-coefficientbound). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.derivativeNormalizerNDSourceBound_coefficientBound_false" (lean := "QuantumBlockEncoding.GHL2025.derivativeNormalizerNDSourceBound_coefficientBound_false")
*Plain-English reading.* Lean checks the proposition indexed as “derivative normalizer nd source bound coefficient bound false”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:3883](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-derivativenormalizerndsourcebound-coefficientbound-false). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.indicatorOracleMatrix" (lean := "QuantumBlockEncoding.GHL2025.indicatorOracleMatrix")
*Plain-English reading.* This definition gives the library's named construction or computation for “indicator oracle matrix”. Honest U\_indic matrix: controlled-X on the indicator qubit, conditioned on the system register being in the bulk window \[K1, K2\].

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Honest U\_indic matrix: controlled-X on the indicator qubit, conditioned on the system register being in the bulk window \[K1, K2\]. For each basis state |j⟩: - Extract systemVal = bits \[1, 1+n) of j - If K1 ≤ systemVal ≤ K2 (bulk row): flip indicator bit - Otherwise (boundary row): identity main.tex:1088-1099 -

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:3896](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-indicatororaclematrix). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.oneTermRobinGate_U_indic" (lean := "QuantumBlockEncoding.GHL2025.oneTermRobinGate_U_indic")
*Plain-English reading.* This definition gives the library's named construction or computation for “one term robin gate u indic”. Gate matrix for U\_indic using the honest permutation matrix.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Gate matrix for U\_indic using the honest permutation matrix. Controlled-X on indicator bit at position 1+2n, conditioned on bulk membership. Unitarity proved: indicatorOracleMatrix\_is\_permutation shows each row and column has exactly one entry equal to 1, so the matrix is a permutation matrix (hence unitary). main.tex:1088-1099 -

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:3914](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-onetermrobingate-u-indic). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.oneTermRobinGate_U_indic_dagger" (lean := "QuantumBlockEncoding.GHL2025.oneTermRobinGate_U_indic_dagger")
*Plain-English reading.* This definition gives the library's named construction or computation for “one term robin gate u indic dagger”. Theorem-facing Hermitian-conjugate slot for 'U\_indic'.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Theorem-facing Hermitian-conjugate slot for 'U\_indic'. The indicator permutation is self-inverse, so its dagger is represented by the same matrix. This gate record exists to keep the Fig. 1-term Robin transcript faithful; the active backend product is still the seven-gate list unless a separate theorem rewires it.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:3931](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-onetermrobingate-u-indic-dagger). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.oneTermRobinGate_U_indic_dagger_matrix_eq" (lean := "QuantumBlockEncoding.GHL2025.oneTermRobinGate_U_indic_dagger_matrix_eq")
*Plain-English reading.* Lean checks the proposition indexed as “one term robin gate u indic dagger matrix eq”; the hypotheses and conclusion in the code panel fix its exact scope. The theorem-facing 'U\_indic^dagger' slot has the same matrix as 'U\_indic'.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The theorem-facing 'U\_indic^dagger' slot has the same matrix as 'U\_indic'. This is only a transcript bridge. It does not insert the dagger slot into the active seven-gate backend product.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:3947](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-onetermrobingate-u-indic-dagger-matrix-eq). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.sparseAmplitudeOracleDTMatrix" (lean := "QuantumBlockEncoding.GHL2025.sparseAmplitudeOracleDTMatrix")
*Plain-English reading.* This definition gives the library's named construction or computation for “sparse amplitude oracle dt matrix”. Honest O\_DT^S diagonal matrix: encodes the sparse amplitude data on the diagonal for bulk rows (indicator=1) and acts as identity for boundary rows (indicator=0).

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Honest O\_DT^S diagonal matrix: encodes the sparse amplitude data on the diagonal for bulk rows (indicator=1) and acts as identity for boundary rows (indicator=0). For each compound basis state |j⟩: - If indicator bit = 0 (boundary row): diagonal entry = Coeff.rat 1 (identity) - If indicator bit = 1 (bulk row): diagonal entry = robinSparseAmplitudeValue(n, s, i) - Off-diagonal entries are zero. NOTE: The paper's actual O\_\{D^T\}^S (Lemma 3, main.tex:822-849) is a controlled rotation on the ancilla qubit, not a diagonal matrix. This diagonal encoding exercises the amplitude data pathway; the rotation structure is a proof obligation. main.tex:822-849 -

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:3965](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-sparseamplitudeoracledtmatrix). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.SparseAmplitudeOracleDTPaperRegisters" (lean := "QuantumBlockEncoding.GHL2025.SparseAmplitudeOracleDTPaperRegisters")
*Plain-English reading.* This record groups the data and proof fields needed for “sparse amplitude oracle dt paper registers”. A proposition-valued field is a requirement until a constructor supplies it. Register values used by the faithful Lemma 3 'O\_DT^S' contract.

*Formal status.* Data contract in the default import surface; proposition-valued fields are obligations, not automatically established facts.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Register values used by the faithful Lemma 3 'O\_DT^S' contract. The compound-index convention stores the rotation ancilla in bit 0, the system row in bits '\[1, 1+n)', the padded sparse register in bits '\[1+n, 1+2n)', and the indicator bit at 'robinIndicatorBitPosition p'. The 'nonAncillaValue' field is 'j >>> 1'; preserving it means that only the ancilla bit may change. Guseynov-Huang-Liu 2025, Lemma 3, arXiv:2506.20478.

*Declaration kind.* structure.

Source: [QuantumBlockEncoding/GHL2025.lean:3994](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-sparseamplitudeoracledtpaperregisters). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.sparseAmplitudeOracleDTPaperRegisters" (lean := "QuantumBlockEncoding.GHL2025.sparseAmplitudeOracleDTPaperRegisters")
*Plain-English reading.* This definition gives the library's named construction or computation for “sparse amplitude oracle dt paper registers”. Extract the Lemma 3 sparse-amplitude oracle registers from a compound basis index.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Extract the Lemma 3 sparse-amplitude oracle registers from a compound basis index. This is a source-contract skeleton for the paper's controlled rotation on the ancilla qubit; it leaves the legacy diagonal data helper available.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:4007](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-sparseamplitudeoracledtpaperregisters). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.sparseAmplitudeOracleDTCosHalf" (lean := "QuantumBlockEncoding.GHL2025.sparseAmplitudeOracleDTCosHalf")
*Plain-English reading.* This definition gives the library's named construction or computation for “sparse amplitude oracle dt cos half”. Symbolic cosine half-angle entry for the Lemma 3 O\_DT^S rotation.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Symbolic cosine half-angle entry for the Lemma 3 O\_DT^S rotation.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:4025](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-sparseamplitudeoracledtcoshalf). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.sparseAmplitudeOracleDTSinHalf" (lean := "QuantumBlockEncoding.GHL2025.sparseAmplitudeOracleDTSinHalf")
*Plain-English reading.* This definition gives the library's named construction or computation for “sparse amplitude oracle dt sin half”. Symbolic sine half-angle entry for the Lemma 3 O\_DT^S rotation.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Symbolic sine half-angle entry for the Lemma 3 O\_DT^S rotation.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:4029](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-sparseamplitudeoracledtsinhalf). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.sparseAmplitudeOracleDTCoefficientNormalizerObligation" (lean := "QuantumBlockEncoding.GHL2025.sparseAmplitudeOracleDTCoefficientNormalizerObligation")
*Plain-English reading.* This definition gives the library's named construction or computation for “sparse amplitude oracle dt coefficient normalizer obligation”. Explicit unresolved source obligation for the symbolic entries in the Lemma 3 'O\_DT^S' rotation skeleton.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Explicit unresolved source obligation for the symbolic entries in the Lemma 3 'O\_DT^S' rotation skeleton. Equation (20) of Guseynov-Huang-Liu 2025 maps '|0>|s>' to an amplitude whose '|0>' component is 'D^(s) / N\_D' and whose complementary component is the square-root normalizer term. The Lean symbols 'sparseAmplitudeOracleDTCosHalf row sparse' and 'sparseAmplitudeOracleDTSinHalf row sparse' are only placeholders until this coefficient/normalizer relation and the corresponding two-by-two unitarity identity are formalized.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:4044](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-sparseamplitudeoracledtcoefficientnormalizerobligation). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.SparseAmplitudeOracleDTCoefficientNormalizerContract" (lean := "QuantumBlockEncoding.GHL2025.SparseAmplitudeOracleDTCoefficientNormalizerContract")
*Plain-English reading.* This record groups the data and proof fields needed for “sparse amplitude oracle dt coefficient normalizer contract”. A proposition-valued field is a requirement until a constructor supplies it. Typed Eq.

*Formal status.* Data contract in the default import surface; proposition-valued fields are obligations, not automatically established facts.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Typed Eq. (20) coefficient-normalizer contract for one 'O\_DT^S' rotation block. This binds the symbolic rotation entries to the concrete Robin sparse coefficient data and the paper's 'N\_D' normalizer without proving the analytic identities. The three obligations stay false until Lean has a coefficient language with the required division, square-root, absolute-value, and unitarity facts.

*Declaration kind.* structure.

Source: [QuantumBlockEncoding/GHL2025.lean:4059](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-sparseamplitudeoracledtcoefficientnormalizercontract). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.sparseAmplitudeOracleDTCoefficientNormalizerContract" (lean := "QuantumBlockEncoding.GHL2025.sparseAmplitudeOracleDTCoefficientNormalizerContract")
*Plain-English reading.* This definition gives the library's named construction or computation for “sparse amplitude oracle dt coefficient normalizer contract”. Default Eq.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Default Eq. (20) coefficient-normalizer contract for a Robin row and global sparse slot. The coefficient is 'robinGlobalSparseAmplitudeValue p.n sparse row'; the rotation entries are the symbols used by 'sparseAmplitudeOracleDTRotationMatrix'.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:4079](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-sparseamplitudeoracledtcoefficientnormalizercontract). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.sparseAmplitudeOracleDTNormalizedCoefficient" (lean := "QuantumBlockEncoding.GHL2025.sparseAmplitudeOracleDTNormalizedCoefficient")
*Plain-English reading.* This definition gives the library's named construction or computation for “sparse amplitude oracle dt normalized coefficient”. Symbolic stand-in for the Lemma 3 normalized coefficient 'D\_j^(s) / N\_D'.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Symbolic stand-in for the Lemma 3 normalized coefficient 'D\_j^(s) / N\_D'. The factor 'Coeff.symbol "N\_D\_inv"' records the intended division by 'N\_D'. It is not a proof that 'N\_D' is nonzero or that the coefficient lies in the unit interval required by Eq. (20); those remain separate obligations.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:4114](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-sparseamplitudeoracledtnormalizedcoefficient). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.SparseAmplitudeOracleDTCoefficientNormalizerProofRoute" (lean := "QuantumBlockEncoding.GHL2025.SparseAmplitudeOracleDTCoefficientNormalizerProofRoute")
*Plain-English reading.* This record groups the data and proof fields needed for “sparse amplitude oracle dt coefficient normalizer proof route”. A proposition-valued field is a requirement until a constructor supplies it. Refined proof route for the 'odts\_coeff\_normalizer' block.

*Formal status.* Data contract in the default import surface; proposition-valued fields are obligations, not automatically established facts.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Refined proof route for the 'odts\_coeff\_normalizer' block. This record separates the typed Eq. (20) data from the analytic obligations: division by 'N\_D', the paper's normalizer bound, the absolute-square term, the complementary square root, and the two-by-two unitarity identity. All proof obligations stay false in Phase 1.

*Declaration kind.* structure.

Source: [QuantumBlockEncoding/GHL2025.lean:4126](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-sparseamplitudeoracledtcoefficientnormalizerproofroute). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.sparseAmplitudeOracleDTCoefficientNormalizerProofRoute" (lean := "QuantumBlockEncoding.GHL2025.sparseAmplitudeOracleDTCoefficientNormalizerProofRoute")
*Plain-English reading.* This definition gives the library's named construction or computation for “sparse amplitude oracle dt coefficient normalizer proof route”. Default refined proof route for one 'O\_DT^S' Eq.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Default refined proof route for one 'O\_DT^S' Eq. (20) coefficient-normalizer block. The route keeps the construction fixed to the paper's controlled rotation and does not promote the gate-level unitarity claim.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:4150](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-sparseamplitudeoracledtcoefficientnormalizerproofroute). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.sparseAmplitudeOracleDTCoefficientNormalizerProofRoute_coefficient" (lean := "QuantumBlockEncoding.GHL2025.sparseAmplitudeOracleDTCoefficientNormalizerProofRoute_coefficient")
*Plain-English reading.* Lean checks the proposition indexed as “sparse amplitude oracle dt coefficient normalizer proof route coefficient”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:4174](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-sparseamplitudeoracledtcoefficientnormalizerproofroute-coefficient). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.sparseAmplitudeOracleDTCoefficientNormalizerProofRoute_normalizerND" (lean := "QuantumBlockEncoding.GHL2025.sparseAmplitudeOracleDTCoefficientNormalizerProofRoute_normalizerND")
*Plain-English reading.* Lean checks the proposition indexed as “sparse amplitude oracle dt coefficient normalizer proof route normalizer nd”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:4179](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-sparseamplitudeoracledtcoefficientnormalizerproofroute-normalizernd). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.sparseAmplitudeOracleDTCoefficientNormalizerProofRoute_normalizedCoefficient" (lean := "QuantumBlockEncoding.GHL2025.sparseAmplitudeOracleDTCoefficientNormalizerProofRoute_normalizedCoefficient")
*Plain-English reading.* Lean checks the proposition indexed as “sparse amplitude oracle dt coefficient normalizer proof route normalized coefficient”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:4184](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-sparseamplitudeoracledtcoefficientnormalizerproofroute-normalizedcoefficient). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.sparseAmplitudeOracleDTCoefficientNormalizerProofRoute_sharedND" (lean := "QuantumBlockEncoding.GHL2025.sparseAmplitudeOracleDTCoefficientNormalizerProofRoute_sharedND")
*Plain-English reading.* Lean checks the proposition indexed as “sparse amplitude oracle dt coefficient normalizer proof route shared nd”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:4189](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-sparseamplitudeoracledtcoefficientnormalizerproofroute-sharednd). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.sparseAmplitudeOracleDTCoefficientNormalizerProofRoute_sourceBound" (lean := "QuantumBlockEncoding.GHL2025.sparseAmplitudeOracleDTCoefficientNormalizerProofRoute_sourceBound")
*Plain-English reading.* Lean checks the proposition indexed as “sparse amplitude oracle dt coefficient normalizer proof route source bound”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:4201](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-sparseamplitudeoracledtcoefficientnormalizerproofroute-sourcebound). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.sparseAmplitudeOracleDTCoefficientNormalizerProofRoute_ketZeroEntry" (lean := "QuantumBlockEncoding.GHL2025.sparseAmplitudeOracleDTCoefficientNormalizerProofRoute_ketZeroEntry")
*Plain-English reading.* Lean checks the proposition indexed as “sparse amplitude oracle dt coefficient normalizer proof route ket zero entry”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:4211](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-sparseamplitudeoracledtcoefficientnormalizerproofroute-ketzeroentry). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.sparseAmplitudeOracleDTCoefficientNormalizerProofRoute_ketOneEntry" (lean := "QuantumBlockEncoding.GHL2025.sparseAmplitudeOracleDTCoefficientNormalizerProofRoute_ketOneEntry")
*Plain-English reading.* Lean checks the proposition indexed as “sparse amplitude oracle dt coefficient normalizer proof route ket one entry”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:4216](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-sparseamplitudeoracledtcoefficientnormalizerproofroute-ketoneentry). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.sparseAmplitudeOracleDTRotationMatrix" (lean := "QuantumBlockEncoding.GHL2025.sparseAmplitudeOracleDTRotationMatrix")
*Plain-English reading.* This definition gives the library's named construction or computation for “sparse amplitude oracle dt rotation matrix”. Faithful Lemma 3 controlled-rotation skeleton for 'O\_DT^S'.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Faithful Lemma 3 controlled-rotation skeleton for 'O\_DT^S'. For columns whose indicator bit is 0, the matrix acts as identity. For columns whose indicator bit is 1, it preserves every non-ancilla bit and applies a symbolic two-by-two rotation on ancilla bit 0. The symbols are indexed by the extracted row and sparse-index values; their connection to the Eq. (20) amplitudes determined by 'robinGlobalSparseAmplitudeValue p.n sparse row / N\_D' remains the coefficient-normalizer proof obligation. Guseynov-Huang-Liu 2025, Lemma 3, arXiv:2506.20478.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:4231](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-sparseamplitudeoracledtrotationmatrix). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.oneTermRobinGate_O_DT_S" (lean := "QuantumBlockEncoding.GHL2025.oneTermRobinGate_O_DT_S")
*Plain-English reading.* This definition gives the library's named construction or computation for “one term robin gate o dt s”. Gate matrix for O\_DT^S using the faithful controlled-rotation skeleton.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Gate matrix for O\_DT^S using the faithful controlled-rotation skeleton. The legacy diagonal helper 'sparseAmplitudeOracleDTMatrix' remains available as the coefficient-data path, but the active gate now preserves all non-ancilla bits and rotates bit 0 when the indicator bit is 1. Unitarity and the normalizer-bound trigonometric identity are not yet formally proved. main.tex:822-849 -

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:4256](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-onetermrobingate-o-dt-s). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.BoundaryRotationPaperRegisters" (lean := "QuantumBlockEncoding.GHL2025.BoundaryRotationPaperRegisters")
*Plain-English reading.* This record groups the data and proof fields needed for “boundary rotation paper registers”. A proposition-valued field is a requirement until a constructor supplies it. Register values used by the faithful 'Ry\_boundary' source contract.

*Formal status.* Data contract in the default import surface; proposition-valued fields are obligations, not automatically established facts.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Register values used by the faithful 'Ry\_boundary' source contract. The compound-index convention is the same one used by the active matrix: ancilla bit 0 is the rotated qubit, bits '\[1, 1+n)' contain the Robin row, the high part of the O\_D register contains sparse index 's', and the indicator bit determines whether the boundary rotation is active. The 'nonAncillaValue' field is preserved by the controlled rotation. Guseynov-Huang-Liu 2025, Fig. 1-term Robin and Eq. angles for Ry, arXiv:2506.20478.

*Declaration kind.* structure.

Source: [QuantumBlockEncoding/GHL2025.lean:4275](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-boundaryrotationpaperregisters). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.boundaryRotationPaperRegisters" (lean := "QuantumBlockEncoding.GHL2025.boundaryRotationPaperRegisters")
*Plain-English reading.* This definition gives the library's named construction or computation for “boundary rotation paper registers”. Extract the 'Ry\_boundary' register fields from a compound basis index.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Extract the 'Ry\_boundary' register fields from a compound basis index. This is a source-contract skeleton; it does not prove the angle identities or unitarity of the symbolic rotation block.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:4288](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-boundaryrotationpaperregisters). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.boundaryRotationCosHalf" (lean := "QuantumBlockEncoding.GHL2025.boundaryRotationCosHalf")
*Plain-English reading.* This definition gives the library's named construction or computation for “boundary rotation cos half”. Symbolic cosine half-angle entry for the 'Ry\_boundary' rotation.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Symbolic cosine half-angle entry for the 'Ry\_boundary' rotation.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:4306](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-boundaryrotationcoshalf). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.boundaryRotationSinHalf" (lean := "QuantumBlockEncoding.GHL2025.boundaryRotationSinHalf")
*Plain-English reading.* This definition gives the library's named construction or computation for “boundary rotation sin half”. Symbolic sine half-angle entry for the 'Ry\_boundary' rotation.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Symbolic sine half-angle entry for the 'Ry\_boundary' rotation.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:4310](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-boundaryrotationsinhalf). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.boundaryRotationAngleNormalizerObligation" (lean := "QuantumBlockEncoding.GHL2025.boundaryRotationAngleNormalizerObligation")
*Plain-English reading.* This definition gives the library's named construction or computation for “boundary rotation angle normalizer obligation”. Explicit unresolved source obligation for the 'Ry\_boundary' angle/normalizer relation.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Explicit unresolved source obligation for the 'Ry\_boundary' angle/normalizer relation. The paper uses angles 'theta\_j^s = arccos(D\_j^(s) / N\_D)' for boundary rows. The Lean symbols 'boundaryRotationCosHalf row sparse' and 'boundaryRotationSinHalf row sparse' are placeholders until the half-angle identities and the two-by-two unitarity relation are formalized.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:4322](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-boundaryrotationanglenormalizerobligation). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.BoundaryRotationAngleNormalizerContract" (lean := "QuantumBlockEncoding.GHL2025.BoundaryRotationAngleNormalizerContract")
*Plain-English reading.* This record groups the data and proof fields needed for “boundary rotation angle normalizer contract”. A proposition-valued field is a requirement until a constructor supplies it. Typed angle/normalizer contract for one 'Ry\_boundary' rotation block.

*Formal status.* Data contract in the default import surface; proposition-valued fields are obligations, not automatically established facts.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Typed angle/normalizer contract for one 'Ry\_boundary' rotation block. This binds the symbolic half-angle entries used by 'boundaryRotationMatrix' to the Robin sparse coefficient source and the paper normalizer 'N\_D'. It records the exact obligations without asserting the arccos relation, half-angle formulas, control condition, or unitarity.

*Declaration kind.* structure.

Source: [QuantumBlockEncoding/GHL2025.lean:4336](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-boundaryrotationanglenormalizercontract). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.boundaryRotationAngleNormalizerContract" (lean := "QuantumBlockEncoding.GHL2025.boundaryRotationAngleNormalizerContract")
*Plain-English reading.* This definition gives the library's named construction or computation for “boundary rotation angle normalizer contract”. Default 'Ry\_boundary' angle/normalizer contract for one Robin row and global sparse slot.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Default 'Ry\_boundary' angle/normalizer contract for one Robin row and global sparse slot. The coefficient is 'robinGlobalSparseAmplitudeValue p.n sparse row'; the rotation entries are the symbols used by 'boundaryRotationMatrix'.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:4359](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-boundaryrotationanglenormalizercontract). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.boundaryRotationAngleNormalizerContract_coefficient" (lean := "QuantumBlockEncoding.GHL2025.boundaryRotationAngleNormalizerContract_coefficient")
*Plain-English reading.* Lean checks the proposition indexed as “boundary rotation angle normalizer contract coefficient”; the hypotheses and conclusion in the code panel fix its exact scope. The coefficient source of the 'Ry\_boundary' angle contract is definitionally the Robin global sparse-slot amplitude data layer.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The coefficient source of the 'Ry\_boundary' angle contract is definitionally the Robin global sparse-slot amplitude data layer.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:4402](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-boundaryrotationanglenormalizercontract-coefficient). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.boundaryRotationNormalizedCoefficient" (lean := "QuantumBlockEncoding.GHL2025.boundaryRotationNormalizedCoefficient")
*Plain-English reading.* This definition gives the library's named construction or computation for “boundary rotation normalized coefficient”. Symbolic stand-in for the paper argument 'D\_j^(s) / N\_D'.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Symbolic stand-in for the paper argument 'D\_j^(s) / N\_D'. The factor 'Coeff.symbol "N\_D\_inv"' is not a proof that 'N\_D' is invertible. It only records the intended normalized coefficient while the required division semantics and nonzero normalizer condition remain explicit obligations.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:4414](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-boundaryrotationnormalizedcoefficient). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.BoundaryRotationAngleNormalizerProofRoute" (lean := "QuantumBlockEncoding.GHL2025.BoundaryRotationAngleNormalizerProofRoute")
*Plain-English reading.* This record groups the data and proof fields needed for “boundary rotation angle normalizer proof route”. A proposition-valued field is a requirement until a constructor supplies it. Refined proof route for the 'ryb\_angle\_normalizer' block.

*Formal status.* Data contract in the default import surface; proposition-valued fields are obligations, not automatically established facts.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Refined proof route for the 'ryb\_angle\_normalizer' block. This record separates the typed data already present in 'BoundaryRotationAngleNormalizerContract' from the missing analytic semantics: division by 'N\_D', real arccos, square roots, the paper's normalizer bound, and the resulting two-by-two unitarity identity. All proof obligations stay false in Phase 1.

*Declaration kind.* structure.

Source: [QuantumBlockEncoding/GHL2025.lean:4427](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-boundaryrotationanglenormalizerproofroute). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.boundaryRotationAngleNormalizerProofRoute" (lean := "QuantumBlockEncoding.GHL2025.boundaryRotationAngleNormalizerProofRoute")
*Plain-English reading.* This definition gives the library's named construction or computation for “boundary rotation angle normalizer proof route”. Default refined proof route for one 'Ry\_boundary' angle-normalizer block.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Default refined proof route for one 'Ry\_boundary' angle-normalizer block. The route keeps the construction fixed to the paper formula 'theta\_j^s = arccos(D\_j^(s) / N\_D)'. It does not introduce a replacement angle or promote the gate-level unitarity claim.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:4454](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-boundaryrotationanglenormalizerproofroute). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.boundaryRotationAngleNormalizerProofRoute_coefficient" (lean := "QuantumBlockEncoding.GHL2025.boundaryRotationAngleNormalizerProofRoute_coefficient")
*Plain-English reading.* Lean checks the proposition indexed as “boundary rotation angle normalizer proof route coefficient”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:4483](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-boundaryrotationanglenormalizerproofroute-coefficient). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.boundaryRotationAngleNormalizerProofRoute_arccosArgument" (lean := "QuantumBlockEncoding.GHL2025.boundaryRotationAngleNormalizerProofRoute_arccosArgument")
*Plain-English reading.* Lean checks the proposition indexed as “boundary rotation angle normalizer proof route arccos argument”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:4488](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-boundaryrotationanglenormalizerproofroute-arccosargument). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.boundaryRotationAngleNormalizerProofRoute_sharedND" (lean := "QuantumBlockEncoding.GHL2025.boundaryRotationAngleNormalizerProofRoute_sharedND")
*Plain-English reading.* Lean checks the proposition indexed as “boundary rotation angle normalizer proof route shared nd”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:4493](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-boundaryrotationanglenormalizerproofroute-sharednd). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.boundaryRotationAngleNormalizerProofRoute_sourceBound" (lean := "QuantumBlockEncoding.GHL2025.boundaryRotationAngleNormalizerProofRoute_sourceBound")
*Plain-English reading.* Lean checks the proposition indexed as “boundary rotation angle normalizer proof route source bound”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:4503](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-boundaryrotationanglenormalizerproofroute-sourcebound). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.derivativeNormalizerNDSourceBound_sharedRoutes" (lean := "QuantumBlockEncoding.GHL2025.derivativeNormalizerNDSourceBound_sharedRoutes")
*Plain-English reading.* Lean checks the proposition indexed as “derivative normalizer nd source bound shared routes”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:4513](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-derivativenormalizerndsourcebound-sharedroutes). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.robinGlobalSparseAmplitudeValue_sharedNormalizerRoutes" (lean := "QuantumBlockEncoding.GHL2025.robinGlobalSparseAmplitudeValue_sharedNormalizerRoutes")
*Plain-English reading.* Lean checks the proposition indexed as “robin global sparse amplitude value shared normalizer routes”; the hypotheses and conclusion in the code panel fix its exact scope. Bridge showing that the shared 'N\_D' route is now sourced from the active global sparse-slot coefficient table.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Bridge showing that the shared 'N\_D' route is now sourced from the active global sparse-slot coefficient table. This only wires coefficient data through the existing contracts. It does not prove the analytic division, arccos, half-angle, or unitarity obligations.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:4530](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-robinglobalsparseamplitudevalue-sharednormalizerroutes). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.boundaryRotationMatrix" (lean := "QuantumBlockEncoding.GHL2025.boundaryRotationMatrix")
*Plain-English reading.* This definition gives the library's named construction or computation for “boundary rotation matrix”. Honest Ry\_boundary matrix: controlled R\_y rotation on the ancilla qubit (bit 0), conditioned on the indicator bit being 0 (boundary row).

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Honest Ry\_boundary matrix: controlled R\_y rotation on the ancilla qubit (bit 0), conditioned on the indicator bit being 0 (boundary row). For bulk rows (indicator=1): acts as identity (no rotation). For boundary rows (indicator=0): applies R\_y(θ\_j^s) on the ancilla qubit, where θ\_j^s = arccos(D\_j^(s) / N\_D) (main.tex:1115-1120, Eq. angles for Ry). The R\_y(θ) matrix on the ancilla qubit: M(|0⟩, |0⟩) = cos(θ/2), M(|1⟩, |0⟩) = sin(θ/2) M(|0⟩, |1⟩) = -sin(θ/2), M(|1⟩, |1⟩) = cos(θ/2) Rotation entries are symbolic since the exact trigonometric values involve square roots: cos(θ/2) = √((1 + D/N\_D)/2), sin(θ/2) = √((1 - D/N\_D)/2). main.tex:1115-1120 -

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:4557](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-boundaryrotationmatrix). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.oneTermRobinGate_Ry_boundary" (lean := "QuantumBlockEncoding.GHL2025.oneTermRobinGate_Ry_boundary")
*Plain-English reading.* This definition gives the library's named construction or computation for “one term robin gate ry boundary”. Gate matrix for Ry\_boundary using the honest controlled rotation matrix.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Gate matrix for Ry\_boundary using the honest controlled rotation matrix. R\_y rotation on the ancilla qubit for boundary rows (indicator=0); identity for bulk rows (indicator=1). Unitarity not yet formally proved. main.tex:1115-1120 -

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:4592](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-onetermrobingate-ry-boundary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.derivativeNormalizerNDSharedRoute_flags_false" (lean := "QuantumBlockEncoding.GHL2025.derivativeNormalizerNDSharedRoute_flags_false")
*Plain-English reading.* Lean checks the proposition indexed as “derivative normalizer nd shared route flags false”; the hypotheses and conclusion in the code panel fix its exact scope. Guard for the shared 'N\_D' Phase-1 route.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Guard for the shared 'N\_D' Phase-1 route. The source-bound bridges only synchronize the two proof routes. This theorem records that the analytic obligations and the two affected gate unitarity flags still have not been promoted. Guseynov-Huang-Liu 2025, Lemma 3, Eq. (20), and Eq. angles for Ry, arXiv:2506.20478.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:4610](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-derivativenormalizerndsharedroute-flags-false). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.derivativeNormalizerNDSharedRoute_sourceBoundAndFlags" (lean := "QuantumBlockEncoding.GHL2025.derivativeNormalizerNDSharedRoute_sourceBoundAndFlags")
*Plain-English reading.* Lean checks the proposition indexed as “derivative normalizer nd shared route source bound and flags”; the hypotheses and conclusion in the code panel fix its exact scope. Combined Phase-1 guard for the shared 'N\_D' route.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Combined Phase-1 guard for the shared 'N\_D' route. This packages the source-bound bridges for 'O\_DT^S' and 'Ry\_boundary' together with the current false-flag state. It is bookkeeping only: the analytic division, bound, square-root, arccos, half-angle, and unitarity obligations are still unproved.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:4642](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-derivativenormalizerndsharedroute-sourceboundandflags). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.bandedSparseAccessMatrix" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessMatrix")
*Plain-English reading.* This definition gives the library's named construction or computation for “banded sparse access matrix”. Interim O\_D^BS column-map helper, not the faithful Lemma 1 paper oracle.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Interim O\_D^BS column-map helper, not the faithful Lemma 1 paper oracle. It maps |s⟩|i⟩ → |s⟩|col(s,i)⟩ by replacing the system register bits. Bits outside the system register are preserved. The paper contract |0>^(n-l)|s>^l|i>^n -> |r\_si>^n|i>^n is recorded separately in 'defaultBandedSparseAccessPaperContract'; do not use this helper as the unitarity or block-extraction target for the paper oracle. main.tex:784-801 -

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:4695](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccessmatrix). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.oneTermRobinGate_O_D_BS" (lean := "QuantumBlockEncoding.GHL2025.oneTermRobinGate_O_D_BS")
*Plain-English reading.* This definition gives the library's named construction or computation for “one term robin gate o d bs”. Gate record for the faithful Lemma 1 O\_D^BS paper-image matrix skeleton.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Gate record for the faithful Lemma 1 O\_D^BS paper-image matrix skeleton. The matrix uses 'bandedSparseAccessPaperMatrix', which preserves the row register and writes 'r\_si' into the padded sparse-address register. Unitarity, forward correctness, and block extraction remain unproved obligations. Guseynov-Huang-Liu 2025, Lemma 1, arXiv:2506.20478.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:4718](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-onetermrobingate-o-d-bs). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.oneTermRobinGate_O_D_BS_imageFin_eq_one" (lean := "QuantumBlockEncoding.GHL2025.oneTermRobinGate_O_D_BS_imageFin_eq_one")
*Plain-English reading.* Lean checks the proposition indexed as “one term robin gate o d bs image fin eq one”; the hypotheses and conclusion in the code panel fix its exact scope. Active forward 'O\_D^BS' gate entry at the finite paper image.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Active forward 'O\_D^BS' gate entry at the finite paper image.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:4728](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-onetermrobingate-o-d-bs-imagefin-eq-one). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.oneTermRobinGate_O_D_BS_contractDrift_column8_n3" (lean := "QuantumBlockEncoding.GHL2025.oneTermRobinGate_O_D_BS_contractDrift_column8_n3")
*Plain-English reading.* Lean checks the proposition indexed as “one term robin gate o d bs contract drift column 8 n 3”; the hypotheses and conclusion in the code panel fix its exact scope. Concrete contract-drift guard separating the active Lemma 1 paper-image matrix from the legacy sparse-column helper.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Concrete contract-drift guard separating the active Lemma 1 paper-image matrix from the legacy sparse-column helper. For the one-term parameters 'n = 3', 'kappa = 7', source column '8' is sent by the paper-image skeleton to row '40'. The active 'O\_D^BS' gate therefore has entry '(40, 8) = 1' and no entry at '(4, 8)', while the legacy helper still has its historical row-'4' entry. This is only a regression guard; it does not promote unitarity, cleanup, or block extraction.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:4748](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-onetermrobingate-o-d-bs-contractdrift-column8-n3). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.oneTermRobinGate_O_D_BS_boundaryUnusedSparseCollision_n3" (lean := "QuantumBlockEncoding.GHL2025.oneTermRobinGate_O_D_BS_boundaryUnusedSparseCollision_n3")
*Plain-English reading.* Lean checks the proposition indexed as “one term robin gate o d bs boundary unused sparse collision n 3”; the hypotheses and conclusion in the code panel fix its exact scope. Concrete rejected-model collision for the old row-dependent 'O\_D^BS' address.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Concrete rejected-model collision for the old row-dependent 'O\_D^BS' address. For the one-term parameters 'n = 3', 'kappa = 7', boundary row '0' has only three nonzero Robin stencil entries. The old row-dependent address folded sparse index '3' back to the row address, colliding with sparse index '0'. The active global-slot paper image separates these columns; this theorem is retained only as regression memory for the rejected address model.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:4770](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-onetermrobingate-o-d-bs-boundaryunusedsparsecollision-n3). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.oneTermRobinGate_O_D_BS_globalSparseBoundaryNoCollision_n3" (lean := "QuantumBlockEncoding.GHL2025.oneTermRobinGate_O_D_BS_globalSparseBoundaryNoCollision_n3")
*Plain-English reading.* Lean checks the proposition indexed as “one term robin gate o d bs global sparse boundary no collision n 3”; the hypotheses and conclusion in the code panel fix its exact scope. Concrete regression that the corrected active global-slot image separates the old boundary unused-sparse collision columns.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Concrete regression that the corrected active global-slot image separates the old boundary unused-sparse collision columns. No semantic proof flag is promoted: this only checks the active image entries for the two concrete clean columns.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:4796](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-onetermrobingate-o-d-bs-globalsparseboundarynocollision-n3). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperGlobalSlotSource_boundaryColumns_n3" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperGlobalSlotSource_boundaryColumns_n3")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper global slot source boundary columns n 3”; the hypotheses and conclusion in the code panel fix its exact scope. The old boundary collision columns are both in the faithful global-slot source domain even though one of them is outside the rejected row-dependent nonzero-branch classifier.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The old boundary collision columns are both in the faithful global-slot source domain even though one of them is outside the rejected row-dependent nonzero-branch classifier. This is the regression that prevents future lower packets from treating 'bandedSparseAccessPaperValidCleanSource' as the active source predicate.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:4821](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspaperglobalslotsource-boundarycolumns-n3). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperGlobalSlotSource_encodedOutOfRange_n3" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperGlobalSlotSource_encodedOutOfRange_n3")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper global slot source encoded out of range n 3”; the hypotheses and conclusion in the code panel fix its exact scope. Encoded sparse value '7' is the first out-of-range slot for the one-term 'kappa = 7' contract.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Encoded sparse value '7' is the first out-of-range slot for the one-term 'kappa = 7' contract. It is clean in the padded O\_D register but not in the faithful global-slot source domain.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:4836](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspaperglobalslotsource-encodedoutofrange-n3). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.robinFunctionValue" (lean := "QuantumBlockEncoding.GHL2025.robinFunctionValue")
*Plain-English reading.* This definition gives the library's named construction or computation for “robin function value”. Symbolic function value at grid point j.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Symbolic function value at grid point j. Returns Coeff.symbol "f\_x\_j" for each grid index. The paper's O\_f (Theorem amplitude-oracle for piece-wise polynomial function, main.tex:870-910) encodes f(x\_j)/N\_f; the 1/N\_f factor is absorbed into the normalizer α = N\_D · N\_f · κ. main.tex:870-910 -

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:4853](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-robinfunctionvalue). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.FunctionOraclePaperRegisters" (lean := "QuantumBlockEncoding.GHL2025.FunctionOraclePaperRegisters")
*Plain-English reading.* This record groups the data and proof fields needed for “function oracle paper registers”. A proposition-valued field is a requirement until a constructor supplies it. Register values used by the paper-level function oracle 'O\_f' contract.

*Formal status.* Data contract in the default import surface; proposition-valued fields are obligations, not automatically established facts.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Register values used by the paper-level function oracle 'O\_f' contract. The compound-index convention stores the system row in bits '\[1, 1+n)' and stores the 'm\_f' function-oracle workspace immediately above the indicator bit, starting at 'robinIndicatorBitPosition p + 1'. This record is a source-contract skeleton for the paper's clean-workspace equation; it does not assert the amplitude relation or workspace cleanup. Guseynov-Huang-Liu 2025, function-oracle construction, arXiv:2506.20478.

*Declaration kind.* structure.

Source: [QuantumBlockEncoding/GHL2025.lean:4865](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-functionoraclepaperregisters). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.functionOraclePaperRegisters" (lean := "QuantumBlockEncoding.GHL2025.functionOraclePaperRegisters")
*Plain-English reading.* This definition gives the library's named construction or computation for “function oracle paper registers”. Extract the system register and the 'm\_f' function workspace from a compound basis index for the 'O\_f' source contract.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Extract the system register and the 'm\_f' function workspace from a compound basis index for the 'O\_f' source contract. The 'nonMFValue' field is the input index with the 'm\_f' workspace bits cleared. For clean-workspace columns this is the clean-branch basis index appearing in the paper equation.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:4880](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-functionoraclepaperregisters). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.functionOracleNormalizedValue" (lean := "QuantumBlockEncoding.GHL2025.functionOracleNormalizedValue")
*Plain-English reading.* This definition gives the library's named construction or computation for “function oracle normalized value”. Symbolic normalized clean-branch amplitude for the paper's function oracle.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Symbolic normalized clean-branch amplitude for the paper's function oracle. The reciprocal symbol records the intended factor '1 / N\_f' without proving that 'N\_f' is nonzero or that the amplitude is bounded.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:4901](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-functionoraclenormalizedvalue). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.FunctionOraclePaperImage" (lean := "QuantumBlockEncoding.GHL2025.FunctionOraclePaperImage")
*Plain-English reading.* This record groups the data and proof fields needed for “function oracle paper image”. A proposition-valued field is a requirement until a constructor supplies it. Paper-image source contract for one column of the function oracle 'O\_f'.

*Formal status.* Data contract in the default import surface; proposition-valued fields are obligations, not automatically established facts.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Paper-image source contract for one column of the function oracle 'O\_f'. The clean branch records the displayed paper component '(f(x\_i)/N\_f)|0>^mf|i>'. The orthogonal component and all analytic side conditions are tracked as false obligations; this record is not a matrix proof and does not promote the current diagonal helper to a faithful oracle.

*Declaration kind.* structure.

Source: [QuantumBlockEncoding/GHL2025.lean:4912](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-functionoraclepaperimage). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.functionOraclePaperImage" (lean := "QuantumBlockEncoding.GHL2025.functionOraclePaperImage")
*Plain-English reading.* This definition gives the library's named construction or computation for “function oracle paper image”. Build the paper-level 'O\_f' image contract for one compound basis column.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Build the paper-level 'O\_f' image contract for one compound basis column. This captures the register-level target '|0>^mf|i> ↦ (f(x\_i)/N\_f)|0>^mf|i> + |orth\_f(i)>' as data and keeps every unproved semantic claim explicit.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:4936](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-functionoraclepaperimage). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.functionOraclePaperImage_inputRegisters_eq" (lean := "QuantumBlockEncoding.GHL2025.functionOraclePaperImage_inputRegisters_eq")
*Plain-English reading.* Lean checks the proposition indexed as “function oracle paper image input registers eq”; the hypotheses and conclusion in the code panel fix its exact scope. Bridge lemma: the 'O\_f' paper image uses the shared register extractor.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Bridge lemma: the 'O\_f' paper image uses the shared register extractor.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:4977](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-functionoraclepaperimage-inputregisters-eq). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.functionOraclePaperImage_cleanBranchBasisIndex_eq" (lean := "QuantumBlockEncoding.GHL2025.functionOraclePaperImage_cleanBranchBasisIndex_eq")
*Plain-English reading.* Lean checks the proposition indexed as “function oracle paper image clean branch basis index eq”; the hypotheses and conclusion in the code panel fix its exact scope. Bridge lemma: the clean 'O\_f' branch clears only the 'm\_f' workspace bits.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Bridge lemma: the clean 'O\_f' branch clears only the 'm\_f' workspace bits.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:4983](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-functionoraclepaperimage-cleanbranchbasisindex-eq). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.functionOraclePaperImage_cleanBranchSystemValue_eq" (lean := "QuantumBlockEncoding.GHL2025.functionOraclePaperImage_cleanBranchSystemValue_eq")
*Plain-English reading.* Lean checks the proposition indexed as “function oracle paper image clean branch system value eq”; the hypotheses and conclusion in the code panel fix its exact scope. Bridge lemma: the clean 'O\_f' branch preserves the extracted system value.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Bridge lemma: the clean 'O\_f' branch preserves the extracted system value.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:4989](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-functionoraclepaperimage-cleanbranchsystemvalue-eq). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.functionOraclePaperImage_cleanBranchWorkspaceValue_eq" (lean := "QuantumBlockEncoding.GHL2025.functionOraclePaperImage_cleanBranchWorkspaceValue_eq")
*Plain-English reading.* Lean checks the proposition indexed as “function oracle paper image clean branch workspace value eq”; the hypotheses and conclusion in the code panel fix its exact scope. Bridge lemma: the clean 'O\_f' branch has zero 'm\_f' workspace value.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Bridge lemma: the clean 'O\_f' branch has zero 'm\_f' workspace value.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:4995](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-functionoraclepaperimage-cleanbranchworkspacevalue-eq). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.functionOraclePaperImage_cleanBranchAmplitude_eq" (lean := "QuantumBlockEncoding.GHL2025.functionOraclePaperImage_cleanBranchAmplitude_eq")
*Plain-English reading.* Lean checks the proposition indexed as “function oracle paper image clean branch amplitude eq”; the hypotheses and conclusion in the code panel fix its exact scope. Bridge lemma: the clean 'O\_f' branch amplitude is the normalized function value at the system value extracted from the same column.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Bridge lemma: the clean 'O\_f' branch amplitude is the normalized function value at the system value extracted from the same column.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:5003](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-functionoraclepaperimage-cleanbranchamplitude-eq). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.functionOraclePaperImage_cleanWorkspaceBranch_eq" (lean := "QuantumBlockEncoding.GHL2025.functionOraclePaperImage_cleanWorkspaceBranch_eq")
*Plain-English reading.* Lean checks the proposition indexed as “function oracle paper image clean workspace branch eq”; the hypotheses and conclusion in the code panel fix its exact scope. Bridge lemma: the clean-workspace branch flag is inherited from the extractor.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Bridge lemma: the clean-workspace branch flag is inherited from the extractor.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:5009](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-functionoraclepaperimage-cleanworkspacebranch-eq). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.FunctionOracleExternalAmplitudeSourceContract" (lean := "QuantumBlockEncoding.GHL2025.FunctionOracleExternalAmplitudeSourceContract")
*Plain-English reading.* This record groups the data and proof fields needed for “function oracle external amplitude source contract”. A proposition-valued field is a requirement until a constructor supplies it. External source transcript for the O\_f amplitude-oracle theorem cited by GHL2025.

*Formal status.* Data contract in the default import surface; proposition-valued fields are obligations, not automatically established facts.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* External source transcript for the O\_f amplitude-oracle theorem cited by GHL2025. This records the theorem and coordinate-oracle equation used as a source contract for the function oracle. It does not formalize the cited theorem and does not close the analytic facts needed for the Lean 'O\_f' contract.

*Declaration kind.* structure.

Source: [QuantumBlockEncoding/GHL2025.lean:5022](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-functionoracleexternalamplitudesourcecontract). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.functionOracleExternalAmplitudeSourceContract" (lean := "QuantumBlockEncoding.GHL2025.functionOracleExternalAmplitudeSourceContract")
*Plain-English reading.* This definition gives the library's named construction or computation for “function oracle external amplitude source contract”. Default source transcript for GHL2025's function-oracle dependency.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Default source transcript for GHL2025's function-oracle dependency. The GHL2025 theorem cites Guseynov--Liu 2024, arXiv:2411.01131, Theorem 5. The QBE status remains obligation-only: this declaration gives later proof packets a typed source anchor, not a proof of the theorem.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:5047](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-functionoracleexternalamplitudesourcecontract). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.functionOracleExternalAmplitudeSourceContract_sourceAnchor" (lean := "QuantumBlockEncoding.GHL2025.functionOracleExternalAmplitudeSourceContract_sourceAnchor")
*Plain-English reading.* Lean checks the proposition indexed as “function oracle external amplitude source contract source anchor”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:5088](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-functionoracleexternalamplitudesourcecontract-sourceanchor). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.functionOracleExternalAmplitudeSourceContract_flags_false" (lean := "QuantumBlockEncoding.GHL2025.functionOracleExternalAmplitudeSourceContract_flags_false")
*Plain-English reading.* Lean checks the proposition indexed as “function oracle external amplitude source contract flags false”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:5092](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-functionoracleexternalamplitudesourcecontract-flags-false). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.FunctionOracleAmplitudeProofRoute" (lean := "QuantumBlockEncoding.GHL2025.FunctionOracleAmplitudeProofRoute")
*Plain-English reading.* This record groups the data and proof fields needed for “function oracle amplitude proof route”. A proposition-valued field is a requirement until a constructor supplies it. Refined proof route for the 'of\_nf\_amplitude\_route' block.

*Formal status.* Data contract in the default import surface; proposition-valued fields are obligations, not automatically established facts.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Refined proof route for the 'of\_nf\_amplitude\_route' block. The route ties the paper's coordinate-oracle equation to the current Lean source-contract data: the symbolic function value, the 'N\_f' normalizer symbol, the clean-branch amplitude in 'functionOraclePaperImage', and the theorem-level function-oracle obligation. It does not prove that 'N\_f' is nonzero, that 'N\_f\_inv' is an inverse, that the normalizer bound holds, or that the orthogonal component gives a unitary completion.

*Declaration kind.* structure.

Source: [QuantumBlockEncoding/GHL2025.lean:5114](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-functionoracleamplitudeproofroute). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.functionOracleAmplitudeProofRoute" (lean := "QuantumBlockEncoding.GHL2025.functionOracleAmplitudeProofRoute")
*Plain-English reading.* This definition gives the library's named construction or computation for “function oracle amplitude proof route”. Default O\_f amplitude-route contract for one compound basis column.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Default O\_f amplitude-route contract for one compound basis column. The route reuses 'functionOraclePaperImage'; it only packages the dependencies needed before any future proof of the clean-branch amplitude or theorem-level 'FunctionOracleContract.amplitudeCorrect' field.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:5141](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-functionoracleamplitudeproofroute). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.functionOracleAmplitudeProofRoute_sourceAnchor" (lean := "QuantumBlockEncoding.GHL2025.functionOracleAmplitudeProofRoute_sourceAnchor")
*Plain-English reading.* Lean checks the proposition indexed as “function oracle amplitude proof route source anchor”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:5166](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-functionoracleamplitudeproofroute-sourceanchor). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.functionOracleAmplitudeProofRoute_sourceFunctionValue" (lean := "QuantumBlockEncoding.GHL2025.functionOracleAmplitudeProofRoute_sourceFunctionValue")
*Plain-English reading.* Lean checks the proposition indexed as “function oracle amplitude proof route source function value”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:5171](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-functionoracleamplitudeproofroute-sourcefunctionvalue). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.functionOracleAmplitudeProofRoute_normalizerNf" (lean := "QuantumBlockEncoding.GHL2025.functionOracleAmplitudeProofRoute_normalizerNf")
*Plain-English reading.* Lean checks the proposition indexed as “function oracle amplitude proof route normalizer nf”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:5176](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-functionoracleamplitudeproofroute-normalizernf). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.functionOracleAmplitudeProofRoute_normalizedAmplitude" (lean := "QuantumBlockEncoding.GHL2025.functionOracleAmplitudeProofRoute_normalizedAmplitude")
*Plain-English reading.* Lean checks the proposition indexed as “function oracle amplitude proof route normalized amplitude”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:5181](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-functionoracleamplitudeproofroute-normalizedamplitude). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.functionOracleAmplitudeProofRoute_paperImage" (lean := "QuantumBlockEncoding.GHL2025.functionOracleAmplitudeProofRoute_paperImage")
*Plain-English reading.* Lean checks the proposition indexed as “function oracle amplitude proof route paper image”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:5186](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-functionoracleamplitudeproofroute-paperimage). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.functionOracleAmplitudeProofRoute_obligations_reuse_paperImage" (lean := "QuantumBlockEncoding.GHL2025.functionOracleAmplitudeProofRoute_obligations_reuse_paperImage")
*Plain-English reading.* Lean checks the proposition indexed as “function oracle amplitude proof route obligations reuse paper image”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:5196](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-functionoracleamplitudeproofroute-obligations-reuse-paperimage). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.functionOracleAmplitudeProofRoute_externalSourceContract" (lean := "QuantumBlockEncoding.GHL2025.functionOracleAmplitudeProofRoute_externalSourceContract")
*Plain-English reading.* Lean checks the proposition indexed as “function oracle amplitude proof route external source contract”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:5208](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-functionoracleamplitudeproofroute-externalsourcecontract). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.functionOracleAmplitudeProofRoute_flags_false" (lean := "QuantumBlockEncoding.GHL2025.functionOracleAmplitudeProofRoute_flags_false")
*Plain-English reading.* Lean checks the proposition indexed as “function oracle amplitude proof route flags false”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:5224](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-functionoracleamplitudeproofroute-flags-false). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.functionOracleAmplitudeProofRoute_externalSourceAndFlags" (lean := "QuantumBlockEncoding.GHL2025.functionOracleAmplitudeProofRoute_externalSourceAndFlags")
*Plain-English reading.* Lean checks the proposition indexed as “function oracle amplitude proof route external source and flags”; the hypotheses and conclusion in the code panel fix its exact scope. Combined Phase-1 guard for the 'O\_f' external-source route.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Combined Phase-1 guard for the 'O\_f' external-source route. The bridge to the cited amplitude-oracle theorem and the false analytic flags are packaged together so later proof packets cannot use the source transcript as a proof of 'O\_f' amplitude correctness or unitarity.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:5242](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-functionoracleamplitudeproofroute-externalsourceandflags). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.functionOracleOrthogonalEntry" (lean := "QuantumBlockEncoding.GHL2025.functionOracleOrthogonalEntry")
*Plain-English reading.* This definition gives the library's named construction or computation for “function oracle orthogonal entry”. Symbolic matrix entry for the unresolved orthogonal component of 'O\_f'.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Symbolic matrix entry for the unresolved orthogonal component of 'O\_f'. The paper only fixes the clean 'm\_f' branch amplitude 'f(x\_i) / N\_f'; the remaining orthogonal completion is a unitarity obligation. This symbol records one placeholder entry for that unresolved completion without proving orthogonality, normalizer bounds, or unitarity.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:5285](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-functionoracleorthogonalentry). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.functionOraclePaperMatrix" (lean := "QuantumBlockEncoding.GHL2025.functionOraclePaperMatrix")
*Plain-English reading.* This definition gives the library's named construction or computation for “function oracle paper matrix”. Faithful Phase 1 matrix skeleton for the paper-level function oracle 'O\_f'.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Faithful Phase 1 matrix skeleton for the paper-level function oracle 'O\_f'. For each clean-workspace input column, the clean 'm\_f' branch entry is the normalized amplitude recorded by 'functionOraclePaperImage', namely 'f(x\_i) / N\_f' represented as 'functionOracleNormalizedValue'. Other clean-workspace output rows are zero, matching the paper statement that the unresolved component is orthogonal to the clean workspace branch. Non-clean-workspace rows carry symbolic completion entries. For non-clean input columns, the paper does not fix a branch equation, so this skeleton leaves all entries symbolic. The symbolic completion does not prove amplitude correctness, the 'N\_f' bound, orthogonality, or unitarity; those obligations remain false in 'functionOraclePaperImage' and the gate record.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:5305](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-functionoraclepapermatrix). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.functionOraclePaperMatrix_cleanBranch_entry" (lean := "QuantumBlockEncoding.GHL2025.functionOraclePaperMatrix_cleanBranch_entry")
*Plain-English reading.* Lean checks the proposition indexed as “function oracle paper matrix clean branch entry”; the hypotheses and conclusion in the code panel fix its exact scope. The 'O\_f' paper matrix exposes the clean branch amplitude for clean input columns.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The 'O\_f' paper matrix exposes the clean branch amplitude for clean input columns.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:5320](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-functionoraclepapermatrix-cleanbranch-entry). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.functionOraclePaperMatrix_cleanWorkspace_offBranch_zero" (lean := "QuantumBlockEncoding.GHL2025.functionOraclePaperMatrix_cleanWorkspace_offBranch_zero")
*Plain-English reading.* Lean checks the proposition indexed as “function oracle paper matrix clean workspace off branch zero”; the hypotheses and conclusion in the code panel fix its exact scope. Other clean-workspace rows have zero 'O\_f' orthogonal-completion entry.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Other clean-workspace rows have zero 'O\_f' orthogonal-completion entry.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:5330](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-functionoraclepapermatrix-cleanworkspace-offbranch-zero). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.functionOraclePaperMatrix_nonCleanInput_entry" (lean := "QuantumBlockEncoding.GHL2025.functionOraclePaperMatrix_nonCleanInput_entry")
*Plain-English reading.* Lean checks the proposition indexed as “function oracle paper matrix non clean input entry”; the hypotheses and conclusion in the code panel fix its exact scope. Non-clean input columns are left in the symbolic 'O\_f' completion branch.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Non-clean input columns are left in the symbolic 'O\_f' completion branch.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:5340](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-functionoraclepapermatrix-noncleaninput-entry). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.functionOracleMatrix" (lean := "QuantumBlockEncoding.GHL2025.functionOracleMatrix")
*Plain-English reading.* This definition gives the library's named construction or computation for “function oracle matrix”. Helper-only O\_f diagonal matrix: records function values f(x\_j) on the diagonal.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Helper-only O\_f diagonal matrix: records function values f(x\_j) on the diagonal. For each compound basis state |j⟩, extracts the system register value i and sets the diagonal entry to 'robinFunctionValue n i' = Coeff.symbol "f\_x\_i". All off-diagonal entries are zero. The entry depends only on the system register (grid point index), not on the sparse index. The paper's O\_f (Theorem amplitude-oracle for piece-wise polynomial function, main.tex:870-910) encodes f(x\_j)/N\_f via amplitude oracle. The 1/N\_f normalization is absorbed into the block-encoding normalizer α = N\_D · N\_f · κ. This diagonal matrix is not the paper image; the paper-level clean branch and orthogonal-component obligations are recorded by 'functionOraclePaperImage', and the active gate keeps 'unitary.proved := false'. main.tex:870-910 -

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:5365](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-functionoraclematrix). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.oneTermRobinGate_O_f" (lean := "QuantumBlockEncoding.GHL2025.oneTermRobinGate_O_f")
*Plain-English reading.* This definition gives the library's named construction or computation for “one term robin gate o f”. Gate matrix for 'O\_f' using the faithful paper-image matrix skeleton.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Gate matrix for 'O\_f' using the faithful paper-image matrix skeleton. The active matrix now exposes the clean 'm\_f' branch amplitude from 'functionOraclePaperImage'. The legacy diagonal helper 'functionOracleMatrix' remains available only as a function-value data check. Unitarity, amplitude correctness, the 'N\_f' bound, and the orthogonal completion are still unproved. Guseynov-Huang-Liu 2025, Theorem amplitude-oracle for piece-wise polynomial function and Fig. 1-term Robin, arXiv:2506.20478.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:5384](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-onetermrobingate-o-f). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.swapOracleMatrix" (lean := "QuantumBlockEncoding.GHL2025.swapOracleMatrix")
*Plain-English reading.* This definition gives the library's named construction or computation for “swap oracle matrix”. Honest SWAP matrix: permutation matrix swapping the system register (n qubits at bits \[1, 1+n)) with the O\_D^BS register (n qubits at bits \[1+n, 1+2n)).

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Honest SWAP matrix: permutation matrix swapping the system register (n qubits at bits \[1, 1+n)) with the O\_D^BS register (n qubits at bits \[1+n, 1+2n)). For each basis state |j⟩: - Extract block1 = bits \[1, 1+n) of j (system register value) - Extract block2 = bits \[1+n, 1+2n) of j (O\_D^BS register value) - diff = block1 XOR block2 - Swapped index = j XOR (diff <<< 1) XOR (diff <<< (1+n)) When block1 = block2 the SWAP is the identity. All bits outside the two n-qubit blocks (ancilla bit 0, indicator bit 1+2n, mf MSBs) are preserved. The SWAP image-level proof is now promoted: 'swapOracleImage' is proved self-inverse, 'swapOracleMatrix' is proved a finite permutation matrix, and 'oneTermRobinGate\_SWAP.unitary.proved = true'. figure:1\_term\_ROBIN, main.tex:1140 -

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:5410](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-swaporaclematrix). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.swapOracleImage" (lean := "QuantumBlockEncoding.GHL2025.swapOracleImage")
*Plain-English reading.* This definition gives the library's named construction or computation for “swap oracle image”. Image function for the SWAP oracle: swaps two n-qubit register blocks.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Image function for the SWAP oracle: swaps two n-qubit register blocks. For each basis state j, swaps block1 (bits \[1,1+n)) with block2 (bits \[1+n,1+2n)) by XORing with the block difference shifted to each block position. main.tex:1140 -

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:5426](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-swaporacleimage). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.swapOracleDiff" (lean := "QuantumBlockEncoding.GHL2025.swapOracleDiff")
*Plain-English reading.* This definition gives the library's named construction or computation for “swap oracle diff”. The n-bit XOR difference between the two register blocks exchanged by SWAP.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The n-bit XOR difference between the two register blocks exchanged by SWAP. This is the reusable proof-DAG interface for the SWAP image route: preservation of this value after one SWAP is the local ingredient for self-inverse and later finite-domain permutation proofs.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:5441](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-swaporaclediff). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.swapOracleImage_eq_xor_diff" (lean := "QuantumBlockEncoding.GHL2025.swapOracleImage_eq_xor_diff")
*Plain-English reading.* Lean checks the proposition indexed as “swap oracle image eq xor diff”; the hypotheses and conclusion in the code panel fix its exact scope. The SWAP image is the source index XORed by the same difference in both blocks.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The SWAP image is the source index XORed by the same difference in both blocks.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:5449](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-swaporacleimage-eq-xor-diff). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.swapOracleMatrix_eq_image" (lean := "QuantumBlockEncoding.GHL2025.swapOracleMatrix_eq_image")
*Plain-English reading.* Lean checks the proposition indexed as “swap oracle matrix eq image”; the hypotheses and conclusion in the code panel fix its exact scope. swapOracleMatrix entry equals image function check.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* swapOracleMatrix entry equals image function check.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:5456](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-swaporaclematrix-eq-image). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.oneTermRobinGate_SWAP" (lean := "QuantumBlockEncoding.GHL2025.oneTermRobinGate_SWAP")
*Plain-English reading.* This definition gives the library's named construction or computation for “one term robin gate swap”. Gate matrix for SWAP using the honest permutation matrix.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Gate matrix for SWAP using the honest permutation matrix. Swaps system register (bits \[1,n+1)) with O\_D^BS register (bits \[n+1,2n+1)). Unitarity is backed by the proof-DAG permutation bridge below: 'swapOracleMatrix\_is\_permutation'. main.tex:1140 -

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:5468](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-onetermrobingate-swap). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.bandedSparseAccessDaggerMatrix" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessDaggerMatrix")
*Plain-English reading.* This definition gives the library's named construction or computation for “banded sparse access dagger matrix”. Transpose-style matrix for O\_D^BS, sharing the forward sparse-access image map.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Transpose-style matrix for O\_D^BS, sharing the forward sparse-access image map. For each i: compute image(i) using the forward mapping, then check if j = image(i). This is the matrix transpose of bandedSparseAccessMatrix. The inverse/unitarity proof is blocked until the forward boundary column-map contract is reconciled. main.tex:1148 -

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:5483](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccessdaggermatrix). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.oneTermRobinGate_O_D_BS_dagger" (lean := "QuantumBlockEncoding.GHL2025.oneTermRobinGate_O_D_BS_dagger")
*Plain-English reading.* This definition gives the library's named construction or computation for “one term robin gate o d bs dagger”. Gate matrix for '(O\_D^BS)^†' using the transpose-style paper-image matrix.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Gate matrix for '(O\_D^BS)^†' using the transpose-style paper-image matrix. This is paired with 'bandedSparseAccessPaperMatrix'; it does not prove that the transpose is a true inverse on the relevant post-SWAP states. figure:1\_term\_ROBIN and Lemma 1, arXiv:2506.20478.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:5505](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-onetermrobingate-o-d-bs-dagger). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.oneTermRobinGate_O_D_BS_dagger_imageFin_eq_one" (lean := "QuantumBlockEncoding.GHL2025.oneTermRobinGate_O_D_BS_dagger_imageFin_eq_one")
*Plain-English reading.* Lean checks the proposition indexed as “one term robin gate o d bs dagger image fin eq one”; the hypotheses and conclusion in the code panel fix its exact scope. Active '(O\_D^BS)^†' gate entry paired with the finite forward image.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Active '(O\_D^BS)^†' gate entry paired with the finite forward image.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:5515](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-onetermrobingate-o-d-bs-dagger-imagefin-eq-one). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.oneTermRobinGate_O_D_BS_dagger_postSwap_entry_of_preimage" (lean := "QuantumBlockEncoding.GHL2025.oneTermRobinGate_O_D_BS_dagger_postSwap_entry_of_preimage")
*Plain-English reading.* Lean checks the proposition indexed as “one term robin gate o d bs dagger post swap entry of preimage”; the hypotheses and conclusion in the code panel fix its exact scope. Post-SWAP dagger entry from an explicitly supplied paper-image preimage.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Post-SWAP dagger entry from an explicitly supplied paper-image preimage. The hypothesis 'hpre' is the whole inverse-on-range input for this lemma: it does not prove that such a 'pre' exists, that it is unique, or that the dagger cleans the padded sparse-index register. The post-SWAP relation is recorded by 'hpost' for the cleanup proof-DAG interface, but the matrix entry itself is just the active transpose-style paper-image entry.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:5534](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-onetermrobingate-o-d-bs-dagger-postswap-entry-of-preimage). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.BandedSparseAccessPostSwapCleanup" (lean := "QuantumBlockEncoding.GHL2025.BandedSparseAccessPostSwapCleanup")
*Plain-English reading.* This record groups the data and proof fields needed for “banded sparse access post swap cleanup”. A proposition-valued field is a requirement until a constructor supplies it. Proof-carrying interface for a supplied post-SWAP cleanup preimage.

*Formal status.* Data contract in the default import surface; proposition-valued fields are obligations, not automatically established facts.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Proof-carrying interface for a supplied post-SWAP cleanup preimage. The fields intentionally include the hypotheses that are not yet derived: 'postSwap', 'preimage', 'preCleanInput', and 'preAddressBound'. The record only packages consequences of those inputs: the active dagger entry and executable register-cleanup checks for the chosen preimage. Existence, uniqueness, and the paper-level 'daggerCleanup' obligation remain open.

*Declaration kind.* structure.

Source: [QuantumBlockEncoding/GHL2025.lean:5555](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspostswapcleanup). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.bandedSparseAccessPostSwapCleanup_of_preimage" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPostSwapCleanup_of_preimage")
*Plain-English reading.* This definition gives the library's named construction or computation for “banded sparse access post swap cleanup of preimage”. Build the post-SWAP cleanup witness from an explicitly supplied preimage.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Build the post-SWAP cleanup witness from an explicitly supplied preimage. This is the fixed inverse-on-range interface for the next cleanup proof: it does not construct the preimage and does not promote any semantic proof flag.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:5585](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspostswapcleanup-of-preimage). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.oneTermRobinGate_O_D_BS_imageFin_entrySafety" (lean := "QuantumBlockEncoding.GHL2025.oneTermRobinGate_O_D_BS_imageFin_entrySafety")
*Plain-English reading.* Lean checks the proposition indexed as “one term robin gate o d bs image fin entry safety”; the hypotheses and conclusion in the code panel fix its exact scope. Reusable image witness for the active Lemma 1 'O\_D^BS' gate pair.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Reusable image witness for the active Lemma 1 'O\_D^BS' gate pair. This packages the forward entry, transpose-style dagger entry, row roundtrip, written-address roundtrip, and no-spill Boolean under the explicit n-bit address hypothesis. It is not an injectivity, inverse uniqueness, cleanup, or unitarity proof.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:5626](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-onetermrobingate-o-d-bs-imagefin-entrysafety). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.oneTermRobinGate_O_D_BS_globalSlotSource_entrySafety" (lean := "QuantumBlockEncoding.GHL2025.oneTermRobinGate_O_D_BS_globalSlotSource_entrySafety")
*Plain-English reading.* Lean checks the proposition indexed as “one term robin gate o d bs global slot source entry safety”; the hypotheses and conclusion in the code panel fix its exact scope. Global-source specialization of the active Lemma 1 'O\_D^BS' entry witness.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Global-source specialization of the active Lemma 1 'O\_D^BS' entry witness. For a finite column in the faithful source domain, the global-slot source predicate supplies the clean padded input and sparse-slot bound, while the '2 <= n' parameter-family hypothesis supplies the n-bit address bound. The result packages the finite image index and paired forward/dagger entries, but does not prove injectivity, cleanup, or unitarity.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:5660](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-onetermrobingate-o-d-bs-globalslotsource-entrysafety). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.oneTermRobinGateMatrixPlaceholders" (lean := "QuantumBlockEncoding.GHL2025.oneTermRobinGateMatrixPlaceholders")
*Plain-English reading.* This definition gives the library's named construction or computation for “one term robin gate matrix placeholders”. List of all 7 gate matrix placeholders for the one-term Robin circuit, in the same order as 'oneTermRobinCircuit'.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* List of all 7 gate matrix placeholders for the one-term Robin circuit, in the same order as 'oneTermRobinCircuit'. figure:1\_term\_ROBIN -

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:5706](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-onetermrobingatematrixplaceholders). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.oneTermRobinPlaceholdersMatch" (lean := "QuantumBlockEncoding.GHL2025.oneTermRobinPlaceholdersMatch")
*Plain-English reading.* Lean checks the proposition indexed as “one term robin placeholders match”; the hypotheses and conclusion in the code panel fix its exact scope. The placeholder gate matrices match the circuit gate labels.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The placeholder gate matrices match the circuit gate labels. This is trivially true because the placeholders were constructed with matching gate constructors. figure:1\_term\_ROBIN -

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:5722](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-onetermrobinplaceholdersmatch). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.oneTermRobinGateMatrixPlaceholders_gateList" (lean := "QuantumBlockEncoding.GHL2025.oneTermRobinGateMatrixPlaceholders_gateList")
*Plain-English reading.* Lean checks the proposition indexed as “one term robin gate matrix placeholders gate list”; the hypotheses and conclusion in the code panel fix its exact scope. The active matrix placeholder list uses the same gate order as Fig.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The active matrix placeholder list uses the same gate order as Fig. 1-term Robin and 'oneTermRobinCircuit'. This is a structural guard only: it prevents a later proof packet from keeping similar-looking proof flags while changing the circuit order.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:5738](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-onetermrobingatematrixplaceholders-gatelist). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.oneTermRobinGateMatrixPlaceholders_unitaryFlags" (lean := "QuantumBlockEncoding.GHL2025.oneTermRobinGateMatrixPlaceholders_unitaryFlags")
*Plain-English reading.* Lean checks the proposition indexed as “one term robin gate matrix placeholders unitary flags”; the hypotheses and conclusion in the code panel fix its exact scope. The active seven-gate matrix list keeps only the locally certified indicator and SWAP gates marked as proved.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The active seven-gate matrix list keeps only the locally certified indicator and SWAP gates marked as proved. This is a Phase 1 guard for Fig. 1-term Robin. It records the current gate-level proof flags without promoting the paper-oracle obligations for 'O\_DT^S', 'Ry\_boundary', 'O\_D^BS', 'O\_f', or '(O\_D^BS)^dagger'.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:5757](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-onetermrobingatematrixplaceholders-unitaryflags). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.indicatorOracleImage" (lean := "QuantumBlockEncoding.GHL2025.indicatorOracleImage")
*Plain-English reading.* This definition gives the library's named construction or computation for “indicator oracle image”. Indicator oracle image function: for each basis state j, computes the image by XORing the indicator bit at position indPos when the system register value is in the bulk window \[K1, K2\].

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Indicator oracle image function: for each basis state j, computes the image by XORing the indicator bit at position indPos when the system register value is in the bulk window \[K1, K2\]. This is a self-inverse permutation. main.tex:1088-1099 -

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:5773](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-indicatororacleimage). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.indicatorOracleMatrix_eq_image" (lean := "QuantumBlockEncoding.GHL2025.indicatorOracleMatrix_eq_image")
*Plain-English reading.* Lean checks the proposition indexed as “indicator oracle matrix eq image”; the hypotheses and conclusion in the code panel fix its exact scope. The indicator oracle matrix entry is 1 exactly when i = indicatorOracleImage j.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The indicator oracle matrix entry is 1 exactly when i = indicatorOracleImage j. main.tex:1088-1099 -

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:5785](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-indicatororaclematrix-eq-image). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.indicatorOracleImage_self_inverse_n1" (lean := "QuantumBlockEncoding.GHL2025.indicatorOracleImage_self_inverse_n1")
*Plain-English reading.* Lean checks the proposition indexed as “indicator oracle image self inverse n 1”; the hypotheses and conclusion in the code panel fix its exact scope. Self-inverse property for n=1: applying indicatorOracleImage twice returns the original value for all j in Fin domain (128 elements).

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Self-inverse property for n=1: applying indicatorOracleImage twice returns the original value for all j in Fin domain (128 elements). Checked by native\_decide over the finite Fin type. main.tex:1088-1099 -

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:5796](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-indicatororacleimage-self-inverse-n1). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.indicatorOracleImage_self_inverse_n3" (lean := "QuantumBlockEncoding.GHL2025.indicatorOracleImage_self_inverse_n3")
*Plain-English reading.* Lean checks the proposition indexed as “indicator oracle image self inverse n 3”; the hypotheses and conclusion in the code panel fix its exact scope. Self-inverse property for n=3: applying indicatorOracleImage twice returns the original value for all j in Fin domain (8192 elements).

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Self-inverse property for n=3: applying indicatorOracleImage twice returns the original value for all j in Fin domain (8192 elements). Checked by native\_decide over the finite Fin type. main.tex:1088-1099 -

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:5810](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-indicatororacleimage-self-inverse-n3). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.indicatorOracleImage_injective_n1" (lean := "QuantumBlockEncoding.GHL2025.indicatorOracleImage_injective_n1")
*Plain-English reading.* Lean checks the proposition indexed as “indicator oracle image injective n 1”; the hypotheses and conclusion in the code panel fix its exact scope. Injectivity for n=1: derived from self-inverse property.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Injectivity for n=1: derived from self-inverse property. main.tex:1088-1099 -

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:5822](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-indicatororacleimage-injective-n1). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.indicatorOracleImage_injective_n3" (lean := "QuantumBlockEncoding.GHL2025.indicatorOracleImage_injective_n3")
*Plain-English reading.* Lean checks the proposition indexed as “indicator oracle image injective n 3”; the hypotheses and conclusion in the code panel fix its exact scope. Injectivity for n=3: derived from self-inverse property.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Injectivity for n=3: derived from self-inverse property. main.tex:1088-1099 -

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:5838](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-indicatororacleimage-injective-n3). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.shiftLeft_land_mask_eq_zero" (lean := "QuantumBlockEncoding.GHL2025.shiftLeft_land_mask_eq_zero")
*Plain-English reading.* Lean checks the proposition indexed as “shift left land mask eq zero”; the hypotheses and conclusion in the code panel fix its exact scope. Cycle 12 helper: (b <<< pos) &&& ((1 <<< n) - 1) = 0 when pos >= n, because b <<< pos has all zeros in bits \[0, pos) >= \[0, n).

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Cycle 12 helper: (b <<< pos) &&& ((1 <<< n) - 1) = 0 when pos >= n, because b <<< pos has all zeros in bits \[0, pos) >= \[0, n).

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:5855](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-shiftleft-land-mask-eq-zero). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.xor_shift_preserve_low" (lean := "QuantumBlockEncoding.GHL2025.xor_shift_preserve_low")
*Plain-English reading.* Lean checks the proposition indexed as “xor shift preserve low”; the hypotheses and conclusion in the code panel fix its exact scope. Cycle 12 helper: XOR with a value shifted left by 'pos' preserves the low 'n' bits when 'pos >= n'.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Cycle 12 helper: XOR with a value shifted left by 'pos' preserves the low 'n' bits when 'pos >= n'. Uses AND-XOR distributivity and the zero mask lemma.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:5872](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-xor-shift-preserve-low). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.xor_shift_preserve_shift_low" (lean := "QuantumBlockEncoding.GHL2025.xor_shift_preserve_shift_low")
*Plain-English reading.* Lean checks the proposition indexed as “xor shift preserve shift low”; the hypotheses and conclusion in the code panel fix its exact scope. Cycle 12 helper: XOR with a high-shifted value preserves low bits after right-shifting.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Cycle 12 helper: XOR with a high-shifted value preserves low bits after right-shifting. ((x ^^^ (b <<< pos)) >>> 1) &&& ((1 <<< n) - 1) = (x >>> 1) &&& ((1 <<< n) - 1) when pos >= 1 + n.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:5883](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-xor-shift-preserve-shift-low). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.swapOracleDiff_lt_two_pow" (lean := "QuantumBlockEncoding.GHL2025.swapOracleDiff_lt_two_pow")
*Plain-English reading.* Lean checks the proposition indexed as “swap oracle diff lt two pow”; the hypotheses and conclusion in the code panel fix its exact scope. SWAP proof-DAG helper: the XOR difference between the two n-bit blocks is itself an n-bit value.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* SWAP proof-DAG helper: the XOR difference between the two n-bit blocks is itself an n-bit value. main.tex:1140 -

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:5908](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-swaporaclediff-lt-two-pow). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.swapOracleDiff_shiftRight_eq_zero" (lean := "QuantumBlockEncoding.GHL2025.swapOracleDiff_shiftRight_eq_zero")
*Plain-English reading.* Lean checks the proposition indexed as “swap oracle diff shift right eq zero”; the hypotheses and conclusion in the code panel fix its exact scope. SWAP proof-DAG helper: right-shifting the n-bit block difference by n removes it.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* SWAP proof-DAG helper: right-shifting the n-bit block difference by n removes it. main.tex:1140 -

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:5926](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-swaporaclediff-shiftright-eq-zero). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.swapOracleDiff_shiftLeft_mask_eq_zero" (lean := "QuantumBlockEncoding.GHL2025.swapOracleDiff_shiftLeft_mask_eq_zero")
*Plain-English reading.* Lean checks the proposition indexed as “swap oracle diff shift left mask eq zero”; the hypotheses and conclusion in the code panel fix its exact scope. SWAP proof-DAG helper: shifting the block difference into the high block leaves zero in the low n-bit mask.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* SWAP proof-DAG helper: shifting the block difference into the high block leaves zero in the low n-bit mask. main.tex:1140 -

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:5941](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-swaporaclediff-shiftleft-mask-eq-zero). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.shiftLeft_lt_two_pow_of_lt" (lean := "QuantumBlockEncoding.GHL2025.shiftLeft_lt_two_pow_of_lt")
*Plain-English reading.* Lean checks the proposition indexed as “shift left lt two pow of lt”; the hypotheses and conclusion in the code panel fix its exact scope. Shifting a bounded value into a register block keeps it inside the total basis width.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Shifting a bounded value into a register block keeps it inside the total basis width.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:5954](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-shiftleft-lt-two-pow-of-lt). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.swapOracleImage_lt_qubitDim" (lean := "QuantumBlockEncoding.GHL2025.swapOracleImage_lt_qubitDim")
*Plain-English reading.* Lean checks the proposition indexed as “swap oracle image lt qubit dim”; the hypotheses and conclusion in the code panel fix its exact scope. SWAP proof-DAG range block: the image of the register-block SWAP stays inside the same full finite basis.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* SWAP proof-DAG range block: the image of the register-block SWAP stays inside the same full finite basis. This is only a range lemma; the finite permutation bridge is proved separately by 'swapOracleMatrix\_is\_permutation'.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:5972](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-swaporacleimage-lt-qubitdim). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.swapOracleImage_block1_eq_block2" (lean := "QuantumBlockEncoding.GHL2025.swapOracleImage_block1_eq_block2")
*Plain-English reading.* Lean checks the proposition indexed as “swap oracle image block 1 eq block 2”; the hypotheses and conclusion in the code panel fix its exact scope. SWAP proof-DAG block: after 'swapOracleImage', the low n-bit register equals the old high n-bit register.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* SWAP proof-DAG block: after 'swapOracleImage', the low n-bit register equals the old high n-bit register. This is the first register-level bit-slice lemma needed for the eventual SWAP self-inverse/permutation proof. main.tex:1140 -

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:6003](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-swaporacleimage-block1-eq-block2). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.swapOracleImage_block2_eq_block1" (lean := "QuantumBlockEncoding.GHL2025.swapOracleImage_block2_eq_block1")
*Plain-English reading.* Lean checks the proposition indexed as “swap oracle image block 2 eq block 1”; the hypotheses and conclusion in the code panel fix its exact scope. SWAP proof-DAG block: after 'swapOracleImage', the high n-bit register equals the old low n-bit register.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* SWAP proof-DAG block: after 'swapOracleImage', the high n-bit register equals the old low n-bit register. This is the symmetric register equation paired with 'swapOracleImage\_block1\_eq\_block2'; it is still only a bit-slice block, not a SWAP unitarity proof. main.tex:1140 -

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:6031](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-swaporacleimage-block2-eq-block1). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.swapOracleDiff_preserved" (lean := "QuantumBlockEncoding.GHL2025.swapOracleDiff_preserved")
*Plain-English reading.* Lean checks the proposition indexed as “swap oracle diff preserved”; the hypotheses and conclusion in the code panel fix its exact scope. SWAP proof-DAG block: the XOR difference between the two exchanged registers is preserved by one SWAP application.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* SWAP proof-DAG block: the XOR difference between the two exchanged registers is preserved by one SWAP application. This uses only the two register block equations and is independent of any gate unitarity flag.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:6065](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-swaporaclediff-preserved). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.xor_two_shifted_masks_cancel" (lean := "QuantumBlockEncoding.GHL2025.xor_two_shifted_masks_cancel")
*Plain-English reading.* Lean checks the proposition indexed as “xor two shifted masks cancel”; the hypotheses and conclusion in the code panel fix its exact scope. XORing the same two shifted masks twice cancels them bitwise.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* XORing the same two shifted masks twice cancels them bitwise.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:6074](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-xor-two-shifted-masks-cancel). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.swapOracleImage_self_inverse" (lean := "QuantumBlockEncoding.GHL2025.swapOracleImage_self_inverse")
*Plain-English reading.* Lean checks the proposition indexed as “swap oracle image self inverse”; the hypotheses and conclusion in the code panel fix its exact scope. SWAP proof-DAG block: the image function is self-inverse.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* SWAP proof-DAG block: the image function is self-inverse. This is the arithmetic image fact reused by the finite permutation-matrix bridge.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:6091](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-swaporacleimage-self-inverse). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.swapOracleImage_injective" (lean := "QuantumBlockEncoding.GHL2025.swapOracleImage_injective")
*Plain-English reading.* Lean checks the proposition indexed as “swap oracle image injective”; the hypotheses and conclusion in the code panel fix its exact scope. SWAP proof-DAG block: injectivity of the image function, derived from the self-inverse arithmetic block without opening the bit-slice proof again.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* SWAP proof-DAG block: injectivity of the image function, derived from the self-inverse arithmetic block without opening the bit-slice proof again.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:6102](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-swaporacleimage-injective). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.swapOracleImage_bijective" (lean := "QuantumBlockEncoding.GHL2025.swapOracleImage_bijective")
*Plain-English reading.* Lean checks the proposition indexed as “swap oracle image bijective”; the hypotheses and conclusion in the code panel fix its exact scope. SWAP proof-DAG block: bijectivity of 'swapOracleImage' on the finite full Hilbert-space basis.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* SWAP proof-DAG block: bijectivity of 'swapOracleImage' on the finite full Hilbert-space basis. The finite map uses 'swapOracleImage\_lt\_qubitDim' for the 'Fin' constructor and 'swapOracleImage\_self\_inverse' for both injectivity and surjectivity.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:6116](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-swaporacleimage-bijective). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.swapOracleMatrix_col_has_one" (lean := "QuantumBlockEncoding.GHL2025.swapOracleMatrix_col_has_one")
*Plain-English reading.* Lean checks the proposition indexed as “swap oracle matrix col has one”; the hypotheses and conclusion in the code panel fix its exact scope. For each SWAP matrix column, the row indexed by 'swapOracleImage' contains the unique '1' entry.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* For each SWAP matrix column, the row indexed by 'swapOracleImage' contains the unique '1' entry.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:6139](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-swaporaclematrix-col-has-one). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.swapOracleMatrix_col_unique" (lean := "QuantumBlockEncoding.GHL2025.swapOracleMatrix_col_unique")
*Plain-English reading.* Lean checks the proposition indexed as “swap oracle matrix col unique”; the hypotheses and conclusion in the code panel fix its exact scope. For each SWAP matrix column, any '1' entry must occur at the row indexed by 'swapOracleImage'.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* For each SWAP matrix column, any '1' entry must occur at the row indexed by 'swapOracleImage'.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:6151](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-swaporaclematrix-col-unique). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.swapOracleMatrix_row_has_one" (lean := "QuantumBlockEncoding.GHL2025.swapOracleMatrix_row_has_one")
*Plain-English reading.* Lean checks the proposition indexed as “swap oracle matrix row has one”; the hypotheses and conclusion in the code panel fix its exact scope. Every SWAP matrix row has a '1' entry, by finite surjectivity.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Every SWAP matrix row has a '1' entry, by finite surjectivity.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:6161](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-swaporaclematrix-row-has-one). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.swapOracleMatrix_row_unique" (lean := "QuantumBlockEncoding.GHL2025.swapOracleMatrix_row_unique")
*Plain-English reading.* Lean checks the proposition indexed as “swap oracle matrix row unique”; the hypotheses and conclusion in the code panel fix its exact scope. Every SWAP matrix row has a unique '1' entry, by finite injectivity.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Every SWAP matrix row has a unique '1' entry, by finite injectivity.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:6176](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-swaporaclematrix-row-unique). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.swapOracleMatrix_is_permutation" (lean := "QuantumBlockEncoding.GHL2025.swapOracleMatrix_is_permutation")
*Plain-English reading.* Lean checks the proposition indexed as “swap oracle matrix is permutation”; the hypotheses and conclusion in the code panel fix its exact scope. SWAP matrix is a finite permutation matrix: every row and column has exactly one entry equal to '1'.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* SWAP matrix is a finite permutation matrix: every row and column has exactly one entry equal to '1'. This closes the SWAP gate-level matrix-semantics bridge while leaving the paper-specific O\_D^BS, O\_f, LCU, and block-extraction obligations unchanged.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:6194](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-swaporaclematrix-is-permutation). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperPostSwap_rowValue_eq_address" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperPostSwap_rowValue_eq_address")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper post swap row value eq address”; the hypotheses and conclusion in the code panel fix its exact scope. After the active Lemma 1 paper image and the SWAP gate, the system-row register contains the paper address 'r\_si'.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* After the active Lemma 1 paper image and the SWAP gate, the system-row register contains the paper address 'r\_si'. This is a post-SWAP register equation under the same n-bit address hypothesis used by the finite image bridge; it does not construct a dagger preimage or promote cleanup.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:6221](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspaperpostswap-rowvalue-eq-address). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperPostSwap_odRegisterValue_eq_rowValue" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperPostSwap_odRegisterValue_eq_rowValue")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper post swap od register value eq row value”; the hypotheses and conclusion in the code panel fix its exact scope. After the active Lemma 1 paper image and the SWAP gate, the O\_D register contains the original row value.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* After the active Lemma 1 paper image and the SWAP gate, the O\_D register contains the original row value. This is the second post-SWAP register equation needed before inverse-on-range cleanup search.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:6238](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspaperpostswap-odregistervalue-eq-rowvalue). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperPostSwapImage_lt_qubitDim_of_address_lt" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperPostSwapImage_lt_qubitDim_of_address_lt")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper post swap image lt qubit dim of address lt”; the hypotheses and conclusion in the code panel fix its exact scope. After the active paper image and SWAP, the post-SWAP column is still a finite basis index whenever the source column is finite and the written paper address is n-bit.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* After the active paper image and SWAP, the post-SWAP column is still a finite basis index whenever the source column is finite and the written paper address is n-bit. This does not prove inverse-on-range or cleanup.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:6254](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspaperpostswapimage-lt-qubitdim-of-address-lt). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperSpliceODRegister" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperSpliceODRegister")
*Plain-English reading.* This definition gives the library's named construction or computation for “banded sparse access paper splice od register”. Replace the 'O\_D^BS' n-bit register of a compound index while preserving the low ancilla/system block and all high-tail bits.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Replace the 'O\_D^BS' n-bit register of a compound index while preserving the low ancilla/system block and all high-tail bits. This is the local splice used to build a post-SWAP cleanup preimage candidate. It does not assert that the chosen 'odValue' is the correct reverse address.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:6274](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspaperspliceodregister). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperImage_eq_splice" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperImage_eq_splice")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper image eq splice”; the hypotheses and conclusion in the code panel fix its exact scope. The paper image is the O\_D-register splice with the computed paper address.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The paper image is the O\_D-register splice with the computed paper address.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:6283](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspaperimage-eq-splice). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperSpliceODRegister_lowBlock_lt_highBase_of_odValue_lt" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperSpliceODRegister_lowBlock_lt_highBase_of_odValue_lt")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper splice od register low block lt high base of od value lt”; the hypotheses and conclusion in the code panel fix its exact scope. The spliced low-and-O\_D block fits below the high-tail boundary for n-bit O\_D values.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The spliced low-and-O\_D block fits below the high-tail boundary for n-bit O\_D values.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:6291](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspaperspliceodregister-lowblock-lt-highbase-of-odvalue-lt). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperSpliceODRegister_mod_lowBase" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperSpliceODRegister_mod_lowBase")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper splice od register mod low base”; the hypotheses and conclusion in the code panel fix its exact scope. Splicing an O\_D value preserves the low ancilla-and-row block.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Splicing an O\_D value preserves the low ancilla-and-row block.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:6329](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspaperspliceodregister-mod-lowbase). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperSpliceODRegister_div_lowBase_mod_eq" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperSpliceODRegister_div_lowBase_mod_eq")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper splice od register div low base mod eq”; the hypotheses and conclusion in the code panel fix its exact scope. Splicing an n-bit O\_D value exposes that value when the O\_D register is extracted.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Splicing an n-bit O\_D value exposes that value when the O\_D register is extracted.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:6364](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspaperspliceodregister-div-lowbase-mod-eq). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperSpliceODRegister_rowValue_eq" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperSpliceODRegister_rowValue_eq")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper splice od register row value eq”; the hypotheses and conclusion in the code panel fix its exact scope. Splicing preserves the row field.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Splicing preserves the row field.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:6407](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspaperspliceodregister-rowvalue-eq). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperSpliceODRegister_odRegisterValue_eq" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperSpliceODRegister_odRegisterValue_eq")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper splice od register od register value eq”; the hypotheses and conclusion in the code panel fix its exact scope. Splicing an n-bit value into the O\_D block makes that value the extracted O\_D register.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Splicing an n-bit value into the O\_D block makes that value the extracted O\_D register.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:6427](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspaperspliceodregister-odregistervalue-eq). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperSpliceODRegister_div_highBase_eq_of_odValue_lt" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperSpliceODRegister_div_highBase_eq_of_odValue_lt")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper splice od register div high base eq of od value lt”; the hypotheses and conclusion in the code panel fix its exact scope. Splicing an n-bit O\_D value preserves all bits above the O\_D register.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Splicing an n-bit O\_D value preserves all bits above the O\_D register.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:6439](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspaperspliceodregister-div-highbase-eq-of-odvalue-lt). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperSpliceODRegister_lt_qubitDim_of_odValue_lt" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperSpliceODRegister_lt_qubitDim_of_odValue_lt")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper splice od register lt qubit dim of od value lt”; the hypotheses and conclusion in the code panel fix its exact scope. Splicing an n-bit O\_D value into a finite compound basis index preserves the full finite-basis range.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Splicing an n-bit O\_D value into a finite compound basis index preserves the full finite-basis range. This is the range counterpart of the splice register equations and does not assert that the chosen O\_D value is semantically correct.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:6471](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspaperspliceodregister-lt-qubitdim-of-odvalue-lt). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperSpliceODRegister_splice_of_odValue_lt" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperSpliceODRegister_splice_of_odValue_lt")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper splice od register splice of od value lt”; the hypotheses and conclusion in the code panel fix its exact scope. Replacing the O\_D block twice is the same as keeping the second replacement.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Replacing the O\_D block twice is the same as keeping the second replacement.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:6515](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspaperspliceodregister-splice-of-odvalue-lt). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperSpliceODRegister_self" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperSpliceODRegister_self")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper splice od register self”; the hypotheses and conclusion in the code panel fix its exact scope. Reconstructing an index from its low, O\_D, and high blocks gives the same index.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Reconstructing an index from its low, O\_D, and high blocks gives the same index.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:6530](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspaperspliceodregister-self). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperCleanODValue" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperCleanODValue")
*Plain-English reading.* This definition gives the library's named construction or computation for “banded sparse access paper clean od value”. Clean 'O\_D^BS' register value whose padded-low part is zero and sparse part is 'sparseValue'.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Clean 'O\_D^BS' register value whose padded-low part is zero and sparse part is 'sparseValue'.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:6572](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspapercleanodvalue). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperCleanODValue_paddedZero_eq_zero" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperCleanODValue_paddedZero_eq_zero")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper clean od value padded zero eq zero”; the hypotheses and conclusion in the code panel fix its exact scope. The clean O\_D value has zeroes in the padded low slice.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The clean O\_D value has zeroes in the padded low slice.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:6577](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspapercleanodvalue-paddedzero-eq-zero). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperCleanODValue_lt_two_pow_of_sparse_lt" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperCleanODValue_lt_two_pow_of_sparse_lt")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper clean od value lt two pow of sparse lt”; the hypotheses and conclusion in the code panel fix its exact scope. A clean sparse value fits in the n-bit O\_D register when the sparse width fits in n.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* A clean sparse value fits in the n-bit O\_D register when the sparse width fits in n.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:6586](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspapercleanodvalue-lt-two-pow-of-sparse-lt). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperCleanODValue_sparseIndex_eq" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperCleanODValue_sparseIndex_eq")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper clean od value sparse index eq”; the hypotheses and conclusion in the code panel fix its exact scope. Extracting the sparse slice from a clean O\_D value recovers the sparse value.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Extracting the sparse slice from a clean O\_D value recovers the sparse value.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:6601](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspapercleanodvalue-sparseindex-eq). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperCleanInput_odRegisterValue_eq_cleanODValue" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperCleanInput_odRegisterValue_eq_cleanODValue")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper clean input od register value eq clean od value”; the hypotheses and conclusion in the code panel fix its exact scope. On a clean Lemma 1 source column, the extracted O\_D register is exactly the canonical clean sparse-register value for its sparse slot.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* On a clean Lemma 1 source column, the extracted O\_D register is exactly the canonical clean sparse-register value for its sparse slot. This is the bit-slice reconstruction block needed to lift address injectivity to full paper-image injectivity. It only uses the executable clean-input predicate; it does not prove any semantic cleanup flag.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:6621](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspapercleaninput-odregistervalue-eq-cleanodvalue). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperImage_injective_on_globalSlotSource" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperImage_injective_on_globalSlotSource")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper image injective on global slot source”; the hypotheses and conclusion in the code panel fix its exact scope. The corrected active 'O\_D^BS' paper image is injective on the faithful global-slot clean source domain.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The corrected active 'O\_D^BS' paper image is injective on the faithful global-slot clean source domain. This is a finite-register proof-DAG block, not a semantic-flag promotion. It combines low-prefix preservation, high-tail preservation, the written-address roundtrip, same-row global-address injectivity, and clean O\_D register reconstruction. The obligation records for inverse-on-range, dagger cleanup, and unitarity remain false.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:6695](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspaperimage-injective-on-globalslotsource). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperPostSwapReverseSparse_lt_two_pow" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperPostSwapReverseSparse_lt_two_pow")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper post swap reverse sparse lt two pow”; the hypotheses and conclusion in the code panel fix its exact scope. The reverse sparse index used by the post-SWAP cleanup candidate fits in the three-bit sparse register for the one-term Robin parameter family.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The reverse sparse index used by the post-SWAP cleanup candidate fits in the three-bit sparse register for the one-term Robin parameter family.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:6822](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspaperpostswapreversesparse-lt-two-pow). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperPostSwapCleanODValue_lt_two_pow" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperPostSwapCleanODValue_lt_two_pow")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper post swap clean od value lt two pow”; the hypotheses and conclusion in the code panel fix its exact scope. The clean O\_D register value spliced into the post-SWAP preimage candidate is n-bit for the one-term Robin parameter family.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The clean O\_D register value spliced into the post-SWAP preimage candidate is n-bit for the one-term Robin parameter family.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:6837](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspaperpostswapcleanodvalue-lt-two-pow). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperPostSwapPreimageCandidate" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperPostSwapPreimageCandidate")
*Plain-English reading.* This definition gives the library's named construction or computation for “banded sparse access paper post swap preimage candidate”. Candidate clean preimage for the column reached by 'O\_D^BS', SWAP, and then '(O\_D^BS)^dagger'.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Candidate clean preimage for the column reached by 'O\_D^BS', SWAP, and then '(O\_D^BS)^dagger'. The candidate keeps the post-SWAP row and high-tail bits, and replaces the 'O\_D^BS' register by a clean padded register whose sparse field is the inverse global slot for the original source slot. The separate Boolean audit below checks whether this candidate is actually a paper-image preimage.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:6865](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspaperpostswappreimagecandidate). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperPostSwapPreimageCandidateChecks" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperPostSwapPreimageCandidateChecks")
*Plain-English reading.* This definition gives the library's named construction or computation for “banded sparse access paper post swap preimage candidate checks”. Executable audit for the post-SWAP preimage candidate.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Executable audit for the post-SWAP preimage candidate. It checks three local facts: the candidate maps by the active paper-image skeleton to the post-SWAP column, the candidate is in the clean padded domain, and the candidate address is n-bit. Even when this Boolean is true for a finite parameter scan, the paper-level dagger cleanup and unitarity flags remain unproved.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:6882](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspaperpostswappreimagecandidatechecks). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperPostSwapPreimageCandidateChecks_of_cleanSource" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperPostSwapPreimageCandidateChecks_of_cleanSource")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper post swap preimage candidate checks of clean source”; the hypotheses and conclusion in the code panel fix its exact scope. The post-SWAP preimage candidate passes the executable image, clean-domain, and address-range checks for clean one-term Robin source columns.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The post-SWAP preimage candidate passes the executable image, clean-domain, and address-range checks for clean one-term Robin source columns. The assumptions keep the current Phase 1 contract explicit: the source column is in the finite basis, the source padded register is clean, and the one-term family uses a three-bit sparse register ('kappa = 7', 'clog2 kappa = 3'). This does not prove uniqueness, dagger cleanup, unitarity, or block extraction.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:6899](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspaperpostswappreimagecandidatechecks-of-cleansource). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperPostSwapPreimageCandidate_lt_qubitDim_of_cleanSource" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperPostSwapPreimageCandidate_lt_qubitDim_of_cleanSource")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper post swap preimage candidate lt qubit dim of clean source”; the hypotheses and conclusion in the code panel fix its exact scope. The clean post-SWAP preimage candidate is a finite basis index for finite clean one-term Robin source columns.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The clean post-SWAP preimage candidate is a finite basis index for finite clean one-term Robin source columns. This only discharges the 'Fin' constructor premise for the conditional cleanup witness; uniqueness and semantic cleanup remain open.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:7006](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspaperpostswappreimagecandidate-lt-qubitdim-of-cleansource). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPostSwapCleanup_of_cleanSourceCandidate" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPostSwapCleanup_of_cleanSourceCandidate")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access post swap cleanup of clean source candidate”; the hypotheses and conclusion in the code panel fix its exact scope. Instantiate the conditional post-SWAP cleanup witness with the clean-source preimage candidate.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Instantiate the conditional post-SWAP cleanup witness with the clean-source preimage candidate. The finite 'post' and 'pre' range facts remain explicit hypotheses. This wrapper converts the accepted Boolean candidate audit into the supplied preimage equality, clean-domain proof, and n-bit address bound required by 'bandedSparseAccessPostSwapCleanup\_of\_preimage'. It does not prove finite range, uniqueness, semantic dagger cleanup, or unitarity.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:7055](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspostswapcleanup-of-cleansourcecandidate). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPostSwapCleanup_of_cleanSourceCandidate_noRange" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPostSwapCleanup_of_cleanSourceCandidate_noRange")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access post swap cleanup of clean source candidate no range”; the hypotheses and conclusion in the code panel fix its exact scope. Instantiate the clean-source post-SWAP cleanup witness without caller-supplied finite-range premises.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Instantiate the clean-source post-SWAP cleanup witness without caller-supplied finite-range premises. The theorem only supplies the 'Fin' range proofs for the already conditional candidate witness; it does not prove uniqueness, dagger cleanup, or either O\_D^BS unitarity flag.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:7096](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspostswapcleanup-of-cleansourcecandidate-norange). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPostSwapCleanup_of_validCleanSourceCandidate_noRange" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPostSwapCleanup_of_validCleanSourceCandidate_noRange")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access post swap cleanup of valid clean source candidate no range”; the hypotheses and conclusion in the code panel fix its exact scope. Feed the row-dependent valid-clean-source predicate into the existing post-SWAP cleanup candidate wrapper.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Feed the row-dependent valid-clean-source predicate into the existing post-SWAP cleanup candidate wrapper. The predicate 'bandedSparseAccessPaperValidCleanSource' is only a Phase 1 source-domain classifier. This theorem records that it supplies the clean padded-register hypothesis required by the cleanup candidate. It does not prove source-domain completeness, unused-branch unitary extension, preimage uniqueness, semantic dagger cleanup, or either O\_D^BS unitarity flag.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:7144](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspostswapcleanup-of-validcleansourcecandidate-norange). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPostSwapCleanup_of_globalSlotSourceCandidate_noRange" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPostSwapCleanup_of_globalSlotSourceCandidate_noRange")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access post swap cleanup of global slot source candidate no range”; the hypotheses and conclusion in the code panel fix its exact scope. Feed the faithful global-slot source predicate into the existing post-SWAP cleanup candidate wrapper.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Feed the faithful global-slot source predicate into the existing post-SWAP cleanup candidate wrapper. This is the active-source analogue of the row-dependent valid-clean-source bridge. It only extracts the padded clean-input fact from 'bandedSparseAccessPaperGlobalSlotSource' and reuses the conditional cleanup candidate. It does not prove preimage uniqueness, semantic dagger cleanup, unitarity, LCU correctness, or block extraction.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:7186](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspostswapcleanup-of-globalslotsourcecandidate-norange). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperPostSwapPreimageCandidateChecks_of_globalSlotSource" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperPostSwapPreimageCandidateChecks_of_globalSlotSource")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper post swap preimage candidate checks of global slot source”; the hypotheses and conclusion in the code panel fix its exact scope. The post-SWAP preimage candidate audit is available on the active global-slot source domain.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The post-SWAP preimage candidate audit is available on the active global-slot source domain. This is the global-source wrapper around the existing clean-source arithmetic block. It proves only the executable candidate check; inverse uniqueness, semantic dagger cleanup, and unitarity remain separate obligations.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:7226](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspaperpostswappreimagecandidatechecks-of-globalslotsource). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperPostSwapPreimageCandidate_lt_qubitDim_of_globalSlotSource" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperPostSwapPreimageCandidate_lt_qubitDim_of_globalSlotSource")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper post swap preimage candidate lt qubit dim of global slot source”; the hypotheses and conclusion in the code panel fix its exact scope. The global-source post-SWAP preimage candidate is a finite basis index.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The global-source post-SWAP preimage candidate is a finite basis index. This theorem only names the range premise needed by the conditional cleanup witness. It does not prove that the candidate is unique or that the dagger cleans every in-range image.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:7245](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspaperpostswappreimagecandidate-lt-qubitdim-of-globalslotsource). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperPostSwapPreimageCandidate_sparseIndex_eq" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperPostSwapPreimageCandidate_sparseIndex_eq")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper post swap preimage candidate sparse index eq”; the hypotheses and conclusion in the code panel fix its exact scope. The post-SWAP preimage candidate has the reverse sparse slot in its extracted clean O\_D register.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The post-SWAP preimage candidate has the reverse sparse slot in its extracted clean O\_D register. This names the splice/sparse-slice calculation used by the cleanup-candidate audit so the unique-preimage route can reuse it instead of repeating the bit-level proof.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:7267](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspaperpostswappreimagecandidate-sparseindex-eq). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperPostSwapPreimageCandidate_globalSlotSource_of_globalSlotSource" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperPostSwapPreimageCandidate_globalSlotSource_of_globalSlotSource")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper post swap preimage candidate global slot source of global slot source”; the hypotheses and conclusion in the code panel fix its exact scope. The post-SWAP preimage candidate is itself an active global-slot source.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The post-SWAP preimage candidate is itself an active global-slot source. The proof combines the executable candidate audit for clean input with the named reverse-slot sparse-slice lemma above. It still does not promote the inverse-on-range, uniqueness, dagger-cleanup, or unitarity obligation flags.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:7312](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspaperpostswappreimagecandidate-globalslotsource-of-globalslotsource). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperPostSwapPreimageCandidate_unique_on_globalSlotSource" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessPaperPostSwapPreimageCandidate_unique_on_globalSlotSource")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access paper post swap preimage candidate unique on global slot source”; the hypotheses and conclusion in the code panel fix its exact scope. Uniqueness of the active global-slot clean preimage for the post-SWAP target.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Uniqueness of the active global-slot clean preimage for the post-SWAP target. Any active global-source column that maps by the corrected paper image to the post-SWAP target must be the named reverse-slot preimage candidate. This is a finite basis-index lemma for the dagger-cleanup route; semantic obligation records remain false.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:7360](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesspaperpostswappreimagecandidate-unique-on-globalslotsource). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.BandedSparseAccessGlobalSlotInverseOnRangeContract" (lean := "QuantumBlockEncoding.GHL2025.BandedSparseAccessGlobalSlotInverseOnRangeContract")
*Plain-English reading.* This record groups the data and proof fields needed for “banded sparse access global slot inverse on range contract”. A proposition-valued field is a requirement until a constructor supplies it. Proof-obligation interface for the active global-slot inverse-on-range route.

*Formal status.* Data contract in the default import surface; proposition-valued fields are obligations, not automatically established facts.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Proof-obligation interface for the active global-slot inverse-on-range route. The record fixes the source predicate, image function, post-SWAP target, and candidate preimage used by the corrected 'O\_D^BS' route. The executable 'candidateChecks' field can be proved from 'bandedSparseAccessPaperGlobalSlotSource' by 'bandedSparseAccessPaperPostSwapPreimageCandidateChecks\_of\_globalSlotSource'. The semantic inverse, uniqueness, injectivity, cleanup, and unitary-extension fields remain false in Phase 1.

*Declaration kind.* structure.

Source: [QuantumBlockEncoding/GHL2025.lean:7419](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccessglobalslotinverseonrangecontract). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.bandedSparseAccessGlobalSlotInverseOnRangeContract" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessGlobalSlotInverseOnRangeContract")
*Plain-English reading.* This definition gives the library's named construction or computation for “banded sparse access global slot inverse on range contract”. Default global-source inverse-on-range contract for one 'O\_D^BS' source column.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Default global-source inverse-on-range contract for one 'O\_D^BS' source column. This records the fixed route for future proof work without promoting any semantic flag or changing the active forward/dagger matrices.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:7443](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccessglobalslotinverseonrangecontract). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessGlobalSlotInverseOnRangeContract_flags_false" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessGlobalSlotInverseOnRangeContract_flags_false")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access global slot inverse on range contract flags false”; the hypotheses and conclusion in the code panel fix its exact scope. The global-source inverse-on-range contract is obligation-only in Phase 1.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The global-source inverse-on-range contract is obligation-only in Phase 1.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:7485](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccessglobalslotinverseonrangecontract-flags-false). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessGlobalSlotInverseOnRangeContract_of_globalSlotSource" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessGlobalSlotInverseOnRangeContract_of_globalSlotSource")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access global slot inverse on range contract of global slot source”; the hypotheses and conclusion in the code panel fix its exact scope. Global-source columns feed the fixed inverse-on-range interface and satisfy the executable candidate audit.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Global-source columns feed the fixed inverse-on-range interface and satisfy the executable candidate audit. The final fields remain false: this theorem does not assert uniqueness, injectivity, semantic dagger cleanup, or unitarity.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:7506](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccessglobalslotinverseonrangecontract-of-globalslotsource). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessGlobalSlotInverseOnRangeContract_uniquePreimageBridge" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessGlobalSlotInverseOnRangeContract_uniquePreimageBridge")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access global slot inverse on range contract unique preimage bridge”; the hypotheses and conclusion in the code panel fix its exact scope. Record-level bridge from the compiled post-SWAP unique-preimage theorem to the global-slot inverse-on-range contract.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Record-level bridge from the compiled post-SWAP unique-preimage theorem to the global-slot inverse-on-range contract. This theorem reflects the finite basis-index evidence in the contract fields: any active global-source preimage of the contract's post-SWAP target is the contract's candidate preimage. It deliberately keeps every semantic obligation flag in the contract false; dagger cleanup and unitarity remain separate Phase 1 obligations.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:7541](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccessglobalslotinverseonrangecontract-uniquepreimagebridge). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessGlobalSlotInverseOnRangeContract_daggerCleanupBridge" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessGlobalSlotInverseOnRangeContract_daggerCleanupBridge")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access global slot inverse on range contract dagger cleanup bridge”; the hypotheses and conclusion in the code panel fix its exact scope. Bridge the global-slot inverse-on-range contract to the concrete post-SWAP dagger cleanup witness.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Bridge the global-slot inverse-on-range contract to the concrete post-SWAP dagger cleanup witness. This theorem constructs the finite post-SWAP column and the named preimage candidate from the active global-source route, then reuses 'BandedSparseAccessPostSwapCleanup' to expose the transpose-style dagger entry and executable register checks. It deliberately keeps the semantic 'daggerCleanup' and 'unitaryExtension' flags false.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:7608](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccessglobalslotinverseonrangecontract-daggercleanupbridge). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessGlobalSlotInverseOnRangeContract_cleanupContractMap" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessGlobalSlotInverseOnRangeContract_cleanupContractMap")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access global slot inverse on range contract cleanup contract map”; the hypotheses and conclusion in the code panel fix its exact scope. Reviewed cleanup-contract map for the active global-slot 'O\_D^BS' route.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Reviewed cleanup-contract map for the active global-slot 'O\_D^BS' route. This wrapper is intentionally non-promoting. It combines the compiled post-SWAP cleanup witness with the record-level unique-preimage bridge, so later cleanup work can consume one theorem exposing the contract target, candidate preimage, active-source uniqueness, and transpose-style dagger entry. The semantic inverse, uniqueness, injectivity, cleanup, and unitary-extension obligation flags remain false.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:7681](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccessglobalslotinverseonrangecontract-cleanupcontractmap). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.defaultBandedSparseAccessPaperContract_cleanupRouteBridge" (lean := "QuantumBlockEncoding.GHL2025.defaultBandedSparseAccessPaperContract_cleanupRouteBridge")
*Plain-English reading.* Lean checks the proposition indexed as “default banded sparse access paper contract cleanup route bridge”; the hypotheses and conclusion in the code panel fix its exact scope. Default-paper-contract cleanup-route bridge for the active global-slot 'O\_D^BS' route.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Default-paper-contract cleanup-route bridge for the active global-slot 'O\_D^BS' route. This theorem ties the compiled cleanup-contract map back to 'defaultBandedSparseAccessPaperContract p'. It exposes the post-SWAP cleanup witness, active-source uniqueness, and transpose-style dagger entry while recording that the paper-contract cleanup and unitary-extension flags, and the two active 'O\_D^BS' gate-unitarity flags, remain false.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:7775](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-defaultbandedsparseaccesspapercontract-cleanuproutebridge). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessGlobalSlotInverseOnRangeContract_daggerOffCandidate_zero" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessGlobalSlotInverseOnRangeContract_daggerOffCandidate_zero")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access global slot inverse on range contract dagger off candidate zero”; the hypotheses and conclusion in the code panel fix its exact scope. Off-candidate dagger entries are zero on the active global-slot source domain.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Off-candidate dagger entries are zero on the active global-slot source domain. For the fixed post-SWAP target in the global-slot inverse-on-range contract, any other active global-source preimage whose index is not the named candidate cannot have a transpose-style '(O\_D^BS)^†' entry into that target. This is a matrix-entry bridge only: it does not promote dagger cleanup or unitary flags.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:7820](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccessglobalslotinverseonrangecontract-daggeroffcandidate-zero). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessGlobalSlotInverseOnRangeContract_restrictedDaggerColumnCleanup" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessGlobalSlotInverseOnRangeContract_restrictedDaggerColumnCleanup")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access global slot inverse on range contract restricted dagger column cleanup”; the hypotheses and conclusion in the code panel fix its exact scope. Restricted active-domain dagger-column cleanup for the global-slot route.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Restricted active-domain dagger-column cleanup for the global-slot route. For the contract post-SWAP target, the named candidate has dagger entry '1', and every other active global-source row has dagger entry '0'. This is only a column statement over 'bandedSparseAccessPaperGlobalSlotSource'; it does not promote semantic cleanup, unitarity, circuit-unitarity, or block-extraction obligations.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:7883](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccessglobalslotinverseonrangecontract-restricteddaggercolumncleanup). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessGlobalSlotInverseOnRangeContract_restrictedDaggerColumnIndicator" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessGlobalSlotInverseOnRangeContract_restrictedDaggerColumnIndicator")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access global slot inverse on range contract restricted dagger column indicator”; the hypotheses and conclusion in the code panel fix its exact scope. Indicator form of the restricted active-domain dagger column.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Indicator form of the restricted active-domain dagger column. This is the same active-source-only column statement as 'bandedSparseAccessGlobalSlotInverseOnRangeContract\_restrictedDaggerColumnCleanup', rewritten as one if-then-else formula. It does not promote inverse, cleanup, unitarity, circuit-unitarity, or block-extraction obligations.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:7972](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccessglobalslotinverseonrangecontract-restricteddaggercolumnindicator). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.BandedSparseAccessCleanupScope" (lean := "QuantumBlockEncoding.GHL2025.BandedSparseAccessCleanupScope")
*Plain-English reading.* This type lists the allowed alternatives for “banded sparse access cleanup scope”; its constructors are the cases that downstream code must handle. Allowed scopes for the next 'O\_D^BS' cleanup theorem packet.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Allowed scopes for the next 'O\_D^BS' cleanup theorem packet. This is planning data for Phase 1 faithful-paper work. Selecting a scope here does not prove cleanup, full-domain injectivity, or unitary extension.

*Declaration kind.* inductive.

Source: [QuantumBlockEncoding/GHL2025.lean:8059](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesscleanupscope). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.BandedSparseAccessCleanupScopeDecision" (lean := "QuantumBlockEncoding.GHL2025.BandedSparseAccessCleanupScopeDecision")
*Plain-English reading.* This record groups the data and proof fields needed for “banded sparse access cleanup scope decision”. A proposition-valued field is a requirement until a constructor supplies it. Non-promoting decision for the next 'O\_D^BS' cleanup theorem domain.

*Formal status.* Data contract in the default import surface; proposition-valued fields are obligations, not automatically established facts.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Non-promoting decision for the next 'O\_D^BS' cleanup theorem domain. The current compiled matrix-entry theorem is restricted to active global-source rows. Full clean-domain cleanup still needs a reversible image rule for every clean unused sparse branch, and full-space cleanup/unitarity still needs a separate reversible-extension argument. This record keeps that boundary machine-checkable before any lower proof packet tries to promote 'daggerCleanup'.

*Declaration kind.* structure.

Source: [QuantumBlockEncoding/GHL2025.lean:8075](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesscleanupscopedecision). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GHL2025.bandedSparseAccessCleanupScopeDecision" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessCleanupScopeDecision")
*Plain-English reading.* This definition gives the library's named construction or computation for “banded sparse access cleanup scope decision”. Default cleanup-scope decision after the restricted dagger-column indicator.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Default cleanup-scope decision after the restricted dagger-column indicator. The selected theorem domain is active global-source only. The surrounding obligation records are copied from the existing paper and full-domain contracts so their 'proved = false' status stays synchronized with the actual contracts.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GHL2025.lean:8095](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesscleanupscopedecision). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessCleanupScopeDecision_activeGlobalSource" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessCleanupScopeDecision_activeGlobalSource")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access cleanup scope decision active global source”; the hypotheses and conclusion in the code panel fix its exact scope. The cleanup-scope decision selects the active global-source theorem and keeps all broader cleanup/unitarity obligations closed to proof-flag promotion.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The cleanup-scope decision selects the active global-source theorem and keeps all broader cleanup/unitarity obligations closed to proof-flag promotion.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:8116](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesscleanupscopedecision-activeglobalsource). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessCleanupScopeDecision_priorPDESourceTranscriptGuard" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessCleanupScopeDecision_priorPDESourceTranscriptGuard")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access cleanup scope decision prior pde source transcript guard”; the hypotheses and conclusion in the code panel fix its exact scope. The cleanup-scope decision does not accept the prior PDE sparse-access transcript as a full-space unitary-extension proof.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The cleanup-scope decision does not accept the prior PDE sparse-access transcript as a full-space unitary-extension proof. This is a guard for the next Phase 1 source-contract packet: the prior paper's equation is recorded as a source anchor, while its resource proof and any Robin-specific reversible-extension use remain unproved in QBE.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:8147](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesscleanupscopedecision-priorpdesourcetranscriptguard). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.bandedSparseAccessCleanupScopeDecision_fullCleanDomainImageRuleBlocked" (lean := "QuantumBlockEncoding.GHL2025.bandedSparseAccessCleanupScopeDecision_fullCleanDomainImageRuleBlocked")
*Plain-English reading.* Lean checks the proposition indexed as “banded sparse access cleanup scope decision full clean domain image rule blocked”; the hypotheses and conclusion in the code panel fix its exact scope. The cleanup-scope decision keeps the full clean-domain image-rule slot blocked.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The cleanup-scope decision keeps the full clean-domain image-rule slot blocked. This guard ties the active-global-source scope choice to the unused-branch source decision and the full clean-domain wrapper. It is not a cleanup or unitarity theorem: the missing image rule remains 'none', proof search remains disabled, and every full clean-domain semantic field stays false.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:8177](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-bandedsparseaccesscleanupscopedecision-fullcleandomainimageruleblocked). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.defaultBandedSparseAccessPaperContract_cleanupRouteBridge_boundaryColumn_n3" (lean := "QuantumBlockEncoding.GHL2025.defaultBandedSparseAccessPaperContract_cleanupRouteBridge_boundaryColumn_n3")
*Plain-English reading.* Lean checks the proposition indexed as “default banded sparse access paper contract cleanup route bridge boundary column n 3”; the hypotheses and conclusion in the code panel fix its exact scope. Concrete boundary-source regression for the default paper-contract cleanup route.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Concrete boundary-source regression for the default paper-contract cleanup route. The historical source column '48' is outside the rejected row-dependent valid source predicate, but it is an active global-slot source for Lemma 1. This instance routes that column through the default cleanup bridge without promoting any 'O\_D^BS' semantic proof flag.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:8216](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-defaultbandedsparseaccesspapercontract-cleanuproutebridge-boundarycolumn-n3). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.robinIndicatorBitPosition_ge" (lean := "QuantumBlockEncoding.GHL2025.robinIndicatorBitPosition_ge")
*Plain-English reading.* Lean checks the proposition indexed as “robin indicator bit position ge”; the hypotheses and conclusion in the code panel fix its exact scope. Cycle 12: robinIndicatorBitPosition = 1 + 2\*p.n, hence >= 1 + p.n.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Cycle 12: robinIndicatorBitPosition = 1 + 2\*p.n, hence >= 1 + p.n.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:8265](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-robinindicatorbitposition-ge). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.indicatorOracleImage_systemVal_preserved" (lean := "QuantumBlockEncoding.GHL2025.indicatorOracleImage_systemVal_preserved")
*Plain-English reading.* Lean checks the proposition indexed as “indicator oracle image system val preserved”; the hypotheses and conclusion in the code panel fix its exact scope. Cycle 12: The system register value is preserved by indicatorOracleImage.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Cycle 12: The system register value is preserved by indicatorOracleImage. XORing with a bit at position indPos = 1 + 2n does not affect bits \[1, 1+n). main.tex:1088-1099 -

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:8275](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-indicatororacleimage-systemval-preserved). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.indicatorOracleImage_isBulk_preserved" (lean := "QuantumBlockEncoding.GHL2025.indicatorOracleImage_isBulk_preserved")
*Plain-English reading.* Lean checks the proposition indexed as “indicator oracle image is bulk preserved”; the hypotheses and conclusion in the code panel fix its exact scope. Cycle 12: The isBulk predicate gives the same result for j and indicatorOracleImage p j, because isBulk only depends on the system register value, which is preserved.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Cycle 12: The isBulk predicate gives the same result for j and indicatorOracleImage p j, because isBulk only depends on the system register value, which is preserved. main.tex:1088-1099 -

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:8289](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-indicatororacleimage-isbulk-preserved). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.indicatorOracleImage_self_inverse" (lean := "QuantumBlockEncoding.GHL2025.indicatorOracleImage_self_inverse")
*Plain-English reading.* Lean checks the proposition indexed as “indicator oracle image self inverse”; the hypotheses and conclusion in the code panel fix its exact scope. Cycle 12: General self-inverse property for indicatorOracleImage.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Cycle 12: General self-inverse property for indicatorOracleImage. Applying the indicator oracle image twice returns the original value for all j, because the indicator bit is XORed twice (and isBulk is preserved). main.tex:1088-1099 -

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:8304](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-indicatororacleimage-self-inverse). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.oneTermRobinGate_U_indic_dagger_selfInverseBridge" (lean := "QuantumBlockEncoding.GHL2025.oneTermRobinGate_U_indic_dagger_selfInverseBridge")
*Plain-English reading.* Lean checks the proposition indexed as “one term robin gate u indic dagger self inverse bridge”; the hypotheses and conclusion in the code panel fix its exact scope. Source-facing bridge for the explicit 'U\_indic^dagger' transcript slot.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Source-facing bridge for the explicit 'U\_indic^dagger' transcript slot. The dagger slot uses the same matrix as 'U\_indic' because the underlying indicator image is self-inverse. This theorem records the bridge used by the conversion window; it does not change the active backend gate list.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:8319](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-onetermrobingate-u-indic-dagger-selfinversebridge). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.indicatorOracleImage_injective" (lean := "QuantumBlockEncoding.GHL2025.indicatorOracleImage_injective")
*Plain-English reading.* Lean checks the proposition indexed as “indicator oracle image injective”; the hypotheses and conclusion in the code panel fix its exact scope. Cycle 12: General injectivity for indicatorOracleImage, derived from self-inverse.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Cycle 12: General injectivity for indicatorOracleImage, derived from self-inverse. main.tex:1088-1099 -

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:8330](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-indicatororacleimage-injective). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.robinIndicatorBitPosition_lt_totalQubits" (lean := "QuantumBlockEncoding.GHL2025.robinIndicatorBitPosition_lt_totalQubits")
*Plain-English reading.* Lean checks the proposition indexed as “robin indicator bit position lt total qubits”; the hypotheses and conclusion in the code panel fix its exact scope. Cycle 12: robinIndicatorBitPosition is strictly below oneTermRobinTotalQubits.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Cycle 12: robinIndicatorBitPosition is strictly below oneTermRobinTotalQubits. indPos = 1 + 2n < 2n + clog2 n + clog2 fp + 5 = totalQubits, since clog2 ≥ 0 and 5 > 1.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:8341](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-robinindicatorbitposition-lt-totalqubits). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.indicatorOracleImage_lt" (lean := "QuantumBlockEncoding.GHL2025.indicatorOracleImage_lt")
*Plain-English reading.* Lean checks the proposition indexed as “indicator oracle image lt”; the hypotheses and conclusion in the code panel fix its exact scope. Cycle 12: indicatorOracleImage preserves the qubitDim bound.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Cycle 12: indicatorOracleImage preserves the qubitDim bound. When j < 2^totalQubits, the image is also < 2^totalQubits, because the XOR operand is either 0 or a single bit at position indPos < totalQubits.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:8352](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-indicatororacleimage-lt). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.indicatorOracleImage_bijective" (lean := "QuantumBlockEncoding.GHL2025.indicatorOracleImage_bijective")
*Plain-English reading.* Lean checks the proposition indexed as “indicator oracle image bijective”; the hypotheses and conclusion in the code panel fix its exact scope. Cycle 12: Bijectivity of indicatorOracleImage on the Fin domain.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Cycle 12: Bijectivity of indicatorOracleImage on the Fin domain. A self-inverse function on a finite type is bijective: injective by cancellation, surjective because image(image(j)) = j. main.tex:1088-1099 -

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:8370](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-indicatororacleimage-bijective). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.indicatorOracleMatrix_col_has_one" (lean := "QuantumBlockEncoding.GHL2025.indicatorOracleMatrix_col_has_one")
*Plain-English reading.* Lean checks the proposition indexed as “indicator oracle matrix col has one”; the hypotheses and conclusion in the code panel fix its exact scope. Cycle 12: For each column j, there is exactly one row i with M\[i\]\[j\] = 1, namely i = ⟨indicatorOracleImage p j.val, ...⟩.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Cycle 12: For each column j, there is exactly one row i with M\[i\]\[j\] = 1, namely i = ⟨indicatorOracleImage p j.val, ...⟩. main.tex:1088-1099 -

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:8393](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-indicatororaclematrix-col-has-one). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.indicatorOracleMatrix_col_unique" (lean := "QuantumBlockEncoding.GHL2025.indicatorOracleMatrix_col_unique")
*Plain-English reading.* Lean checks the proposition indexed as “indicator oracle matrix col unique”; the hypotheses and conclusion in the code panel fix its exact scope. Cycle 12: For each column j, any row i with M\[i\]\[j\] = 1 must equal ⟨indicatorOracleImage p j.val, ...⟩, so the 1-entry is unique per column.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Cycle 12: For each column j, any row i with M\[i\]\[j\] = 1 must equal ⟨indicatorOracleImage p j.val, ...⟩, so the 1-entry is unique per column. main.tex:1088-1099 -

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:8405](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-indicatororaclematrix-col-unique). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.indicatorOracleMatrix_row_has_one" (lean := "QuantumBlockEncoding.GHL2025.indicatorOracleMatrix_row_has_one")
*Plain-English reading.* Lean checks the proposition indexed as “indicator oracle matrix row has one”; the hypotheses and conclusion in the code panel fix its exact scope. Cycle 12: For each row i, there exists a column j with M\[i\]\[j\] = 1, from surjectivity of indicatorOracleImage.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Cycle 12: For each row i, there exists a column j with M\[i\]\[j\] = 1, from surjectivity of indicatorOracleImage. main.tex:1088-1099 -

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:8418](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-indicatororaclematrix-row-has-one). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.indicatorOracleMatrix_row_unique" (lean := "QuantumBlockEncoding.GHL2025.indicatorOracleMatrix_row_unique")
*Plain-English reading.* Lean checks the proposition indexed as “indicator oracle matrix row unique”; the hypotheses and conclusion in the code panel fix its exact scope. Cycle 12: For each row i, the column j with M\[i\]\[j\] = 1 is unique, from injectivity of indicatorOracleImage.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Cycle 12: For each row i, the column j with M\[i\]\[j\] = 1 is unique, from injectivity of indicatorOracleImage. main.tex:1088-1099 -

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:8436](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-indicatororaclematrix-row-unique). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GHL2025.indicatorOracleMatrix_is_permutation" (lean := "QuantumBlockEncoding.GHL2025.indicatorOracleMatrix_is_permutation")
*Plain-English reading.* Lean checks the proposition indexed as “indicator oracle matrix is permutation”; the hypotheses and conclusion in the code panel fix its exact scope. Cycle 12: indicatorOracleMatrix is a permutation matrix: each row has exactly one entry equal to 1, and each column has exactly one entry equal to 1.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Cycle 12: indicatorOracleMatrix is a permutation matrix: each row has exactly one entry equal to 1, and each column has exactly one entry equal to 1. This follows from indicatorOracleImage being a bijection on the Fin domain. main.tex:1088-1099 -

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GHL2025.lean:8452](../../../../library/modules/ghl2025/#decl-quantumblockencoding-ghl2025-indicatororaclematrix-is-permutation). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/Robin/ComplexLCU.lean

29 explicit public declarations, in source order.

:::definition "QuantumBlockEncoding.Robin.ComplexLCU.realOrthogonalRotation" (lean := "QuantumBlockEncoding.Robin.ComplexLCU.realOrthogonalRotation")
*Plain-English reading.* This definition gives the library's named construction or computation for “real orthogonal rotation”. A real planar rotation with explicit cosine and sine entries, embedded in 'ℂ'.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* A real planar rotation with explicit cosine and sine entries, embedded in 'ℂ'.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/ComplexLCU.lean:23](../../../../library/modules/robin-complexlcu/#decl-quantumblockencoding-robin-complexlcu-realorthogonalrotation). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.ComplexLCU.realOrthogonalRotation_unitary" (lean := "QuantumBlockEncoding.Robin.ComplexLCU.realOrthogonalRotation_unitary")
*Plain-English reading.* Lean checks the proposition indexed as “real orthogonal rotation unitary”; the hypotheses and conclusion in the code panel fix its exact scope. A real planar rotation is unitary whenever its two entries lie on the unit circle.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* A real planar rotation is unitary whenever its two entries lie on the unit circle.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/ComplexLCU.lean:32](../../../../library/modules/robin-complexlcu/#decl-quantumblockencoding-robin-complexlcu-realorthogonalrotation-unitary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.ComplexLCU.realRotation" (lean := "QuantumBlockEncoding.Robin.ComplexLCU.realRotation")
*Plain-English reading.* This definition gives the library's named construction or computation for “real rotation”. A real planar rotation, parameterized by an angle.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* A real planar rotation, parameterized by an angle.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/ComplexLCU.lean:60](../../../../library/modules/robin-complexlcu/#decl-quantumblockencoding-robin-complexlcu-realrotation). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.ComplexLCU.realRotation_zero_zero" (lean := "QuantumBlockEncoding.Robin.ComplexLCU.realRotation_zero_zero")
*Plain-English reading.* Lean checks the proposition indexed as “real rotation zero zero”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/ComplexLCU.lean:64](../../../../library/modules/robin-complexlcu/#decl-quantumblockencoding-robin-complexlcu-realrotation-zero-zero). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.ComplexLCU.realRotation_zero_one" (lean := "QuantumBlockEncoding.Robin.ComplexLCU.realRotation_zero_one")
*Plain-English reading.* Lean checks the proposition indexed as “real rotation zero one”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/ComplexLCU.lean:68](../../../../library/modules/robin-complexlcu/#decl-quantumblockencoding-robin-complexlcu-realrotation-zero-one). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.ComplexLCU.realRotation_one_zero" (lean := "QuantumBlockEncoding.Robin.ComplexLCU.realRotation_one_zero")
*Plain-English reading.* Lean checks the proposition indexed as “real rotation one zero”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/ComplexLCU.lean:72](../../../../library/modules/robin-complexlcu/#decl-quantumblockencoding-robin-complexlcu-realrotation-one-zero). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.ComplexLCU.realRotation_one_one" (lean := "QuantumBlockEncoding.Robin.ComplexLCU.realRotation_one_one")
*Plain-English reading.* Lean checks the proposition indexed as “real rotation one one”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/ComplexLCU.lean:76](../../../../library/modules/robin-complexlcu/#decl-quantumblockencoding-robin-complexlcu-realrotation-one-one). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.ComplexLCU.realRotation_unitary" (lean := "QuantumBlockEncoding.Robin.ComplexLCU.realRotation_unitary")
*Plain-English reading.* Lean checks the proposition indexed as “real rotation unitary”; the hypotheses and conclusion in the code panel fix its exact scope. Every real planar rotation is unitary over 'ℂ'.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Every real planar rotation is unitary over 'ℂ'.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/ComplexLCU.lean:81](../../../../library/modules/robin-complexlcu/#decl-quantumblockencoding-robin-complexlcu-realrotation-unitary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.ComplexLCU.amplitudeRotation" (lean := "QuantumBlockEncoding.Robin.ComplexLCU.amplitudeRotation")
*Plain-English reading.* This definition gives the library's named construction or computation for “amplitude rotation”. Rotation whose clean entry is intended to encode 'coefficient'.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Rotation whose clean entry is intended to encode 'coefficient'.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/ComplexLCU.lean:88](../../../../library/modules/robin-complexlcu/#decl-quantumblockencoding-robin-complexlcu-amplituderotation). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.ComplexLCU.amplitudeRotation_unitary" (lean := "QuantumBlockEncoding.Robin.ComplexLCU.amplitudeRotation_unitary")
*Plain-English reading.* Lean checks the proposition indexed as “amplitude rotation unitary”; the hypotheses and conclusion in the code panel fix its exact scope. The amplitude rotation is unitary without any domain hypothesis.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The amplitude rotation is unitary without any domain hypothesis.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/ComplexLCU.lean:93](../../../../library/modules/robin-complexlcu/#decl-quantumblockencoding-robin-complexlcu-amplituderotation-unitary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.ComplexLCU.amplitudeRotation_cleanEntry" (lean := "QuantumBlockEncoding.Robin.ComplexLCU.amplitudeRotation_cleanEntry")
*Plain-English reading.* Lean checks the proposition indexed as “amplitude rotation clean entry”; the hypotheses and conclusion in the code panel fix its exact scope. Under the standard arccos domain, the clean entry is exactly the coefficient.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Under the standard arccos domain, the clean entry is exactly the coefficient.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/ComplexLCU.lean:99](../../../../library/modules/robin-complexlcu/#decl-quantumblockencoding-robin-complexlcu-amplituderotation-cleanentry). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.ComplexLCU.reindex_unitary" (lean := "QuantumBlockEncoding.Robin.ComplexLCU.reindex_unitary")
*Plain-English reading.* Lean checks the proposition indexed as “reindex unitary”; the hypotheses and conclusion in the code panel fix its exact scope. Reindexing rows and columns by the same equivalence preserves unitarity.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Reindexing rows and columns by the same equivalence preserves unitarity.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/ComplexLCU.lean:105](../../../../library/modules/robin-complexlcu/#decl-quantumblockencoding-robin-complexlcu-reindex-unitary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.ComplexLCU.blockDiagonal_unitary" (lean := "QuantumBlockEncoding.Robin.ComplexLCU.blockDiagonal_unitary")
*Plain-English reading.* Lean checks the proposition indexed as “block diagonal unitary”; the hypotheses and conclusion in the code panel fix its exact scope. A finite family of unitary blocks is unitary when placed block-diagonally.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* A finite family of unitary blocks is unitary when placed block-diagonally.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/ComplexLCU.lean:122](../../../../library/modules/robin-complexlcu/#decl-quantumblockencoding-robin-complexlcu-blockdiagonal-unitary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.ComplexLCU.equivPermutationMatrix" (lean := "QuantumBlockEncoding.Robin.ComplexLCU.equivPermutationMatrix")
*Plain-English reading.* This definition gives the library's named construction or computation for “equiv permutation matrix”. Matrix of a finite basis permutation.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Matrix of a finite basis permutation.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/ComplexLCU.lean:148](../../../../library/modules/robin-complexlcu/#decl-quantumblockencoding-robin-complexlcu-equivpermutationmatrix). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.ComplexLCU.equivPermutationMatrix_unitary" (lean := "QuantumBlockEncoding.Robin.ComplexLCU.equivPermutationMatrix_unitary")
*Plain-English reading.* Lean checks the proposition indexed as “equiv permutation matrix unitary”; the hypotheses and conclusion in the code panel fix its exact scope. Every equivalence induces a unitary permutation matrix.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Every equivalence induces a unitary permutation matrix.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/ComplexLCU.lean:154](../../../../library/modules/robin-complexlcu/#decl-quantumblockencoding-robin-complexlcu-equivpermutationmatrix-unitary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.ComplexLCU.equivPermutationMatrix_mul_apply" (lean := "QuantumBlockEncoding.Robin.ComplexLCU.equivPermutationMatrix_mul_apply")
*Plain-English reading.* Lean checks the proposition indexed as “equiv permutation matrix mul apply”; the hypotheses and conclusion in the code panel fix its exact scope. Multiplication by a permutation matrix applies the inverse permutation to rows.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Multiplication by a permutation matrix applies the inverse permutation to rows.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/ComplexLCU.lean:167](../../../../library/modules/robin-complexlcu/#decl-quantumblockencoding-robin-complexlcu-equivpermutationmatrix-mul-apply). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.ComplexLCU.mul_equivPermutationMatrix_apply" (lean := "QuantumBlockEncoding.Robin.ComplexLCU.mul_equivPermutationMatrix_apply")
*Plain-English reading.* Lean checks the proposition indexed as “mul equiv permutation matrix apply”; the hypotheses and conclusion in the code panel fix its exact scope. Multiplication on the right by a permutation matrix applies the permutation to columns.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Multiplication on the right by a permutation matrix applies the permutation to columns.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/ComplexLCU.lean:191](../../../../library/modules/robin-complexlcu/#decl-quantumblockencoding-robin-complexlcu-mul-equivpermutationmatrix-apply). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.ComplexLCU.equivPermutationMatrix_mul" (lean := "QuantumBlockEncoding.Robin.ComplexLCU.equivPermutationMatrix_mul")
*Plain-English reading.* Lean checks the proposition indexed as “equiv permutation matrix mul”; the hypotheses and conclusion in the code panel fix its exact scope. Chronological composition of finite basis permutations.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Chronological composition of finite basis permutations.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/ComplexLCU.lean:206](../../../../library/modules/robin-complexlcu/#decl-quantumblockencoding-robin-complexlcu-equivpermutationmatrix-mul). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.ComplexLCU.equivPermutationMatrix_conjugates_reindex" (lean := "QuantumBlockEncoding.Robin.ComplexLCU.equivPermutationMatrix_conjugates_reindex")
*Plain-English reading.* Lean checks the proposition indexed as “equiv permutation matrix conjugates reindex”; the hypotheses and conclusion in the code panel fix its exact scope. Conjugating a reindexed operator by an involutive basis permutation is the same as composing that permutation into the indexing equivalence.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Conjugating a reindexed operator by an involutive basis permutation is the same as composing that permutation into the indexing equivalence.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/ComplexLCU.lean:227](../../../../library/modules/robin-complexlcu/#decl-quantumblockencoding-robin-complexlcu-equivpermutationmatrix-conjugates-reindex). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.ComplexLCU.LCUIndex" (lean := "QuantumBlockEncoding.Robin.ComplexLCU.LCUIndex")
*Plain-English reading.* This abbreviation gives a shorter name to the type or expression used for “lcu index”. Product-register index for coefficient, selector, and system registers.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Product-register index for coefficient, selector, and system registers.

*Declaration kind.* abbrev.

Source: [QuantumBlockEncoding/Robin/ComplexLCU.lean:250](../../../../library/modules/robin-complexlcu/#decl-quantumblockencoding-robin-complexlcu-lcuindex). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.ComplexLCU.selectorLift" (lean := "QuantumBlockEncoding.Robin.ComplexLCU.selectorLift")
*Plain-English reading.* This definition gives the library's named construction or computation for “selector lift”. Lift selector PREPARE to coefficient × selector × system.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Lift selector PREPARE to coefficient × selector × system.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/ComplexLCU.lean:254](../../../../library/modules/robin-complexlcu/#decl-quantumblockencoding-robin-complexlcu-selectorlift). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.ComplexLCU.selectorLift_unitary" (lean := "QuantumBlockEncoding.Robin.ComplexLCU.selectorLift_unitary")
*Plain-English reading.* Lean checks the proposition indexed as “selector lift unitary”; the hypotheses and conclusion in the code panel fix its exact scope. A unitary selector PREPARE remains unitary after tensoring with identities.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* A unitary selector PREPARE remains unitary after tensoring with identities.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/ComplexLCU.lean:267](../../../../library/modules/robin-complexlcu/#decl-quantumblockencoding-robin-complexlcu-selectorlift-unitary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.ComplexLCU.amplitudeLift" (lean := "QuantumBlockEncoding.Robin.ComplexLCU.amplitudeLift")
*Plain-English reading.* This definition gives the library's named construction or computation for “amplitude lift”. Lift a coefficient-unitary family controlled by selector and system.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Lift a coefficient-unitary family controlled by selector and system.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/ComplexLCU.lean:284](../../../../library/modules/robin-complexlcu/#decl-quantumblockencoding-robin-complexlcu-amplitudelift). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.ComplexLCU.amplitudeLift_unitary" (lean := "QuantumBlockEncoding.Robin.ComplexLCU.amplitudeLift_unitary")
*Plain-English reading.* Lean checks the proposition indexed as “amplitude lift unitary”; the hypotheses and conclusion in the code panel fix its exact scope. A controlled family of unitary amplitude blocks is unitary.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* A controlled family of unitary amplitude blocks is unitary.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/ComplexLCU.lean:297](../../../../library/modules/robin-complexlcu/#decl-quantumblockencoding-robin-complexlcu-amplitudelift-unitary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.ComplexLCU.controlledSystemEquiv" (lean := "QuantumBlockEncoding.Robin.ComplexLCU.controlledSystemEquiv")
*Plain-English reading.* This definition gives the library's named construction or computation for “controlled system equiv”. SELECT equivalence preserving coefficient and selector and permuting the system.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* SELECT equivalence preserving coefficient and selector and permuting the system.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/ComplexLCU.lean:315](../../../../library/modules/robin-complexlcu/#decl-quantumblockencoding-robin-complexlcu-controlledsystemequiv). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.ComplexLCU.selectLift" (lean := "QuantumBlockEncoding.Robin.ComplexLCU.selectLift")
*Plain-English reading.* This definition gives the library's named construction or computation for “select lift”. Logical SELECT matrix for a family of system permutations.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Logical SELECT matrix for a family of system permutations.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/ComplexLCU.lean:330](../../../../library/modules/robin-complexlcu/#decl-quantumblockencoding-robin-complexlcu-selectlift). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.ComplexLCU.selectLift_unitary" (lean := "QuantumBlockEncoding.Robin.ComplexLCU.selectLift_unitary")
*Plain-English reading.* Lean checks the proposition indexed as “select lift unitary”; the hypotheses and conclusion in the code panel fix its exact scope. SELECT is unitary because it is a basis permutation.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* SELECT is unitary because it is a basis permutation.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/ComplexLCU.lean:340](../../../../library/modules/robin-complexlcu/#decl-quantumblockencoding-robin-complexlcu-selectlift-unitary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.ComplexLCU.prepareAmplitudeSelectUnprepare" (lean := "QuantumBlockEncoding.Robin.ComplexLCU.prepareAmplitudeSelectUnprepare")
*Plain-English reading.* This definition gives the library's named construction or computation for “prepare amplitude select unprepare”. PREPARE → amplitude → SELECT → unprepare logical matrix.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* PREPARE → amplitude → SELECT → unprepare logical matrix.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/ComplexLCU.lean:352](../../../../library/modules/robin-complexlcu/#decl-quantumblockencoding-robin-complexlcu-prepareamplitudeselectunprepare). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.ComplexLCU.prepareAmplitudeSelectUnprepare_unitary" (lean := "QuantumBlockEncoding.Robin.ComplexLCU.prepareAmplitudeSelectUnprepare_unitary")
*Plain-English reading.* Lean checks the proposition indexed as “prepare amplitude select unprepare unitary”; the hypotheses and conclusion in the code panel fix its exact scope. The complete logical LCU composition is unitary from its local certificates.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The complete logical LCU composition is unitary from its local certificates.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/ComplexLCU.lean:370](../../../../library/modules/robin-complexlcu/#decl-quantumblockencoding-robin-complexlcu-prepareamplitudeselectunprepare-unitary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/Robin/ComplexLCUProjection.lean

8 explicit public declarations, in source order.

:::theorem "QuantumBlockEncoding.Robin.ComplexLCU.amplitudeLift_apply" (lean := "QuantumBlockEncoding.Robin.ComplexLCU.amplitudeLift_apply")
*Plain-English reading.* Lean checks the proposition indexed as “amplitude lift apply”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/ComplexLCUProjection.lean:17](../../../../library/modules/robin-complexlcuprojection/#decl-quantumblockencoding-robin-complexlcu-amplitudelift-apply). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.ComplexLCU.selectLift_apply" (lean := "QuantumBlockEncoding.Robin.ComplexLCU.selectLift_apply")
*Plain-English reading.* Lean checks the proposition indexed as “select lift apply”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/ComplexLCUProjection.lean:31](../../../../library/modules/robin-complexlcuprojection/#decl-quantumblockencoding-robin-complexlcu-selectlift-apply). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.ComplexLCU.selectorLift_cleanColumn_apply" (lean := "QuantumBlockEncoding.Robin.ComplexLCU.selectorLift_cleanColumn_apply")
*Plain-English reading.* Lean checks the proposition indexed as “selector lift clean column apply”; the hypotheses and conclusion in the code panel fix its exact scope. A selector lift has one coefficient/system delta on a clean input column.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* A selector lift has one coefficient/system delta on a clean input column.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/ComplexLCUProjection.lean:43](../../../../library/modules/robin-complexlcuprojection/#decl-quantumblockencoding-robin-complexlcu-selectorlift-cleancolumn-apply). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.ComplexLCU.star_selectorLift_cleanRow_apply" (lean := "QuantumBlockEncoding.Robin.ComplexLCU.star_selectorLift_cleanRow_apply")
*Plain-English reading.* Lean checks the proposition indexed as “star selector lift clean row apply”; the hypotheses and conclusion in the code panel fix its exact scope. The clean PREPARE bra has the conjugate selector entry and two deltas.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The clean PREPARE bra has the conjugate selector entry and two deltas.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/ComplexLCUProjection.lean:63](../../../../library/modules/robin-complexlcuprojection/#decl-quantumblockencoding-robin-complexlcu-star-selectorlift-cleanrow-apply). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.ComplexLCU.amplitudeLift_mul_selectorLift_clean" (lean := "QuantumBlockEncoding.Robin.ComplexLCU.amplitudeLift_mul_selectorLift_clean")
*Plain-English reading.* Lean checks the proposition indexed as “amplitude lift mul selector lift clean”; the hypotheses and conclusion in the code panel fix its exact scope. Amplitude followed by selector preparation, evaluated on a clean input.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Amplitude followed by selector preparation, evaluated on a clean input.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/ComplexLCUProjection.lean:83](../../../../library/modules/robin-complexlcuprojection/#decl-quantumblockencoding-robin-complexlcu-amplitudelift-mul-selectorlift-clean). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.ComplexLCU.selectLift_mul_amplitudeLift_mul_selectorLift_clean" (lean := "QuantumBlockEncoding.Robin.ComplexLCU.selectLift_mul_amplitudeLift_mul_selectorLift_clean")
*Plain-English reading.* Lean checks the proposition indexed as “select lift mul amplitude lift mul selector lift clean”; the hypotheses and conclusion in the code panel fix its exact scope. SELECT applied after amplitude and PREPARE, on one clean input column.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* SELECT applied after amplitude and PREPARE, on one clean input column.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/ComplexLCUProjection.lean:113](../../../../library/modules/robin-complexlcuprojection/#decl-quantumblockencoding-robin-complexlcu-selectlift-mul-amplitudelift-mul-selectorlift-clean). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.ComplexLCU.star_selectorLift_mul_clean" (lean := "QuantumBlockEncoding.Robin.ComplexLCU.star_selectorLift_mul_clean")
*Plain-English reading.* Lean checks the proposition indexed as “star selector lift mul clean”; the hypotheses and conclusion in the code panel fix its exact scope. Project an arbitrary right factor through the clean PREPARE bra.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Project an arbitrary right factor through the clean PREPARE bra.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/ComplexLCUProjection.lean:147](../../../../library/modules/robin-complexlcuprojection/#decl-quantumblockencoding-robin-complexlcu-star-selectorlift-mul-clean). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.ComplexLCU.prepareAmplitudeSelectUnprepare_cleanEntry" (lean := "QuantumBlockEncoding.Robin.ComplexLCU.prepareAmplitudeSelectUnprepare_cleanEntry")
*Plain-English reading.* Lean checks the proposition indexed as “prepare amplitude select unprepare clean entry”; the hypotheses and conclusion in the code panel fix its exact scope. Exact projected clean entry of PREPARE/amplitude/SELECT/unprepare.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Exact projected clean entry of PREPARE/amplitude/SELECT/unprepare.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/ComplexLCUProjection.lean:172](../../../../library/modules/robin-complexlcuprojection/#decl-quantumblockencoding-robin-complexlcu-prepareamplitudeselectunprepare-cleanentry). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/Robin/EvolvedCandidates.lean

15 explicit public declarations, in source order.

:::definition "QuantumBlockEncoding.Robin.warmRobinFiveShiftCleanFormula" (lean := "QuantumBlockEncoding.Robin.warmRobinFiveShiftCleanFormula")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin five shift clean formula”. Clean branch predicted by the uniform-five LCU construction.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Clean branch predicted by the uniform-five LCU construction.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/EvolvedCandidates.lean:19](../../../../library/modules/robin-evolvedcandidates/#decl-quantumblockencoding-robin-warmrobinfiveshiftcleanformula). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFiveShiftCleanFormula_eq_target" (lean := "QuantumBlockEncoding.Robin.warmRobinFiveShiftCleanFormula_eq_target")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin five shift clean formula eq target”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/EvolvedCandidates.lean:25](../../../../library/modules/robin-evolvedcandidates/#decl-quantumblockencoding-robin-warmrobinfiveshiftcleanformula-eq-target). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinHadamard8CleanFormula" (lean := "QuantumBlockEncoding.Robin.warmRobinHadamard8CleanFormula")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin hadamard 8 clean formula”. Clean branch predicted by the uniform Hadamard-8 LCU construction.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Clean branch predicted by the uniform Hadamard-8 LCU construction.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/EvolvedCandidates.lean:32](../../../../library/modules/robin-evolvedcandidates/#decl-quantumblockencoding-robin-warmrobinhadamard8cleanformula). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinHadamard8CleanFormula_eq_target" (lean := "QuantumBlockEncoding.Robin.warmRobinHadamard8CleanFormula_eq_target")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin hadamard 8 clean formula eq target”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/EvolvedCandidates.lean:38](../../../../library/modules/robin-evolvedcandidates/#decl-quantumblockencoding-robin-warmrobinhadamard8cleanformula-eq-target). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinSevenToEightSlot" (lean := "QuantumBlockEncoding.Robin.warmRobinSevenToEightSlot")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin seven to eight slot”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/EvolvedCandidates.lean:44](../../../../library/modules/robin-evolvedcandidates/#decl-quantumblockencoding-robin-warmrobinseventoeightslot). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinSevenSlotPerm" (lean := "QuantumBlockEncoding.Robin.warmRobinSevenSlotPerm")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin seven slot perm”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/EvolvedCandidates.lean:48](../../../../library/modules/robin-evolvedcandidates/#decl-quantumblockencoding-robin-warmrobinsevenslotperm). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinSevenSlotWeight" (lean := "QuantumBlockEncoding.Robin.warmRobinSevenSlotWeight")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin seven slot weight”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/EvolvedCandidates.lean:51](../../../../library/modules/robin-evolvedcandidates/#decl-quantumblockencoding-robin-warmrobinsevenslotweight). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinSevenSlotDecomposition" (lean := "QuantumBlockEncoding.Robin.warmRobinSevenSlotDecomposition")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin seven slot decomposition”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/EvolvedCandidates.lean:56](../../../../library/modules/robin-evolvedcandidates/#decl-quantumblockencoding-robin-warmrobinsevenslotdecomposition). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinSplitSeven_duplicate_nonzero_access" (lean := "QuantumBlockEncoding.Robin.warmRobinSplitSeven_duplicate_nonzero_access")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin split seven duplicate nonzero access”; the hypotheses and conclusion in the code panel fix its exact scope. The historical split-seven route is a weighted-permutation LCU, not a sparse-access enumeration: two nonzero terms can address the same entry.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The historical split-seven route is a weighted-permutation LCU, not a sparse-access enumeration: two nonzero terms can address the same entry.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/EvolvedCandidates.lean:66](../../../../library/modules/robin-evolvedcandidates/#decl-quantumblockencoding-robin-warmrobinsplitseven-duplicate-nonzero-access). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinSevenSlotAmplitude" (lean := "QuantumBlockEncoding.Robin.warmRobinSevenSlotAmplitude")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin seven slot amplitude”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/EvolvedCandidates.lean:73](../../../../library/modules/robin-evolvedcandidates/#decl-quantumblockencoding-robin-warmrobinsevenslotamplitude). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinSevenSlotAmplitude_bounded" (lean := "QuantumBlockEncoding.Robin.warmRobinSevenSlotAmplitude_bounded")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin seven slot amplitude bounded”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/EvolvedCandidates.lean:76](../../../../library/modules/robin-evolvedcandidates/#decl-quantumblockencoding-robin-warmrobinsevenslotamplitude-bounded). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinSevenSlotCleanFormula" (lean := "QuantumBlockEncoding.Robin.warmRobinSevenSlotCleanFormula")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin seven slot clean formula”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/EvolvedCandidates.lean:80](../../../../library/modules/robin-evolvedcandidates/#decl-quantumblockencoding-robin-warmrobinsevenslotcleanformula). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinSevenSlotCleanFormula_eq_target" (lean := "QuantumBlockEncoding.Robin.warmRobinSevenSlotCleanFormula_eq_target")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin seven slot clean formula eq target”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/EvolvedCandidates.lean:86](../../../../library/modules/robin-evolvedcandidates/#decl-quantumblockencoding-robin-warmrobinsevenslotcleanformula-eq-target). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinHistoricalStructuralCandidateBlockedLeaf" (lean := "QuantumBlockEncoding.Robin.warmRobinHistoricalStructuralCandidateBlockedLeaf")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin historical structural candidate blocked leaf”. Precise promotion blocker for the genuinely structural-only routes.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Precise promotion blocker for the genuinely structural-only routes.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/EvolvedCandidates.lean:93](../../../../library/modules/robin-evolvedcandidates/#decl-quantumblockencoding-robin-warmrobinhistoricalstructuralcandidateblockedleaf). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinStructuralCandidateBlockedLeaf" (lean := "QuantumBlockEncoding.Robin.warmRobinStructuralCandidateBlockedLeaf")
*Plain-English reading.* This abbreviation gives a shorter name to the type or expression used for “warm robin structural candidate blocked leaf”. Historical compatibility alias scoped to structural-only candidates.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Historical compatibility alias scoped to structural-only candidates.

*Declaration kind.* abbrev.

Source: [QuantumBlockEncoding/Robin/EvolvedCandidates.lean:97](../../../../library/modules/robin-evolvedcandidates/#decl-quantumblockencoding-robin-warmrobinstructuralcandidateblockedleaf). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/Robin/Figure4Loaders.lean

36 explicit public declarations, in source order.

:::definition "QuantumBlockEncoding.Robin.warmRobinPaperLiteralBoundaryAngle" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperLiteralBoundaryAngle")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin paper literal boundary angle”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4Loaders.lean:13](../../../../library/modules/robin-figure4loaders/#decl-quantumblockencoding-robin-warmrobinpaperliteralboundaryangle). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinExecutableStandardRyBoundaryAngle" (lean := "QuantumBlockEncoding.Robin.warmRobinExecutableStandardRyBoundaryAngle")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin executable standard ry boundary angle”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4Loaders.lean:17](../../../../library/modules/robin-figure4loaders/#decl-quantumblockencoding-robin-warmrobinexecutablestandardryboundaryangle). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinCorrectedEq27BoundaryAngle" (lean := "QuantumBlockEncoding.Robin.warmRobinCorrectedEq27BoundaryAngle")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin corrected eq 27 boundary angle”. Corrected reading of Eq.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Corrected reading of Eq. (27) for the standard quantum-computing 'R\_y' convention. The displayed single-'arccos' expression in arXiv:2506.20478 is retained above only as a literal transcript of the source typo.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4Loaders.lean:26](../../../../library/modules/robin-figure4loaders/#decl-quantumblockencoding-robin-warmrobincorrectedeq27boundaryangle). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinCorrectedEq27BoundaryAngle_eq_executable" (lean := "QuantumBlockEncoding.Robin.warmRobinCorrectedEq27BoundaryAngle_eq_executable")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin corrected eq 27 boundary angle eq executable”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4Loaders.lean:30](../../../../library/modules/robin-figure4loaders/#decl-quantumblockencoding-robin-warmrobincorrectedeq27boundaryangle-eq-executable). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinCorrectedEq27_standardRy_cleanAmplitude" (lean := "QuantumBlockEncoding.Robin.warmRobinCorrectedEq27_standardRy_cleanAmplitude")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin corrected eq 27 standard ry clean amplitude”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4Loaders.lean:35](../../../../library/modules/robin-figure4loaders/#decl-quantumblockencoding-robin-warmrobincorrectedeq27-standardry-cleanamplitude). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinBoundaryAngle_zero_guard" (lean := "QuantumBlockEncoding.Robin.warmRobinBoundaryAngle_zero_guard")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin boundary angle zero guard”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4Loaders.lean:44](../../../../library/modules/robin-figure4loaders/#decl-quantumblockencoding-robin-warmrobinboundaryangle-zero-guard). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4BulkCoefficient_abs_le_one" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4BulkCoefficient_abs_le_one")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 bulk coefficient abs le one”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4Loaders.lean:52](../../../../library/modules/robin-figure4loaders/#decl-quantumblockencoding-robin-warmrobinfigure4bulkcoefficient-abs-le-one). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4SourceCoefficient_abs_le_one" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4SourceCoefficient_abs_le_one")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 source coefficient abs le one”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4Loaders.lean:58](../../../../library/modules/robin-figure4loaders/#decl-quantumblockencoding-robin-warmrobinfigure4sourcecoefficient-abs-le-one). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFigure4BulkControlWires" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4BulkControlWires")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin figure 4 bulk control wires”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4Loaders.lean:66](../../../../library/modules/robin-figure4loaders/#decl-quantumblockencoding-robin-warmrobinfigure4bulkcontrolwires). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4BulkControlWires_ne_target" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4BulkControlWires_ne_target")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 bulk control wires ne target”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4Loaders.lean:72](../../../../library/modules/robin-figure4loaders/#decl-quantumblockencoding-robin-warmrobinfigure4bulkcontrolwires-ne-target). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFigure4BulkControlSlot" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4BulkControlSlot")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin figure 4 bulk control slot”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4Loaders.lean:77](../../../../library/modules/robin-figure4loaders/#decl-quantumblockencoding-robin-warmrobinfigure4bulkcontrolslot). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFigure4BulkLoaderAngle" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4BulkLoaderAngle")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin figure 4 bulk loader angle”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4Loaders.lean:80](../../../../library/modules/robin-figure4loaders/#decl-quantumblockencoding-robin-warmrobinfigure4bulkloaderangle). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4BulkLoaderRy" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4BulkLoaderRy")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 bulk loader ry”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4Loaders.lean:89](../../../../library/modules/robin-figure4loaders/#decl-quantumblockencoding-robin-warmrobinfigure4bulkloaderry). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFigure4BulkLoaderCircuit" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4BulkLoaderCircuit")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin figure 4 bulk loader circuit”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4Loaders.lean:111](../../../../library/modules/robin-figure4loaders/#decl-quantumblockencoding-robin-warmrobinfigure4bulkloadercircuit). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFigure4BulkLoaderProgram" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4BulkLoaderProgram")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin figure 4 bulk loader program”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4Loaders.lean:116](../../../../library/modules/robin-figure4loaders/#decl-quantumblockencoding-robin-warmrobinfigure4bulkloaderprogram). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4BulkLoaderProgram_eval" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4BulkLoaderProgram_eval")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 bulk loader program eval”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4Loaders.lean:120](../../../../library/modules/robin-figure4loaders/#decl-quantumblockencoding-robin-warmrobinfigure4bulkloaderprogram-eval). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFigure4BoundaryControlWires" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4BoundaryControlWires")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin figure 4 boundary control wires”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4Loaders.lean:133](../../../../library/modules/robin-figure4loaders/#decl-quantumblockencoding-robin-warmrobinfigure4boundarycontrolwires). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4BoundaryControlWires_ne_target" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4BoundaryControlWires_ne_target")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 boundary control wires ne target”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4Loaders.lean:142](../../../../library/modules/robin-figure4loaders/#decl-quantumblockencoding-robin-warmrobinfigure4boundarycontrolwires-ne-target). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFigure4BoundaryControlSlot" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4BoundaryControlSlot")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin figure 4 boundary control slot”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4Loaders.lean:147](../../../../library/modules/robin-figure4loaders/#decl-quantumblockencoding-robin-warmrobinfigure4boundarycontrolslot). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFigure4BoundaryControlColumn" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4BoundaryControlColumn")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin figure 4 boundary control column”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4Loaders.lean:150](../../../../library/modules/robin-figure4loaders/#decl-quantumblockencoding-robin-warmrobinfigure4boundarycontrolcolumn). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFigure4BoundaryLoaderAngle" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4BoundaryLoaderAngle")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin figure 4 boundary loader angle”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4Loaders.lean:153](../../../../library/modules/robin-figure4loaders/#decl-quantumblockencoding-robin-warmrobinfigure4boundaryloaderangle). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4BoundaryLoaderRy" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4BoundaryLoaderRy")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 boundary loader ry”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4Loaders.lean:165](../../../../library/modules/robin-figure4loaders/#decl-quantumblockencoding-robin-warmrobinfigure4boundaryloaderry). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFigure4BoundaryLoaderCircuit" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4BoundaryLoaderCircuit")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin figure 4 boundary loader circuit”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4Loaders.lean:193](../../../../library/modules/robin-figure4loaders/#decl-quantumblockencoding-robin-warmrobinfigure4boundaryloadercircuit). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFigure4BoundaryLoaderProgram" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4BoundaryLoaderProgram")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin figure 4 boundary loader program”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4Loaders.lean:198](../../../../library/modules/robin-figure4loaders/#decl-quantumblockencoding-robin-warmrobinfigure4boundaryloaderprogram). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4BoundaryLoaderProgram_eval" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4BoundaryLoaderProgram_eval")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 boundary loader program eval”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4Loaders.lean:202](../../../../library/modules/robin-figure4loaders/#decl-quantumblockencoding-robin-warmrobinfigure4boundaryloaderprogram-eval). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFigure4DerivativeLoaderProgram" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4DerivativeLoaderProgram")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin figure 4 derivative loader program”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4Loaders.lean:215](../../../../library/modules/robin-figure4loaders/#decl-quantumblockencoding-robin-warmrobinfigure4derivativeloaderprogram). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4DerivativeLoaderProgram_eval" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4DerivativeLoaderProgram_eval")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 derivative loader program eval”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4Loaders.lean:219](../../../../library/modules/robin-figure4loaders/#decl-quantumblockencoding-robin-warmrobinfigure4derivativeloaderprogram-eval). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFigure4IndicatorValue" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4IndicatorValue")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin figure 4 indicator value”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4Loaders.lean:232](../../../../library/modules/robin-figure4loaders/#decl-quantumblockencoding-robin-warmrobinfigure4indicatorvalue). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFigure4BulkControlInput" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4BulkControlInput")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin figure 4 bulk control input”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4Loaders.lean:235](../../../../library/modules/robin-figure4loaders/#decl-quantumblockencoding-robin-warmrobinfigure4bulkcontrolinput). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFigure4BoundaryControlInput" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4BoundaryControlInput")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin figure 4 boundary control input”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4Loaders.lean:242](../../../../library/modules/robin-figure4loaders/#decl-quantumblockencoding-robin-warmrobinfigure4boundarycontrolinput). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4BulkControlSlot_input" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4BulkControlSlot_input")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 bulk control slot input”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4Loaders.lean:252](../../../../library/modules/robin-figure4loaders/#decl-quantumblockencoding-robin-warmrobinfigure4bulkcontrolslot-input). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4BulkControlInput_indicator" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4BulkControlInput_indicator")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 bulk control input indicator”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4Loaders.lean:258](../../../../library/modules/robin-figure4loaders/#decl-quantumblockencoding-robin-warmrobinfigure4bulkcontrolinput-indicator). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4BoundaryControlSlot_input" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4BoundaryControlSlot_input")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 boundary control slot input”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4Loaders.lean:263](../../../../library/modules/robin-figure4loaders/#decl-quantumblockencoding-robin-warmrobinfigure4boundarycontrolslot-input). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4BoundaryControlColumn_input" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4BoundaryControlColumn_input")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 boundary control column input”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4Loaders.lean:269](../../../../library/modules/robin-figure4loaders/#decl-quantumblockencoding-robin-warmrobinfigure4boundarycontrolcolumn-input). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4BoundaryControlInput_indicator" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4BoundaryControlInput_indicator")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 boundary control input indicator”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4Loaders.lean:275](../../../../library/modules/robin-figure4loaders/#decl-quantumblockencoding-robin-warmrobinfigure4boundarycontrolinput-indicator). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4DerivativeLoader_cleanEntry" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4DerivativeLoader_cleanEntry")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 derivative loader clean entry”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4Loaders.lean:280](../../../../library/modules/robin-figure4loaders/#decl-quantumblockencoding-robin-warmrobinfigure4derivativeloader-cleanentry). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/Robin/Figure4MiddlePrimitive.lean

35 explicit public declarations, in source order.

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4IndicatorBasisEquiv_clean" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4IndicatorBasisEquiv_clean")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 indicator basis equiv clean”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4MiddlePrimitive.lean:17](../../../../library/modules/robin-figure4middleprimitive/#decl-quantumblockencoding-robin-warmrobinfigure4indicatorbasisequiv-clean). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4BulkAssignment_transportInput" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4BulkAssignment_transportInput")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 bulk assignment transport input”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4MiddlePrimitive.lean:25](../../../../library/modules/robin-figure4middleprimitive/#decl-quantumblockencoding-robin-warmrobinfigure4bulkassignment-transportinput). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4BoundaryAssignment_transportInput" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4BoundaryAssignment_transportInput")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 boundary assignment transport input”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4MiddlePrimitive.lean:34](../../../../library/modules/robin-figure4middleprimitive/#decl-quantumblockencoding-robin-warmrobinfigure4boundaryassignment-transportinput). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4DerivativeLoader_physicalCleanEntry" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4DerivativeLoader_physicalCleanEntry")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 derivative loader physical clean entry”; the hypotheses and conclusion in the code panel fix its exact scope. The physical loader's clean coefficient entry is the exact source coefficient.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The physical loader's clean coefficient entry is the exact source coefficient. The proof reduces the two nine-wire block diagonals to their single two-dimensional standard-RY product.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4MiddlePrimitive.lean:46](../../../../library/modules/robin-figure4middleprimitive/#decl-quantumblockencoding-robin-warmrobinfigure4derivativeloader-physicalcleanentry). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4TransportInput_splitCoefficient_fst" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4TransportInput_splitCoefficient_fst")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 transport input split coefficient fst”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4MiddlePrimitive.lean:65](../../../../library/modules/robin-figure4middleprimitive/#decl-quantumblockencoding-robin-warmrobinfigure4transportinput-splitcoefficient-fst). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4TransportInput_splitCoefficient_context" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4TransportInput_splitCoefficient_context")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 transport input split coefficient context”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4MiddlePrimitive.lean:72](../../../../library/modules/robin-figure4middleprimitive/#decl-quantumblockencoding-robin-warmrobinfigure4transportinput-splitcoefficient-context). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4TransportInput_splitCoefficient_context_iff" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4TransportInput_splitCoefficient_context_iff")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 transport input split coefficient context iff”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4MiddlePrimitive.lean:80](../../../../library/modules/robin-figure4middleprimitive/#decl-quantumblockencoding-robin-warmrobinfigure4transportinput-splitcoefficient-context-iff). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4DerivativeLoader_entry" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4DerivativeLoader_entry")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 derivative loader entry”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4MiddlePrimitive.lean:93](../../../../library/modules/robin-figure4middleprimitive/#decl-quantumblockencoding-robin-warmrobinfigure4derivativeloader-entry). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFigure4FullRegisterSwapPrimitiveProgram" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4FullRegisterSwapPrimitiveProgram")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin figure 4 full register swap primitive program”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4MiddlePrimitive.lean:158](../../../../library/modules/robin-figure4middleprimitive/#decl-quantumblockencoding-robin-warmrobinfigure4fullregisterswapprimitiveprogram). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4RegisterSwapProgram_eval_full" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4RegisterSwapProgram_eval_full")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 register swap program eval full”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4MiddlePrimitive.lean:162](../../../../library/modules/robin-figure4middleprimitive/#decl-quantumblockencoding-robin-warmrobinfigure4registerswapprogram-eval-full). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFigure4HomogeneousProgram" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4HomogeneousProgram")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin figure 4 homogeneous program”. The homogeneous 'f=1' stage is constant-folded to the exact identity.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The homogeneous 'f=1' stage is constant-folded to the exact identity.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4MiddlePrimitive.lean:169](../../../../library/modules/robin-figure4middleprimitive/#decl-quantumblockencoding-robin-warmrobinfigure4homogeneousprogram). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4HomogeneousProgram_eval" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4HomogeneousProgram_eval")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 homogeneous program eval”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4MiddlePrimitive.lean:172](../../../../library/modules/robin-figure4middleprimitive/#decl-quantumblockencoding-robin-warmrobinfigure4homogeneousprogram-eval). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFigure4PostLoaderProgram" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4PostLoaderProgram")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin figure 4 post loader program”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4MiddlePrimitive.lean:176](../../../../library/modules/robin-figure4middleprimitive/#decl-quantumblockencoding-robin-warmrobinfigure4postloaderprogram). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFigure4PostLoaderBasisEquiv" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4PostLoaderBasisEquiv")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin figure 4 post loader basis equiv”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4MiddlePrimitive.lean:183](../../../../library/modules/robin-figure4middleprimitive/#decl-quantumblockencoding-robin-warmrobinfigure4postloaderbasisequiv). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4PostLoaderProgram_eval" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4PostLoaderProgram_eval")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 post loader program eval”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4MiddlePrimitive.lean:190](../../../../library/modules/robin-figure4middleprimitive/#decl-quantumblockencoding-robin-warmrobinfigure4postloaderprogram-eval). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4PostLoader_cleanAction" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4PostLoader_cleanAction")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 post loader clean action”; the hypotheses and conclusion in the code panel fix its exact scope. The post-loader basis permutation restores both work wires and the physical selector, while transporting the selected source row into the system register.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The post-loader basis permutation restores both work wires and the physical selector, while transporting the selected source row into the system register.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4MiddlePrimitive.lean:213](../../../../library/modules/robin-figure4middleprimitive/#decl-quantumblockencoding-robin-warmrobinfigure4postloader-cleanaction). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFigure4SourceDTColumn" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4SourceDTColumn")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin figure 4 source dt column”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4MiddlePrimitive.lean:248](../../../../library/modules/robin-figure4middleprimitive/#decl-quantumblockencoding-robin-warmrobinfigure4sourcedtcolumn). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4SourceDTColumn_inverse" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4SourceDTColumn_inverse")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 source dt column inverse”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4MiddlePrimitive.lean:252](../../../../library/modules/robin-figure4middleprimitive/#decl-quantumblockencoding-robin-warmrobinfigure4sourcedtcolumn-inverse). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4SourceDTColumn_eq_iff" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4SourceDTColumn_eq_iff")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 source dt column eq iff”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4MiddlePrimitive.lean:257](../../../../library/modules/robin-figure4middleprimitive/#decl-quantumblockencoding-robin-warmrobinfigure4sourcedtcolumn-eq-iff). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4PostLoader_inverseCleanAction" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4PostLoader_inverseCleanAction")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 post loader inverse clean action”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4MiddlePrimitive.lean:263](../../../../library/modules/robin-figure4middleprimitive/#decl-quantumblockencoding-robin-warmrobinfigure4postloader-inversecleanaction). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFigure4MiddleProgram" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4MiddleProgram")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin figure 4 middle program”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4MiddlePrimitive.lean:275](../../../../library/modules/robin-figure4middleprimitive/#decl-quantumblockencoding-robin-warmrobinfigure4middleprogram). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4MiddleProgram_eval" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4MiddleProgram_eval")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 middle program eval”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4MiddlePrimitive.lean:280](../../../../library/modules/robin-figure4middleprimitive/#decl-quantumblockencoding-robin-warmrobinfigure4middleprogram-eval). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4MiddleProgram_cleanEntry" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4MiddleProgram_cleanEntry")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 middle program clean entry”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4MiddlePrimitive.lean:291](../../../../library/modules/robin-figure4middleprimitive/#decl-quantumblockencoding-robin-warmrobinfigure4middleprogram-cleanentry). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFigure4ThroughIndicator" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4ThroughIndicator")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin figure 4 through indicator”. The post-loader basis permutation restores both work wires and the physical selector, while transporting the selected source row into the system register.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The post-loader basis permutation restores both work wires and the physical selector, while transporting the selected source row into the system register. -/ theorem warmRobinFigure4PostLoader\_cleanAction (slot column : Fin 8) (coefficient : Fin 2) : warmRobinFigure4PostLoaderBasisEquiv (warmRobinFigure4TransportInput slot column coefficient (warmRobinFigure4IndicatorValue column)) = warmRobinFigure4TransportInput slot (warmRobinSourceDTRow slot column) coefficient 0 := by rw \[warmRobinFigure4PostLoaderBasisEquiv\] simp only \[Equiv.trans\_apply\] rw \[warmRobinFigure4DTSparseAccess\_transportInput\] have indicatorForward := warmRobinFigure4IndicatorBasisEquiv\_clean (warmRobinSourceDTRow slot column) column coefficient have indicatorCleanup : warmRobinFigure4DTIndicatorBasisEquiv.symm (warmRobinFigure4TransportInput (warmRobinSourceDTRow slot column) column coefficient (warmRobinFigure4IndicatorValue column)) = warmRobinFigure4TransportInput (warmRobinSourceDTRow slot column) column coefficient 0 := by rw \[← indicatorForward, Equiv.symm\_apply\_apply\] rw \[indicatorCleanup\] rw \[warmRobinFigure4RegisterSwap\_transportInput\] let sourceRow := warmRobinSourceDTRow slot column have dForward : warmRobinFigure4DSparseAccessBasisEquiv (warmRobinFigure4TransportInput slot sourceRow coefficient 0) = warmRobinFigure4TransportInput column sourceRow coefficient 0 := by rw \[warmRobinFigure4DSparseAccess\_transportInput\] rw \[show (⟨(sourceRow.val + (warmRobinFigure4DOffset slot).val) % 8, Nat.mod\_lt \_ (by decide)⟩ : Fin 8) = column by exact warmRobinFigure4DOffset\_after\_DT slot column\] rw \[← dForward, Equiv.symm\_apply\_apply\] def warmRobinFigure4SourceDTColumn (slot row : Fin 8) : Fin 8 := ⟨(row.val + 8 - (warmRobinSourceDTOffset slot).val) % 8, Nat.mod\_lt \_ (by decide)⟩ theorem warmRobinFigure4SourceDTColumn\_inverse (slot row : Fin 8) : warmRobinSourceDTRow slot (warmRobinFigure4SourceDTColumn slot row) = row := by fin\_cases slot <;> fin\_cases row <;> native\_decide theorem warmRobinFigure4SourceDTColumn\_eq\_iff (slot row column : Fin 8) : warmRobinFigure4SourceDTColumn slot row = column ↔ warmRobinSourceDTRow slot column = row := by fin\_cases slot <;> fin\_cases row <;> fin\_cases column <;> native\_decide theorem warmRobinFigure4PostLoader\_inverseCleanAction (slot row : Fin 8) (coefficient : Fin 2) : warmRobinFigure4PostLoaderBasisEquiv.symm (warmRobinFigure4TransportInput slot row coefficient 0) = warmRobinFigure4TransportInput slot (warmRobinFigure4SourceDTColumn slot row) coefficient (warmRobinFigure4IndicatorValue (warmRobinFigure4SourceDTColumn slot row)) := by apply warmRobinFigure4PostLoaderBasisEquiv.injective rw \[Equiv.apply\_symm\_apply, warmRobinFigure4PostLoader\_cleanAction, warmRobinFigure4SourceDTColumn\_inverse\] noncomputable def warmRobinFigure4MiddleProgram : PrimitiveProgram 9 := PrimitiveProgram.seq warmRobinFigure4DTIndicatorProgram (PrimitiveProgram.seq warmRobinFigure4DerivativeLoaderProgram warmRobinFigure4PostLoaderProgram) theorem warmRobinFigure4MiddleProgram\_eval : evalPrimitiveProgram warmRobinFigure4MiddleProgram = ComplexLCU.equivPermutationMatrix warmRobinFigure4PostLoaderBasisEquiv \* evalPrimitiveProgram warmRobinFigure4DerivativeLoaderProgram \* ComplexLCU.equivPermutationMatrix warmRobinFigure4DTIndicatorBasisEquiv := by unfold warmRobinFigure4MiddleProgram rw \[evalPrimitiveProgram\_seq, evalPrimitiveProgram\_seq, warmRobinFigure4DTIndicatorProgram\_eval, warmRobinFigure4PostLoaderProgram\_eval\] theorem warmRobinFigure4MiddleProgram\_cleanEntry (coefficientRow coefficientColumn : Fin 2) (leftSlot rightSlot row column : Fin 8) : evalPrimitiveProgram warmRobinFigure4MiddleProgram (warmRobinFigure4TransportInput leftSlot row coefficientRow 0) (warmRobinFigure4TransportInput rightSlot column coefficientColumn 0) = if leftSlot = rightSlot ∧ warmRobinSourceDTRow rightSlot column = row then ComplexLCU.amplitudeRotation (((warmRobinFigure4SourceCoefficient rightSlot column : Rat) : Real)) coefficientRow coefficientColumn else 0 := by rw \[warmRobinFigure4MiddleProgram\_eval\] rw \[ComplexLCU.mul\_equivPermutationMatrix\_apply, ComplexLCU.equivPermutationMatrix\_mul\_apply\] rw \[warmRobinFigure4IndicatorBasisEquiv\_clean\] rw \[warmRobinFigure4PostLoader\_inverseCleanAction, warmRobinFigure4DerivativeLoader\_entry\] by\_cases slots : leftSlot = rightSlot · subst leftSlot simp only \[true\_and\] simp only \[warmRobinFigure4SourceDTColumn\_eq\_iff\] · simp \[slots\] /-! Named chronological stage roots used by the generated documentation.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4MiddlePrimitive.lean:317](../../../../library/modules/robin-figure4middleprimitive/#decl-quantumblockencoding-robin-warmrobinfigure4throughindicator). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4_after_indicator" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4_after_indicator")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 after indicator”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4MiddlePrimitive.lean:321](../../../../library/modules/robin-figure4middleprimitive/#decl-quantumblockencoding-robin-warmrobinfigure4-after-indicator). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFigure4ThroughDerivative" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4ThroughDerivative")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin figure 4 through derivative”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4MiddlePrimitive.lean:327](../../../../library/modules/robin-figure4middleprimitive/#decl-quantumblockencoding-robin-warmrobinfigure4throughderivative). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4_after_derivative_loader" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4_after_derivative_loader")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 after derivative loader”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4MiddlePrimitive.lean:331](../../../../library/modules/robin-figure4middleprimitive/#decl-quantumblockencoding-robin-warmrobinfigure4-after-derivative-loader). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFigure4ThroughDTAccess" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4ThroughDTAccess")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin figure 4 through dt access”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4MiddlePrimitive.lean:337](../../../../library/modules/robin-figure4middleprimitive/#decl-quantumblockencoding-robin-warmrobinfigure4throughdtaccess). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4_after_DT_sparse_access" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4_after_DT_sparse_access")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 after dt sparse access”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4MiddlePrimitive.lean:341](../../../../library/modules/robin-figure4middleprimitive/#decl-quantumblockencoding-robin-warmrobinfigure4-after-dt-sparse-access). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFigure4ThroughIndicatorCleanup" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4ThroughIndicatorCleanup")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin figure 4 through indicator cleanup”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4MiddlePrimitive.lean:347](../../../../library/modules/robin-figure4middleprimitive/#decl-quantumblockencoding-robin-warmrobinfigure4throughindicatorcleanup). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4_after_indicator_cleanup" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4_after_indicator_cleanup")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 after indicator cleanup”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4MiddlePrimitive.lean:351](../../../../library/modules/robin-figure4middleprimitive/#decl-quantumblockencoding-robin-warmrobinfigure4-after-indicator-cleanup). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFigure4ThroughSwap" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4ThroughSwap")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin figure 4 through swap”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4MiddlePrimitive.lean:357](../../../../library/modules/robin-figure4middleprimitive/#decl-quantumblockencoding-robin-warmrobinfigure4throughswap). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4_after_swap" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4_after_swap")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 after swap”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4MiddlePrimitive.lean:361](../../../../library/modules/robin-figure4middleprimitive/#decl-quantumblockencoding-robin-warmrobinfigure4-after-swap). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFigure4ThroughTransportedCleanup" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4ThroughTransportedCleanup")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin figure 4 through transported cleanup”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4MiddlePrimitive.lean:367](../../../../library/modules/robin-figure4middleprimitive/#decl-quantumblockencoding-robin-warmrobinfigure4throughtransportedcleanup). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4_after_transported_cleanup" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4_after_transported_cleanup")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 after transported cleanup”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4MiddlePrimitive.lean:371](../../../../library/modules/robin-figure4middleprimitive/#decl-quantumblockencoding-robin-warmrobinfigure4-after-transported-cleanup). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/Robin/Figure4PreparePrimitive.lean

23 explicit public declarations, in source order.

:::definition "QuantumBlockEncoding.Robin.WarmRobinFigure4FullSystem" (lean := "QuantumBlockEncoding.Robin.WarmRobinFigure4FullSystem")
*Plain-English reading.* This abbreviation gives a shorter name to the type or expression used for “warm robin figure 4 full system”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* abbrev.

Source: [QuantumBlockEncoding/Robin/Figure4PreparePrimitive.lean:15](../../../../library/modules/robin-figure4prepareprimitive/#decl-quantumblockencoding-robin-warmrobinfigure4fullsystem). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFigure4EncodeBits" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4EncodeBits")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin figure 4 encode bits”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4PreparePrimitive.lean:17](../../../../library/modules/robin-figure4prepareprimitive/#decl-quantumblockencoding-robin-warmrobinfigure4encodebits). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFigure4BitsIndex" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4BitsIndex")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin figure 4 bits index”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4PreparePrimitive.lean:30](../../../../library/modules/robin-figure4prepareprimitive/#decl-quantumblockencoding-robin-warmrobinfigure4bitsindex). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4BitsIndex_bijective" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4BitsIndex_bijective")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 bits index bijective”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4PreparePrimitive.lean:35](../../../../library/modules/robin-figure4prepareprimitive/#decl-quantumblockencoding-robin-warmrobinfigure4bitsindex-bijective). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFigure4BitsEquiv" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4BitsEquiv")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin figure 4 bits equiv”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4PreparePrimitive.lean:39](../../../../library/modules/robin-figure4prepareprimitive/#decl-quantumblockencoding-robin-warmrobinfigure4bitsequiv). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4BitsEquiv_apply" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4BitsEquiv_apply")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 bits equiv apply”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4PreparePrimitive.lean:45](../../../../library/modules/robin-figure4prepareprimitive/#decl-quantumblockencoding-robin-warmrobinfigure4bitsequiv-apply). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4BitsEquiv_encode" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4BitsEquiv_encode")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 bits equiv encode”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4PreparePrimitive.lean:48](../../../../library/modules/robin-figure4prepareprimitive/#decl-quantumblockencoding-robin-warmrobinfigure4bitsequiv-encode). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFigure4PrepareMiddleWires" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4PrepareMiddleWires")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin figure 4 prepare middle wires”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4PreparePrimitive.lean:57](../../../../library/modules/robin-figure4prepareprimitive/#decl-quantumblockencoding-robin-warmrobinfigure4preparemiddlewires). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4PrepareMiddleWires_ne_target" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4PrepareMiddleWires_ne_target")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 prepare middle wires ne target”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4PreparePrimitive.lean:59](../../../../library/modules/robin-figure4prepareprimitive/#decl-quantumblockencoding-robin-warmrobinfigure4preparemiddlewires-ne-target). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFigure4PrepareLowWires" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4PrepareLowWires")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin figure 4 prepare low wires”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4PreparePrimitive.lean:64](../../../../library/modules/robin-figure4prepareprimitive/#decl-quantumblockencoding-robin-warmrobinfigure4preparelowwires). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4PrepareLowWires_ne_target" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4PrepareLowWires_ne_target")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 prepare low wires ne target”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4PreparePrimitive.lean:68](../../../../library/modules/robin-figure4prepareprimitive/#decl-quantumblockencoding-robin-warmrobinfigure4preparelowwires-ne-target). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4SelectorBits_decode" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4SelectorBits_decode")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 selector bits decode”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4PreparePrimitive.lean:72](../../../../library/modules/robin-figure4prepareprimitive/#decl-quantumblockencoding-robin-warmrobinfigure4selectorbits-decode). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4PrepareHighContext_iff" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4PrepareHighContext_iff")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 prepare high context iff”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4PreparePrimitive.lean:78](../../../../library/modules/robin-figure4prepareprimitive/#decl-quantumblockencoding-robin-warmrobinfigure4preparehighcontext-iff). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4PrepareHighPhysical_eval" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4PrepareHighPhysical_eval")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 prepare high physical eval”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4PreparePrimitive.lean:91](../../../../library/modules/robin-figure4prepareprimitive/#decl-quantumblockencoding-robin-warmrobinfigure4preparehighphysical-eval). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4PrepareMiddleContext_iff" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4PrepareMiddleContext_iff")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 prepare middle context iff”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4PreparePrimitive.lean:125](../../../../library/modules/robin-figure4prepareprimitive/#decl-quantumblockencoding-robin-warmrobinfigure4preparemiddlecontext-iff). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4PrepareMiddlePhysical_eval" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4PrepareMiddlePhysical_eval")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 prepare middle physical eval”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4PreparePrimitive.lean:138](../../../../library/modules/robin-figure4prepareprimitive/#decl-quantumblockencoding-robin-warmrobinfigure4preparemiddlephysical-eval). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4PrepareLowContext_iff" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4PrepareLowContext_iff")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 prepare low context iff”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4PreparePrimitive.lean:184](../../../../library/modules/robin-figure4prepareprimitive/#decl-quantumblockencoding-robin-warmrobinfigure4preparelowcontext-iff). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4PrepareLowPhysical_eval" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4PrepareLowPhysical_eval")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 prepare low physical eval”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4PreparePrimitive.lean:197](../../../../library/modules/robin-figure4prepareprimitive/#decl-quantumblockencoding-robin-warmrobinfigure4preparelowphysical-eval). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFigure4SelectorPrepareCircuit" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4SelectorPrepareCircuit")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin figure 4 selector prepare circuit”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4PreparePrimitive.lean:243](../../../../library/modules/robin-figure4prepareprimitive/#decl-quantumblockencoding-robin-warmrobinfigure4selectorpreparecircuit). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFigure4SelectorPrepareProgram" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4SelectorPrepareProgram")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin figure 4 selector prepare program”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4PreparePrimitive.lean:252](../../../../library/modules/robin-figure4prepareprimitive/#decl-quantumblockencoding-robin-warmrobinfigure4selectorprepareprogram). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4_after_prepare" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4_after_prepare")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 after prepare”; the hypotheses and conclusion in the code panel fix its exact scope. Required stage root: the physical first stage is the exact selector lift.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Required stage root: the physical first stage is the exact selector lift.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4PreparePrimitive.lean:257](../../../../library/modules/robin-figure4prepareprimitive/#decl-quantumblockencoding-robin-warmrobinfigure4-after-prepare). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFigure4SelectorUnprepareProgram" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4SelectorUnprepareProgram")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin figure 4 selector unprepare program”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4PreparePrimitive.lean:288](../../../../library/modules/robin-figure4prepareprimitive/#decl-quantumblockencoding-robin-warmrobinfigure4selectorunprepareprogram). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4_after_unprepare" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4_after_unprepare")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 after unprepare”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4PreparePrimitive.lean:291](../../../../library/modules/robin-figure4prepareprimitive/#decl-quantumblockencoding-robin-warmrobinfigure4-after-unprepare). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/Robin/Figure4Primitive.lean

54 explicit public declarations, in source order.

:::definition "QuantumBlockEncoding.Robin.warmRobinRowBulkIndicatorProgram" (lean := "QuantumBlockEncoding.Robin.warmRobinRowBulkIndicatorProgram")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin row bulk indicator program”. Historical row-bulk indicator for rows 2 through 5 of 'D'.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Historical row-bulk indicator for rows 2 through 5 of 'D'. Figure 4 acts on 'D^T', so this circuit is retained only as a source-audit guard.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4Primitive.lean:19](../../../../library/modules/robin-figure4primitive/#decl-quantumblockencoding-robin-warmrobinrowbulkindicatorprogram). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinRowBulkIndicatorBasisEquiv" (lean := "QuantumBlockEncoding.Robin.warmRobinRowBulkIndicatorBasisEquiv")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin row bulk indicator basis equiv”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4Primitive.lean:22](../../../../library/modules/robin-figure4primitive/#decl-quantumblockencoding-robin-warmrobinrowbulkindicatorbasisequiv). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinRowBulkIndicatorBasisAction" (lean := "QuantumBlockEncoding.Robin.warmRobinRowBulkIndicatorBasisAction")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin row bulk indicator basis action”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4Primitive.lean:27](../../../../library/modules/robin-figure4primitive/#decl-quantumblockencoding-robin-warmrobinrowbulkindicatorbasisaction). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinRowBulkIndicatorProgram_eval" (lean := "QuantumBlockEncoding.Robin.warmRobinRowBulkIndicatorProgram_eval")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin row bulk indicator program eval”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4Primitive.lean:37](../../../../library/modules/robin-figure4primitive/#decl-quantumblockencoding-robin-warmrobinrowbulkindicatorprogram-eval). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.primitiveSwapCircuit" (lean := "QuantumBlockEncoding.Robin.primitiveSwapCircuit")
*Plain-English reading.* This definition gives the library's named construction or computation for “primitive swap circuit”. One physical SWAP expanded into the allowed primitive basis.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* One physical SWAP expanded into the allowed primitive basis.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4Primitive.lean:46](../../../../library/modules/robin-figure4primitive/#decl-quantumblockencoding-robin-primitiveswapcircuit). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.primitiveSwapBasisEquiv" (lean := "QuantumBlockEncoding.Robin.primitiveSwapBasisEquiv")
*Plain-English reading.* This definition gives the library's named construction or computation for “primitive swap basis equiv”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4Primitive.lean:51](../../../../library/modules/robin-figure4primitive/#decl-quantumblockencoding-robin-primitiveswapbasisequiv). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.primitiveSwapCircuit_eval" (lean := "QuantumBlockEncoding.Robin.primitiveSwapCircuit_eval")
*Plain-English reading.* Lean checks the proposition indexed as “primitive swap circuit eval”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4Primitive.lean:57](../../../../library/modules/robin-figure4primitive/#decl-quantumblockencoding-robin-primitiveswapcircuit-eval). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFigure4RegisterSwapProgram" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4RegisterSwapProgram")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin figure 4 register swap program”. Swap the two fixed three-qubit registers with three actual SWAPs.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Swap the two fixed three-qubit registers with three actual SWAPs.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4Primitive.lean:69](../../../../library/modules/robin-figure4primitive/#decl-quantumblockencoding-robin-warmrobinfigure4registerswapprogram). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFigure4RegisterSwapBasisEquiv" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4RegisterSwapBasisEquiv")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin figure 4 register swap basis equiv”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4Primitive.lean:74](../../../../library/modules/robin-figure4primitive/#decl-quantumblockencoding-robin-warmrobinfigure4registerswapbasisequiv). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4RegisterSwapProgram_eval" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4RegisterSwapProgram_eval")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 register swap program eval”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4Primitive.lean:80](../../../../library/modules/robin-figure4primitive/#decl-quantumblockencoding-robin-warmrobinfigure4registerswapprogram-eval). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4RegisterSwapBasisAction" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4RegisterSwapBasisAction")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 register swap basis action”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4Primitive.lean:92](../../../../library/modules/robin-figure4primitive/#decl-quantumblockencoding-robin-warmrobinfigure4registerswapbasisaction). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4RegisterSwapProgram_counts" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4RegisterSwapProgram_counts")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 register swap program counts”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4Primitive.lean:99](../../../../library/modules/robin-figure4primitive/#decl-quantumblockencoding-robin-warmrobinfigure4registerswapprogram-counts). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinHomogeneousCoefficientOracle" (lean := "QuantumBlockEncoding.Robin.warmRobinHomogeneousCoefficientOracle")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin homogeneous coefficient oracle”. For homogeneous 'f=1', the coefficient oracle is physically empty.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* For homogeneous 'f=1', the coefficient oracle is physically empty.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4Primitive.lean:105](../../../../library/modules/robin-figure4primitive/#decl-quantumblockencoding-robin-warmrobinhomogeneouscoefficientoracle). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinHomogeneousCoefficientOracle_eq_identity" (lean := "QuantumBlockEncoding.Robin.warmRobinHomogeneousCoefficientOracle_eq_identity")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin homogeneous coefficient oracle eq identity”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4Primitive.lean:107](../../../../library/modules/robin-figure4primitive/#decl-quantumblockencoding-robin-warmrobinhomogeneouscoefficientoracle-eq-identity). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinRowBulkIndicatorCleanup" (lean := "QuantumBlockEncoding.Robin.warmRobinRowBulkIndicatorCleanup")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin row bulk indicator cleanup”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4Primitive.lean:111](../../../../library/modules/robin-figure4primitive/#decl-quantumblockencoding-robin-warmrobinrowbulkindicatorcleanup). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFigure4DTIndicatorReversibleProgram" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4DTIndicatorReversibleProgram")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin figure 4 dt indicator reversible program”. Two disjoint pattern-controlled flips: '011' and '100'.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Two disjoint pattern-controlled flips: '011' and '100'. Wires 'q3-q5' hold the system column, 'q7' is the indicator, and 'q8' is reusable clean workspace.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4Primitive.lean:125](../../../../library/modules/robin-figure4primitive/#decl-quantumblockencoding-robin-warmrobinfigure4dtindicatorreversibleprogram). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFigure4DTIndicatorBasisEquiv" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4DTIndicatorBasisEquiv")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin figure 4 dt indicator basis equiv”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4Primitive.lean:134](../../../../library/modules/robin-figure4primitive/#decl-quantumblockencoding-robin-warmrobinfigure4dtindicatorbasisequiv). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFigure4SystemBits" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4SystemBits")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin figure 4 system bits”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4Primitive.lean:138](../../../../library/modules/robin-figure4primitive/#decl-quantumblockencoding-robin-warmrobinfigure4systembits). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4DTIndicatorProgram_basisAction" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4DTIndicatorProgram_basisAction")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 dt indicator program basis action”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4Primitive.lean:141](../../../../library/modules/robin-figure4primitive/#decl-quantumblockencoding-robin-warmrobinfigure4dtindicatorprogram-basisaction). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4DTIndicatorProgram_workspaceClean" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4DTIndicatorProgram_workspaceClean")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 dt indicator program workspace clean”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4Primitive.lean:151](../../../../library/modules/robin-figure4primitive/#decl-quantumblockencoding-robin-warmrobinfigure4dtindicatorprogram-workspaceclean). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFigure4DTIndicatorProgram" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4DTIndicatorProgram")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin figure 4 dt indicator program”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4Primitive.lean:156](../../../../library/modules/robin-figure4primitive/#decl-quantumblockencoding-robin-warmrobinfigure4dtindicatorprogram). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4DTIndicatorProgram_eval" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4DTIndicatorProgram_eval")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 dt indicator program eval”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4Primitive.lean:159](../../../../library/modules/robin-figure4primitive/#decl-quantumblockencoding-robin-warmrobinfigure4dtindicatorprogram-eval). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4DTIndicatorProgram_noOracleCalls" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4DTIndicatorProgram_noOracleCalls")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 dt indicator program no oracle calls”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4Primitive.lean:165](../../../../library/modules/robin-figure4primitive/#decl-quantumblockencoding-robin-warmrobinfigure4dtindicatorprogram-nooraclecalls). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFigure4AddressBits" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4AddressBits")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin figure 4 address bits”. Two disjoint pattern-controlled flips: '011' and '100'.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Two disjoint pattern-controlled flips: '011' and '100'. Wires 'q3-q5' hold the system column, 'q7' is the indicator, and 'q8' is reusable clean workspace. -/ def warmRobinFigure4DTIndicatorReversibleProgram : ReversibleProgram 9 := \[ .x 5 \] ++ cleanC3XReversibleProgram 3 4 5 7 8 (by decide) (by decide) (by decide) (by decide) (by decide) (by decide) ++ \[ .x 5, .x 3, .x 4 \] ++ cleanC3XReversibleProgram 3 4 5 7 8 (by decide) (by decide) (by decide) (by decide) (by decide) (by decide) ++ \[ .x 4, .x 3 \] def warmRobinFigure4DTIndicatorBasisEquiv : PrimitiveBasis 9 ≃ PrimitiveBasis 9 := evalReversibleProgram warmRobinFigure4DTIndicatorReversibleProgram def warmRobinFigure4SystemBits (bits : PrimitiveBasis 9) : Fin 8 := ⟨(bits 3).val + 2 \* (bits 4).val + 4 \* (bits 5).val, by omega⟩ theorem warmRobinFigure4DTIndicatorProgram\_basisAction (bits : PrimitiveBasis 9) (workspaceClean : bits 8 = 0) : let output := warmRobinFigure4DTIndicatorBasisEquiv bits output 7 = (if warmRobinFigure4TransposeBulk (warmRobinFigure4SystemBits bits) then flipBit (bits 7) else bits 7) ∧ output 8 = 0 ∧ (∀ wire : Fin 9, wire ≠ 7 → wire ≠ 8 → output wire = bits wire) := by native\_decide +revert theorem warmRobinFigure4DTIndicatorProgram\_workspaceClean (bits : PrimitiveBasis 9) (workspaceClean : bits 8 = 0) : warmRobinFigure4DTIndicatorBasisEquiv bits 8 = 0 := (warmRobinFigure4DTIndicatorProgram\_basisAction bits workspaceClean).2.1 noncomputable def warmRobinFigure4DTIndicatorProgram : PrimitiveProgram 9 := compileReversibleProgram warmRobinFigure4DTIndicatorReversibleProgram theorem warmRobinFigure4DTIndicatorProgram\_eval : evalPrimitiveProgram warmRobinFigure4DTIndicatorProgram = ComplexLCU.equivPermutationMatrix warmRobinFigure4DTIndicatorBasisEquiv := by exact compileReversibleProgram\_eval \_ theorem warmRobinFigure4DTIndicatorProgram\_noOracleCalls : warmRobinFigure4DTIndicatorProgram.resource.oracleCalls = 0 := PrimitiveCircuit.resource\_oracleCalls\_eq\_zero \_ /-! number number  Distinct D-transpose and D sparse-access programs

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4Primitive.lean:171](../../../../library/modules/robin-figure4primitive/#decl-quantumblockencoding-robin-warmrobinfigure4addressbits). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFigure4DTSparseAccessReversibleProgram" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4DTSparseAccessReversibleProgram")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin figure 4 dt sparse access reversible program”. Convert slot 's' to 's XOR 3', then add the system column modulo eight.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Convert slot 's' to 's XOR 3', then add the system column modulo eight.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4Primitive.lean:175](../../../../library/modules/robin-figure4primitive/#decl-quantumblockencoding-robin-warmrobinfigure4dtsparseaccessreversibleprogram). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFigure4DTSparseAccessBasisEquiv" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4DTSparseAccessBasisEquiv")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin figure 4 dt sparse access basis equiv”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4Primitive.lean:187](../../../../library/modules/robin-figure4primitive/#decl-quantumblockencoding-robin-warmrobinfigure4dtsparseaccessbasisequiv). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4DTSparseAccessProgram_cleanAction" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4DTSparseAccessProgram_cleanAction")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 dt sparse access program clean action”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4Primitive.lean:191](../../../../library/modules/robin-figure4primitive/#decl-quantumblockencoding-robin-warmrobinfigure4dtsparseaccessprogram-cleanaction). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFigure4DTSparseAccessProgram" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4DTSparseAccessProgram")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin figure 4 dt sparse access program”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4Primitive.lean:201](../../../../library/modules/robin-figure4primitive/#decl-quantumblockencoding-robin-warmrobinfigure4dtsparseaccessprogram). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4DTSparseAccessProgram_eval" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4DTSparseAccessProgram_eval")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 dt sparse access program eval”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4Primitive.lean:204](../../../../library/modules/robin-figure4primitive/#decl-quantumblockencoding-robin-warmrobinfigure4dtsparseaccessprogram-eval). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFigure4DSparseAccessReversibleProgram" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4DSparseAccessReversibleProgram")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin figure 4 d sparse access reversible program”. Convert slot 's' to 's+5', then add the second register modulo eight.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Convert slot 's' to 's+5', then add the second register modulo eight.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4Primitive.lean:211](../../../../library/modules/robin-figure4primitive/#decl-quantumblockencoding-robin-warmrobinfigure4dsparseaccessreversibleprogram). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFigure4DSparseAccessBasisEquiv" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4DSparseAccessBasisEquiv")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin figure 4 d sparse access basis equiv”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4Primitive.lean:226](../../../../library/modules/robin-figure4primitive/#decl-quantumblockencoding-robin-warmrobinfigure4dsparseaccessbasisequiv). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4DSparseAccessProgram_cleanAction" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4DSparseAccessProgram_cleanAction")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 d sparse access program clean action”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4Primitive.lean:230](../../../../library/modules/robin-figure4primitive/#decl-quantumblockencoding-robin-warmrobinfigure4dsparseaccessprogram-cleanaction). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFigure4DSparseAccessProgram" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4DSparseAccessProgram")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin figure 4 d sparse access program”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4Primitive.lean:241](../../../../library/modules/robin-figure4primitive/#decl-quantumblockencoding-robin-warmrobinfigure4dsparseaccessprogram). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4DSparseAccessProgram_eval" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4DSparseAccessProgram_eval")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 d sparse access program eval”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4Primitive.lean:244](../../../../library/modules/robin-figure4primitive/#decl-quantumblockencoding-robin-warmrobinfigure4dsparseaccessprogram-eval). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFigure4TransportInput" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4TransportInput")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin figure 4 transport input”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4Primitive.lean:250](../../../../library/modules/robin-figure4primitive/#decl-quantumblockencoding-robin-warmrobinfigure4transportinput). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4TransportInput_workspace" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4TransportInput_workspace")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 transport input workspace”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4Primitive.lean:263](../../../../library/modules/robin-figure4primitive/#decl-quantumblockencoding-robin-warmrobinfigure4transportinput-workspace). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4TransportInput_address" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4TransportInput_address")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 transport input address”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4Primitive.lean:268](../../../../library/modules/robin-figure4primitive/#decl-quantumblockencoding-robin-warmrobinfigure4transportinput-address). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4TransportInput_system" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4TransportInput_system")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 transport input system”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4Primitive.lean:275](../../../../library/modules/robin-figure4primitive/#decl-quantumblockencoding-robin-warmrobinfigure4transportinput-system). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4TransportInput_coefficient" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4TransportInput_coefficient")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 transport input coefficient”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4Primitive.lean:282](../../../../library/modules/robin-figure4primitive/#decl-quantumblockencoding-robin-warmrobinfigure4transportinput-coefficient). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4TransportInput_indicator" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4TransportInput_indicator")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 transport input indicator”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4Primitive.lean:288](../../../../library/modules/robin-figure4primitive/#decl-quantumblockencoding-robin-warmrobinfigure4transportinput-indicator). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFigure4RegisterSwapReversibleProgram" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4RegisterSwapReversibleProgram")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin figure 4 register swap reversible program”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4Primitive.lean:294](../../../../library/modules/robin-figure4primitive/#decl-quantumblockencoding-robin-warmrobinfigure4registerswapreversibleprogram). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFigure4RegisterSwapFullBasisEquiv" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4RegisterSwapFullBasisEquiv")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin figure 4 register swap full basis equiv”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4Primitive.lean:300](../../../../library/modules/robin-figure4primitive/#decl-quantumblockencoding-robin-warmrobinfigure4registerswapfullbasisequiv). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4AddressBits_decode" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4AddressBits_decode")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 address bits decode”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4Primitive.lean:304](../../../../library/modules/robin-figure4primitive/#decl-quantumblockencoding-robin-warmrobinfigure4addressbits-decode). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4SystemBits_decode" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4SystemBits_decode")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 system bits decode”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4Primitive.lean:310](../../../../library/modules/robin-figure4primitive/#decl-quantumblockencoding-robin-warmrobinfigure4systembits-decode). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4Basis_ext" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4Basis_ext")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 basis ext”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4Primitive.lean:316](../../../../library/modules/robin-figure4primitive/#decl-quantumblockencoding-robin-warmrobinfigure4basis-ext). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4DTSparseAccess_transportInput" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4DTSparseAccess_transportInput")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 dt sparse access transport input”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4Primitive.lean:337](../../../../library/modules/robin-figure4primitive/#decl-quantumblockencoding-robin-warmrobinfigure4dtsparseaccess-transportinput). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4RegisterSwap_transportInput" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4RegisterSwap_transportInput")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 register swap transport input”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4Primitive.lean:355](../../../../library/modules/robin-figure4primitive/#decl-quantumblockencoding-robin-warmrobinfigure4registerswap-transportinput). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4DOffset_after_DT" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4DOffset_after_DT")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 d offset after dt”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4Primitive.lean:362](../../../../library/modules/robin-figure4primitive/#decl-quantumblockencoding-robin-warmrobinfigure4doffset-after-dt). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4DSparseAccess_transportInput" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4DSparseAccess_transportInput")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 d sparse access transport input”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4Primitive.lean:369](../../../../library/modules/robin-figure4primitive/#decl-quantumblockencoding-robin-warmrobinfigure4dsparseaccess-transportinput). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4TransportedPostSwapCleanup" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4TransportedPostSwapCleanup")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 transported post swap cleanup”; the hypotheses and conclusion in the code panel fix its exact scope. Central cleanup root: D-transpose access, register transport, and inverse D access restore the original slot while leaving the transported row in the system register and returning 'q8' to zero.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Central cleanup root: D-transpose access, register transport, and inverse D access restore the original slot while leaving the transported row in the system register and returning 'q8' to zero.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4Primitive.lean:391](../../../../library/modules/robin-figure4primitive/#decl-quantumblockencoding-robin-warmrobinfigure4transportedpostswapcleanup). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4SparseWorkspaceClean" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4SparseWorkspaceClean")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 sparse workspace clean”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4Primitive.lean:430](../../../../library/modules/robin-figure4primitive/#decl-quantumblockencoding-robin-warmrobinfigure4sparseworkspaceclean). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFigure4FormerOpenPrimitiveContracts" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4FormerOpenPrimitiveContracts")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin figure 4 former open primitive contracts”. Historical pre-T3 audit list.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Historical pre-T3 audit list. 'Figure4T3.lean' now closes these obligations for fixed N=8, homogeneous f=1, and the standard-RY-corrected executable convention.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4Primitive.lean:442](../../../../library/modules/robin-figure4primitive/#decl-quantumblockencoding-robin-warmrobinfigure4formeropenprimitivecontracts). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFigure4OpenPrimitiveContracts" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4OpenPrimitiveContracts")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin figure 4 open primitive contracts”. No primitive obligations remain open for the fixed-N8 Figure-4 route.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* No primitive obligations remain open for the fixed-N8 Figure-4 route.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4Primitive.lean:451](../../../../library/modules/robin-figure4primitive/#decl-quantumblockencoding-robin-warmrobinfigure4openprimitivecontracts). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4OpenPrimitiveContracts_eq_nil" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4OpenPrimitiveContracts_eq_nil")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 open primitive contracts eq nil”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4Primitive.lean:453](../../../../library/modules/robin-figure4primitive/#decl-quantumblockencoding-robin-warmrobinfigure4openprimitivecontracts-eq-nil). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/Robin/Figure4SourceData.lean

15 explicit public declarations, in source order.

:::definition "QuantumBlockEncoding.Robin.warmRobinFigure4DOffset" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4DOffset")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin figure 4 d offset”. Offset table for the non-transposed sparse access used after register transport.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Offset table for the non-transposed sparse access used after register transport.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4SourceData.lean:16](../../../../library/modules/robin-figure4sourcedata/#decl-quantumblockencoding-robin-warmrobinfigure4doffset). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4DOffset_table" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4DOffset_table")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 d offset table”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4SourceData.lean:19](../../../../library/modules/robin-figure4sourcedata/#decl-quantumblockencoding-robin-warmrobinfigure4doffset-table). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4DT_D_offsets_cancel" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4DT_D_offsets_cancel")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 dt d offsets cancel”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4SourceData.lean:23](../../../../library/modules/robin-figure4sourcedata/#decl-quantumblockencoding-robin-warmrobinfigure4dt-d-offsets-cancel). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinPeriodicIntegerReference" (lean := "QuantumBlockEncoding.Robin.warmRobinPeriodicIntegerReference")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin periodic integer reference”. Periodic fourth-order integer stencil indexed by cyclic row offset.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Periodic fourth-order integer stencil indexed by cyclic row offset.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4SourceData.lean:29](../../../../library/modules/robin-figure4sourcedata/#decl-quantumblockencoding-robin-warmrobinperiodicintegerreference). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinPeriodic_rows_two_through_five" (lean := "QuantumBlockEncoding.Robin.warmRobinPeriodic_rows_two_through_five")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin periodic rows two through five”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4SourceData.lean:36](../../../../library/modules/robin-figure4sourcedata/#decl-quantumblockencoding-robin-warmrobinperiodic-rows-two-through-five). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinPeriodic_columns_three_and_four" (lean := "QuantumBlockEncoding.Robin.warmRobinPeriodic_columns_three_and_four")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin periodic columns three and four”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4SourceData.lean:43](../../../../library/modules/robin-figure4sourcedata/#decl-quantumblockencoding-robin-warmrobinperiodic-columns-three-and-four). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFigure4TransposeBulk" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4TransposeBulk")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin figure 4 transpose bulk”. Figure 4 acts on a row of 'D^T', equivalently a column of 'D'.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Figure 4 acts on a row of 'D^T', equivalently a column of 'D'.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4SourceData.lean:50](../../../../library/modules/robin-figure4sourcedata/#decl-quantumblockencoding-robin-warmrobinfigure4transposebulk). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4TransposeBulk_matches_periodic" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4TransposeBulk_matches_periodic")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 transpose bulk matches periodic”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4SourceData.lean:58](../../../../library/modules/robin-figure4sourcedata/#decl-quantumblockencoding-robin-warmrobinfigure4transposebulk-matches-periodic). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4_column_two_not_transpose_bulk" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4_column_two_not_transpose_bulk")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 column two not transpose bulk”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4SourceData.lean:64](../../../../library/modules/robin-figure4sourcedata/#decl-quantumblockencoding-robin-warmrobinfigure4-column-two-not-transpose-bulk). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4_column_five_not_transpose_bulk" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4_column_five_not_transpose_bulk")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 column five not transpose bulk”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4SourceData.lean:69](../../../../library/modules/robin-figure4sourcedata/#decl-quantumblockencoding-robin-warmrobinfigure4-column-five-not-transpose-bulk). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFigure4BulkCoefficient" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4BulkCoefficient")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin figure 4 bulk coefficient”. Interior derivative coefficients by physical selector slot.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Interior derivative coefficients by physical selector slot.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4SourceData.lean:75](../../../../library/modules/robin-figure4sourcedata/#decl-quantumblockencoding-robin-warmrobinfigure4bulkcoefficient). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFigure4BoundaryCoefficient" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4BoundaryCoefficient")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin figure 4 boundary coefficient”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4SourceData.lean:85](../../../../library/modules/robin-figure4sourcedata/#decl-quantumblockencoding-robin-warmrobinfigure4boundarycoefficient). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFigure4SourceCoefficient" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4SourceCoefficient")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin figure 4 source coefficient”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4SourceData.lean:88](../../../../library/modules/robin-figure4sourcedata/#decl-quantumblockencoding-robin-warmrobinfigure4sourcecoefficient). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4SourceCoefficient_eq_weight" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4SourceCoefficient_eq_weight")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 source coefficient eq weight”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4SourceData.lean:91](../../../../library/modules/robin-figure4sourcedata/#decl-quantumblockencoding-robin-warmrobinfigure4sourcecoefficient-eq-weight). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4SourceCoefficient_branch" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4SourceCoefficient_branch")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 source coefficient branch”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4SourceData.lean:96](../../../../library/modules/robin-figure4sourcedata/#decl-quantumblockencoding-robin-warmrobinfigure4sourcecoefficient-branch). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/Robin/Figure4T3.lean

19 explicit public declarations, in source order.

:::definition "QuantumBlockEncoding.Robin.warmRobinFigure4PrimitiveProgram" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4PrimitiveProgram")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin figure 4 primitive program”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4T3.lean:18](../../../../library/modules/robin-figure4t3/#decl-quantumblockencoding-robin-warmrobinfigure4primitiveprogram). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4AllWorkspaceClean" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4AllWorkspaceClean")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 all workspace clean”; the hypotheses and conclusion in the code panel fix its exact scope. Both Figure-4 work wires return clean on every selector/system branch.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Both Figure-4 work wires return clean on every selector/system branch. The coefficient loader may superpose 'q6', but it does not alter this classical context, so the statement is quantified over both coefficient basis values.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4T3.lean:26](../../../../library/modules/robin-figure4t3/#decl-quantumblockencoding-robin-warmrobinfigure4allworkspaceclean). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFigure4LogicalMiddle" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4LogicalMiddle")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin figure 4 logical middle”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4T3.lean:37](../../../../library/modules/robin-figure4t3/#decl-quantumblockencoding-robin-warmrobinfigure4logicalmiddle). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4LogicalMiddle_apply" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4LogicalMiddle_apply")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 logical middle apply”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4T3.lean:44](../../../../library/modules/robin-figure4t3/#decl-quantumblockencoding-robin-warmrobinfigure4logicalmiddle-apply). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4LogicalMiddle_cleanEntry" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4LogicalMiddle_cleanEntry")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 logical middle clean entry”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4T3.lean:53](../../../../library/modules/robin-figure4t3/#decl-quantumblockencoding-robin-warmrobinfigure4logicalmiddle-cleanentry). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4Primitive_eval_eq_logical" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4Primitive_eval_eq_logical")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 primitive eval eq logical”; the hypotheses and conclusion in the code panel fix its exact scope. Exact physical-program semantics, including all macro compiler phases.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Exact physical-program semantics, including all macro compiler phases.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4T3.lean:88](../../../../library/modules/robin-figure4t3/#decl-quantumblockencoding-robin-warmrobinfigure4primitive-eval-eq-logical). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4Logical_cleanEntry" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4Logical_cleanEntry")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 logical clean entry”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4T3.lean:136](../../../../library/modules/robin-figure4t3/#decl-quantumblockencoding-robin-warmrobinfigure4logical-cleanentry). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFigure4PrimitiveFlatUnitary" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4PrimitiveFlatUnitary")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin figure 4 primitive flat unitary”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4T3.lean:226](../../../../library/modules/robin-figure4t3/#decl-quantumblockencoding-robin-warmrobinfigure4primitiveflatunitary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4PrimitiveCircuit_unitary" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4PrimitiveCircuit_unitary")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 primitive circuit unitary”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4T3.lean:231](../../../../library/modules/robin-figure4t3/#decl-quantumblockencoding-robin-warmrobinfigure4primitivecircuit-unitary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFigure4PrimitiveCleanIndex" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4PrimitiveCleanIndex")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin figure 4 primitive clean index”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4T3.lean:237](../../../../library/modules/robin-figure4t3/#decl-quantumblockencoding-robin-warmrobinfigure4primitivecleanindex). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4PrimitiveCircuit_cleanEntry" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4PrimitiveCircuit_cleanEntry")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 primitive circuit clean entry”; the hypotheses and conclusion in the code panel fix its exact scope. Required whole-circuit clean entry; this is symbolic, not numerical.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Required whole-circuit clean entry; this is symbolic, not numerical.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4T3.lean:243](../../../../library/modules/robin-figure4t3/#decl-quantumblockencoding-robin-warmrobinfigure4primitivecircuit-cleanentry). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4PrimitiveCircuit_cleanBlock" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4PrimitiveCircuit_cleanBlock")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 primitive circuit clean block”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4T3.lean:267](../../../../library/modules/robin-figure4t3/#decl-quantumblockencoding-robin-warmrobinfigure4primitivecircuit-cleanblock). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFigure4PrimitiveBlockContainsTarget" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4PrimitiveBlockContainsTarget")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin figure 4 primitive block contains target”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4T3.lean:276](../../../../library/modules/robin-figure4t3/#decl-quantumblockencoding-robin-warmrobinfigure4primitiveblockcontainstarget). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4PrimitiveBlockContainsTarget_proof" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4PrimitiveBlockContainsTarget_proof")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 primitive block contains target proof”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Figure4T3.lean:284](../../../../library/modules/robin-figure4t3/#decl-quantumblockencoding-robin-warmrobinfigure4primitiveblockcontainstarget-proof). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFigure4PrimitivePresentation" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4PrimitivePresentation")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin figure 4 primitive presentation”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4T3.lean:290](../../../../library/modules/robin-figure4t3/#decl-quantumblockencoding-robin-warmrobinfigure4primitivepresentation). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFigure4PrimitiveResource" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4PrimitiveResource")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin figure 4 primitive resource”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4T3.lean:298](../../../../library/modules/robin-figure4t3/#decl-quantumblockencoding-robin-warmrobinfigure4primitiveresource). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFigure4PrimitiveOperatorCandidate" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4PrimitiveOperatorCandidate")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin figure 4 primitive operator candidate”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4T3.lean:301](../../../../library/modules/robin-figure4t3/#decl-quantumblockencoding-robin-warmrobinfigure4primitiveoperatorcandidate). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFigure4PrimitiveRefinement" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4PrimitiveRefinement")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin figure 4 primitive refinement”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4T3.lean:318](../../../../library/modules/robin-figure4t3/#decl-quantumblockencoding-robin-warmrobinfigure4primitiverefinement). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFigure4PrimitiveVerifiedBlockEncoding" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4PrimitiveVerifiedBlockEncoding")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin figure 4 primitive verified block encoding”. Fixed-N8, f=1, standard-RY-corrected Figure-4 realization.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Fixed-N8, f=1, standard-RY-corrected Figure-4 realization.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Figure4T3.lean:333](../../../../library/modules/robin-figure4t3/#decl-quantumblockencoding-robin-warmrobinfigure4primitiveverifiedblockencoding). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/Robin/FixedN3Data.lean

6 explicit public declarations, in source order.

:::definition "QuantumBlockEncoding.Robin.warmRobinIntegerTarget" (lean := "QuantumBlockEncoding.Robin.warmRobinIntegerTarget")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin integer target”. The integer matrix 'M = 12 A', kept integral for finite decomposition proofs.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The integer matrix 'M = 12 A', kept integral for finite decomposition proofs.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/FixedN3Data.lean:17](../../../../library/modules/robin-fixedn3data/#decl-quantumblockencoding-robin-warmrobinintegertarget). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinIntegerTargetRat" (lean := "QuantumBlockEncoding.Robin.warmRobinIntegerTargetRat")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin integer target rat”. Rational view of the frozen integer target.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Rational view of the frozen integer target.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/FixedN3Data.lean:30](../../../../library/modules/robin-fixedn3data/#decl-quantumblockencoding-robin-warmrobinintegertargetrat). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinIntegerTarget_eq_twelve_mul_target" (lean := "QuantumBlockEncoding.Robin.warmRobinIntegerTarget_eq_twelve_mul_target")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin integer target eq twelve mul target”; the hypotheses and conclusion in the code panel fix its exact scope. Exact fixed-instance identity 'M = 12 A'.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Exact fixed-instance identity 'M = 12 A'.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/FixedN3Data.lean:34](../../../../library/modules/robin-fixedn3data/#decl-quantumblockencoding-robin-warmrobinintegertarget-eq-twelve-mul-target). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobin_normalized_eq_integer_div_224" (lean := "QuantumBlockEncoding.Robin.warmRobin_normalized_eq_integer_div_224")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin normalized eq integer div 224”; the hypotheses and conclusion in the code panel fix its exact scope. Exact normalized target identity 'A / (56/3) = M / 224'.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Exact normalized target identity 'A / (56/3) = M / 224'.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/FixedN3Data.lean:40](../../../../library/modules/robin-fixedn3data/#decl-quantumblockencoding-robin-warmrobin-normalized-eq-integer-div-224). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinCleanEmbed" (lean := "QuantumBlockEncoding.Robin.warmRobinCleanEmbed")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin clean embed”. Signal-first clean embedding for the fixed 'signal x system' convention.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Signal-first clean embedding for the fixed 'signal x system' convention.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/FixedN3Data.lean:47](../../../../library/modules/robin-fixedn3data/#decl-quantumblockencoding-robin-warmrobincleanembed). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinCleanEmbed_value" (lean := "QuantumBlockEncoding.Robin.warmRobinCleanEmbed_value")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin clean embed value”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/FixedN3Data.lean:50](../../../../library/modules/robin-fixedn3data/#decl-quantumblockencoding-robin-warmrobincleanembed-value). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/Robin/Hadamard8BlockEncoding.lean

24 explicit public declarations, in source order.

:::definition "QuantumBlockEncoding.Robin.warmRobinHadamard8IndexEquiv" (lean := "QuantumBlockEncoding.Robin.warmRobinHadamard8IndexEquiv")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin hadamard 8 index equiv”. Flatten coefficient × selector × system into the seven-qubit basis.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Flatten coefficient × selector × system into the seven-qubit basis.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Hadamard8BlockEncoding.lean:22](../../../../library/modules/robin-hadamard8blockencoding/#decl-quantumblockencoding-robin-warmrobinhadamard8indexequiv). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinHadamard8CleanIndex" (lean := "QuantumBlockEncoding.Robin.warmRobinHadamard8CleanIndex")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin hadamard 8 clean index”. The clean coefficient/selector branch embedded in the flat seven-qubit basis.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The clean coefficient/selector branch embedded in the flat seven-qubit basis.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Hadamard8BlockEncoding.lean:28](../../../../library/modules/robin-hadamard8blockencoding/#decl-quantumblockencoding-robin-warmrobinhadamard8cleanindex). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinHadamard8CleanIndex_value" (lean := "QuantumBlockEncoding.Robin.warmRobinHadamard8CleanIndex_value")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin hadamard 8 clean index value”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Hadamard8BlockEncoding.lean:31](../../../../library/modules/robin-hadamard8blockencoding/#decl-quantumblockencoding-robin-warmrobinhadamard8cleanindex-value). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinHadamard8FlatUnitary" (lean := "QuantumBlockEncoding.Robin.warmRobinHadamard8FlatUnitary")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin hadamard 8 flat unitary”. The product-register logical unitary reindexed as a seven-qubit matrix.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The product-register logical unitary reindexed as a seven-qubit matrix.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Hadamard8BlockEncoding.lean:36](../../../../library/modules/robin-hadamard8blockencoding/#decl-quantumblockencoding-robin-warmrobinhadamard8flatunitary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinHadamard8FlatUnitary_unitary" (lean := "QuantumBlockEncoding.Robin.warmRobinHadamard8FlatUnitary_unitary")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin hadamard 8 flat unitary unitary”; the hypotheses and conclusion in the code panel fix its exact scope. Reindexing preserves the exact Mathlib unitary-group certificate.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Reindexing preserves the exact Mathlib unitary-group certificate.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Hadamard8BlockEncoding.lean:42](../../../../library/modules/robin-hadamard8blockencoding/#decl-quantumblockencoding-robin-warmrobinhadamard8flatunitary-unitary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinHadamard8FlatUnitary_reindex" (lean := "QuantumBlockEncoding.Robin.warmRobinHadamard8FlatUnitary_reindex")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin hadamard 8 flat unitary reindex”; the hypotheses and conclusion in the code panel fix its exact scope. Applying the reindexed matrix at reindexed indices recovers the product entry.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Applying the reindexed matrix at reindexed indices recovers the product entry.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Hadamard8BlockEncoding.lean:49](../../../../library/modules/robin-hadamard8blockencoding/#decl-quantumblockencoding-robin-warmrobinhadamard8flatunitary-reindex). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinUniformBitPrepare_cleanColumn" (lean := "QuantumBlockEncoding.Robin.warmRobinUniformBitPrepare_cleanColumn")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin uniform bit prepare clean column”; the hypotheses and conclusion in the code panel fix its exact scope. Every row of the one-bit PREPARE has the same clean-column amplitude.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Every row of the one-bit PREPARE has the same clean-column amplitude.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Hadamard8BlockEncoding.lean:58](../../../../library/modules/robin-hadamard8blockencoding/#decl-quantumblockencoding-robin-warmrobinuniformbitprepare-cleancolumn). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinHadamardBitsPrepare_cleanColumn" (lean := "QuantumBlockEncoding.Robin.warmRobinHadamardBitsPrepare_cleanColumn")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin hadamard bits prepare clean column”; the hypotheses and conclusion in the code panel fix its exact scope. The three-bit tensor PREPARE has a uniform clean column.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The three-bit tensor PREPARE has a uniform clean column.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Hadamard8BlockEncoding.lean:64](../../../../library/modules/robin-hadamard8blockencoding/#decl-quantumblockencoding-robin-warmrobinhadamardbitsprepare-cleancolumn). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinHadamard8SelectorPrepare_cleanColumn" (lean := "QuantumBlockEncoding.Robin.warmRobinHadamard8SelectorPrepare_cleanColumn")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin hadamard 8 selector prepare clean column”; the hypotheses and conclusion in the code panel fix its exact scope. The flattened selector PREPARE still has a uniform clean column.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The flattened selector PREPARE still has a uniform clean column.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Hadamard8BlockEncoding.lean:75](../../../../library/modules/robin-hadamard8blockencoding/#decl-quantumblockencoding-robin-warmrobinhadamard8selectorprepare-cleancolumn). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinHadamard8SelectorPrepare_probability" (lean := "QuantumBlockEncoding.Robin.warmRobinHadamard8SelectorPrepare_probability")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin hadamard 8 selector prepare probability”; the hypotheses and conclusion in the code panel fix its exact scope. Squared magnitude of each selector amplitude is exactly '1/8'.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Squared magnitude of each selector amplitude is exactly '1/8'.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Hadamard8BlockEncoding.lean:90](../../../../library/modules/robin-hadamard8blockencoding/#decl-quantumblockencoding-robin-warmrobinhadamard8selectorprepare-probability). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinHadamard8Coefficient_complex" (lean := "QuantumBlockEncoding.Robin.warmRobinHadamard8Coefficient_complex")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin hadamard 8 coefficient complex”; the hypotheses and conclusion in the code panel fix its exact scope. Rational and real-complex views of a slot coefficient agree.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Rational and real-complex views of a slot coefficient agree.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Hadamard8BlockEncoding.lean:110](../../../../library/modules/robin-hadamard8blockencoding/#decl-quantumblockencoding-robin-warmrobinhadamard8coefficient-complex). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinHadamard8LogicalUnitary_cleanEntry" (lean := "QuantumBlockEncoding.Robin.warmRobinHadamard8LogicalUnitary_cleanEntry")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin hadamard 8 logical unitary clean entry”; the hypotheses and conclusion in the code panel fix its exact scope. The reusable clean-entry expansion specializes to the Robin eight-slot formula.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The reusable clean-entry expansion specializes to the Robin eight-slot formula.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Hadamard8BlockEncoding.lean:117](../../../../library/modules/robin-hadamard8blockencoding/#decl-quantumblockencoding-robin-warmrobinhadamard8logicalunitary-cleanentry). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinHadamard8LogicalUnitary_cleanEntry_eq_target" (lean := "QuantumBlockEncoding.Robin.warmRobinHadamard8LogicalUnitary_cleanEntry_eq_target")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin hadamard 8 logical unitary clean entry eq target”; the hypotheses and conclusion in the code panel fix its exact scope. The product-register clean entry is the normalized fixed Robin target.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The product-register clean entry is the normalized fixed Robin target.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Hadamard8BlockEncoding.lean:155](../../../../library/modules/robin-hadamard8blockencoding/#decl-quantumblockencoding-robin-warmrobinhadamard8logicalunitary-cleanentry-eq-target). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinHadamard8FlatUnitary_cleanBlock" (lean := "QuantumBlockEncoding.Robin.warmRobinHadamard8FlatUnitary_cleanBlock")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin hadamard 8 flat unitary clean block”; the hypotheses and conclusion in the code panel fix its exact scope. The flat seven-qubit clean block is the normalized fixed Robin operator.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The flat seven-qubit clean block is the normalized fixed Robin operator.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Hadamard8BlockEncoding.lean:165](../../../../library/modules/robin-hadamard8blockencoding/#decl-quantumblockencoding-robin-warmrobinhadamard8flatunitary-cleanblock). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinComplexTarget" (lean := "QuantumBlockEncoding.Robin.warmRobinComplexTarget")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin complex target”. Complex view of the fixed Robin target used by the operator-first API.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Complex view of the fixed Robin target used by the operator-first API.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Hadamard8BlockEncoding.lean:177](../../../../library/modules/robin-hadamard8blockencoding/#decl-quantumblockencoding-robin-warmrobincomplextarget). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinQueryTarget" (lean := "QuantumBlockEncoding.Robin.warmRobinQueryTarget")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin query target”. Operator-first target contract for the fixed homogeneous Robin benchmark.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Operator-first target contract for the fixed homogeneous Robin benchmark.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Hadamard8BlockEncoding.lean:181](../../../../library/modules/robin-hadamard8blockencoding/#decl-quantumblockencoding-robin-warmrobinquerytarget). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinHadamard8T2Schedule" (lean := "QuantumBlockEncoding.Robin.warmRobinHadamard8T2Schedule")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin hadamard 8 t 2 schedule”. Four logical stages with the three selector Hadamards made explicit.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Four logical stages with the three selector Hadamards made explicit.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Hadamard8BlockEncoding.lean:190](../../../../library/modules/robin-hadamard8blockencoding/#decl-quantumblockencoding-robin-warmrobinhadamard8t2schedule). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinHadamard8T2Circuit" (lean := "QuantumBlockEncoding.Robin.warmRobinHadamard8T2Circuit")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin hadamard 8 t 2 circuit”. Logical gate list associated with the fair T2 schedule.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Logical gate list associated with the fair T2 schedule.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Hadamard8BlockEncoding.lean:202](../../../../library/modules/robin-hadamard8blockencoding/#decl-quantumblockencoding-robin-warmrobinhadamard8t2circuit). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinHadamard8T2Resource" (lean := "QuantumBlockEncoding.Robin.warmRobinHadamard8T2Resource")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin hadamard 8 t 2 resource”. Resource record under the logical-stage convention, not a T3 primitive count.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Resource record under the logical-stage convention, not a T3 primitive count.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Hadamard8BlockEncoding.lean:206](../../../../library/modules/robin-hadamard8blockencoding/#decl-quantumblockencoding-robin-warmrobinhadamard8t2resource). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinHadamard8BlockContainsTarget" (lean := "QuantumBlockEncoding.Robin.warmRobinHadamard8BlockContainsTarget")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin hadamard 8 block contains target”. The exact block predicate attached to the operator candidate.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The exact block predicate attached to the operator candidate.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Hadamard8BlockEncoding.lean:210](../../../../library/modules/robin-hadamard8blockencoding/#decl-quantumblockencoding-robin-warmrobinhadamard8blockcontainstarget). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinHadamard8BlockContainsTarget_proof" (lean := "QuantumBlockEncoding.Robin.warmRobinHadamard8BlockContainsTarget_proof")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin hadamard 8 block contains target proof”; the hypotheses and conclusion in the code panel fix its exact scope. The clean-block predicate follows from the specialized clean-entry theorem.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The clean-block predicate follows from the specialized clean-entry theorem.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Hadamard8BlockEncoding.lean:219](../../../../library/modules/robin-hadamard8blockencoding/#decl-quantumblockencoding-robin-warmrobinhadamard8blockcontainstarget-proof). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinHadamard8OperatorCandidate" (lean := "QuantumBlockEncoding.Robin.warmRobinHadamard8OperatorCandidate")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin hadamard 8 operator candidate”. Fixed 'N=8' Hadamard-8 candidate at the exact logical-unitary tier.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Fixed 'N=8' Hadamard-8 candidate at the exact logical-unitary tier.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Hadamard8BlockEncoding.lean:226](../../../../library/modules/robin-hadamard8blockencoding/#decl-quantumblockencoding-robin-warmrobinhadamard8operatorcandidate). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinHadamard8VerifiedBlockEncoding" (lean := "QuantumBlockEncoding.Robin.warmRobinHadamard8VerifiedBlockEncoding")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin hadamard 8 verified block encoding”. Fully verified T2 block encoding of the fixed Robin matrix.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Fully verified T2 block encoding of the fixed Robin matrix.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Hadamard8BlockEncoding.lean:246](../../../../library/modules/robin-hadamard8blockencoding/#decl-quantumblockencoding-robin-warmrobinhadamard8verifiedblockencoding). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinHadamard8T3BlockedLeaf" (lean := "QuantumBlockEncoding.Robin.warmRobinHadamard8T3BlockedLeaf")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin hadamard 8 t 3 blocked leaf”. Honest boundary: primitive synthesis/refinement is still a separate T3 theorem.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Honest boundary: primitive synthesis/refinement is still a separate T3 theorem.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Hadamard8BlockEncoding.lean:253](../../../../library/modules/robin-hadamard8blockencoding/#decl-quantumblockencoding-robin-warmrobinhadamard8t3blockedleaf). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/Robin/Hadamard8Verified.lean

16 explicit public declarations, in source order.

:::definition "QuantumBlockEncoding.Robin.WarmRobinHadamardBits" (lean := "QuantumBlockEncoding.Robin.WarmRobinHadamardBits")
*Plain-English reading.* This abbreviation gives a shorter name to the type or expression used for “warm robin hadamard bits”. Three binary selector wires before flattening to 'Fin 8'.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Three binary selector wires before flattening to 'Fin 8'.

*Declaration kind.* abbrev.

Source: [QuantumBlockEncoding/Robin/Hadamard8Verified.lean:20](../../../../library/modules/robin-hadamard8verified/#decl-quantumblockencoding-robin-warmrobinhadamardbits). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinHadamardBitsEquiv" (lean := "QuantumBlockEncoding.Robin.warmRobinHadamardBitsEquiv")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin hadamard bits equiv”. Signal-register order used by the eight-slot selector.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Signal-register order used by the eight-slot selector.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Hadamard8Verified.lean:23](../../../../library/modules/robin-hadamard8verified/#decl-quantumblockencoding-robin-warmrobinhadamardbitsequiv). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinUniformBitPrepare" (lean := "QuantumBlockEncoding.Robin.warmRobinUniformBitPrepare")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin uniform bit prepare”. One uniform binary PREPARE rotation.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* One uniform binary PREPARE rotation.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Hadamard8Verified.lean:28](../../../../library/modules/robin-hadamard8verified/#decl-quantumblockencoding-robin-warmrobinuniformbitprepare). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinUniformBitPrepare_unitary" (lean := "QuantumBlockEncoding.Robin.warmRobinUniformBitPrepare_unitary")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin uniform bit prepare unitary”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Hadamard8Verified.lean:32](../../../../library/modules/robin-hadamard8verified/#decl-quantumblockencoding-robin-warmrobinuniformbitprepare-unitary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinHadamardBitsPrepare" (lean := "QuantumBlockEncoding.Robin.warmRobinHadamardBitsPrepare")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin hadamard bits prepare”. Tensor product PREPARE on three binary selector wires.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Tensor product PREPARE on three binary selector wires.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Hadamard8Verified.lean:41](../../../../library/modules/robin-hadamard8verified/#decl-quantumblockencoding-robin-warmrobinhadamardbitsprepare). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinHadamardBitsPrepare_unitary" (lean := "QuantumBlockEncoding.Robin.warmRobinHadamardBitsPrepare_unitary")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin hadamard bits prepare unitary”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Hadamard8Verified.lean:46](../../../../library/modules/robin-hadamard8verified/#decl-quantumblockencoding-robin-warmrobinhadamardbitsprepare-unitary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinHadamard8SelectorPrepare" (lean := "QuantumBlockEncoding.Robin.warmRobinHadamard8SelectorPrepare")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin hadamard 8 selector prepare”. The eight-dimensional selector PREPARE in the flattened selector basis.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The eight-dimensional selector PREPARE in the flattened selector basis.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Hadamard8Verified.lean:56](../../../../library/modules/robin-hadamard8verified/#decl-quantumblockencoding-robin-warmrobinhadamard8selectorprepare). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinHadamard8SelectorPrepare_unitary" (lean := "QuantumBlockEncoding.Robin.warmRobinHadamard8SelectorPrepare_unitary")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin hadamard 8 selector prepare unitary”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Hadamard8Verified.lean:61](../../../../library/modules/robin-hadamard8verified/#decl-quantumblockencoding-robin-warmrobinhadamard8selectorprepare-unitary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinHadamard8Coefficient" (lean := "QuantumBlockEncoding.Robin.warmRobinHadamard8Coefficient")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin hadamard 8 coefficient”. Real clean amplitude used by selector slot and system column.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Real clean amplitude used by selector slot and system column.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Hadamard8Verified.lean:68](../../../../library/modules/robin-hadamard8verified/#decl-quantumblockencoding-robin-warmrobinhadamard8coefficient). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinHadamard8Coefficient_abs_le_one" (lean := "QuantumBlockEncoding.Robin.warmRobinHadamard8Coefficient_abs_le_one")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin hadamard 8 coefficient abs le one”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Hadamard8Verified.lean:71](../../../../library/modules/robin-hadamard8verified/#decl-quantumblockencoding-robin-warmrobinhadamard8coefficient-abs-le-one). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinHadamard8Rotation" (lean := "QuantumBlockEncoding.Robin.warmRobinHadamard8Rotation")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin hadamard 8 rotation”. Controlled two-dimensional amplitude block.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Controlled two-dimensional amplitude block.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Hadamard8Verified.lean:79](../../../../library/modules/robin-hadamard8verified/#decl-quantumblockencoding-robin-warmrobinhadamard8rotation). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinHadamard8Rotation_unitary" (lean := "QuantumBlockEncoding.Robin.warmRobinHadamard8Rotation_unitary")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin hadamard 8 rotation unitary”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Hadamard8Verified.lean:83](../../../../library/modules/robin-hadamard8verified/#decl-quantumblockencoding-robin-warmrobinhadamard8rotation-unitary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinHadamard8Rotation_cleanEntry" (lean := "QuantumBlockEncoding.Robin.warmRobinHadamard8Rotation_cleanEntry")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin hadamard 8 rotation clean entry”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Hadamard8Verified.lean:88](../../../../library/modules/robin-hadamard8verified/#decl-quantumblockencoding-robin-warmrobinhadamard8rotation-cleanentry). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinHadamard8SystemEquiv" (lean := "QuantumBlockEncoding.Robin.warmRobinHadamard8SystemEquiv")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin hadamard 8 system equiv”. Each certified system permutation is packaged as an equivalence.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Each certified system permutation is packaged as an equivalence.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Hadamard8Verified.lean:96](../../../../library/modules/robin-hadamard8verified/#decl-quantumblockencoding-robin-warmrobinhadamard8systemequiv). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinHadamard8LogicalUnitary" (lean := "QuantumBlockEncoding.Robin.warmRobinHadamard8LogicalUnitary")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin hadamard 8 logical unitary”. Product-register matrix before flattening to seven qubits.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Product-register matrix before flattening to seven qubits.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/Hadamard8Verified.lean:102](../../../../library/modules/robin-hadamard8verified/#decl-quantumblockencoding-robin-warmrobinhadamard8logicalunitary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinHadamard8LogicalUnitary_unitary" (lean := "QuantumBlockEncoding.Robin.warmRobinHadamard8LogicalUnitary_unitary")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin hadamard 8 logical unitary unitary”; the hypotheses and conclusion in the code panel fix its exact scope. The complete Hadamard-8 logical matrix is a standard complex unitary.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The complete Hadamard-8 logical matrix is a standard complex unitary.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/Hadamard8Verified.lean:112](../../../../library/modules/robin-hadamard8verified/#decl-quantumblockencoding-robin-warmrobinhadamard8logicalunitary-unitary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/Robin/PaperSevenAmplitudePrimitive.lean

16 explicit public declarations, in source order.

:::definition "QuantumBlockEncoding.Robin.warmRobinPaperSevenAmplitudeControlWires" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenAmplitudeControlWires")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin paper seven amplitude control wires”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/PaperSevenAmplitudePrimitive.lean:14](../../../../library/modules/robin-papersevenamplitudeprimitive/#decl-quantumblockencoding-robin-warmrobinpapersevenamplitudecontrolwires). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinPaperSevenAmplitudeControlWires_ne_target" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenAmplitudeControlWires_ne_target")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin paper seven amplitude control wires ne target”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/PaperSevenAmplitudePrimitive.lean:17](../../../../library/modules/robin-papersevenamplitudeprimitive/#decl-quantumblockencoding-robin-warmrobinpapersevenamplitudecontrolwires-ne-target). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinPaperSevenAmplitudeSystem" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenAmplitudeSystem")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin paper seven amplitude system”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/PaperSevenAmplitudePrimitive.lean:24](../../../../library/modules/robin-papersevenamplitudeprimitive/#decl-quantumblockencoding-robin-warmrobinpapersevenamplitudesystem). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinPaperSevenAmplitudeSelector" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenAmplitudeSelector")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin paper seven amplitude selector”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/PaperSevenAmplitudePrimitive.lean:28](../../../../library/modules/robin-papersevenamplitudeprimitive/#decl-quantumblockencoding-robin-warmrobinpapersevenamplitudeselector). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinPaperSevenAmplitudeAngle" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenAmplitudeAngle")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin paper seven amplitude angle”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/PaperSevenAmplitudePrimitive.lean:32](../../../../library/modules/robin-papersevenamplitudeprimitive/#decl-quantumblockencoding-robin-warmrobinpapersevenamplitudeangle). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinPaperSevenAmplitudeRy_eq_rotation" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenAmplitudeRy_eq_rotation")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin paper seven amplitude ry eq rotation”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/PaperSevenAmplitudePrimitive.lean:44](../../../../library/modules/robin-papersevenamplitudeprimitive/#decl-quantumblockencoding-robin-warmrobinpapersevenamplitudery-eq-rotation). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinPaperSevenAmplitudeContextIndex" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenAmplitudeContextIndex")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin paper seven amplitude context index”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/PaperSevenAmplitudePrimitive.lean:58](../../../../library/modules/robin-papersevenamplitudeprimitive/#decl-quantumblockencoding-robin-warmrobinpapersevenamplitudecontextindex). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinPaperSevenAmplitudeContextIndex_bijective" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenAmplitudeContextIndex_bijective")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin paper seven amplitude context index bijective”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/PaperSevenAmplitudePrimitive.lean:70](../../../../library/modules/robin-papersevenamplitudeprimitive/#decl-quantumblockencoding-robin-warmrobinpapersevenamplitudecontextindex-bijective). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinPaperSevenAmplitudeContextEquiv" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenAmplitudeContextEquiv")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin paper seven amplitude context equiv”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/PaperSevenAmplitudePrimitive.lean:74](../../../../library/modules/robin-papersevenamplitudeprimitive/#decl-quantumblockencoding-robin-warmrobinpapersevenamplitudecontextequiv). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinPaperSevenAmplitudeContextEquiv_apply" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenAmplitudeContextEquiv_apply")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin paper seven amplitude context equiv apply”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/PaperSevenAmplitudePrimitive.lean:80](../../../../library/modules/robin-papersevenamplitudeprimitive/#decl-quantumblockencoding-robin-warmrobinpapersevenamplitudecontextequiv-apply). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinPaperSevenAmplitude_context_iff" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenAmplitude_context_iff")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin paper seven amplitude context iff”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/PaperSevenAmplitudePrimitive.lean:85](../../../../library/modules/robin-papersevenamplitudeprimitive/#decl-quantumblockencoding-robin-warmrobinpapersevenamplitude-context-iff). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinPaperSevenControlledRy_eq_amplitudeLift" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenControlledRy_eq_amplitudeLift")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin paper seven controlled ry eq amplitude lift”; the hypotheses and conclusion in the code panel fix its exact scope. Exact equality between the physical six-control RY block and the logical amplitude lift, including the otherwise dirty 'q7' workspace coordinate.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Exact equality between the physical six-control RY block and the logical amplitude lift, including the otherwise dirty 'q7' workspace coordinate.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/PaperSevenAmplitudePrimitive.lean:100](../../../../library/modules/robin-papersevenamplitudeprimitive/#decl-quantumblockencoding-robin-warmrobinpapersevencontrolledry-eq-amplitudelift). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinPaperSevenAmplitudeCircuit" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenAmplitudeCircuit")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin paper seven amplitude circuit”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/PaperSevenAmplitudePrimitive.lean:135](../../../../library/modules/robin-papersevenamplitudeprimitive/#decl-quantumblockencoding-robin-warmrobinpapersevenamplitudecircuit). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinPaperSevenAmplitudeProgram" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenAmplitudeProgram")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin paper seven amplitude program”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/PaperSevenAmplitudePrimitive.lean:140](../../../../library/modules/robin-papersevenamplitudeprimitive/#decl-quantumblockencoding-robin-warmrobinpapersevenamplitudeprogram). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinPaperSevenAmplitudeProgram_eval" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenAmplitudeProgram_eval")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin paper seven amplitude program eval”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/PaperSevenAmplitudePrimitive.lean:144](../../../../library/modules/robin-papersevenamplitudeprimitive/#decl-quantumblockencoding-robin-warmrobinpapersevenamplitudeprogram-eval). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinPaperSevenAmplitudeProgram_noOracleCalls" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenAmplitudeProgram_noOracleCalls")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin paper seven amplitude program no oracle calls”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/PaperSevenAmplitudePrimitive.lean:158](../../../../library/modules/robin-papersevenamplitudeprimitive/#decl-quantumblockencoding-robin-warmrobinpapersevenamplitudeprogram-nooraclecalls). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/Robin/PaperSevenLogicalUnitary.lean

27 explicit public declarations, in source order.

:::definition "QuantumBlockEncoding.Robin.warmRobinPaperSevenSystemPerm" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenSystemPerm")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin paper seven system perm”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/PaperSevenLogicalUnitary.lean:20](../../../../library/modules/robin-papersevenlogicalunitary/#decl-quantumblockencoding-robin-warmrobinpapersevensystemperm). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinPaperSevenSystemPerm_bijective" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenSystemPerm_bijective")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin paper seven system perm bijective”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/PaperSevenLogicalUnitary.lean:23](../../../../library/modules/robin-papersevenlogicalunitary/#decl-quantumblockencoding-robin-warmrobinpapersevensystemperm-bijective). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinPaperSevenSystemEquiv" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenSystemEquiv")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin paper seven system equiv”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/PaperSevenLogicalUnitary.lean:27](../../../../library/modules/robin-papersevenlogicalunitary/#decl-quantumblockencoding-robin-warmrobinpapersevensystemequiv). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinPaperSevenSystemEquiv_apply" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenSystemEquiv_apply")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin paper seven system equiv apply”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/PaperSevenLogicalUnitary.lean:32](../../../../library/modules/robin-papersevenlogicalunitary/#decl-quantumblockencoding-robin-warmrobinpapersevensystemequiv-apply). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinPaperSevenCoefficientRat" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenCoefficientRat")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin paper seven coefficient rat”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/PaperSevenLogicalUnitary.lean:37](../../../../library/modules/robin-papersevenlogicalunitary/#decl-quantumblockencoding-robin-warmrobinpapersevencoefficientrat). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinPaperSevenCoefficientRat_abs_le_one" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenCoefficientRat_abs_le_one")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin paper seven coefficient rat abs le one”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/PaperSevenLogicalUnitary.lean:40](../../../../library/modules/robin-papersevenlogicalunitary/#decl-quantumblockencoding-robin-warmrobinpapersevencoefficientrat-abs-le-one). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinPaperSevenCoefficient" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenCoefficient")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin paper seven coefficient”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/PaperSevenLogicalUnitary.lean:45](../../../../library/modules/robin-papersevenlogicalunitary/#decl-quantumblockencoding-robin-warmrobinpapersevencoefficient). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinPaperSevenCoefficient_abs_le_one" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenCoefficient_abs_le_one")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin paper seven coefficient abs le one”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/PaperSevenLogicalUnitary.lean:48](../../../../library/modules/robin-papersevenlogicalunitary/#decl-quantumblockencoding-robin-warmrobinpapersevencoefficient-abs-le-one). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinPaperSevenRotation" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenRotation")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin paper seven rotation”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/PaperSevenLogicalUnitary.lean:57](../../../../library/modules/robin-papersevenlogicalunitary/#decl-quantumblockencoding-robin-warmrobinpapersevenrotation). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinPaperSevenRotation_unitary" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenRotation_unitary")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin paper seven rotation unitary”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/PaperSevenLogicalUnitary.lean:61](../../../../library/modules/robin-papersevenlogicalunitary/#decl-quantumblockencoding-robin-warmrobinpapersevenrotation-unitary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinPaperSevenRotation_cleanEntry" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenRotation_cleanEntry")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin paper seven rotation clean entry”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/PaperSevenLogicalUnitary.lean:66](../../../../library/modules/robin-papersevenlogicalunitary/#decl-quantumblockencoding-robin-warmrobinpapersevenrotation-cleanentry). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinPaperSevenLogicalUnitary" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenLogicalUnitary")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin paper seven logical unitary”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/PaperSevenLogicalUnitary.lean:73](../../../../library/modules/robin-papersevenlogicalunitary/#decl-quantumblockencoding-robin-warmrobinpapersevenlogicalunitary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinPaperSevenLogicalUnitary_unitary" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenLogicalUnitary_unitary")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin paper seven logical unitary unitary”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/PaperSevenLogicalUnitary.lean:82](../../../../library/modules/robin-papersevenlogicalunitary/#decl-quantumblockencoding-robin-warmrobinpapersevenlogicalunitary-unitary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinPaperSevenLogicalUnitary_cleanEntry" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenLogicalUnitary_cleanEntry")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin paper seven logical unitary clean entry”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/PaperSevenLogicalUnitary.lean:90](../../../../library/modules/robin-papersevenlogicalunitary/#decl-quantumblockencoding-robin-warmrobinpapersevenlogicalunitary-cleanentry). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinPaperSevenLogicalUnitary_cleanBlock" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenLogicalUnitary_cleanBlock")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin paper seven logical unitary clean block”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/PaperSevenLogicalUnitary.lean:135](../../../../library/modules/robin-papersevenlogicalunitary/#decl-quantumblockencoding-robin-warmrobinpapersevenlogicalunitary-cleanblock). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinPaperSevenIndexEquiv" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenIndexEquiv")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin paper seven index equiv”. Flatten coefficient, selector, and system registers to seven qubits.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Flatten coefficient, selector, and system registers to seven qubits.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/PaperSevenLogicalUnitary.lean:147](../../../../library/modules/robin-papersevenlogicalunitary/#decl-quantumblockencoding-robin-warmrobinpapersevenindexequiv). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinPaperSevenCleanIndex" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenCleanIndex")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin paper seven clean index”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/PaperSevenLogicalUnitary.lean:152](../../../../library/modules/robin-papersevenlogicalunitary/#decl-quantumblockencoding-robin-warmrobinpapersevencleanindex). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinPaperSevenFlatUnitary" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenFlatUnitary")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin paper seven flat unitary”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/PaperSevenLogicalUnitary.lean:156](../../../../library/modules/robin-papersevenlogicalunitary/#decl-quantumblockencoding-robin-warmrobinpapersevenflatunitary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinPaperSevenFlatUnitary_unitary" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenFlatUnitary_unitary")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin paper seven flat unitary unitary”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/PaperSevenLogicalUnitary.lean:161](../../../../library/modules/robin-papersevenlogicalunitary/#decl-quantumblockencoding-robin-warmrobinpapersevenflatunitary-unitary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinPaperSevenFlatUnitary_cleanBlock" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenFlatUnitary_cleanBlock")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin paper seven flat unitary clean block”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/PaperSevenLogicalUnitary.lean:167](../../../../library/modules/robin-papersevenlogicalunitary/#decl-quantumblockencoding-robin-warmrobinpapersevenflatunitary-cleanblock). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinPaperSevenBlockContainsTarget" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenBlockContainsTarget")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin paper seven block contains target”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/PaperSevenLogicalUnitary.lean:183](../../../../library/modules/robin-papersevenlogicalunitary/#decl-quantumblockencoding-robin-warmrobinpapersevenblockcontainstarget). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinPaperSevenBlockContainsTarget_proof" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenBlockContainsTarget_proof")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin paper seven block contains target proof”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/PaperSevenLogicalUnitary.lean:191](../../../../library/modules/robin-papersevenlogicalunitary/#decl-quantumblockencoding-robin-warmrobinpapersevenblockcontainstarget-proof). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinPaperSevenT2Schedule" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenT2Schedule")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin paper seven t 2 schedule”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/PaperSevenLogicalUnitary.lean:197](../../../../library/modules/robin-papersevenlogicalunitary/#decl-quantumblockencoding-robin-warmrobinpapersevent2schedule). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinPaperSevenT2Circuit" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenT2Circuit")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin paper seven t 2 circuit”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/PaperSevenLogicalUnitary.lean:203](../../../../library/modules/robin-papersevenlogicalunitary/#decl-quantumblockencoding-robin-warmrobinpapersevent2circuit). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinPaperSevenT2Resource" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenT2Resource")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin paper seven t 2 resource”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/PaperSevenLogicalUnitary.lean:206](../../../../library/modules/robin-papersevenlogicalunitary/#decl-quantumblockencoding-robin-warmrobinpapersevent2resource). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinPaperSevenOperatorCandidate" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenOperatorCandidate")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin paper seven operator candidate”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/PaperSevenLogicalUnitary.lean:209](../../../../library/modules/robin-papersevenlogicalunitary/#decl-quantumblockencoding-robin-warmrobinpapersevenoperatorcandidate). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinPaperSevenVerifiedBlockEncoding" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenVerifiedBlockEncoding")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin paper seven verified block encoding”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/PaperSevenLogicalUnitary.lean:227](../../../../library/modules/robin-papersevenlogicalunitary/#decl-quantumblockencoding-robin-warmrobinpapersevenverifiedblockencoding). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/Robin/PaperSevenPrepare.lean

20 explicit public declarations, in source order.

:::definition "QuantumBlockEncoding.Robin.warmRobinUniformSevenHighAngle" (lean := "QuantumBlockEncoding.Robin.warmRobinUniformSevenHighAngle")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin uniform seven high angle”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/PaperSevenPrepare.lean:17](../../../../library/modules/robin-papersevenprepare/#decl-quantumblockencoding-robin-warmrobinuniformsevenhighangle). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinUniformSevenTailAngle" (lean := "QuantumBlockEncoding.Robin.warmRobinUniformSevenTailAngle")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin uniform seven tail angle”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/PaperSevenPrepare.lean:20](../../../../library/modules/robin-papersevenprepare/#decl-quantumblockencoding-robin-warmrobinuniformseventailangle). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinUniformSevenMiddleAngles" (lean := "QuantumBlockEncoding.Robin.warmRobinUniformSevenMiddleAngles")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin uniform seven middle angles”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/PaperSevenPrepare.lean:23](../../../../library/modules/robin-papersevenprepare/#decl-quantumblockencoding-robin-warmrobinuniformsevenmiddleangles). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinUniformSevenLowAngles" (lean := "QuantumBlockEncoding.Robin.warmRobinUniformSevenLowAngles")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin uniform seven low angles”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/PaperSevenPrepare.lean:28](../../../../library/modules/robin-papersevenprepare/#decl-quantumblockencoding-robin-warmrobinuniformsevenlowangles). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinUniformSevenMiddleWires" (lean := "QuantumBlockEncoding.Robin.warmRobinUniformSevenMiddleWires")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin uniform seven middle wires”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/PaperSevenPrepare.lean:33](../../../../library/modules/robin-papersevenprepare/#decl-quantumblockencoding-robin-warmrobinuniformsevenmiddlewires). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinUniformSevenMiddleWires_ne_target" (lean := "QuantumBlockEncoding.Robin.warmRobinUniformSevenMiddleWires_ne_target")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin uniform seven middle wires ne target”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/PaperSevenPrepare.lean:35](../../../../library/modules/robin-papersevenprepare/#decl-quantumblockencoding-robin-warmrobinuniformsevenmiddlewires-ne-target). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinUniformSevenLowWires" (lean := "QuantumBlockEncoding.Robin.warmRobinUniformSevenLowWires")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin uniform seven low wires”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/PaperSevenPrepare.lean:40](../../../../library/modules/robin-papersevenprepare/#decl-quantumblockencoding-robin-warmrobinuniformsevenlowwires). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinUniformSevenLowWires_ne_target" (lean := "QuantumBlockEncoding.Robin.warmRobinUniformSevenLowWires_ne_target")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin uniform seven low wires ne target”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/PaperSevenPrepare.lean:44](../../../../library/modules/robin-papersevenprepare/#decl-quantumblockencoding-robin-warmrobinuniformsevenlowwires-ne-target). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinUniformSevenPrepareCircuit" (lean := "QuantumBlockEncoding.Robin.warmRobinUniformSevenPrepareCircuit")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin uniform seven prepare circuit”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/PaperSevenPrepare.lean:48](../../../../library/modules/robin-papersevenprepare/#decl-quantumblockencoding-robin-warmrobinuniformsevenpreparecircuit). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinUniformSevenPrepareProgram" (lean := "QuantumBlockEncoding.Robin.warmRobinUniformSevenPrepareProgram")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin uniform seven prepare program”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/PaperSevenPrepare.lean:56](../../../../library/modules/robin-papersevenprepare/#decl-quantumblockencoding-robin-warmrobinuniformsevenprepareprogram). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinUniformSevenPrepareMatrix" (lean := "QuantumBlockEncoding.Robin.warmRobinUniformSevenPrepareMatrix")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin uniform seven prepare matrix”. Independent stagewise matrix specification for the padded selector.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Independent stagewise matrix specification for the padded selector.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/PaperSevenPrepare.lean:61](../../../../library/modules/robin-papersevenprepare/#decl-quantumblockencoding-robin-warmrobinuniformsevenpreparematrix). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinUniformSevenPrepareProgram_eval" (lean := "QuantumBlockEncoding.Robin.warmRobinUniformSevenPrepareProgram_eval")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin uniform seven prepare program eval”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/PaperSevenPrepare.lean:70](../../../../library/modules/robin-papersevenprepare/#decl-quantumblockencoding-robin-warmrobinuniformsevenprepareprogram-eval). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinPaperSevenSelectorPrepare_unitary" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenSelectorPrepare_unitary")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin paper seven selector prepare unitary”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/PaperSevenPrepare.lean:84](../../../../library/modules/robin-papersevenprepare/#decl-quantumblockencoding-robin-warmrobinpapersevenselectorprepare-unitary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinUniformSevenPrepare_noOracleCalls" (lean := "QuantumBlockEncoding.Robin.warmRobinUniformSevenPrepare_noOracleCalls")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin uniform seven prepare no oracle calls”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/PaperSevenPrepare.lean:90](../../../../library/modules/robin-papersevenprepare/#decl-quantumblockencoding-robin-warmrobinuniformsevenprepare-nooraclecalls). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinUniformSevenPrepare_counts" (lean := "QuantumBlockEncoding.Robin.warmRobinUniformSevenPrepare_counts")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin uniform seven prepare counts”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/PaperSevenPrepare.lean:94](../../../../library/modules/robin-papersevenprepare/#decl-quantumblockencoding-robin-warmrobinuniformsevenprepare-counts). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinPaperSevenPaddedSlot" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenPaddedSlot")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin paper seven padded slot”. The source selector has eight physical states even though only seven are active.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source selector has eight physical states even though only seven are active. This prevents accidental use of 'Fin 7' as a three-qubit register.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/PaperSevenPrepare.lean:111](../../../../library/modules/robin-papersevenprepare/#decl-quantumblockencoding-robin-warmrobinpapersevenpaddedslot). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinPaperSevenPaddedSlot_seven" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenPaddedSlot_seven")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin paper seven padded slot seven”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/PaperSevenPrepare.lean:114](../../../../library/modules/robin-papersevenprepare/#decl-quantumblockencoding-robin-warmrobinpapersevenpaddedslot-seven). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinPaperSevenSelectorPrepare" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenSelectorPrepare")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin paper seven selector prepare”. The physical three-qubit PREPARE, flattened with the repository's declared little-endian convention.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The physical three-qubit PREPARE, flattened with the repository's declared little-endian convention.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/PaperSevenPrepare.lean:119](../../../../library/modules/robin-papersevenprepare/#decl-quantumblockencoding-robin-warmrobinpapersevenselectorprepare). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinPaperSevenSelectorPrepare_unitary_flat" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenSelectorPrepare_unitary_flat")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin paper seven selector prepare unitary flat”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/PaperSevenPrepare.lean:124](../../../../library/modules/robin-papersevenprepare/#decl-quantumblockencoding-robin-warmrobinpapersevenselectorprepare-unitary-flat). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinUniformSevenPrepare_probability" (lean := "QuantumBlockEncoding.Robin.warmRobinUniformSevenPrepare_probability")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin uniform seven prepare probability”; the hypotheses and conclusion in the code panel fix its exact scope. The theorem uses probabilities directly, so no arbitrary clean-column phase convention for '1 / sqrt 7' enters the LCU proof.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The theorem uses probabilities directly, so no arbitrary clean-column phase convention for '1 / sqrt 7' enters the LCU proof.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/PaperSevenPrepare.lean:134](../../../../library/modules/robin-papersevenprepare/#decl-quantumblockencoding-robin-warmrobinuniformsevenprepare-probability). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/Robin/PaperSevenPreparePrimitive.lean

19 explicit public declarations, in source order.

:::definition "QuantumBlockEncoding.Robin.warmRobinPaperSevenSelectorHighMatrix" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenSelectorHighMatrix")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin paper seven selector high matrix”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/PaperSevenPreparePrimitive.lean:13](../../../../library/modules/robin-papersevenprepareprimitive/#decl-quantumblockencoding-robin-warmrobinpapersevenselectorhighmatrix). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinPaperSevenSelectorMiddleMatrix" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenSelectorMiddleMatrix")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin paper seven selector middle matrix”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/PaperSevenPreparePrimitive.lean:18](../../../../library/modules/robin-papersevenprepareprimitive/#decl-quantumblockencoding-robin-warmrobinpapersevenselectormiddlematrix). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinPaperSevenSelectorLowMatrix" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenSelectorLowMatrix")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin paper seven selector low matrix”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/PaperSevenPreparePrimitive.lean:25](../../../../library/modules/robin-papersevenprepareprimitive/#decl-quantumblockencoding-robin-warmrobinpapersevenselectorlowmatrix). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinPaperSevenSelectorStages_eq_prepare" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenSelectorStages_eq_prepare")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin paper seven selector stages eq prepare”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/PaperSevenPreparePrimitive.lean:31](../../../../library/modules/robin-papersevenprepareprimitive/#decl-quantumblockencoding-robin-warmrobinpapersevenselectorstages-eq-prepare). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinPaperSevenMiddlePhysicalWires" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenMiddlePhysicalWires")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin paper seven middle physical wires”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/PaperSevenPreparePrimitive.lean:43](../../../../library/modules/robin-papersevenprepareprimitive/#decl-quantumblockencoding-robin-warmrobinpapersevenmiddlephysicalwires). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinPaperSevenMiddlePhysicalWires_ne_target" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenMiddlePhysicalWires_ne_target")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin paper seven middle physical wires ne target”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/PaperSevenPreparePrimitive.lean:45](../../../../library/modules/robin-papersevenprepareprimitive/#decl-quantumblockencoding-robin-warmrobinpapersevenmiddlephysicalwires-ne-target). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinPaperSevenLowPhysicalWires" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenLowPhysicalWires")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin paper seven low physical wires”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/PaperSevenPreparePrimitive.lean:51](../../../../library/modules/robin-papersevenprepareprimitive/#decl-quantumblockencoding-robin-warmrobinpapersevenlowphysicalwires). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinPaperSevenLowPhysicalWires_ne_target" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenLowPhysicalWires_ne_target")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin paper seven low physical wires ne target”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/PaperSevenPreparePrimitive.lean:55](../../../../library/modules/robin-papersevenprepareprimitive/#decl-quantumblockencoding-robin-warmrobinpapersevenlowphysicalwires-ne-target). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinPaperSevenSelectorBits_decode" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenSelectorBits_decode")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin paper seven selector bits decode”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/PaperSevenPreparePrimitive.lean:60](../../../../library/modules/robin-papersevenprepareprimitive/#decl-quantumblockencoding-robin-warmrobinpapersevenselectorbits-decode). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinPaperSevenHighContext_iff" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenHighContext_iff")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin paper seven high context iff”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/PaperSevenPreparePrimitive.lean:66](../../../../library/modules/robin-papersevenprepareprimitive/#decl-quantumblockencoding-robin-warmrobinpapersevenhighcontext-iff). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinPaperSevenHighPhysical_eval" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenHighPhysical_eval")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin paper seven high physical eval”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/PaperSevenPreparePrimitive.lean:79](../../../../library/modules/robin-papersevenprepareprimitive/#decl-quantumblockencoding-robin-warmrobinpapersevenhighphysical-eval). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinPaperSevenMiddleContext_iff" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenMiddleContext_iff")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin paper seven middle context iff”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/PaperSevenPreparePrimitive.lean:113](../../../../library/modules/robin-papersevenprepareprimitive/#decl-quantumblockencoding-robin-warmrobinpapersevenmiddlecontext-iff). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinPaperSevenMiddlePhysical_eval" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenMiddlePhysical_eval")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin paper seven middle physical eval”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/PaperSevenPreparePrimitive.lean:126](../../../../library/modules/robin-papersevenprepareprimitive/#decl-quantumblockencoding-robin-warmrobinpapersevenmiddlephysical-eval). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinPaperSevenLowContext_iff" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenLowContext_iff")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin paper seven low context iff”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/PaperSevenPreparePrimitive.lean:173](../../../../library/modules/robin-papersevenprepareprimitive/#decl-quantumblockencoding-robin-warmrobinpapersevenlowcontext-iff). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinPaperSevenLowPhysical_eval" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenLowPhysical_eval")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin paper seven low physical eval”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/PaperSevenPreparePrimitive.lean:186](../../../../library/modules/robin-papersevenprepareprimitive/#decl-quantumblockencoding-robin-warmrobinpapersevenlowphysical-eval). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinPaperSevenSelectorPrepareCircuit" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenSelectorPrepareCircuit")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin paper seven selector prepare circuit”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/PaperSevenPreparePrimitive.lean:233](../../../../library/modules/robin-papersevenprepareprimitive/#decl-quantumblockencoding-robin-warmrobinpapersevenselectorpreparecircuit). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinPaperSevenSelectorPrepareCircuit_eval" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenSelectorPrepareCircuit_eval")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin paper seven selector prepare circuit eval”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/PaperSevenPreparePrimitive.lean:243](../../../../library/modules/robin-papersevenprepareprimitive/#decl-quantumblockencoding-robin-warmrobinpapersevenselectorpreparecircuit-eval). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinPaperSevenSelectorUnprepareCircuit" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenSelectorUnprepareCircuit")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin paper seven selector unprepare circuit”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/PaperSevenPreparePrimitive.lean:270](../../../../library/modules/robin-papersevenprepareprimitive/#decl-quantumblockencoding-robin-warmrobinpapersevenselectorunpreparecircuit). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinPaperSevenSelectorUnprepareCircuit_eval" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenSelectorUnprepareCircuit_eval")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin paper seven selector unprepare circuit eval”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/PaperSevenPreparePrimitive.lean:273](../../../../library/modules/robin-papersevenprepareprimitive/#decl-quantumblockencoding-robin-warmrobinpapersevenselectorunpreparecircuit-eval). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/Robin/PaperSevenPrimitive.lean

29 explicit public declarations, in source order.

:::definition "QuantumBlockEncoding.Robin.warmRobinPaperSevenSelectReversibleProgram" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenSelectReversibleProgram")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin paper seven select reversible program”. Physical SELECT in the declared eight-wire order.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Physical SELECT in the declared eight-wire order. CCX remains only in the proof IR and is compiled away by 'compileReversibleProgram'.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/PaperSevenPrimitive.lean:19](../../../../library/modules/robin-papersevenprimitive/#decl-quantumblockencoding-robin-warmrobinpapersevenselectreversibleprogram). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinPaperSevenSelectBasisEquiv" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenSelectBasisEquiv")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin paper seven select basis equiv”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/PaperSevenPrimitive.lean:34](../../../../library/modules/robin-papersevenprimitive/#decl-quantumblockencoding-robin-warmrobinpapersevenselectbasisequiv). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinPaperSevenSystemBits" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenSystemBits")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin paper seven system bits”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/PaperSevenPrimitive.lean:38](../../../../library/modules/robin-papersevenprimitive/#decl-quantumblockencoding-robin-warmrobinpapersevensystembits). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinPaperSevenSelectorBits" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenSelectorBits")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin paper seven selector bits”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/PaperSevenPrimitive.lean:41](../../../../library/modules/robin-papersevenprimitive/#decl-quantumblockencoding-robin-warmrobinpapersevenselectorbits). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinPaperSevenSelectProgram_cleanAction" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenSelectProgram_cleanAction")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin paper seven select program clean action”; the hypotheses and conclusion in the code panel fix its exact scope. Clean-workspace action of the source SELECT.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Clean-workspace action of the source SELECT.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/PaperSevenPrimitive.lean:45](../../../../library/modules/robin-papersevenprimitive/#decl-quantumblockencoding-robin-warmrobinpapersevenselectprogram-cleanaction). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinPaperSevenSelectProgram_workspaceClean" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenSelectProgram_workspaceClean")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin paper seven select program workspace clean”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/PaperSevenPrimitive.lean:56](../../../../library/modules/robin-papersevenprimitive/#decl-quantumblockencoding-robin-warmrobinpapersevenselectprogram-workspaceclean). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinPaperSevenSelectProgram" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenSelectProgram")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin paper seven select program”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/PaperSevenPrimitive.lean:61](../../../../library/modules/robin-papersevenprimitive/#decl-quantumblockencoding-robin-warmrobinpapersevenselectprogram). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinPaperSevenSelectProgram_eval" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenSelectProgram_eval")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin paper seven select program eval”; the hypotheses and conclusion in the code panel fix its exact scope. Exact primitive matrix for source SELECT, including its exact compiler phase.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Exact primitive matrix for source SELECT, including its exact compiler phase.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/PaperSevenPrimitive.lean:66](../../../../library/modules/robin-papersevenprimitive/#decl-quantumblockencoding-robin-warmrobinpapersevenselectprogram-eval). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinPaperSevenSelectProgram_noOracleCalls" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenSelectProgram_noOracleCalls")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin paper seven select program no oracle calls”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/PaperSevenPrimitive.lean:72](../../../../library/modules/robin-papersevenprimitive/#decl-quantumblockencoding-robin-warmrobinpapersevenselectprogram-nooraclecalls). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.WarmRobinPaperSevenFullSystem" (lean := "QuantumBlockEncoding.Robin.WarmRobinPaperSevenFullSystem")
*Plain-English reading.* This abbreviation gives a shorter name to the type or expression used for “warm robin paper seven full system”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* abbrev.

Source: [QuantumBlockEncoding/Robin/PaperSevenPrimitive.lean:76](../../../../library/modules/robin-papersevenprimitive/#decl-quantumblockencoding-robin-warmrobinpapersevenfullsystem). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinPaperSevenEncodeBits" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenEncodeBits")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin paper seven encode bits”. Encode the declared register product into the physical wire order.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Encode the declared register product into the physical wire order.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/PaperSevenPrimitive.lean:79](../../../../library/modules/robin-papersevenprimitive/#decl-quantumblockencoding-robin-warmrobinpapersevenencodebits). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinPaperSevenBitsIndex" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenBitsIndex")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin paper seven bits index”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/PaperSevenPrimitive.lean:91](../../../../library/modules/robin-papersevenprimitive/#decl-quantumblockencoding-robin-warmrobinpapersevenbitsindex). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinPaperSevenBitsIndex_bijective" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenBitsIndex_bijective")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin paper seven bits index bijective”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/PaperSevenPrimitive.lean:96](../../../../library/modules/robin-papersevenprimitive/#decl-quantumblockencoding-robin-warmrobinpapersevenbitsindex-bijective). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinPaperSevenBitsEquiv" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenBitsEquiv")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin paper seven bits equiv”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/PaperSevenPrimitive.lean:100](../../../../library/modules/robin-papersevenprimitive/#decl-quantumblockencoding-robin-warmrobinpapersevenbitsequiv). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinPaperSevenBitsEquiv_apply" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenBitsEquiv_apply")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin paper seven bits equiv apply”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/PaperSevenPrimitive.lean:106](../../../../library/modules/robin-papersevenprimitive/#decl-quantumblockencoding-robin-warmrobinpapersevenbitsequiv-apply). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinBitXor" (lean := "QuantumBlockEncoding.Robin.warmRobinBitXor")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin bit xor”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/PaperSevenPrimitive.lean:110](../../../../library/modules/robin-papersevenprimitive/#decl-quantumblockencoding-robin-warmrobinbitxor). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinBitAnd" (lean := "QuantumBlockEncoding.Robin.warmRobinBitAnd")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin bit and”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/PaperSevenPrimitive.lean:113](../../../../library/modules/robin-papersevenprimitive/#decl-quantumblockencoding-robin-warmrobinbitand). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinPaperSevenFullSystemPerm" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenFullSystemPerm")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin paper seven full system perm”. Explicit full-space action of the reversible adder.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Explicit full-space action of the reversible adder. On dirty workspace this records the actual extension instead of claiming tensor identity.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/PaperSevenPrimitive.lean:118](../../../../library/modules/robin-papersevenprimitive/#decl-quantumblockencoding-robin-warmrobinpapersevenfullsystemperm). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinPaperSevenFullSystemPerm_bijective" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenFullSystemPerm_bijective")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin paper seven full system perm bijective”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/PaperSevenPrimitive.lean:133](../../../../library/modules/robin-papersevenprimitive/#decl-quantumblockencoding-robin-warmrobinpapersevenfullsystemperm-bijective). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinPaperSevenFullSystemEquiv" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenFullSystemEquiv")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin paper seven full system equiv”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/PaperSevenPrimitive.lean:137](../../../../library/modules/robin-papersevenprimitive/#decl-quantumblockencoding-robin-warmrobinpapersevenfullsystemequiv). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinPaperSevenFullSystemEquiv_apply" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenFullSystemEquiv_apply")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin paper seven full system equiv apply”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/PaperSevenPrimitive.lean:142](../../../../library/modules/robin-papersevenprimitive/#decl-quantumblockencoding-robin-warmrobinpapersevenfullsystemequiv-apply). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinPaperSevenFullSystemEquiv_clean" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenFullSystemEquiv_clean")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin paper seven full system equiv clean”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/PaperSevenPrimitive.lean:147](../../../../library/modules/robin-papersevenprimitive/#decl-quantumblockencoding-robin-warmrobinpapersevenfullsystemequiv-clean). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinPaperSevenSelectBasisAction_index" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenSelectBasisAction_index")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin paper seven select basis action index”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/PaperSevenPrimitive.lean:154](../../../../library/modules/robin-papersevenprimitive/#decl-quantumblockencoding-robin-warmrobinpapersevenselectbasisaction-index). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinPaperSevenSelectProgram_eval_reindexed" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenSelectProgram_eval_reindexed")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin paper seven select program eval reindexed”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/PaperSevenPrimitive.lean:173](../../../../library/modules/robin-papersevenprimitive/#decl-quantumblockencoding-robin-warmrobinpapersevenselectprogram-eval-reindexed). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinPaperSevenWorkspaceRotation" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenWorkspaceRotation")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin paper seven workspace rotation”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/PaperSevenPrimitive.lean:196](../../../../library/modules/robin-papersevenprimitive/#decl-quantumblockencoding-robin-warmrobinpapersevenworkspacerotation). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinPaperSevenWorkspaceRotation_unitary" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenWorkspaceRotation_unitary")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin paper seven workspace rotation unitary”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/PaperSevenPrimitive.lean:201](../../../../library/modules/robin-papersevenprimitive/#decl-quantumblockencoding-robin-warmrobinpapersevenworkspacerotation-unitary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinPaperSevenWorkspaceLogicalUnitary" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenWorkspaceLogicalUnitary")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin paper seven workspace logical unitary”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/PaperSevenPrimitive.lean:207](../../../../library/modules/robin-papersevenprimitive/#decl-quantumblockencoding-robin-warmrobinpapersevenworkspacelogicalunitary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinPaperSevenWorkspaceLogicalUnitary_unitary" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenWorkspaceLogicalUnitary_unitary")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin paper seven workspace logical unitary unitary”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/PaperSevenPrimitive.lean:218](../../../../library/modules/robin-papersevenprimitive/#decl-quantumblockencoding-robin-warmrobinpapersevenworkspacelogicalunitary-unitary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinPaperSevenWorkspaceLogicalUnitary_cleanEntry" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenWorkspaceLogicalUnitary_cleanEntry")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin paper seven workspace logical unitary clean entry”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/PaperSevenPrimitive.lean:227](../../../../library/modules/robin-papersevenprimitive/#decl-quantumblockencoding-robin-warmrobinpapersevenworkspacelogicalunitary-cleanentry). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/Robin/PaperSevenT3.lean

19 explicit public declarations, in source order.

:::definition "QuantumBlockEncoding.Robin.warmRobinPaperSevenSelectorPrepareProgram" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenSelectorPrepareProgram")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin paper seven selector prepare program”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/PaperSevenT3.lean:13](../../../../library/modules/robin-papersevent3/#decl-quantumblockencoding-robin-warmrobinpapersevenselectorprepareprogram). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinPaperSevenSelectorPrepareProgram_eval" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenSelectorPrepareProgram_eval")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin paper seven selector prepare program eval”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/PaperSevenT3.lean:18](../../../../library/modules/robin-papersevent3/#decl-quantumblockencoding-robin-warmrobinpapersevenselectorprepareprogram-eval). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinPaperSevenPrimitiveProgram" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenPrimitiveProgram")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin paper seven primitive program”. Chronological exact primitive source program.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Chronological exact primitive source program.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/PaperSevenT3.lean:33](../../../../library/modules/robin-papersevent3/#decl-quantumblockencoding-robin-warmrobinpapersevenprimitiveprogram). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinPaperSevenPrimitive_eval_eq_logical" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenPrimitive_eval_eq_logical")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin paper seven primitive eval eq logical”; the hypotheses and conclusion in the code panel fix its exact scope. Required T3 semantic root.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Required T3 semantic root. Equality includes the exact accumulated global phase and uses the actual reversible extension on dirty 'q7'.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/PaperSevenT3.lean:41](../../../../library/modules/robin-papersevent3/#decl-quantumblockencoding-robin-warmrobinpapersevenprimitive-eval-eq-logical). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinPaperSevenBitsEquiv_encode" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenBitsEquiv_encode")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin paper seven bits equiv encode”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/PaperSevenT3.lean:71](../../../../library/modules/robin-papersevent3/#decl-quantumblockencoding-robin-warmrobinpapersevenbitsequiv-encode). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinPaperSevenPrimitiveFlatUnitary" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenPrimitiveFlatUnitary")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin paper seven primitive flat unitary”. Flat eight-qubit unitary used by the operator-first block-encoding API.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Flat eight-qubit unitary used by the operator-first block-encoding API.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/PaperSevenT3.lean:81](../../../../library/modules/robin-papersevent3/#decl-quantumblockencoding-robin-warmrobinpapersevenprimitiveflatunitary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinPaperSevenPrimitiveFlatUnitary_unitary" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenPrimitiveFlatUnitary_unitary")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin paper seven primitive flat unitary unitary”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/PaperSevenT3.lean:86](../../../../library/modules/robin-papersevent3/#decl-quantumblockencoding-robin-warmrobinpapersevenprimitiveflatunitary-unitary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinPaperSevenPrimitiveCleanIndex" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenPrimitiveCleanIndex")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin paper seven primitive clean index”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/PaperSevenT3.lean:92](../../../../library/modules/robin-papersevent3/#decl-quantumblockencoding-robin-warmrobinpapersevenprimitivecleanindex). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinPaperSevenPrimitive_cleanBlock" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenPrimitive_cleanBlock")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin paper seven primitive clean block”; the hypotheses and conclusion in the code panel fix its exact scope. The physical primitive program has the exact 'M/224 = A/(56/3)' clean block; no numerical matrix comparison is used.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The physical primitive program has the exact 'M/224 = A/(56/3)' clean block; no numerical matrix comparison is used.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/PaperSevenT3.lean:99](../../../../library/modules/robin-papersevent3/#decl-quantumblockencoding-robin-warmrobinpapersevenprimitive-cleanblock). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinPaperSevenPrimitiveBlockContainsTarget" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenPrimitiveBlockContainsTarget")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin paper seven primitive block contains target”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/PaperSevenT3.lean:117](../../../../library/modules/robin-papersevent3/#decl-quantumblockencoding-robin-warmrobinpapersevenprimitiveblockcontainstarget). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinPaperSevenPrimitiveBlockContainsTarget_proof" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenPrimitiveBlockContainsTarget_proof")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin paper seven primitive block contains target proof”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/PaperSevenT3.lean:125](../../../../library/modules/robin-papersevent3/#decl-quantumblockencoding-robin-warmrobinpapersevenprimitiveblockcontainstarget-proof). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinPaperSevenFullSystemEquiv_workspace" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenFullSystemEquiv_workspace")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin paper seven full system equiv workspace”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/PaperSevenT3.lean:193](../../../../library/modules/robin-papersevent3/#decl-quantumblockencoding-robin-warmrobinpapersevenfullsystemequiv-workspace). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinPaperSevenPrimitive_workspaceClean" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenPrimitive_workspaceClean")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin paper seven primitive workspace clean”; the hypotheses and conclusion in the code panel fix its exact scope. Matrix-level workspace restoration: a clean input column has no amplitude on a dirty workspace output row.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Matrix-level workspace restoration: a clean input column has no amplitude on a dirty workspace output row.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/PaperSevenT3.lean:227](../../../../library/modules/robin-papersevent3/#decl-quantumblockencoding-robin-warmrobinpapersevenprimitive-workspaceclean). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinPaperSevenPrimitivePresentation" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenPrimitivePresentation")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin paper seven primitive presentation”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/PaperSevenT3.lean:239](../../../../library/modules/robin-papersevent3/#decl-quantumblockencoding-robin-warmrobinpapersevenprimitivepresentation). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinPaperSevenPrimitiveResource" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenPrimitiveResource")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin paper seven primitive resource”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/PaperSevenT3.lean:247](../../../../library/modules/robin-papersevent3/#decl-quantumblockencoding-robin-warmrobinpapersevenprimitiveresource). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinPaperSevenPrimitive_resource_faithful" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenPrimitive_resource_faithful")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin paper seven primitive resource faithful”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/PaperSevenT3.lean:250](../../../../library/modules/robin-papersevent3/#decl-quantumblockencoding-robin-warmrobinpapersevenprimitive-resource-faithful). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinPaperSevenPrimitiveOperatorCandidate" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenPrimitiveOperatorCandidate")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin paper seven primitive operator candidate”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/PaperSevenT3.lean:254](../../../../library/modules/robin-papersevent3/#decl-quantumblockencoding-robin-warmrobinpapersevenprimitiveoperatorcandidate). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinPaperSevenPrimitiveRefinement" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenPrimitiveRefinement")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin paper seven primitive refinement”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/PaperSevenT3.lean:271](../../../../library/modules/robin-papersevent3/#decl-quantumblockencoding-robin-warmrobinpapersevenprimitiverefinement). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinPaperSevenPrimitiveVerifiedBlockEncoding" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenPrimitiveVerifiedBlockEncoding")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin paper seven primitive verified block encoding”. Exact primitive verified block encoding for the paper-seven source route.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Exact primitive verified block encoding for the paper-seven source route.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/PaperSevenT3.lean:280](../../../../library/modules/robin-papersevent3/#decl-quantumblockencoding-robin-warmrobinpapersevenprimitiveverifiedblockencoding). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/Robin/ResourceComparison.lean

13 explicit public declarations, in source order.

:::definition "QuantumBlockEncoding.Robin.RobinSemanticTier" (lean := "QuantumBlockEncoding.Robin.RobinSemanticTier")
*Plain-English reading.* This type lists the allowed alternatives for “robin semantic tier”; its constructors are the cases that downstream code must handle.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* inductive.

Source: [QuantumBlockEncoding/Robin/ResourceComparison.lean:9](../../../../library/modules/robin-resourcecomparison/#decl-quantumblockencoding-robin-robinsemantictier). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.RobinResourceRow" (lean := "QuantumBlockEncoding.Robin.RobinResourceRow")
*Plain-English reading.* This record groups the data and proof fields needed for “robin resource row”. A proposition-valued field is a requirement until a constructor supplies it.

*Formal status.* Data contract in the default import surface; proposition-valued fields are obligations, not automatically established facts.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* structure.

Source: [QuantumBlockEncoding/Robin/ResourceComparison.lean:16](../../../../library/modules/robin-resourcecomparison/#decl-quantumblockencoding-robin-robinresourcerow). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinPrimitiveConvention" (lean := "QuantumBlockEncoding.Robin.warmRobinPrimitiveConvention")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin primitive convention”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/ResourceComparison.lean:24](../../../../library/modules/robin-resourcecomparison/#decl-quantumblockencoding-robin-warmrobinprimitiveconvention). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinPaperLiteralTranscriptResourceRow" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperLiteralTranscriptResourceRow")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin paper literal transcript resource row”. Historical paper-literal transcript row.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Historical paper-literal transcript row. This is not the certified fixed-N8 standard-RY realization, whose exact primitive cost is recorded in 'T3ResourceComparison.lean'.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/ResourceComparison.lean:30](../../../../library/modules/robin-resourcecomparison/#decl-quantumblockencoding-robin-warmrobinpaperliteraltranscriptresourcerow). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFiveShiftResourceRow" (lean := "QuantumBlockEncoding.Robin.warmRobinFiveShiftResourceRow")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin five shift resource row”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/ResourceComparison.lean:38](../../../../library/modules/robin-resourcecomparison/#decl-quantumblockencoding-robin-warmrobinfiveshiftresourcerow). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinHadamard8ResourceRow" (lean := "QuantumBlockEncoding.Robin.warmRobinHadamard8ResourceRow")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin hadamard 8 resource row”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/ResourceComparison.lean:45](../../../../library/modules/robin-resourcecomparison/#decl-quantumblockencoding-robin-warmrobinhadamard8resourcerow). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.RobinComparison" (lean := "QuantumBlockEncoding.Robin.RobinComparison")
*Plain-English reading.* This type lists the allowed alternatives for “robin comparison”; its constructors are the cases that downstream code must handle.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* inductive.

Source: [QuantumBlockEncoding/Robin/ResourceComparison.lean:52](../../../../library/modules/robin-resourcecomparison/#decl-quantumblockencoding-robin-robincomparison). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.compareRobinRows" (lean := "QuantumBlockEncoding.Robin.compareRobinRows")
*Plain-English reading.* This definition gives the library's named construction or computation for “compare robin rows”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/ResourceComparison.lean:58](../../../../library/modules/robin-resourcecomparison/#decl-quantumblockencoding-robin-comparerobinrows). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFiveShift_paperTranscript_incomparable" (lean := "QuantumBlockEncoding.Robin.warmRobinFiveShift_paperTranscript_incomparable")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin five shift paper transcript incomparable”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/ResourceComparison.lean:71](../../../../library/modules/robin-resourcecomparison/#decl-quantumblockencoding-robin-warmrobinfiveshift-papertranscript-incomparable). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinHadamard8_paperTranscript_incomparable" (lean := "QuantumBlockEncoding.Robin.warmRobinHadamard8_paperTranscript_incomparable")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin hadamard 8 paper transcript incomparable”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/ResourceComparison.lean:78](../../../../library/modules/robin-resourcecomparison/#decl-quantumblockencoding-robin-warmrobinhadamard8-papertranscript-incomparable). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinSourceResourceRow" (lean := "QuantumBlockEncoding.Robin.warmRobinSourceResourceRow")
*Plain-English reading.* This abbreviation gives a shorter name to the type or expression used for “warm robin source resource row”. Historical compatibility alias; this row is generic and paper-literal, not the certified fixed-N8 standard-RY source realization.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Historical compatibility alias; this row is generic and paper-literal, not the certified fixed-N8 standard-RY source realization.

*Declaration kind.* abbrev.

Source: [QuantumBlockEncoding/Robin/ResourceComparison.lean:87](../../../../library/modules/robin-resourcecomparison/#decl-quantumblockencoding-robin-warmrobinsourceresourcerow). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFiveShift_source_incomparable" (lean := "QuantumBlockEncoding.Robin.warmRobinFiveShift_source_incomparable")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin five shift source incomparable”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/ResourceComparison.lean:90](../../../../library/modules/robin-resourcecomparison/#decl-quantumblockencoding-robin-warmrobinfiveshift-source-incomparable). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinHadamard8_source_incomparable" (lean := "QuantumBlockEncoding.Robin.warmRobinHadamard8_source_incomparable")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin hadamard 8 source incomparable”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/ResourceComparison.lean:95](../../../../library/modules/robin-resourcecomparison/#decl-quantumblockencoding-robin-warmrobinhadamard8-source-incomparable). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/Robin/SixSlotOptimal.lean

20 explicit public declarations, in source order.

:::definition "QuantumBlockEncoding.Robin.warmRobinSixSlotPerm" (lean := "QuantumBlockEncoding.Robin.warmRobinSixSlotPerm")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin six slot perm”. Six finite basis permutations, represented column-to-row.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Six finite basis permutations, represented column-to-row.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SixSlotOptimal.lean:17](../../../../library/modules/robin-sixslotoptimal/#decl-quantumblockencoding-robin-warmrobinsixslotperm). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinSixSlotWeight" (lean := "QuantumBlockEncoding.Robin.warmRobinSixSlotWeight")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin six slot weight”. Integer coefficient table for the six-slot certificate.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Integer coefficient table for the six-slot certificate.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SixSlotOptimal.lean:34](../../../../library/modules/robin-sixslotoptimal/#decl-quantumblockencoding-robin-warmrobinsixslotweight). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinSixSlotCap" (lean := "QuantumBlockEncoding.Robin.warmRobinSixSlotCap")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin six slot cap”. Per-slot absolute coefficient caps.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Per-slot absolute coefficient caps.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SixSlotOptimal.lean:49](../../../../library/modules/robin-sixslotoptimal/#decl-quantumblockencoding-robin-warmrobinsixslotcap). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinSixSlotPerm_bijective" (lean := "QuantumBlockEncoding.Robin.warmRobinSixSlotPerm_bijective")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin six slot perm bijective”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SixSlotOptimal.lean:56](../../../../library/modules/robin-sixslotoptimal/#decl-quantumblockencoding-robin-warmrobinsixslotperm-bijective). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinSixSlotDecomposition" (lean := "QuantumBlockEncoding.Robin.warmRobinSixSlotDecomposition")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin six slot decomposition”; the hypotheses and conclusion in the code panel fix its exact scope. Exact reconstruction of the integer target.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Exact reconstruction of the integer target.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SixSlotOptimal.lean:61](../../../../library/modules/robin-sixslotoptimal/#decl-quantumblockencoding-robin-warmrobinsixslotdecomposition). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinSixSlotWeight_natAbs_le_cap" (lean := "QuantumBlockEncoding.Robin.warmRobinSixSlotWeight_natAbs_le_cap")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin six slot weight nat abs le cap”; the hypotheses and conclusion in the code panel fix its exact scope. Every coefficient is bounded by its declared slot cap.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Every coefficient is bounded by its declared slot cap.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SixSlotOptimal.lean:70](../../../../library/modules/robin-sixslotoptimal/#decl-quantumblockencoding-robin-warmrobinsixslotweight-natabs-le-cap). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinSixSlotCap_sum_eq_eighty" (lean := "QuantumBlockEncoding.Robin.warmRobinSixSlotCap_sum_eq_eighty")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin six slot cap sum eq eighty”; the hypotheses and conclusion in the code panel fix its exact scope. The six caps sum to 80.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The six caps sum to 80.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SixSlotOptimal.lean:77](../../../../library/modules/robin-sixslotoptimal/#decl-quantumblockencoding-robin-warmrobinsixslotcap-sum-eq-eighty). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinSixSlotPrepareProbability" (lean := "QuantumBlockEncoding.Robin.warmRobinSixSlotPrepareProbability")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin six slot prepare probability”. Probability assigned to one selector slot by the intrinsic PREPARE.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Probability assigned to one selector slot by the intrinsic PREPARE.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SixSlotOptimal.lean:82](../../../../library/modules/robin-sixslotoptimal/#decl-quantumblockencoding-robin-warmrobinsixslotprepareprobability). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinSixSlotIntrinsicAmplitude" (lean := "QuantumBlockEncoding.Robin.warmRobinSixSlotIntrinsicAmplitude")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin six slot intrinsic amplitude”. Intrinsic clean coefficient 'weight / cap'.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Intrinsic clean coefficient 'weight / cap'.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SixSlotOptimal.lean:86](../../../../library/modules/robin-sixslotoptimal/#decl-quantumblockencoding-robin-warmrobinsixslotintrinsicamplitude). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinSixSlotIntrinsicAmplitude_bounded" (lean := "QuantumBlockEncoding.Robin.warmRobinSixSlotIntrinsicAmplitude_bounded")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin six slot intrinsic amplitude bounded”; the hypotheses and conclusion in the code panel fix its exact scope. All intrinsic amplitude coefficients lie in '\[-1,1\]'.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* All intrinsic amplitude coefficients lie in '\[-1,1\]'.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SixSlotOptimal.lean:92](../../../../library/modules/robin-sixslotoptimal/#decl-quantumblockencoding-robin-warmrobinsixslotintrinsicamplitude-bounded). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinSixSlotFixedAmplitude" (lean := "QuantumBlockEncoding.Robin.warmRobinSixSlotFixedAmplitude")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin six slot fixed amplitude”. Coefficient for the fixed 'M/224' comparison contract.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Coefficient for the fixed 'M/224' comparison contract.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SixSlotOptimal.lean:98](../../../../library/modules/robin-sixslotoptimal/#decl-quantumblockencoding-robin-warmrobinsixslotfixedamplitude). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinSixSlotFixedAmplitude_bounded" (lean := "QuantumBlockEncoding.Robin.warmRobinSixSlotFixedAmplitude_bounded")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin six slot fixed amplitude bounded”; the hypotheses and conclusion in the code panel fix its exact scope. Fixed-normalizer amplitudes are uniformly bounded by '5/14'.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Fixed-normalizer amplitudes are uniformly bounded by '5/14'.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SixSlotOptimal.lean:103](../../../../library/modules/robin-sixslotoptimal/#decl-quantumblockencoding-robin-warmrobinsixslotfixedamplitude-bounded). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinSixSlotIntrinsicCleanFormula" (lean := "QuantumBlockEncoding.Robin.warmRobinSixSlotIntrinsicCleanFormula")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin six slot intrinsic clean formula”. Structural clean formula at the intrinsic normalizer.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Structural clean formula at the intrinsic normalizer.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SixSlotOptimal.lean:109](../../../../library/modules/robin-sixslotoptimal/#decl-quantumblockencoding-robin-warmrobinsixslotintrinsiccleanformula). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinSixSlotIntrinsicCleanFormula_eq_target" (lean := "QuantumBlockEncoding.Robin.warmRobinSixSlotIntrinsicCleanFormula_eq_target")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin six slot intrinsic clean formula eq target”; the hypotheses and conclusion in the code panel fix its exact scope. The intrinsic formula is exactly 'A / (20/3) = M/80'.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The intrinsic formula is exactly 'A / (20/3) = M/80'.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SixSlotOptimal.lean:117](../../../../library/modules/robin-sixslotoptimal/#decl-quantumblockencoding-robin-warmrobinsixslotintrinsiccleanformula-eq-target). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinSixSlotFixedCleanFormula" (lean := "QuantumBlockEncoding.Robin.warmRobinSixSlotFixedCleanFormula")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin six slot fixed clean formula”. Structural clean formula under the established fixed normalizer.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Structural clean formula under the established fixed normalizer.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SixSlotOptimal.lean:124](../../../../library/modules/robin-sixslotoptimal/#decl-quantumblockencoding-robin-warmrobinsixslotfixedcleanformula). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinSixSlotFixedCleanFormula_eq_target" (lean := "QuantumBlockEncoding.Robin.warmRobinSixSlotFixedCleanFormula_eq_target")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin six slot fixed clean formula eq target”; the hypotheses and conclusion in the code panel fix its exact scope. The fixed formula is exactly 'A / (56/3) = M/224'.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The fixed formula is exactly 'A / (56/3) = M/224'.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SixSlotOptimal.lean:132](../../../../library/modules/robin-sixslotoptimal/#decl-quantumblockencoding-robin-warmrobinsixslotfixedcleanformula-eq-target). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinIntegerColumnL1" (lean := "QuantumBlockEncoding.Robin.warmRobinIntegerColumnL1")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin integer column l 1”. Absolute column sum of the integer target.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Absolute column sum of the integer target.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SixSlotOptimal.lean:140](../../../../library/modules/robin-sixslotoptimal/#decl-quantumblockencoding-robin-warmrobinintegercolumnl1). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinIntegerColumnL1_le_eighty" (lean := "QuantumBlockEncoding.Robin.warmRobinIntegerColumnL1_le_eighty")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin integer column l 1 le eighty”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SixSlotOptimal.lean:143](../../../../library/modules/robin-sixslotoptimal/#decl-quantumblockencoding-robin-warmrobinintegercolumnl1-le-eighty). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinIntegerColumnOneL1_eq_eighty" (lean := "QuantumBlockEncoding.Robin.warmRobinIntegerColumnOneL1_eq_eighty")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin integer column one l 1 eq eighty”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SixSlotOptimal.lean:147](../../../../library/modules/robin-sixslotoptimal/#decl-quantumblockencoding-robin-warmrobinintegercolumnonel1-eq-eighty). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinSixSlotCap_sum_eq_maxColumnL1" (lean := "QuantumBlockEncoding.Robin.warmRobinSixSlotCap_sum_eq_maxColumnL1")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin six slot cap sum eq max column l 1”; the hypotheses and conclusion in the code panel fix its exact scope. The cap sum attains the largest absolute column sum of the target.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The cap sum attains the largest absolute column sum of the target.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SixSlotOptimal.lean:152](../../../../library/modules/robin-sixslotoptimal/#decl-quantumblockencoding-robin-warmrobinsixslotcap-sum-eq-maxcolumnl1). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/Robin/SourceBaseline.lean

16 explicit public declarations, in source order.

:::definition "QuantumBlockEncoding.Robin.warmRobinPrintedBoundaryAngle" (lean := "QuantumBlockEncoding.Robin.warmRobinPrintedBoundaryAngle")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin printed boundary angle”. Literal angle printed in the source-side formula.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Literal angle printed in the source-side formula.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SourceBaseline.lean:16](../../../../library/modules/robin-sourcebaseline/#decl-quantumblockencoding-robin-warmrobinprintedboundaryangle). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinStandardRyBoundaryAngle" (lean := "QuantumBlockEncoding.Robin.warmRobinStandardRyBoundaryAngle")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin standard ry boundary angle”. Angle required by the standard 'Ry(theta)' half-angle convention.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Angle required by the standard 'Ry(theta)' half-angle convention.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SourceBaseline.lean:20](../../../../library/modules/robin-sourcebaseline/#decl-quantumblockencoding-robin-warmrobinstandardryboundaryangle). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinStandardRyBoundaryAngle_eq_twice_printed" (lean := "QuantumBlockEncoding.Robin.warmRobinStandardRyBoundaryAngle_eq_twice_printed")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin standard ry boundary angle eq twice printed”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SourceBaseline.lean:23](../../../../library/modules/robin-sourcebaseline/#decl-quantumblockencoding-robin-warmrobinstandardryboundaryangle-eq-twice-printed). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinRegisterSwapPairs" (lean := "QuantumBlockEncoding.Robin.warmRobinRegisterSwapPairs")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin register swap pairs”. The actual logical wire pairs for swapping two three-qubit registers.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The actual logical wire pairs for swapping two three-qubit registers.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SourceBaseline.lean:29](../../../../library/modules/robin-sourcebaseline/#decl-quantumblockencoding-robin-warmrobinregisterswappairs). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinRegisterSwapCircuit" (lean := "QuantumBlockEncoding.Robin.warmRobinRegisterSwapCircuit")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin register swap circuit”. Executable transcript fragment: three real SWAPs, never 'swap 0 0'.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Executable transcript fragment: three real SWAPs, never 'swap 0 0'.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SourceBaseline.lean:33](../../../../library/modules/robin-sourcebaseline/#decl-quantumblockencoding-robin-warmrobinregisterswapcircuit). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinRegisterSwapCircuit_gateList" (lean := "QuantumBlockEncoding.Robin.warmRobinRegisterSwapCircuit_gateList")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin register swap circuit gate list”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SourceBaseline.lean:36](../../../../library/modules/robin-sourcebaseline/#decl-quantumblockencoding-robin-warmrobinregisterswapcircuit-gatelist). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinRegisterSwapCircuit_length" (lean := "QuantumBlockEncoding.Robin.warmRobinRegisterSwapCircuit_length")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin register swap circuit length”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SourceBaseline.lean:41](../../../../library/modules/robin-sourcebaseline/#decl-quantumblockencoding-robin-warmrobinregisterswapcircuit-length). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinRegisterSwapWire" (lean := "QuantumBlockEncoding.Robin.warmRobinRegisterSwapWire")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin register swap wire”. Fixed wire-index action induced by the register swap.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Fixed wire-index action induced by the register swap.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SourceBaseline.lean:46](../../../../library/modules/robin-sourcebaseline/#decl-quantumblockencoding-robin-warmrobinregisterswapwire). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinRegisterSwapWire_involution" (lean := "QuantumBlockEncoding.Robin.warmRobinRegisterSwapWire_involution")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin register swap wire involution”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SourceBaseline.lean:52](../../../../library/modules/robin-sourcebaseline/#decl-quantumblockencoding-robin-warmrobinregisterswapwire-involution). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinRegisterSwapWire_bijective" (lean := "QuantumBlockEncoding.Robin.warmRobinRegisterSwapWire_bijective")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin register swap wire bijective”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SourceBaseline.lean:56](../../../../library/modules/robin-sourcebaseline/#decl-quantumblockencoding-robin-warmrobinregisterswapwire-bijective). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinGenericSourceOpenContracts" (lean := "QuantumBlockEncoding.Robin.warmRobinGenericSourceOpenContracts")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin generic source open contracts”. Obligations outside the fixed benchmark: arbitrary size and literal-source routes.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Obligations outside the fixed benchmark: arbitrary size and literal-source routes.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SourceBaseline.lean:66](../../../../library/modules/robin-sourcebaseline/#decl-quantumblockencoding-robin-warmrobingenericsourceopencontracts). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinGenericSourceOpenContracts_nonempty" (lean := "QuantumBlockEncoding.Robin.warmRobinGenericSourceOpenContracts_nonempty")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin generic source open contracts nonempty”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SourceBaseline.lean:73](../../../../library/modules/robin-sourcebaseline/#decl-quantumblockencoding-robin-warmrobingenericsourceopencontracts-nonempty). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFixedN8SourceOpenContracts" (lean := "QuantumBlockEncoding.Robin.warmRobinFixedN8SourceOpenContracts")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin fixed n 8 source open contracts”. The fixed-N8, f=1, standard-RY-corrected source route is closed.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The fixed-N8, f=1, standard-RY-corrected source route is closed.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SourceBaseline.lean:78](../../../../library/modules/robin-sourcebaseline/#decl-quantumblockencoding-robin-warmrobinfixedn8sourceopencontracts). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFixedN8SourceOpenContracts_eq_nil" (lean := "QuantumBlockEncoding.Robin.warmRobinFixedN8SourceOpenContracts_eq_nil")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin fixed n 8 source open contracts eq nil”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SourceBaseline.lean:80](../../../../library/modules/robin-sourcebaseline/#decl-quantumblockencoding-robin-warmrobinfixedn8sourceopencontracts-eq-nil). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinSourceOpenContracts" (lean := "QuantumBlockEncoding.Robin.warmRobinSourceOpenContracts")
*Plain-English reading.* This abbreviation gives a shorter name to the type or expression used for “warm robin source open contracts”. Historical compatibility alias for the generic, arbitrary-n and paper-literal obligations.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Historical compatibility alias for the generic, arbitrary-n and paper-literal obligations. It does not describe the certified fixed-N8 route.

*Declaration kind.* abbrev.

Source: [QuantumBlockEncoding/Robin/SourceBaseline.lean:85](../../../../library/modules/robin-sourcebaseline/#decl-quantumblockencoding-robin-warmrobinsourceopencontracts). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinSourceOpenContracts_nonempty" (lean := "QuantumBlockEncoding.Robin.warmRobinSourceOpenContracts_nonempty")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin source open contracts nonempty”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SourceBaseline.lean:88](../../../../library/modules/robin-sourcebaseline/#decl-quantumblockencoding-robin-warmrobinsourceopencontracts-nonempty). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/Robin/SourceSevenSparseData.lean

18 explicit public declarations, in source order.

:::definition "QuantumBlockEncoding.Robin.warmRobinSourceDTOffset" (lean := "QuantumBlockEncoding.Robin.warmRobinSourceDTOffset")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin source dt offset”. The source 'D^T' offset ordering, expressed in the physical selector.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source 'D^T' offset ordering, expressed in the physical selector.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SourceSevenSparseData.lean:16](../../../../library/modules/robin-sourcesevensparsedata/#decl-quantumblockencoding-robin-warmrobinsourcedtoffset). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinSourceDTOffset_table" (lean := "QuantumBlockEncoding.Robin.warmRobinSourceDTOffset_table")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin source dt offset table”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SourceSevenSparseData.lean:19](../../../../library/modules/robin-sourcesevensparsedata/#decl-quantumblockencoding-robin-warmrobinsourcedtoffset-table). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinSourceDTRow" (lean := "QuantumBlockEncoding.Robin.warmRobinSourceDTRow")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin source dt row”. Sparse row addressed by one physical slot at a fixed source column.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Sparse row addressed by one physical slot at a fixed source column.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SourceSevenSparseData.lean:24](../../../../library/modules/robin-sourcesevensparsedata/#decl-quantumblockencoding-robin-warmrobinsourcedtrow). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinSourceSevenWeight" (lean := "QuantumBlockEncoding.Robin.warmRobinSourceSevenWeight")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin source seven weight”. Exact integer value returned by the source sparse-value oracle.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Exact integer value returned by the source sparse-value oracle.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SourceSevenSparseData.lean:29](../../../../library/modules/robin-sourcesevensparsedata/#decl-quantumblockencoding-robin-warmrobinsourcesevenweight). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinSourceSevenWeight_slot7_zero" (lean := "QuantumBlockEncoding.Robin.warmRobinSourceSevenWeight_slot7_zero")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin source seven weight slot 7 zero”; the hypotheses and conclusion in the code panel fix its exact scope. Slot seven is the zero diagonal used only for physical padding.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Slot seven is the zero diagonal used only for physical padding.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SourceSevenSparseData.lean:33](../../../../library/modules/robin-sourcesevensparsedata/#decl-quantumblockencoding-robin-warmrobinsourcesevenweight-slot7-zero). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinSourceDTRow_bijective_in_slot" (lean := "QuantumBlockEncoding.Robin.warmRobinSourceDTRow_bijective_in_slot")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin source dt row bijective in slot”; the hypotheses and conclusion in the code panel fix its exact scope. At a fixed column, the eight physical slots enumerate eight distinct rows.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* At a fixed column, the eight physical slots enumerate eight distinct rows.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SourceSevenSparseData.lean:38](../../../../library/modules/robin-sourcesevensparsedata/#decl-quantumblockencoding-robin-warmrobinsourcedtrow-bijective-in-slot). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinSourceDTRow_bijective_in_column" (lean := "QuantumBlockEncoding.Robin.warmRobinSourceDTRow_bijective_in_column")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin source dt row bijective in column”; the hypotheses and conclusion in the code panel fix its exact scope. At a fixed slot, cyclic sparse access is a permutation of the columns.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* At a fixed slot, cyclic sparse access is a permutation of the columns.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SourceSevenSparseData.lean:43](../../../../library/modules/robin-sourcesevensparsedata/#decl-quantumblockencoding-robin-warmrobinsourcedtrow-bijective-in-column). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinSourceSevenSparseDecomposition" (lean := "QuantumBlockEncoding.Robin.warmRobinSourceSevenSparseDecomposition")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin source seven sparse decomposition”; the hypotheses and conclusion in the code panel fix its exact scope. Exact sparse-access decomposition with seven active physical states.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Exact sparse-access decomposition with seven active physical states.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SourceSevenSparseData.lean:48](../../../../library/modules/robin-sourcesevensparsedata/#decl-quantumblockencoding-robin-warmrobinsourcesevensparsedecomposition). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinSourceND" (lean := "QuantumBlockEncoding.Robin.warmRobinSourceND")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin source nd”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SourceSevenSparseData.lean:56](../../../../library/modules/robin-sourcesevensparsedata/#decl-quantumblockencoding-robin-warmrobinsourcend). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinSourceNf" (lean := "QuantumBlockEncoding.Robin.warmRobinSourceNf")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin source nf”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SourceSevenSparseData.lean:57](../../../../library/modules/robin-sourcesevensparsedata/#decl-quantumblockencoding-robin-warmrobinsourcenf). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinSourceKappa" (lean := "QuantumBlockEncoding.Robin.warmRobinSourceKappa")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin source kappa”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SourceSevenSparseData.lean:58](../../../../library/modules/robin-sourcesevensparsedata/#decl-quantumblockencoding-robin-warmrobinsourcekappa). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinSourceAmplitude_eq_integer_div_32" (lean := "QuantumBlockEncoding.Robin.warmRobinSourceAmplitude_eq_integer_div_32")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin source amplitude eq integer div 32”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SourceSevenSparseData.lean:60](../../../../library/modules/robin-sourcesevensparsedata/#decl-quantumblockencoding-robin-warmrobinsourceamplitude-eq-integer-div-32). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinSourceAlpha_eq" (lean := "QuantumBlockEncoding.Robin.warmRobinSourceAlpha_eq")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin source alpha eq”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SourceSevenSparseData.lean:67](../../../../library/modules/robin-sourcesevensparsedata/#decl-quantumblockencoding-robin-warmrobinsourcealpha-eq). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinSourceSevenSelectorProbability" (lean := "QuantumBlockEncoding.Robin.warmRobinSourceSevenSelectorProbability")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin source seven selector probability”. Selector probability in the clean column of padded-seven PREPARE.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Selector probability in the clean column of padded-seven PREPARE.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SourceSevenSparseData.lean:72](../../../../library/modules/robin-sourcesevensparsedata/#decl-quantumblockencoding-robin-warmrobinsourcesevenselectorprobability). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinSourceSevenPaddedCoefficient" (lean := "QuantumBlockEncoding.Robin.warmRobinSourceSevenPaddedCoefficient")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin source seven padded coefficient”. Coefficient loaded by an active sparse slot.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Coefficient loaded by an active sparse slot. The padded slot is assigned the identity coefficient because its clean PREPARE probability is zero.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SourceSevenSparseData.lean:77](../../../../library/modules/robin-sourcesevensparsedata/#decl-quantumblockencoding-robin-warmrobinsourcesevenpaddedcoefficient). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinSourceSevenCleanFormula" (lean := "QuantumBlockEncoding.Robin.warmRobinSourceSevenCleanFormula")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin source seven clean formula”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SourceSevenSparseData.lean:80](../../../../library/modules/robin-sourcesevensparsedata/#decl-quantumblockencoding-robin-warmrobinsourcesevencleanformula). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinSourceSevenCleanFormula_eq_integer_div_224" (lean := "QuantumBlockEncoding.Robin.warmRobinSourceSevenCleanFormula_eq_integer_div_224")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin source seven clean formula eq integer div 224”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SourceSevenSparseData.lean:87](../../../../library/modules/robin-sourcesevensparsedata/#decl-quantumblockencoding-robin-warmrobinsourcesevencleanformula-eq-integer-div-224). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinSourceSevenCleanFormula_eq_target" (lean := "QuantumBlockEncoding.Robin.warmRobinSourceSevenCleanFormula_eq_target")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin source seven clean formula eq target”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SourceSevenSparseData.lean:93](../../../../library/modules/robin-sourcesevensparsedata/#decl-quantumblockencoding-robin-warmrobinsourcesevencleanformula-eq-target). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/Robin/SymmetryFourSlot.lean

21 explicit public declarations, in source order.

:::definition "QuantumBlockEncoding.Robin.warmRobinReverse8" (lean := "QuantumBlockEncoding.Robin.warmRobinReverse8")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin reverse 8”. Reverse an eight-dimensional basis index.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Reverse an eight-dimensional basis index.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlot.lean:17](../../../../library/modules/robin-symmetryfourslot/#decl-quantumblockencoding-robin-warmrobinreverse8). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinReverse8_value" (lean := "QuantumBlockEncoding.Robin.warmRobinReverse8_value")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin reverse 8 value”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlot.lean:20](../../../../library/modules/robin-symmetryfourslot/#decl-quantumblockencoding-robin-warmrobinreverse8-value). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinReverse8_involution" (lean := "QuantumBlockEncoding.Robin.warmRobinReverse8_involution")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin reverse 8 involution”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlot.lean:24](../../../../library/modules/robin-symmetryfourslot/#decl-quantumblockencoding-robin-warmrobinreverse8-involution). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinIntegerTarget_centrosymmetric" (lean := "QuantumBlockEncoding.Robin.warmRobinIntegerTarget_centrosymmetric")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin integer target centrosymmetric”; the hypotheses and conclusion in the code panel fix its exact scope. The fixed integer Robin matrix is invariant under simultaneous reversal.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The fixed integer Robin matrix is invariant under simultaneous reversal.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlot.lean:29](../../../../library/modules/robin-symmetryfourslot/#decl-quantumblockencoding-robin-warmrobinintegertarget-centrosymmetric). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinPairLow" (lean := "QuantumBlockEncoding.Robin.warmRobinPairLow")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin pair low”. Embed the low representative of a reversal pair.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Embed the low representative of a reversal pair.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlot.lean:36](../../../../library/modules/robin-symmetryfourslot/#decl-quantumblockencoding-robin-warmrobinpairlow). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinPairHigh" (lean := "QuantumBlockEncoding.Robin.warmRobinPairHigh")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin pair high”. Embed the high representative paired with 'index'.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Embed the high representative paired with 'index'.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlot.lean:40](../../../../library/modules/robin-symmetryfourslot/#decl-quantumblockencoding-robin-warmrobinpairhigh). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinSymmetryPlusBlock" (lean := "QuantumBlockEncoding.Robin.warmRobinSymmetryPlusBlock")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin symmetry plus block”. Integer matrix in the symmetric reversal sector.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Integer matrix in the symmetric reversal sector.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlot.lean:44](../../../../library/modules/robin-symmetryfourslot/#decl-quantumblockencoding-robin-warmrobinsymmetryplusblock). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinSymmetryMinusBlock" (lean := "QuantumBlockEncoding.Robin.warmRobinSymmetryMinusBlock")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin symmetry minus block”. Integer matrix in the antisymmetric reversal sector.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Integer matrix in the antisymmetric reversal sector.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlot.lean:49](../../../../library/modules/robin-symmetryfourslot/#decl-quantumblockencoding-robin-warmrobinsymmetryminusblock). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinSymmetryFourShiftPerm" (lean := "QuantumBlockEncoding.Robin.warmRobinSymmetryFourShiftPerm")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin symmetry four shift perm”. The four cyclic permutations used in both symmetry sectors.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The four cyclic permutations used in both symmetry sectors.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlot.lean:54](../../../../library/modules/robin-symmetryfourslot/#decl-quantumblockencoding-robin-warmrobinsymmetryfourshiftperm). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinSymmetryFourShiftPerm_bijective" (lean := "QuantumBlockEncoding.Robin.warmRobinSymmetryFourShiftPerm_bijective")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin symmetry four shift perm bijective”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlot.lean:57](../../../../library/modules/robin-symmetryfourslot/#decl-quantumblockencoding-robin-warmrobinsymmetryfourshiftperm-bijective). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinSymmetryPlusWeight" (lean := "QuantumBlockEncoding.Robin.warmRobinSymmetryPlusWeight")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin symmetry plus weight”. Integer weights for the symmetric sector.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Integer weights for the symmetric sector.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlot.lean:62](../../../../library/modules/robin-symmetryfourslot/#decl-quantumblockencoding-robin-warmrobinsymmetryplusweight). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinSymmetryMinusWeight" (lean := "QuantumBlockEncoding.Robin.warmRobinSymmetryMinusWeight")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin symmetry minus weight”. Integer weights for the antisymmetric sector.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Integer weights for the antisymmetric sector.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlot.lean:70](../../../../library/modules/robin-symmetryfourslot/#decl-quantumblockencoding-robin-warmrobinsymmetryminusweight). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinSymmetryPlusFourShiftDecomposition" (lean := "QuantumBlockEncoding.Robin.warmRobinSymmetryPlusFourShiftDecomposition")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin symmetry plus four shift decomposition”; the hypotheses and conclusion in the code panel fix its exact scope. Exact four-shift decomposition of the symmetric sector.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Exact four-shift decomposition of the symmetric sector.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlot.lean:78](../../../../library/modules/robin-symmetryfourslot/#decl-quantumblockencoding-robin-warmrobinsymmetryplusfourshiftdecomposition). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinSymmetryMinusFourShiftDecomposition" (lean := "QuantumBlockEncoding.Robin.warmRobinSymmetryMinusFourShiftDecomposition")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin symmetry minus four shift decomposition”; the hypotheses and conclusion in the code panel fix its exact scope. Exact four-shift decomposition of the antisymmetric sector.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Exact four-shift decomposition of the antisymmetric sector.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlot.lean:87](../../../../library/modules/robin-symmetryfourslot/#decl-quantumblockencoding-robin-warmrobinsymmetryminusfourshiftdecomposition). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinSymmetryFourShiftWeight" (lean := "QuantumBlockEncoding.Robin.warmRobinSymmetryFourShiftWeight")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin symmetry four shift weight”. Select the weight table by symmetry sector ('0' symmetric, '1' antisymmetric).

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Select the weight table by symmetry sector ('0' symmetric, '1' antisymmetric).

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlot.lean:96](../../../../library/modules/robin-symmetryfourslot/#decl-quantumblockencoding-robin-warmrobinsymmetryfourshiftweight). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinSymmetryFourShiftAmplitude" (lean := "QuantumBlockEncoding.Robin.warmRobinSymmetryFourShiftAmplitude")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin symmetry four shift amplitude”. Clean coefficient used by the four-slot amplitude loader.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Clean coefficient used by the four-slot amplitude loader.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlot.lean:104](../../../../library/modules/robin-symmetryfourslot/#decl-quantumblockencoding-robin-warmrobinsymmetryfourshiftamplitude). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinSymmetryFourShiftAmplitude_bounded" (lean := "QuantumBlockEncoding.Robin.warmRobinSymmetryFourShiftAmplitude_bounded")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin symmetry four shift amplitude bounded”; the hypotheses and conclusion in the code panel fix its exact scope. Every four-slot amplitude lies in the unit interval.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Every four-slot amplitude lies in the unit interval.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlot.lean:109](../../../../library/modules/robin-symmetryfourslot/#decl-quantumblockencoding-robin-warmrobinsymmetryfourshiftamplitude-bounded). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinSymmetryFourShiftCleanFormula" (lean := "QuantumBlockEncoding.Robin.warmRobinSymmetryFourShiftCleanFormula")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin symmetry four shift clean formula”. Structural clean formula in one symmetry sector.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Structural clean formula in one symmetry sector.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlot.lean:116](../../../../library/modules/robin-symmetryfourslot/#decl-quantumblockencoding-robin-warmrobinsymmetryfourshiftcleanformula). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinSymmetryPlusFourShiftCleanFormula_eq" (lean := "QuantumBlockEncoding.Robin.warmRobinSymmetryPlusFourShiftCleanFormula_eq")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin symmetry plus four shift clean formula eq”; the hypotheses and conclusion in the code panel fix its exact scope. The symmetric-sector clean formula is exactly 'M₊ / 224'.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The symmetric-sector clean formula is exactly 'M₊ / 224'.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlot.lean:124](../../../../library/modules/robin-symmetryfourslot/#decl-quantumblockencoding-robin-warmrobinsymmetryplusfourshiftcleanformula-eq). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinSymmetryMinusFourShiftCleanFormula_eq" (lean := "QuantumBlockEncoding.Robin.warmRobinSymmetryMinusFourShiftCleanFormula_eq")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin symmetry minus four shift clean formula eq”; the hypotheses and conclusion in the code panel fix its exact scope. The antisymmetric-sector clean formula is exactly 'M₋ / 224'.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The antisymmetric-sector clean formula is exactly 'M₋ / 224'.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlot.lean:131](../../../../library/modules/robin-symmetryfourslot/#decl-quantumblockencoding-robin-warmrobinsymmetryminusfourshiftcleanformula-eq). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinSymmetryBlocks_columnOne_fullSupport" (lean := "QuantumBlockEncoding.Robin.warmRobinSymmetryBlocks_columnOne_fullSupport")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin symmetry blocks column one full support”; the hypotheses and conclusion in the code panel fix its exact scope. Both symmetry blocks have four nonzero entries in column one.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Both symmetry blocks have four nonzero entries in column one.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlot.lean:138](../../../../library/modules/robin-symmetryfourslot/#decl-quantumblockencoding-robin-warmrobinsymmetryblocks-columnone-fullsupport). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/Robin/SymmetryFourSlotBlockEncoding.lean

50 explicit public declarations, in source order.

:::definition "QuantumBlockEncoding.Robin.warmRobinPairSystemToOriginal" (lean := "QuantumBlockEncoding.Robin.warmRobinPairSystemToOriginal")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin pair system to original”. Map a reversal-pair coordinate back to the original 'Fin 8' basis.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Map a reversal-pair coordinate back to the original 'Fin 8' basis.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotBlockEncoding.lean:24](../../../../library/modules/robin-symmetryfourslotblockencoding/#decl-quantumblockencoding-robin-warmrobinpairsystemtooriginal). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinPairSystemToOriginal_zero" (lean := "QuantumBlockEncoding.Robin.warmRobinPairSystemToOriginal_zero")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin pair system to original zero”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotBlockEncoding.lean:31](../../../../library/modules/robin-symmetryfourslotblockencoding/#decl-quantumblockencoding-robin-warmrobinpairsystemtooriginal-zero). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinPairSystemToOriginal_one" (lean := "QuantumBlockEncoding.Robin.warmRobinPairSystemToOriginal_one")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin pair system to original one”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotBlockEncoding.lean:35](../../../../library/modules/robin-symmetryfourslotblockencoding/#decl-quantumblockencoding-robin-warmrobinpairsystemtooriginal-one). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinPairSystemToOriginal_bijective" (lean := "QuantumBlockEncoding.Robin.warmRobinPairSystemToOriginal_bijective")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin pair system to original bijective”; the hypotheses and conclusion in the code panel fix its exact scope. Pair coordinates enumerate the original eight basis states exactly once.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Pair coordinates enumerate the original eight basis states exactly once.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotBlockEncoding.lean:40](../../../../library/modules/robin-symmetryfourslotblockencoding/#decl-quantumblockencoding-robin-warmrobinpairsystemtooriginal-bijective). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinPairSystemEquiv" (lean := "QuantumBlockEncoding.Robin.warmRobinPairSystemEquiv")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin pair system equiv”. Equivalence between pair coordinates and the original Robin basis.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Equivalence between pair coordinates and the original Robin basis.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotBlockEncoding.lean:45](../../../../library/modules/robin-symmetryfourslotblockencoding/#decl-quantumblockencoding-robin-warmrobinpairsystemequiv). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinPairSystemEquiv_apply" (lean := "QuantumBlockEncoding.Robin.warmRobinPairSystemEquiv_apply")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin pair system equiv apply”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotBlockEncoding.lean:50](../../../../library/modules/robin-symmetryfourslotblockencoding/#decl-quantumblockencoding-robin-warmrobinpairsystemequiv-apply). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinPairSystemToOriginal_symm" (lean := "QuantumBlockEncoding.Robin.warmRobinPairSystemToOriginal_symm")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin pair system to original symm”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotBlockEncoding.lean:55](../../../../library/modules/robin-symmetryfourslotblockencoding/#decl-quantumblockencoding-robin-warmrobinpairsystemtooriginal-symm). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinSymmetryBasisChange" (lean := "QuantumBlockEncoding.Robin.warmRobinSymmetryBasisChange")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin symmetry basis change”. The symmetry-sector-to-pair basis change: one exact Hadamard-like rotation.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The symmetry-sector-to-pair basis change: one exact Hadamard-like rotation.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotBlockEncoding.lean:61](../../../../library/modules/robin-symmetryfourslotblockencoding/#decl-quantumblockencoding-robin-warmrobinsymmetrybasischange). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinSymmetryBasisChange_unitary" (lean := "QuantumBlockEncoding.Robin.warmRobinSymmetryBasisChange_unitary")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin symmetry basis change unitary”; the hypotheses and conclusion in the code panel fix its exact scope. The symmetry basis change is unitary.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The symmetry basis change is unitary.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotBlockEncoding.lean:67](../../../../library/modules/robin-symmetryfourslotblockencoding/#decl-quantumblockencoding-robin-warmrobinsymmetrybasischange-unitary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.star_warmRobinUniformBitPrepare_apply" (lean := "QuantumBlockEncoding.Robin.star_warmRobinUniformBitPrepare_apply")
*Plain-English reading.* Lean checks the proposition indexed as “star warm robin uniform bit prepare apply”; the hypotheses and conclusion in the code panel fix its exact scope. Every entry of the real two-dimensional basis change is self-conjugate.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Every entry of the real two-dimensional basis change is self-conjugate.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotBlockEncoding.lean:75](../../../../library/modules/robin-symmetryfourslotblockencoding/#decl-quantumblockencoding-robin-star-warmrobinuniformbitprepare-apply). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinUniformScalar_square_complex" (lean := "QuantumBlockEncoding.Robin.warmRobinUniformScalar_square_complex")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin uniform scalar square complex”; the hypotheses and conclusion in the code panel fix its exact scope. The common real selector amplitude has squared magnitude '1/2'.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The common real selector amplitude has squared magnitude '1/2'.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotBlockEncoding.lean:88](../../../../library/modules/robin-symmetryfourslotblockencoding/#decl-quantumblockencoding-robin-warmrobinuniformscalar-square-complex). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinSymmetryBasisChange_apply" (lean := "QuantumBlockEncoding.Robin.warmRobinSymmetryBasisChange_apply")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin symmetry basis change apply”; the hypotheses and conclusion in the code panel fix its exact scope. Entry formula for the exact symmetry basis change.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Entry formula for the exact symmetry basis change.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotBlockEncoding.lean:101](../../../../library/modules/robin-symmetryfourslotblockencoding/#decl-quantumblockencoding-robin-warmrobinsymmetrybasischange-apply). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.star_warmRobinSymmetryBasisChange_apply" (lean := "QuantumBlockEncoding.Robin.star_warmRobinSymmetryBasisChange_apply")
*Plain-English reading.* Lean checks the proposition indexed as “star warm robin symmetry basis change apply”; the hypotheses and conclusion in the code panel fix its exact scope. Entry formula for the adjoint symmetry basis change.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Entry formula for the adjoint symmetry basis change.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotBlockEncoding.lean:111](../../../../library/modules/robin-symmetryfourslotblockencoding/#decl-quantumblockencoding-robin-star-warmrobinsymmetrybasischange-apply). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinSymmetryBasisChange_mul_pairRow" (lean := "QuantumBlockEncoding.Robin.warmRobinSymmetryBasisChange_mul_pairRow")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin symmetry basis change mul pair row”; the hypotheses and conclusion in the code panel fix its exact scope. Left multiplication preserves the pair index and sums only over sectors.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Left multiplication preserves the pair index and sums only over sectors.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotBlockEncoding.lean:123](../../../../library/modules/robin-symmetryfourslotblockencoding/#decl-quantumblockencoding-robin-warmrobinsymmetrybasischange-mul-pairrow). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.mul_star_warmRobinSymmetryBasisChange_pairColumn" (lean := "QuantumBlockEncoding.Robin.mul_star_warmRobinSymmetryBasisChange_pairColumn")
*Plain-English reading.* Lean checks the proposition indexed as “mul star warm robin symmetry basis change pair column”; the hypotheses and conclusion in the code panel fix its exact scope. Right multiplication by the adjoint also preserves the pair index.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Right multiplication by the adjoint also preserves the pair index.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotBlockEncoding.lean:145](../../../../library/modules/robin-symmetryfourslotblockencoding/#decl-quantumblockencoding-robin-mul-star-warmrobinsymmetrybasischange-paircolumn). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinIntegerTarget_pair_high_low" (lean := "QuantumBlockEncoding.Robin.warmRobinIntegerTarget_pair_high_low")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin integer target pair high low”; the hypotheses and conclusion in the code panel fix its exact scope. Lower-left pair block equals the upper-right pair block by centrosymmetry.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Lower-left pair block equals the upper-right pair block by centrosymmetry.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotBlockEncoding.lean:167](../../../../library/modules/robin-symmetryfourslotblockencoding/#decl-quantumblockencoding-robin-warmrobinintegertarget-pair-high-low). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinIntegerTarget_pair_high_high" (lean := "QuantumBlockEncoding.Robin.warmRobinIntegerTarget_pair_high_high")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin integer target pair high high”; the hypotheses and conclusion in the code panel fix its exact scope. The high-high pair block equals the low-low block.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The high-high pair block equals the low-low block.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotBlockEncoding.lean:176](../../../../library/modules/robin-symmetryfourslotblockencoding/#decl-quantumblockencoding-robin-warmrobinintegertarget-pair-high-high). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinPairNormalizedTargetRat" (lean := "QuantumBlockEncoding.Robin.warmRobinPairNormalizedTargetRat")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin pair normalized target rat”. The fixed integer target, reordered by reversal pairs and divided by '224'.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The fixed integer target, reordered by reversal pairs and divided by '224'.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotBlockEncoding.lean:185](../../../../library/modules/robin-symmetryfourslotblockencoding/#decl-quantumblockencoding-robin-warmrobinpairnormalizedtargetrat). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinPairNormalizedTargetRat_zero_zero" (lean := "QuantumBlockEncoding.Robin.warmRobinPairNormalizedTargetRat_zero_zero")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin pair normalized target rat zero zero”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotBlockEncoding.lean:192](../../../../library/modules/robin-symmetryfourslotblockencoding/#decl-quantumblockencoding-robin-warmrobinpairnormalizedtargetrat-zero-zero). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinPairNormalizedTargetRat_zero_one" (lean := "QuantumBlockEncoding.Robin.warmRobinPairNormalizedTargetRat_zero_one")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin pair normalized target rat zero one”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotBlockEncoding.lean:199](../../../../library/modules/robin-symmetryfourslotblockencoding/#decl-quantumblockencoding-robin-warmrobinpairnormalizedtargetrat-zero-one). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinPairNormalizedTargetRat_one_zero" (lean := "QuantumBlockEncoding.Robin.warmRobinPairNormalizedTargetRat_one_zero")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin pair normalized target rat one zero”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotBlockEncoding.lean:206](../../../../library/modules/robin-symmetryfourslotblockencoding/#decl-quantumblockencoding-robin-warmrobinpairnormalizedtargetrat-one-zero). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinPairNormalizedTargetRat_one_one" (lean := "QuantumBlockEncoding.Robin.warmRobinPairNormalizedTargetRat_one_one")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin pair normalized target rat one one”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotBlockEncoding.lean:216](../../../../library/modules/robin-symmetryfourslotblockencoding/#decl-quantumblockencoding-robin-warmrobinpairnormalizedtargetrat-one-one). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinPairNormalizedTargetComplex" (lean := "QuantumBlockEncoding.Robin.warmRobinPairNormalizedTargetComplex")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin pair normalized target complex”. Complex view of the pair-ordered normalized Robin target.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Complex view of the pair-ordered normalized Robin target.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotBlockEncoding.lean:226](../../../../library/modules/robin-symmetryfourslotblockencoding/#decl-quantumblockencoding-robin-warmrobinpairnormalizedtargetcomplex). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFourSlotSectorTargetComplex" (lean := "QuantumBlockEncoding.Robin.warmRobinFourSlotSectorTargetComplex")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin four slot sector target complex”. Complex view of the direct-sum sector target.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Complex view of the direct-sum sector target.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotBlockEncoding.lean:231](../../../../library/modules/robin-symmetryfourslotblockencoding/#decl-quantumblockencoding-robin-warmrobinfourslotsectortargetcomplex). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinSymmetryBasisChange_conjugates_target" (lean := "QuantumBlockEncoding.Robin.warmRobinSymmetryBasisChange_conjugates_target")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin symmetry basis change conjugates target”; the hypotheses and conclusion in the code panel fix its exact scope. The exact symmetry transform reconstructs the pair-ordered Robin matrix.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The exact symmetry transform reconstructs the pair-ordered Robin matrix.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotBlockEncoding.lean:276](../../../../library/modules/robin-symmetryfourslotblockencoding/#decl-quantumblockencoding-robin-warmrobinsymmetrybasischange-conjugates-target). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFourSlotPairLogicalUnitary" (lean := "QuantumBlockEncoding.Robin.warmRobinFourSlotPairLogicalUnitary")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin four slot pair logical unitary”. Conjugate the sector logical unitary back to reversal-pair coordinates.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Conjugate the sector logical unitary back to reversal-pair coordinates.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotBlockEncoding.lean:322](../../../../library/modules/robin-symmetryfourslotblockencoding/#decl-quantumblockencoding-robin-warmrobinfourslotpairlogicalunitary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFourSlotPairLogicalUnitary_unitary" (lean := "QuantumBlockEncoding.Robin.warmRobinFourSlotPairLogicalUnitary_unitary")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin four slot pair logical unitary unitary”; the hypotheses and conclusion in the code panel fix its exact scope. The pair-basis logical unitary remains exactly unitary.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The pair-basis logical unitary remains exactly unitary.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotBlockEncoding.lean:330](../../../../library/modules/robin-symmetryfourslotblockencoding/#decl-quantumblockencoding-robin-warmrobinfourslotpairlogicalunitary-unitary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFourSlotPairLogicalUnitary_cleanSystemBlock" (lean := "QuantumBlockEncoding.Robin.warmRobinFourSlotPairLogicalUnitary_cleanSystemBlock")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin four slot pair logical unitary clean system block”; the hypotheses and conclusion in the code panel fix its exact scope. Its clean system block is the pair-ordered normalized Robin target.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Its clean system block is the pair-ordered normalized Robin target.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotBlockEncoding.lean:339](../../../../library/modules/robin-symmetryfourslotblockencoding/#decl-quantumblockencoding-robin-warmrobinfourslotpairlogicalunitary-cleansystemblock). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFourSlotPairLogicalUnitary_cleanEntry" (lean := "QuantumBlockEncoding.Robin.warmRobinFourSlotPairLogicalUnitary_cleanEntry")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin four slot pair logical unitary clean entry”; the hypotheses and conclusion in the code panel fix its exact scope. Entry form of the pair-basis clean-block certificate.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Entry form of the pair-basis clean-block certificate.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotBlockEncoding.lean:348](../../../../library/modules/robin-symmetryfourslotblockencoding/#decl-quantumblockencoding-robin-warmrobinfourslotpairlogicalunitary-cleanentry). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFourSlotProductSystemEquiv" (lean := "QuantumBlockEncoding.Robin.warmRobinFourSlotProductSystemEquiv")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin four slot product system equiv”. Reindex only the system component from pair order to original 'Fin 8'.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Reindex only the system component from pair order to original 'Fin 8'.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotBlockEncoding.lean:358](../../../../library/modules/robin-symmetryfourslotblockencoding/#decl-quantumblockencoding-robin-warmrobinfourslotproductsystemequiv). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFourSlotOriginalIndexEquiv" (lean := "QuantumBlockEncoding.Robin.warmRobinFourSlotOriginalIndexEquiv")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin four slot original index equiv”. Flatten coefficient × selector × original system into six qubits.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Flatten coefficient × selector × original system into six qubits.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotBlockEncoding.lean:365](../../../../library/modules/robin-symmetryfourslotblockencoding/#decl-quantumblockencoding-robin-warmrobinfourslotoriginalindexequiv). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFourSlotProductBitsEquiv" (lean := "QuantumBlockEncoding.Robin.warmRobinFourSlotProductBitsEquiv")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin four slot product bits equiv”. The six primitive wires interpreted as coefficient, selector, and system registers through the already-certified T2 flattening.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The six primitive wires interpreted as coefficient, selector, and system registers through the already-certified T2 flattening. Consequently q0--q2 are system, q3--q4 are selector, and q5 is coefficient.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotBlockEncoding.lean:373](../../../../library/modules/robin-symmetryfourslotblockencoding/#decl-quantumblockencoding-robin-warmrobinfourslotproductbitsequiv). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFourSlotProductBitsEquiv_index" (lean := "QuantumBlockEncoding.Robin.warmRobinFourSlotProductBitsEquiv_index")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin four slot product bits equiv index”; the hypotheses and conclusion in the code panel fix its exact scope. Primitive little-endian indexing is exactly the original T2 index map.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Primitive little-endian indexing is exactly the original T2 index map.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotBlockEncoding.lean:378](../../../../library/modules/robin-symmetryfourslotblockencoding/#decl-quantumblockencoding-robin-warmrobinfourslotproductbitsequiv-index). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFourSlotProductBitsEquiv_all_basis" (lean := "QuantumBlockEncoding.Robin.warmRobinFourSlotProductBitsEquiv_all_basis")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin four slot product bits equiv all basis”; the hypotheses and conclusion in the code panel fix its exact scope. Regression over all 64 basis states, stated as the concrete register formula consumed by executable backends.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Regression over all 64 basis states, stated as the concrete register formula consumed by executable backends.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotBlockEncoding.lean:386](../../../../library/modules/robin-symmetryfourslotblockencoding/#decl-quantumblockencoding-robin-warmrobinfourslotproductbitsequiv-all-basis). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFourSlotIndexEquiv" (lean := "QuantumBlockEncoding.Robin.warmRobinFourSlotIndexEquiv")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin four slot index equiv”. Combined system reindexing and six-qubit flattening.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Combined system reindexing and six-qubit flattening.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotBlockEncoding.lean:397](../../../../library/modules/robin-symmetryfourslotblockencoding/#decl-quantumblockencoding-robin-warmrobinfourslotindexequiv). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFourSlotCleanIndex" (lean := "QuantumBlockEncoding.Robin.warmRobinFourSlotCleanIndex")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin four slot clean index”. Flat clean index for an original Robin system basis state.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Flat clean index for an original Robin system basis state.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotBlockEncoding.lean:404](../../../../library/modules/robin-symmetryfourslotblockencoding/#decl-quantumblockencoding-robin-warmrobinfourslotcleanindex). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFourSlotFlatUnitary" (lean := "QuantumBlockEncoding.Robin.warmRobinFourSlotFlatUnitary")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin four slot flat unitary”. Six-qubit matrix in the original system order.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Six-qubit matrix in the original system order.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotBlockEncoding.lean:409](../../../../library/modules/robin-symmetryfourslotblockencoding/#decl-quantumblockencoding-robin-warmrobinfourslotflatunitary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFourSlotFlatUnitary_unitary" (lean := "QuantumBlockEncoding.Robin.warmRobinFourSlotFlatUnitary_unitary")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin four slot flat unitary unitary”; the hypotheses and conclusion in the code panel fix its exact scope. Reindexing preserves exact unitarity.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Reindexing preserves exact unitarity.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotBlockEncoding.lean:415](../../../../library/modules/robin-symmetryfourslotblockencoding/#decl-quantumblockencoding-robin-warmrobinfourslotflatunitary-unitary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFourSlotFlatUnitary_reindex" (lean := "QuantumBlockEncoding.Robin.warmRobinFourSlotFlatUnitary_reindex")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin four slot flat unitary reindex”; the hypotheses and conclusion in the code panel fix its exact scope. Applying the flat matrix at flattened indices recovers the product entry.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Applying the flat matrix at flattened indices recovers the product entry.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotBlockEncoding.lean:422](../../../../library/modules/robin-symmetryfourslotblockencoding/#decl-quantumblockencoding-robin-warmrobinfourslotflatunitary-reindex). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinPairNormalizedTargetComplex_symm" (lean := "QuantumBlockEncoding.Robin.warmRobinPairNormalizedTargetComplex_symm")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin pair normalized target complex symm”; the hypotheses and conclusion in the code panel fix its exact scope. Pair-ordered target at inverse-reindexed indices is the original target.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Pair-ordered target at inverse-reindexed indices is the original target.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotBlockEncoding.lean:432](../../../../library/modules/robin-symmetryfourslotblockencoding/#decl-quantumblockencoding-robin-warmrobinpairnormalizedtargetcomplex-symm). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFourSlotFlatUnitary_cleanBlock" (lean := "QuantumBlockEncoding.Robin.warmRobinFourSlotFlatUnitary_cleanBlock")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin four slot flat unitary clean block”; the hypotheses and conclusion in the code panel fix its exact scope. The flat clean block is exactly the original fixed Robin target.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The flat clean block is exactly the original fixed Robin target.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotBlockEncoding.lean:442](../../../../library/modules/robin-symmetryfourslotblockencoding/#decl-quantumblockencoding-robin-warmrobinfourslotflatunitary-cleanblock). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFourSlotT2Schedule" (lean := "QuantumBlockEncoding.Robin.warmRobinFourSlotT2Schedule")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin four slot t 2 schedule”. Fair T2 logical-stage schedule for the four-slot construction.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Fair T2 logical-stage schedule for the four-slot construction.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotBlockEncoding.lean:458](../../../../library/modules/robin-symmetryfourslotblockencoding/#decl-quantumblockencoding-robin-warmrobinfourslott2schedule). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFourSlotT2Circuit" (lean := "QuantumBlockEncoding.Robin.warmRobinFourSlotT2Circuit")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin four slot t 2 circuit”. The corresponding logical gate list.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The corresponding logical gate list.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotBlockEncoding.lean:470](../../../../library/modules/robin-symmetryfourslotblockencoding/#decl-quantumblockencoding-robin-warmrobinfourslott2circuit). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFourSlotT2Resource" (lean := "QuantumBlockEncoding.Robin.warmRobinFourSlotT2Resource")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin four slot t 2 resource”. Exact resource row under the declared T2 logical-stage convention.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Exact resource row under the declared T2 logical-stage convention.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotBlockEncoding.lean:474](../../../../library/modules/robin-symmetryfourslotblockencoding/#decl-quantumblockencoding-robin-warmrobinfourslott2resource). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFourSlotBlockContainsTarget" (lean := "QuantumBlockEncoding.Robin.warmRobinFourSlotBlockContainsTarget")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin four slot block contains target”. The operator-first clean-block predicate for the four-slot route.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The operator-first clean-block predicate for the four-slot route.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotBlockEncoding.lean:478](../../../../library/modules/robin-symmetryfourslotblockencoding/#decl-quantumblockencoding-robin-warmrobinfourslotblockcontainstarget). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFourSlotBlockContainsTarget_proof" (lean := "QuantumBlockEncoding.Robin.warmRobinFourSlotBlockContainsTarget_proof")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin four slot block contains target proof”; the hypotheses and conclusion in the code panel fix its exact scope. The clean-block predicate is discharged by the original-basis theorem.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The clean-block predicate is discharged by the original-basis theorem.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotBlockEncoding.lean:487](../../../../library/modules/robin-symmetryfourslotblockencoding/#decl-quantumblockencoding-robin-warmrobinfourslotblockcontainstarget-proof). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFourSlotOperatorCandidate" (lean := "QuantumBlockEncoding.Robin.warmRobinFourSlotOperatorCandidate")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin four slot operator candidate”. Four-slot T2 candidate for the fixed 'N=8' Robin target.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Four-slot T2 candidate for the fixed 'N=8' Robin target.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotBlockEncoding.lean:494](../../../../library/modules/robin-symmetryfourslotblockencoding/#decl-quantumblockencoding-robin-warmrobinfourslotoperatorcandidate). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFourSlotVerifiedBlockEncoding" (lean := "QuantumBlockEncoding.Robin.warmRobinFourSlotVerifiedBlockEncoding")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin four slot verified block encoding”. Fully verified T2 block encoding for the four-slot symmetry route.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Fully verified T2 block encoding for the four-slot symmetry route.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotBlockEncoding.lean:514](../../../../library/modules/robin-symmetryfourslotblockencoding/#decl-quantumblockencoding-robin-warmrobinfourslotverifiedblockencoding). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFourSlotT2Cost_betterThan_hadamard8" (lean := "QuantumBlockEncoding.Robin.warmRobinFourSlotT2Cost_betterThan_hadamard8")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin four slot t 2 cost better than hadamard 8”; the hypotheses and conclusion in the code panel fix its exact scope. Under one T2 logical-stage convention, four slots strictly improve the Hadamard-8 route: gate count and depth tie, while the clean layout uses one fewer auxiliary qubit.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Under one T2 logical-stage convention, four slots strictly improve the Hadamard-8 route: gate count and depth tie, while the clean layout uses one fewer auxiliary qubit.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotBlockEncoding.lean:523](../../../../library/modules/robin-symmetryfourslotblockencoding/#decl-quantumblockencoding-robin-warmrobinfourslott2cost-betterthan-hadamard8). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFourSlotT3BlockedLeaf" (lean := "QuantumBlockEncoding.Robin.warmRobinFourSlotT3BlockedLeaf")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin four slot t 3 blocked leaf”. Honest boundary: primitive synthesis and refinement remain a T3 obligation.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Honest boundary: primitive synthesis and refinement remain a T3 obligation.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotBlockEncoding.lean:535](../../../../library/modules/robin-symmetryfourslotblockencoding/#decl-quantumblockencoding-robin-warmrobinfourslott3blockedleaf). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/Robin/SymmetryFourSlotLogicalUnitary.lean

29 explicit public declarations, in source order.

:::definition "QuantumBlockEncoding.Robin.WarmRobinFourSlotBits" (lean := "QuantumBlockEncoding.Robin.WarmRobinFourSlotBits")
*Plain-English reading.* This abbreviation gives a shorter name to the type or expression used for “warm robin four slot bits”. Two binary selector wires before flattening to 'Fin 4'.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Two binary selector wires before flattening to 'Fin 4'.

*Declaration kind.* abbrev.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotLogicalUnitary.lean:21](../../../../library/modules/robin-symmetryfourslotlogicalunitary/#decl-quantumblockencoding-robin-warmrobinfourslotbits). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFourSlotBitsEquiv" (lean := "QuantumBlockEncoding.Robin.warmRobinFourSlotBitsEquiv")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin four slot bits equiv”. Flatten the two selector wires.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Flatten the two selector wires.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotLogicalUnitary.lean:24](../../../../library/modules/robin-symmetryfourslotlogicalunitary/#decl-quantumblockencoding-robin-warmrobinfourslotbitsequiv). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFourSlotBitsPrepare" (lean := "QuantumBlockEncoding.Robin.warmRobinFourSlotBitsPrepare")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin four slot bits prepare”. Uniform two-bit selector PREPARE.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Uniform two-bit selector PREPARE.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotLogicalUnitary.lean:28](../../../../library/modules/robin-symmetryfourslotlogicalunitary/#decl-quantumblockencoding-robin-warmrobinfourslotbitsprepare). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFourSlotBitsPrepare_unitary" (lean := "QuantumBlockEncoding.Robin.warmRobinFourSlotBitsPrepare_unitary")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin four slot bits prepare unitary”; the hypotheses and conclusion in the code panel fix its exact scope. The two-bit selector PREPARE is unitary.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The two-bit selector PREPARE is unitary.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotLogicalUnitary.lean:33](../../../../library/modules/robin-symmetryfourslotlogicalunitary/#decl-quantumblockencoding-robin-warmrobinfourslotbitsprepare-unitary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFourSlotSelectorPrepare" (lean := "QuantumBlockEncoding.Robin.warmRobinFourSlotSelectorPrepare")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin four slot selector prepare”. Flattened four-slot selector PREPARE.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Flattened four-slot selector PREPARE.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotLogicalUnitary.lean:41](../../../../library/modules/robin-symmetryfourslotlogicalunitary/#decl-quantumblockencoding-robin-warmrobinfourslotselectorprepare). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFourSlotSelectorPrepare_unitary" (lean := "QuantumBlockEncoding.Robin.warmRobinFourSlotSelectorPrepare_unitary")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin four slot selector prepare unitary”; the hypotheses and conclusion in the code panel fix its exact scope. Reindexing preserves selector unitarity.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Reindexing preserves selector unitarity.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotLogicalUnitary.lean:47](../../../../library/modules/robin-symmetryfourslotlogicalunitary/#decl-quantumblockencoding-robin-warmrobinfourslotselectorprepare-unitary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFourSlotBitsPrepare_cleanColumn" (lean := "QuantumBlockEncoding.Robin.warmRobinFourSlotBitsPrepare_cleanColumn")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin four slot bits prepare clean column”; the hypotheses and conclusion in the code panel fix its exact scope. Uniform clean-column amplitude before selector flattening.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Uniform clean-column amplitude before selector flattening.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotLogicalUnitary.lean:54](../../../../library/modules/robin-symmetryfourslotlogicalunitary/#decl-quantumblockencoding-robin-warmrobinfourslotbitsprepare-cleancolumn). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFourSlotSelectorPrepare_cleanColumn" (lean := "QuantumBlockEncoding.Robin.warmRobinFourSlotSelectorPrepare_cleanColumn")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin four slot selector prepare clean column”; the hypotheses and conclusion in the code panel fix its exact scope. Uniform clean-column amplitude after selector flattening.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Uniform clean-column amplitude after selector flattening.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotLogicalUnitary.lean:63](../../../../library/modules/robin-symmetryfourslotlogicalunitary/#decl-quantumblockencoding-robin-warmrobinfourslotselectorprepare-cleancolumn). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFourSlotSelectorPrepare_probability" (lean := "QuantumBlockEncoding.Robin.warmRobinFourSlotSelectorPrepare_probability")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin four slot selector prepare probability”; the hypotheses and conclusion in the code panel fix its exact scope. Every selector slot has probability exactly '1/4' in the clean column.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Every selector slot has probability exactly '1/4' in the clean column.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotLogicalUnitary.lean:76](../../../../library/modules/robin-symmetryfourslotlogicalunitary/#decl-quantumblockencoding-robin-warmrobinfourslotselectorprepare-probability). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.WarmRobinSymmetrySystem" (lean := "QuantumBlockEncoding.Robin.WarmRobinSymmetrySystem")
*Plain-English reading.* This abbreviation gives a shorter name to the type or expression used for “warm robin symmetry system”. Sector and reversal-pair coordinate used by the middle logical unitary.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Sector and reversal-pair coordinate used by the middle logical unitary.

*Declaration kind.* abbrev.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotLogicalUnitary.lean:94](../../../../library/modules/robin-symmetryfourslotlogicalunitary/#decl-quantumblockencoding-robin-warmrobinsymmetrysystem). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFourSlotSystemPerm" (lean := "QuantumBlockEncoding.Robin.warmRobinFourSlotSystemPerm")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin four slot system perm”. A four-shift SELECT preserves the symmetry sector.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* A four-shift SELECT preserves the symmetry sector.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotLogicalUnitary.lean:97](../../../../library/modules/robin-symmetryfourslotlogicalunitary/#decl-quantumblockencoding-robin-warmrobinfourslotsystemperm). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFourSlotSystemPerm_bijective" (lean := "QuantumBlockEncoding.Robin.warmRobinFourSlotSystemPerm_bijective")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin four slot system perm bijective”; the hypotheses and conclusion in the code panel fix its exact scope. Each sector-preserving four-shift SELECT is a basis bijection.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Each sector-preserving four-shift SELECT is a basis bijection.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotLogicalUnitary.lean:103](../../../../library/modules/robin-symmetryfourslotlogicalunitary/#decl-quantumblockencoding-robin-warmrobinfourslotsystemperm-bijective). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFourSlotSystemEquiv" (lean := "QuantumBlockEncoding.Robin.warmRobinFourSlotSystemEquiv")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin four slot system equiv”. Package the sector-preserving SELECT as an equivalence.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Package the sector-preserving SELECT as an equivalence.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotLogicalUnitary.lean:108](../../../../library/modules/robin-symmetryfourslotlogicalunitary/#decl-quantumblockencoding-robin-warmrobinfourslotsystemequiv). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFourSlotCoefficient" (lean := "QuantumBlockEncoding.Robin.warmRobinFourSlotCoefficient")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin four slot coefficient”. Real amplitude encoded by a selector slot and sector-system column.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Real amplitude encoded by a selector slot and sector-system column.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotLogicalUnitary.lean:114](../../../../library/modules/robin-symmetryfourslotlogicalunitary/#decl-quantumblockencoding-robin-warmrobinfourslotcoefficient). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFourSlotCoefficient_abs_le_one" (lean := "QuantumBlockEncoding.Robin.warmRobinFourSlotCoefficient_abs_le_one")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin four slot coefficient abs le one”; the hypotheses and conclusion in the code panel fix its exact scope. Every four-slot real amplitude lies in '\[-1,1\]'.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Every four-slot real amplitude lies in '\[-1,1\]'.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotLogicalUnitary.lean:119](../../../../library/modules/robin-symmetryfourslotlogicalunitary/#decl-quantumblockencoding-robin-warmrobinfourslotcoefficient-abs-le-one). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFourSlotRotation" (lean := "QuantumBlockEncoding.Robin.warmRobinFourSlotRotation")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin four slot rotation”. Controlled coefficient rotation for the four-slot route.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Controlled coefficient rotation for the four-slot route.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotLogicalUnitary.lean:131](../../../../library/modules/robin-symmetryfourslotlogicalunitary/#decl-quantumblockencoding-robin-warmrobinfourslotrotation). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFourSlotRotation_unitary" (lean := "QuantumBlockEncoding.Robin.warmRobinFourSlotRotation_unitary")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin four slot rotation unitary”; the hypotheses and conclusion in the code panel fix its exact scope. Every controlled coefficient rotation is unitary.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Every controlled coefficient rotation is unitary.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotLogicalUnitary.lean:137](../../../../library/modules/robin-symmetryfourslotlogicalunitary/#decl-quantumblockencoding-robin-warmrobinfourslotrotation-unitary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFourSlotRotation_cleanEntry" (lean := "QuantumBlockEncoding.Robin.warmRobinFourSlotRotation_cleanEntry")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin four slot rotation clean entry”; the hypotheses and conclusion in the code panel fix its exact scope. The clean coefficient entry is the desired signed amplitude.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The clean coefficient entry is the desired signed amplitude.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotLogicalUnitary.lean:144](../../../../library/modules/robin-symmetryfourslotlogicalunitary/#decl-quantumblockencoding-robin-warmrobinfourslotrotation-cleanentry). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFourSlotCoefficient_complex" (lean := "QuantumBlockEncoding.Robin.warmRobinFourSlotCoefficient_complex")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin four slot coefficient complex”; the hypotheses and conclusion in the code panel fix its exact scope. Rational and real-complex views of the four-slot coefficient agree.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Rational and real-complex views of the four-slot coefficient agree.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotLogicalUnitary.lean:153](../../../../library/modules/robin-symmetryfourslotlogicalunitary/#decl-quantumblockencoding-robin-warmrobinfourslotcoefficient-complex). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFourSlotMiddleLogicalUnitary" (lean := "QuantumBlockEncoding.Robin.warmRobinFourSlotMiddleLogicalUnitary")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin four slot middle logical unitary”. Product-register logical unitary in the symmetry-sector system basis.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Product-register logical unitary in the symmetry-sector system basis.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotLogicalUnitary.lean:161](../../../../library/modules/robin-symmetryfourslotlogicalunitary/#decl-quantumblockencoding-robin-warmrobinfourslotmiddlelogicalunitary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFourSlotMiddleLogicalUnitary_unitary" (lean := "QuantumBlockEncoding.Robin.warmRobinFourSlotMiddleLogicalUnitary_unitary")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin four slot middle logical unitary unitary”; the hypotheses and conclusion in the code panel fix its exact scope. The complete four-slot middle construction is exactly unitary.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The complete four-slot middle construction is exactly unitary.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotLogicalUnitary.lean:171](../../../../library/modules/robin-symmetryfourslotlogicalunitary/#decl-quantumblockencoding-robin-warmrobinfourslotmiddlelogicalunitary-unitary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFourSlotSectorCleanFormula" (lean := "QuantumBlockEncoding.Robin.warmRobinFourSlotSectorCleanFormula")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin four slot sector clean formula”. Structural clean formula on the full sector-system basis.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Structural clean formula on the full sector-system basis.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotLogicalUnitary.lean:180](../../../../library/modules/robin-symmetryfourslotlogicalunitary/#decl-quantumblockencoding-robin-warmrobinfourslotsectorcleanformula). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFourSlotSectorCleanFormula_same" (lean := "QuantumBlockEncoding.Robin.warmRobinFourSlotSectorCleanFormula_same")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin four slot sector clean formula same”; the hypotheses and conclusion in the code panel fix its exact scope. On one sector, the full-system formula is the existing four-shift formula.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* On one sector, the full-system formula is the existing four-shift formula.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotLogicalUnitary.lean:190](../../../../library/modules/robin-symmetryfourslotlogicalunitary/#decl-quantumblockencoding-robin-warmrobinfourslotsectorcleanformula-same). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFourSlotSectorCleanFormula_cross" (lean := "QuantumBlockEncoding.Robin.warmRobinFourSlotSectorCleanFormula_cross")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin four slot sector clean formula cross”; the hypotheses and conclusion in the code panel fix its exact scope. Cross-sector clean entries vanish because SELECT preserves the sector.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Cross-sector clean entries vanish because SELECT preserves the sector.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotLogicalUnitary.lean:200](../../../../library/modules/robin-symmetryfourslotlogicalunitary/#decl-quantumblockencoding-robin-warmrobinfourslotsectorcleanformula-cross). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFourSlotSectorTarget" (lean := "QuantumBlockEncoding.Robin.warmRobinFourSlotSectorTarget")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin four slot sector target”. Direct-sum normalized target in the symmetry-sector basis.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Direct-sum normalized target in the symmetry-sector basis.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotLogicalUnitary.lean:210](../../../../library/modules/robin-symmetryfourslotlogicalunitary/#decl-quantumblockencoding-robin-warmrobinfourslotsectortarget). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFourSlotSectorCleanFormula_eq_target" (lean := "QuantumBlockEncoding.Robin.warmRobinFourSlotSectorCleanFormula_eq_target")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin four slot sector clean formula eq target”; the hypotheses and conclusion in the code panel fix its exact scope. The structural clean formula is exactly the normalized direct sum.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The structural clean formula is exactly the normalized direct sum.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotLogicalUnitary.lean:221](../../../../library/modules/robin-symmetryfourslotlogicalunitary/#decl-quantumblockencoding-robin-warmrobinfourslotsectorcleanformula-eq-target). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFourSlotMiddleLogicalUnitary_cleanEntry" (lean := "QuantumBlockEncoding.Robin.warmRobinFourSlotMiddleLogicalUnitary_cleanEntry")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin four slot middle logical unitary clean entry”; the hypotheses and conclusion in the code panel fix its exact scope. The reusable clean-entry expansion specializes to the four-slot sector formula.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The reusable clean-entry expansion specializes to the four-slot sector formula.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotLogicalUnitary.lean:239](../../../../library/modules/robin-symmetryfourslotlogicalunitary/#decl-quantumblockencoding-robin-warmrobinfourslotmiddlelogicalunitary-cleanentry). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFourSlotMiddleLogicalUnitary_cleanEntry_eq_target" (lean := "QuantumBlockEncoding.Robin.warmRobinFourSlotMiddleLogicalUnitary_cleanEntry_eq_target")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin four slot middle logical unitary clean entry eq target”; the hypotheses and conclusion in the code panel fix its exact scope. The middle logical clean block is exactly the normalized sector target.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The middle logical clean block is exactly the normalized sector target.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotLogicalUnitary.lean:278](../../../../library/modules/robin-symmetryfourslotlogicalunitary/#decl-quantumblockencoding-robin-warmrobinfourslotmiddlelogicalunitary-cleanentry-eq-target). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFourSlotMiddleLogicalUnitary_cleanSystemBlock" (lean := "QuantumBlockEncoding.Robin.warmRobinFourSlotMiddleLogicalUnitary_cleanSystemBlock")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin four slot middle logical unitary clean system block”; the hypotheses and conclusion in the code panel fix its exact scope. Matrix form of the middle clean-block certificate.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Matrix form of the middle clean-block certificate.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotLogicalUnitary.lean:287](../../../../library/modules/robin-symmetryfourslotlogicalunitary/#decl-quantumblockencoding-robin-warmrobinfourslotmiddlelogicalunitary-cleansystemblock). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/Robin/SymmetryFourSlotPrimitive.lean

10 explicit public declarations, in source order.

:::definition "QuantumBlockEncoding.Robin.warmRobinPairCoordinateCircuit" (lean := "QuantumBlockEncoding.Robin.warmRobinPairCoordinateCircuit")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin pair coordinate circuit”. System-wire order is '(p0, p1, sector)'.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* System-wire order is '(p0, p1, sector)'.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotPrimitive.lean:17](../../../../library/modules/robin-symmetryfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinpaircoordinatecircuit). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinPairCoordinateBasisEquiv" (lean := "QuantumBlockEncoding.Robin.warmRobinPairCoordinateBasisEquiv")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin pair coordinate basis equiv”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotPrimitive.lean:20](../../../../library/modules/robin-symmetryfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinpaircoordinatebasisequiv). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinPairCoordinateCircuit_eval_eq" (lean := "QuantumBlockEncoding.Robin.warmRobinPairCoordinateCircuit_eval_eq")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin pair coordinate circuit eval eq”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotPrimitive.lean:25](../../../../library/modules/robin-symmetryfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinpaircoordinatecircuit-eval-eq). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinPairBits" (lean := "QuantumBlockEncoding.Robin.warmRobinPairBits")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin pair bits”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotPrimitive.lean:51](../../../../library/modules/robin-symmetryfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinpairbits). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinOriginalBitsValue" (lean := "QuantumBlockEncoding.Robin.warmRobinOriginalBitsValue")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin original bits value”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotPrimitive.lean:58](../../../../library/modules/robin-symmetryfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinoriginalbitsvalue). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinPairCoordinateCircuit_image" (lean := "QuantumBlockEncoding.Robin.warmRobinPairCoordinateCircuit_image")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin pair coordinate circuit image”; the hypotheses and conclusion in the code panel fix its exact scope. The two CX gates implement the non-free pair-coordinate reindex exactly.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The two CX gates implement the non-free pair-coordinate reindex exactly.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotPrimitive.lean:62](../../../../library/modules/robin-symmetryfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinpaircoordinatecircuit-image). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinPairCoordinateCircuit_bijective" (lean := "QuantumBlockEncoding.Robin.warmRobinPairCoordinateCircuit_bijective")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin pair coordinate circuit bijective”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotPrimitive.lean:70](../../../../library/modules/robin-symmetryfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinpaircoordinatecircuit-bijective). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFourSlotExactAngle" (lean := "QuantumBlockEncoding.Robin.warmRobinFourSlotExactAngle")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin four slot exact angle”. Lean-owned exact standard-RY angle for one loader branch.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Lean-owned exact standard-RY angle for one loader branch.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotPrimitive.lean:75](../../../../library/modules/robin-symmetryfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinfourslotexactangle). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFourSlotExactAngle_eval" (lean := "QuantumBlockEncoding.Robin.warmRobinFourSlotExactAngle_eval")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin four slot exact angle eval”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotPrimitive.lean:83](../../../../library/modules/robin-symmetryfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinfourslotexactangle-eval). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFourSlotExactRy_eq_rotation" (lean := "QuantumBlockEncoding.Robin.warmRobinFourSlotExactRy_eq_rotation")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin four slot exact ry eq rotation”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryFourSlotPrimitive.lean:89](../../../../library/modules/robin-symmetryfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinfourslotexactry-eq-rotation). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/Robin/SymmetryXorFourSlotLogicalUnitary.lean

28 explicit public declarations, in source order.

:::definition "QuantumBlockEncoding.Robin.warmRobinSymmetryXorPerm" (lean := "QuantumBlockEncoding.Robin.warmRobinSymmetryXorPerm")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin symmetry xor perm”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotLogicalUnitary.lean:17](../../../../library/modules/robin-symmetryxorfourslotlogicalunitary/#decl-quantumblockencoding-robin-warmrobinsymmetryxorperm). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinSymmetryXorPerm_bijective" (lean := "QuantumBlockEncoding.Robin.warmRobinSymmetryXorPerm_bijective")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin symmetry xor perm bijective”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotLogicalUnitary.lean:21](../../../../library/modules/robin-symmetryxorfourslotlogicalunitary/#decl-quantumblockencoding-robin-warmrobinsymmetryxorperm-bijective). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinSymmetrySectorBlock" (lean := "QuantumBlockEncoding.Robin.warmRobinSymmetrySectorBlock")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin symmetry sector block”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotLogicalUnitary.lean:25](../../../../library/modules/robin-symmetryxorfourslotlogicalunitary/#decl-quantumblockencoding-robin-warmrobinsymmetrysectorblock). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinSymmetryXorWeight" (lean := "QuantumBlockEncoding.Robin.warmRobinSymmetryXorWeight")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin symmetry xor weight”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotLogicalUnitary.lean:29](../../../../library/modules/robin-symmetryxorfourslotlogicalunitary/#decl-quantumblockencoding-robin-warmrobinsymmetryxorweight). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinSymmetryXorAmplitude" (lean := "QuantumBlockEncoding.Robin.warmRobinSymmetryXorAmplitude")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin symmetry xor amplitude”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotLogicalUnitary.lean:34](../../../../library/modules/robin-symmetryxorfourslotlogicalunitary/#decl-quantumblockencoding-robin-warmrobinsymmetryxoramplitude). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinSymmetryXorAmplitude_bounded" (lean := "QuantumBlockEncoding.Robin.warmRobinSymmetryXorAmplitude_bounded")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin symmetry xor amplitude bounded”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotLogicalUnitary.lean:38](../../../../library/modules/robin-symmetryxorfourslotlogicalunitary/#decl-quantumblockencoding-robin-warmrobinsymmetryxoramplitude-bounded). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinSymmetryXorDecomposition" (lean := "QuantumBlockEncoding.Robin.warmRobinSymmetryXorDecomposition")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin symmetry xor decomposition”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotLogicalUnitary.lean:43](../../../../library/modules/robin-symmetryxorfourslotlogicalunitary/#decl-quantumblockencoding-robin-warmrobinsymmetryxordecomposition). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinXorFourSlotSystemPerm" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotSystemPerm")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin xor four slot system perm”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotLogicalUnitary.lean:52](../../../../library/modules/robin-symmetryxorfourslotlogicalunitary/#decl-quantumblockencoding-robin-warmrobinxorfourslotsystemperm). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinXorFourSlotSystemPerm_bijective" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotSystemPerm_bijective")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin xor four slot system perm bijective”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotLogicalUnitary.lean:57](../../../../library/modules/robin-symmetryxorfourslotlogicalunitary/#decl-quantumblockencoding-robin-warmrobinxorfourslotsystemperm-bijective). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinXorFourSlotSystemEquiv" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotSystemEquiv")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin xor four slot system equiv”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotLogicalUnitary.lean:61](../../../../library/modules/robin-symmetryxorfourslotlogicalunitary/#decl-quantumblockencoding-robin-warmrobinxorfourslotsystemequiv). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinXorFourSlotSystemEquiv_apply" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotSystemEquiv_apply")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin xor four slot system equiv apply”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotLogicalUnitary.lean:66](../../../../library/modules/robin-symmetryxorfourslotlogicalunitary/#decl-quantumblockencoding-robin-warmrobinxorfourslotsystemequiv-apply). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinXorFourSlotCoefficient" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotCoefficient")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin xor four slot coefficient”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotLogicalUnitary.lean:71](../../../../library/modules/robin-symmetryxorfourslotlogicalunitary/#decl-quantumblockencoding-robin-warmrobinxorfourslotcoefficient). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinXorFourSlotCoefficient_abs_le_one" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotCoefficient_abs_le_one")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin xor four slot coefficient abs le one”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotLogicalUnitary.lean:75](../../../../library/modules/robin-symmetryxorfourslotlogicalunitary/#decl-quantumblockencoding-robin-warmrobinxorfourslotcoefficient-abs-le-one). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinXorFourSlotRotation" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotRotation")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin xor four slot rotation”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotLogicalUnitary.lean:84](../../../../library/modules/robin-symmetryxorfourslotlogicalunitary/#decl-quantumblockencoding-robin-warmrobinxorfourslotrotation). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinXorFourSlotRotation_unitary" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotRotation_unitary")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin xor four slot rotation unitary”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotLogicalUnitary.lean:89](../../../../library/modules/robin-symmetryxorfourslotlogicalunitary/#decl-quantumblockencoding-robin-warmrobinxorfourslotrotation-unitary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinXorFourSlotRotation_cleanEntry" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotRotation_cleanEntry")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin xor four slot rotation clean entry”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotLogicalUnitary.lean:95](../../../../library/modules/robin-symmetryxorfourslotlogicalunitary/#decl-quantumblockencoding-robin-warmrobinxorfourslotrotation-cleanentry). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinXorFourSlotMiddleLogicalUnitary" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotMiddleLogicalUnitary")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin xor four slot middle logical unitary”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotLogicalUnitary.lean:103](../../../../library/modules/robin-symmetryxorfourslotlogicalunitary/#decl-quantumblockencoding-robin-warmrobinxorfourslotmiddlelogicalunitary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinXorFourSlotMiddleLogicalUnitary_unitary" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotMiddleLogicalUnitary_unitary")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin xor four slot middle logical unitary unitary”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotLogicalUnitary.lean:112](../../../../library/modules/robin-symmetryxorfourslotlogicalunitary/#decl-quantumblockencoding-robin-warmrobinxorfourslotmiddlelogicalunitary-unitary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinXorFourSlotSectorCleanFormula" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotSectorCleanFormula")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin xor four slot sector clean formula”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotLogicalUnitary.lean:120](../../../../library/modules/robin-symmetryxorfourslotlogicalunitary/#decl-quantumblockencoding-robin-warmrobinxorfourslotsectorcleanformula). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinXorFourSlotCleanFormula_eq_target" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotCleanFormula_eq_target")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin xor four slot clean formula eq target”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotLogicalUnitary.lean:128](../../../../library/modules/robin-symmetryxorfourslotlogicalunitary/#decl-quantumblockencoding-robin-warmrobinxorfourslotcleanformula-eq-target). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinXorFourSlotMiddleLogicalUnitary_cleanEntry" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotMiddleLogicalUnitary_cleanEntry")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin xor four slot middle logical unitary clean entry”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotLogicalUnitary.lean:137](../../../../library/modules/robin-symmetryxorfourslotlogicalunitary/#decl-quantumblockencoding-robin-warmrobinxorfourslotmiddlelogicalunitary-cleanentry). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinXorFourSlotMiddleLogicalUnitary_cleanSystemBlock" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotMiddleLogicalUnitary_cleanSystemBlock")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin xor four slot middle logical unitary clean system block”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotLogicalUnitary.lean:178](../../../../library/modules/robin-symmetryxorfourslotlogicalunitary/#decl-quantumblockencoding-robin-warmrobinxorfourslotmiddlelogicalunitary-cleansystemblock). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinXorFourSlotPairLogicalUnitary" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotPairLogicalUnitary")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin xor four slot pair logical unitary”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotLogicalUnitary.lean:187](../../../../library/modules/robin-symmetryxorfourslotlogicalunitary/#decl-quantumblockencoding-robin-warmrobinxorfourslotpairlogicalunitary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinXorFourSlotPairLogicalUnitary_unitary" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotPairLogicalUnitary_unitary")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin xor four slot pair logical unitary unitary”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotLogicalUnitary.lean:194](../../../../library/modules/robin-symmetryxorfourslotlogicalunitary/#decl-quantumblockencoding-robin-warmrobinxorfourslotpairlogicalunitary-unitary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinXorFourSlotPairLogicalUnitary_cleanSystemBlock" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotPairLogicalUnitary_cleanSystemBlock")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin xor four slot pair logical unitary clean system block”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotLogicalUnitary.lean:202](../../../../library/modules/robin-symmetryxorfourslotlogicalunitary/#decl-quantumblockencoding-robin-warmrobinxorfourslotpairlogicalunitary-cleansystemblock). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinXorFourSlotFlatUnitary" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotFlatUnitary")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin xor four slot flat unitary”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotLogicalUnitary.lean:210](../../../../library/modules/robin-symmetryxorfourslotlogicalunitary/#decl-quantumblockencoding-robin-warmrobinxorfourslotflatunitary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinXorFourSlotFlatUnitary_unitary" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotFlatUnitary_unitary")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin xor four slot flat unitary unitary”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotLogicalUnitary.lean:215](../../../../library/modules/robin-symmetryxorfourslotlogicalunitary/#decl-quantumblockencoding-robin-warmrobinxorfourslotflatunitary-unitary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinXorFourSlotFlatUnitary_cleanBlock" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotFlatUnitary_cleanBlock")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin xor four slot flat unitary clean block”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotLogicalUnitary.lean:221](../../../../library/modules/robin-symmetryxorfourslotlogicalunitary/#decl-quantumblockencoding-robin-warmrobinxorfourslotflatunitary-cleanblock). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean

75 explicit public declarations, in source order.

:::definition "QuantumBlockEncoding.Robin.warmRobinXorFourSlotSelectCircuit" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotSelectCircuit")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin xor four slot select circuit”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean:18](../../../../library/modules/robin-symmetryxorfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinxorfourslotselectcircuit). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinXorFourSlotSelectBasisEquiv" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotSelectBasisEquiv")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin xor four slot select basis equiv”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean:21](../../../../library/modules/robin-symmetryxorfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinxorfourslotselectbasisequiv). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinXorFourSlotSelectBasisAction_eq_perm" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotSelectBasisAction_eq_perm")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin xor four slot select basis action eq perm”; the hypotheses and conclusion in the code panel fix its exact scope. The two selected system bits are XORed with the selector, while the sector, selector, and coefficient wires are unchanged.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The two selected system bits are XORed with the selector, while the sector, selector, and coefficient wires are unchanged.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean:28](../../../../library/modules/robin-symmetryxorfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinxorfourslotselectbasisaction-eq-perm). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinXorFourSlotSelectProgram_eval" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotSelectProgram_eval")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin xor four slot select program eval”; the hypotheses and conclusion in the code panel fix its exact scope. Exact matrix semantics of the physical XOR SELECT program.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Exact matrix semantics of the physical XOR SELECT program.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean:39](../../../../library/modules/robin-symmetryxorfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinxorfourslotselectprogram-eval). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinXorFourSlotSelectProgram_noOracleCalls" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotSelectProgram_noOracleCalls")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin xor four slot select program no oracle calls”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean:65](../../../../library/modules/robin-symmetryxorfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinxorfourslotselectprogram-nooraclecalls). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinXorFourSlotControlWires" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotControlWires")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin xor four slot control wires”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean:69](../../../../library/modules/robin-symmetryxorfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinxorfourslotcontrolwires). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinXorFourSlotControlWires_ne_target" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotControlWires_ne_target")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin xor four slot control wires ne target”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean:72](../../../../library/modules/robin-symmetryxorfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinxorfourslotcontrolwires-ne-target). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinXorFourSlotControlPair" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotControlPair")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin xor four slot control pair”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean:78](../../../../library/modules/robin-symmetryxorfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinxorfourslotcontrolpair). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinXorFourSlotControlSector" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotControlSector")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin xor four slot control sector”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean:82](../../../../library/modules/robin-symmetryxorfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinxorfourslotcontrolsector). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinXorFourSlotControlSelector" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotControlSelector")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin xor four slot control selector”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean:85](../../../../library/modules/robin-symmetryxorfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinxorfourslotcontrolselector). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinXorFourSlotAmplitudeAngle" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotAmplitudeAngle")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin xor four slot amplitude angle”. Exact standard-RY angle for each of the 32 multiplexor branches.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Exact standard-RY angle for each of the 32 multiplexor branches.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean:90](../../../../library/modules/robin-symmetryxorfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinxorfourslotamplitudeangle). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinXorFourSlotAmplitudeAngle_eval" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotAmplitudeAngle_eval")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin xor four slot amplitude angle eval”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean:102](../../../../library/modules/robin-symmetryxorfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinxorfourslotamplitudeangle-eval). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinXorFourSlotAmplitudeRy_eq_rotation" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotAmplitudeRy_eq_rotation")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin xor four slot amplitude ry eq rotation”; the hypotheses and conclusion in the code panel fix its exact scope. Every exact angle denotes the corresponding T2 amplitude rotation.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Every exact angle denotes the corresponding T2 amplitude rotation.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean:113](../../../../library/modules/robin-symmetryxorfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinxorfourslotamplitudery-eq-rotation). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinXorFourSlotContextIndex" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotContextIndex")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin xor four slot context index”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean:130](../../../../library/modules/robin-symmetryxorfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinxorfourslotcontextindex). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinXorFourSlotContextIndex_bijective" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotContextIndex_bijective")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin xor four slot context index bijective”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean:139](../../../../library/modules/robin-symmetryxorfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinxorfourslotcontextindex-bijective). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinXorFourSlotContextEquiv" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotContextEquiv")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin xor four slot context equiv”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean:143](../../../../library/modules/robin-symmetryxorfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinxorfourslotcontextequiv). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinXorFourSlotContextEquiv_apply" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotContextEquiv_apply")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin xor four slot context equiv apply”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean:149](../../../../library/modules/robin-symmetryxorfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinxorfourslotcontextequiv-apply). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinXorFourSlotMiddleBitsEquiv" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotMiddleBitsEquiv")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin xor four slot middle bits equiv”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean:154](../../../../library/modules/robin-symmetryxorfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinxorfourslotmiddlebitsequiv). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinXorFourSlotMiddleBitsEquiv_apply" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotMiddleBitsEquiv_apply")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin xor four slot middle bits equiv apply”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean:161](../../../../library/modules/robin-symmetryxorfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinxorfourslotmiddlebitsequiv-apply). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinXorFourSlotSelectBasisAction_middleIndex" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotSelectBasisAction_middleIndex")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin xor four slot select basis action middle index”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean:169](../../../../library/modules/robin-symmetryxorfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinxorfourslotselectbasisaction-middleindex). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinXorFourSlotSelectProgram_eval_reindexed" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotSelectProgram_eval_reindexed")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin xor four slot select program eval reindexed”; the hypotheses and conclusion in the code panel fix its exact scope. Matrix-level SELECT refinement under the exact middle-register reindex.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Matrix-level SELECT refinement under the exact middle-register reindex.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean:190](../../../../library/modules/robin-symmetryxorfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinxorfourslotselectprogram-eval-reindexed). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinXorFourSlotControlledRy_eq_amplitudeLift" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotControlledRy_eq_amplitudeLift")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin xor four slot controlled ry eq amplitude lift”; the hypotheses and conclusion in the code panel fix its exact scope. The generic five-control multiplexor is exactly the T2 amplitude lift after the explicit little-endian product-register reindexing.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The generic five-control multiplexor is exactly the T2 amplitude lift after the explicit little-endian product-register reindexing.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean:217](../../../../library/modules/robin-symmetryxorfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinxorfourslotcontrolledry-eq-amplitudelift). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinXorFourSlotSelectorLowMatrix" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotSelectorLowMatrix")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin xor four slot selector low matrix”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean:260](../../../../library/modules/robin-symmetryxorfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinxorfourslotselectorlowmatrix). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinXorFourSlotSelectorHighMatrix" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotSelectorHighMatrix")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin xor four slot selector high matrix”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean:265](../../../../library/modules/robin-symmetryxorfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinxorfourslotselectorhighmatrix). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinXorFourSlotSelectorHighLow_eq_prepare" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotSelectorHighLow_eq_prepare")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin xor four slot selector high low eq prepare”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean:270](../../../../library/modules/robin-symmetryxorfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinxorfourslotselectorhighlow-eq-prepare). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinXorFourSlotSelectorPrepareCircuit" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotSelectorPrepareCircuit")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin xor four slot selector prepare circuit”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean:282](../../../../library/modules/robin-symmetryxorfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinxorfourslotselectorpreparecircuit). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFourSlotBitsEquiv_symm_pack" (lean := "QuantumBlockEncoding.Robin.warmRobinFourSlotBitsEquiv_symm_pack")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin four slot bits equiv symm pack”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean:285](../../../../library/modules/robin-symmetryxorfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinfourslotbitsequiv-symm-pack). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinXorFourSlotLowContext_iff" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotLowContext_iff")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin xor four slot low context iff”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean:291](../../../../library/modules/robin-symmetryxorfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinxorfourslotlowcontext-iff). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinXorFourSlotHighContext_iff" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotHighContext_iff")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin xor four slot high context iff”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean:300](../../../../library/modules/robin-symmetryxorfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinxorfourslothighcontext-iff). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinXorFourSlotSelectorLow_eval" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotSelectorLow_eval")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin xor four slot selector low eval”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean:309](../../../../library/modules/robin-symmetryxorfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinxorfourslotselectorlow-eval). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinXorFourSlotSelectorHigh_eval" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotSelectorHigh_eval")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin xor four slot selector high eval”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean:334](../../../../library/modules/robin-symmetryxorfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinxorfourslotselectorhigh-eval). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinXorFourSlotSelectorPrepareCircuit_eval" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotSelectorPrepareCircuit_eval")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin xor four slot selector prepare circuit eval”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean:359](../../../../library/modules/robin-symmetryxorfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinxorfourslotselectorpreparecircuit-eval). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinXorFourSlotSelectorUnprepareCircuit" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotSelectorUnprepareCircuit")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin xor four slot selector unprepare circuit”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean:378](../../../../library/modules/robin-symmetryxorfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinxorfourslotselectorunpreparecircuit). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinXorFourSlotSelectorUnprepareCircuit_eval" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotSelectorUnprepareCircuit_eval")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin xor four slot selector unprepare circuit eval”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean:381](../../../../library/modules/robin-symmetryxorfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinxorfourslotselectorunpreparecircuit-eval). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinXorFourSlotAmplitudeCircuit" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotAmplitudeCircuit")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin xor four slot amplitude circuit”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean:394](../../../../library/modules/robin-symmetryxorfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinxorfourslotamplitudecircuit). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinXorFourSlotAmplitudeProgram" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotAmplitudeProgram")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin xor four slot amplitude program”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean:399](../../../../library/modules/robin-symmetryxorfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinxorfourslotamplitudeprogram). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinXorFourSlotAmplitudeProgram_eval" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotAmplitudeProgram_eval")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin xor four slot amplitude program eval”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean:403](../../../../library/modules/robin-symmetryxorfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinxorfourslotamplitudeprogram-eval). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinXorFourSlotAmplitudeCircuit_eval_reindexed" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotAmplitudeCircuit_eval_reindexed")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin xor four slot amplitude circuit eval reindexed”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean:416](../../../../library/modules/robin-symmetryxorfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinxorfourslotamplitudecircuit-eval-reindexed). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinXorFourSlotPrimitiveMiddleCircuit" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotPrimitiveMiddleCircuit")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin xor four slot primitive middle circuit”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean:425](../../../../library/modules/robin-symmetryxorfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinxorfourslotprimitivemiddlecircuit). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinXorFourSlotPrimitiveMiddle_eval" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotPrimitiveMiddle_eval")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin xor four slot primitive middle eval”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean:432](../../../../library/modules/robin-symmetryxorfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinxorfourslotprimitivemiddle-eval). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinXorFourSlotPairCoordinateCircuit" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotPairCoordinateCircuit")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin xor four slot pair coordinate circuit”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean:449](../../../../library/modules/robin-symmetryxorfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinxorfourslotpaircoordinatecircuit). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinXorFourSlotPairCoordinateBasisEquiv" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotPairCoordinateBasisEquiv")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin xor four slot pair coordinate basis equiv”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean:452](../../../../library/modules/robin-symmetryxorfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinxorfourslotpaircoordinatebasisequiv). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinXorFourSlotPairCoordinateCircuit_eval" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotPairCoordinateCircuit_eval")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin xor four slot pair coordinate circuit eval”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean:457](../../../../library/modules/robin-symmetryxorfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinxorfourslotpaircoordinatecircuit-eval). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinXorFourSlotPairCoordinateBasisEquiv_involutive" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotPairCoordinateBasisEquiv_involutive")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin xor four slot pair coordinate basis equiv involutive”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean:485](../../../../library/modules/robin-symmetryxorfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinxorfourslotpaircoordinatebasisequiv-involutive). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinXorFourSlotSymmetryContext_iff" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotSymmetryContext_iff")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin xor four slot symmetry context iff”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean:491](../../../../library/modules/robin-symmetryxorfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinxorfourslotsymmetrycontext-iff). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinXorFourSlotSymmetryPrepareCircuit" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotSymmetryPrepareCircuit")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin xor four slot symmetry prepare circuit”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean:502](../../../../library/modules/robin-symmetryxorfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinxorfourslotsymmetrypreparecircuit). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinXorFourSlotSymmetryPrepareCircuit_eval" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotSymmetryPrepareCircuit_eval")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin xor four slot symmetry prepare circuit eval”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean:505](../../../../library/modules/robin-symmetryxorfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinxorfourslotsymmetrypreparecircuit-eval). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinXorFourSlotSymmetryUnprepareCircuit" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotSymmetryUnprepareCircuit")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin xor four slot symmetry unprepare circuit”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean:531](../../../../library/modules/robin-symmetryxorfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinxorfourslotsymmetryunpreparecircuit). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinXorFourSlotSymmetryUnprepareCircuit_eval" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotSymmetryUnprepareCircuit_eval")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin xor four slot symmetry unprepare circuit eval”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean:534](../../../../library/modules/robin-symmetryxorfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinxorfourslotsymmetryunpreparecircuit-eval). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinXorFourSlotPrimitivePairCircuit" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotPrimitivePairCircuit")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin xor four slot primitive pair circuit”. The middle logical unitary conjugated back from symmetry-sector to pair coordinates, still expressed on the six named primitive wires.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The middle logical unitary conjugated back from symmetry-sector to pair coordinates, still expressed on the six named primitive wires.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean:548](../../../../library/modules/robin-symmetryxorfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinxorfourslotprimitivepaircircuit). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinXorFourSlotPrimitivePair_eval" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotPrimitivePair_eval")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin xor four slot primitive pair eval”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean:554](../../../../library/modules/robin-symmetryxorfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinxorfourslotprimitivepair-eval). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinXorFourSlotOriginalBitsEquiv" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotOriginalBitsEquiv")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin xor four slot original bits equiv”. The basis interpretation after the physical pair-coordinate CX stage.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The basis interpretation after the physical pair-coordinate CX stage.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean:569](../../../../library/modules/robin-symmetryxorfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinxorfourslotoriginalbitsequiv). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinXorFourSlotPrimitiveCircuit" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotPrimitiveCircuit")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin xor four slot primitive circuit”. The pair-coordinate circuit, the logical pair circuit, and its inverse in the chronological physical order required by the six-wire implementation.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The pair-coordinate circuit, the logical pair circuit, and its inverse in the chronological physical order required by the six-wire implementation.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean:577](../../../../library/modules/robin-symmetryxorfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinxorfourslotprimitivecircuit). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinXorFourSlotPrimitive_eval_reindexedPair" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotPrimitive_eval_reindexedPair")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin xor four slot primitive eval reindexed pair”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean:582](../../../../library/modules/robin-symmetryxorfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinxorfourslotprimitive-eval-reindexedpair). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinXorFourSlotOriginalBitsEquiv_index" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotOriginalBitsEquiv_index")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin xor four slot original bits equiv index”; the hypotheses and conclusion in the code panel fix its exact scope. The physical pair-coordinate convention agrees with the original Robin system order and the repository-wide six-wire little-endian convention.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The physical pair-coordinate convention agrees with the original Robin system order and the repository-wide six-wire little-endian convention.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean:600](../../../../library/modules/robin-symmetryxorfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinxorfourslotoriginalbitsequiv-index). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinXorFourSlotPrimitive_eval_eq_flatUnitary" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotPrimitive_eval_eq_flatUnitary")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin xor four slot primitive eval eq flat unitary”; the hypotheses and conclusion in the code panel fix its exact scope. Exact T3 refinement root: the primitive '\{X, RY, RZ, CX\}' circuit denotes the XOR four-slot T2 unitary after the explicit little-endian reindexing.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Exact T3 refinement root: the primitive '\{X, RY, RZ, CX\}' circuit denotes the XOR four-slot T2 unitary after the explicit little-endian reindexing.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean:618](../../../../library/modules/robin-symmetryxorfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinxorfourslotprimitive-eval-eq-flatunitary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinXorFourSlotPrimitiveProgram" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotPrimitiveProgram")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin xor four slot primitive program”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean:640](../../../../library/modules/robin-symmetryxorfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinxorfourslotprimitiveprogram). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinXorFourSlotPrimitiveProgram_eval" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotPrimitiveProgram_eval")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin xor four slot primitive program eval”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean:644](../../../../library/modules/robin-symmetryxorfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinxorfourslotprimitiveprogram-eval). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinXorFourSlotPrimitiveCleanIndex" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotPrimitiveCleanIndex")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin xor four slot primitive clean index”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean:657](../../../../library/modules/robin-symmetryxorfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinxorfourslotprimitivecleanindex). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinXorFourSlotPrimitive_cleanBlock" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotPrimitive_cleanBlock")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin xor four slot primitive clean block”; the hypotheses and conclusion in the code panel fix its exact scope. The executable primitive circuit has the required exact clean block.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The executable primitive circuit has the required exact clean block.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean:662](../../../../library/modules/robin-symmetryxorfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinxorfourslotprimitive-cleanblock). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinXorFourSlotPrimitive_unitary" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotPrimitive_unitary")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin xor four slot primitive unitary”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean:675](../../../../library/modules/robin-symmetryxorfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinxorfourslotprimitive-unitary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinXorFourSlotPrimitive_noOracleCalls" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotPrimitive_noOracleCalls")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin xor four slot primitive no oracle calls”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean:680](../../../../library/modules/robin-symmetryxorfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinxorfourslotprimitive-nooraclecalls). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinXorFourSlotPrimitivePresentation" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotPrimitivePresentation")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin xor four slot primitive presentation”. Presentation-only conversion into the repository's legacy circuit list.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Presentation-only conversion into the repository's legacy circuit list. The authoritative T3 semantics and resources remain those of PrimitiveCircuit.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean:686](../../../../library/modules/robin-symmetryxorfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinxorfourslotprimitivepresentation). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinXorFourSlotPrimitiveResource" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotPrimitiveResource")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin xor four slot primitive resource”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean:694](../../../../library/modules/robin-symmetryxorfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinxorfourslotprimitiveresource). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinXorFourSlotPrimitive_resource_faithful" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotPrimitive_resource_faithful")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin xor four slot primitive resource faithful”; the hypotheses and conclusion in the code panel fix its exact scope. Resource ownership is definitional: no handwritten gate or depth tuple is used by the promoted candidate.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Resource ownership is definitional: no handwritten gate or depth tuple is used by the promoted candidate.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean:699](../../../../library/modules/robin-symmetryxorfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinxorfourslotprimitive-resource-faithful). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinXorFourSlotPrimitiveBlockContainsTarget" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotPrimitiveBlockContainsTarget")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin xor four slot primitive block contains target”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean:703](../../../../library/modules/robin-symmetryxorfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinxorfourslotprimitiveblockcontainstarget). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinXorFourSlotPrimitiveBlockContainsTarget_proof" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotPrimitiveBlockContainsTarget_proof")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin xor four slot primitive block contains target proof”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean:711](../../../../library/modules/robin-symmetryxorfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinxorfourslotprimitiveblockcontainstarget-proof). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinXorFourSlotPrimitiveOperatorCandidate" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotPrimitiveOperatorCandidate")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin xor four slot primitive operator candidate”. T3 candidate whose resource row is computed from its exact primitive program.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* T3 candidate whose resource row is computed from its exact primitive program. The refinement theorem above links this circuit to this unitary.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean:719](../../../../library/modules/robin-symmetryxorfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinxorfourslotprimitiveoperatorcandidate). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinXorFourSlotPrimitiveRefinement" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotPrimitiveRefinement")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin xor four slot primitive refinement”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean:736](../../../../library/modules/robin-symmetryxorfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinxorfourslotprimitiverefinement). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinXorFourSlotPrimitiveVerifiedBlockEncoding" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotPrimitiveVerifiedBlockEncoding")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin xor four slot primitive verified block encoding”. Exact primitive verified block encoding for the XOR evolved route.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Exact primitive verified block encoding for the XOR evolved route.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean:744](../../../../library/modules/robin-symmetryxorfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinxorfourslotprimitiveverifiedblockencoding). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFourSlotT3FlatUnitary" (lean := "QuantumBlockEncoding.Robin.warmRobinFourSlotT3FlatUnitary")
*Plain-English reading.* This abbreviation gives a shorter name to the type or expression used for “warm robin four slot t 3 flat unitary”. Dashboard-compatible names explicitly pointing to the XOR T3 route.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Dashboard-compatible names explicitly pointing to the XOR T3 route.

*Declaration kind.* abbrev.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean:751](../../../../library/modules/robin-symmetryxorfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinfourslott3flatunitary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFourSlotPrimitive_eval_eq_flatUnitary" (lean := "QuantumBlockEncoding.Robin.warmRobinFourSlotPrimitive_eval_eq_flatUnitary")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin four slot primitive eval eq flat unitary”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean:754](../../../../library/modules/robin-symmetryxorfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinfourslotprimitive-eval-eq-flatunitary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFourSlotPrimitiveVerifiedBlockEncoding" (lean := "QuantumBlockEncoding.Robin.warmRobinFourSlotPrimitiveVerifiedBlockEncoding")
*Plain-English reading.* This abbreviation gives a shorter name to the type or expression used for “warm robin four slot primitive verified block encoding”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* abbrev.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean:761](../../../../library/modules/robin-symmetryxorfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinfourslotprimitiveverifiedblockencoding). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinXorFourSlotAmplitudeCircuit_counts" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotAmplitudeCircuit_counts")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin xor four slot amplitude circuit counts”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean:764](../../../../library/modules/robin-symmetryxorfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinxorfourslotamplitudecircuit-counts). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinXorFourSlotAmplitudeProgram_noOracleCalls" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotAmplitudeProgram_noOracleCalls")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin xor four slot amplitude program no oracle calls”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SymmetryXorFourSlotPrimitive.lean:769](../../../../library/modules/robin-symmetryxorfourslotprimitive/#decl-quantumblockencoding-robin-warmrobinxorfourslotamplitudeprogram-nooraclecalls). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/Robin/SystemConjugation.lean

10 explicit public declarations, in source order.

:::definition "QuantumBlockEncoding.Robin.ComplexLCU.systemLift" (lean := "QuantumBlockEncoding.Robin.ComplexLCU.systemLift")
*Plain-English reading.* This definition gives the library's named construction or computation for “system lift”. Lift a system-register matrix through coefficient and selector identities.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Lift a system-register matrix through coefficient and selector identities.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SystemConjugation.lean:19](../../../../library/modules/robin-systemconjugation/#decl-quantumblockencoding-robin-complexlcu-systemlift). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.ComplexLCU.systemLift_unitary" (lean := "QuantumBlockEncoding.Robin.ComplexLCU.systemLift_unitary")
*Plain-English reading.* Lean checks the proposition indexed as “system lift unitary”; the hypotheses and conclusion in the code panel fix its exact scope. A unitary system operation remains unitary after the identity lifts.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* A unitary system operation remains unitary after the identity lifts.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SystemConjugation.lean:32](../../../../library/modules/robin-systemconjugation/#decl-quantumblockencoding-robin-complexlcu-systemlift-unitary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.ComplexLCU.systemLift_apply" (lean := "QuantumBlockEncoding.Robin.ComplexLCU.systemLift_apply")
*Plain-English reading.* Lean checks the proposition indexed as “system lift apply”; the hypotheses and conclusion in the code panel fix its exact scope. Entry formula: coefficient and selector are Kronecker deltas.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Entry formula: coefficient and selector are Kronecker deltas.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SystemConjugation.lean:49](../../../../library/modules/robin-systemconjugation/#decl-quantumblockencoding-robin-complexlcu-systemlift-apply). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.ComplexLCU.star_systemLift_apply" (lean := "QuantumBlockEncoding.Robin.ComplexLCU.star_systemLift_apply")
*Plain-English reading.* Lean checks the proposition indexed as “star system lift apply”; the hypotheses and conclusion in the code panel fix its exact scope. Entry formula for the adjoint system lift.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Entry formula for the adjoint system lift.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SystemConjugation.lean:68](../../../../library/modules/robin-systemconjugation/#decl-quantumblockencoding-robin-complexlcu-star-systemlift-apply). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.ComplexLCU.systemLift_mul_cleanRow" (lean := "QuantumBlockEncoding.Robin.ComplexLCU.systemLift_mul_cleanRow")
*Plain-English reading.* Lean checks the proposition indexed as “system lift mul clean row”; the hypotheses and conclusion in the code panel fix its exact scope. Left multiplication by a lifted system matrix on a clean row.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Left multiplication by a lifted system matrix on a clean row.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SystemConjugation.lean:89](../../../../library/modules/robin-systemconjugation/#decl-quantumblockencoding-robin-complexlcu-systemlift-mul-cleanrow). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.ComplexLCU.mul_star_systemLift_cleanColumn" (lean := "QuantumBlockEncoding.Robin.ComplexLCU.mul_star_systemLift_cleanColumn")
*Plain-English reading.* Lean checks the proposition indexed as “mul star system lift clean column”; the hypotheses and conclusion in the code panel fix its exact scope. Right multiplication by the adjoint lift on a clean column.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Right multiplication by the adjoint lift on a clean column.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SystemConjugation.lean:121](../../../../library/modules/robin-systemconjugation/#decl-quantumblockencoding-robin-complexlcu-mul-star-systemlift-cleancolumn). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.ComplexLCU.cleanSystemBlock" (lean := "QuantumBlockEncoding.Robin.ComplexLCU.cleanSystemBlock")
*Plain-English reading.* This definition gives the library's named construction or computation for “clean system block”. Extract the coefficient/selector clean block as a system matrix.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Extract the coefficient/selector clean block as a system matrix.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SystemConjugation.lean:154](../../../../library/modules/robin-systemconjugation/#decl-quantumblockencoding-robin-complexlcu-cleansystemblock). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.ComplexLCU.conjugateSystem" (lean := "QuantumBlockEncoding.Robin.ComplexLCU.conjugateSystem")
*Plain-English reading.* This definition gives the library's named construction or computation for “conjugate system”. Conjugate a full logical matrix only on its system register.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Conjugate a full logical matrix only on its system register.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/SystemConjugation.lean:165](../../../../library/modules/robin-systemconjugation/#decl-quantumblockencoding-robin-complexlcu-conjugatesystem). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.ComplexLCU.conjugateSystem_unitary" (lean := "QuantumBlockEncoding.Robin.ComplexLCU.conjugateSystem_unitary")
*Plain-English reading.* Lean checks the proposition indexed as “conjugate system unitary”; the hypotheses and conclusion in the code panel fix its exact scope. System conjugation preserves unitarity.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* System conjugation preserves unitarity.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SystemConjugation.lean:182](../../../../library/modules/robin-systemconjugation/#decl-quantumblockencoding-robin-complexlcu-conjugatesystem-unitary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.ComplexLCU.cleanSystemBlock_conjugateSystem" (lean := "QuantumBlockEncoding.Robin.ComplexLCU.cleanSystemBlock_conjugateSystem")
*Plain-English reading.* Lean checks the proposition indexed as “clean system block conjugate system”; the hypotheses and conclusion in the code panel fix its exact scope. Conjugating the full logical matrix conjugates exactly its clean system block.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Conjugating the full logical matrix conjugates exactly its clean system block.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/SystemConjugation.lean:209](../../../../library/modules/robin-systemconjugation/#decl-quantumblockencoding-robin-complexlcu-cleansystemblock-conjugatesystem). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/Robin/T3ResourceComparison.lean

8 explicit public declarations, in source order.

:::theorem "QuantumBlockEncoding.Robin.warmRobinPaperSevenPrimitiveResource_exact" (lean := "QuantumBlockEncoding.Robin.warmRobinPaperSevenPrimitiveResource_exact")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin paper seven primitive resource exact”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/T3ResourceComparison.lean:20](../../../../library/modules/robin-t3resourcecomparison/#decl-quantumblockencoding-robin-warmrobinpapersevenprimitiveresource-exact). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinXorFourSlotPrimitiveResource_exact" (lean := "QuantumBlockEncoding.Robin.warmRobinXorFourSlotPrimitiveResource_exact")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin xor four slot primitive resource exact”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/T3ResourceComparison.lean:30](../../../../library/modules/robin-t3resourcecomparison/#decl-quantumblockencoding-robin-warmrobinxorfourslotprimitiveresource-exact). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFigure4PrimitiveResource_exact" (lean := "QuantumBlockEncoding.Robin.warmRobinFigure4PrimitiveResource_exact")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin figure 4 primitive resource exact”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/T3ResourceComparison.lean:40](../../../../library/modules/robin-t3resourcecomparison/#decl-quantumblockencoding-robin-warmrobinfigure4primitiveresource-exact). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFourSlotT3Cost_betterThan_paperSeven" (lean := "QuantumBlockEncoding.Robin.warmRobinFourSlotT3Cost_betterThan_paperSeven")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin four slot t 3 cost better than paper seven”; the hypotheses and conclusion in the code panel fix its exact scope. The accepted XOR route uses 106 gates versus the source normal form's 312; the later score fields therefore do not decide this comparison.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The accepted XOR route uses 106 gates versus the source normal form's 312; the later score fields therefore do not decide this comparison.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/T3ResourceComparison.lean:52](../../../../library/modules/robin-t3resourcecomparison/#decl-quantumblockencoding-robin-warmrobinfourslott3cost-betterthan-paperseven). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFourSlotT3Cost_betterThan_figure4" (lean := "QuantumBlockEncoding.Robin.warmRobinFourSlotT3Cost_betterThan_figure4")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin four slot t 3 cost better than figure 4”; the hypotheses and conclusion in the code panel fix its exact scope. Under the fixed exact primitive convention, the XOR four-slot route uses 106 gates while the fixed-N8 Figure-4 realization uses 881.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Under the fixed exact primitive convention, the XOR four-slot route uses 106 gates while the fixed-N8 Figure-4 realization uses 881. This theorem does not assert optimality outside that declared compiler and score convention.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/T3ResourceComparison.lean:66](../../../../library/modules/robin-t3resourcecomparison/#decl-quantumblockencoding-robin-warmrobinfourslott3cost-betterthan-figure4). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinBestVerified" (lean := "QuantumBlockEncoding.Robin.warmRobinBestVerified")
*Plain-English reading.* This abbreviation gives a shorter name to the type or expression used for “warm robin best verified”. The best fully verified Robin candidate under the frozen T3 comparison.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The best fully verified Robin candidate under the frozen T3 comparison.

*Declaration kind.* abbrev.

Source: [QuantumBlockEncoding/Robin/T3ResourceComparison.lean:78](../../../../library/modules/robin-t3resourcecomparison/#decl-quantumblockencoding-robin-warmrobinbestverified). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.paperLevelWinnerCertified" (lean := "QuantumBlockEncoding.Robin.paperLevelWinnerCertified")
*Plain-English reading.* This definition gives the library's named construction or computation for “paper level winner certified”. Machine-readable publication guard, enabled only after both source-side same-tier comparisons have compiled.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Machine-readable publication guard, enabled only after both source-side same-tier comparisons have compiled.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/T3ResourceComparison.lean:84](../../../../library/modules/robin-t3resourcecomparison/#decl-quantumblockencoding-robin-paperlevelwinnercertified). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinPublicationState_consistent" (lean := "QuantumBlockEncoding.Robin.warmRobinPublicationState_consistent")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin publication state consistent”; the hypotheses and conclusion in the code panel fix its exact scope. Publication guard tying the certified fixed-instance winner to empty fixed-N8 source and primitive-obligation lists.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Publication guard tying the certified fixed-instance winner to empty fixed-N8 source and primitive-obligation lists.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/T3ResourceComparison.lean:88](../../../../library/modules/robin-t3resourcecomparison/#decl-quantumblockencoding-robin-warmrobinpublicationstate-consistent). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/Robin/WeightedPermutation.lean

15 explicit public declarations, in source order.

:::definition "QuantumBlockEncoding.Robin.warmRobinFiveShiftPerm" (lean := "QuantumBlockEncoding.Robin.warmRobinFiveShiftPerm")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin five shift perm”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/WeightedPermutation.lean:10](../../../../library/modules/robin-weightedpermutation/#decl-quantumblockencoding-robin-warmrobinfiveshiftperm). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFiveShiftInverse" (lean := "QuantumBlockEncoding.Robin.warmRobinFiveShiftInverse")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin five shift inverse”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/WeightedPermutation.lean:19](../../../../library/modules/robin-weightedpermutation/#decl-quantumblockencoding-robin-warmrobinfiveshiftinverse). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFiveShiftWeight" (lean := "QuantumBlockEncoding.Robin.warmRobinFiveShiftWeight")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin five shift weight”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/WeightedPermutation.lean:28](../../../../library/modules/robin-weightedpermutation/#decl-quantumblockencoding-robin-warmrobinfiveshiftweight). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFiveShift_leftInverse" (lean := "QuantumBlockEncoding.Robin.warmRobinFiveShift_leftInverse")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin five shift left inverse”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/WeightedPermutation.lean:36](../../../../library/modules/robin-weightedpermutation/#decl-quantumblockencoding-robin-warmrobinfiveshift-leftinverse). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFiveShift_rightInverse" (lean := "QuantumBlockEncoding.Robin.warmRobinFiveShift_rightInverse")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin five shift right inverse”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/WeightedPermutation.lean:40](../../../../library/modules/robin-weightedpermutation/#decl-quantumblockencoding-robin-warmrobinfiveshift-rightinverse). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFiveShiftPerm_bijective" (lean := "QuantumBlockEncoding.Robin.warmRobinFiveShiftPerm_bijective")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin five shift perm bijective”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/WeightedPermutation.lean:44](../../../../library/modules/robin-weightedpermutation/#decl-quantumblockencoding-robin-warmrobinfiveshiftperm-bijective). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFiveShiftDecomposition" (lean := "QuantumBlockEncoding.Robin.warmRobinFiveShiftDecomposition")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin five shift decomposition”; the hypotheses and conclusion in the code panel fix its exact scope. Exact 64-entry five-shift decomposition, with columns mapped to rows.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Exact 64-entry five-shift decomposition, with columns mapped to rows.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/WeightedPermutation.lean:55](../../../../library/modules/robin-weightedpermutation/#decl-quantumblockencoding-robin-warmrobinfiveshiftdecomposition). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinFiveShiftAmplitude" (lean := "QuantumBlockEncoding.Robin.warmRobinFiveShiftAmplitude")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin five shift amplitude”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/WeightedPermutation.lean:63](../../../../library/modules/robin-weightedpermutation/#decl-quantumblockencoding-robin-warmrobinfiveshiftamplitude). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinFiveShiftAmplitude_bounded" (lean := "QuantumBlockEncoding.Robin.warmRobinFiveShiftAmplitude_bounded")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin five shift amplitude bounded”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/WeightedPermutation.lean:66](../../../../library/modules/robin-weightedpermutation/#decl-quantumblockencoding-robin-warmrobinfiveshiftamplitude-bounded). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinEightSlotPerm" (lean := "QuantumBlockEncoding.Robin.warmRobinEightSlotPerm")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin eight slot perm”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/WeightedPermutation.lean:70](../../../../library/modules/robin-weightedpermutation/#decl-quantumblockencoding-robin-warmrobineightslotperm). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinEightSlotWeight" (lean := "QuantumBlockEncoding.Robin.warmRobinEightSlotWeight")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin eight slot weight”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/WeightedPermutation.lean:81](../../../../library/modules/robin-weightedpermutation/#decl-quantumblockencoding-robin-warmrobineightslotweight). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinEightSlotDecomposition" (lean := "QuantumBlockEncoding.Robin.warmRobinEightSlotDecomposition")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin eight slot decomposition”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/WeightedPermutation.lean:92](../../../../library/modules/robin-weightedpermutation/#decl-quantumblockencoding-robin-warmrobineightslotdecomposition). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinEightSlotPerm_bijective" (lean := "QuantumBlockEncoding.Robin.warmRobinEightSlotPerm_bijective")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin eight slot perm bijective”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/WeightedPermutation.lean:100](../../../../library/modules/robin-weightedpermutation/#decl-quantumblockencoding-robin-warmrobineightslotperm-bijective). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.Robin.warmRobinEightSlotAmplitude" (lean := "QuantumBlockEncoding.Robin.warmRobinEightSlotAmplitude")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin eight slot amplitude”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/Robin/WeightedPermutation.lean:104](../../../../library/modules/robin-weightedpermutation/#decl-quantumblockencoding-robin-warmrobineightslotamplitude). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Robin.warmRobinEightSlotAmplitude_bounded" (lean := "QuantumBlockEncoding.Robin.warmRobinEightSlotAmplitude_bounded")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin eight slot amplitude bounded”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/Robin/WeightedPermutation.lean:107](../../../../library/modules/robin-weightedpermutation/#decl-quantumblockencoding-robin-warmrobineightslotamplitude-bounded). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/RobinEvolution.lean

17 explicit public declarations, in source order.

:::definition "QuantumBlockEncoding.RobinEvolution.warmRobinTarget" (lean := "QuantumBlockEncoding.RobinEvolution.warmRobinTarget")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin target”. The fixed eight-dimensional homogeneous-Robin benchmark matrix.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The fixed eight-dimensional homogeneous-Robin benchmark matrix.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/RobinEvolution.lean:14](../../../../library/modules/robinevolution/#decl-quantumblockencoding-robinevolution-warmrobintarget). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.RobinEvolution.warmRobinNormalizer" (lean := "QuantumBlockEncoding.RobinEvolution.warmRobinNormalizer")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin normalizer”. Exact normalizer frozen by the warm/cold comparison contract.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Exact normalizer frozen by the warm/cold comparison contract.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/RobinEvolution.lean:53](../../../../library/modules/robinevolution/#decl-quantumblockencoding-robinevolution-warmrobinnormalizer). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.RobinEvolution.warmRobinCleanSignalIndex" (lean := "QuantumBlockEncoding.RobinEvolution.warmRobinCleanSignalIndex")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin clean signal index”. The clean signal basis index is zero.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The clean signal basis index is zero.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/RobinEvolution.lean:56](../../../../library/modules/robinevolution/#decl-quantumblockencoding-robinevolution-warmrobincleansignalindex). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.RobinEvolution.warmRobinSignalFirstIndex" (lean := "QuantumBlockEncoding.RobinEvolution.warmRobinSignalFirstIndex")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin signal first index”. Signal-first flattening of a signal index and an eight-dimensional system index.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Signal-first flattening of a signal index and an eight-dimensional system index.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/RobinEvolution.lean:59](../../../../library/modules/robinevolution/#decl-quantumblockencoding-robinevolution-warmrobinsignalfirstindex). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.RobinEvolution.warmRobinTarget_eq_eval_robinDerivativeMatrix" (lean := "QuantumBlockEncoding.RobinEvolution.warmRobinTarget_eq_eval_robinDerivativeMatrix")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin target eq eval robin derivative matrix”; the hypotheses and conclusion in the code panel fix its exact scope. Evaluating the symbolic Robin stencil at homogeneous boundary data gives the fixed rational benchmark entrywise.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Evaluating the symbolic Robin stencil at homogeneous boundary data gives the fixed rational benchmark entrywise.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/RobinEvolution.lean:64](../../../../library/modules/robinevolution/#decl-quantumblockencoding-robinevolution-warmrobintarget-eq-eval-robinderivativematrix). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.RobinEvolution.warmRobinParameters" (lean := "QuantumBlockEncoding.RobinEvolution.warmRobinParameters")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin parameters”. Fixed paper-seeded parameters for the eight-dimensional warm instance.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Fixed paper-seeded parameters for the eight-dimensional warm instance.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/RobinEvolution.lean:80](../../../../library/modules/robinevolution/#decl-quantumblockencoding-robinevolution-warmrobinparameters). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.RobinEvolution.warmRobinSourceLayout" (lean := "QuantumBlockEncoding.RobinEvolution.warmRobinSourceLayout")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin source layout”. The theorem-level register layout specialized to the warm instance.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The theorem-level register layout specialized to the warm instance.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/RobinEvolution.lean:84](../../../../library/modules/robinevolution/#decl-quantumblockencoding-robinevolution-warmrobinsourcelayout). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.RobinEvolution.warmRobinVisiblePartition" (lean := "QuantumBlockEncoding.RobinEvolution.warmRobinVisiblePartition")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin visible partition”. The visible source register partition specialized to the warm instance.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The visible source register partition specialized to the warm instance.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/RobinEvolution.lean:88](../../../../library/modules/robinevolution/#decl-quantumblockencoding-robinevolution-warmrobinvisiblepartition). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.RobinEvolution.warmRobinSourceCircuit" (lean := "QuantumBlockEncoding.RobinEvolution.warmRobinSourceCircuit")
*Plain-English reading.* This definition gives the library's named construction or computation for “warm robin source circuit”. The source-ordered ten-label paper transcript for the warm instance.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source-ordered ten-label paper transcript for the warm instance. The register SWAP remains a label here; executable semantics live in 'Robin.SourceBaseline.warmRobinRegisterSwapCircuit'.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/RobinEvolution.lean:94](../../../../library/modules/robinevolution/#decl-quantumblockencoding-robinevolution-warmrobinsourcecircuit). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.RobinEvolution.warmRobinSourceCircuit_gateList" (lean := "QuantumBlockEncoding.RobinEvolution.warmRobinSourceCircuit_gateList")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin source circuit gate list”; the hypotheses and conclusion in the code panel fix its exact scope. The warm adapter preserves the exact source order of all ten blocks.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The warm adapter preserves the exact source order of all ten blocks.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/RobinEvolution.lean:108](../../../../library/modules/robinevolution/#decl-quantumblockencoding-robinevolution-warmrobinsourcecircuit-gatelist). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.RobinEvolution.warmRobinSourceCircuit_length" (lean := "QuantumBlockEncoding.RobinEvolution.warmRobinSourceCircuit_length")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin source circuit length”; the hypotheses and conclusion in the code panel fix its exact scope. The source-facing warm transcript contains exactly ten blocks.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The source-facing warm transcript contains exactly ten blocks.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/RobinEvolution.lean:124](../../../../library/modules/robinevolution/#decl-quantumblockencoding-robinevolution-warmrobinsourcecircuit-length). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.RobinEvolution.warmRobinParameters_spec" (lean := "QuantumBlockEncoding.RobinEvolution.warmRobinParameters_spec")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin parameters spec”; the hypotheses and conclusion in the code panel fix its exact scope. Concrete fields of the fixed warm parameter adapter.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* Concrete fields of the fixed warm parameter adapter.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/RobinEvolution.lean:130](../../../../library/modules/robinevolution/#decl-quantumblockencoding-robinevolution-warmrobinparameters-spec). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.RobinEvolution.warmRobinSourceLayout_spec" (lean := "QuantumBlockEncoding.RobinEvolution.warmRobinSourceLayout_spec")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin source layout spec”; the hypotheses and conclusion in the code panel fix its exact scope. The theorem layout is '(system, signal, pure ancilla) = (3, 9, 6)'.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The theorem layout is '(system, signal, pure ancilla) = (3, 9, 6)'.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/RobinEvolution.lean:138](../../../../library/modules/robinevolution/#decl-quantumblockencoding-robinevolution-warmrobinsourcelayout-spec). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.RobinEvolution.warmRobinVisiblePartition_spec" (lean := "QuantumBlockEncoding.RobinEvolution.warmRobinVisiblePartition_spec")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin visible partition spec”; the hypotheses and conclusion in the code panel fix its exact scope. The visible source partition has widths '(5, 1, 3, 0, 3, 1)'.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The visible source partition has widths '(5, 1, 3, 0, 3, 1)'.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/RobinEvolution.lean:145](../../../../library/modules/robinevolution/#decl-quantumblockencoding-robinevolution-warmrobinvisiblepartition-spec). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.RobinEvolution.warmRobinTotalQubits_eq" (lean := "QuantumBlockEncoding.RobinEvolution.warmRobinTotalQubits_eq")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin total qubits eq”; the hypotheses and conclusion in the code panel fix its exact scope. The visible source register partition occupies thirteen qubits.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The visible source register partition occupies thirteen qubits.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/RobinEvolution.lean:155](../../../../library/modules/robinevolution/#decl-quantumblockencoding-robinevolution-warmrobintotalqubits-eq). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.RobinEvolution.warmRobinEffectiveSignalQubits_eq" (lean := "QuantumBlockEncoding.RobinEvolution.warmRobinEffectiveSignalQubits_eq")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin effective signal qubits eq”; the hypotheses and conclusion in the code panel fix its exact scope. The clean projection covers all ten non-system wires.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The clean projection covers all ten non-system wires.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/RobinEvolution.lean:160](../../../../library/modules/robinevolution/#decl-quantumblockencoding-robinevolution-warmrobineffectivesignalqubits-eq). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.RobinEvolution.warmRobinIndicatorCertificate" (lean := "QuantumBlockEncoding.RobinEvolution.warmRobinIndicatorCertificate")
*Plain-English reading.* Lean checks the proposition indexed as “warm robin indicator certificate”; the hypotheses and conclusion in the code panel fix its exact scope. The fixed warm indicator and its dagger form one self-inverse permutation pair.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Paper-facing backend models and concrete Robin-boundary example artifacts.

*Technical source note.* The fixed warm indicator and its dagger form one self-inverse permutation pair.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/RobinEvolution.lean:166](../../../../library/modules/robinevolution/#decl-quantumblockencoding-robinevolution-warmrobinindicatorcertificate). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::
