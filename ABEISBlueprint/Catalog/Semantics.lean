import QuantumBlockEncoding
import Verso
import VersoManual
import VersoBlueprint

open Verso.Genre
open Verso.Genre.Manual
open Informal

set_option linter.hashCommand false
set_option linter.style.longLine false
set_option verso.blueprint.externalCode.strictResolve true

#doc (Manual) "Declaration catalog: Semantics" =>
%%%
file := "catalog-semantics"
%%%

This chapter is generated from the Lean source. Every node denotes one explicit public
declaration, and every Lean link is checked during the Blueprint build. Definitions appear
in source order before later results whenever the source module does so.

Reader orientation: Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope. Each card separates an accessible
reading cue from formal status, the source docstring, and the authoritative Lean panel.
The standalone Library Explorer adds full-text search and filters across every chapter.

# QuantumBlockEncoding/AdjacentGivens.lean

62 explicit public declarations, in source order.

:::definition "QuantumBlockEncoding.AdjacentGivens.rotateRows" (lean := "QuantumBlockEncoding.AdjacentGivens.rotateRows")
*Plain-English reading.* This definition gives the library's named construction or computation for “rotate rows”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/AdjacentGivens.lean:20](../../../../library/modules/adjacentgivens/#decl-quantumblockencoding-adjacentgivens-rotaterows). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.AdjacentGivens.planeMatrix" (lean := "QuantumBlockEncoding.AdjacentGivens.planeMatrix")
*Plain-English reading.* This definition gives the library's named construction or computation for “plane matrix”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/AdjacentGivens.lean:27](../../../../library/modules/adjacentgivens/#decl-quantumblockencoding-adjacentgivens-planematrix). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.AdjacentGivens.planeMatrix_mul" (lean := "QuantumBlockEncoding.AdjacentGivens.planeMatrix_mul")
*Plain-English reading.* Lean checks the proposition indexed as “plane matrix mul”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/AdjacentGivens.lean:30](../../../../library/modules/adjacentgivens/#decl-quantumblockencoding-adjacentgivens-planematrix-mul). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.AdjacentGivens.rotateRows_inverse" (lean := "QuantumBlockEncoding.AdjacentGivens.rotateRows_inverse")
*Plain-English reading.* Lean checks the proposition indexed as “rotate rows inverse”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/AdjacentGivens.lean:42](../../../../library/modules/adjacentgivens/#decl-quantumblockencoding-adjacentgivens-rotaterows-inverse). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.AdjacentGivens.planeMatrix_inverse" (lean := "QuantumBlockEncoding.AdjacentGivens.planeMatrix_inverse")
*Plain-English reading.* Lean checks the proposition indexed as “plane matrix inverse”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/AdjacentGivens.lean:57](../../../../library/modules/adjacentgivens/#decl-quantumblockencoding-adjacentgivens-planematrix-inverse). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.AdjacentGivens.planeMatrix_transpose" (lean := "QuantumBlockEncoding.AdjacentGivens.planeMatrix_transpose")
*Plain-English reading.* Lean checks the proposition indexed as “plane matrix transpose”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/AdjacentGivens.lean:62](../../../../library/modules/adjacentgivens/#decl-quantumblockencoding-adjacentgivens-planematrix-transpose). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.AdjacentGivens.planeMatrix_orthogonal" (lean := "QuantumBlockEncoding.AdjacentGivens.planeMatrix_orthogonal")
*Plain-English reading.* Lean checks the proposition indexed as “plane matrix orthogonal”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/AdjacentGivens.lean:70](../../../../library/modules/adjacentgivens/#decl-quantumblockencoding-adjacentgivens-planematrix-orthogonal). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.AdjacentGivens.rotateRows_preserves_orthogonal" (lean := "QuantumBlockEncoding.AdjacentGivens.rotateRows_preserves_orthogonal")
*Plain-English reading.* Lean checks the proposition indexed as “rotate rows preserves orthogonal”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/AdjacentGivens.lean:74](../../../../library/modules/adjacentgivens/#decl-quantumblockencoding-adjacentgivens-rotaterows-preserves-orthogonal). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.AdjacentGivens.splitAngle_real_firstColumn" (lean := "QuantumBlockEncoding.AdjacentGivens.splitAngle_real_firstColumn")
*Plain-English reading.* Lean checks the proposition indexed as “split angle real first column”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/AdjacentGivens.lean:84](../../../../library/modules/adjacentgivens/#decl-quantumblockencoding-adjacentgivens-splitangle-real-firstcolumn). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.AdjacentGivens.eliminationAngle" (lean := "QuantumBlockEncoding.AdjacentGivens.eliminationAngle")
*Plain-English reading.* This definition gives the library's named construction or computation for “elimination angle”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/AdjacentGivens.lean:99](../../../../library/modules/adjacentgivens/#decl-quantumblockencoding-adjacentgivens-eliminationangle). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.AdjacentGivens.eliminationAngle_zero" (lean := "QuantumBlockEncoding.AdjacentGivens.eliminationAngle_zero")
*Plain-English reading.* Lean checks the proposition indexed as “elimination angle zero”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/AdjacentGivens.lean:101](../../../../library/modules/adjacentgivens/#decl-quantumblockencoding-adjacentgivens-eliminationangle-zero). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.AdjacentGivens.eliminate_pair" (lean := "QuantumBlockEncoding.AdjacentGivens.eliminate_pair")
*Plain-English reading.* Lean checks the proposition indexed as “eliminate pair”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/AdjacentGivens.lean:104](../../../../library/modules/adjacentgivens/#decl-quantumblockencoding-adjacentgivens-eliminate-pair). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.AdjacentGivens.pairNorm_eq_zero_iff" (lean := "QuantumBlockEncoding.AdjacentGivens.pairNorm_eq_zero_iff")
*Plain-English reading.* Lean checks the proposition indexed as “pair norm eq zero iff”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/AdjacentGivens.lean:121](../../../../library/modules/adjacentgivens/#decl-quantumblockencoding-adjacentgivens-pairnorm-eq-zero-iff). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.AdjacentGivens.pairNorm_pos_of_nonzero" (lean := "QuantumBlockEncoding.AdjacentGivens.pairNorm_pos_of_nonzero")
*Plain-English reading.* Lean checks the proposition indexed as “pair norm pos of nonzero”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/AdjacentGivens.lean:130](../../../../library/modules/adjacentgivens/#decl-quantumblockencoding-adjacentgivens-pairnorm-pos-of-nonzero). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.AdjacentGivens.eliminateEntry" (lean := "QuantumBlockEncoding.AdjacentGivens.eliminateEntry")
*Plain-English reading.* This definition gives the library's named construction or computation for “eliminate entry”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/AdjacentGivens.lean:141](../../../../library/modules/adjacentgivens/#decl-quantumblockencoding-adjacentgivens-eliminateentry). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.AdjacentGivens.eliminateEntry_pivot" (lean := "QuantumBlockEncoding.AdjacentGivens.eliminateEntry_pivot")
*Plain-English reading.* Lean checks the proposition indexed as “eliminate entry pivot”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/AdjacentGivens.lean:145](../../../../library/modules/adjacentgivens/#decl-quantumblockencoding-adjacentgivens-eliminateentry-pivot). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.AdjacentGivens.eliminateEntry_unchanged" (lean := "QuantumBlockEncoding.AdjacentGivens.eliminateEntry_unchanged")
*Plain-English reading.* Lean checks the proposition indexed as “eliminate entry unchanged”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/AdjacentGivens.lean:151](../../../../library/modules/adjacentgivens/#decl-quantumblockencoding-adjacentgivens-eliminateentry-unchanged). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.AdjacentGivens.eliminateEntry_preserves_zero_column" (lean := "QuantumBlockEncoding.AdjacentGivens.eliminateEntry_preserves_zero_column")
*Plain-English reading.* Lean checks the proposition indexed as “eliminate entry preserves zero column”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/AdjacentGivens.lean:156](../../../../library/modules/adjacentgivens/#decl-quantumblockencoding-adjacentgivens-eliminateentry-preserves-zero-column). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.AdjacentGivens.eliminateEntry_preserves_orthogonal" (lean := "QuantumBlockEncoding.AdjacentGivens.eliminateEntry_preserves_orthogonal")
*Plain-English reading.* Lean checks the proposition indexed as “eliminate entry preserves orthogonal”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/AdjacentGivens.lean:162](../../../../library/modules/adjacentgivens/#decl-quantumblockencoding-adjacentgivens-eliminateentry-preserves-orthogonal). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.AdjacentGivens.eliminateEntry_zero_pair" (lean := "QuantumBlockEncoding.AdjacentGivens.eliminateEntry_zero_pair")
*Plain-English reading.* Lean checks the proposition indexed as “eliminate entry zero pair”; the hypotheses and conclusion in the code panel fix its exact scope. A zero pair produces an actual identity step, without division by zero.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* A zero pair produces an actual identity step, without division by zero.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/AdjacentGivens.lean:168](../../../../library/modules/adjacentgivens/#decl-quantumblockencoding-adjacentgivens-eliminateentry-zero-pair). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.AdjacentGivens.Step" (lean := "QuantumBlockEncoding.AdjacentGivens.Step")
*Plain-English reading.* This record groups the data and proof fields needed for “step”. A proposition-valued field is a requirement until a constructor supplies it. An actual adjacent-row rotation, carrying the precise ordered support.

*Formal status.* Data contract in the default import surface; proposition-valued fields are obligations, not automatically established facts.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* An actual adjacent-row rotation, carrying the precise ordered support.

*Declaration kind.* structure.

Source: [QuantumBlockEncoding/AdjacentGivens.lean:176](../../../../library/modules/adjacentgivens/#decl-quantumblockencoding-adjacentgivens-step). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.AdjacentGivens.Step.distinct" (lean := "QuantumBlockEncoding.AdjacentGivens.Step.distinct")
*Plain-English reading.* Lean checks the proposition indexed as “distinct”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/AdjacentGivens.lean:182](../../../../library/modules/adjacentgivens/#decl-quantumblockencoding-adjacentgivens-step-distinct). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.AdjacentGivens.Step.matrix" (lean := "QuantumBlockEncoding.AdjacentGivens.Step.matrix")
*Plain-English reading.* This definition gives the library's named construction or computation for “matrix”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/AdjacentGivens.lean:188](../../../../library/modules/adjacentgivens/#decl-quantumblockencoding-adjacentgivens-step-matrix). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.AdjacentGivens.applySteps" (lean := "QuantumBlockEncoding.AdjacentGivens.applySteps")
*Plain-English reading.* This definition gives the library's named construction or computation for “apply steps”. Chronological action: the first listed matrix acts first.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Chronological action: the first listed matrix acts first.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/AdjacentGivens.lean:192](../../../../library/modules/adjacentgivens/#decl-quantumblockencoding-adjacentgivens-applysteps). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.AdjacentGivens.Step.inverse" (lean := "QuantumBlockEncoding.AdjacentGivens.Step.inverse")
*Plain-English reading.* This definition gives the library's named construction or computation for “inverse”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/AdjacentGivens.lean:197](../../../../library/modules/adjacentgivens/#decl-quantumblockencoding-adjacentgivens-step-inverse). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.AdjacentGivens.applySteps_append" (lean := "QuantumBlockEncoding.AdjacentGivens.applySteps_append")
*Plain-English reading.* Lean checks the proposition indexed as “apply steps append”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/AdjacentGivens.lean:200](../../../../library/modules/adjacentgivens/#decl-quantumblockencoding-adjacentgivens-applysteps-append). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.AdjacentGivens.applySteps_reverse_inverse" (lean := "QuantumBlockEncoding.AdjacentGivens.applySteps_reverse_inverse")
*Plain-English reading.* Lean checks the proposition indexed as “apply steps reverse inverse”; the hypotheses and conclusion in the code panel fix its exact scope. Reversing the chronological list and negating every angle exactly undoes it.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Reversing the chronological list and negating every angle exactly undoes it.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/AdjacentGivens.lean:208](../../../../library/modules/adjacentgivens/#decl-quantumblockencoding-adjacentgivens-applysteps-reverse-inverse). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.AdjacentGivens.columnSweep" (lean := "QuantumBlockEncoding.AdjacentGivens.columnSweep")
*Plain-English reading.* This definition gives the library's named construction or computation for “column sweep”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/AdjacentGivens.lean:222](../../../../library/modules/adjacentgivens/#decl-quantumblockencoding-adjacentgivens-columnsweep). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.AdjacentGivens.columnSweepSteps" (lean := "QuantumBlockEncoding.AdjacentGivens.columnSweepSteps")
*Plain-English reading.* This definition gives the library's named construction or computation for “column sweep steps”. The list is computed from the changing matrix, not supplied as a certificate.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The list is computed from the changing matrix, not supplied as a certificate.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/AdjacentGivens.lean:231](../../../../library/modules/adjacentgivens/#decl-quantumblockencoding-adjacentgivens-columnsweepsteps). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.AdjacentGivens.columnSweepSteps_length" (lean := "QuantumBlockEncoding.AdjacentGivens.columnSweepSteps_length")
*Plain-English reading.* Lean checks the proposition indexed as “column sweep steps length”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/AdjacentGivens.lean:241](../../../../library/modules/adjacentgivens/#decl-quantumblockencoding-adjacentgivens-columnsweepsteps-length). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.AdjacentGivens.columnSweepSteps_action" (lean := "QuantumBlockEncoding.AdjacentGivens.columnSweepSteps_action")
*Plain-English reading.* Lean checks the proposition indexed as “column sweep steps action”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/AdjacentGivens.lean:248](../../../../library/modules/adjacentgivens/#decl-quantumblockencoding-adjacentgivens-columnsweepsteps-action). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.AdjacentGivens.columnSweep_exact_recovery" (lean := "QuantumBlockEncoding.AdjacentGivens.columnSweep_exact_recovery")
*Plain-English reading.* Lean checks the proposition indexed as “column sweep exact recovery”; the hypotheses and conclusion in the code panel fix its exact scope. The original matrix is recovered from the computed residual.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The original matrix is recovered from the computed residual. No identity or full-decomposition assumption is hidden in this statement.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/AdjacentGivens.lean:259](../../../../library/modules/adjacentgivens/#decl-quantumblockencoding-adjacentgivens-columnsweep-exact-recovery). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.AdjacentGivens.columnSweep_outside" (lean := "QuantumBlockEncoding.AdjacentGivens.columnSweep_outside")
*Plain-English reading.* Lean checks the proposition indexed as “column sweep outside”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/AdjacentGivens.lean:265](../../../../library/modules/adjacentgivens/#decl-quantumblockencoding-adjacentgivens-columnsweep-outside). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.AdjacentGivens.columnSweep_zeroed" (lean := "QuantumBlockEncoding.AdjacentGivens.columnSweep_zeroed")
*Plain-English reading.* Lean checks the proposition indexed as “column sweep zeroed”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/AdjacentGivens.lean:283](../../../../library/modules/adjacentgivens/#decl-quantumblockencoding-adjacentgivens-columnsweep-zeroed). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.AdjacentGivens.columnSweep_preserves_zero_column" (lean := "QuantumBlockEncoding.AdjacentGivens.columnSweep_preserves_zero_column")
*Plain-English reading.* Lean checks the proposition indexed as “column sweep preserves zero column”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/AdjacentGivens.lean:305](../../../../library/modules/adjacentgivens/#decl-quantumblockencoding-adjacentgivens-columnsweep-preserves-zero-column). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.AdjacentGivens.columnSweep_preserves_orthogonal" (lean := "QuantumBlockEncoding.AdjacentGivens.columnSweep_preserves_orthogonal")
*Plain-English reading.* Lean checks the proposition indexed as “column sweep preserves orthogonal”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/AdjacentGivens.lean:325](../../../../library/modules/adjacentgivens/#decl-quantumblockencoding-adjacentgivens-columnsweep-preserves-orthogonal). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.AdjacentGivens.det_two_row_mix" (lean := "QuantumBlockEncoding.AdjacentGivens.det_two_row_mix")
*Plain-English reading.* Lean checks the proposition indexed as “det two row mix”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/AdjacentGivens.lean:341](../../../../library/modules/adjacentgivens/#decl-quantumblockencoding-adjacentgivens-det-two-row-mix). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.AdjacentGivens.rotateRows_det" (lean := "QuantumBlockEncoding.AdjacentGivens.rotateRows_det")
*Plain-English reading.* Lean checks the proposition indexed as “rotate rows det”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/AdjacentGivens.lean:367](../../../../library/modules/adjacentgivens/#decl-quantumblockencoding-adjacentgivens-rotaterows-det). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.AdjacentGivens.planeMatrix_det" (lean := "QuantumBlockEncoding.AdjacentGivens.planeMatrix_det")
*Plain-English reading.* Lean checks the proposition indexed as “plane matrix det”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/AdjacentGivens.lean:381](../../../../library/modules/adjacentgivens/#decl-quantumblockencoding-adjacentgivens-planematrix-det). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.AdjacentGivens.columnSweep_preserves_det" (lean := "QuantumBlockEncoding.AdjacentGivens.columnSweep_preserves_det")
*Plain-English reading.* Lean checks the proposition indexed as “column sweep preserves det”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/AdjacentGivens.lean:385](../../../../library/modules/adjacentgivens/#decl-quantumblockencoding-adjacentgivens-columnsweep-preserves-det). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.AdjacentGivens.PrefixIdentity" (lean := "QuantumBlockEncoding.AdjacentGivens.PrefixIdentity")
*Plain-English reading.* This definition gives the library's named construction or computation for “prefix identity”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/AdjacentGivens.lean:400](../../../../library/modules/adjacentgivens/#decl-quantumblockencoding-adjacentgivens-prefixidentity). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.AdjacentGivens.orthogonal_zero_of_fixed_column" (lean := "QuantumBlockEncoding.AdjacentGivens.orthogonal_zero_of_fixed_column")
*Plain-English reading.* Lean checks the proposition indexed as “orthogonal zero of fixed column”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/AdjacentGivens.lean:403](../../../../library/modules/adjacentgivens/#decl-quantumblockencoding-adjacentgivens-orthogonal-zero-of-fixed-column). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.AdjacentGivens.columnSweep_prefix" (lean := "QuantumBlockEncoding.AdjacentGivens.columnSweep_prefix")
*Plain-English reading.* Lean checks the proposition indexed as “column sweep prefix”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/AdjacentGivens.lean:410](../../../../library/modules/adjacentgivens/#decl-quantumblockencoding-adjacentgivens-columnsweep-prefix). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.AdjacentGivens.columnSweep_pivot_nonneg" (lean := "QuantumBlockEncoding.AdjacentGivens.columnSweep_pivot_nonneg")
*Plain-English reading.* Lean checks the proposition indexed as “column sweep pivot nonneg”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/AdjacentGivens.lean:422](../../../../library/modules/adjacentgivens/#decl-quantumblockencoding-adjacentgivens-columnsweep-pivot-nonneg). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.AdjacentGivens.orthogonal_supported_column_sq" (lean := "QuantumBlockEncoding.AdjacentGivens.orthogonal_supported_column_sq")
*Plain-English reading.* Lean checks the proposition indexed as “orthogonal supported column sq”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/AdjacentGivens.lean:441](../../../../library/modules/adjacentgivens/#decl-quantumblockencoding-adjacentgivens-orthogonal-supported-column-sq). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.AdjacentGivens.columnSweep_prefix_succ" (lean := "QuantumBlockEncoding.AdjacentGivens.columnSweep_prefix_succ")
*Plain-English reading.* Lean checks the proposition indexed as “column sweep prefix succ”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/AdjacentGivens.lean:457](../../../../library/modules/adjacentgivens/#decl-quantumblockencoding-adjacentgivens-columnsweep-prefix-succ). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.AdjacentGivens.matrix_eq_one_of_full_prefix" (lean := "QuantumBlockEncoding.AdjacentGivens.matrix_eq_one_of_full_prefix")
*Plain-English reading.* Lean checks the proposition indexed as “matrix eq one of full prefix”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/AdjacentGivens.lean:485](../../../../library/modules/adjacentgivens/#decl-quantumblockencoding-adjacentgivens-matrix-eq-one-of-full-prefix). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.AdjacentGivens.matrix_eq_one_of_last_prefix" (lean := "QuantumBlockEncoding.AdjacentGivens.matrix_eq_one_of_last_prefix")
*Plain-English reading.* Lean checks the proposition indexed as “matrix eq one of last prefix”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/AdjacentGivens.lean:490](../../../../library/modules/adjacentgivens/#decl-quantumblockencoding-adjacentgivens-matrix-eq-one-of-last-prefix). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.AdjacentGivens.fullSweep" (lean := "QuantumBlockEncoding.AdjacentGivens.fullSweep")
*Plain-English reading.* This definition gives the library's named construction or computation for “full sweep”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/AdjacentGivens.lean:527](../../../../library/modules/adjacentgivens/#decl-quantumblockencoding-adjacentgivens-fullsweep). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.AdjacentGivens.fullSweepSteps" (lean := "QuantumBlockEncoding.AdjacentGivens.fullSweepSteps")
*Plain-English reading.* This definition gives the library's named construction or computation for “full sweep steps”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/AdjacentGivens.lean:535](../../../../library/modules/adjacentgivens/#decl-quantumblockencoding-adjacentgivens-fullsweepsteps). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.AdjacentGivens.fullSweepSteps_action" (lean := "QuantumBlockEncoding.AdjacentGivens.fullSweepSteps_action")
*Plain-English reading.* Lean checks the proposition indexed as “full sweep steps action”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/AdjacentGivens.lean:544](../../../../library/modules/adjacentgivens/#decl-quantumblockencoding-adjacentgivens-fullsweepsteps-action). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.AdjacentGivens.fullSweepSteps_length_twice" (lean := "QuantumBlockEncoding.AdjacentGivens.fullSweepSteps_length_twice")
*Plain-English reading.* Lean checks the proposition indexed as “full sweep steps length twice”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/AdjacentGivens.lean:556](../../../../library/modules/adjacentgivens/#decl-quantumblockencoding-adjacentgivens-fullsweepsteps-length-twice). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.AdjacentGivens.fullSweepSteps_length" (lean := "QuantumBlockEncoding.AdjacentGivens.fullSweepSteps_length")
*Plain-English reading.* Lean checks the proposition indexed as “full sweep steps length”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/AdjacentGivens.lean:570](../../../../library/modules/adjacentgivens/#decl-quantumblockencoding-adjacentgivens-fullsweepsteps-length). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.AdjacentGivens.fullSweep_eq_one" (lean := "QuantumBlockEncoding.AdjacentGivens.fullSweep_eq_one")
*Plain-English reading.* Lean checks the proposition indexed as “full sweep eq one”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/AdjacentGivens.lean:576](../../../../library/modules/adjacentgivens/#decl-quantumblockencoding-adjacentgivens-fullsweep-eq-one). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.AdjacentGivens.decomposeSO" (lean := "QuantumBlockEncoding.AdjacentGivens.decomposeSO")
*Plain-English reading.* This definition gives the library's named construction or computation for “decompose so”. A computed finite list of adjacent RY planes in chronological circuit order.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* A computed finite list of adjacent RY planes in chronological circuit order.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/AdjacentGivens.lean:597](../../../../library/modules/adjacentgivens/#decl-quantumblockencoding-adjacentgivens-decomposeso). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.AdjacentGivens.decomposeSO_action" (lean := "QuantumBlockEncoding.AdjacentGivens.decomposeSO_action")
*Plain-English reading.* Lean checks the proposition indexed as “decompose so action”; the hypotheses and conclusion in the code panel fix its exact scope. Every real determinant-one orthogonal matrix is exactly the action of the constructed adjacent-plane list.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Every real determinant-one orthogonal matrix is exactly the action of the constructed adjacent-plane list. The final identity is proved, not supplied.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/AdjacentGivens.lean:602](../../../../library/modules/adjacentgivens/#decl-quantumblockencoding-adjacentgivens-decomposeso-action). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.AdjacentGivens.decomposeSO_length" (lean := "QuantumBlockEncoding.AdjacentGivens.decomposeSO_length")
*Plain-English reading.* Lean checks the proposition indexed as “decompose so length”; the hypotheses and conclusion in the code panel fix its exact scope. Including harmless identity rotations at zero pivots gives an exact count.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Including harmless identity rotations at zero pivots gives an exact count.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/AdjacentGivens.lean:611](../../../../library/modules/adjacentgivens/#decl-quantumblockencoding-adjacentgivens-decomposeso-length). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.AdjacentGivens.planeMatrix_selected_entry" (lean := "QuantumBlockEncoding.AdjacentGivens.planeMatrix_selected_entry")
*Plain-English reading.* Lean checks the proposition indexed as “plane matrix selected entry”; the hypotheses and conclusion in the code panel fix its exact scope. The ordered two-dimensional block uses exactly the selected-RY convention.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The ordered two-dimensional block uses exactly the selected-RY convention.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/AdjacentGivens.lean:617](../../../../library/modules/adjacentgivens/#decl-quantumblockencoding-adjacentgivens-planematrix-selected-entry). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.AdjacentGivens.planeMatrix_fixed_column" (lean := "QuantumBlockEncoding.AdjacentGivens.planeMatrix_fixed_column")
*Plain-English reading.* Lean checks the proposition indexed as “plane matrix fixed column”; the hypotheses and conclusion in the code panel fix its exact scope. Every basis vector outside the selected pair is fixed, including its sign.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Every basis vector outside the selected pair is fixed, including its sign.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/AdjacentGivens.lean:626](../../../../library/modules/adjacentgivens/#decl-quantumblockencoding-adjacentgivens-planematrix-fixed-column). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.AdjacentGivens.stepsMatrix" (lean := "QuantumBlockEncoding.AdjacentGivens.stepsMatrix")
*Plain-English reading.* This definition gives the library's named construction or computation for “steps matrix”. Chronological matrix product, matching the circuit list convention.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Chronological matrix product, matching the circuit list convention.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/AdjacentGivens.lean:633](../../../../library/modules/adjacentgivens/#decl-quantumblockencoding-adjacentgivens-stepsmatrix). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.AdjacentGivens.applySteps_eq_matrix_mul" (lean := "QuantumBlockEncoding.AdjacentGivens.applySteps_eq_matrix_mul")
*Plain-English reading.* Lean checks the proposition indexed as “apply steps eq matrix mul”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/AdjacentGivens.lean:637](../../../../library/modules/adjacentgivens/#decl-quantumblockencoding-adjacentgivens-applysteps-eq-matrix-mul). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.AdjacentGivens.decomposeSO_matrix" (lean := "QuantumBlockEncoding.AdjacentGivens.decomposeSO_matrix")
*Plain-English reading.* Lean checks the proposition indexed as “decompose so matrix”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/AdjacentGivens.lean:644](../../../../library/modules/adjacentgivens/#decl-quantumblockencoding-adjacentgivens-decomposeso-matrix). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/CircuitSemantics.lean

41 explicit public declarations, in source order.

:::definition "QuantumBlockEncoding.qubitDim" (lean := "QuantumBlockEncoding.qubitDim")
*Plain-English reading.* This definition gives the library's named construction or computation for “qubit dim”. A finite-dimensional basis size for an 'n'-qubit register.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* A finite-dimensional basis size for an 'n'-qubit register.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/CircuitSemantics.lean:16](../../../../library/modules/circuitsemantics/#decl-quantumblockencoding-qubitdim). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.SemanticObligation" (lean := "QuantumBlockEncoding.SemanticObligation")
*Plain-English reading.* This record groups the data and proof fields needed for “semantic obligation”. A proposition-valued field is a requirement until a constructor supplies it. Structured semantic obligation for the matrix layer.

*Formal status.* Data contract in the default import surface; proposition-valued fields are obligations, not automatically established facts.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Structured semantic obligation for the matrix layer. This mirrors 'GHL2025.ObligationRecord' without importing 'GHL2025', so the semantics backend can stay below paper-specific files in the import graph.

*Declaration kind.* structure.

Source: [QuantumBlockEncoding/CircuitSemantics.lean:25](../../../../library/modules/circuitsemantics/#decl-quantumblockencoding-semanticobligation). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GateMatrix" (lean := "QuantumBlockEncoding.GateMatrix")
*Plain-English reading.* This record groups the data and proof fields needed for “gate matrix”. A proposition-valued field is a requirement until a constructor supplies it. One gate together with its matrix on the full 'qubits'-qubit Hilbert space.

*Formal status.* Data contract in the default import surface; proposition-valued fields are obligations, not automatically established facts.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* One gate together with its matrix on the full 'qubits'-qubit Hilbert space. The matrix is supplied by a lower-level certificate for the gate family.

*Declaration kind.* structure.

Source: [QuantumBlockEncoding/CircuitSemantics.lean:35](../../../../library/modules/circuitsemantics/#decl-quantumblockencoding-gatematrix). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.gateMatricesMatchCircuit" (lean := "QuantumBlockEncoding.gateMatricesMatchCircuit")
*Plain-English reading.* This definition gives the library's named construction or computation for “gate matrices match circuit”. Check that a list of gate matrices labels exactly the same circuit gates.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Check that a list of gate matrices labels exactly the same circuit gates.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/CircuitSemantics.lean:41](../../../../library/modules/circuitsemantics/#decl-quantumblockencoding-gatematricesmatchcircuit). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.evalGateMatrices" (lean := "QuantumBlockEncoding.evalGateMatrices")
*Plain-English reading.* This definition gives the library's named construction or computation for “eval gate matrices”. Evaluate a list of full-space gate matrices to a circuit matrix.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Evaluate a list of full-space gate matrices to a circuit matrix. The fold uses the usual right-action convention for a circuit list '\[g₁, g₂, ...\]': the resulting matrix is 'g\_k \* ... \* g₂ \* g₁'.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/CircuitSemantics.lean:54](../../../../library/modules/circuitsemantics/#decl-quantumblockencoding-evalgatematrices). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Matrix.evalWith_foldl_add_mul" (lean := "QuantumBlockEncoding.Matrix.evalWith_foldl_add_mul")
*Plain-English reading.* Lean checks the proposition indexed as “eval with foldl add mul”; the hypotheses and conclusion in the code panel fix its exact scope. Evaluate one symbolic matrix-product entry as a concrete finite Rat fold.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Evaluate one symbolic matrix-product entry as a concrete finite Rat fold. The project-local 'Coeff' matrices are syntactic, so a raw 'Matrix.mul' entry does not simplify away zero summands. This lemma moves the finite product entry through 'Coeff.evalWith', where later path-isolation proofs can use ordinary rational arithmetic without expanding the whole symbolic expression.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/CircuitSemantics.lean:71](../../../../library/modules/circuitsemantics/#decl-quantumblockencoding-matrix-evalwith-foldl-add-mul). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Matrix.evalWith_mul_apply" (lean := "QuantumBlockEncoding.Matrix.evalWith_mul_apply")
*Plain-English reading.* Lean checks the proposition indexed as “eval with mul apply”; the hypotheses and conclusion in the code panel fix its exact scope. Evaluate one entry of 'Matrix.mul' by evaluating each path contribution.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Evaluate one entry of 'Matrix.mul' by evaluating each path contribution. This is the local matrix-semantics block needed before a focused Robin seven-gate path proof can avoid syntactic 'Coeff.add' blow-up.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/CircuitSemantics.lean:92](../../../../library/modules/circuitsemantics/#decl-quantumblockencoding-matrix-evalwith-mul-apply). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Matrix.evalWith_mul_eq_zero_of_all_paths_zero" (lean := "QuantumBlockEncoding.Matrix.evalWith_mul_eq_zero_of_all_paths_zero")
*Plain-English reading.* Lean checks the proposition indexed as “eval with mul eq zero of all paths zero”; the hypotheses and conclusion in the code panel fix its exact scope. Evaluate one matrix-product entry as zero when every evaluated path contribution is zero.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Evaluate one matrix-product entry as zero when every evaluated path contribution is zero. This is the zero-support companion to 'evalWith\_mul\_unique\_path'. It lets paper-specific product proofs avoid expanding a large symbolic 'Coeff' fold when they have already isolated gate-local support facts.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/CircuitSemantics.lean:131](../../../../library/modules/circuitsemantics/#decl-quantumblockencoding-matrix-evalwith-mul-eq-zero-of-all-paths-zero). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Matrix.evalWith_mul_unique_path" (lean := "QuantumBlockEncoding.Matrix.evalWith_mul_unique_path")
*Plain-English reading.* Lean checks the proposition indexed as “eval with mul unique path”; the hypotheses and conclusion in the code panel fix its exact scope. Evaluate one matrix-product entry when all evaluated paths except 'k0' vanish.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Evaluate one matrix-product entry when all evaluated paths except 'k0' vanish. This is the reusable path-isolation block for later Robin gamma3 work: a theorem about the seven-gate product can first prove zero-support facts for all unwanted intermediate states, then reduce the evaluated product to the single surviving contribution.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/CircuitSemantics.lean:304](../../../../library/modules/circuitsemantics/#decl-quantumblockencoding-matrix-evalwith-mul-unique-path). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Matrix.evalWith_mul_two_path" (lean := "QuantumBlockEncoding.Matrix.evalWith_mul_two_path")
*Plain-English reading.* Lean checks the proposition indexed as “eval with mul two path”; the hypotheses and conclusion in the code panel fix its exact scope. Evaluate one matrix-product entry when all evaluated paths except 'k0' and 'k1' vanish.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Evaluate one matrix-product entry when all evaluated paths except 'k0' and 'k1' vanish. This is the two-path companion to 'evalWith\_mul\_unique\_path'. A seven-gate product proof can first establish that only two intermediate rows contribute, then reduce the evaluated product to their sum using this theorem.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/CircuitSemantics.lean:328](../../../../library/modules/circuitsemantics/#decl-quantumblockencoding-matrix-evalwith-mul-two-path). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Matrix.evalWith_mul_identity_right_apply" (lean := "QuantumBlockEncoding.Matrix.evalWith_mul_identity_right_apply")
*Plain-English reading.* Lean checks the proposition indexed as “eval with mul identity right apply”; the hypotheses and conclusion in the code panel fix its exact scope. Evaluating a symbolic matrix after multiplying on the right by the identity recovers the evaluated entry.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Evaluating a symbolic matrix after multiplying on the right by the identity recovers the evaluated entry. The statement is evaluation-level, not syntactic: 'Coeff' deliberately stores matrix products as explicit fold expressions, so the raw 'Coeff' term still contains zero summands.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/CircuitSemantics.lean:355](../../../../library/modules/circuitsemantics/#decl-quantumblockencoding-matrix-evalwith-mul-identity-right-apply). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.Matrix.cast_square_apply" (lean := "QuantumBlockEncoding.Matrix.cast_square_apply")
*Plain-English reading.* Lean checks the proposition indexed as “cast square apply”; the hypotheses and conclusion in the code panel fix its exact scope. Entry-level bridge for square matrix casts along a dimension equality.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Entry-level bridge for square matrix casts along a dimension equality. This keeps paper-specific finite-entry proofs from unfolding a large casted matrix when the only content is that the row and column values are unchanged.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/CircuitSemantics.lean:371](../../../../library/modules/circuitsemantics/#decl-quantumblockencoding-matrix-cast-square-apply). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.evalWith_evalGateMatrices_single" (lean := "QuantumBlockEncoding.evalWith_evalGateMatrices_single")
*Plain-English reading.* Lean checks the proposition indexed as “eval with eval gate matrices single”; the hypotheses and conclusion in the code panel fix its exact scope. Evaluation-level single-gate reduction for 'evalGateMatrices'.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Evaluation-level single-gate reduction for 'evalGateMatrices'. This is the entry helper for prepared composite gates: the matrix semantics of a singleton gate list evaluates to the supplied gate matrix entry, even though the underlying symbolic 'Coeff' expression is still a folded multiplication by the identity matrix.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/CircuitSemantics.lean:389](../../../../library/modules/circuitsemantics/#decl-quantumblockencoding-evalwith-evalgatematrices-single). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.CircuitMatrixSemantics" (lean := "QuantumBlockEncoding.CircuitMatrixSemantics")
*Plain-English reading.* This record groups the data and proof fields needed for “circuit matrix semantics”. A proposition-valued field is a requirement until a constructor supplies it. Circuit-level matrix semantics assembled from gate-level matrices.

*Formal status.* Data contract in the default import surface; proposition-valued fields are obligations, not automatically established facts.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Circuit-level matrix semantics assembled from gate-level matrices. This does not certify that individual oracle matrices are correct; it gives the project a stable Lean target for composing those certificates once they exist.

*Declaration kind.* structure.

Source: [QuantumBlockEncoding/CircuitSemantics.lean:404](../../../../library/modules/circuitsemantics/#decl-quantumblockencoding-circuitmatrixsemantics). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.CircuitMatrixSemantics.ofGateMatrices" (lean := "QuantumBlockEncoding.CircuitMatrixSemantics.ofGateMatrices")
*Plain-English reading.* This definition gives the library's named construction or computation for “of gate matrices”. Build circuit semantics directly from aligned gate matrices.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Build circuit semantics directly from aligned gate matrices.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/CircuitSemantics.lean:415](../../../../library/modules/circuitsemantics/#decl-quantumblockencoding-circuitmatrixsemantics-ofgatematrices). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.PreparedCircuitEntryTarget" (lean := "QuantumBlockEncoding.PreparedCircuitEntryTarget")
*Plain-English reading.* This record groups the data and proof fields needed for “prepared circuit entry target”. A proposition-valued field is a requirement until a constructor supplies it. Typed target for relating an active circuit-matrix entry to a prepared composition entry.

*Formal status.* Data contract in the default import surface; proposition-valued fields are obligations, not automatically established facts.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Typed target for relating an active circuit-matrix entry to a prepared composition entry. This is intentionally only an interface. It records the two matrix entries and the exact equality a paper-specific composition backend must prove; it does not assert that the active circuit already contains the prepared blocks.

*Declaration kind.* structure.

Source: [QuantumBlockEncoding/CircuitSemantics.lean:436](../../../../library/modules/circuitsemantics/#decl-quantumblockencoding-preparedcircuitentrytarget). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.PreparedCircuitEntryTarget.entryEqualityStatement" (lean := "QuantumBlockEncoding.PreparedCircuitEntryTarget.entryEqualityStatement")
*Plain-English reading.* This definition gives the library's named construction or computation for “entry equality statement”. The prepared-composition equality required by the target.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The prepared-composition equality required by the target.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/CircuitSemantics.lean:454](../../../../library/modules/circuitsemantics/#decl-quantumblockencoding-preparedcircuitentrytarget-entryequalitystatement). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.PreparedCircuitEntryTarget.matrixEntryEqualityStatement" (lean := "QuantumBlockEncoding.PreparedCircuitEntryTarget.matrixEntryEqualityStatement")
*Plain-English reading.* This definition gives the library's named construction or computation for “matrix entry equality statement”. The same equality stated directly on the backing matrices.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The same equality stated directly on the backing matrices.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/CircuitSemantics.lean:459](../../../../library/modules/circuitsemantics/#decl-quantumblockencoding-preparedcircuitentrytarget-matrixentryequalitystatement). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.PreparedCircuitEntryTarget.entryEqualityStatement_iff_matrixEntryEqualityStatement" (lean := "QuantumBlockEncoding.PreparedCircuitEntryTarget.entryEqualityStatement_iff_matrixEntryEqualityStatement")
*Plain-English reading.* Lean checks the proposition indexed as “entry equality statement iff matrix entry equality statement”; the hypotheses and conclusion in the code panel fix its exact scope. The cached entry equality is equivalent to the backing matrix-entry equality.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The cached entry equality is equivalent to the backing matrix-entry equality. Paper-specific targets can prove whichever side their local backend exposes without changing the semantic obligation being tracked.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/CircuitSemantics.lean:470](../../../../library/modules/circuitsemantics/#decl-quantumblockencoding-preparedcircuitentrytarget-entryequalitystatement-iff-matrixentryequalitystatement). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.BlockExtractionTarget" (lean := "QuantumBlockEncoding.BlockExtractionTarget")
*Plain-English reading.* This record groups the data and proof fields needed for “block extraction target”. A proposition-valued field is a requirement until a constructor supplies it. A paper-level block-extraction target against a concrete circuit matrix.

*Formal status.* Data contract in the default import surface; proposition-valued fields are obligations, not automatically established facts.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* A paper-level block-extraction target against a concrete circuit matrix. The current project can now state the missing equation in matrix terms. The actual block projection from signal/system registers remains a later proof obligation, tracked explicitly by 'blockProjection' and 'blockCorrect'.

*Declaration kind.* structure.

Source: [QuantumBlockEncoding/CircuitSemantics.lean:502](../../../../library/modules/circuitsemantics/#decl-quantumblockencoding-blockextractiontarget). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.blockExtractionBranchContributionSum" (lean := "QuantumBlockEncoding.blockExtractionBranchContributionSum")
*Plain-English reading.* This definition gives the library's named construction or computation for “block extraction branch contribution sum”. Fold a finite family of branch contributions into one projected block entry.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Fold a finite family of branch contributions into one projected block entry. This is deliberately minimal: it provides a typed target for paper-specific projection/summation proofs without assuming commutativity, a ring structure, or a normal form for symbolic coefficients.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/CircuitSemantics.lean:520](../../../../library/modules/circuitsemantics/#decl-quantumblockencoding-blockextractionbranchcontributionsum). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.BlockExtractionBranchContributionTarget" (lean := "QuantumBlockEncoding.BlockExtractionBranchContributionTarget")
*Plain-English reading.* This record groups the data and proof fields needed for “block extraction branch contribution target”. A proposition-valued field is a requirement until a constructor supplies it. Typed interface for decomposing one block-extracted matrix entry into finite branch contributions.

*Formal status.* Data contract in the default import surface; proposition-valued fields are obligations, not automatically established facts.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Typed interface for decomposing one block-extracted matrix entry into finite branch contributions. The interface records the candidate contribution family and the exact block-entry and branch-sum propositions that must be proved. It is not itself a proof that the family is sourced from the backend or that the branch sum equals the block entry; those remain explicit semantic obligations.

*Declaration kind.* structure.

Source: [QuantumBlockEncoding/CircuitSemantics.lean:535](../../../../library/modules/circuitsemantics/#decl-quantumblockencoding-blockextractionbranchcontributiontarget). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.BlockExtractionBranchContributionTarget.selectedBranchStatement" (lean := "QuantumBlockEncoding.BlockExtractionBranchContributionTarget.selectedBranchStatement")
*Plain-English reading.* This definition gives the library's named construction or computation for “selected branch statement”. The selected-branch identity exposed by the target.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The selected-branch identity exposed by the target.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/CircuitSemantics.lean:559](../../../../library/modules/circuitsemantics/#decl-quantumblockencoding-blockextractionbranchcontributiontarget-selectedbranchstatement). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.BlockExtractionBranchContributionTarget.projectionSummationStatement" (lean := "QuantumBlockEncoding.BlockExtractionBranchContributionTarget.projectionSummationStatement")
*Plain-English reading.* This definition gives the library's named construction or computation for “projection summation statement”. The projection/summation theorem still required for the target.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The projection/summation theorem still required for the target.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/CircuitSemantics.lean:569](../../../../library/modules/circuitsemantics/#decl-quantumblockencoding-blockextractionbranchcontributiontarget-projectionsummationstatement). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.BlockExtractionBranchContributionTarget.backendExpansionStatement" (lean := "QuantumBlockEncoding.BlockExtractionBranchContributionTarget.backendExpansionStatement")
*Plain-English reading.* This definition gives the library's named construction or computation for “backend expansion statement”. The backend expansion theorem needed to close 'projectionSummationStatement'.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The backend expansion theorem needed to close 'projectionSummationStatement'. This version is stated directly in terms of the extraction target's block matrix entry and the candidate branch-contribution fold. It is useful as a proof-DAG interface because paper-specific projection backends can target this statement without depending on the record's cached 'blockEntry' and 'branchSum' fields.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/CircuitSemantics.lean:586](../../../../library/modules/circuitsemantics/#decl-quantumblockencoding-blockextractionbranchcontributiontarget-backendexpansionstatement). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.BlockExtractionBranchContributionTarget.selectedBranchStatement_of_eq" (lean := "QuantumBlockEncoding.BlockExtractionBranchContributionTarget.selectedBranchStatement_of_eq")
*Plain-English reading.* Lean checks the proposition indexed as “selected branch statement of eq”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/CircuitSemantics.lean:595](../../../../library/modules/circuitsemantics/#decl-quantumblockencoding-blockextractionbranchcontributiontarget-selectedbranchstatement-of-eq). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.BlockExtractionBranchContributionTarget.projectionSummationStatement_iff_backendExpansionStatement" (lean := "QuantumBlockEncoding.BlockExtractionBranchContributionTarget.projectionSummationStatement_iff_backendExpansionStatement")
*Plain-English reading.* Lean checks the proposition indexed as “projection summation statement iff backend expansion statement”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/CircuitSemantics.lean:603](../../../../library/modules/circuitsemantics/#decl-quantumblockencoding-blockextractionbranchcontributiontarget-projectionsummationstatement-iff-backendexpansionstatement). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.BlockExtractionBranchContributionTarget.projectionSummationStatement_of_backendExpansionStatement" (lean := "QuantumBlockEncoding.BlockExtractionBranchContributionTarget.projectionSummationStatement_of_backendExpansionStatement")
*Plain-English reading.* Lean checks the proposition indexed as “projection summation statement of backend expansion statement”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/CircuitSemantics.lean:629](../../../../library/modules/circuitsemantics/#decl-quantumblockencoding-blockextractionbranchcontributiontarget-projectionsummationstatement-of-backendexpansionstatement). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.BlockExtractionBranchContributionTarget.backendExpansionStatement_of_projectionSummationStatement" (lean := "QuantumBlockEncoding.BlockExtractionBranchContributionTarget.backendExpansionStatement_of_projectionSummationStatement")
*Plain-English reading.* Lean checks the proposition indexed as “backend expansion statement of projection summation statement”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/CircuitSemantics.lean:640](../../../../library/modules/circuitsemantics/#decl-quantumblockencoding-blockextractionbranchcontributiontarget-backendexpansionstatement-of-projectionsummationstatement). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.CircuitBlockEncodingClaim" (lean := "QuantumBlockEncoding.CircuitBlockEncodingClaim")
*Plain-English reading.* This record groups the data and proof fields needed for “circuit block encoding claim”. A proposition-valued field is a requirement until a constructor supplies it. A circuit-level block encoding claim bundling a circuit matrix semantics with a block extraction target and a dimension compatibility proof.

*Formal status.* Data contract in the default import surface; proposition-valued fields are obligations, not automatically established facts.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* A circuit-level block encoding claim bundling a circuit matrix semantics with a block extraction target and a dimension compatibility proof. The 'blockCorrect' obligation tracks the main mathematical claim: (⟨signalIdx| ⊗ I) U (|signalIdx⟩ ⊗ I) = targetMatrix / normalizer. This does not assert the claim is true; it records what needs proving.

*Declaration kind.* structure.

Source: [QuantumBlockEncoding/CircuitSemantics.lean:661](../../../../library/modules/circuitsemantics/#decl-quantumblockencoding-circuitblockencodingclaim). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.FiniteBlockCompositionContract" (lean := "QuantumBlockEncoding.FiniteBlockCompositionContract")
*Plain-English reading.* This record groups the data and proof fields needed for “finite block composition contract”. A proposition-valued field is a requirement until a constructor supplies it. Typed contract for a finite-dimensional LCU/block-composition step.

*Formal status.* Data contract in the default import surface; proposition-valued fields are obligations, not automatically established facts.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Typed contract for a finite-dimensional LCU/block-composition step. This is intentionally contract-only: it states the exact matrix objects and obligations that a later theorem must connect, without treating a cited LCU result or a paper theorem as a Lean proof.

*Declaration kind.* structure.

Source: [QuantumBlockEncoding/CircuitSemantics.lean:676](../../../../library/modules/circuitsemantics/#decl-quantumblockencoding-finiteblockcompositioncontract). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.signalSystemBlockRowIndex" (lean := "QuantumBlockEncoding.signalSystemBlockRowIndex")
*Plain-English reading.* This definition gives the library's named construction or computation for “signal system block row index”. Compound row index for a signal value and a system-row index.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Compound row index for a signal value and a system-row index.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/CircuitSemantics.lean:696](../../../../library/modules/circuitsemantics/#decl-quantumblockencoding-signalsystemblockrowindex). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.signalSystemBlockColIndex" (lean := "QuantumBlockEncoding.signalSystemBlockColIndex")
*Plain-English reading.* This definition gives the library's named construction or computation for “signal system block col index”. Compound column index for a signal value and a system-column index.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Compound column index for a signal value and a system-column index.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/CircuitSemantics.lean:700](../../../../library/modules/circuitsemantics/#decl-quantumblockencoding-signalsystemblockcolindex). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.signalSystemBlockRowIndex_zero" (lean := "QuantumBlockEncoding.signalSystemBlockRowIndex_zero")
*Plain-English reading.* Lean checks the proposition indexed as “signal system block row index zero”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/CircuitSemantics.lean:703](../../../../library/modules/circuitsemantics/#decl-quantumblockencoding-signalsystemblockrowindex-zero). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.signalSystemBlockColIndex_zero" (lean := "QuantumBlockEncoding.signalSystemBlockColIndex_zero")
*Plain-English reading.* Lean checks the proposition indexed as “signal system block col index zero”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/CircuitSemantics.lean:707](../../../../library/modules/circuitsemantics/#decl-quantumblockencoding-signalsystemblockcolindex-zero). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.signalSystemBlockRowIndex_lt" (lean := "QuantumBlockEncoding.signalSystemBlockRowIndex_lt")
*Plain-English reading.* Lean checks the proposition indexed as “signal system block row index lt”; the hypotheses and conclusion in the code panel fix its exact scope. The row compound index stays inside a signal × row matrix.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The row compound index stays inside a signal × row matrix.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/CircuitSemantics.lean:712](../../../../library/modules/circuitsemantics/#decl-quantumblockencoding-signalsystemblockrowindex-lt). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.signalSystemBlockColIndex_lt" (lean := "QuantumBlockEncoding.signalSystemBlockColIndex_lt")
*Plain-English reading.* Lean checks the proposition indexed as “signal system block col index lt”; the hypotheses and conclusion in the code panel fix its exact scope. The column compound index stays inside a signal × column matrix.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The column compound index stays inside a signal × column matrix.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/CircuitSemantics.lean:727](../../../../library/modules/circuitsemantics/#decl-quantumblockencoding-signalsystemblockcolindex-lt). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.signalSystemBlockProjection" (lean := "QuantumBlockEncoding.signalSystemBlockProjection")
*Plain-English reading.* This definition gives the library's named construction or computation for “signal system block projection”. Block projection: extract the '(signalIdx, signalIdx)' block from a signal × system matrix.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Block projection: extract the '(signalIdx, signalIdx)' block from a signal × system matrix. Given a matrix M of size '(signalDim \* rows) × (signalDim \* cols)', the helpers 'signalSystemBlockRowIndex' and 'signalSystemBlockColIndex' map a pair '(i, j)' of system indices to the compound row and column indices in the full matrix that correspond to signal register value 'idx' and system indices '(i, j)'. The block '(⟨signalIdx| ⊗ I) M (|signalIdx⟩ ⊗ I)' is then: blockMatrix i j = M (signalIdx \* rows + i) (signalIdx \* cols + j)

*Declaration kind.* def.

Source: [QuantumBlockEncoding/CircuitSemantics.lean:753](../../../../library/modules/circuitsemantics/#decl-quantumblockencoding-signalsystemblockprojection). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.signalSystemBlockProjection_apply" (lean := "QuantumBlockEncoding.signalSystemBlockProjection_apply")
*Plain-English reading.* Lean checks the proposition indexed as “signal system block projection apply”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/CircuitSemantics.lean:764](../../../../library/modules/circuitsemantics/#decl-quantumblockencoding-signalsystemblockprojection-apply). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.totalCircuitQubits" (lean := "QuantumBlockEncoding.totalCircuitQubits")
*Plain-English reading.* This definition gives the library's named construction or computation for “total circuit qubits”. Total qubits needed for a circuit operating on 'system' system qubits and 'signal' signal qubits.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Total qubits needed for a circuit operating on 'system' system qubits and 'signal' signal qubits.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/CircuitSemantics.lean:778](../../../../library/modules/circuitsemantics/#decl-quantumblockencoding-totalcircuitqubits). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.CircuitMatrixSemantics.blockExtractionTarget" (lean := "QuantumBlockEncoding.CircuitMatrixSemantics.blockExtractionTarget")
*Plain-English reading.* This definition gives the library's named construction or computation for “block extraction target”. Build a BlockExtractionTarget from a CircuitMatrixSemantics by computing the block projection.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Build a BlockExtractionTarget from a CircuitMatrixSemantics by computing the block projection. The circuit matrix is square with dimension 'signalDim \* dim', and we extract the '(signalIdx, signalIdx)' block.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/CircuitSemantics.lean:786](../../../../library/modules/circuitsemantics/#decl-quantumblockencoding-circuitmatrixsemantics-blockextractiontarget). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/ConcreteSemantics.lean

25 explicit public declarations, in source order.

:::definition "QuantumBlockEncoding.ConcreteSemantics.FiniteMatrix" (lean := "QuantumBlockEncoding.ConcreteSemantics.FiniteMatrix")
*Plain-English reading.* This abbreviation gives a shorter name to the type or expression used for “finite matrix”. A Mathlib finite matrix, definitionally compatible with ABEIS 'Matrix'.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* A Mathlib finite matrix, definitionally compatible with ABEIS 'Matrix'.

*Declaration kind.* abbrev.

Source: [QuantumBlockEncoding/ConcreteSemantics.lean:25](../../../../library/modules/concretesemantics/#decl-quantumblockencoding-concretesemantics-finitematrix). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.ConcreteSemantics.StateVector" (lean := "QuantumBlockEncoding.ConcreteSemantics.StateVector")
*Plain-English reading.* This abbreviation gives a shorter name to the type or expression used for “state vector”. A finite column vector.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* A finite column vector.

*Declaration kind.* abbrev.

Source: [QuantumBlockEncoding/ConcreteSemantics.lean:29](../../../../library/modules/concretesemantics/#decl-quantumblockencoding-concretesemantics-statevector). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.ConcreteSemantics.basisKet" (lean := "QuantumBlockEncoding.ConcreteSemantics.basisKet")
*Plain-English reading.* This definition gives the library's named construction or computation for “basis ket”. A computational-basis ket in the concrete finite backend.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* A computational-basis ket in the concrete finite backend.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/ConcreteSemantics.lean:33](../../../../library/modules/concretesemantics/#decl-quantumblockencoding-concretesemantics-basisket). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ConcreteSemantics.basisKet_apply" (lean := "QuantumBlockEncoding.ConcreteSemantics.basisKet_apply")
*Plain-English reading.* Lean checks the proposition indexed as “basis ket apply”; the hypotheses and conclusion in the code panel fix its exact scope. A computational-basis ket is exactly a Kronecker delta at its named index.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* A computational-basis ket is exactly a Kronecker delta at its named index. Keeping this lemma in the concrete backend avoids exposing 'Pi.single' inside fixed-width circuit proofs.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ConcreteSemantics.lean:40](../../../../library/modules/concretesemantics/#decl-quantumblockencoding-concretesemantics-basisket-apply). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.ConcreteSemantics.zeroKet" (lean := "QuantumBlockEncoding.ConcreteSemantics.zeroKet")
*Plain-English reading.* This definition gives the library's named construction or computation for “zero ket”. The all-zero computational-basis ket for an 'n'-qubit register.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The all-zero computational-basis ket for an 'n'-qubit register.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/ConcreteSemantics.lean:50](../../../../library/modules/concretesemantics/#decl-quantumblockencoding-concretesemantics-zeroket). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.ConcreteSemantics.applyVec" (lean := "QuantumBlockEncoding.ConcreteSemantics.applyVec")
*Plain-English reading.* This definition gives the library's named construction or computation for “apply vec”. Matrix-vector action using Mathlib's finite sum semantics.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Matrix-vector action using Mathlib's finite sum semantics.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/ConcreteSemantics.lean:55](../../../../library/modules/concretesemantics/#decl-quantumblockencoding-concretesemantics-applyvec). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.ConcreteSemantics.ComplexUnitaryGate" (lean := "QuantumBlockEncoding.ConcreteSemantics.ComplexUnitaryGate")
*Plain-English reading.* This record groups the data and proof fields needed for “complex unitary gate”. A proposition-valued field is a requirement until a constructor supplies it. A finite complex gate whose unitarity is the standard Mathlib unitary-group predicate rather than an unconstrained proposition.

*Formal status.* Data contract in the default import surface; proposition-valued fields are obligations, not automatically established facts.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* A finite complex gate whose unitarity is the standard Mathlib unitary-group predicate rather than an unconstrained proposition.

*Declaration kind.* structure.

Source: [QuantumBlockEncoding/ConcreteSemantics.lean:64](../../../../library/modules/concretesemantics/#decl-quantumblockencoding-concretesemantics-complexunitarygate). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ConcreteSemantics.applyVec_basisKet" (lean := "QuantumBlockEncoding.ConcreteSemantics.applyVec_basisKet")
*Plain-English reading.* Lean checks the proposition indexed as “apply vec basis ket”; the hypotheses and conclusion in the code panel fix its exact scope. Acting on a basis ket selects the corresponding matrix column.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Acting on a basis ket selects the corresponding matrix column.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ConcreteSemantics.lean:70](../../../../library/modules/concretesemantics/#decl-quantumblockencoding-concretesemantics-applyvec-basisket). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ConcreteSemantics.applyVec_twoBasisSuperposition" (lean := "QuantumBlockEncoding.ConcreteSemantics.applyVec_twoBasisSuperposition")
*Plain-English reading.* Lean checks the proposition indexed as “apply vec two basis superposition”; the hypotheses and conclusion in the code panel fix its exact scope. Acting on a two-term sparse superposition needs only the two named matrix columns.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Acting on a two-term sparse superposition needs only the two named matrix columns. Keeping this linearity fact in the concrete backend prevents every finite state-preparation witness from rebuilding a large 'mulVec' proof term.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ConcreteSemantics.lean:79](../../../../library/modules/concretesemantics/#decl-quantumblockencoding-concretesemantics-applyvec-twobasissuperposition). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ConcreteSemantics.applyVec_twoBasisSuperposition_apply" (lean := "QuantumBlockEncoding.ConcreteSemantics.applyVec_twoBasisSuperposition_apply")
*Plain-English reading.* Lean checks the proposition indexed as “apply vec two basis superposition apply”; the hypotheses and conclusion in the code panel fix its exact scope. Coordinate form of 'applyVec\_twoBasisSuperposition'.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Coordinate form of 'applyVec\_twoBasisSuperposition'. Paper-route proofs use this compiled scalar interface so the kernel never has to rebuild a whole vector rewrite just to read one output amplitude.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ConcreteSemantics.lean:93](../../../../library/modules/concretesemantics/#decl-quantumblockencoding-concretesemantics-applyvec-twobasissuperposition-apply). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ConcreteSemantics.applyVec_zeroKet" (lean := "QuantumBlockEncoding.ConcreteSemantics.applyVec_zeroKet")
*Plain-English reading.* Lean checks the proposition indexed as “apply vec zero ket”; the hypotheses and conclusion in the code panel fix its exact scope. Acting on the all-zero ket selects column zero.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Acting on the all-zero ket selects column zero.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ConcreteSemantics.lean:103](../../../../library/modules/concretesemantics/#decl-quantumblockencoding-concretesemantics-applyvec-zeroket). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ConcreteSemantics.firstColumnMatches_iff_applyVec_zeroKet" (lean := "QuantumBlockEncoding.ConcreteSemantics.firstColumnMatches_iff_applyVec_zeroKet")
*Plain-English reading.* Lean checks the proposition indexed as “first column matches iff apply vec zero ket”; the hypotheses and conclusion in the code panel fix its exact scope. The ABEIS first-column contract is exactly the state-action equation 'U |0^n> = |psi>' in the concrete finite matrix backend.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The ABEIS first-column contract is exactly the state-action equation 'U |0^n> = |psi>' in the concrete finite matrix backend.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ConcreteSemantics.lean:114](../../../../library/modules/concretesemantics/#decl-quantumblockencoding-concretesemantics-firstcolumnmatches-iff-applyvec-zeroket). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.ConcreteSemantics.ComplexStatePreparationCertificate" (lean := "QuantumBlockEncoding.ConcreteSemantics.ComplexStatePreparationCertificate")
*Plain-English reading.* This record groups the data and proof fields needed for “complex state preparation certificate”. A proposition-valued field is a requirement until a constructor supplies it. Concrete state-preparation evidence.

*Formal status.* Data contract in the default import surface; proposition-valued fields are obligations, not automatically established facts.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Concrete state-preparation evidence. This is an optional final semantic layer: existing symbolic and rational candidates do not need to use it, but a complex candidate cannot enter this record without standard unitarity and state action.

*Declaration kind.* structure.

Source: [QuantumBlockEncoding/ConcreteSemantics.lean:133](../../../../library/modules/concretesemantics/#decl-quantumblockencoding-concretesemantics-complexstatepreparationcertificate). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.ConcreteSemantics.ComplexStatePreparationCertificate.candidate" (lean := "QuantumBlockEncoding.ConcreteSemantics.ComplexStatePreparationCertificate.candidate")
*Plain-English reading.* This definition gives the library's named construction or computation for “candidate”. Repackage concrete semantics in the existing generic candidate interface.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Repackage concrete semantics in the existing generic candidate interface.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/ConcreteSemantics.lean:143](../../../../library/modules/concretesemantics/#decl-quantumblockencoding-concretesemantics-complexstatepreparationcertificate-candidate). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.ConcreteSemantics.ComplexStatePreparationCertificate.verified" (lean := "QuantumBlockEncoding.ConcreteSemantics.ComplexStatePreparationCertificate.verified")
*Plain-English reading.* This definition gives the library's named construction or computation for “verified”. Promote a concrete certificate to the existing verified wrapper.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Promote a concrete certificate to the existing verified wrapper. The generic 'isUnitary' field is instantiated by, rather than substituted for, the Mathlib unitary-group predicate.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/ConcreteSemantics.lean:162](../../../../library/modules/concretesemantics/#decl-quantumblockencoding-concretesemantics-complexstatepreparationcertificate-verified). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ConcreteSemantics.ComplexStatePreparationCertificate.preparesVector" (lean := "QuantumBlockEncoding.ConcreteSemantics.ComplexStatePreparationCertificate.preparesVector")
*Plain-English reading.* Lean checks the proposition indexed as “prepares vector”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ConcreteSemantics.lean:175](../../../../library/modules/concretesemantics/#decl-quantumblockencoding-concretesemantics-complexstatepreparationcertificate-preparesvector). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.ConcreteSemantics.ProductRegisterMatrix" (lean := "QuantumBlockEncoding.ConcreteSemantics.ProductRegisterMatrix")
*Plain-English reading.* This abbreviation gives a shorter name to the type or expression used for “product register matrix”. A matrix indexed by an explicit signal-register/system-register product.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* A matrix indexed by an explicit signal-register/system-register product.

*Declaration kind.* abbrev.

Source: [QuantumBlockEncoding/ConcreteSemantics.lean:183](../../../../library/modules/concretesemantics/#decl-quantumblockencoding-concretesemantics-productregistermatrix). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.ConcreteSemantics.flatToProductRegister" (lean := "QuantumBlockEncoding.ConcreteSemantics.flatToProductRegister")
*Plain-English reading.* This definition gives the library's named construction or computation for “flat to product register”. View a flattened signal-system matrix through explicit product-register indices.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* View a flattened signal-system matrix through explicit product-register indices. The signal register is high-order and the system register low-order.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/ConcreteSemantics.lean:190](../../../../library/modules/concretesemantics/#decl-quantumblockencoding-concretesemantics-flattoproductregister). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.ConcreteSemantics.productRegisterBlockProjection" (lean := "QuantumBlockEncoding.ConcreteSemantics.productRegisterBlockProjection")
*Plain-English reading.* This definition gives the library's named construction or computation for “product register block projection”. Project one signal branch from an explicit product-register matrix.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Project one signal branch from an explicit product-register matrix.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/ConcreteSemantics.lean:201](../../../../library/modules/concretesemantics/#decl-quantumblockencoding-concretesemantics-productregisterblockprojection). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ConcreteSemantics.productRegisterBlockProjection_flatToProductRegister" (lean := "QuantumBlockEncoding.ConcreteSemantics.productRegisterBlockProjection_flatToProductRegister")
*Plain-English reading.* Lean checks the proposition indexed as “product register block projection flat to product register”; the hypotheses and conclusion in the code panel fix its exact scope. Product-register projection after viewing a flat matrix is definitionally the existing ABEIS flattened block projection.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Product-register projection after viewing a flat matrix is definitionally the existing ABEIS flattened block projection.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ConcreteSemantics.lean:210](../../../../library/modules/concretesemantics/#decl-quantumblockencoding-concretesemantics-productregisterblockprojection-flattoproductregister). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ConcreteSemantics.productIndex_val_eq_signalSystemBlockRowIndex" (lean := "QuantumBlockEncoding.ConcreteSemantics.productIndex_val_eq_signalSystemBlockRowIndex")
*Plain-English reading.* Lean checks the proposition indexed as “product index val eq signal system block row index”; the hypotheses and conclusion in the code panel fix its exact scope. The classic product index and circuit-semantics row index have the same value.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The classic product index and circuit-semantics row index have the same value.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ConcreteSemantics.lean:220](../../../../library/modules/concretesemantics/#decl-quantumblockencoding-concretesemantics-productindex-val-eq-signalsystemblockrowindex). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ConcreteSemantics.signalSystemBlockProjection_eq_cleanBlockProduct" (lean := "QuantumBlockEncoding.ConcreteSemantics.signalSystemBlockProjection_eq_cleanBlockProduct")
*Plain-English reading.* Lean checks the proposition indexed as “signal system block projection eq clean block product”; the hypotheses and conclusion in the code panel fix its exact scope. The classic rational clean block and the generic circuit-semantics projection are the same pointwise matrix under the shared register order.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The classic rational clean block and the generic circuit-semantics projection are the same pointwise matrix under the shared register order.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ConcreteSemantics.lean:232](../../../../library/modules/concretesemantics/#decl-quantumblockencoding-concretesemantics-signalsystemblockprojection-eq-cleanblockproduct). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.ConcreteSemantics.cleanBasisActionAmplitude" (lean := "QuantumBlockEncoding.ConcreteSemantics.cleanBasisActionAmplitude")
*Plain-English reading.* This definition gives the library's named construction or computation for “clean basis action amplitude”. The clean output amplitude obtained by applying 'operator' to a clean signal-system basis input.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The clean output amplitude obtained by applying 'operator' to a clean signal-system basis input. Naming this quantity makes the two common block-encoding proof styles explicit: prove the projected matrix block, or prove the clean branch of the action on every basis input.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/ConcreteSemantics.lean:249](../../../../library/modules/concretesemantics/#decl-quantumblockencoding-concretesemantics-cleanbasisactionamplitude). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ConcreteSemantics.cleanBasisActionAmplitude_eq_signalSystemBlockProjection" (lean := "QuantumBlockEncoding.ConcreteSemantics.cleanBasisActionAmplitude_eq_signalSystemBlockProjection")
*Plain-English reading.* Lean checks the proposition indexed as “clean basis action amplitude eq signal system block projection”; the hypotheses and conclusion in the code panel fix its exact scope. Acting on a clean basis input and reading a clean output is one projected-block entry.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Acting on a clean basis input and reading a clean output is one projected-block entry.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ConcreteSemantics.lean:259](../../../../library/modules/concretesemantics/#decl-quantumblockencoding-concretesemantics-cleanbasisactionamplitude-eq-signalsystemblockprojection). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ConcreteSemantics.pointwiseProjection_iff_cleanBasisAction" (lean := "QuantumBlockEncoding.ConcreteSemantics.pointwiseProjection_iff_cleanBasisAction")
*Plain-English reading.* Lean checks the proposition indexed as “pointwise projection iff clean basis action”; the hypotheses and conclusion in the code panel fix its exact scope. Finite-dimensional bridge between the projected-block definition and the clean-branch action proof.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Finite-dimensional bridge between the projected-block definition and the clean-branch action proof. Linearity then extends the basis statement to an arbitrary system state; any normalized orthogonal failure branch is additional unitarity evidence, not a different block-encoding contract.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ConcreteSemantics.lean:277](../../../../library/modules/concretesemantics/#decl-quantumblockencoding-concretesemantics-pointwiseprojection-iff-cleanbasisaction). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/ConstructiveIsometryCompletion.lean

31 explicit public declarations, in source order.

:::definition "QuantumBlockEncoding.ConstructiveIsometryCompletion.PrefixColumns" (lean := "QuantumBlockEncoding.ConstructiveIsometryCompletion.PrefixColumns")
*Plain-English reading.* This definition gives the library's named construction or computation for “prefix columns”. The first 'k' rectangular columns are their corresponding coordinate vectors.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The first 'k' rectangular columns are their corresponding coordinate vectors.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/ConstructiveIsometryCompletion.lean:19](../../../../library/modules/constructiveisometrycompletion/#decl-quantumblockencoding-constructiveisometrycompletion-prefixcolumns). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ConstructiveIsometryCompletion.rotateRows_isometry" (lean := "QuantumBlockEncoding.ConstructiveIsometryCompletion.rotateRows_isometry")
*Plain-English reading.* Lean checks the proposition indexed as “rotate rows isometry”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ConstructiveIsometryCompletion.lean:24](../../../../library/modules/constructiveisometrycompletion/#decl-quantumblockencoding-constructiveisometrycompletion-rotaterows-isometry). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ConstructiveIsometryCompletion.columnSweep_isometry" (lean := "QuantumBlockEncoding.ConstructiveIsometryCompletion.columnSweep_isometry")
*Plain-English reading.* Lean checks the proposition indexed as “column sweep isometry”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ConstructiveIsometryCompletion.lean:33](../../../../library/modules/constructiveisometrycompletion/#decl-quantumblockencoding-constructiveisometrycompletion-columnsweep-isometry). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ConstructiveIsometryCompletion.zero_of_fixed_column" (lean := "QuantumBlockEncoding.ConstructiveIsometryCompletion.zero_of_fixed_column")
*Plain-English reading.* Lean checks the proposition indexed as “zero of fixed column”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ConstructiveIsometryCompletion.lean:47](../../../../library/modules/constructiveisometrycompletion/#decl-quantumblockencoding-constructiveisometrycompletion-zero-of-fixed-column). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ConstructiveIsometryCompletion.columnSweep_prefix" (lean := "QuantumBlockEncoding.ConstructiveIsometryCompletion.columnSweep_prefix")
*Plain-English reading.* Lean checks the proposition indexed as “column sweep prefix”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ConstructiveIsometryCompletion.lean:55](../../../../library/modules/constructiveisometrycompletion/#decl-quantumblockencoding-constructiveisometrycompletion-columnsweep-prefix). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ConstructiveIsometryCompletion.supported_column_sq" (lean := "QuantumBlockEncoding.ConstructiveIsometryCompletion.supported_column_sq")
*Plain-English reading.* Lean checks the proposition indexed as “supported column sq”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ConstructiveIsometryCompletion.lean:69](../../../../library/modules/constructiveisometrycompletion/#decl-quantumblockencoding-constructiveisometrycompletion-supported-column-sq). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ConstructiveIsometryCompletion.columnSweep_prefix_succ" (lean := "QuantumBlockEncoding.ConstructiveIsometryCompletion.columnSweep_prefix_succ")
*Plain-English reading.* Lean checks the proposition indexed as “column sweep prefix succ”; the hypotheses and conclusion in the code panel fix its exact scope. A nonfinal rectangular isometry column is swept to a positive unit pivot.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* A nonfinal rectangular isometry column is swept to a positive unit pivot.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ConstructiveIsometryCompletion.lean:88](../../../../library/modules/constructiveisometrycompletion/#decl-quantumblockencoding-constructiveisometrycompletion-columnsweep-prefix-succ). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ConstructiveIsometryCompletion.sweep_prefix" (lean := "QuantumBlockEncoding.ConstructiveIsometryCompletion.sweep_prefix")
*Plain-English reading.* Lean checks the proposition indexed as “sweep prefix”; the hypotheses and conclusion in the code panel fix its exact scope. The shared rectangular sweep fixes every processed isometry column.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The shared rectangular sweep fixes every processed isometry column.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ConstructiveIsometryCompletion.lean:126](../../../../library/modules/constructiveisometrycompletion/#decl-quantumblockencoding-constructiveisometrycompletion-sweep-prefix). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ConstructiveIsometryCompletion.reduced_prefix" (lean := "QuantumBlockEncoding.ConstructiveIsometryCompletion.reduced_prefix")
*Plain-English reading.* Lean checks the proposition indexed as “reduced prefix”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ConstructiveIsometryCompletion.lean:140](../../../../library/modules/constructiveisometrycompletion/#decl-quantumblockencoding-constructiveisometrycompletion-reduced-prefix). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.ConstructiveIsometryCompletion.prefixCompletion" (lean := "QuantumBlockEncoding.ConstructiveIsometryCompletion.prefixCompletion")
*Plain-English reading.* This definition gives the library's named construction or computation for “prefix completion”. Inverse of the explicitly computed row rotations; no matrix witness is chosen.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Inverse of the explicitly computed row rotations; no matrix witness is chosen.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/ConstructiveIsometryCompletion.lean:147](../../../../library/modules/constructiveisometrycompletion/#decl-quantumblockencoding-constructiveisometrycompletion-prefixcompletion). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ConstructiveIsometryCompletion.prefixCompletion_orthogonal" (lean := "QuantumBlockEncoding.ConstructiveIsometryCompletion.prefixCompletion_orthogonal")
*Plain-English reading.* Lean checks the proposition indexed as “prefix completion orthogonal”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ConstructiveIsometryCompletion.lean:150](../../../../library/modules/constructiveisometrycompletion/#decl-quantumblockencoding-constructiveisometrycompletion-prefixcompletion-orthogonal). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ConstructiveIsometryCompletion.prefixCompletion_det" (lean := "QuantumBlockEncoding.ConstructiveIsometryCompletion.prefixCompletion_det")
*Plain-English reading.* Lean checks the proposition indexed as “prefix completion det”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ConstructiveIsometryCompletion.lean:155](../../../../library/modules/constructiveisometrycompletion/#decl-quantumblockencoding-constructiveisometrycompletion-prefixcompletion-det). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ConstructiveIsometryCompletion.prefixCompletion_columns" (lean := "QuantumBlockEncoding.ConstructiveIsometryCompletion.prefixCompletion_columns")
*Plain-English reading.* Lean checks the proposition indexed as “prefix completion columns”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ConstructiveIsometryCompletion.lean:159](../../../../library/modules/constructiveisometrycompletion/#decl-quantumblockencoding-constructiveisometrycompletion-prefixcompletion-columns). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.ConstructiveIsometryCompletion.extendPrefix" (lean := "QuantumBlockEncoding.ConstructiveIsometryCompletion.extendPrefix")
*Plain-English reading.* This definition gives the library's named construction or computation for “extend prefix”. Greedy swaps deterministically extend a finite prefix injection.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Greedy swaps deterministically extend a finite prefix injection.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/ConstructiveIsometryCompletion.lean:167](../../../../library/modules/constructiveisometrycompletion/#decl-quantumblockencoding-constructiveisometrycompletion-extendprefix). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ConstructiveIsometryCompletion.extendPrefix_agrees" (lean := "QuantumBlockEncoding.ConstructiveIsometryCompletion.extendPrefix_agrees")
*Plain-English reading.* Lean checks the proposition indexed as “extend prefix agrees”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ConstructiveIsometryCompletion.lean:174](../../../../library/modules/constructiveisometrycompletion/#decl-quantumblockencoding-constructiveisometrycompletion-extendprefix-agrees). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.ConstructiveIsometryCompletion.unusedPosition" (lean := "QuantumBlockEncoding.ConstructiveIsometryCompletion.unusedPosition")
*Plain-English reading.* This definition gives the library's named construction or computation for “unused position”. The first unused original coordinate is carried to an unused physical label.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The first unused original coordinate is carried to an unused physical label.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/ConstructiveIsometryCompletion.lean:201](../../../../library/modules/constructiveisometrycompletion/#decl-quantumblockencoding-constructiveisometrycompletion-unusedposition). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ConstructiveIsometryCompletion.unusedPosition_ne" (lean := "QuantumBlockEncoding.ConstructiveIsometryCompletion.unusedPosition_ne")
*Plain-English reading.* Lean checks the proposition indexed as “unused position ne”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ConstructiveIsometryCompletion.lean:204](../../../../library/modules/constructiveisometrycompletion/#decl-quantumblockencoding-constructiveisometrycompletion-unusedposition-ne). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.ConstructiveIsometryCompletion.permuteColumns" (lean := "QuantumBlockEncoding.ConstructiveIsometryCompletion.permuteColumns")
*Plain-English reading.* This definition gives the library's named construction or computation for “permute columns”. Send original column 'a' to physical column 'p a'.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Send original column 'a' to physical column 'p a'.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/ConstructiveIsometryCompletion.lean:214](../../../../library/modules/constructiveisometrycompletion/#decl-quantumblockencoding-constructiveisometrycompletion-permutecolumns). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ConstructiveIsometryCompletion.permuteColumns_apply" (lean := "QuantumBlockEncoding.ConstructiveIsometryCompletion.permuteColumns_apply")
*Plain-English reading.* Lean checks the proposition indexed as “permute columns apply”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ConstructiveIsometryCompletion.lean:218](../../../../library/modules/constructiveisometrycompletion/#decl-quantumblockencoding-constructiveisometrycompletion-permutecolumns-apply). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ConstructiveIsometryCompletion.permuteColumns_orthogonal" (lean := "QuantumBlockEncoding.ConstructiveIsometryCompletion.permuteColumns_orthogonal")
*Plain-English reading.* Lean checks the proposition indexed as “permute columns orthogonal”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ConstructiveIsometryCompletion.lean:223](../../../../library/modules/constructiveisometrycompletion/#decl-quantumblockencoding-constructiveisometrycompletion-permutecolumns-orthogonal). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ConstructiveIsometryCompletion.permuteColumns_det" (lean := "QuantumBlockEncoding.ConstructiveIsometryCompletion.permuteColumns_det")
*Plain-English reading.* Lean checks the proposition indexed as “permute columns det”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ConstructiveIsometryCompletion.lean:231](../../../../library/modules/constructiveisometrycompletion/#decl-quantumblockencoding-constructiveisometrycompletion-permutecolumns-det). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.ConstructiveIsometryCompletion.orientColumns" (lean := "QuantumBlockEncoding.ConstructiveIsometryCompletion.orientColumns")
*Plain-English reading.* This definition gives the library's named construction or computation for “orient columns”. Correct only an unused column, using finite permutation parity, not a determinant test.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Correct only an unused column, using finite permutation parity, not a determinant test.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/ConstructiveIsometryCompletion.lean:237](../../../../library/modules/constructiveisometrycompletion/#decl-quantumblockencoding-constructiveisometrycompletion-orientcolumns). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ConstructiveIsometryCompletion.orientColumns_orthogonal" (lean := "QuantumBlockEncoding.ConstructiveIsometryCompletion.orientColumns_orthogonal")
*Plain-English reading.* Lean checks the proposition indexed as “orient columns orthogonal”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ConstructiveIsometryCompletion.lean:242](../../../../library/modules/constructiveisometrycompletion/#decl-quantumblockencoding-constructiveisometrycompletion-orientcolumns-orthogonal). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ConstructiveIsometryCompletion.orientColumns_det" (lean := "QuantumBlockEncoding.ConstructiveIsometryCompletion.orientColumns_det")
*Plain-English reading.* Lean checks the proposition indexed as “orient columns det”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ConstructiveIsometryCompletion.lean:256](../../../../library/modules/constructiveisometrycompletion/#decl-quantumblockencoding-constructiveisometrycompletion-orientcolumns-det). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ConstructiveIsometryCompletion.orientColumns_preserves" (lean := "QuantumBlockEncoding.ConstructiveIsometryCompletion.orientColumns_preserves")
*Plain-English reading.* Lean checks the proposition indexed as “orient columns preserves”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ConstructiveIsometryCompletion.lean:269](../../../../library/modules/constructiveisometrycompletion/#decl-quantumblockencoding-constructiveisometrycompletion-orientcolumns-preserves). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.ConstructiveIsometryCompletion.placeColumns" (lean := "QuantumBlockEncoding.ConstructiveIsometryCompletion.placeColumns")
*Plain-English reading.* This definition gives the library's named construction or computation for “place columns”. Explicit physical-column placement of a prefix SO completion.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Explicit physical-column placement of a prefix SO completion.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/ConstructiveIsometryCompletion.lean:279](../../../../library/modules/constructiveisometrycompletion/#decl-quantumblockencoding-constructiveisometrycompletion-placecolumns). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ConstructiveIsometryCompletion.placeColumns_spec" (lean := "QuantumBlockEncoding.ConstructiveIsometryCompletion.placeColumns_spec")
*Plain-English reading.* Lean checks the proposition indexed as “place columns spec”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ConstructiveIsometryCompletion.lean:283](../../../../library/modules/constructiveisometrycompletion/#decl-quantumblockencoding-constructiveisometrycompletion-placecolumns-spec). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.ConstructiveIsometryCompletion.complete" (lean := "QuantumBlockEncoding.ConstructiveIsometryCompletion.complete")
*Plain-English reading.* This definition gives the library's named construction or computation for “complete”. Actual deterministic SO matrix with columns at the prescribed physical positions.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Actual deterministic SO matrix with columns at the prescribed physical positions.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/ConstructiveIsometryCompletion.lean:298](../../../../library/modules/constructiveisometrycompletion/#decl-quantumblockencoding-constructiveisometrycompletion-complete). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ConstructiveIsometryCompletion.complete_spec" (lean := "QuantumBlockEncoding.ConstructiveIsometryCompletion.complete_spec")
*Plain-English reading.* Lean checks the proposition indexed as “complete spec”; the hypotheses and conclusion in the code panel fix its exact scope. The supplied hypothesis is only the input column isometry; the returned matrix is computed by the named producer, not supplied or selected existentially.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The supplied hypothesis is only the input column isometry; the returned matrix is computed by the named producer, not supplied or selected existentially.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ConstructiveIsometryCompletion.lean:304](../../../../library/modules/constructiveisometrycompletion/#decl-quantumblockencoding-constructiveisometrycompletion-complete-spec). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.ConstructiveIsometryCompletion.completeNamed" (lean := "QuantumBlockEncoding.ConstructiveIsometryCompletion.completeNamed")
*Plain-English reading.* This definition gives the library's named construction or computation for “complete named”. Transport using a supplied explicit coordinate equivalence, not an arbitrary enumeration chosen for the named basis.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Transport using a supplied explicit coordinate equivalence, not an arbitrary enumeration chosen for the named basis.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/ConstructiveIsometryCompletion.lean:317](../../../../library/modules/constructiveisometrycompletion/#decl-quantumblockencoding-constructiveisometrycompletion-completenamed). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ConstructiveIsometryCompletion.completeNamed_spec" (lean := "QuantumBlockEncoding.ConstructiveIsometryCompletion.completeNamed_spec")
*Plain-English reading.* Lean checks the proposition indexed as “complete named spec”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ConstructiveIsometryCompletion.lean:323](../../../../library/modules/constructiveisometrycompletion/#decl-quantumblockencoding-constructiveisometrycompletion-completenamed-spec). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/ConstructiveIsometryLocal.lean

5 explicit public declarations, in source order.

:::definition "QuantumBlockEncoding.ConstructiveIsometryLocal.coordinates" (lean := "QuantumBlockEncoding.ConstructiveIsometryLocal.coordinates")
*Plain-English reading.* This definition gives the library's named construction or computation for “coordinates”. Low bond wires, highest emitted bit, with an explicit finite coordinate map.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Low bond wires, highest emitted bit, with an explicit finite coordinate map.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/ConstructiveIsometryLocal.lean:15](../../../../library/modules/constructiveisometrylocal/#decl-quantumblockencoding-constructiveisometrylocal-coordinates). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.ConstructiveIsometryLocal.localCompletion" (lean := "QuantumBlockEncoding.ConstructiveIsometryLocal.localCompletion")
*Plain-English reading.* This definition gives the library's named construction or computation for “local completion”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/ConstructiveIsometryLocal.lean:18](../../../../library/modules/constructiveisometrylocal/#decl-quantumblockencoding-constructiveisometrylocal-localcompletion). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ConstructiveIsometryLocal.localCompletion_spec" (lean := "QuantumBlockEncoding.ConstructiveIsometryLocal.localCompletion_spec")
*Plain-English reading.* Lean checks the proposition indexed as “local completion spec”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ConstructiveIsometryLocal.lean:26](../../../../library/modules/constructiveisometrylocal/#decl-quantumblockencoding-constructiveisometrylocal-localcompletion-spec). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.ConstructiveIsometryLocal.completeStage" (lean := "QuantumBlockEncoding.ConstructiveIsometryLocal.completeStage")
*Plain-English reading.* This definition gives the library's named construction or computation for “complete stage”. Actual stage matrix derived from the chain's real occupied columns.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Actual stage matrix derived from the chain's real occupied columns.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/ConstructiveIsometryLocal.lean:34](../../../../library/modules/constructiveisometrylocal/#decl-quantumblockencoding-constructiveisometrylocal-completestage). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ConstructiveIsometryLocal.completeStage_spec" (lean := "QuantumBlockEncoding.ConstructiveIsometryLocal.completeStage_spec")
*Plain-English reading.* Lean checks the proposition indexed as “complete stage spec”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ConstructiveIsometryLocal.lean:39](../../../../library/modules/constructiveisometrylocal/#decl-quantumblockencoding-constructiveisometrylocal-completestage-spec). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/ConstructiveTensorTrain.lean

15 explicit public declarations, in source order.

:::definition "QuantumBlockEncoding.ConstructiveTensorTrain.CoreFactorization" (lean := "QuantumBlockEncoding.ConstructiveTensorTrain.CoreFactorization")
*Plain-English reading.* This record groups the data and proof fields needed for “core factorization”. A proposition-valued field is a requirement until a constructor supplies it. The physical bit/right-bond indexing is retained in the returned core.

*Formal status.* Data contract in the default import surface; proposition-valued fields are obligations, not automatically established facts.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The physical bit/right-bond indexing is retained in the returned core.

*Declaration kind.* structure.

Source: [QuantumBlockEncoding/ConstructiveTensorTrain.lean:21](../../../../library/modules/constructivetensortrain/#decl-quantumblockencoding-constructivetensortrain-corefactorization). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.ConstructiveTensorTrain.factorCore" (lean := "QuantumBlockEncoding.ConstructiveTensorTrain.factorCore")
*Plain-English reading.* This definition gives the library's named construction or computation for “factor core”. Relabel the deterministic matrix factors by the explicit product index.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Relabel the deterministic matrix factors by the explicit product index.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/ConstructiveTensorTrain.lean:28](../../../../library/modules/constructivetensortrain/#decl-quantumblockencoding-constructivetensortrain-factorcore). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.ConstructiveTensorTrain.Result" (lean := "QuantumBlockEncoding.ConstructiveTensorTrain.Result")
*Plain-English reading.* This record groups the data and proof fields needed for “result”. A proposition-valued field is a requirement until a constructor supplies it. Concrete canonical data, indexed by the precise original chain.

*Formal status.* Data contract in the default import surface; proposition-valued fields are obligations, not automatically established facts.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Concrete canonical data, indexed by the precise original chain.

*Declaration kind.* structure.

Source: [QuantumBlockEncoding/ConstructiveTensorTrain.lean:41](../../../../library/modules/constructivetensortrain/#decl-quantumblockencoding-constructivetensortrain-result). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.ConstructiveTensorTrain.canonicalize" (lean := "QuantumBlockEncoding.ConstructiveTensorTrain.canonicalize")
*Plain-English reading.* This definition gives the library's named construction or computation for “canonicalize”. Structural recursion on the source chain; no factor or basis selection.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Structural recursion on the source chain; no factor or basis selection.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/ConstructiveTensorTrain.lean:50](../../../../library/modules/constructivetensortrain/#decl-quantumblockencoding-constructivetensortrain-canonicalize). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ConstructiveTensorTrain.canonicalize_rightCanonical" (lean := "QuantumBlockEncoding.ConstructiveTensorTrain.canonicalize_rightCanonical")
*Plain-English reading.* Lean checks the proposition indexed as “canonicalize right canonical”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ConstructiveTensorTrain.lean:73](../../../../library/modules/constructivetensortrain/#decl-quantumblockencoding-constructivetensortrain-canonicalize-rightcanonical). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ConstructiveTensorTrain.canonicalize_rankReduced" (lean := "QuantumBlockEncoding.ConstructiveTensorTrain.canonicalize_rankReduced")
*Plain-English reading.* Lean checks the proposition indexed as “canonicalize rank reduced”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ConstructiveTensorTrain.lean:76](../../../../library/modules/constructivetensortrain/#decl-quantumblockencoding-constructivetensortrain-canonicalize-rankreduced). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ConstructiveTensorTrain.canonicalize_action" (lean := "QuantumBlockEncoding.ConstructiveTensorTrain.canonicalize_action")
*Plain-English reading.* Lean checks the proposition indexed as “canonicalize action”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ConstructiveTensorTrain.lean:79](../../../../library/modules/constructivetensortrain/#decl-quantumblockencoding-constructivetensortrain-canonicalize-action). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ConstructiveTensorTrain.canonicalize_maxBond_le" (lean := "QuantumBlockEncoding.ConstructiveTensorTrain.canonicalize_maxBond_le")
*Plain-English reading.* Lean checks the proposition indexed as “canonicalize max bond le”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ConstructiveTensorTrain.lean:83](../../../../library/modules/constructivetensortrain/#decl-quantumblockencoding-constructivetensortrain-canonicalize-maxbond-le). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.ConstructiveTensorTrain.boundary" (lean := "QuantumBlockEncoding.ConstructiveTensorTrain.boundary")
*Plain-English reading.* This definition gives the library's named construction or computation for “boundary”. The returned residual converts an original boundary into its new boundary.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The returned residual converts an original boundary into its new boundary.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/ConstructiveTensorTrain.lean:88](../../../../library/modules/constructivetensortrain/#decl-quantumblockencoding-constructivetensortrain-boundary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ConstructiveTensorTrain.boundary_action" (lean := "QuantumBlockEncoding.ConstructiveTensorTrain.boundary_action")
*Plain-English reading.* Lean checks the proposition indexed as “boundary action”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ConstructiveTensorTrain.lean:91](../../../../library/modules/constructivetensortrain/#decl-quantumblockencoding-constructivetensortrain-boundary-action). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ConstructiveTensorTrain.boundary_mass" (lean := "QuantumBlockEncoding.ConstructiveTensorTrain.boundary_mass")
*Plain-English reading.* Lean checks the proposition indexed as “boundary mass”; the hypotheses and conclusion in the code panel fix its exact scope. Total source mass is obtained from the small returned residual boundary.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Total source mass is obtained from the small returned residual boundary.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ConstructiveTensorTrain.lean:98](../../../../library/modules/constructivetensortrain/#decl-quantumblockencoding-constructivetensortrain-boundary-mass). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ConstructiveTensorTrain.boundary_normalized" (lean := "QuantumBlockEncoding.ConstructiveTensorTrain.boundary_normalized")
*Plain-English reading.* Lean checks the proposition indexed as “boundary normalized”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ConstructiveTensorTrain.lean:103](../../../../library/modules/constructivetensortrain/#decl-quantumblockencoding-constructivetensortrain-boundary-normalized). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.ConstructiveTensorTrain.stateBoundary" (lean := "QuantumBlockEncoding.ConstructiveTensorTrain.stateBoundary")
*Plain-English reading.* This definition gives the library's named construction or computation for “state boundary”. Concrete scalar-boundary state data, without requiring normalization.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Concrete scalar-boundary state data, without requiring normalization.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/ConstructiveTensorTrain.lean:108](../../../../library/modules/constructivetensortrain/#decl-quantumblockencoding-constructivetensortrain-stateboundary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ConstructiveTensorTrain.stateBoundary_action" (lean := "QuantumBlockEncoding.ConstructiveTensorTrain.stateBoundary_action")
*Plain-English reading.* Lean checks the proposition indexed as “state boundary action”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ConstructiveTensorTrain.lean:111](../../../../library/modules/constructivetensortrain/#decl-quantumblockencoding-constructivetensortrain-stateboundary-action). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ConstructiveTensorTrain.stateBoundary_normalized" (lean := "QuantumBlockEncoding.ConstructiveTensorTrain.stateBoundary_normalized")
*Plain-English reading.* Lean checks the proposition indexed as “state boundary normalized”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ConstructiveTensorTrain.lean:117](../../../../library/modules/constructivetensortrain/#decl-quantumblockencoding-constructivetensortrain-stateboundary-normalized). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/ConstructiveTensorTrainCompiler.lean

18 explicit public declarations, in source order.

:::definition "QuantumBlockEncoding.ConstructiveTensorTrainCompiler.absorbBoundary" (lean := "QuantumBlockEncoding.ConstructiveTensorTrainCompiler.absorbBoundary")
*Plain-English reading.* This definition gives the library's named construction or computation for “absorb boundary”. Absorb the signed residual boundary into the actual first core.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Absorb the signed residual boundary into the actual first core.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/ConstructiveTensorTrainCompiler.lean:23](../../../../library/modules/constructivetensortraincompiler/#decl-quantumblockencoding-constructivetensortraincompiler-absorbboundary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ConstructiveTensorTrainCompiler.absorbBoundary_canonical" (lean := "QuantumBlockEncoding.ConstructiveTensorTrainCompiler.absorbBoundary_canonical")
*Plain-English reading.* Lean checks the proposition indexed as “absorb boundary canonical”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ConstructiveTensorTrainCompiler.lean:27](../../../../library/modules/constructivetensortraincompiler/#decl-quantumblockencoding-constructivetensortraincompiler-absorbboundary-canonical). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ConstructiveTensorTrainCompiler.absorbBoundary_contract" (lean := "QuantumBlockEncoding.ConstructiveTensorTrainCompiler.absorbBoundary_contract")
*Plain-English reading.* Lean checks the proposition indexed as “absorb boundary contract”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ConstructiveTensorTrainCompiler.lean:33](../../../../library/modules/constructivetensortraincompiler/#decl-quantumblockencoding-constructivetensortraincompiler-absorbboundary-contract). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ConstructiveTensorTrainCompiler.absorbBoundary_maxBond" (lean := "QuantumBlockEncoding.ConstructiveTensorTrainCompiler.absorbBoundary_maxBond")
*Plain-English reading.* Lean checks the proposition indexed as “absorb boundary max bond”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ConstructiveTensorTrainCompiler.lean:39](../../../../library/modules/constructivetensortraincompiler/#decl-quantumblockencoding-constructivetensortraincompiler-absorbboundary-maxbond). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.ConstructiveTensorTrainCompiler.unitBoundary" (lean := "QuantumBlockEncoding.ConstructiveTensorTrainCompiler.unitBoundary")
*Plain-English reading.* This definition gives the library's named construction or computation for “unit boundary”. Concrete scalar-boundary canonical train, with no separate initialization circuit.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Concrete scalar-boundary canonical train, with no separate initialization circuit.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/ConstructiveTensorTrainCompiler.lean:45](../../../../library/modules/constructivetensortraincompiler/#decl-quantumblockencoding-constructivetensortraincompiler-unitboundary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ConstructiveTensorTrainCompiler.unitBoundary_canonical" (lean := "QuantumBlockEncoding.ConstructiveTensorTrainCompiler.unitBoundary_canonical")
*Plain-English reading.* Lean checks the proposition indexed as “unit boundary canonical”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ConstructiveTensorTrainCompiler.lean:49](../../../../library/modules/constructivetensortraincompiler/#decl-quantumblockencoding-constructivetensortraincompiler-unitboundary-canonical). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ConstructiveTensorTrainCompiler.unitBoundary_contract" (lean := "QuantumBlockEncoding.ConstructiveTensorTrainCompiler.unitBoundary_contract")
*Plain-English reading.* Lean checks the proposition indexed as “unit boundary contract”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ConstructiveTensorTrainCompiler.lean:55](../../../../library/modules/constructivetensortraincompiler/#decl-quantumblockencoding-constructivetensortraincompiler-unitboundary-contract). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ConstructiveTensorTrainCompiler.unitBoundary_maxBond_le" (lean := "QuantumBlockEncoding.ConstructiveTensorTrainCompiler.unitBoundary_maxBond_le")
*Plain-English reading.* Lean checks the proposition indexed as “unit boundary max bond le”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ConstructiveTensorTrainCompiler.lean:61](../../../../library/modules/constructivetensortraincompiler/#decl-quantumblockencoding-constructivetensortraincompiler-unitboundary-maxbond-le). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.ConstructiveTensorTrainCompiler.stage" (lean := "QuantumBlockEncoding.ConstructiveTensorTrainCompiler.stage")
*Plain-English reading.* This definition gives the library's named construction or computation for “stage”. Every local primitive list is computed from its actual completed SO matrix.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Every local primitive list is computed from its actual completed SO matrix.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/ConstructiveTensorTrainCompiler.lean:70](../../../../library/modules/constructivetensortraincompiler/#decl-quantumblockencoding-constructivetensortraincompiler-stage). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ConstructiveTensorTrainCompiler.stage_gateCount" (lean := "QuantumBlockEncoding.ConstructiveTensorTrainCompiler.stage_gateCount")
*Plain-English reading.* Lean checks the proposition indexed as “stage gate count”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ConstructiveTensorTrainCompiler.lean:74](../../../../library/modules/constructivetensortraincompiler/#decl-quantumblockencoding-constructivetensortraincompiler-stage-gatecount). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ConstructiveTensorTrainCompiler.stage_columns" (lean := "QuantumBlockEncoding.ConstructiveTensorTrainCompiler.stage_columns")
*Plain-English reading.* Lean checks the proposition indexed as “stage columns”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ConstructiveTensorTrainCompiler.lean:79](../../../../library/modules/constructivetensortraincompiler/#decl-quantumblockencoding-constructivetensortraincompiler-stage-columns). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.ConstructiveTensorTrainCompiler.compile" (lean := "QuantumBlockEncoding.ConstructiveTensorTrainCompiler.compile")
*Plain-English reading.* This definition gives the library's named construction or computation for “compile”. Final physical circuit: data occupy the low wires, and the clean bond the high wires.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Final physical circuit: data occupy the low wires, and the clean bond the high wires.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/ConstructiveTensorTrainCompiler.lean:90](../../../../library/modules/constructivetensortraincompiler/#decl-quantumblockencoding-constructivetensortraincompiler-compile). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ConstructiveTensorTrainCompiler.compile_gateCount" (lean := "QuantumBlockEncoding.ConstructiveTensorTrainCompiler.compile_gateCount")
*Plain-English reading.* Lean checks the proposition indexed as “compile gate count”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ConstructiveTensorTrainCompiler.lean:95](../../../../library/modules/constructivetensortraincompiler/#decl-quantumblockencoding-constructivetensortraincompiler-compile-gatecount). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ConstructiveTensorTrainCompiler.compile_depth" (lean := "QuantumBlockEncoding.ConstructiveTensorTrainCompiler.compile_depth")
*Plain-English reading.* Lean checks the proposition indexed as “compile depth”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ConstructiveTensorTrainCompiler.lean:100](../../../../library/modules/constructivetensortraincompiler/#decl-quantumblockencoding-constructivetensortraincompiler-compile-depth). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ConstructiveTensorTrainCompiler.compile_unitary" (lean := "QuantumBlockEncoding.ConstructiveTensorTrainCompiler.compile_unitary")
*Plain-English reading.* Lean checks the proposition indexed as “compile unitary”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ConstructiveTensorTrainCompiler.lean:105](../../../../library/modules/constructivetensortraincompiler/#decl-quantumblockencoding-constructivetensortraincompiler-compile-unitary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ConstructiveTensorTrainCompiler.compile_columns" (lean := "QuantumBlockEncoding.ConstructiveTensorTrainCompiler.compile_columns")
*Plain-English reading.* Lean checks the proposition indexed as “compile columns”; the hypotheses and conclusion in the code panel fix its exact scope. All data words and all bond sectors, including every non-clean output.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* All data words and all bond sectors, including every non-clean output.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ConstructiveTensorTrainCompiler.lean:112](../../../../library/modules/constructivetensortraincompiler/#decl-quantumblockencoding-constructivetensortraincompiler-compile-columns). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ConstructiveTensorTrainCompiler.compile_word" (lean := "QuantumBlockEncoding.ConstructiveTensorTrainCompiler.compile_word")
*Plain-English reading.* Lean checks the proposition indexed as “compile word”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ConstructiveTensorTrainCompiler.lean:123](../../../../library/modules/constructivetensortraincompiler/#decl-quantumblockencoding-constructivetensortraincompiler-compile-word). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ConstructiveTensorTrainCompiler.compile_spec" (lean := "QuantumBlockEncoding.ConstructiveTensorTrainCompiler.compile_spec")
*Plain-English reading.* Lean checks the proposition indexed as “compile spec”; the hypotheses and conclusion in the code panel fix its exact scope. Complete quantum correctness and resource certificate for the actual producer.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Complete quantum correctness and resource certificate for the actual producer.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ConstructiveTensorTrainCompiler.lean:134](../../../../library/modules/constructivetensortraincompiler/#decl-quantumblockencoding-constructivetensortraincompiler-compile-spec). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/ConstructiveThinLQ.lean

8 explicit public declarations, in source order.

:::definition "QuantumBlockEncoding.ConstructiveThinLQ.Factorization" (lean := "QuantumBlockEncoding.ConstructiveThinLQ.Factorization")
*Plain-English reading.* This record groups the data and proof fields needed for “factorization”. A proposition-valued field is a requirement until a constructor supplies it. Actual factors together with the same two equations as 'ThinLQ'.

*Formal status.* Data contract in the default import surface; proposition-valued fields are obligations, not automatically established facts.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Actual factors together with the same two equations as 'ThinLQ'.

*Declaration kind.* structure.

Source: [QuantumBlockEncoding/ConstructiveThinLQ.lean:20](../../../../library/modules/constructivethinlq/#decl-quantumblockencoding-constructivethinlq-factorization). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.ConstructiveThinLQ.wideR" (lean := "QuantumBlockEncoding.ConstructiveThinLQ.wideR")
*Plain-English reading.* This definition gives the library's named construction or computation for “wide r”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/ConstructiveThinLQ.lean:26](../../../../library/modules/constructivethinlq/#decl-quantumblockencoding-constructivethinlq-wider). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.ConstructiveThinLQ.wideQ" (lean := "QuantumBlockEncoding.ConstructiveThinLQ.wideQ")
*Plain-English reading.* This definition gives the library's named construction or computation for “wide q”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/ConstructiveThinLQ.lean:30](../../../../library/modules/constructivethinlq/#decl-quantumblockencoding-constructivethinlq-wideq). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ConstructiveThinLQ.wide_factorization" (lean := "QuantumBlockEncoding.ConstructiveThinLQ.wide_factorization")
*Plain-English reading.* Lean checks the proposition indexed as “wide factorization”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ConstructiveThinLQ.lean:34](../../../../library/modules/constructivethinlq/#decl-quantumblockencoding-constructivethinlq-wide-factorization). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ConstructiveThinLQ.wide_orthogonal" (lean := "QuantumBlockEncoding.ConstructiveThinLQ.wide_orthogonal")
*Plain-English reading.* Lean checks the proposition indexed as “wide orthogonal”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ConstructiveThinLQ.lean:51](../../../../library/modules/constructivethinlq/#decl-quantumblockencoding-constructivethinlq-wide-orthogonal). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.ConstructiveThinLQ.factorOfLE" (lean := "QuantumBlockEncoding.ConstructiveThinLQ.factorOfLE")
*Plain-English reading.* This definition gives the library's named construction or computation for “factor of le”. A deterministic wide-matrix supplier, including zero and repeated rows.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* A deterministic wide-matrix supplier, including zero and repeated rows.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/ConstructiveThinLQ.lean:61](../../../../library/modules/constructivethinlq/#decl-quantumblockencoding-constructivethinlq-factorofle). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.ConstructiveThinLQ.factor" (lean := "QuantumBlockEncoding.ConstructiveThinLQ.factor")
*Plain-English reading.* This definition gives the library's named construction or computation for “factor”. All shapes are handled by an explicit dimension comparison and recursion.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* All shapes are handled by an explicit dimension comparison and recursion.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/ConstructiveThinLQ.lean:69](../../../../library/modules/constructivethinlq/#decl-quantumblockencoding-constructivethinlq-factor). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ConstructiveThinLQ.factor_correct" (lean := "QuantumBlockEncoding.ConstructiveThinLQ.factor_correct")
*Plain-English reading.* Lean checks the proposition indexed as “factor correct”; the hypotheses and conclusion in the code panel fix its exact scope. The concrete output satisfies the existing all-shape thin-LQ contract.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The concrete output satisfies the existing all-shape thin-LQ contract.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ConstructiveThinLQ.lean:78](../../../../library/modules/constructivethinlq/#decl-quantumblockencoding-constructivethinlq-factor-correct). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/GrayBasis.lean

8 explicit public declarations, in source order.

:::definition "QuantumBlockEncoding.GrayBasis.twist" (lean := "QuantumBlockEncoding.GrayBasis.twist")
*Plain-English reading.* This definition gives the library's named construction or computation for “twist”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GrayBasis.lean:9](../../../../library/modules/graybasis/#decl-quantumblockencoding-graybasis-twist). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GrayBasis.equiv" (lean := "QuantumBlockEncoding.GrayBasis.equiv")
*Plain-English reading.* This definition gives the library's named construction or computation for “equiv”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GrayBasis.lean:15](../../../../library/modules/graybasis/#decl-quantumblockencoding-graybasis-equiv). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GrayBasis.headBit" (lean := "QuantumBlockEncoding.GrayBasis.headBit")
*Plain-English reading.* This definition gives the library's named construction or computation for “head bit”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GrayBasis.lean:24](../../../../library/modules/graybasis/#decl-quantumblockencoding-graybasis-headbit). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GrayBasis.equiv_head" (lean := "QuantumBlockEncoding.GrayBasis.equiv_head")
*Plain-English reading.* Lean checks the proposition indexed as “equiv head”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GrayBasis.lean:28](../../../../library/modules/graybasis/#decl-quantumblockencoding-graybasis-equiv-head). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GrayBasis.equiv_tail" (lean := "QuantumBlockEncoding.GrayBasis.equiv_tail")
*Plain-English reading.* Lean checks the proposition indexed as “equiv tail”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GrayBasis.lean:31](../../../../library/modules/graybasis/#decl-quantumblockencoding-graybasis-equiv-tail). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GrayBasis.headBit_even" (lean := "QuantumBlockEncoding.GrayBasis.headBit_even")
*Plain-English reading.* Lean checks the proposition indexed as “head bit even”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GrayBasis.lean:37](../../../../library/modules/graybasis/#decl-quantumblockencoding-graybasis-headbit-even). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GrayBasis.headBit_odd" (lean := "QuantumBlockEncoding.GrayBasis.headBit_odd")
*Plain-English reading.* Lean checks the proposition indexed as “head bit odd”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GrayBasis.lean:44](../../../../library/modules/graybasis/#decl-quantumblockencoding-graybasis-headbit-odd). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GrayBasis.adjacent" (lean := "QuantumBlockEncoding.GrayBasis.adjacent")
*Plain-English reading.* Lean checks the proposition indexed as “adjacent”; the hypotheses and conclusion in the code panel fix its exact scope. Numerically adjacent Gray labels differ by exactly one physical X action.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Numerically adjacent Gray labels differ by exactly one physical X action.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GrayBasis.lean:55](../../../../library/modules/graybasis/#decl-quantumblockencoding-graybasis-adjacent). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/GrayGivensCompiler.lean

26 explicit public declarations, in source order.

:::theorem "QuantumBlockEncoding.GrayGivensCompiler.bit_eq_or_flip" (lean := "QuantumBlockEncoding.GrayGivensCompiler.bit_eq_or_flip")
*Plain-English reading.* Lean checks the proposition indexed as “bit eq or flip”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GrayGivensCompiler.lean:12](../../../../library/modules/graygivenscompiler/#decl-quantumblockencoding-graygivenscompiler-bit-eq-or-flip). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GrayGivensCompiler.x_context" (lean := "QuantumBlockEncoding.GrayGivensCompiler.x_context")
*Plain-English reading.* Lean checks the proposition indexed as “x context”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GrayGivensCompiler.lean:15](../../../../library/modules/graygivenscompiler/#decl-quantumblockencoding-graygivenscompiler-x-context). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GrayGivensCompiler.same_context_iff" (lean := "QuantumBlockEncoding.GrayGivensCompiler.same_context_iff")
*Plain-English reading.* Lean checks the proposition indexed as “same context iff”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GrayGivensCompiler.lean:21](../../../../library/modules/graygivenscompiler/#decl-quantumblockencoding-graygivenscompiler-same-context-iff). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GrayGivensCompiler.controlsEquiv" (lean := "QuantumBlockEncoding.GrayGivensCompiler.controlsEquiv")
*Plain-English reading.* This definition gives the library's named construction or computation for “controls equiv”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GrayGivensCompiler.lean:39](../../../../library/modules/graygivenscompiler/#decl-quantumblockencoding-graygivenscompiler-controlsequiv). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GrayGivensCompiler.realTransport" (lean := "QuantumBlockEncoding.GrayGivensCompiler.realTransport")
*Plain-English reading.* This definition gives the library's named construction or computation for “real transport”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GrayGivensCompiler.lean:44](../../../../library/modules/graygivenscompiler/#decl-quantumblockencoding-graygivenscompiler-realtransport). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GrayGivensCompiler.transport" (lean := "QuantumBlockEncoding.GrayGivensCompiler.transport")
*Plain-English reading.* This definition gives the library's named construction or computation for “transport”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GrayGivensCompiler.lean:49](../../../../library/modules/graygivenscompiler/#decl-quantumblockencoding-graygivenscompiler-transport). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GrayGivensCompiler.edge_plane_transport" (lean := "QuantumBlockEncoding.GrayGivensCompiler.edge_plane_transport")
*Plain-English reading.* Lean checks the proposition indexed as “edge plane transport”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GrayGivensCompiler.lean:55](../../../../library/modules/graygivenscompiler/#decl-quantumblockencoding-graygivenscompiler-edge-plane-transport). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GrayGivensCompiler.stepTarget" (lean := "QuantumBlockEncoding.GrayGivensCompiler.stepTarget")
*Plain-English reading.* This definition gives the library's named construction or computation for “step target”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GrayGivensCompiler.lean:88](../../../../library/modules/graygivenscompiler/#decl-quantumblockencoding-graygivenscompiler-steptarget). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GrayGivensCompiler.stepTarget_action" (lean := "QuantumBlockEncoding.GrayGivensCompiler.stepTarget_action")
*Plain-English reading.* Lean checks the proposition indexed as “step target action”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GrayGivensCompiler.lean:91](../../../../library/modules/graygivenscompiler/#decl-quantumblockencoding-graygivenscompiler-steptarget-action). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GrayGivensCompiler.selectedStep" (lean := "QuantumBlockEncoding.GrayGivensCompiler.selectedStep")
*Plain-English reading.* This definition gives the library's named construction or computation for “selected step”. One actual selected-rotation instruction.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* One actual selected-rotation instruction. Reversed target-bit order negates the RY angle, while every non-target wire is an explicit control.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GrayGivensCompiler.lean:98](../../../../library/modules/graygivenscompiler/#decl-quantumblockencoding-graygivenscompiler-selectedstep). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GrayGivensCompiler.selectedStep_matrix" (lean := "QuantumBlockEncoding.GrayGivensCompiler.selectedStep_matrix")
*Plain-English reading.* Lean checks the proposition indexed as “selected step matrix”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GrayGivensCompiler.lean:107](../../../../library/modules/graygivenscompiler/#decl-quantumblockencoding-graygivenscompiler-selectedstep-matrix). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GrayGivensCompiler.compileSteps" (lean := "QuantumBlockEncoding.GrayGivensCompiler.compileSteps")
*Plain-English reading.* This definition gives the library's named construction or computation for “compile steps”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GrayGivensCompiler.lean:123](../../../../library/modules/graygivenscompiler/#decl-quantumblockencoding-graygivenscompiler-compilesteps). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GrayGivensCompiler.compileSteps_eval" (lean := "QuantumBlockEncoding.GrayGivensCompiler.compileSteps_eval")
*Plain-English reading.* Lean checks the proposition indexed as “compile steps eval”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GrayGivensCompiler.lean:126](../../../../library/modules/graygivenscompiler/#decl-quantumblockencoding-graygivenscompiler-compilesteps-eval). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GrayGivensCompiler.compileSteps_gateCount" (lean := "QuantumBlockEncoding.GrayGivensCompiler.compileSteps_gateCount")
*Plain-English reading.* Lean checks the proposition indexed as “compile steps gate count”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GrayGivensCompiler.lean:136](../../../../library/modules/graygivenscompiler/#decl-quantumblockencoding-graygivenscompiler-compilesteps-gatecount). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GrayGivensCompiler.compileSOGray" (lean := "QuantumBlockEncoding.GrayGivensCompiler.compileSOGray")
*Plain-English reading.* This definition gives the library's named construction or computation for “compile so gray”. Input coordinates here are Gray-ordered natural indices.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Input coordinates here are Gray-ordered natural indices.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GrayGivensCompiler.lean:141](../../../../library/modules/graygivenscompiler/#decl-quantumblockencoding-graygivenscompiler-compilesogray). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GrayGivensCompiler.compileSOGray_eval" (lean := "QuantumBlockEncoding.GrayGivensCompiler.compileSOGray_eval")
*Plain-English reading.* Lean checks the proposition indexed as “compile so gray eval”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GrayGivensCompiler.lean:145](../../../../library/modules/graygivenscompiler/#decl-quantumblockencoding-graygivenscompiler-compilesogray-eval). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GrayGivensCompiler.compileSOGray_cubic_bound" (lean := "QuantumBlockEncoding.GrayGivensCompiler.compileSOGray_cubic_bound")
*Plain-English reading.* Lean checks the proposition indexed as “compile so gray cubic bound”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GrayGivensCompiler.lean:151](../../../../library/modules/graygivenscompiler/#decl-quantumblockencoding-graygivenscompiler-compilesogray-cubic-bound). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GrayGivensCompiler.grayCoordinates" (lean := "QuantumBlockEncoding.GrayGivensCompiler.grayCoordinates")
*Plain-English reading.* This definition gives the library's named construction or computation for “gray coordinates”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GrayGivensCompiler.lean:159](../../../../library/modules/graygivenscompiler/#decl-quantumblockencoding-graygivenscompiler-graycoordinates). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.GrayGivensCompiler.compileSO" (lean := "QuantumBlockEncoding.GrayGivensCompiler.compileSO")
*Plain-English reading.* This definition gives the library's named construction or computation for “compile so”. A circuit on the original named wires; Gray order is internal only.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* A circuit on the original named wires; Gray order is internal only.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/GrayGivensCompiler.lean:165](../../../../library/modules/graygivenscompiler/#decl-quantumblockencoding-graygivenscompiler-compileso). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GrayGivensCompiler.grayCoordinates_orthogonal" (lean := "QuantumBlockEncoding.GrayGivensCompiler.grayCoordinates_orthogonal")
*Plain-English reading.* Lean checks the proposition indexed as “gray coordinates orthogonal”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GrayGivensCompiler.lean:169](../../../../library/modules/graygivenscompiler/#decl-quantumblockencoding-graygivenscompiler-graycoordinates-orthogonal). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GrayGivensCompiler.grayCoordinates_det" (lean := "QuantumBlockEncoding.GrayGivensCompiler.grayCoordinates_det")
*Plain-English reading.* Lean checks the proposition indexed as “gray coordinates det”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GrayGivensCompiler.lean:177](../../../../library/modules/graygivenscompiler/#decl-quantumblockencoding-graygivenscompiler-graycoordinates-det). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GrayGivensCompiler.compileSO_eval" (lean := "QuantumBlockEncoding.GrayGivensCompiler.compileSO_eval")
*Plain-English reading.* Lean checks the proposition indexed as “compile so eval”; the hypotheses and conclusion in the code panel fix its exact scope. No assumed plane realization or Gray adjacency: the actual finite primitive list realizes the original real SO matrix embedded in complex amplitudes.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* No assumed plane realization or Gray adjacency: the actual finite primitive list realizes the original real SO matrix embedded in complex amplitudes.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GrayGivensCompiler.lean:184](../../../../library/modules/graygivenscompiler/#decl-quantumblockencoding-graygivenscompiler-compileso-eval). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GrayGivensCompiler.compileSO_cubic_bound" (lean := "QuantumBlockEncoding.GrayGivensCompiler.compileSO_cubic_bound")
*Plain-English reading.* Lean checks the proposition indexed as “compile so cubic bound”; the hypotheses and conclusion in the code panel fix its exact scope. With 'S=2^q', the exact recursive selected-RY backend needs at most '6\*S^3' primitive gates and no oracle calls, on the existing 'q+1' wires.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* With 'S=2^q', the exact recursive selected-RY backend needs at most '6\*S^3' primitive gates and no oracle calls, on the existing 'q+1' wires.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GrayGivensCompiler.lean:196](../../../../library/modules/graygivenscompiler/#decl-quantumblockencoding-graygivenscompiler-compileso-cubic-bound). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GrayGivensCompiler.compileSO_gateCount" (lean := "QuantumBlockEncoding.GrayGivensCompiler.compileSO_gateCount")
*Plain-English reading.* Lean checks the proposition indexed as “compile so gate count”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GrayGivensCompiler.lean:201](../../../../library/modules/graygivenscompiler/#decl-quantumblockencoding-graygivenscompiler-compileso-gatecount). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GrayGivensCompiler.compileSO_ryCount" (lean := "QuantumBlockEncoding.GrayGivensCompiler.compileSO_ryCount")
*Plain-English reading.* Lean checks the proposition indexed as “compile so ry count”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GrayGivensCompiler.lean:207](../../../../library/modules/graygivenscompiler/#decl-quantumblockencoding-graygivenscompiler-compileso-rycount). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.GrayGivensCompiler.compileSO_cxCount" (lean := "QuantumBlockEncoding.GrayGivensCompiler.compileSO_cxCount")
*Plain-English reading.* Lean checks the proposition indexed as “compile so cx count”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/GrayGivensCompiler.lean:213](../../../../library/modules/graygivenscompiler/#decl-quantumblockencoding-graygivenscompiler-compileso-cxcount). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/MatrixProductChain.lean

13 explicit public declarations, in source order.

:::definition "QuantumBlockEncoding.MatrixProductChain.Kernel" (lean := "QuantumBlockEncoding.MatrixProductChain.Kernel")
*Plain-English reading.* This abbreviation gives a shorter name to the type or expression used for “kernel”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* abbrev.

Source: [QuantumBlockEncoding/MatrixProductChain.lean:11](../../../../library/modules/matrixproductchain/#decl-quantumblockencoding-matrixproductchain-kernel). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.MatrixProductChain.readout" (lean := "QuantumBlockEncoding.MatrixProductChain.readout")
*Plain-English reading.* This definition gives the library's named construction or computation for “readout”. Chronological finite matrix contraction, read from the first emitted bit.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Chronological finite matrix contraction, read from the first emitted bit.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/MatrixProductChain.lean:14](../../../../library/modules/matrixproductchain/#decl-quantumblockencoding-matrixproductchain-readout). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.MatrixProductChain.tailChain" (lean := "QuantumBlockEncoding.MatrixProductChain.tailChain")
*Plain-English reading.* This definition gives the library's named construction or computation for “tail chain”. Absorb the terminal vector into the last core, leaving terminal rank one.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Absorb the terminal vector into the last core, leaving terminal rank one.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/MatrixProductChain.lean:20](../../../../library/modules/matrixproductchain/#decl-quantumblockencoding-matrixproductchain-tailchain). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.MatrixProductChain.tailChain_contract" (lean := "QuantumBlockEncoding.MatrixProductChain.tailChain_contract")
*Plain-English reading.* Lean checks the proposition indexed as “tail chain contract”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/MatrixProductChain.lean:26](../../../../library/modules/matrixproductchain/#decl-quantumblockencoding-matrixproductchain-tailchain-contract). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.MatrixProductChain.closeLeft" (lean := "QuantumBlockEncoding.MatrixProductChain.closeLeft")
*Plain-English reading.* This definition gives the library's named construction or computation for “close left”. Contract an explicit left boundary into the first core only.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Contract an explicit left boundary into the first core only.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/MatrixProductChain.lean:38](../../../../library/modules/matrixproductchain/#decl-quantumblockencoding-matrixproductchain-closeleft). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.MatrixProductChain.closeLeft_contract" (lean := "QuantumBlockEncoding.MatrixProductChain.closeLeft_contract")
*Plain-English reading.* Lean checks the proposition indexed as “close left contract”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/MatrixProductChain.lean:42](../../../../library/modules/matrixproductchain/#decl-quantumblockencoding-matrixproductchain-closeleft-contract). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.MatrixProductChain.ofKernel" (lean := "QuantumBlockEncoding.MatrixProductChain.ofKernel")
*Plain-English reading.* This definition gives the library's named construction or computation for “of kernel”. A scalar-boundary train whose coefficients are built from small matrices.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* A scalar-boundary train whose coefficients are built from small matrices.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/MatrixProductChain.lean:52](../../../../library/modules/matrixproductchain/#decl-quantumblockencoding-matrixproductchain-ofkernel). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.MatrixProductChain.ofKernel_contract" (lean := "QuantumBlockEncoding.MatrixProductChain.ofKernel_contract")
*Plain-English reading.* Lean checks the proposition indexed as “of kernel contract”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/MatrixProductChain.lean:55](../../../../library/modules/matrixproductchain/#decl-quantumblockencoding-matrixproductchain-ofkernel-contract). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.MatrixProductChain.tailChain_maxBond" (lean := "QuantumBlockEncoding.MatrixProductChain.tailChain_maxBond")
*Plain-English reading.* Lean checks the proposition indexed as “tail chain max bond”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/MatrixProductChain.lean:62](../../../../library/modules/matrixproductchain/#decl-quantumblockencoding-matrixproductchain-tailchain-maxbond). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.MatrixProductChain.ofKernel_maxBond" (lean := "QuantumBlockEncoding.MatrixProductChain.ofKernel_maxBond")
*Plain-English reading.* Lean checks the proposition indexed as “of kernel max bond”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/MatrixProductChain.lean:68](../../../../library/modules/matrixproductchain/#decl-quantumblockencoding-matrixproductchain-ofkernel-maxbond). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.MatrixProductChain.storedScalars" (lean := "QuantumBlockEncoding.MatrixProductChain.storedScalars")
*Plain-English reading.* This definition gives the library's named construction or computation for “stored scalars”. Number of entries in the explicit dense \*local\* cores.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Number of entries in the explicit dense \*local\* cores.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/MatrixProductChain.lean:75](../../../../library/modules/matrixproductchain/#decl-quantumblockencoding-matrixproductchain-storedscalars). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.MatrixProductChain.storedScalars_le" (lean := "QuantumBlockEncoding.MatrixProductChain.storedScalars_le")
*Plain-English reading.* Lean checks the proposition indexed as “stored scalars le”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/MatrixProductChain.lean:79](../../../../library/modules/matrixproductchain/#decl-quantumblockencoding-matrixproductchain-storedscalars-le). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.MatrixProductChain.ofKernel_storedScalars" (lean := "QuantumBlockEncoding.MatrixProductChain.ofKernel_storedScalars")
*Plain-English reading.* Lean checks the proposition indexed as “of kernel stored scalars”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/MatrixProductChain.lean:95](../../../../library/modules/matrixproductchain/#decl-quantumblockencoding-matrixproductchain-ofkernel-storedscalars). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/ModularAdder3.lean

22 explicit public declarations, in source order.

:::definition "QuantumBlockEncoding.primitiveXProgram" (lean := "QuantumBlockEncoding.primitiveXProgram")
*Plain-English reading.* This definition gives the library's named construction or computation for “primitive x program”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/ModularAdder3.lean:16](../../../../library/modules/modularadder3/#decl-quantumblockencoding-primitivexprogram). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.primitiveXProgram_eval" (lean := "QuantumBlockEncoding.primitiveXProgram_eval")
*Plain-English reading.* Lean checks the proposition indexed as “primitive x program eval”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ModularAdder3.lean:21](../../../../library/modules/modularadder3/#decl-quantumblockencoding-primitivexprogram-eval). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.compileReversibleGate" (lean := "QuantumBlockEncoding.compileReversibleGate")
*Plain-English reading.* This definition gives the library's named construction or computation for “compile reversible gate”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/ModularAdder3.lean:31](../../../../library/modules/modularadder3/#decl-quantumblockencoding-compilereversiblegate). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.compileReversibleGate_eval" (lean := "QuantumBlockEncoding.compileReversibleGate_eval")
*Plain-English reading.* Lean checks the proposition indexed as “compile reversible gate eval”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ModularAdder3.lean:40](../../../../library/modules/modularadder3/#decl-quantumblockencoding-compilereversiblegate-eval). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.compileReversibleProgram" (lean := "QuantumBlockEncoding.compileReversibleProgram")
*Plain-English reading.* This definition gives the library's named construction or computation for “compile reversible program”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/ModularAdder3.lean:52](../../../../library/modules/modularadder3/#decl-quantumblockencoding-compilereversibleprogram). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.compileReversibleProgram_eval" (lean := "QuantumBlockEncoding.compileReversibleProgram_eval")
*Plain-English reading.* Lean checks the proposition indexed as “compile reversible program eval”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ModularAdder3.lean:58](../../../../library/modules/modularadder3/#decl-quantumblockencoding-compilereversibleprogram-eval). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.cleanC3XReversibleProgram" (lean := "QuantumBlockEncoding.cleanC3XReversibleProgram")
*Plain-English reading.* This definition gives the library's named construction or computation for “clean c 3 x reversible program”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/ModularAdder3.lean:75](../../../../library/modules/modularadder3/#decl-quantumblockencoding-cleanc3xreversibleprogram). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.cleanC3XBasisEquiv" (lean := "QuantumBlockEncoding.cleanC3XBasisEquiv")
*Plain-English reading.* This definition gives the library's named construction or computation for “clean c 3 x basis equiv”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/ModularAdder3.lean:88](../../../../library/modules/modularadder3/#decl-quantumblockencoding-cleanc3xbasisequiv). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.c3xBasisAction" (lean := "QuantumBlockEncoding.c3xBasisAction")
*Plain-English reading.* This definition gives the library's named construction or computation for “c 3 x basis action”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/ModularAdder3.lean:102](../../../../library/modules/modularadder3/#decl-quantumblockencoding-c3xbasisaction). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.cleanC3XBasisAction" (lean := "QuantumBlockEncoding.cleanC3XBasisAction")
*Plain-English reading.* Lean checks the proposition indexed as “clean c 3 x basis action”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ModularAdder3.lean:109](../../../../library/modules/modularadder3/#decl-quantumblockencoding-cleanc3xbasisaction). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.cleanC3XWorkspaceClean" (lean := "QuantumBlockEncoding.cleanC3XWorkspaceClean")
*Plain-English reading.* Lean checks the proposition indexed as “clean c 3 x workspace clean”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ModularAdder3.lean:143](../../../../library/modules/modularadder3/#decl-quantumblockencoding-cleanc3xworkspaceclean). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.cleanC3XPrimitiveProgram" (lean := "QuantumBlockEncoding.cleanC3XPrimitiveProgram")
*Plain-English reading.* This definition gives the library's named construction or computation for “clean c 3 x primitive program”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/ModularAdder3.lean:165](../../../../library/modules/modularadder3/#decl-quantumblockencoding-cleanc3xprimitiveprogram). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.cleanC3XPrimitiveProgram_eval" (lean := "QuantumBlockEncoding.cleanC3XPrimitiveProgram_eval")
*Plain-English reading.* Lean checks the proposition indexed as “clean c 3 x primitive program eval”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ModularAdder3.lean:178](../../../../library/modules/modularadder3/#decl-quantumblockencoding-cleanc3xprimitiveprogram-eval). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.littleEndian3Value" (lean := "QuantumBlockEncoding.littleEndian3Value")
*Plain-English reading.* This definition gives the library's named construction or computation for “little endian 3 value”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/ModularAdder3.lean:198](../../../../library/modules/modularadder3/#decl-quantumblockencoding-littleendian3value). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.modularAdd3ReversibleProgram" (lean := "QuantumBlockEncoding.modularAdd3ReversibleProgram")
*Plain-English reading.* This definition gives the library's named construction or computation for “modular add 3 reversible program”. Wire order is 'a0,a1,a2,b0,b1,b2,work'.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Wire order is 'a0,a1,a2,b0,b1,b2,work'.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/ModularAdder3.lean:203](../../../../library/modules/modularadder3/#decl-quantumblockencoding-modularadd3reversibleprogram). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.modularAdd3BasisEquiv" (lean := "QuantumBlockEncoding.modularAdd3BasisEquiv")
*Plain-English reading.* This definition gives the library's named construction or computation for “modular add 3 basis equiv”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/ModularAdder3.lean:214](../../../../library/modules/modularadder3/#decl-quantumblockencoding-modularadd3basisequiv). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.modularAdd3_cleanAction" (lean := "QuantumBlockEncoding.modularAdd3_cleanAction")
*Plain-English reading.* Lean checks the proposition indexed as “modular add 3 clean action”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ModularAdder3.lean:217](../../../../library/modules/modularadder3/#decl-quantumblockencoding-modularadd3-cleanaction). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.modularAdd3PrimitiveProgram" (lean := "QuantumBlockEncoding.modularAdd3PrimitiveProgram")
*Plain-English reading.* This definition gives the library's named construction or computation for “modular add 3 primitive program”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/ModularAdder3.lean:227](../../../../library/modules/modularadder3/#decl-quantumblockencoding-modularadd3primitiveprogram). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.modularAdd3Primitive_eval" (lean := "QuantumBlockEncoding.modularAdd3Primitive_eval")
*Plain-English reading.* Lean checks the proposition indexed as “modular add 3 primitive eval”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ModularAdder3.lean:230](../../../../library/modules/modularadder3/#decl-quantumblockencoding-modularadd3primitive-eval). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.modularAdd3Primitive_workspaceClean" (lean := "QuantumBlockEncoding.modularAdd3Primitive_workspaceClean")
*Plain-English reading.* Lean checks the proposition indexed as “modular add 3 primitive workspace clean”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ModularAdder3.lean:235](../../../../library/modules/modularadder3/#decl-quantumblockencoding-modularadd3primitive-workspaceclean). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.modularAdd3Primitive_resource_faithful" (lean := "QuantumBlockEncoding.modularAdd3Primitive_resource_faithful")
*Plain-English reading.* Lean checks the proposition indexed as “modular add 3 primitive resource faithful”; the hypotheses and conclusion in the code panel fix its exact scope. The resource is definitionally computed from the emitted primitive list.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The resource is definitionally computed from the emitted primitive list.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ModularAdder3.lean:241](../../../../library/modules/modularadder3/#decl-quantumblockencoding-modularadd3primitive-resource-faithful). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.modularAdd3Primitive_oracleCalls_eq_zero" (lean := "QuantumBlockEncoding.modularAdd3Primitive_oracleCalls_eq_zero")
*Plain-English reading.* Lean checks the proposition indexed as “modular add 3 primitive oracle calls eq zero”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ModularAdder3.lean:245](../../../../library/modules/modularadder3/#decl-quantumblockencoding-modularadd3primitive-oraclecalls-eq-zero). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/PrimitiveBasisLE.lean

33 explicit public declarations, in source order.

:::definition "QuantumBlockEncoding.primitiveBasisLEEquiv" (lean := "QuantumBlockEncoding.primitiveBasisLEEquiv")
*Plain-English reading.* This definition gives the library's named construction or computation for “primitive basis le equiv”. Convert named primitive bits to a flat little-endian matrix index.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Convert named primitive bits to a flat little-endian matrix index.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PrimitiveBasisLE.lean:15](../../../../library/modules/primitivebasisle/#decl-quantumblockencoding-primitivebasisleequiv). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.primitiveBasisLEEquiv_zero_apply" (lean := "QuantumBlockEncoding.primitiveBasisLEEquiv_zero_apply")
*Plain-English reading.* Lean checks the proposition indexed as “primitive basis le equiv zero apply”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveBasisLE.lean:28](../../../../library/modules/primitivebasisle/#decl-quantumblockencoding-primitivebasisleequiv-zero-apply). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.primitiveBasisLEEquiv_succ_value" (lean := "QuantumBlockEncoding.primitiveBasisLEEquiv_succ_value")
*Plain-English reading.* Lean checks the proposition indexed as “primitive basis le equiv succ value”; the hypotheses and conclusion in the code panel fix its exact scope. The recursive equation makes the little-endian convention inspectable.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The recursive equation makes the little-endian convention inspectable.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveBasisLE.lean:33](../../../../library/modules/primitivebasisle/#decl-quantumblockencoding-primitivebasisleequiv-succ-value). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.primitiveBasisLEEquiv_six_value" (lean := "QuantumBlockEncoding.primitiveBasisLEEquiv_six_value")
*Plain-English reading.* Lean checks the proposition indexed as “primitive basis le equiv six value”; the hypotheses and conclusion in the code panel fix its exact scope. Six-wire expansion used by the fixed Robin executable benchmark.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Six-wire expansion used by the fixed Robin executable benchmark.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveBasisLE.lean:41](../../../../library/modules/primitivebasisle/#decl-quantumblockencoding-primitivebasisleequiv-six-value). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.primitiveBits2LE" (lean := "QuantumBlockEncoding.primitiveBits2LE")
*Plain-English reading.* This definition gives the library's named construction or computation for “primitive bits 2 le”. Explicit inverse used by finite two-wire state-preparation proofs.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Explicit inverse used by finite two-wire state-preparation proofs.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PrimitiveBasisLE.lean:48](../../../../library/modules/primitivebasisle/#decl-quantumblockencoding-primitivebits2le). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.primitiveBasisLEEquiv_two_symm" (lean := "QuantumBlockEncoding.primitiveBasisLEEquiv_two_symm")
*Plain-English reading.* Lean checks the proposition indexed as “primitive basis le equiv two symm”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveBasisLE.lean:52](../../../../library/modules/primitivebasisle/#decl-quantumblockencoding-primitivebasisleequiv-two-symm). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.primitiveBasisLEEquiv_two_symm_wire_zero" (lean := "QuantumBlockEncoding.primitiveBasisLEEquiv_two_symm_wire_zero")
*Plain-English reading.* Lean checks the proposition indexed as “primitive basis le equiv two symm wire zero”; the hypotheses and conclusion in the code panel fix its exact scope. Fixed-width coordinate reductions whose domain exactly matches the 'gridSize'-indexed finite matrix backend.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Fixed-width coordinate reductions whose domain exactly matches the 'gridSize'-indexed finite matrix backend.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveBasisLE.lean:58](../../../../library/modules/primitivebasisle/#decl-quantumblockencoding-primitivebasisleequiv-two-symm-wire-zero). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.primitiveBasisLEEquiv_two_symm_wire_one" (lean := "QuantumBlockEncoding.primitiveBasisLEEquiv_two_symm_wire_one")
*Plain-English reading.* Lean checks the proposition indexed as “primitive basis le equiv two symm wire one”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveBasisLE.lean:64](../../../../library/modules/primitivebasisle/#decl-quantumblockencoding-primitivebasisleequiv-two-symm-wire-one). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.primitiveBasisLEEquiv_two_symm_0" (lean := "QuantumBlockEncoding.primitiveBasisLEEquiv_two_symm_0")
*Plain-English reading.* Lean checks the proposition indexed as “primitive basis le equiv two symm 0”; the hypotheses and conclusion in the code panel fix its exact scope. Concrete inverse images used after 'fin\_cases'; these avoid relying on type normalization between 'Fin (gridSize 2)' and 'Fin 4'.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Concrete inverse images used after 'fin\_cases'; these avoid relying on type normalization between 'Fin (gridSize 2)' and 'Fin 4'.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveBasisLE.lean:72](../../../../library/modules/primitivebasisle/#decl-quantumblockencoding-primitivebasisleequiv-two-symm-0). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.primitiveBasisLEEquiv_two_symm_1" (lean := "QuantumBlockEncoding.primitiveBasisLEEquiv_two_symm_1")
*Plain-English reading.* Lean checks the proposition indexed as “primitive basis le equiv two symm 1”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveBasisLE.lean:76](../../../../library/modules/primitivebasisle/#decl-quantumblockencoding-primitivebasisleequiv-two-symm-1). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.primitiveBasisLEEquiv_two_symm_2" (lean := "QuantumBlockEncoding.primitiveBasisLEEquiv_two_symm_2")
*Plain-English reading.* Lean checks the proposition indexed as “primitive basis le equiv two symm 2”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveBasisLE.lean:80](../../../../library/modules/primitivebasisle/#decl-quantumblockencoding-primitivebasisleequiv-two-symm-2). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.primitiveBasisLEEquiv_two_symm_3" (lean := "QuantumBlockEncoding.primitiveBasisLEEquiv_two_symm_3")
*Plain-English reading.* Lean checks the proposition indexed as “primitive basis le equiv two symm 3”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveBasisLE.lean:84](../../../../library/modules/primitivebasisle/#decl-quantumblockencoding-primitivebasisleequiv-two-symm-3). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.primitiveBits2LEWithout" (lean := "QuantumBlockEncoding.primitiveBits2LEWithout")
*Plain-English reading.* This definition gives the library's named construction or computation for “primitive bits 2 le without”. Encode the non-target wire of a two-qubit little-endian basis state.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Encode the non-target wire of a two-qubit little-endian basis state.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PrimitiveBasisLE.lean:90](../../../../library/modules/primitivebasisle/#decl-quantumblockencoding-primitivebits2lewithout). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.primitiveBits2LEGridWithout" (lean := "QuantumBlockEncoding.primitiveBits2LEGridWithout")
*Plain-English reading.* This definition gives the library's named construction or computation for “primitive bits 2 le grid without”. Same context code, but with the unreduced 'gridSize' domain used by the concrete matrix semantics.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Same context code, but with the unreduced 'gridSize' domain used by the concrete matrix semantics.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PrimitiveBasisLE.lean:97](../../../../library/modules/primitivebasisle/#decl-quantumblockencoding-primitivebits2legridwithout). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.splitPrimitiveWire_primitiveBits2LE_context_eq" (lean := "QuantumBlockEncoding.splitPrimitiveWire_primitiveBits2LE_context_eq")
*Plain-English reading.* Lean checks the proposition indexed as “split primitive wire primitive bits 2 le context eq”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveBasisLE.lean:103](../../../../library/modules/primitivebasisle/#decl-quantumblockencoding-splitprimitivewire-primitivebits2le-context-eq). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.splitPrimitiveWire_primitiveBasisLEEquiv_two_symm_context_eq" (lean := "QuantumBlockEncoding.splitPrimitiveWire_primitiveBasisLEEquiv_two_symm_context_eq")
*Plain-English reading.* Lean checks the proposition indexed as “split primitive wire primitive basis le equiv two symm context eq”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveBasisLE.lean:111](../../../../library/modules/primitivebasisle/#decl-quantumblockencoding-splitprimitivewire-primitivebasisleequiv-two-symm-context-eq). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.primitiveBits3LE" (lean := "QuantumBlockEncoding.primitiveBits3LE")
*Plain-English reading.* This definition gives the library's named construction or computation for “primitive bits 3 le”. Explicit inverse used by finite three-wire compiler proofs.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Explicit inverse used by finite three-wire compiler proofs.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PrimitiveBasisLE.lean:120](../../../../library/modules/primitivebasisle/#decl-quantumblockencoding-primitivebits3le). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.primitiveBasisLEEquiv_three_symm" (lean := "QuantumBlockEncoding.primitiveBasisLEEquiv_three_symm")
*Plain-English reading.* Lean checks the proposition indexed as “primitive basis le equiv three symm”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveBasisLE.lean:125](../../../../library/modules/primitivebasisle/#decl-quantumblockencoding-primitivebasisleequiv-three-symm). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.primitiveBasisLEEquiv_three_symm_wire_zero" (lean := "QuantumBlockEncoding.primitiveBasisLEEquiv_three_symm_wire_zero")
*Plain-English reading.* Lean checks the proposition indexed as “primitive basis le equiv three symm wire zero”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveBasisLE.lean:129](../../../../library/modules/primitivebasisle/#decl-quantumblockencoding-primitivebasisleequiv-three-symm-wire-zero). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.primitiveBasisLEEquiv_three_symm_wire_one" (lean := "QuantumBlockEncoding.primitiveBasisLEEquiv_three_symm_wire_one")
*Plain-English reading.* Lean checks the proposition indexed as “primitive basis le equiv three symm wire one”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveBasisLE.lean:135](../../../../library/modules/primitivebasisle/#decl-quantumblockencoding-primitivebasisleequiv-three-symm-wire-one). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.primitiveBasisLEEquiv_three_symm_wire_two" (lean := "QuantumBlockEncoding.primitiveBasisLEEquiv_three_symm_wire_two")
*Plain-English reading.* Lean checks the proposition indexed as “primitive basis le equiv three symm wire two”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveBasisLE.lean:141](../../../../library/modules/primitivebasisle/#decl-quantumblockencoding-primitivebasisleequiv-three-symm-wire-two). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.primitiveBasisLEEquiv_three_symm_0" (lean := "QuantumBlockEncoding.primitiveBasisLEEquiv_three_symm_0")
*Plain-English reading.* Lean checks the proposition indexed as “primitive basis le equiv three symm 0”; the hypotheses and conclusion in the code panel fix its exact scope. Concrete inverse images for all eight three-qubit basis states.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Concrete inverse images for all eight three-qubit basis states.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveBasisLE.lean:148](../../../../library/modules/primitivebasisle/#decl-quantumblockencoding-primitivebasisleequiv-three-symm-0). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.primitiveBasisLEEquiv_three_symm_1" (lean := "QuantumBlockEncoding.primitiveBasisLEEquiv_three_symm_1")
*Plain-English reading.* Lean checks the proposition indexed as “primitive basis le equiv three symm 1”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveBasisLE.lean:152](../../../../library/modules/primitivebasisle/#decl-quantumblockencoding-primitivebasisleequiv-three-symm-1). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.primitiveBasisLEEquiv_three_symm_2" (lean := "QuantumBlockEncoding.primitiveBasisLEEquiv_three_symm_2")
*Plain-English reading.* Lean checks the proposition indexed as “primitive basis le equiv three symm 2”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveBasisLE.lean:156](../../../../library/modules/primitivebasisle/#decl-quantumblockencoding-primitivebasisleequiv-three-symm-2). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.primitiveBasisLEEquiv_three_symm_3" (lean := "QuantumBlockEncoding.primitiveBasisLEEquiv_three_symm_3")
*Plain-English reading.* Lean checks the proposition indexed as “primitive basis le equiv three symm 3”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveBasisLE.lean:160](../../../../library/modules/primitivebasisle/#decl-quantumblockencoding-primitivebasisleequiv-three-symm-3). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.primitiveBasisLEEquiv_three_symm_4" (lean := "QuantumBlockEncoding.primitiveBasisLEEquiv_three_symm_4")
*Plain-English reading.* Lean checks the proposition indexed as “primitive basis le equiv three symm 4”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveBasisLE.lean:164](../../../../library/modules/primitivebasisle/#decl-quantumblockencoding-primitivebasisleequiv-three-symm-4). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.primitiveBasisLEEquiv_three_symm_5" (lean := "QuantumBlockEncoding.primitiveBasisLEEquiv_three_symm_5")
*Plain-English reading.* Lean checks the proposition indexed as “primitive basis le equiv three symm 5”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveBasisLE.lean:168](../../../../library/modules/primitivebasisle/#decl-quantumblockencoding-primitivebasisleequiv-three-symm-5). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.primitiveBasisLEEquiv_three_symm_6" (lean := "QuantumBlockEncoding.primitiveBasisLEEquiv_three_symm_6")
*Plain-English reading.* Lean checks the proposition indexed as “primitive basis le equiv three symm 6”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveBasisLE.lean:172](../../../../library/modules/primitivebasisle/#decl-quantumblockencoding-primitivebasisleequiv-three-symm-6). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.primitiveBasisLEEquiv_three_symm_7" (lean := "QuantumBlockEncoding.primitiveBasisLEEquiv_three_symm_7")
*Plain-English reading.* Lean checks the proposition indexed as “primitive basis le equiv three symm 7”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveBasisLE.lean:176](../../../../library/modules/primitivebasisle/#decl-quantumblockencoding-primitivebasisleequiv-three-symm-7). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.primitiveBits3LEWithout" (lean := "QuantumBlockEncoding.primitiveBits3LEWithout")
*Plain-English reading.* This definition gives the library's named construction or computation for “primitive bits 3 le without”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PrimitiveBasisLE.lean:181](../../../../library/modules/primitivebasisle/#decl-quantumblockencoding-primitivebits3lewithout). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.primitiveBits3LEGridWithout" (lean := "QuantumBlockEncoding.primitiveBits3LEGridWithout")
*Plain-English reading.* This definition gives the library's named construction or computation for “primitive bits 3 le grid without”. Grid-sized companion of 'primitiveBits3LEWithout', used before the type normalizer has turned 'Fin (gridSize 3)' into 'Fin 8'.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Grid-sized companion of 'primitiveBits3LEWithout', used before the type normalizer has turned 'Fin (gridSize 3)' into 'Fin 8'.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PrimitiveBasisLE.lean:189](../../../../library/modules/primitivebasisle/#decl-quantumblockencoding-primitivebits3legridwithout). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.splitPrimitiveWire_primitiveBits3LE_context_eq" (lean := "QuantumBlockEncoding.splitPrimitiveWire_primitiveBits3LE_context_eq")
*Plain-English reading.* Lean checks the proposition indexed as “split primitive wire primitive bits 3 le context eq”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveBasisLE.lean:196](../../../../library/modules/primitivebasisle/#decl-quantumblockencoding-splitprimitivewire-primitivebits3le-context-eq). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.splitPrimitiveWire_primitiveBasisLEEquiv_three_symm_context_eq" (lean := "QuantumBlockEncoding.splitPrimitiveWire_primitiveBasisLEEquiv_three_symm_context_eq")
*Plain-English reading.* Lean checks the proposition indexed as “split primitive wire primitive basis le equiv three symm context eq”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveBasisLE.lean:204](../../../../library/modules/primitivebasisle/#decl-quantumblockencoding-splitprimitivewire-primitivebasisleequiv-three-symm-context-eq). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/PrimitiveCircuit.lean

39 explicit public declarations, in source order.

:::definition "QuantumBlockEncoding.ExactAngle" (lean := "QuantumBlockEncoding.ExactAngle")
*Plain-English reading.* This type lists the allowed alternatives for “exact angle”; its constructors are the cases that downstream code must handle.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* inductive.

Source: [QuantumBlockEncoding/PrimitiveCircuit.lean:17](../../../../library/modules/primitivecircuit/#decl-quantumblockencoding-exactangle). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.ExactAngle.eval" (lean := "QuantumBlockEncoding.ExactAngle.eval")
*Plain-English reading.* This definition gives the library's named construction or computation for “eval”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PrimitiveCircuit.lean:46](../../../../library/modules/primitivecircuit/#decl-quantumblockencoding-exactangle-eval). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ExactAngle.eval_add" (lean := "QuantumBlockEncoding.ExactAngle.eval_add")
*Plain-English reading.* Lean checks the proposition indexed as “eval add”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveCircuit.lean:57](../../../../library/modules/primitivecircuit/#decl-quantumblockencoding-exactangle-eval-add). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ExactAngle.eval_neg" (lean := "QuantumBlockEncoding.ExactAngle.eval_neg")
*Plain-English reading.* Lean checks the proposition indexed as “eval neg”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveCircuit.lean:60](../../../../library/modules/primitivecircuit/#decl-quantumblockencoding-exactangle-eval-neg). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ExactAngle.eval_scale" (lean := "QuantumBlockEncoding.ExactAngle.eval_scale")
*Plain-English reading.* Lean checks the proposition indexed as “eval scale”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveCircuit.lean:63](../../../../library/modules/primitivecircuit/#decl-quantumblockencoding-exactangle-eval-scale). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.ExactAngle.sub" (lean := "QuantumBlockEncoding.ExactAngle.sub")
*Plain-English reading.* This definition gives the library's named construction or computation for “sub”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PrimitiveCircuit.lean:66](../../../../library/modules/primitivecircuit/#decl-quantumblockencoding-exactangle-sub). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.ExactAngle.halfAdd" (lean := "QuantumBlockEncoding.ExactAngle.halfAdd")
*Plain-English reading.* This definition gives the library's named construction or computation for “half add”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PrimitiveCircuit.lean:69](../../../../library/modules/primitivecircuit/#decl-quantumblockencoding-exactangle-halfadd). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.ExactAngle.halfSub" (lean := "QuantumBlockEncoding.ExactAngle.halfSub")
*Plain-English reading.* This definition gives the library's named construction or computation for “half sub”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PrimitiveCircuit.lean:72](../../../../library/modules/primitivecircuit/#decl-quantumblockencoding-exactangle-halfsub). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ExactAngle.eval_sub" (lean := "QuantumBlockEncoding.ExactAngle.eval_sub")
*Plain-English reading.* Lean checks the proposition indexed as “eval sub”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveCircuit.lean:75](../../../../library/modules/primitivecircuit/#decl-quantumblockencoding-exactangle-eval-sub). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ExactAngle.eval_half_add" (lean := "QuantumBlockEncoding.ExactAngle.eval_half_add")
*Plain-English reading.* Lean checks the proposition indexed as “eval half add”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveCircuit.lean:79](../../../../library/modules/primitivecircuit/#decl-quantumblockencoding-exactangle-eval-half-add). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ExactAngle.eval_half_sub" (lean := "QuantumBlockEncoding.ExactAngle.eval_half_sub")
*Plain-English reading.* Lean checks the proposition indexed as “eval half sub”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveCircuit.lean:86](../../../../library/modules/primitivecircuit/#decl-quantumblockencoding-exactangle-eval-half-sub). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.PrimitiveGate" (lean := "QuantumBlockEncoding.PrimitiveGate")
*Plain-English reading.* This type lists the allowed alternatives for “primitive gate”; its constructors are the cases that downstream code must handle.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* inductive.

Source: [QuantumBlockEncoding/PrimitiveCircuit.lean:95](../../../../library/modules/primitivecircuit/#decl-quantumblockencoding-primitivegate). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.PrimitiveCircuit" (lean := "QuantumBlockEncoding.PrimitiveCircuit")
*Plain-English reading.* This abbreviation gives a shorter name to the type or expression used for “primitive circuit”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* abbrev.

Source: [QuantumBlockEncoding/PrimitiveCircuit.lean:101](../../../../library/modules/primitivecircuit/#decl-quantumblockencoding-primitivecircuit). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.PrimitiveProgram" (lean := "QuantumBlockEncoding.PrimitiveProgram")
*Plain-English reading.* This record groups the data and proof fields needed for “primitive program”. A proposition-valued field is a requirement until a constructor supplies it. A primitive circuit together with an exact global phase.

*Formal status.* Data contract in the default import surface; proposition-valued fields are obligations, not automatically established facts.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* A primitive circuit together with an exact global phase.

*Declaration kind.* structure.

Source: [QuantumBlockEncoding/PrimitiveCircuit.lean:104](../../../../library/modules/primitivecircuit/#decl-quantumblockencoding-primitiveprogram). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.PrimitiveGate.dagger" (lean := "QuantumBlockEncoding.PrimitiveGate.dagger")
*Plain-English reading.* This definition gives the library's named construction or computation for “dagger”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PrimitiveCircuit.lean:110](../../../../library/modules/primitivecircuit/#decl-quantumblockencoding-primitivegate-dagger). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.PrimitiveGate.touched" (lean := "QuantumBlockEncoding.PrimitiveGate.touched")
*Plain-English reading.* This definition gives the library's named construction or computation for “touched”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PrimitiveCircuit.lean:116](../../../../library/modules/primitivecircuit/#decl-quantumblockencoding-primitivegate-touched). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.PrimitiveGate.oneQubitCount" (lean := "QuantumBlockEncoding.PrimitiveGate.oneQubitCount")
*Plain-English reading.* This definition gives the library's named construction or computation for “one qubit count”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PrimitiveCircuit.lean:120](../../../../library/modules/primitivecircuit/#decl-quantumblockencoding-primitivegate-onequbitcount). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.PrimitiveGate.twoQubitCount" (lean := "QuantumBlockEncoding.PrimitiveGate.twoQubitCount")
*Plain-English reading.* This definition gives the library's named construction or computation for “two qubit count”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PrimitiveCircuit.lean:124](../../../../library/modules/primitivecircuit/#decl-quantumblockencoding-primitivegate-twoqubitcount). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.PrimitiveCircuit.gateCount" (lean := "QuantumBlockEncoding.PrimitiveCircuit.gateCount")
*Plain-English reading.* This definition gives the library's named construction or computation for “gate count”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PrimitiveCircuit.lean:132](../../../../library/modules/primitivecircuit/#decl-quantumblockencoding-primitivecircuit-gatecount). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.PrimitiveCircuit.oneQubitCount" (lean := "QuantumBlockEncoding.PrimitiveCircuit.oneQubitCount")
*Plain-English reading.* This definition gives the library's named construction or computation for “one qubit count”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PrimitiveCircuit.lean:135](../../../../library/modules/primitivecircuit/#decl-quantumblockencoding-primitivecircuit-onequbitcount). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.PrimitiveCircuit.twoQubitCount" (lean := "QuantumBlockEncoding.PrimitiveCircuit.twoQubitCount")
*Plain-English reading.* This definition gives the library's named construction or computation for “two qubit count”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PrimitiveCircuit.lean:138](../../../../library/modules/primitivecircuit/#decl-quantumblockencoding-primitivecircuit-twoqubitcount). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.PrimitiveCircuit.ryCount" (lean := "QuantumBlockEncoding.PrimitiveCircuit.ryCount")
*Plain-English reading.* This definition gives the library's named construction or computation for “ry count”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PrimitiveCircuit.lean:141](../../../../library/modules/primitivecircuit/#decl-quantumblockencoding-primitivecircuit-rycount). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.PrimitiveCircuit.cxCount" (lean := "QuantumBlockEncoding.PrimitiveCircuit.cxCount")
*Plain-English reading.* This definition gives the library's named construction or computation for “cx count”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PrimitiveCircuit.lean:146](../../../../library/modules/primitivecircuit/#decl-quantumblockencoding-primitivecircuit-cxcount). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.PrimitiveCircuit.ryCount_append" (lean := "QuantumBlockEncoding.PrimitiveCircuit.ryCount_append")
*Plain-English reading.* Lean checks the proposition indexed as “ry count append”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveCircuit.lean:151](../../../../library/modules/primitivecircuit/#decl-quantumblockencoding-primitivecircuit-rycount-append). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.PrimitiveCircuit.cxCount_append" (lean := "QuantumBlockEncoding.PrimitiveCircuit.cxCount_append")
*Plain-English reading.* Lean checks the proposition indexed as “cx count append”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveCircuit.lean:156](../../../../library/modules/primitivecircuit/#decl-quantumblockencoding-primitivecircuit-cxcount-append). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.PrimitiveCircuit.ryCount_singleton_ry" (lean := "QuantumBlockEncoding.PrimitiveCircuit.ryCount_singleton_ry")
*Plain-English reading.* Lean checks the proposition indexed as “ry count singleton ry”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveCircuit.lean:161](../../../../library/modules/primitivecircuit/#decl-quantumblockencoding-primitivecircuit-rycount-singleton-ry). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.PrimitiveCircuit.ryCount_singleton_cx" (lean := "QuantumBlockEncoding.PrimitiveCircuit.ryCount_singleton_cx")
*Plain-English reading.* Lean checks the proposition indexed as “ry count singleton cx”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveCircuit.lean:166](../../../../library/modules/primitivecircuit/#decl-quantumblockencoding-primitivecircuit-rycount-singleton-cx). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.PrimitiveCircuit.cxCount_singleton_ry" (lean := "QuantumBlockEncoding.PrimitiveCircuit.cxCount_singleton_ry")
*Plain-English reading.* Lean checks the proposition indexed as “cx count singleton ry”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveCircuit.lean:171](../../../../library/modules/primitivecircuit/#decl-quantumblockencoding-primitivecircuit-cxcount-singleton-ry). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.PrimitiveCircuit.cxCount_singleton_cx" (lean := "QuantumBlockEncoding.PrimitiveCircuit.cxCount_singleton_cx")
*Plain-English reading.* Lean checks the proposition indexed as “cx count singleton cx”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveCircuit.lean:176](../../../../library/modules/primitivecircuit/#decl-quantumblockencoding-primitivecircuit-cxcount-singleton-cx). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.PrimitiveCircuit.nextWireDepth" (lean := "QuantumBlockEncoding.PrimitiveCircuit.nextWireDepth")
*Plain-English reading.* This definition gives the library's named construction or computation for “next wire depth”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PrimitiveCircuit.lean:181](../../../../library/modules/primitivecircuit/#decl-quantumblockencoding-primitivecircuit-nextwiredepth). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.PrimitiveCircuit.wireDepths" (lean := "QuantumBlockEncoding.PrimitiveCircuit.wireDepths")
*Plain-English reading.* This definition gives the library's named construction or computation for “wire depths”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PrimitiveCircuit.lean:186](../../../../library/modules/primitivecircuit/#decl-quantumblockencoding-primitivecircuit-wiredepths). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.PrimitiveCircuit.depth" (lean := "QuantumBlockEncoding.PrimitiveCircuit.depth")
*Plain-English reading.* This definition gives the library's named construction or computation for “depth”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PrimitiveCircuit.lean:190](../../../../library/modules/primitivecircuit/#decl-quantumblockencoding-primitivecircuit-depth). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.PrimitiveCircuit.resource" (lean := "QuantumBlockEncoding.PrimitiveCircuit.resource")
*Plain-English reading.* This definition gives the library's named construction or computation for “resource”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PrimitiveCircuit.lean:193](../../../../library/modules/primitivecircuit/#decl-quantumblockencoding-primitivecircuit-resource). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.PrimitiveCircuit.gateCount_eq_length" (lean := "QuantumBlockEncoding.PrimitiveCircuit.gateCount_eq_length")
*Plain-English reading.* Lean checks the proposition indexed as “gate count eq length”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveCircuit.lean:197](../../../../library/modules/primitivecircuit/#decl-quantumblockencoding-primitivecircuit-gatecount-eq-length). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.PrimitiveCircuit.resource_oracleCalls_eq_zero" (lean := "QuantumBlockEncoding.PrimitiveCircuit.resource_oracleCalls_eq_zero")
*Plain-English reading.* Lean checks the proposition indexed as “resource oracle calls eq zero”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveCircuit.lean:201](../../../../library/modules/primitivecircuit/#decl-quantumblockencoding-primitivecircuit-resource-oraclecalls-eq-zero). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.PrimitiveProgram.identity" (lean := "QuantumBlockEncoding.PrimitiveProgram.identity")
*Plain-English reading.* This definition gives the library's named construction or computation for “identity”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PrimitiveCircuit.lean:209](../../../../library/modules/primitivecircuit/#decl-quantumblockencoding-primitiveprogram-identity). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.PrimitiveProgram.seq" (lean := "QuantumBlockEncoding.PrimitiveProgram.seq")
*Plain-English reading.* This definition gives the library's named construction or computation for “seq”. Execute 'left', then 'right', using chronological list semantics.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Execute 'left', then 'right', using chronological list semantics.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PrimitiveCircuit.lean:214](../../../../library/modules/primitivecircuit/#decl-quantumblockencoding-primitiveprogram-seq). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.PrimitiveProgram.dagger" (lean := "QuantumBlockEncoding.PrimitiveProgram.dagger")
*Plain-English reading.* This definition gives the library's named construction or computation for “dagger”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PrimitiveCircuit.lean:219](../../../../library/modules/primitivecircuit/#decl-quantumblockencoding-primitiveprogram-dagger). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.PrimitiveProgram.resource" (lean := "QuantumBlockEncoding.PrimitiveProgram.resource")
*Plain-English reading.* This definition gives the library's named construction or computation for “resource”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PrimitiveCircuit.lean:224](../../../../library/modules/primitivecircuit/#decl-quantumblockencoding-primitiveprogram-resource). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/PrimitiveDepthBound.lean

5 explicit public declarations, in source order.

:::theorem "QuantumBlockEncoding.PrimitiveCircuit.nextWireDepth_le" (lean := "QuantumBlockEncoding.PrimitiveCircuit.nextWireDepth_le")
*Plain-English reading.* Lean checks the proposition indexed as “next wire depth le”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveDepthBound.lean:12](../../../../library/modules/primitivedepthbound/#decl-quantumblockencoding-primitivecircuit-nextwiredepth-le). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.PrimitiveCircuit.foldl_nextWireDepth_le" (lean := "QuantumBlockEncoding.PrimitiveCircuit.foldl_nextWireDepth_le")
*Plain-English reading.* Lean checks the proposition indexed as “foldl next wire depth le”; the hypotheses and conclusion in the code panel fix its exact scope. Each scheduled instruction raises the global upper bound by at most one, from any supplied initial wire-depth profile.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Each scheduled instruction raises the global upper bound by at most one, from any supplied initial wire-depth profile.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveDepthBound.lean:23](../../../../library/modules/primitivedepthbound/#decl-quantumblockencoding-primitivecircuit-foldl-nextwiredepth-le). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.PrimitiveCircuit.wireDepths_le_gateCount" (lean := "QuantumBlockEncoding.PrimitiveCircuit.wireDepths_le_gateCount")
*Plain-English reading.* Lean checks the proposition indexed as “wire depths le gate count”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveDepthBound.lean:35](../../../../library/modules/primitivedepthbound/#decl-quantumblockencoding-primitivecircuit-wiredepths-le-gatecount). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.PrimitiveCircuit.depth_le_gateCount" (lean := "QuantumBlockEncoding.PrimitiveCircuit.depth_le_gateCount")
*Plain-English reading.* Lean checks the proposition indexed as “depth le gate count”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveDepthBound.lean:40](../../../../library/modules/primitivedepthbound/#decl-quantumblockencoding-primitivecircuit-depth-le-gatecount). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.PrimitiveCircuit.resource_depth_le_gateCount" (lean := "QuantumBlockEncoding.PrimitiveCircuit.resource_depth_le_gateCount")
*Plain-English reading.* Lean checks the proposition indexed as “resource depth le gate count”; the hypotheses and conclusion in the code panel fix its exact scope. The actual reported resource depth, including scheduling parallelism.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The actual reported resource depth, including scheduling parallelism.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveDepthBound.lean:45](../../../../library/modules/primitivedepthbound/#decl-quantumblockencoding-primitivecircuit-resource-depth-le-gatecount). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/PrimitiveMacros.lean

43 explicit public declarations, in source order.

:::definition "QuantumBlockEncoding.hadamardMatrix" (lean := "QuantumBlockEncoding.hadamardMatrix")
*Plain-English reading.* This definition gives the library's named construction or computation for “hadamard matrix”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PrimitiveMacros.lean:15](../../../../library/modules/primitivemacros/#decl-quantumblockencoding-hadamardmatrix). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.phaseMatrix" (lean := "QuantumBlockEncoding.phaseMatrix")
*Plain-English reading.* This definition gives the library's named construction or computation for “phase matrix”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PrimitiveMacros.lean:19](../../../../library/modules/primitivemacros/#decl-quantumblockencoding-phasematrix). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.hadamardMatrix_apply" (lean := "QuantumBlockEncoding.hadamardMatrix_apply")
*Plain-English reading.* Lean checks the proposition indexed as “hadamard matrix apply”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveMacros.lean:24](../../../../library/modules/primitivemacros/#decl-quantumblockencoding-hadamardmatrix-apply). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.phaseMatrix_apply" (lean := "QuantumBlockEncoding.phaseMatrix_apply")
*Plain-English reading.* Lean checks the proposition indexed as “phase matrix apply”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveMacros.lean:41](../../../../library/modules/primitivemacros/#decl-quantumblockencoding-phasematrix-apply). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.primitiveHProgram" (lean := "QuantumBlockEncoding.primitiveHProgram")
*Plain-English reading.* This definition gives the library's named construction or computation for “primitive h program”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PrimitiveMacros.lean:76](../../../../library/modules/primitivemacros/#decl-quantumblockencoding-primitivehprogram). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.primitiveTProgram" (lean := "QuantumBlockEncoding.primitiveTProgram")
*Plain-English reading.* This definition gives the library's named construction or computation for “primitive t program”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PrimitiveMacros.lean:82](../../../../library/modules/primitivemacros/#decl-quantumblockencoding-primitivetprogram). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.primitiveTdgProgram" (lean := "QuantumBlockEncoding.primitiveTdgProgram")
*Plain-English reading.* This definition gives the library's named construction or computation for “primitive tdg program”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PrimitiveMacros.lean:87](../../../../library/modules/primitivemacros/#decl-quantumblockencoding-primitivetdgprogram). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.evalGlobalPhase_pi_div_two" (lean := "QuantumBlockEncoding.evalGlobalPhase_pi_div_two")
*Plain-English reading.* Lean checks the proposition indexed as “eval global phase pi div two”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveMacros.lean:92](../../../../library/modules/primitivemacros/#decl-quantumblockencoding-evalglobalphase-pi-div-two). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.liftPrimitiveOneQubit_mul" (lean := "QuantumBlockEncoding.liftPrimitiveOneQubit_mul")
*Plain-English reading.* Lean checks the proposition indexed as “lift primitive one qubit mul”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveMacros.lean:98](../../../../library/modules/primitivemacros/#decl-quantumblockencoding-liftprimitiveonequbit-mul). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.smul_liftPrimitiveOneQubit" (lean := "QuantumBlockEncoding.smul_liftPrimitiveOneQubit")
*Plain-English reading.* Lean checks the proposition indexed as “smul lift primitive one qubit”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveMacros.lean:108](../../../../library/modules/primitivemacros/#decl-quantumblockencoding-smul-liftprimitiveonequbit). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.primitiveHProgram_eval" (lean := "QuantumBlockEncoding.primitiveHProgram_eval")
*Plain-English reading.* Lean checks the proposition indexed as “primitive h program eval”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveMacros.lean:117](../../../../library/modules/primitivemacros/#decl-quantumblockencoding-primitivehprogram-eval). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.primitiveTProgram_eval" (lean := "QuantumBlockEncoding.primitiveTProgram_eval")
*Plain-English reading.* Lean checks the proposition indexed as “primitive t program eval”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveMacros.lean:132](../../../../library/modules/primitivemacros/#decl-quantumblockencoding-primitivetprogram-eval). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.primitiveTdgProgram_eval" (lean := "QuantumBlockEncoding.primitiveTdgProgram_eval")
*Plain-English reading.* Lean checks the proposition indexed as “primitive tdg program eval”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveMacros.lean:152](../../../../library/modules/primitivemacros/#decl-quantumblockencoding-primitivetdgprogram-eval). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.phasePermutationMatrix" (lean := "QuantumBlockEncoding.phasePermutationMatrix")
*Plain-English reading.* This definition gives the library's named construction or computation for “phase permutation matrix”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PrimitiveMacros.lean:174](../../../../library/modules/primitivemacros/#decl-quantumblockencoding-phasepermutationmatrix). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.phasePermutationMatrix_mul" (lean := "QuantumBlockEncoding.phasePermutationMatrix_mul")
*Plain-English reading.* Lean checks the proposition indexed as “phase permutation matrix mul”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveMacros.lean:180](../../../../library/modules/primitivemacros/#decl-quantumblockencoding-phasepermutationmatrix-mul). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.evalPrimitiveCx_eq_phasePermutationMatrix" (lean := "QuantumBlockEncoding.evalPrimitiveCx_eq_phasePermutationMatrix")
*Plain-English reading.* Lean checks the proposition indexed as “eval primitive cx eq phase permutation matrix”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveMacros.lean:198](../../../../library/modules/primitivemacros/#decl-quantumblockencoding-evalprimitivecx-eq-phasepermutationmatrix). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.liftPhaseMatrix_eq_phasePermutationMatrix" (lean := "QuantumBlockEncoding.liftPhaseMatrix_eq_phasePermutationMatrix")
*Plain-English reading.* Lean checks the proposition indexed as “lift phase matrix eq phase permutation matrix”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveMacros.lean:206](../../../../library/modules/primitivemacros/#decl-quantumblockencoding-liftphasematrix-eq-phasepermutationmatrix). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.primitiveCxProgram" (lean := "QuantumBlockEncoding.primitiveCxProgram")
*Plain-English reading.* This definition gives the library's named construction or computation for “primitive cx program”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PrimitiveMacros.lean:232](../../../../library/modules/primitivemacros/#decl-quantumblockencoding-primitivecxprogram). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.primitiveCxProgram_eval" (lean := "QuantumBlockEncoding.primitiveCxProgram_eval")
*Plain-English reading.* Lean checks the proposition indexed as “primitive cx program eval”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveMacros.lean:237](../../../../library/modules/primitivemacros/#decl-quantumblockencoding-primitivecxprogram-eval). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.primitiveTProgram_eval_monomial" (lean := "QuantumBlockEncoding.primitiveTProgram_eval_monomial")
*Plain-English reading.* Lean checks the proposition indexed as “primitive t program eval monomial”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveMacros.lean:249](../../../../library/modules/primitivemacros/#decl-quantumblockencoding-primitivetprogram-eval-monomial). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.primitiveTdgProgram_eval_monomial" (lean := "QuantumBlockEncoding.primitiveTdgProgram_eval_monomial")
*Plain-English reading.* Lean checks the proposition indexed as “primitive tdg program eval monomial”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveMacros.lean:258](../../../../library/modules/primitivemacros/#decl-quantumblockencoding-primitivetdgprogram-eval-monomial). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.MonomialProgram" (lean := "QuantumBlockEncoding.MonomialProgram")
*Plain-English reading.* This record groups the data and proof fields needed for “monomial program”. A proposition-valued field is a requirement until a constructor supplies it.

*Formal status.* Data contract in the default import surface; proposition-valued fields are obligations, not automatically established facts.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* structure.

Source: [QuantumBlockEncoding/PrimitiveMacros.lean:267](../../../../library/modules/primitivemacros/#decl-quantumblockencoding-monomialprogram). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.MonomialProgram.seq" (lean := "QuantumBlockEncoding.MonomialProgram.seq")
*Plain-English reading.* This definition gives the library's named construction or computation for “seq”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PrimitiveMacros.lean:276](../../../../library/modules/primitivemacros/#decl-quantumblockencoding-monomialprogram-seq). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.MonomialProgram.cx" (lean := "QuantumBlockEncoding.MonomialProgram.cx")
*Plain-English reading.* This definition gives the library's named construction or computation for “cx”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PrimitiveMacros.lean:285](../../../../library/modules/primitivemacros/#decl-quantumblockencoding-monomialprogram-cx). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.MonomialProgram.t" (lean := "QuantumBlockEncoding.MonomialProgram.t")
*Plain-English reading.* This definition gives the library's named construction or computation for “t”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PrimitiveMacros.lean:292](../../../../library/modules/primitivemacros/#decl-quantumblockencoding-monomialprogram-t). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.MonomialProgram.tdg" (lean := "QuantumBlockEncoding.MonomialProgram.tdg")
*Plain-English reading.* This definition gives the library's named construction or computation for “tdg”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PrimitiveMacros.lean:299](../../../../library/modules/primitivemacros/#decl-quantumblockencoding-monomialprogram-tdg). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.primitiveCCXMiddle" (lean := "QuantumBlockEncoding.primitiveCCXMiddle")
*Plain-English reading.* This definition gives the library's named construction or computation for “primitive ccx middle”. The phase-only middle of the standard exact Toffoli decomposition.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The phase-only middle of the standard exact Toffoli decomposition.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PrimitiveMacros.lean:309](../../../../library/modules/primitivemacros/#decl-quantumblockencoding-primitiveccxmiddle). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.primitiveCCXProgram" (lean := "QuantumBlockEncoding.primitiveCCXProgram")
*Plain-English reading.* This definition gives the library's named construction or computation for “primitive ccx program”. The exact primitive program uses the requested H/T/Tdg/CX chronology.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The exact primitive program uses the requested H/T/Tdg/CX chronology.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PrimitiveMacros.lean:330](../../../../library/modules/primitivemacros/#decl-quantumblockencoding-primitiveccxprogram). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.primitiveCCXMiddle_permutation_eq_refl" (lean := "QuantumBlockEncoding.primitiveCCXMiddle_permutation_eq_refl")
*Plain-English reading.* Lean checks the proposition indexed as “primitive ccx middle permutation eq refl”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveMacros.lean:339](../../../../library/modules/primitivemacros/#decl-quantumblockencoding-primitiveccxmiddle-permutation-eq-refl). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.primitiveCCXMiddle_phase_eq_ccz" (lean := "QuantumBlockEncoding.primitiveCCXMiddle_phase_eq_ccz")
*Plain-English reading.* Lean checks the proposition indexed as “primitive ccx middle phase eq ccz”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveMacros.lean:363](../../../../library/modules/primitivemacros/#decl-quantumblockencoding-primitiveccxmiddle-phase-eq-ccz). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.cczMatrix" (lean := "QuantumBlockEncoding.cczMatrix")
*Plain-English reading.* This definition gives the library's named construction or computation for “ccz matrix”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PrimitiveMacros.lean:384](../../../../library/modules/primitivemacros/#decl-quantumblockencoding-cczmatrix). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.primitiveCCXMiddle_eval" (lean := "QuantumBlockEncoding.primitiveCCXMiddle_eval")
*Plain-English reading.* Lean checks the proposition indexed as “primitive ccx middle eval”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveMacros.lean:392](../../../../library/modules/primitivemacros/#decl-quantumblockencoding-primitiveccxmiddle-eval). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.zMatrix" (lean := "QuantumBlockEncoding.zMatrix")
*Plain-English reading.* This definition gives the library's named construction or computation for “z matrix”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PrimitiveMacros.lean:407](../../../../library/modules/primitivemacros/#decl-quantumblockencoding-zmatrix). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.hadamard_mul_hadamard" (lean := "QuantumBlockEncoding.hadamard_mul_hadamard")
*Plain-English reading.* Lean checks the proposition indexed as “hadamard mul hadamard”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveMacros.lean:410](../../../../library/modules/primitivemacros/#decl-quantumblockencoding-hadamard-mul-hadamard). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.hadamard_mul_z_mul_hadamard" (lean := "QuantumBlockEncoding.hadamard_mul_z_mul_hadamard")
*Plain-English reading.* Lean checks the proposition indexed as “hadamard mul z mul hadamard”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveMacros.lean:419](../../../../library/modules/primitivemacros/#decl-quantumblockencoding-hadamard-mul-z-mul-hadamard). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.liftPrimitiveOneQubit_eq_blockDiagonal" (lean := "QuantumBlockEncoding.liftPrimitiveOneQubit_eq_blockDiagonal")
*Plain-English reading.* Lean checks the proposition indexed as “lift primitive one qubit eq block diagonal”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveMacros.lean:430](../../../../library/modules/primitivemacros/#decl-quantumblockencoding-liftprimitiveonequbit-eq-blockdiagonal). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.cczTargetBlock" (lean := "QuantumBlockEncoding.cczTargetBlock")
*Plain-English reading.* This definition gives the library's named construction or computation for “ccz target block”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PrimitiveMacros.lean:440](../../../../library/modules/primitivemacros/#decl-quantumblockencoding-ccztargetblock). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.cczMatrix_eq_blockDiagonal" (lean := "QuantumBlockEncoding.cczMatrix_eq_blockDiagonal")
*Plain-English reading.* Lean checks the proposition indexed as “ccz matrix eq block diagonal”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveMacros.lean:448](../../../../library/modules/primitivemacros/#decl-quantumblockencoding-cczmatrix-eq-blockdiagonal). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.ccxTargetBlock" (lean := "QuantumBlockEncoding.ccxTargetBlock")
*Plain-English reading.* This definition gives the library's named construction or computation for “ccx target block”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PrimitiveMacros.lean:500](../../../../library/modules/primitivemacros/#decl-quantumblockencoding-ccxtargetblock). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.equivPermutationMatrix_ccx_eq_blockDiagonal" (lean := "QuantumBlockEncoding.equivPermutationMatrix_ccx_eq_blockDiagonal")
*Plain-English reading.* Lean checks the proposition indexed as “equiv permutation matrix ccx eq block diagonal”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveMacros.lean:508](../../../../library/modules/primitivemacros/#decl-quantumblockencoding-equivpermutationmatrix-ccx-eq-blockdiagonal). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.hadamard_conjugates_ccz" (lean := "QuantumBlockEncoding.hadamard_conjugates_ccz")
*Plain-English reading.* Lean checks the proposition indexed as “hadamard conjugates ccz”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveMacros.lean:580](../../../../library/modules/primitivemacros/#decl-quantumblockencoding-hadamard-conjugates-ccz). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.primitiveCCXProgram_eval" (lean := "QuantumBlockEncoding.primitiveCCXProgram_eval")
*Plain-English reading.* Lean checks the proposition indexed as “primitive ccx program eval”; the hypotheses and conclusion in the code panel fix its exact scope. The requested H/T/Tdg/CX decomposition is exactly Toffoli, including its global phase.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The requested H/T/Tdg/CX decomposition is exactly Toffoli, including its global phase.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveMacros.lean:611](../../../../library/modules/primitivemacros/#decl-quantumblockencoding-primitiveccxprogram-eval). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.primitiveCCXProgramRefinement" (lean := "QuantumBlockEncoding.primitiveCCXProgramRefinement")
*Plain-English reading.* This definition gives the library's named construction or computation for “primitive ccx program refinement”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PrimitiveMacros.lean:624](../../../../library/modules/primitivemacros/#decl-quantumblockencoding-primitiveccxprogramrefinement). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/PrimitiveRefinement.lean

5 explicit public declarations, in source order.

:::definition "QuantumBlockEncoding.PrimitiveProgramRefinement" (lean := "QuantumBlockEncoding.PrimitiveProgramRefinement")
*Plain-English reading.* This record groups the data and proof fields needed for “primitive program refinement”. A proposition-valued field is a requirement until a constructor supplies it. Exact refinement for a primitive program, including its declared global phase.

*Formal status.* Data contract in the default import surface; proposition-valued fields are obligations, not automatically established facts.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Exact refinement for a primitive program, including its declared global phase. This is the acceptance record used by phase-sensitive macro compilers.

*Declaration kind.* structure.

Source: [QuantumBlockEncoding/PrimitiveRefinement.lean:15](../../../../library/modules/primitiverefinement/#decl-quantumblockencoding-primitiveprogramrefinement). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.PrimitiveProgramRefinement.resource" (lean := "QuantumBlockEncoding.PrimitiveProgramRefinement.resource")
*Plain-English reading.* This definition gives the library's named construction or computation for “resource”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PrimitiveRefinement.lean:20](../../../../library/modules/primitiverefinement/#decl-quantumblockencoding-primitiveprogramrefinement-resource). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.PrimitiveProgramRefinement.oracleCalls_eq_zero" (lean := "QuantumBlockEncoding.PrimitiveProgramRefinement.oracleCalls_eq_zero")
*Plain-English reading.* Lean checks the proposition indexed as “oracle calls eq zero”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveRefinement.lean:24](../../../../library/modules/primitiverefinement/#decl-quantumblockencoding-primitiveprogramrefinement-oraclecalls-eq-zero). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.PrimitiveRefinement.resource" (lean := "QuantumBlockEncoding.PrimitiveRefinement.resource")
*Plain-English reading.* This definition gives the library's named construction or computation for “resource”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PrimitiveRefinement.lean:29](../../../../library/modules/primitiverefinement/#decl-quantumblockencoding-primitiverefinement-resource). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.PrimitiveRefinement.oracleCalls_eq_zero" (lean := "QuantumBlockEncoding.PrimitiveRefinement.oracleCalls_eq_zero")
*Plain-English reading.* Lean checks the proposition indexed as “oracle calls eq zero”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveRefinement.lean:33](../../../../library/modules/primitiverefinement/#decl-quantumblockencoding-primitiverefinement-oraclecalls-eq-zero). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/PrimitiveSemantics.lean

54 explicit public declarations, in source order.

:::definition "QuantumBlockEncoding.standardRyMatrix" (lean := "QuantumBlockEncoding.standardRyMatrix")
*Plain-English reading.* This definition gives the library's named construction or computation for “standard ry matrix”. Standard 'RY(theta)' in the convention used by Qiskit and OpenQASM 3.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Standard 'RY(theta)' in the convention used by Qiskit and OpenQASM 3.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PrimitiveSemantics.lean:18](../../../../library/modules/primitivesemantics/#decl-quantumblockencoding-standardrymatrix). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.standardRyMatrix_zero" (lean := "QuantumBlockEncoding.standardRyMatrix_zero")
*Plain-English reading.* Lean checks the proposition indexed as “standard ry matrix zero”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveSemantics.lean:22](../../../../library/modules/primitivesemantics/#decl-quantumblockencoding-standardrymatrix-zero). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.standardRyMatrix_add" (lean := "QuantumBlockEncoding.standardRyMatrix_add")
*Plain-English reading.* Lean checks the proposition indexed as “standard ry matrix add”; the hypotheses and conclusion in the code panel fix its exact scope. Standard rotations compose by adding their physical angles.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Standard rotations compose by adding their physical angles.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveSemantics.lean:28](../../../../library/modules/primitivesemantics/#decl-quantumblockencoding-standardrymatrix-add). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.star_complex_cos_ofReal" (lean := "QuantumBlockEncoding.star_complex_cos_ofReal")
*Plain-English reading.* Lean checks the proposition indexed as “star complex cos of real”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveSemantics.lean:39](../../../../library/modules/primitivesemantics/#decl-quantumblockencoding-star-complex-cos-ofreal). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.conj_complex_cos_ofReal" (lean := "QuantumBlockEncoding.conj_complex_cos_ofReal")
*Plain-English reading.* Lean checks the proposition indexed as “conj complex cos of real”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveSemantics.lean:43](../../../../library/modules/primitivesemantics/#decl-quantumblockencoding-conj-complex-cos-ofreal). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.star_complex_sin_ofReal" (lean := "QuantumBlockEncoding.star_complex_sin_ofReal")
*Plain-English reading.* Lean checks the proposition indexed as “star complex sin of real”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveSemantics.lean:48](../../../../library/modules/primitivesemantics/#decl-quantumblockencoding-star-complex-sin-ofreal). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.conj_complex_sin_ofReal" (lean := "QuantumBlockEncoding.conj_complex_sin_ofReal")
*Plain-English reading.* Lean checks the proposition indexed as “conj complex sin of real”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveSemantics.lean:52](../../../../library/modules/primitivesemantics/#decl-quantumblockencoding-conj-complex-sin-ofreal). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.complex_ofReal_div_two" (lean := "QuantumBlockEncoding.complex_ofReal_div_two")
*Plain-English reading.* Lean checks the proposition indexed as “complex of real div two”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveSemantics.lean:57](../../../../library/modules/primitivesemantics/#decl-quantumblockencoding-complex-ofreal-div-two). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.conj_complex_cos_ofReal_div_two" (lean := "QuantumBlockEncoding.conj_complex_cos_ofReal_div_two")
*Plain-English reading.* Lean checks the proposition indexed as “conj complex cos of real div two”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveSemantics.lean:61](../../../../library/modules/primitivesemantics/#decl-quantumblockencoding-conj-complex-cos-ofreal-div-two). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.conj_complex_sin_ofReal_div_two" (lean := "QuantumBlockEncoding.conj_complex_sin_ofReal_div_two")
*Plain-English reading.* Lean checks the proposition indexed as “conj complex sin of real div two”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveSemantics.lean:66](../../../../library/modules/primitivesemantics/#decl-quantumblockencoding-conj-complex-sin-ofreal-div-two). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.standardRyMatrix_neg" (lean := "QuantumBlockEncoding.standardRyMatrix_neg")
*Plain-English reading.* Lean checks the proposition indexed as “standard ry matrix neg”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveSemantics.lean:71](../../../../library/modules/primitivesemantics/#decl-quantumblockencoding-standardrymatrix-neg). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.xMatrix" (lean := "QuantumBlockEncoding.xMatrix")
*Plain-English reading.* This definition gives the library's named construction or computation for “x matrix”. Pauli X in the same two-dimensional basis as 'standardRyMatrix'.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Pauli X in the same two-dimensional basis as 'standardRyMatrix'.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PrimitiveSemantics.lean:80](../../../../library/modules/primitivesemantics/#decl-quantumblockencoding-xmatrix). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.xMatrix_conjugates_standardRy" (lean := "QuantumBlockEncoding.xMatrix_conjugates_standardRy")
*Plain-English reading.* Lean checks the proposition indexed as “x matrix conjugates standard ry”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveSemantics.lean:83](../../../../library/modules/primitivesemantics/#decl-quantumblockencoding-xmatrix-conjugates-standardry). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.standardRyMatrix_unitary" (lean := "QuantumBlockEncoding.standardRyMatrix_unitary")
*Plain-English reading.* Lean checks the proposition indexed as “standard ry matrix unitary”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveSemantics.lean:91](../../../../library/modules/primitivesemantics/#decl-quantumblockencoding-standardrymatrix-unitary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.standardRyMatrix_two_arccos_eq_amplitudeRotation" (lean := "QuantumBlockEncoding.standardRyMatrix_two_arccos_eq_amplitudeRotation")
*Plain-English reading.* Lean checks the proposition indexed as “standard ry matrix two arccos eq amplitude rotation”; the hypotheses and conclusion in the code panel fix its exact scope. The exact half-angle correction from standard 'RY' to the logical loader.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The exact half-angle correction from standard 'RY' to the logical loader.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveSemantics.lean:96](../../../../library/modules/primitivesemantics/#decl-quantumblockencoding-standardrymatrix-two-arccos-eq-amplituderotation). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.standardRyMatrix_pi_div_two_eq_warmRobinUniformBitPrepare" (lean := "QuantumBlockEncoding.standardRyMatrix_pi_div_two_eq_warmRobinUniformBitPrepare")
*Plain-English reading.* Lean checks the proposition indexed as “standard ry matrix pi div two eq warm robin uniform bit prepare”; the hypotheses and conclusion in the code panel fix its exact scope. The symmetry PREPARE is exactly a standard 'RY(pi/2)', not an opaque H.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The symmetry PREPARE is exactly a standard 'RY(pi/2)', not an opaque H.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveSemantics.lean:106](../../../../library/modules/primitivesemantics/#decl-quantumblockencoding-standardrymatrix-pi-div-two-eq-warmrobinuniformbitprepare). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.PrimitiveBasis" (lean := "QuantumBlockEncoding.PrimitiveBasis")
*Plain-English reading.* This abbreviation gives a shorter name to the type or expression used for “primitive basis”. Computational-basis bit strings with one named coordinate per qubit.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Computational-basis bit strings with one named coordinate per qubit.

*Declaration kind.* abbrev.

Source: [QuantumBlockEncoding/PrimitiveSemantics.lean:116](../../../../library/modules/primitivesemantics/#decl-quantumblockencoding-primitivebasis). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.flipBit" (lean := "QuantumBlockEncoding.flipBit")
*Plain-English reading.* This definition gives the library's named construction or computation for “flip bit”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PrimitiveSemantics.lean:118](../../../../library/modules/primitivesemantics/#decl-quantumblockencoding-flipbit). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.flipBit_flipBit" (lean := "QuantumBlockEncoding.flipBit_flipBit")
*Plain-English reading.* Lean checks the proposition indexed as “flip bit flip bit”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveSemantics.lean:120](../../../../library/modules/primitivesemantics/#decl-quantumblockencoding-flipbit-flipbit). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.xBasisAction" (lean := "QuantumBlockEncoding.xBasisAction")
*Plain-English reading.* This definition gives the library's named construction or computation for “x basis action”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PrimitiveSemantics.lean:123](../../../../library/modules/primitivesemantics/#decl-quantumblockencoding-xbasisaction). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.xBasisAction_involutive" (lean := "QuantumBlockEncoding.xBasisAction_involutive")
*Plain-English reading.* Lean checks the proposition indexed as “x basis action involutive”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveSemantics.lean:127](../../../../library/modules/primitivesemantics/#decl-quantumblockencoding-xbasisaction-involutive). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.xBasisEquiv" (lean := "QuantumBlockEncoding.xBasisEquiv")
*Plain-English reading.* This definition gives the library's named construction or computation for “x basis equiv”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PrimitiveSemantics.lean:136](../../../../library/modules/primitivesemantics/#decl-quantumblockencoding-xbasisequiv). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.cxBasisAction" (lean := "QuantumBlockEncoding.cxBasisAction")
*Plain-English reading.* This definition gives the library's named construction or computation for “cx basis action”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PrimitiveSemantics.lean:143](../../../../library/modules/primitivesemantics/#decl-quantumblockencoding-cxbasisaction). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.cxBasisAction_involutive" (lean := "QuantumBlockEncoding.cxBasisAction_involutive")
*Plain-English reading.* Lean checks the proposition indexed as “cx basis action involutive”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveSemantics.lean:147](../../../../library/modules/primitivesemantics/#decl-quantumblockencoding-cxbasisaction-involutive). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.cxBasisEquiv" (lean := "QuantumBlockEncoding.cxBasisEquiv")
*Plain-English reading.* This definition gives the library's named construction or computation for “cx basis equiv”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PrimitiveSemantics.lean:158](../../../../library/modules/primitivesemantics/#decl-quantumblockencoding-cxbasisequiv). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.OtherPrimitiveWires" (lean := "QuantumBlockEncoding.OtherPrimitiveWires")
*Plain-English reading.* This abbreviation gives a shorter name to the type or expression used for “other primitive wires”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* abbrev.

Source: [QuantumBlockEncoding/PrimitiveSemantics.lean:166](../../../../library/modules/primitivesemantics/#decl-quantumblockencoding-otherprimitivewires). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.splitPrimitiveWire" (lean := "QuantumBlockEncoding.splitPrimitiveWire")
*Plain-English reading.* This definition gives the library's named construction or computation for “split primitive wire”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PrimitiveSemantics.lean:169](../../../../library/modules/primitivesemantics/#decl-quantumblockencoding-splitprimitivewire). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.splitPrimitiveWire_other_apply" (lean := "QuantumBlockEncoding.splitPrimitiveWire_other_apply")
*Plain-English reading.* Lean checks the proposition indexed as “split primitive wire other apply”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveSemantics.lean:188](../../../../library/modules/primitivesemantics/#decl-quantumblockencoding-splitprimitivewire-other-apply). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.liftPrimitiveOneQubit" (lean := "QuantumBlockEncoding.liftPrimitiveOneQubit")
*Plain-English reading.* This definition gives the library's named construction or computation for “lift primitive one qubit”. Lift a one-qubit matrix to a named wire, leaving every other wire fixed.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Lift a one-qubit matrix to a named wire, leaving every other wire fixed.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PrimitiveSemantics.lean:194](../../../../library/modules/primitivesemantics/#decl-quantumblockencoding-liftprimitiveonequbit). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.liftPrimitiveOneQubit_apply" (lean := "QuantumBlockEncoding.liftPrimitiveOneQubit_apply")
*Plain-English reading.* Lean checks the proposition indexed as “lift primitive one qubit apply”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveSemantics.lean:202](../../../../library/modules/primitivesemantics/#decl-quantumblockencoding-liftprimitiveonequbit-apply). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.liftPrimitiveOneQubit_unitary" (lean := "QuantumBlockEncoding.liftPrimitiveOneQubit_unitary")
*Plain-English reading.* Lean checks the proposition indexed as “lift primitive one qubit unitary”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveSemantics.lean:220](../../../../library/modules/primitivesemantics/#decl-quantumblockencoding-liftprimitiveonequbit-unitary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.standardRzMatrix" (lean := "QuantumBlockEncoding.standardRzMatrix")
*Plain-English reading.* This definition gives the library's named construction or computation for “standard rz matrix”. Standard exact 'RZ(theta)' matrix, including its phase convention.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Standard exact 'RZ(theta)' matrix, including its phase convention.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PrimitiveSemantics.lean:232](../../../../library/modules/primitivesemantics/#decl-quantumblockencoding-standardrzmatrix). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.standardRzMatrix_unitary" (lean := "QuantumBlockEncoding.standardRzMatrix_unitary")
*Plain-English reading.* Lean checks the proposition indexed as “standard rz matrix unitary”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveSemantics.lean:241](../../../../library/modules/primitivesemantics/#decl-quantumblockencoding-standardrzmatrix-unitary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.standardRzMatrix_neg" (lean := "QuantumBlockEncoding.standardRzMatrix_neg")
*Plain-English reading.* Lean checks the proposition indexed as “standard rz matrix neg”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveSemantics.lean:263](../../../../library/modules/primitivesemantics/#decl-quantumblockencoding-standardrzmatrix-neg). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.star_equivPermutationMatrix" (lean := "QuantumBlockEncoding.star_equivPermutationMatrix")
*Plain-English reading.* Lean checks the proposition indexed as “star equiv permutation matrix”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveSemantics.lean:286](../../../../library/modules/primitivesemantics/#decl-quantumblockencoding-star-equivpermutationmatrix). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.star_liftPrimitiveOneQubit" (lean := "QuantumBlockEncoding.star_liftPrimitiveOneQubit")
*Plain-English reading.* Lean checks the proposition indexed as “star lift primitive one qubit”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveSemantics.lean:306](../../../../library/modules/primitivesemantics/#decl-quantumblockencoding-star-liftprimitiveonequbit). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.evalPrimitiveGate" (lean := "QuantumBlockEncoding.evalPrimitiveGate")
*Plain-English reading.* This definition gives the library's named construction or computation for “eval primitive gate”. Exact matrix denotation of one primitive instruction.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Exact matrix denotation of one primitive instruction.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PrimitiveSemantics.lean:323](../../../../library/modules/primitivesemantics/#decl-quantumblockencoding-evalprimitivegate). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.evalPrimitiveGate_unitary" (lean := "QuantumBlockEncoding.evalPrimitiveGate_unitary")
*Plain-English reading.* Lean checks the proposition indexed as “eval primitive gate unitary”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveSemantics.lean:331](../../../../library/modules/primitivesemantics/#decl-quantumblockencoding-evalprimitivegate-unitary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.xBasisEquiv_symm" (lean := "QuantumBlockEncoding.xBasisEquiv_symm")
*Plain-English reading.* Lean checks the proposition indexed as “x basis equiv symm”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveSemantics.lean:342](../../../../library/modules/primitivesemantics/#decl-quantumblockencoding-xbasisequiv-symm). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.cxBasisEquiv_symm" (lean := "QuantumBlockEncoding.cxBasisEquiv_symm")
*Plain-English reading.* Lean checks the proposition indexed as “cx basis equiv symm”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveSemantics.lean:346](../../../../library/modules/primitivesemantics/#decl-quantumblockencoding-cxbasisequiv-symm). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.evalPrimitiveGate_dagger" (lean := "QuantumBlockEncoding.evalPrimitiveGate_dagger")
*Plain-English reading.* Lean checks the proposition indexed as “eval primitive gate dagger”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveSemantics.lean:352](../../../../library/modules/primitivesemantics/#decl-quantumblockencoding-evalprimitivegate-dagger). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.evalPrimitiveCircuit" (lean := "QuantumBlockEncoding.evalPrimitiveCircuit")
*Plain-English reading.* This definition gives the library's named construction or computation for “eval primitive circuit”. Chronological circuit evaluation: later instructions multiply on the left.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Chronological circuit evaluation: later instructions multiply on the left.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PrimitiveSemantics.lean:374](../../../../library/modules/primitivesemantics/#decl-quantumblockencoding-evalprimitivecircuit). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.evalPrimitiveCircuit_unitary" (lean := "QuantumBlockEncoding.evalPrimitiveCircuit_unitary")
*Plain-English reading.* Lean checks the proposition indexed as “eval primitive circuit unitary”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveSemantics.lean:379](../../../../library/modules/primitivesemantics/#decl-quantumblockencoding-evalprimitivecircuit-unitary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.evalPrimitiveCircuit_append" (lean := "QuantumBlockEncoding.evalPrimitiveCircuit_append")
*Plain-English reading.* Lean checks the proposition indexed as “eval primitive circuit append”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveSemantics.lean:389](../../../../library/modules/primitivesemantics/#decl-quantumblockencoding-evalprimitivecircuit-append). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.evalPrimitiveCircuit_dagger" (lean := "QuantumBlockEncoding.evalPrimitiveCircuit_dagger")
*Plain-English reading.* Lean checks the proposition indexed as “eval primitive circuit dagger”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveSemantics.lean:400](../../../../library/modules/primitivesemantics/#decl-quantumblockencoding-evalprimitivecircuit-dagger). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.evalGlobalPhase" (lean := "QuantumBlockEncoding.evalGlobalPhase")
*Plain-English reading.* This definition gives the library's named construction or computation for “eval global phase”. Unit-modulus scalar represented by an exact global phase.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Unit-modulus scalar represented by an exact global phase.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PrimitiveSemantics.lean:413](../../../../library/modules/primitivesemantics/#decl-quantumblockencoding-evalglobalphase). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.evalGlobalPhase_unitary" (lean := "QuantumBlockEncoding.evalGlobalPhase_unitary")
*Plain-English reading.* Lean checks the proposition indexed as “eval global phase unitary”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveSemantics.lean:416](../../../../library/modules/primitivesemantics/#decl-quantumblockencoding-evalglobalphase-unitary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.evalGlobalPhase_neg" (lean := "QuantumBlockEncoding.evalGlobalPhase_neg")
*Plain-English reading.* Lean checks the proposition indexed as “eval global phase neg”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveSemantics.lean:430](../../../../library/modules/primitivesemantics/#decl-quantumblockencoding-evalglobalphase-neg). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.evalPrimitiveProgram" (lean := "QuantumBlockEncoding.evalPrimitiveProgram")
*Plain-English reading.* This definition gives the library's named construction or computation for “eval primitive program”. Exact program semantics, with the same 'exp(i phase)' convention used by Qiskit and OpenQASM 3.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Exact program semantics, with the same 'exp(i phase)' convention used by Qiskit and OpenQASM 3.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PrimitiveSemantics.lean:439](../../../../library/modules/primitivesemantics/#decl-quantumblockencoding-evalprimitiveprogram). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.evalPrimitiveProgram_identity" (lean := "QuantumBlockEncoding.evalPrimitiveProgram_identity")
*Plain-English reading.* Lean checks the proposition indexed as “eval primitive program identity”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveSemantics.lean:444](../../../../library/modules/primitivesemantics/#decl-quantumblockencoding-evalprimitiveprogram-identity). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.evalPrimitiveProgram_seq" (lean := "QuantumBlockEncoding.evalPrimitiveProgram_seq")
*Plain-English reading.* Lean checks the proposition indexed as “eval primitive program seq”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveSemantics.lean:449](../../../../library/modules/primitivesemantics/#decl-quantumblockencoding-evalprimitiveprogram-seq). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.evalPrimitiveProgram_unitary" (lean := "QuantumBlockEncoding.evalPrimitiveProgram_unitary")
*Plain-English reading.* Lean checks the proposition indexed as “eval primitive program unitary”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveSemantics.lean:463](../../../../library/modules/primitivesemantics/#decl-quantumblockencoding-evalprimitiveprogram-unitary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.evalPrimitiveProgram_dagger" (lean := "QuantumBlockEncoding.evalPrimitiveProgram_dagger")
*Plain-English reading.* Lean checks the proposition indexed as “eval primitive program dagger”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveSemantics.lean:470](../../../../library/modules/primitivesemantics/#decl-quantumblockencoding-evalprimitiveprogram-dagger). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.PrimitiveRefinement" (lean := "QuantumBlockEncoding.PrimitiveRefinement")
*Plain-English reading.* This record groups the data and proof fields needed for “primitive refinement”. A proposition-valued field is a requirement until a constructor supplies it. A typed primitive refinement records exact equality, not equality up to phase.

*Formal status.* Data contract in the default import surface; proposition-valued fields are obligations, not automatically established facts.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* A typed primitive refinement records exact equality, not equality up to phase.

*Declaration kind.* structure.

Source: [QuantumBlockEncoding/PrimitiveSemantics.lean:482](../../../../library/modules/primitivesemantics/#decl-quantumblockencoding-primitiverefinement). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/PrimitiveWireRename.lean

12 explicit public declarations, in source order.

:::definition "QuantumBlockEncoding.PrimitiveWireRename.basisEquiv" (lean := "QuantumBlockEncoding.PrimitiveWireRename.basisEquiv")
*Plain-English reading.* This definition gives the library's named construction or computation for “basis equiv”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PrimitiveWireRename.lean:10](../../../../library/modules/primitivewirerename/#decl-quantumblockencoding-primitivewirerename-basisequiv). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.PrimitiveWireRename.gate" (lean := "QuantumBlockEncoding.PrimitiveWireRename.gate")
*Plain-English reading.* This definition gives the library's named construction or computation for “gate”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PrimitiveWireRename.lean:16](../../../../library/modules/primitivewirerename/#decl-quantumblockencoding-primitivewirerename-gate). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.PrimitiveWireRename.circuit" (lean := "QuantumBlockEncoding.PrimitiveWireRename.circuit")
*Plain-English reading.* This definition gives the library's named construction or computation for “circuit”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PrimitiveWireRename.lean:22](../../../../library/modules/primitivewirerename/#decl-quantumblockencoding-primitivewirerename-circuit). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.PrimitiveWireRename.matrix" (lean := "QuantumBlockEncoding.PrimitiveWireRename.matrix")
*Plain-English reading.* This definition gives the library's named construction or computation for “matrix”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PrimitiveWireRename.lean:25](../../../../library/modules/primitivewirerename/#decl-quantumblockencoding-primitivewirerename-matrix). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.PrimitiveWireRename.matrix_apply" (lean := "QuantumBlockEncoding.PrimitiveWireRename.matrix_apply")
*Plain-English reading.* Lean checks the proposition indexed as “matrix apply”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveWireRename.lean:30](../../../../library/modules/primitivewirerename/#decl-quantumblockencoding-primitivewirerename-matrix-apply). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.PrimitiveWireRename.matrix_mul" (lean := "QuantumBlockEncoding.PrimitiveWireRename.matrix_mul")
*Plain-English reading.* Lean checks the proposition indexed as “matrix mul”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveWireRename.lean:35](../../../../library/modules/primitivewirerename/#decl-quantumblockencoding-primitivewirerename-matrix-mul). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.PrimitiveWireRename.oneQubit" (lean := "QuantumBlockEncoding.PrimitiveWireRename.oneQubit")
*Plain-English reading.* Lean checks the proposition indexed as “one qubit”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveWireRename.lean:58](../../../../library/modules/primitivewirerename/#decl-quantumblockencoding-primitivewirerename-onequbit). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.PrimitiveWireRename.eval_gate" (lean := "QuantumBlockEncoding.PrimitiveWireRename.eval_gate")
*Plain-English reading.* Lean checks the proposition indexed as “eval gate”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveWireRename.lean:83](../../../../library/modules/primitivewirerename/#decl-quantumblockencoding-primitivewirerename-eval-gate). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.PrimitiveWireRename.eval_circuit" (lean := "QuantumBlockEncoding.PrimitiveWireRename.eval_circuit")
*Plain-English reading.* Lean checks the proposition indexed as “eval circuit”; the hypotheses and conclusion in the code panel fix its exact scope. One actual renamed primitive list, with both boundary index maps exposed.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* One actual renamed primitive list, with both boundary index maps exposed.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveWireRename.lean:112](../../../../library/modules/primitivewirerename/#decl-quantumblockencoding-primitivewirerename-eval-circuit). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.PrimitiveWireRename.gateCount" (lean := "QuantumBlockEncoding.PrimitiveWireRename.gateCount")
*Plain-English reading.* Lean checks the proposition indexed as “gate count”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveWireRename.lean:120](../../../../library/modules/primitivewirerename/#decl-quantumblockencoding-primitivewirerename-gatecount). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.PrimitiveWireRename.ryCount" (lean := "QuantumBlockEncoding.PrimitiveWireRename.ryCount")
*Plain-English reading.* Lean checks the proposition indexed as “ry count”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveWireRename.lean:123](../../../../library/modules/primitivewirerename/#decl-quantumblockencoding-primitivewirerename-rycount). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.PrimitiveWireRename.cxCount" (lean := "QuantumBlockEncoding.PrimitiveWireRename.cxCount")
*Plain-English reading.* Lean checks the proposition indexed as “cx count”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PrimitiveWireRename.lean:129](../../../../library/modules/primitivewirerename/#decl-quantumblockencoding-primitivewirerename-cxcount). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/PromiseGateOptimization.lean

17 explicit public declarations, in source order.

:::definition "QuantumBlockEncoding.PromiseGateOptimization.liftTargetEquiv" (lean := "QuantumBlockEncoding.PromiseGateOptimization.liftTargetEquiv")
*Plain-English reading.* This definition gives the library's named construction or computation for “lift target equiv”. Apply a target permutation without changing its control register.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Apply a target permutation without changing its control register.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PromiseGateOptimization.lean:23](../../../../library/modules/promisegateoptimization/#decl-quantumblockencoding-promisegateoptimization-lifttargetequiv). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.PromiseGateOptimization.controlledTargetEquiv" (lean := "QuantumBlockEncoding.PromiseGateOptimization.controlledTargetEquiv")
*Plain-English reading.* This definition gives the library's named construction or computation for “controlled target equiv”. Apply the target permutation exactly on the 'true' control branch.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Apply the target permutation exactly on the 'true' control branch.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PromiseGateOptimization.lean:28](../../../../library/modules/promisegateoptimization/#decl-quantumblockencoding-promisegateoptimization-controlledtargetequiv). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.PromiseGateOptimization.conjugatedTargetEquiv" (lean := "QuantumBlockEncoding.PromiseGateOptimization.conjugatedTargetEquiv")
*Plain-English reading.* This definition gives the library's named construction or computation for “conjugated target equiv”. Chronological 'V', then 'U', then 'V†'.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Chronological 'V', then 'U', then 'V†'.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PromiseGateOptimization.lean:42](../../../../library/modules/promisegateoptimization/#decl-quantumblockencoding-promisegateoptimization-conjugatedtargetequiv). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.PromiseGateOptimization.controlledConjugation_equiv" (lean := "QuantumBlockEncoding.PromiseGateOptimization.controlledConjugation_equiv")
*Plain-English reading.* Lean checks the proposition indexed as “controlled conjugation equiv”; the hypotheses and conclusion in the code panel fix its exact scope. Figure 3(a): controlling 'V† U V' is equivalent to leaving 'V' and 'V†' uncontrolled and controlling only 'U'.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Figure 3(a): controlling 'V† U V' is equivalent to leaving 'V' and 'V†' uncontrolled and controlling only 'U'.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PromiseGateOptimization.lean:48](../../../../library/modules/promisegateoptimization/#decl-quantumblockencoding-promisegateoptimization-controlledconjugation-equiv). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.PromiseGateOptimization.controlledConjugation_matrix" (lean := "QuantumBlockEncoding.PromiseGateOptimization.controlledConjugation_matrix")
*Plain-English reading.* Lean checks the proposition indexed as “controlled conjugation matrix”; the hypotheses and conclusion in the code panel fix its exact scope. Matrix form of the controlled-conjugation identity.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Matrix form of the controlled-conjugation identity.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PromiseGateOptimization.lean:61](../../../../library/modules/promisegateoptimization/#decl-quantumblockencoding-promisegateoptimization-controlledconjugation-matrix). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.PromiseGateOptimization.WeakPromiseSpec" (lean := "QuantumBlockEncoding.PromiseGateOptimization.WeakPromiseSpec")
*Plain-English reading.* This definition gives the library's named construction or computation for “weak promise spec”. Exact clean-branch contract for a weak promise gate.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Exact clean-branch contract for a weak promise gate. No behavior is required away from 'cleanPromise'.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PromiseGateOptimization.lean:75](../../../../library/modules/promisegateoptimization/#decl-quantumblockencoding-promisegateoptimization-weakpromisespec). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.PromiseGateOptimization.StrongPromiseSpec" (lean := "QuantumBlockEncoding.PromiseGateOptimization.StrongPromiseSpec")
*Plain-English reading.* This definition gives the library's named construction or computation for “strong promise spec”. A strong promise gate additionally restores its promise register for every basis input.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* A strong promise gate additionally restores its promise register for every basis input.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PromiseGateOptimization.lean:83](../../../../library/modules/promisegateoptimization/#decl-quantumblockencoding-promisegateoptimization-strongpromisespec). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.PromiseGateOptimization.StrongPromiseSpec.weak" (lean := "QuantumBlockEncoding.PromiseGateOptimization.StrongPromiseSpec.weak")
*Plain-English reading.* Lean checks the proposition indexed as “weak”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PromiseGateOptimization.lean:89](../../../../library/modules/promisegateoptimization/#decl-quantumblockencoding-promisegateoptimization-strongpromisespec-weak). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.PromiseGateOptimization.toggleDirtyFlagEquiv" (lean := "QuantumBlockEncoding.PromiseGateOptimization.toggleDirtyFlagEquiv")
*Plain-English reading.* This definition gives the library's named construction or computation for “toggle dirty flag equiv”. Toggle a possibly dirty flag exactly when the control predicate holds.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Toggle a possibly dirty flag exactly when the control predicate holds.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PromiseGateOptimization.lean:97](../../../../library/modules/promisegateoptimization/#decl-quantumblockencoding-promisegateoptimization-toggledirtyflagequiv). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.PromiseGateOptimization.dirtyFlagControlledTargetEquiv" (lean := "QuantumBlockEncoding.PromiseGateOptimization.dirtyFlagControlledTargetEquiv")
*Plain-English reading.* This definition gives the library's named construction or computation for “dirty flag controlled target equiv”. Apply the target when the dirty flag is set, preserving key and flag.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Apply the target when the dirty flag is set, preserving key and flag.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PromiseGateOptimization.lean:111](../../../../library/modules/promisegateoptimization/#decl-quantumblockencoding-promisegateoptimization-dirtyflagcontrolledtargetequiv). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.PromiseGateOptimization.dirtyControlledInvolutionEquiv" (lean := "QuantumBlockEncoding.PromiseGateOptimization.dirtyControlledInvolutionEquiv")
*Plain-English reading.* This definition gives the library's named construction or computation for “dirty controlled involution equiv”. Compute-use-uncompute-use protocol from Figure 2(a), right-hand side.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Compute-use-uncompute-use protocol from Figure 2(a), right-hand side.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PromiseGateOptimization.lean:125](../../../../library/modules/promisegateoptimization/#decl-quantumblockencoding-promisegateoptimization-dirtycontrolledinvolutionequiv). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.PromiseGateOptimization.dirtyControlledInvolution_action" (lean := "QuantumBlockEncoding.PromiseGateOptimization.dirtyControlledInvolution_action")
*Plain-English reading.* Lean checks the proposition indexed as “dirty controlled involution action”; the hypotheses and conclusion in the code panel fix its exact scope. A dirty flag is restored and the requested controlled target is applied, provided the target is involutory.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* A dirty flag is restored and the requested controlled target is applied, provided the target is involutory.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PromiseGateOptimization.lean:135](../../../../library/modules/promisegateoptimization/#decl-quantumblockencoding-promisegateoptimization-dirtycontrolledinvolution-action). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.PromiseGateOptimization.dirtyControlledInvolution_unitary" (lean := "QuantumBlockEncoding.PromiseGateOptimization.dirtyControlledInvolution_unitary")
*Plain-English reading.* Lean checks the proposition indexed as “dirty controlled involution unitary”; the hypotheses and conclusion in the code panel fix its exact scope. The dirty-flag protocol is unitary because it is a basis permutation.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The dirty-flag protocol is unitary because it is a basis permutation.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PromiseGateOptimization.lean:146](../../../../library/modules/promisegateoptimization/#decl-quantumblockencoding-promisegateoptimization-dirtycontrolledinvolution-unitary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.PromiseGateOptimization.ControlledProtocolCost" (lean := "QuantumBlockEncoding.PromiseGateOptimization.ControlledProtocolCost")
*Plain-English reading.* This record groups the data and proof fields needed for “controlled protocol cost”. A proposition-valued field is a requirement until a constructor supplies it. Abstract operation counts exposed to the ASPBE planner.

*Formal status.* Data contract in the default import surface; proposition-valued fields are obligations, not automatically established facts.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Abstract operation counts exposed to the ASPBE planner.

*Declaration kind.* structure.

Source: [QuantumBlockEncoding/PromiseGateOptimization.lean:156](../../../../library/modules/promisegateoptimization/#decl-quantumblockencoding-promisegateoptimization-controlledprotocolcost). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.PromiseGateOptimization.cleanFlagProtocolCost" (lean := "QuantumBlockEncoding.PromiseGateOptimization.cleanFlagProtocolCost")
*Plain-English reading.* This definition gives the library's named construction or computation for “clean flag protocol cost”. Standard clean-flag construction: compute, use, uncompute.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Standard clean-flag construction: compute, use, uncompute.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PromiseGateOptimization.lean:164](../../../../library/modules/promisegateoptimization/#decl-quantumblockencoding-promisegateoptimization-cleanflagprotocolcost). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.PromiseGateOptimization.dirtyFlagProtocolCost" (lean := "QuantumBlockEncoding.PromiseGateOptimization.dirtyFlagProtocolCost")
*Plain-English reading.* This definition gives the library's named construction or computation for “dirty flag protocol cost”. Involutory dirty-flag construction: one extra controlled target use.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Involutory dirty-flag construction: one extra controlled target use.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/PromiseGateOptimization.lean:171](../../../../library/modules/promisegateoptimization/#decl-quantumblockencoding-promisegateoptimization-dirtyflagprotocolcost). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.PromiseGateOptimization.dirtyFlag_replaces_cleanFlag" (lean := "QuantumBlockEncoding.PromiseGateOptimization.dirtyFlag_replaces_cleanFlag")
*Plain-English reading.* Lean checks the proposition indexed as “dirty flag replaces clean flag”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/PromiseGateOptimization.lean:177](../../../../library/modules/promisegateoptimization/#decl-quantumblockencoding-promisegateoptimization-dirtyflag-replaces-cleanflag). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/RealIsometryCompletion.lean

7 explicit public declarations, in source order.

:::theorem "QuantumBlockEncoding.RealIsometryCompletion.exists_orthogonal_completion" (lean := "QuantumBlockEncoding.RealIsometryCompletion.exists_orthogonal_completion")
*Plain-English reading.* Lean checks the proposition indexed as “exists orthogonal completion”; the hypotheses and conclusion in the code panel fix its exact scope. An arbitrary injection specifies the physical positions of the active columns, so no assumption that they form a prefix is needed.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* An arbitrary injection specifies the physical positions of the active columns, so no assumption that they form a prefix is needed.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/RealIsometryCompletion.lean:20](../../../../library/modules/realisometrycompletion/#decl-quantumblockencoding-realisometrycompletion-exists-orthogonal-completion). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.RealIsometryCompletion.signFlip" (lean := "QuantumBlockEncoding.RealIsometryCompletion.signFlip")
*Plain-English reading.* This definition gives the library's named construction or computation for “sign flip”. Change the sign of one chosen column; every other column is unchanged.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Change the sign of one chosen column; every other column is unchanged.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/RealIsometryCompletion.lean:62](../../../../library/modules/realisometrycompletion/#decl-quantumblockencoding-realisometrycompletion-signflip). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.RealIsometryCompletion.signFlip_orthogonal" (lean := "QuantumBlockEncoding.RealIsometryCompletion.signFlip_orthogonal")
*Plain-English reading.* Lean checks the proposition indexed as “sign flip orthogonal”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/RealIsometryCompletion.lean:65](../../../../library/modules/realisometrycompletion/#decl-quantumblockencoding-realisometrycompletion-signflip-orthogonal). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.RealIsometryCompletion.signFlip_det" (lean := "QuantumBlockEncoding.RealIsometryCompletion.signFlip_det")
*Plain-English reading.* Lean checks the proposition indexed as “sign flip det”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/RealIsometryCompletion.lean:74](../../../../library/modules/realisometrycompletion/#decl-quantumblockencoding-realisometrycompletion-signflip-det). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.RealIsometryCompletion.mul_signFlip_preserves" (lean := "QuantumBlockEncoding.RealIsometryCompletion.mul_signFlip_preserves")
*Plain-English reading.* Lean checks the proposition indexed as “mul sign flip preserves”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/RealIsometryCompletion.lean:77](../../../../library/modules/realisometrycompletion/#decl-quantumblockencoding-realisometrycompletion-mul-signflip-preserves). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.RealIsometryCompletion.exists_specialOrthogonal_completion_of_unused" (lean := "QuantumBlockEncoding.RealIsometryCompletion.exists_specialOrthogonal_completion_of_unused")
*Plain-English reading.* Lean checks the proposition indexed as “exists special orthogonal completion of unused”; the hypotheses and conclusion in the code panel fix its exact scope. Correct a negative determinant using a known unused column.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Correct a negative determinant using a known unused column. All active columns remain exact, and the result lies in SO(N), not merely O(N).

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/RealIsometryCompletion.lean:83](../../../../library/modules/realisometrycompletion/#decl-quantumblockencoding-realisometrycompletion-exists-specialorthogonal-completion-of-unused). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.RealIsometryCompletion.exists_specialOrthogonal_completion" (lean := "QuantumBlockEncoding.RealIsometryCompletion.exists_specialOrthogonal_completion")
*Plain-English reading.* Lean checks the proposition indexed as “exists special orthogonal completion”; the hypotheses and conclusion in the code panel fix its exact scope. A strict active-dimension bound guarantees a spare orientation column.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* A strict active-dimension bound guarantees a spare orientation column.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/RealIsometryCompletion.lean:107](../../../../library/modules/realisometrycompletion/#decl-quantumblockencoding-realisometrycompletion-exists-specialorthogonal-completion). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/RectangularGivens.lean

19 explicit public declarations, in source order.

:::definition "QuantumBlockEncoding.RectangularGivens.sweep" (lean := "QuantumBlockEncoding.RectangularGivens.sweep")
*Plain-English reading.* This definition gives the library's named construction or computation for “sweep”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/RectangularGivens.lean:16](../../../../library/modules/rectangulargivens/#decl-quantumblockencoding-rectangulargivens-sweep). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.RectangularGivens.sweepSteps" (lean := "QuantumBlockEncoding.RectangularGivens.sweepSteps")
*Plain-English reading.* This definition gives the library's named construction or computation for “sweep steps”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/RectangularGivens.lean:26](../../../../library/modules/rectangulargivens/#decl-quantumblockencoding-rectangulargivens-sweepsteps). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.RectangularGivens.sweepSteps_action" (lean := "QuantumBlockEncoding.RectangularGivens.sweepSteps_action")
*Plain-English reading.* Lean checks the proposition indexed as “sweep steps action”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/RectangularGivens.lean:36](../../../../library/modules/rectangulargivens/#decl-quantumblockencoding-rectangulargivens-sweepsteps-action). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.RectangularGivens.UpperPrefix" (lean := "QuantumBlockEncoding.RectangularGivens.UpperPrefix")
*Plain-English reading.* This definition gives the library's named construction or computation for “upper prefix”. Previously eliminated columns vanish strictly below their diagonal.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Previously eliminated columns vanish strictly below their diagonal.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/RectangularGivens.lean:49](../../../../library/modules/rectangulargivens/#decl-quantumblockencoding-rectangulargivens-upperprefix). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.RectangularGivens.columnSweep_upperPrefix" (lean := "QuantumBlockEncoding.RectangularGivens.columnSweep_upperPrefix")
*Plain-English reading.* Lean checks the proposition indexed as “column sweep upper prefix”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/RectangularGivens.lean:52](../../../../library/modules/rectangulargivens/#decl-quantumblockencoding-rectangulargivens-columnsweep-upperprefix). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.RectangularGivens.sweep_upperPrefix" (lean := "QuantumBlockEncoding.RectangularGivens.sweep_upperPrefix")
*Plain-English reading.* Lean checks the proposition indexed as “sweep upper prefix”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/RectangularGivens.lean:67](../../../../library/modules/rectangulargivens/#decl-quantumblockencoding-rectangulargivens-sweep-upperprefix). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.RectangularGivens.sweepSteps_length_le" (lean := "QuantumBlockEncoding.RectangularGivens.sweepSteps_length_le")
*Plain-English reading.* Lean checks the proposition indexed as “sweep steps length le”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/RectangularGivens.lean:85](../../../../library/modules/rectangulargivens/#decl-quantumblockencoding-rectangulargivens-sweepsteps-length-le). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.RectangularGivens.stepsMatrix_orthogonal" (lean := "QuantumBlockEncoding.RectangularGivens.stepsMatrix_orthogonal")
*Plain-English reading.* Lean checks the proposition indexed as “steps matrix orthogonal”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/RectangularGivens.lean:100](../../../../library/modules/rectangulargivens/#decl-quantumblockencoding-rectangulargivens-stepsmatrix-orthogonal). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.RectangularGivens.stepsMatrix_det" (lean := "QuantumBlockEncoding.RectangularGivens.stepsMatrix_det")
*Plain-English reading.* Lean checks the proposition indexed as “steps matrix det”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/RectangularGivens.lean:110](../../../../library/modules/rectangulargivens/#decl-quantumblockencoding-rectangulargivens-stepsmatrix-det). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.RectangularGivens.decompose" (lean := "QuantumBlockEncoding.RectangularGivens.decompose")
*Plain-English reading.* This definition gives the library's named construction or computation for “decompose”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/RectangularGivens.lean:118](../../../../library/modules/rectangulargivens/#decl-quantumblockencoding-rectangulargivens-decompose). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.RectangularGivens.reduced" (lean := "QuantumBlockEncoding.RectangularGivens.reduced")
*Plain-English reading.* This definition gives the library's named construction or computation for “reduced”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/RectangularGivens.lean:121](../../../../library/modules/rectangulargivens/#decl-quantumblockencoding-rectangulargivens-reduced). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.RectangularGivens.transform" (lean := "QuantumBlockEncoding.RectangularGivens.transform")
*Plain-English reading.* This definition gives the library's named construction or computation for “transform”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/RectangularGivens.lean:124](../../../../library/modules/rectangulargivens/#decl-quantumblockencoding-rectangulargivens-transform). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.RectangularGivens.decompose_action" (lean := "QuantumBlockEncoding.RectangularGivens.decompose_action")
*Plain-English reading.* Lean checks the proposition indexed as “decompose action”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/RectangularGivens.lean:127](../../../../library/modules/rectangulargivens/#decl-quantumblockencoding-rectangulargivens-decompose-action). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.RectangularGivens.transform_mul" (lean := "QuantumBlockEncoding.RectangularGivens.transform_mul")
*Plain-English reading.* Lean checks the proposition indexed as “transform mul”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/RectangularGivens.lean:130](../../../../library/modules/rectangulargivens/#decl-quantumblockencoding-rectangulargivens-transform-mul). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.RectangularGivens.reduced_zero_below" (lean := "QuantumBlockEncoding.RectangularGivens.reduced_zero_below")
*Plain-English reading.* Lean checks the proposition indexed as “reduced zero below”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/RectangularGivens.lean:135](../../../../library/modules/rectangulargivens/#decl-quantumblockencoding-rectangulargivens-reduced-zero-below). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.RectangularGivens.decompose_length_le" (lean := "QuantumBlockEncoding.RectangularGivens.decompose_length_le")
*Plain-English reading.* Lean checks the proposition indexed as “decompose length le”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/RectangularGivens.lean:141](../../../../library/modules/rectangulargivens/#decl-quantumblockencoding-rectangulargivens-decompose-length-le). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.RectangularGivens.transform_orthogonal" (lean := "QuantumBlockEncoding.RectangularGivens.transform_orthogonal")
*Plain-English reading.* Lean checks the proposition indexed as “transform orthogonal”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/RectangularGivens.lean:144](../../../../library/modules/rectangulargivens/#decl-quantumblockencoding-rectangulargivens-transform-orthogonal). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.RectangularGivens.transform_det" (lean := "QuantumBlockEncoding.RectangularGivens.transform_det")
*Plain-English reading.* Lean checks the proposition indexed as “transform det”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/RectangularGivens.lean:147](../../../../library/modules/rectangulargivens/#decl-quantumblockencoding-rectangulargivens-transform-det). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.RectangularGivens.exact_recovery" (lean := "QuantumBlockEncoding.RectangularGivens.exact_recovery")
*Plain-English reading.* Lean checks the proposition indexed as “exact recovery”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/RectangularGivens.lean:150](../../../../library/modules/rectangulargivens/#decl-quantumblockencoding-rectangulargivens-exact-recovery). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/ReversibleClassical.lean

7 explicit public declarations, in source order.

:::definition "QuantumBlockEncoding.ReversibleGate" (lean := "QuantumBlockEncoding.ReversibleGate")
*Plain-English reading.* This type lists the allowed alternatives for “reversible gate”; its constructors are the cases that downstream code must handle.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* inductive.

Source: [QuantumBlockEncoding/ReversibleClassical.lean:14](../../../../library/modules/reversibleclassical/#decl-quantumblockencoding-reversiblegate). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.ReversibleProgram" (lean := "QuantumBlockEncoding.ReversibleProgram")
*Plain-English reading.* This abbreviation gives a shorter name to the type or expression used for “reversible program”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* abbrev.

Source: [QuantumBlockEncoding/ReversibleClassical.lean:22](../../../../library/modules/reversibleclassical/#decl-quantumblockencoding-reversibleprogram). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.ccxBasisAction" (lean := "QuantumBlockEncoding.ccxBasisAction")
*Plain-English reading.* This definition gives the library's named construction or computation for “ccx basis action”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/ReversibleClassical.lean:24](../../../../library/modules/reversibleclassical/#decl-quantumblockencoding-ccxbasisaction). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ccxBasisAction_involutive" (lean := "QuantumBlockEncoding.ccxBasisAction_involutive")
*Plain-English reading.* Lean checks the proposition indexed as “ccx basis action involutive”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ReversibleClassical.lean:30](../../../../library/modules/reversibleclassical/#decl-quantumblockencoding-ccxbasisaction-involutive). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.ccxBasisEquiv" (lean := "QuantumBlockEncoding.ccxBasisEquiv")
*Plain-English reading.* This definition gives the library's named construction or computation for “ccx basis equiv”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/ReversibleClassical.lean:52](../../../../library/modules/reversibleclassical/#decl-quantumblockencoding-ccxbasisequiv). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.evalReversibleGate" (lean := "QuantumBlockEncoding.evalReversibleGate")
*Plain-English reading.* This definition gives the library's named construction or computation for “eval reversible gate”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/ReversibleClassical.lean:63](../../../../library/modules/reversibleclassical/#decl-quantumblockencoding-evalreversiblegate). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.evalReversibleProgram" (lean := "QuantumBlockEncoding.evalReversibleProgram")
*Plain-English reading.* This definition gives the library's named construction or computation for “eval reversible program”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/ReversibleClassical.lean:70](../../../../library/modules/reversibleclassical/#decl-quantumblockencoding-evalreversibleprogram). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/SelectedRyPlane.lean

25 explicit public declarations, in source order.

:::definition "QuantumBlockEncoding.selectedRyAngles" (lean := "QuantumBlockEncoding.selectedRyAngles")
*Plain-English reading.* This definition gives the library's named construction or computation for “selected ry angles”. One nonzero entry in a multiplexed angle table; all other branches are identity.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* One nonzero entry in a multiplexed angle table; all other branches are identity.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/SelectedRyPlane.lean:20](../../../../library/modules/selectedryplane/#decl-quantumblockencoding-selectedryangles). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.compileSelectedRy" (lean := "QuantumBlockEncoding.compileSelectedRy")
*Plain-English reading.* This definition gives the library's named construction or computation for “compile selected ry”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/SelectedRyPlane.lean:24](../../../../library/modules/selectedryplane/#decl-quantumblockencoding-compileselectedry). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.compileSelectedRy_eval_block" (lean := "QuantumBlockEncoding.compileSelectedRy_eval_block")
*Plain-English reading.* Lean checks the proposition indexed as “compile selected ry eval block”; the hypotheses and conclusion in the code panel fix its exact scope. General block version, allowing unused passive wires.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* General block version, allowing unused passive wires.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/SelectedRyPlane.lean:33](../../../../library/modules/selectedryplane/#decl-quantumblockencoding-compileselectedry-eval-block). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.realRyPlaneBlock" (lean := "QuantumBlockEncoding.realRyPlaneBlock")
*Plain-English reading.* This definition gives the library's named construction or computation for “real ry plane block”. The real matrix underlying the standard half-angle RY convention.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The real matrix underlying the standard half-angle RY convention.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/SelectedRyPlane.lean:42](../../../../library/modules/selectedryplane/#decl-quantumblockencoding-realryplaneblock). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.standardRyMatrix_eq_realRyPlaneBlock" (lean := "QuantumBlockEncoding.standardRyMatrix_eq_realRyPlaneBlock")
*Plain-English reading.* Lean checks the proposition indexed as “standard ry matrix eq real ry plane block”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/SelectedRyPlane.lean:47](../../../../library/modules/selectedryplane/#decl-quantumblockencoding-standardrymatrix-eq-realryplaneblock). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.realRyPlaneBlock_zero" (lean := "QuantumBlockEncoding.realRyPlaneBlock_zero")
*Plain-English reading.* Lean checks the proposition indexed as “real ry plane block zero”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/SelectedRyPlane.lean:52](../../../../library/modules/selectedryplane/#decl-quantumblockencoding-realryplaneblock-zero). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.selectedRyPlaneMatrix" (lean := "QuantumBlockEncoding.selectedRyPlaneMatrix")
*Plain-English reading.* This definition gives the library's named construction or computation for “selected ry plane matrix”. A real two-level plane: the chosen pair is ordered by target bit 0, then 1.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* A real two-level plane: the chosen pair is ordered by target bit 0, then 1. Every other computational-basis vector is fixed, including its sign.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/SelectedRyPlane.lean:58](../../../../library/modules/selectedryplane/#decl-quantumblockencoding-selectedryplanematrix). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.primitiveControlAssignment_eq_iff" (lean := "QuantumBlockEncoding.primitiveControlAssignment_eq_iff")
*Plain-English reading.* Lean checks the proposition indexed as “primitive control assignment eq iff”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/SelectedRyPlane.lean:67](../../../../library/modules/selectedryplane/#decl-quantumblockencoding-primitivecontrolassignment-eq-iff). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.compileSelectedRy_eval_plane" (lean := "QuantumBlockEncoding.compileSelectedRy_eval_plane")
*Plain-English reading.* Lean checks the proposition indexed as “compile selected ry eval plane”; the hypotheses and conclusion in the code panel fix its exact scope. Full-control specialization: an actual finite RY/CX circuit equals the complex embedding of the explicitly real two-level plane.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Full-control specialization: an actual finite RY/CX circuit equals the complex embedding of the explicitly real two-level plane.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/SelectedRyPlane.lean:82](../../../../library/modules/selectedryplane/#decl-quantumblockencoding-compileselectedry-eval-plane). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.selectedRyPlaneMatrix_fixed_column" (lean := "QuantumBlockEncoding.selectedRyPlaneMatrix_fixed_column")
*Plain-English reading.* Lean checks the proposition indexed as “selected ry plane matrix fixed column”; the hypotheses and conclusion in the code panel fix its exact scope. Entry-level complement statement: no amplitude or phase is changed outside the selected pair.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Entry-level complement statement: no amplitude or phase is changed outside the selected pair.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/SelectedRyPlane.lean:122](../../../../library/modules/selectedryplane/#decl-quantumblockencoding-selectedryplanematrix-fixed-column). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.selectedRyPlaneMatrix_selected_entry" (lean := "QuantumBlockEncoding.selectedRyPlaneMatrix_selected_entry")
*Plain-English reading.* Lean checks the proposition indexed as “selected ry plane matrix selected entry”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/SelectedRyPlane.lean:131](../../../../library/modules/selectedryplane/#decl-quantumblockencoding-selectedryplanematrix-selected-entry). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.compileSelectedRy_ryCount" (lean := "QuantumBlockEncoding.compileSelectedRy_ryCount")
*Plain-English reading.* Lean checks the proposition indexed as “compile selected ry ry count”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/SelectedRyPlane.lean:143](../../../../library/modules/selectedryplane/#decl-quantumblockencoding-compileselectedry-rycount). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.compileSelectedRy_cxCount" (lean := "QuantumBlockEncoding.compileSelectedRy_cxCount")
*Plain-English reading.* Lean checks the proposition indexed as “compile selected ry cx count”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/SelectedRyPlane.lean:150](../../../../library/modules/selectedryplane/#decl-quantumblockencoding-compileselectedry-cxcount). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.SelectedRyStep" (lean := "QuantumBlockEncoding.SelectedRyStep")
*Plain-English reading.* This record groups the data and proof fields needed for “selected ry step”. A proposition-valued field is a requirement until a constructor supplies it. A concrete selected-rotation instruction, not an assumed target operator.

*Formal status.* Data contract in the default import surface; proposition-valued fields are obligations, not automatically established facts.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* A concrete selected-rotation instruction, not an assumed target operator.

*Declaration kind.* structure.

Source: [QuantumBlockEncoding/SelectedRyPlane.lean:159](../../../../library/modules/selectedryplane/#decl-quantumblockencoding-selectedrystep). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.SelectedRyStep.compile" (lean := "QuantumBlockEncoding.SelectedRyStep.compile")
*Plain-English reading.* This definition gives the library's named construction or computation for “compile”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/SelectedRyPlane.lean:166](../../../../library/modules/selectedryplane/#decl-quantumblockencoding-selectedrystep-compile). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.SelectedRyStep.matrix" (lean := "QuantumBlockEncoding.SelectedRyStep.matrix")
*Plain-English reading.* This definition gives the library's named construction or computation for “matrix”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/SelectedRyPlane.lean:170](../../../../library/modules/selectedryplane/#decl-quantumblockencoding-selectedrystep-matrix). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.compileSelectedRySteps" (lean := "QuantumBlockEncoding.compileSelectedRySteps")
*Plain-English reading.* This definition gives the library's named construction or computation for “compile selected ry steps”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/SelectedRyPlane.lean:176](../../../../library/modules/selectedryplane/#decl-quantumblockencoding-compileselectedrysteps). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.selectedRyStepsMatrix" (lean := "QuantumBlockEncoding.selectedRyStepsMatrix")
*Plain-English reading.* This definition gives the library's named construction or computation for “selected ry steps matrix”. Chronological product: the last listed stage multiplies on the left.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Chronological product: the last listed stage multiplies on the left.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/SelectedRyPlane.lean:181](../../../../library/modules/selectedryplane/#decl-quantumblockencoding-selectedrystepsmatrix). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.compileSelectedRySteps_eval" (lean := "QuantumBlockEncoding.compileSelectedRySteps_eval")
*Plain-English reading.* Lean checks the proposition indexed as “compile selected ry steps eval”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/SelectedRyPlane.lean:187](../../../../library/modules/selectedryplane/#decl-quantumblockencoding-compileselectedrysteps-eval). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.compileSelectedRySteps_ryCount" (lean := "QuantumBlockEncoding.compileSelectedRySteps_ryCount")
*Plain-English reading.* Lean checks the proposition indexed as “compile selected ry steps ry count”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/SelectedRyPlane.lean:201](../../../../library/modules/selectedryplane/#decl-quantumblockencoding-compileselectedrysteps-rycount). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.compileSelectedRySteps_cxCount" (lean := "QuantumBlockEncoding.compileSelectedRySteps_cxCount")
*Plain-English reading.* Lean checks the proposition indexed as “compile selected ry steps cx count”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/SelectedRyPlane.lean:213](../../../../library/modules/selectedryplane/#decl-quantumblockencoding-compileselectedrysteps-cxcount). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.compileSelectedRySteps_noOracle" (lean := "QuantumBlockEncoding.compileSelectedRySteps_noOracle")
*Plain-English reading.* Lean checks the proposition indexed as “compile selected ry steps no oracle”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/SelectedRyPlane.lean:225](../../../../library/modules/selectedryplane/#decl-quantumblockencoding-compileselectedrysteps-nooracle). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.compileUniformlyControlledRy_gateCount" (lean := "QuantumBlockEncoding.compileUniformlyControlledRy_gateCount")
*Plain-English reading.* Lean checks the proposition indexed as “compile uniformly controlled ry gate count”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/SelectedRyPlane.lean:229](../../../../library/modules/selectedryplane/#decl-quantumblockencoding-compileuniformlycontrolledry-gatecount). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.compileSelectedRySteps_gateCount" (lean := "QuantumBlockEncoding.compileSelectedRySteps_gateCount")
*Plain-English reading.* Lean checks the proposition indexed as “compile selected ry steps gate count”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/SelectedRyPlane.lean:244](../../../../library/modules/selectedryplane/#decl-quantumblockencoding-compileselectedrysteps-gatecount). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.compileSelectedRySteps_cubic_bound" (lean := "QuantumBlockEncoding.compileSelectedRySteps_cubic_bound")
*Plain-English reading.* Lean checks the proposition indexed as “compile selected ry steps cubic bound”; the hypotheses and conclusion in the code panel fix its exact scope. Concrete cubic bound for a supplied finite list.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Concrete cubic bound for a supplied finite list. The length hypothesis is the decomposition obligation, not a claim that arbitrary stages already have such decompositions. With 'S = 2^controls', at most 'n (2S)(2S-1)/2' selected planes compile to at most '6 n S^3' primitive gates, with no oracle calls.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/SelectedRyPlane.lean:263](../../../../library/modules/selectedryplane/#decl-quantumblockencoding-compileselectedrysteps-cubic-bound). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/SelectedRyTrace.lean

10 explicit public declarations, in source order.

:::definition "QuantumBlockEncoding.SelectedRyTrace.Gate" (lean := "QuantumBlockEncoding.SelectedRyTrace.Gate")
*Plain-English reading.* This type lists the allowed alternatives for “gate”; its constructors are the cases that downstream code must handle.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* inductive.

Source: [QuantumBlockEncoding/SelectedRyTrace.lean:14](../../../../library/modules/selectedrytrace/#decl-quantumblockencoding-selectedrytrace-gate). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.SelectedRyTrace.Gate.instantiate" (lean := "QuantumBlockEncoding.SelectedRyTrace.Gate.instantiate")
*Plain-English reading.* This definition gives the library's named construction or computation for “instantiate”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/SelectedRyTrace.lean:19](../../../../library/modules/selectedrytrace/#decl-quantumblockencoding-selectedrytrace-gate-instantiate). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.SelectedRyTrace.instantiate" (lean := "QuantumBlockEncoding.SelectedRyTrace.instantiate")
*Plain-English reading.* This definition gives the library's named construction or computation for “instantiate”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/SelectedRyTrace.lean:23](../../../../library/modules/selectedrytrace/#decl-quantumblockencoding-selectedrytrace-instantiate). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.SelectedRyTrace.compile" (lean := "QuantumBlockEncoding.SelectedRyTrace.compile")
*Plain-English reading.* This definition gives the library's named construction or computation for “compile”. The supplied tuple order is the recursive control order.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The supplied tuple order is the recursive control order. No dense data-state amplitude table is needed: these coefficients concern only the local controls.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/SelectedRyTrace.lean:28](../../../../library/modules/selectedrytrace/#decl-quantumblockencoding-selectedrytrace-compile). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.SelectedRyTrace.eval_congr" (lean := "QuantumBlockEncoding.SelectedRyTrace.eval_congr")
*Plain-English reading.* Lean checks the proposition indexed as “eval congr”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/SelectedRyTrace.lean:43](../../../../library/modules/selectedrytrace/#decl-quantumblockencoding-selectedrytrace-eval-congr). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.SelectedRyTrace.compile_refines" (lean := "QuantumBlockEncoding.SelectedRyTrace.compile_refines")
*Plain-English reading.* Lean checks the proposition indexed as “compile refines”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/SelectedRyTrace.lean:53](../../../../library/modules/selectedrytrace/#decl-quantumblockencoding-selectedrytrace-compile-refines). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.SelectedRyTrace.selected" (lean := "QuantumBlockEncoding.SelectedRyTrace.selected")
*Plain-English reading.* This definition gives the library's named construction or computation for “selected”. A selected plane has one coefficient equal to one; all other controls select zero.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* A selected plane has one coefficient equal to one; all other controls select zero.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/SelectedRyTrace.lean:90](../../../../library/modules/selectedrytrace/#decl-quantumblockencoding-selectedrytrace-selected). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.SelectedRyTrace.selected_refines" (lean := "QuantumBlockEncoding.SelectedRyTrace.selected_refines")
*Plain-English reading.* Lean checks the proposition indexed as “selected refines”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/SelectedRyTrace.lean:95](../../../../library/modules/selectedrytrace/#decl-quantumblockencoding-selectedrytrace-selected-refines). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.SelectedRyTrace.compile_length" (lean := "QuantumBlockEncoding.SelectedRyTrace.compile_length")
*Plain-English reading.* Lean checks the proposition indexed as “compile length”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/SelectedRyTrace.lean:105](../../../../library/modules/selectedrytrace/#decl-quantumblockencoding-selectedrytrace-compile-length). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.SelectedRyTrace.selected_gateCount" (lean := "QuantumBlockEncoding.SelectedRyTrace.selected_gateCount")
*Plain-English reading.* Lean checks the proposition indexed as “selected gate count”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/SelectedRyTrace.lean:117](../../../../library/modules/selectedrytrace/#decl-quantumblockencoding-selectedrytrace-selected-gatecount). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/SequentialBondPreparation.lean

35 explicit public declarations, in source order.

:::definition "QuantumBlockEncoding.SequentialBondPreparation.Core" (lean := "QuantumBlockEncoding.SequentialBondPreparation.Core")
*Plain-English reading.* This abbreviation gives a shorter name to the type or expression used for “core”. A core emits one bit and changes the bond.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* A core emits one bit and changes the bond.

*Declaration kind.* abbrev.

Source: [QuantumBlockEncoding/SequentialBondPreparation.lean:29](../../../../library/modules/sequentialbondpreparation/#decl-quantumblockencoding-sequentialbondpreparation-core). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.SequentialBondPreparation.Stage" (lean := "QuantumBlockEncoding.SequentialBondPreparation.Stage")
*Plain-English reading.* This abbreviation gives a shorter name to the type or expression used for “stage”. The square, full local matrix; its nonzero-bit input columns are not specified by a tensor core and must be supplied by an actual completion.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The square, full local matrix; its nonzero-bit input columns are not specified by a tensor core and must be supplied by an actual completion.

*Declaration kind.* abbrev.

Source: [QuantumBlockEncoding/SequentialBondPreparation.lean:33](../../../../library/modules/sequentialbondpreparation/#decl-quantumblockencoding-sequentialbondpreparation-stage). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.SequentialBondPreparation.BondState" (lean := "QuantumBlockEncoding.SequentialBondPreparation.BondState")
*Plain-English reading.* This abbreviation gives a shorter name to the type or expression used for “bond state”. State amplitudes after exactly 'n' output bits have been emitted.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* State amplitudes after exactly 'n' output bits have been emitted.

*Declaration kind.* abbrev.

Source: [QuantumBlockEncoding/SequentialBondPreparation.lean:36](../../../../library/modules/sequentialbondpreparation/#decl-quantumblockencoding-sequentialbondpreparation-bondstate). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.SequentialBondPreparation.freshZero" (lean := "QuantumBlockEncoding.SequentialBondPreparation.freshZero")
*Plain-English reading.* This definition gives the library's named construction or computation for “fresh zero”. Add the next output bit in zero, without changing any existing amplitude.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Add the next output bit in zero, without changing any existing amplitude.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/SequentialBondPreparation.lean:39](../../../../library/modules/sequentialbondpreparation/#decl-quantumblockencoding-sequentialbondpreparation-freshzero). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.SequentialBondPreparation.liftStage" (lean := "QuantumBlockEncoding.SequentialBondPreparation.liftStage")
*Plain-English reading.* This definition gives the library's named construction or computation for “lift stage”. Local stage tensored with the identity on all already emitted bits.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Local stage tensored with the identity on all already emitted bits.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/SequentialBondPreparation.lean:44](../../../../library/modules/sequentialbondpreparation/#decl-quantumblockencoding-sequentialbondpreparation-liftstage). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.SequentialBondPreparation.liftStage_apply" (lean := "QuantumBlockEncoding.SequentialBondPreparation.liftStage_apply")
*Plain-English reading.* Lean checks the proposition indexed as “lift stage apply”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/SequentialBondPreparation.lean:50](../../../../library/modules/sequentialbondpreparation/#decl-quantumblockencoding-sequentialbondpreparation-liftstage-apply). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.SequentialBondPreparation.step" (lean := "QuantumBlockEncoding.SequentialBondPreparation.step")
*Plain-English reading.* This definition gives the library's named construction or computation for “step”. A genuine matrix-vector action followed only by a basis regrouping.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* A genuine matrix-vector action followed only by a basis regrouping.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/SequentialBondPreparation.lean:56](../../../../library/modules/sequentialbondpreparation/#decl-quantumblockencoding-sequentialbondpreparation-step). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.SequentialBondPreparation.step_apply" (lean := "QuantumBlockEncoding.SequentialBondPreparation.step_apply")
*Plain-English reading.* Lean checks the proposition indexed as “step apply”; the hypotheses and conclusion in the code panel fix its exact scope. Only the clean-input columns of a local stage affect an emitted state.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Only the clean-input columns of a local stage affect an emitted state. The sum over passive prefixes and fresh-bit inputs collapses exactly.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/SequentialBondPreparation.lean:64](../../../../library/modules/sequentialbondpreparation/#decl-quantumblockencoding-sequentialbondpreparation-step-apply). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.SequentialBondPreparation.liftStage_unitary" (lean := "QuantumBlockEncoding.SequentialBondPreparation.liftStage_unitary")
*Plain-English reading.* Lean checks the proposition indexed as “lift stage unitary”; the hypotheses and conclusion in the code panel fix its exact scope. Unitarity of the full local completion gives unitarity after adding arbitrarily many passive emitted wires.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Unitarity of the full local completion gives unitarity after adding arbitrarily many passive emitted wires.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/SequentialBondPreparation.lean:72](../../../../library/modules/sequentialbondpreparation/#decl-quantumblockencoding-sequentialbondpreparation-liftstage-unitary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.SequentialBondPreparation.coreSlice" (lean := "QuantumBlockEncoding.SequentialBondPreparation.coreSlice")
*Plain-English reading.* This definition gives the library's named construction or computation for “core slice”. Fixing an emitted bit leaves a bond-to-bond transfer matrix.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Fixing an emitted bit leaves a bond-to-bond transfer matrix.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/SequentialBondPreparation.lean:81](../../../../library/modules/sequentialbondpreparation/#decl-quantumblockencoding-sequentialbondpreparation-coreslice). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.SequentialBondPreparation.transfer" (lean := "QuantumBlockEncoding.SequentialBondPreparation.transfer")
*Plain-English reading.* This definition gives the library's named construction or computation for “transfer”. Chronological tensor contraction.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Chronological tensor contraction. The last emitted core multiplies on the left, in the same convention as 'evalPrimitiveCircuit'.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/SequentialBondPreparation.lean:86](../../../../library/modules/sequentialbondpreparation/#decl-quantumblockencoding-sequentialbondpreparation-transfer). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.SequentialBondPreparation.run" (lean := "QuantumBlockEncoding.SequentialBondPreparation.run")
*Plain-English reading.* This definition gives the library's named construction or computation for “run”. Explicit initial boundary, followed by sequential matrix actions.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Explicit initial boundary, followed by sequential matrix actions. At stage zero there are no output bits and the bond amplitude is 'boundary'.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/SequentialBondPreparation.lean:93](../../../../library/modules/sequentialbondpreparation/#decl-quantumblockencoding-sequentialbondpreparation-run). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.SequentialBondPreparation.run_eq_transfer" (lean := "QuantumBlockEncoding.SequentialBondPreparation.run_eq_transfer")
*Plain-English reading.* Lean checks the proposition indexed as “run eq transfer”; the hypotheses and conclusion in the code panel fix its exact scope. Local clean-column equalities suffice to identify the complete state with the tensor contraction at every length; no global state action is assumed.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Local clean-column equalities suffice to identify the complete state with the tensor contraction at every length; no global state action is assumed.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/SequentialBondPreparation.lean:100](../../../../library/modules/sequentialbondpreparation/#decl-quantumblockencoding-sequentialbondpreparation-run-eq-transfer). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.SequentialBondPreparation.basisBoundary" (lean := "QuantumBlockEncoding.SequentialBondPreparation.basisBoundary")
*Plain-English reading.* This definition gives the library's named construction or computation for “basis boundary”. Initial computational-basis boundary for the bond.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Initial computational-basis boundary for the bond.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/SequentialBondPreparation.lean:113](../../../../library/modules/sequentialbondpreparation/#decl-quantumblockencoding-sequentialbondpreparation-basisboundary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.SequentialBondPreparation.run_basisBoundary" (lean := "QuantumBlockEncoding.SequentialBondPreparation.run_basisBoundary")
*Plain-English reading.* Lean checks the proposition indexed as “run basis boundary”; the hypotheses and conclusion in the code panel fix its exact scope. Starting from a basis boundary extracts the corresponding transfer column.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Starting from a basis boundary extracts the corresponding transfer column.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/SequentialBondPreparation.lean:116](../../../../library/modules/sequentialbondpreparation/#decl-quantumblockencoding-sequentialbondpreparation-run-basisboundary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.SequentialBondPreparation.transfer_boundary_supported" (lean := "QuantumBlockEncoding.SequentialBondPreparation.transfer_boundary_supported")
*Plain-English reading.* Lean checks the proposition indexed as “transfer boundary supported”; the hypotheses and conclusion in the code panel fix its exact scope. If every core is zero outside the next active bond set, its contracted state has that support.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* If every core is zero outside the next active bond set, its contracted state has that support. This allows ranks to shrink inside a fixed register.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/SequentialBondPreparation.lean:125](../../../../library/modules/sequentialbondpreparation/#decl-quantumblockencoding-sequentialbondpreparation-transfer-boundary-supported). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.SequentialBondPreparation.run_eq_transfer_of_supported" (lean := "QuantumBlockEncoding.SequentialBondPreparation.run_eq_transfer_of_supported")
*Plain-English reading.* Lean checks the proposition indexed as “run eq transfer of supported”; the hypotheses and conclusion in the code panel fix its exact scope. Rank-changing version of 'run\_eq\_transfer': only columns for occupied input bond labels must agree.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Rank-changing version of 'run\_eq\_transfer': only columns for occupied input bond labels must agree. Unused completion columns may be arbitrary. The support invariant is proved from the cores, not assumed of the full run.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/SequentialBondPreparation.lean:140](../../../../library/modules/sequentialbondpreparation/#decl-quantumblockencoding-sequentialbondpreparation-run-eq-transfer-of-supported). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.SequentialBondPreparation.run_supported" (lean := "QuantumBlockEncoding.SequentialBondPreparation.run_supported")
*Plain-English reading.* Lean checks the proposition indexed as “run supported”; the hypotheses and conclusion in the code panel fix its exact scope. No leakage to inactive labels at any intermediate stage.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* No leakage to inactive labels at any intermediate stage.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/SequentialBondPreparation.lean:166](../../../../library/modules/sequentialbondpreparation/#decl-quantumblockencoding-sequentialbondpreparation-run-supported). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.SequentialBondPreparation.terminalCore" (lean := "QuantumBlockEncoding.SequentialBondPreparation.terminalCore")
*Plain-English reading.* This definition gives the library's named construction or computation for “terminal core”. A terminal bond of dimension one, embedded at a chosen padded label.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* A terminal bond of dimension one, embedded at a chosen padded label.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/SequentialBondPreparation.lean:177](../../../../library/modules/sequentialbondpreparation/#decl-quantumblockencoding-sequentialbondpreparation-terminalcore). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.SequentialBondPreparation.step_terminalCore" (lean := "QuantumBlockEncoding.SequentialBondPreparation.step_terminalCore")
*Plain-English reading.* Lean checks the proposition indexed as “step terminal core”; the hypotheses and conclusion in the code panel fix its exact scope. Appending a rank-one terminal bond factors the \*whole\* state as a clean bond times the contracted output amplitude; this is not a projection theorem.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Appending a rank-one terminal bond factors the \*whole\* state as a clean bond times the contracted output amplitude; this is not a projection theorem.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/SequentialBondPreparation.lean:182](../../../../library/modules/sequentialbondpreparation/#decl-quantumblockencoding-sequentialbondpreparation-step-terminalcore). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.SequentialBondPreparation.step_terminalCore_no_leakage" (lean := "QuantumBlockEncoding.SequentialBondPreparation.step_terminalCore_no_leakage")
*Plain-English reading.* Lean checks the proposition indexed as “step terminal core no leakage”; the hypotheses and conclusion in the code panel fix its exact scope. Zero leakage to every non-clean bond label, with no measurement or postselection.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Zero leakage to every non-clean bond label, with no measurement or postselection. Local terminal-column synthesis is the explicit prerequisite.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/SequentialBondPreparation.lean:194](../../../../library/modules/sequentialbondpreparation/#decl-quantumblockencoding-sequentialbondpreparation-step-terminalcore-no-leakage). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.SequentialBondPreparation.step_terminalCore_of_supported" (lean := "QuantumBlockEncoding.SequentialBondPreparation.step_terminalCore_of_supported")
*Plain-English reading.* Lean checks the proposition indexed as “step terminal core of supported”; the hypotheses and conclusion in the code panel fix its exact scope. Padded-register terminal cleanup on the occupied input subspace only.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Padded-register terminal cleanup on the occupied input subspace only. Requiring this terminal action on \*all\* padded input columns would be incompatible with unitarity for bond dimension greater than two.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/SequentialBondPreparation.lean:204](../../../../library/modules/sequentialbondpreparation/#decl-quantumblockencoding-sequentialbondpreparation-step-terminalcore-of-supported). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.SequentialBondPreparation.run_terminal_clean" (lean := "QuantumBlockEncoding.SequentialBondPreparation.run_terminal_clean")
*Plain-English reading.* Lean checks the proposition indexed as “run terminal clean”; the hypotheses and conclusion in the code panel fix its exact scope. Complete state action after a supported sequential run and one terminal stage.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Complete state action after a supported sequential run and one terminal stage. The target is the explicit transfer contraction, and every non-clean bond amplitude is zero. No postselection, target-state oracle, or global state-action equality is among the hypotheses.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/SequentialBondPreparation.lean:228](../../../../library/modules/sequentialbondpreparation/#decl-quantumblockencoding-sequentialbondpreparation-run-terminal-clean). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.SequentialBondPreparation.localBasisEquiv" (lean := "QuantumBlockEncoding.SequentialBondPreparation.localBasisEquiv")
*Plain-English reading.* This definition gives the library's named construction or computation for “local basis equiv”. The local primitive circuit uses the low 'q' wires for the bond and its highest wire for the fresh emitted bit.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The local primitive circuit uses the low 'q' wires for the bond and its highest wire for the fresh emitted bit. This equivalence makes that order explicit.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/SequentialBondPreparation.lean:248](../../../../library/modules/sequentialbondpreparation/#decl-quantumblockencoding-sequentialbondpreparation-localbasisequiv). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.SequentialBondPreparation.circuitStage" (lean := "QuantumBlockEncoding.SequentialBondPreparation.circuitStage")
*Plain-English reading.* This definition gives the library's named construction or computation for “circuit stage”. Exact local stage supplied by a primitive circuit, not an opaque oracle.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Exact local stage supplied by a primitive circuit, not an opaque oracle.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/SequentialBondPreparation.lean:252](../../../../library/modules/sequentialbondpreparation/#decl-quantumblockencoding-sequentialbondpreparation-circuitstage). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.SequentialBondPreparation.circuitStage_apply" (lean := "QuantumBlockEncoding.SequentialBondPreparation.circuitStage_apply")
*Plain-English reading.* Lean checks the proposition indexed as “circuit stage apply”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/SequentialBondPreparation.lean:256](../../../../library/modules/sequentialbondpreparation/#decl-quantumblockencoding-sequentialbondpreparation-circuitstage-apply). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.SequentialBondPreparation.circuitStage_unitary" (lean := "QuantumBlockEncoding.SequentialBondPreparation.circuitStage_unitary")
*Plain-English reading.* Lean checks the proposition indexed as “circuit stage unitary”; the hypotheses and conclusion in the code panel fix its exact scope. Primitive stages have checked full unitarity, independently of whether their active columns implement the intended tensor cores.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Primitive stages have checked full unitarity, independently of whether their active columns implement the intended tensor cores.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/SequentialBondPreparation.lean:264](../../../../library/modules/sequentialbondpreparation/#decl-quantumblockencoding-sequentialbondpreparation-circuitstage-unitary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.SequentialBondPreparation.run_circuitStages_eq_transfer" (lean := "QuantumBlockEncoding.SequentialBondPreparation.run_circuitStages_eq_transfer")
*Plain-English reading.* Lean checks the proposition indexed as “run circuit stages eq transfer”; the hypotheses and conclusion in the code panel fix its exact scope. Direct instantiation with primitive-circuit stages.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Direct instantiation with primitive-circuit stages. The sole compiler alignment premise is stated on active clean-input columns, in actual 'evalPrimitiveCircuit' semantics. This does not synthesize these circuits or prove a gate-count bound.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/SequentialBondPreparation.lean:272](../../../../library/modules/sequentialbondpreparation/#decl-quantumblockencoding-sequentialbondpreparation-run-circuitstages-eq-transfer). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.SequentialBondPreparation.amplitudeMass" (lean := "QuantumBlockEncoding.SequentialBondPreparation.amplitudeMass")
*Plain-English reading.* This definition gives the library's named construction or computation for “amplitude mass”. Sum of amplitude squared moduli, represented as a complex scalar with zero imaginary part.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Sum of amplitude squared moduli, represented as a complex scalar with zero imaginary part. Keeping the star-product form avoids norm rewrites.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/SequentialBondPreparation.lean:288](../../../../library/modules/sequentialbondpreparation/#decl-quantumblockencoding-sequentialbondpreparation-amplitudemass). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.SequentialBondPreparation.amplitudeMass_mulVec" (lean := "QuantumBlockEncoding.SequentialBondPreparation.amplitudeMass_mulVec")
*Plain-English reading.* Lean checks the proposition indexed as “amplitude mass mul vec”; the hypotheses and conclusion in the code panel fix its exact scope. Full unitary matrix action preserves total amplitude mass.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Full unitary matrix action preserves total amplitude mass.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/SequentialBondPreparation.lean:292](../../../../library/modules/sequentialbondpreparation/#decl-quantumblockencoding-sequentialbondpreparation-amplitudemass-mulvec). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.SequentialBondPreparation.amplitudeMass_freshZero" (lean := "QuantumBlockEncoding.SequentialBondPreparation.amplitudeMass_freshZero")
*Plain-English reading.* Lean checks the proposition indexed as “amplitude mass fresh zero”; the hypotheses and conclusion in the code panel fix its exact scope. Introducing a zero bit is norm-preserving, not a postselection.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Introducing a zero bit is norm-preserving, not a postselection.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/SequentialBondPreparation.lean:304](../../../../library/modules/sequentialbondpreparation/#decl-quantumblockencoding-sequentialbondpreparation-amplitudemass-freshzero). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.SequentialBondPreparation.stepBasisEquiv" (lean := "QuantumBlockEncoding.SequentialBondPreparation.stepBasisEquiv")
*Plain-English reading.* This definition gives the library's named construction or computation for “step basis equiv”. The regrouping of passive prefix, newly emitted bit, and bond labels.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The regrouping of passive prefix, newly emitted bit, and bond labels.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/SequentialBondPreparation.lean:309](../../../../library/modules/sequentialbondpreparation/#decl-quantumblockencoding-sequentialbondpreparation-stepbasisequiv). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.SequentialBondPreparation.amplitudeMass_step" (lean := "QuantumBlockEncoding.SequentialBondPreparation.amplitudeMass_step")
*Plain-English reading.* Lean checks the proposition indexed as “amplitude mass step”; the hypotheses and conclusion in the code panel fix its exact scope. Sequential stages preserve total mass even when their occupied bond subspaces have different dimensions.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Sequential stages preserve total mass even when their occupied bond subspaces have different dimensions.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/SequentialBondPreparation.lean:316](../../../../library/modules/sequentialbondpreparation/#decl-quantumblockencoding-sequentialbondpreparation-amplitudemass-step). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.SequentialBondPreparation.amplitudeMass_run" (lean := "QuantumBlockEncoding.SequentialBondPreparation.amplitudeMass_run")
*Plain-English reading.* Lean checks the proposition indexed as “amplitude mass run”; the hypotheses and conclusion in the code panel fix its exact scope. Full sequential action is norm-preserving for every length.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Full sequential action is norm-preserving for every length.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/SequentialBondPreparation.lean:327](../../../../library/modules/sequentialbondpreparation/#decl-quantumblockencoding-sequentialbondpreparation-amplitudemass-run). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.SequentialBondPreparation.amplitudeMass_run_basisBoundary" (lean := "QuantumBlockEncoding.SequentialBondPreparation.amplitudeMass_run_basisBoundary")
*Plain-English reading.* Lean checks the proposition indexed as “amplitude mass run basis boundary”; the hypotheses and conclusion in the code panel fix its exact scope. A unitary sequential run starting at one bond basis label has total probability one at every stage.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* A unitary sequential run starting at one bond basis label has total probability one at every stage.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/SequentialBondPreparation.lean:336](../../../../library/modules/sequentialbondpreparation/#decl-quantumblockencoding-sequentialbondpreparation-amplitudemass-run-basisboundary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/SequentialPrimitiveAssembly.lean

37 explicit public declarations, in source order.

:::definition "QuantumBlockEncoding.SequentialPrimitiveAssembly.pad" (lean := "QuantumBlockEncoding.SequentialPrimitiveAssembly.pad")
*Plain-English reading.* This definition gives the library's named construction or computation for “pad”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/SequentialPrimitiveAssembly.lean:14](../../../../library/modules/sequentialprimitiveassembly/#decl-quantumblockencoding-sequentialprimitiveassembly-pad). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.SequentialPrimitiveAssembly.padMatrix" (lean := "QuantumBlockEncoding.SequentialPrimitiveAssembly.padMatrix")
*Plain-English reading.* This definition gives the library's named construction or computation for “pad matrix”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/SequentialPrimitiveAssembly.lean:18](../../../../library/modules/sequentialprimitiveassembly/#decl-quantumblockencoding-sequentialprimitiveassembly-padmatrix). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.SequentialPrimitiveAssembly.eval_pad" (lean := "QuantumBlockEncoding.SequentialPrimitiveAssembly.eval_pad")
*Plain-English reading.* Lean checks the proposition indexed as “eval pad”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/SequentialPrimitiveAssembly.lean:24](../../../../library/modules/sequentialprimitiveassembly/#decl-quantumblockencoding-sequentialprimitiveassembly-eval-pad). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.SequentialPrimitiveAssembly.padMatrix_append" (lean := "QuantumBlockEncoding.SequentialPrimitiveAssembly.padMatrix_append")
*Plain-English reading.* Lean checks the proposition indexed as “pad matrix append”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/SequentialPrimitiveAssembly.lean:30](../../../../library/modules/sequentialprimitiveassembly/#decl-quantumblockencoding-sequentialprimitiveassembly-padmatrix-append). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.SequentialPrimitiveAssembly.pad_ryCount" (lean := "QuantumBlockEncoding.SequentialPrimitiveAssembly.pad_ryCount")
*Plain-English reading.* Lean checks the proposition indexed as “pad ry count”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/SequentialPrimitiveAssembly.lean:51](../../../../library/modules/sequentialprimitiveassembly/#decl-quantumblockencoding-sequentialprimitiveassembly-pad-rycount). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.SequentialPrimitiveAssembly.pad_cxCount" (lean := "QuantumBlockEncoding.SequentialPrimitiveAssembly.pad_cxCount")
*Plain-English reading.* Lean checks the proposition indexed as “pad cx count”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/SequentialPrimitiveAssembly.lean:57](../../../../library/modules/sequentialprimitiveassembly/#decl-quantumblockencoding-sequentialprimitiveassembly-pad-cxcount). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.SequentialPrimitiveAssembly.pad_gateCount" (lean := "QuantumBlockEncoding.SequentialPrimitiveAssembly.pad_gateCount")
*Plain-English reading.* Lean checks the proposition indexed as “pad gate count”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/SequentialPrimitiveAssembly.lean:63](../../../../library/modules/sequentialprimitiveassembly/#decl-quantumblockencoding-sequentialprimitiveassembly-pad-gatecount). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.SequentialPrimitiveAssembly.stageWires" (lean := "QuantumBlockEncoding.SequentialPrimitiveAssembly.stageWires")
*Plain-English reading.* This definition gives the library's named construction or computation for “stage wires”. Preserve bond indices and send the local fresh bit to the new highest wire.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Preserve bond indices and send the local fresh bit to the new highest wire.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/SequentialPrimitiveAssembly.lean:70](../../../../library/modules/sequentialprimitiveassembly/#decl-quantumblockencoding-sequentialprimitiveassembly-stagewires). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.SequentialPrimitiveAssembly.stageWires_fresh" (lean := "QuantumBlockEncoding.SequentialPrimitiveAssembly.stageWires_fresh")
*Plain-English reading.* Lean checks the proposition indexed as “stage wires fresh”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/SequentialPrimitiveAssembly.lean:89](../../../../library/modules/sequentialprimitiveassembly/#decl-quantumblockencoding-sequentialprimitiveassembly-stagewires-fresh). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.SequentialPrimitiveAssembly.stageWires_bond" (lean := "QuantumBlockEncoding.SequentialPrimitiveAssembly.stageWires_bond")
*Plain-English reading.* Lean checks the proposition indexed as “stage wires bond”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/SequentialPrimitiveAssembly.lean:93](../../../../library/modules/sequentialprimitiveassembly/#decl-quantumblockencoding-sequentialprimitiveassembly-stagewires-bond). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.SequentialPrimitiveAssembly.stageWires_basis" (lean := "QuantumBlockEncoding.SequentialPrimitiveAssembly.stageWires_basis")
*Plain-English reading.* Lean checks the proposition indexed as “stage wires basis”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/SequentialPrimitiveAssembly.lean:97](../../../../library/modules/sequentialprimitiveassembly/#decl-quantumblockencoding-sequentialprimitiveassembly-stagewires-basis). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.SequentialPrimitiveAssembly.placeStage" (lean := "QuantumBlockEncoding.SequentialPrimitiveAssembly.placeStage")
*Plain-English reading.* This definition gives the library's named construction or computation for “place stage”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/SequentialPrimitiveAssembly.lean:108](../../../../library/modules/sequentialprimitiveassembly/#decl-quantumblockencoding-sequentialprimitiveassembly-placestage). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.SequentialPrimitiveAssembly.placedMatrix" (lean := "QuantumBlockEncoding.SequentialPrimitiveAssembly.placedMatrix")
*Plain-English reading.* This definition gives the library's named construction or computation for “placed matrix”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/SequentialPrimitiveAssembly.lean:112](../../../../library/modules/sequentialprimitiveassembly/#decl-quantumblockencoding-sequentialprimitiveassembly-placedmatrix). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.SequentialPrimitiveAssembly.eval_placeStage" (lean := "QuantumBlockEncoding.SequentialPrimitiveAssembly.eval_placeStage")
*Plain-English reading.* Lean checks the proposition indexed as “eval place stage”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/SequentialPrimitiveAssembly.lean:117](../../../../library/modules/sequentialprimitiveassembly/#decl-quantumblockencoding-sequentialprimitiveassembly-eval-placestage). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.SequentialPrimitiveAssembly.placedMatrix_apply" (lean := "QuantumBlockEncoding.SequentialPrimitiveAssembly.placedMatrix_apply")
*Plain-English reading.* Lean checks the proposition indexed as “placed matrix apply”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/SequentialPrimitiveAssembly.lean:122](../../../../library/modules/sequentialprimitiveassembly/#decl-quantumblockencoding-sequentialprimitiveassembly-placedmatrix-apply). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.SequentialPrimitiveAssembly.placeStage_ryCount" (lean := "QuantumBlockEncoding.SequentialPrimitiveAssembly.placeStage_ryCount")
*Plain-English reading.* Lean checks the proposition indexed as “place stage ry count”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/SequentialPrimitiveAssembly.lean:130](../../../../library/modules/sequentialprimitiveassembly/#decl-quantumblockencoding-sequentialprimitiveassembly-placestage-rycount). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.SequentialPrimitiveAssembly.placeStage_cxCount" (lean := "QuantumBlockEncoding.SequentialPrimitiveAssembly.placeStage_cxCount")
*Plain-English reading.* Lean checks the proposition indexed as “place stage cx count”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/SequentialPrimitiveAssembly.lean:133](../../../../library/modules/sequentialprimitiveassembly/#decl-quantumblockencoding-sequentialprimitiveassembly-placestage-cxcount). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.SequentialPrimitiveAssembly.placeStage_gateCount" (lean := "QuantumBlockEncoding.SequentialPrimitiveAssembly.placeStage_gateCount")
*Plain-English reading.* Lean checks the proposition indexed as “place stage gate count”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/SequentialPrimitiveAssembly.lean:136](../../../../library/modules/sequentialprimitiveassembly/#decl-quantumblockencoding-sequentialprimitiveassembly-placestage-gatecount). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.SequentialPrimitiveAssembly.assemble" (lean := "QuantumBlockEncoding.SequentialPrimitiveAssembly.assemble")
*Plain-English reading.* This definition gives the library's named construction or computation for “assemble”. Assemble one actual primitive list, with each bond stage on its final wires.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Assemble one actual primitive list, with each bond stage on its final wires.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/SequentialPrimitiveAssembly.lean:141](../../../../library/modules/sequentialprimitiveassembly/#decl-quantumblockencoding-sequentialprimitiveassembly-assemble). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.SequentialPrimitiveAssembly.assembledMatrix" (lean := "QuantumBlockEncoding.SequentialPrimitiveAssembly.assembledMatrix")
*Plain-English reading.* This definition gives the library's named construction or computation for “assembled matrix”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/SequentialPrimitiveAssembly.lean:146](../../../../library/modules/sequentialprimitiveassembly/#decl-quantumblockencoding-sequentialprimitiveassembly-assembledmatrix). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.SequentialPrimitiveAssembly.eval_assemble" (lean := "QuantumBlockEncoding.SequentialPrimitiveAssembly.eval_assemble")
*Plain-English reading.* Lean checks the proposition indexed as “eval assemble”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/SequentialPrimitiveAssembly.lean:153](../../../../library/modules/sequentialprimitiveassembly/#decl-quantumblockencoding-sequentialprimitiveassembly-eval-assemble). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.SequentialPrimitiveAssembly.assembledMatrix_clean_column" (lean := "QuantumBlockEncoding.SequentialPrimitiveAssembly.assembledMatrix_clean_column")
*Plain-English reading.* Lean checks the proposition indexed as “assembled matrix clean column”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/SequentialPrimitiveAssembly.lean:161](../../../../library/modules/sequentialprimitiveassembly/#decl-quantumblockencoding-sequentialprimitiveassembly-assembledmatrix-clean-column). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.SequentialPrimitiveAssembly.assemble_clean_column" (lean := "QuantumBlockEncoding.SequentialPrimitiveAssembly.assemble_clean_column")
*Plain-English reading.* Lean checks the proposition indexed as “assemble clean column”; the hypotheses and conclusion in the code panel fix its exact scope. A flattened primitive circuit has exactly the sequential state action; the fresh data inputs are all zero and every output amplitude is covered.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* A flattened primitive circuit has exactly the sequential state action; the fresh data inputs are all zero and every output amplitude is covered.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/SequentialPrimitiveAssembly.lean:199](../../../../library/modules/sequentialprimitiveassembly/#decl-quantumblockencoding-sequentialprimitiveassembly-assemble-clean-column). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.SequentialPrimitiveAssembly.run_boundary_decomposition" (lean := "QuantumBlockEncoding.SequentialPrimitiveAssembly.run_boundary_decomposition")
*Plain-English reading.* Lean checks the proposition indexed as “run boundary decomposition”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/SequentialPrimitiveAssembly.lean:208](../../../../library/modules/sequentialprimitiveassembly/#decl-quantumblockencoding-sequentialprimitiveassembly-run-boundary-decomposition). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.SequentialPrimitiveAssembly.withInitial" (lean := "QuantumBlockEncoding.SequentialPrimitiveAssembly.withInitial")
*Plain-English reading.* This definition gives the library's named construction or computation for “with initial”. The initial bond vector is prepared by an actual circuit, never supplied as a free state.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The initial bond vector is prepared by an actual circuit, never supplied as a free state. Its gate cost is added to the sequential-stage costs.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/SequentialPrimitiveAssembly.lean:224](../../../../library/modules/sequentialprimitiveassembly/#decl-quantumblockencoding-sequentialprimitiveassembly-withinitial). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.SequentialPrimitiveAssembly.withInitial_column" (lean := "QuantumBlockEncoding.SequentialPrimitiveAssembly.withInitial_column")
*Plain-English reading.* Lean checks the proposition indexed as “with initial column”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/SequentialPrimitiveAssembly.lean:228](../../../../library/modules/sequentialprimitiveassembly/#decl-quantumblockencoding-sequentialprimitiveassembly-withinitial-column). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.SequentialPrimitiveAssembly.assemble_ryCount" (lean := "QuantumBlockEncoding.SequentialPrimitiveAssembly.assemble_ryCount")
*Plain-English reading.* Lean checks the proposition indexed as “assemble ry count”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/SequentialPrimitiveAssembly.lean:243](../../../../library/modules/sequentialprimitiveassembly/#decl-quantumblockencoding-sequentialprimitiveassembly-assemble-rycount). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.SequentialPrimitiveAssembly.assemble_cxCount" (lean := "QuantumBlockEncoding.SequentialPrimitiveAssembly.assemble_cxCount")
*Plain-English reading.* Lean checks the proposition indexed as “assemble cx count”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/SequentialPrimitiveAssembly.lean:251](../../../../library/modules/sequentialprimitiveassembly/#decl-quantumblockencoding-sequentialprimitiveassembly-assemble-cxcount). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.SequentialPrimitiveAssembly.assemble_noOracle" (lean := "QuantumBlockEncoding.SequentialPrimitiveAssembly.assemble_noOracle")
*Plain-English reading.* Lean checks the proposition indexed as “assemble no oracle”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/SequentialPrimitiveAssembly.lean:259](../../../../library/modules/sequentialprimitiveassembly/#decl-quantumblockencoding-sequentialprimitiveassembly-assemble-nooracle). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.SequentialPrimitiveAssembly.withInitial_ryCount" (lean := "QuantumBlockEncoding.SequentialPrimitiveAssembly.withInitial_ryCount")
*Plain-English reading.* Lean checks the proposition indexed as “with initial ry count”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/SequentialPrimitiveAssembly.lean:262](../../../../library/modules/sequentialprimitiveassembly/#decl-quantumblockencoding-sequentialprimitiveassembly-withinitial-rycount). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.SequentialPrimitiveAssembly.withInitial_cxCount" (lean := "QuantumBlockEncoding.SequentialPrimitiveAssembly.withInitial_cxCount")
*Plain-English reading.* Lean checks the proposition indexed as “with initial cx count”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/SequentialPrimitiveAssembly.lean:268](../../../../library/modules/sequentialprimitiveassembly/#decl-quantumblockencoding-sequentialprimitiveassembly-withinitial-cxcount). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.SequentialPrimitiveAssembly.withInitial_primitive_bound" (lean := "QuantumBlockEncoding.SequentialPrimitiveAssembly.withInitial_primitive_bound")
*Plain-English reading.* Lean checks the proposition indexed as “with initial primitive bound”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/SequentialPrimitiveAssembly.lean:274](../../../../library/modules/sequentialprimitiveassembly/#decl-quantumblockencoding-sequentialprimitiveassembly-withinitial-primitive-bound). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.SequentialPrimitiveAssembly.outputWires" (lean := "QuantumBlockEncoding.SequentialPrimitiveAssembly.outputWires")
*Plain-English reading.* This definition gives the library's named construction or computation for “output wires”. Place the first emitted bit at the most-significant data wire, and the bond after the data register.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Place the first emitted bit at the most-significant data wire, and the bond after the data register. This relabels every instruction, not the state.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/SequentialPrimitiveAssembly.lean:289](../../../../library/modules/sequentialprimitiveassembly/#decl-quantumblockencoding-sequentialprimitiveassembly-outputwires). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.SequentialPrimitiveAssembly.outputWires_basis" (lean := "QuantumBlockEncoding.SequentialPrimitiveAssembly.outputWires_basis")
*Plain-English reading.* Lean checks the proposition indexed as “output wires basis”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/SequentialPrimitiveAssembly.lean:299](../../../../library/modules/sequentialprimitiveassembly/#decl-quantumblockencoding-sequentialprimitiveassembly-outputwires-basis). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.SequentialPrimitiveAssembly.publicCircuit" (lean := "QuantumBlockEncoding.SequentialPrimitiveAssembly.publicCircuit")
*Plain-English reading.* This definition gives the library's named construction or computation for “public circuit”. Physical output convention: low data wires are little-endian, clean bond wires follow them.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Physical output convention: low data wires are little-endian, clean bond wires follow them. Both preparation and stages incur their actual gate costs.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/SequentialPrimitiveAssembly.lean:309](../../../../library/modules/sequentialprimitiveassembly/#decl-quantumblockencoding-sequentialprimitiveassembly-publiccircuit). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.SequentialPrimitiveAssembly.publicCircuit_column" (lean := "QuantumBlockEncoding.SequentialPrimitiveAssembly.publicCircuit_column")
*Plain-English reading.* Lean checks the proposition indexed as “public circuit column”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/SequentialPrimitiveAssembly.lean:313](../../../../library/modules/sequentialprimitiveassembly/#decl-quantumblockencoding-sequentialprimitiveassembly-publiccircuit-column). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.SequentialPrimitiveAssembly.publicCircuit_primitive_bound" (lean := "QuantumBlockEncoding.SequentialPrimitiveAssembly.publicCircuit_primitive_bound")
*Plain-English reading.* Lean checks the proposition indexed as “public circuit primitive bound”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/SequentialPrimitiveAssembly.lean:323](../../../../library/modules/sequentialprimitiveassembly/#decl-quantumblockencoding-sequentialprimitiveassembly-publiccircuit-primitive-bound). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/StoredGivens.lean

60 explicit public declarations, in source order.

:::definition "QuantumBlockEncoding.StoredGivens.Op" (lean := "QuantumBlockEncoding.StoredGivens.Op")
*Plain-English reading.* This type lists the allowed alternatives for “op”; its constructors are the cases that downstream code must handle.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* inductive.

Source: [QuantumBlockEncoding/StoredGivens.lean:21](../../../../library/modules/storedgivens/#decl-quantumblockencoding-storedgivens-op). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredGivens.Cost" (lean := "QuantumBlockEncoding.StoredGivens.Cost")
*Plain-English reading.* This abbreviation gives a shorter name to the type or expression used for “cost”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* abbrev.

Source: [QuantumBlockEncoding/StoredGivens.lean:25](../../../../library/modules/storedgivens/#decl-quantumblockencoding-storedgivens-cost). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredGivens.tick" (lean := "QuantumBlockEncoding.StoredGivens.tick")
*Plain-English reading.* This definition gives the library's named construction or computation for “tick”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredGivens.lean:27](../../../../library/modules/storedgivens/#decl-quantumblockencoding-storedgivens-tick). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredGivens.Run" (lean := "QuantumBlockEncoding.StoredGivens.Run")
*Plain-English reading.* This record groups the data and proof fields needed for “run”. A proposition-valued field is a requirement until a constructor supplies it.

*Formal status.* Data contract in the default import surface; proposition-valued fields are obligations, not automatically established facts.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* structure.

Source: [QuantumBlockEncoding/StoredGivens.lean:29](../../../../library/modules/storedgivens/#decl-quantumblockencoding-storedgivens-run). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredGivens.Run.pure" (lean := "QuantumBlockEncoding.StoredGivens.Run.pure")
*Plain-English reading.* This definition gives the library's named construction or computation for “pure”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredGivens.lean:33](../../../../library/modules/storedgivens/#decl-quantumblockencoding-storedgivens-run-pure). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredGivens.Run.bind" (lean := "QuantumBlockEncoding.StoredGivens.Run.bind")
*Plain-English reading.* This definition gives the library's named construction or computation for “bind”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredGivens.lean:35](../../../../library/modules/storedgivens/#decl-quantumblockencoding-storedgivens-run-bind). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredGivens.charge" (lean := "QuantumBlockEncoding.StoredGivens.charge")
*Plain-English reading.* This definition gives the library's named construction or computation for “charge”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredGivens.lean:43](../../../../library/modules/storedgivens/#decl-quantumblockencoding-storedgivens-charge). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredGivens.add" (lean := "QuantumBlockEncoding.StoredGivens.add")
*Plain-English reading.* This definition gives the library's named construction or computation for “add”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredGivens.lean:45](../../../../library/modules/storedgivens/#decl-quantumblockencoding-storedgivens-add). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredGivens.sub" (lean := "QuantumBlockEncoding.StoredGivens.sub")
*Plain-English reading.* This definition gives the library's named construction or computation for “sub”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredGivens.lean:46](../../../../library/modules/storedgivens/#decl-quantumblockencoding-storedgivens-sub). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredGivens.mul" (lean := "QuantumBlockEncoding.StoredGivens.mul")
*Plain-English reading.* This definition gives the library's named construction or computation for “mul”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredGivens.lean:47](../../../../library/modules/storedgivens/#decl-quantumblockencoding-storedgivens-mul). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredGivens.div" (lean := "QuantumBlockEncoding.StoredGivens.div")
*Plain-English reading.* This definition gives the library's named construction or computation for “div”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredGivens.lean:48](../../../../library/modules/storedgivens/#decl-quantumblockencoding-storedgivens-div). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredGivens.sqrt" (lean := "QuantumBlockEncoding.StoredGivens.sqrt")
*Plain-English reading.* This definition gives the library's named construction or computation for “sqrt”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredGivens.lean:49](../../../../library/modules/storedgivens/#decl-quantumblockencoding-storedgivens-sqrt). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredGivens.arccos" (lean := "QuantumBlockEncoding.StoredGivens.arccos")
*Plain-English reading.* This definition gives the library's named construction or computation for “arccos”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredGivens.lean:50](../../../../library/modules/storedgivens/#decl-quantumblockencoding-storedgivens-arccos). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredGivens.cos" (lean := "QuantumBlockEncoding.StoredGivens.cos")
*Plain-English reading.* This definition gives the library's named construction or computation for “cos”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredGivens.lean:51](../../../../library/modules/storedgivens/#decl-quantumblockencoding-storedgivens-cos). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredGivens.sin" (lean := "QuantumBlockEncoding.StoredGivens.sin")
*Plain-English reading.* This definition gives the library's named construction or computation for “sin”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredGivens.lean:52](../../../../library/modules/storedgivens/#decl-quantumblockencoding-storedgivens-sin). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredGivens.zeroTest" (lean := "QuantumBlockEncoding.StoredGivens.zeroTest")
*Plain-English reading.* This definition gives the library's named construction or computation for “zero test”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredGivens.lean:53](../../../../library/modules/storedgivens/#decl-quantumblockencoding-storedgivens-zerotest). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredGivens.signTest" (lean := "QuantumBlockEncoding.StoredGivens.signTest")
*Plain-English reading.* This definition gives the library's named construction or computation for “sign test”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredGivens.lean:54](../../../../library/modules/storedgivens/#decl-quantumblockencoding-storedgivens-signtest). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredGivens.read" (lean := "QuantumBlockEncoding.StoredGivens.read")
*Plain-English reading.* This definition gives the library's named construction or computation for “read”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredGivens.lean:56](../../../../library/modules/storedgivens/#decl-quantumblockencoding-storedgivens-read). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredGivens.collect" (lean := "QuantumBlockEncoding.StoredGivens.collect")
*Plain-English reading.* This definition gives the library's named construction or computation for “collect”. Two materialized passes: write counted entries, read them for projection and summation, and write the projected output.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Two materialized passes: write counted entries, read them for projection and summation, and write the projected output. Counter words are metadata.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredGivens.lean:60](../../../../library/modules/storedgivens/#decl-quantumblockencoding-storedgivens-collect). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredGivens.replace" (lean := "QuantumBlockEncoding.StoredGivens.replace")
*Plain-English reading.* This definition gives the library's named construction or computation for “replace”. Persistent full-copy replacement; row references are stored words.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Persistent full-copy replacement; row references are stored words.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredGivens.lean:67](../../../../library/modules/storedgivens/#decl-quantumblockencoding-storedgivens-replace). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredGivens.StoredMatrix" (lean := "QuantumBlockEncoding.StoredGivens.StoredMatrix")
*Plain-English reading.* This abbreviation gives a shorter name to the type or expression used for “stored matrix”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* abbrev.

Source: [QuantumBlockEncoding/StoredGivens.lean:70](../../../../library/modules/storedgivens/#decl-quantumblockencoding-storedgivens-storedmatrix). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredGivens.denote" (lean := "QuantumBlockEncoding.StoredGivens.denote")
*Plain-English reading.* This definition gives the library's named construction or computation for “denote”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredGivens.lean:72](../../../../library/modules/storedgivens/#decl-quantumblockencoding-storedgivens-denote). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredGivens.materialize" (lean := "QuantumBlockEncoding.StoredGivens.materialize")
*Plain-English reading.* This definition gives the library's named construction or computation for “materialize”. Materializing an input callback is an explicit boundary: this constructor charges storage but does not certify the callback's scalar evaluation cost.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Materializing an input callback is an explicit boundary: this constructor charges storage but does not certify the callback's scalar evaluation cost.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredGivens.lean:77](../../../../library/modules/storedgivens/#decl-quantumblockencoding-storedgivens-materialize). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredGivens.collect_value" (lean := "QuantumBlockEncoding.StoredGivens.collect_value")
*Plain-English reading.* Lean checks the proposition indexed as “collect value”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredGivens.lean:80](../../../../library/modules/storedgivens/#decl-quantumblockencoding-storedgivens-collect-value). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredGivens.collect_cost" (lean := "QuantumBlockEncoding.StoredGivens.collect_cost")
*Plain-English reading.* Lean checks the proposition indexed as “collect cost”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredGivens.lean:83](../../../../library/modules/storedgivens/#decl-quantumblockencoding-storedgivens-collect-cost). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredGivens.angle" (lean := "QuantumBlockEncoding.StoredGivens.angle")
*Plain-English reading.* This definition gives the library's named construction or computation for “angle”. Compute the norm once, then the exact signed angle once.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Compute the norm once, then the exact signed angle once.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredGivens.lean:88](../../../../library/modules/storedgivens/#decl-quantumblockencoding-storedgivens-angle). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredGivens.angle_value" (lean := "QuantumBlockEncoding.StoredGivens.angle_value")
*Plain-English reading.* Lean checks the proposition indexed as “angle value”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredGivens.lean:102](../../../../library/modules/storedgivens/#decl-quantumblockencoding-storedgivens-angle-value). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredGivens.coefficients" (lean := "QuantumBlockEncoding.StoredGivens.coefficients")
*Plain-English reading.* This definition gives the library's named construction or computation for “coefficients”. One angle and one pair of trigonometric coefficients are shared by all entries of the two output rows.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* One angle and one pair of trigonometric coefficients are shared by all entries of the two output rows.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredGivens.lean:110](../../../../library/modules/storedgivens/#decl-quantumblockencoding-storedgivens-coefficients). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredGivens.coefficients_value" (lean := "QuantumBlockEncoding.StoredGivens.coefficients_value")
*Plain-English reading.* Lean checks the proposition indexed as “coefficients value”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredGivens.lean:117](../../../../library/modules/storedgivens/#decl-quantumblockencoding-storedgivens-coefficients-value). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredGivens.entryPair" (lean := "QuantumBlockEncoding.StoredGivens.entryPair")
*Plain-English reading.* This definition gives the library's named construction or computation for “entry pair”. The six actual field operations for a pair of entries.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The six actual field operations for a pair of entries.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredGivens.lean:123](../../../../library/modules/storedgivens/#decl-quantumblockencoding-storedgivens-entrypair). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredGivens.entryPair_value" (lean := "QuantumBlockEncoding.StoredGivens.entryPair_value")
*Plain-English reading.* Lean checks the proposition indexed as “entry pair value”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredGivens.lean:132](../../../../library/modules/storedgivens/#decl-quantumblockencoding-storedgivens-entrypair-value). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredGivens.entryPair_cost" (lean := "QuantumBlockEncoding.StoredGivens.entryPair_cost")
*Plain-English reading.* Lean checks the proposition indexed as “entry pair cost”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredGivens.lean:135](../../../../library/modules/storedgivens/#decl-quantumblockencoding-storedgivens-entrypair-cost). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredGivens.rowPair" (lean := "QuantumBlockEncoding.StoredGivens.rowPair")
*Plain-English reading.* This definition gives the library's named construction or computation for “row pair”. Materialize both rows from a single stored vector of computed pairs.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Materialize both rows from a single stored vector of computed pairs.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredGivens.lean:141](../../../../library/modules/storedgivens/#decl-quantumblockencoding-storedgivens-rowpair). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredGivens.rowPair_value" (lean := "QuantumBlockEncoding.StoredGivens.rowPair_value")
*Plain-English reading.* Lean checks the proposition indexed as “row pair value”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredGivens.lean:155](../../../../library/modules/storedgivens/#decl-quantumblockencoding-storedgivens-rowpair-value). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredGivens.rotate" (lean := "QuantumBlockEncoding.StoredGivens.rotate")
*Plain-English reading.* This definition gives the library's named construction or computation for “rotate”. Cached input rows, materialized output rows, then two stored replacements.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Cached input rows, materialized output rows, then two stored replacements.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredGivens.lean:161](../../../../library/modules/storedgivens/#decl-quantumblockencoding-storedgivens-rotate). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredGivens.rotate_value" (lean := "QuantumBlockEncoding.StoredGivens.rotate_value")
*Plain-English reading.* Lean checks the proposition indexed as “rotate value”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredGivens.lean:169](../../../../library/modules/storedgivens/#decl-quantumblockencoding-storedgivens-rotate-value). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredGivens.Elimination" (lean := "QuantumBlockEncoding.StoredGivens.Elimination")
*Plain-English reading.* This record groups the data and proof fields needed for “elimination”. A proposition-valued field is a requirement until a constructor supplies it.

*Formal status.* Data contract in the default import surface; proposition-valued fields are obligations, not automatically established facts.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* structure.

Source: [QuantumBlockEncoding/StoredGivens.lean:188](../../../../library/modules/storedgivens/#decl-quantumblockencoding-storedgivens-elimination). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredGivens.eliminate" (lean := "QuantumBlockEncoding.StoredGivens.eliminate")
*Plain-English reading.* This definition gives the library's named construction or computation for “eliminate”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredGivens.lean:192](../../../../library/modules/storedgivens/#decl-quantumblockencoding-storedgivens-eliminate). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredGivens.eliminate_angle" (lean := "QuantumBlockEncoding.StoredGivens.eliminate_angle")
*Plain-English reading.* Lean checks the proposition indexed as “eliminate angle”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredGivens.lean:202](../../../../library/modules/storedgivens/#decl-quantumblockencoding-storedgivens-eliminate-angle). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredGivens.eliminate_matrix" (lean := "QuantumBlockEncoding.StoredGivens.eliminate_matrix")
*Plain-English reading.* Lean checks the proposition indexed as “eliminate matrix”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredGivens.lean:207](../../../../library/modules/storedgivens/#decl-quantumblockencoding-storedgivens-eliminate-matrix). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredGivens.Sweep" (lean := "QuantumBlockEncoding.StoredGivens.Sweep")
*Plain-English reading.* This record groups the data and proof fields needed for “sweep”. A proposition-valued field is a requirement until a constructor supplies it.

*Formal status.* Data contract in the default import surface; proposition-valued fields are obligations, not automatically established facts.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* structure.

Source: [QuantumBlockEncoding/StoredGivens.lean:213](../../../../library/modules/storedgivens/#decl-quantumblockencoding-storedgivens-sweep). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredGivens.columnSweep" (lean := "QuantumBlockEncoding.StoredGivens.columnSweep")
*Plain-English reading.* This definition gives the library's named construction or computation for “column sweep”. One recursion returns both the residual and its actual chronological log.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* One recursion returns both the residual and its actual chronological log. No separate rerun is used to generate angles or the next matrix.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredGivens.lean:219](../../../../library/modules/storedgivens/#decl-quantumblockencoding-storedgivens-columnsweep). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredGivens.columnSweep_matrix" (lean := "QuantumBlockEncoding.StoredGivens.columnSweep_matrix")
*Plain-English reading.* Lean checks the proposition indexed as “column sweep matrix”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredGivens.lean:231](../../../../library/modules/storedgivens/#decl-quantumblockencoding-storedgivens-columnsweep-matrix). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredGivens.columnSweep_steps" (lean := "QuantumBlockEncoding.StoredGivens.columnSweep_steps")
*Plain-English reading.* Lean checks the proposition indexed as “column sweep steps”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredGivens.lean:242](../../../../library/modules/storedgivens/#decl-quantumblockencoding-storedgivens-columnsweep-steps). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredGivens.angleBudget" (lean := "QuantumBlockEncoding.StoredGivens.angleBudget")
*Plain-English reading.* This definition gives the library's named construction or computation for “angle budget”. Branch-independent upper bound; the zero pair uses fewer operations.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Branch-independent upper bound; the zero pair uses fewer operations.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredGivens.lean:254](../../../../library/modules/storedgivens/#decl-quantumblockencoding-storedgivens-anglebudget). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredGivens.angle_cost_le" (lean := "QuantumBlockEncoding.StoredGivens.angle_cost_le")
*Plain-English reading.* Lean checks the proposition indexed as “angle cost le”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredGivens.lean:257](../../../../library/modules/storedgivens/#decl-quantumblockencoding-storedgivens-angle-cost-le). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredGivens.coefficientBudget" (lean := "QuantumBlockEncoding.StoredGivens.coefficientBudget")
*Plain-English reading.* This definition gives the library's named construction or computation for “coefficient budget”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredGivens.lean:263](../../../../library/modules/storedgivens/#decl-quantumblockencoding-storedgivens-coefficientbudget). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredGivens.coefficients_cost_le" (lean := "QuantumBlockEncoding.StoredGivens.coefficients_cost_le")
*Plain-English reading.* Lean checks the proposition indexed as “coefficients cost le”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredGivens.lean:265](../../../../library/modules/storedgivens/#decl-quantumblockencoding-storedgivens-coefficients-cost-le). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredGivens.rowPair_cost" (lean := "QuantumBlockEncoding.StoredGivens.rowPair_cost")
*Plain-English reading.* Lean checks the proposition indexed as “row pair cost”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredGivens.lean:272](../../../../library/modules/storedgivens/#decl-quantumblockencoding-storedgivens-rowpair-cost). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredGivens.rotate_cost" (lean := "QuantumBlockEncoding.StoredGivens.rotate_cost")
*Plain-English reading.* Lean checks the proposition indexed as “rotate cost”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredGivens.lean:279](../../../../library/modules/storedgivens/#decl-quantumblockencoding-storedgivens-rotate-cost). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredGivens.eliminationBudget" (lean := "QuantumBlockEncoding.StoredGivens.eliminationBudget")
*Plain-English reading.* This definition gives the library's named construction or computation for “elimination budget”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredGivens.lean:287](../../../../library/modules/storedgivens/#decl-quantumblockencoding-storedgivens-eliminationbudget). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredGivens.eliminate_cost_le" (lean := "QuantumBlockEncoding.StoredGivens.eliminate_cost_le")
*Plain-English reading.* Lean checks the proposition indexed as “eliminate cost le”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredGivens.lean:292](../../../../library/modules/storedgivens/#decl-quantumblockencoding-storedgivens-eliminate-cost-le). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredGivens.stepBudget" (lean := "QuantumBlockEncoding.StoredGivens.stepBudget")
*Plain-English reading.* This definition gives the library's named construction or computation for “step budget”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredGivens.lean:300](../../../../library/modules/storedgivens/#decl-quantumblockencoding-storedgivens-stepbudget). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredGivens.columnSweep_cost_succ" (lean := "QuantumBlockEncoding.StoredGivens.columnSweep_cost_succ")
*Plain-English reading.* Lean checks the proposition indexed as “column sweep cost succ”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredGivens.lean:302](../../../../library/modules/storedgivens/#decl-quantumblockencoding-storedgivens-columnsweep-cost-succ). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredGivens.columnSweep_cost_le" (lean := "QuantumBlockEncoding.StoredGivens.columnSweep_cost_le")
*Plain-English reading.* Lean checks the proposition indexed as “column sweep cost le”; the hypotheses and conclusion in the code panel fix its exact scope. Symbolic bound on the actual fused producer, not on just its log length.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Symbolic bound on the actual fused producer, not on just its log length.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredGivens.lean:313](../../../../library/modules/storedgivens/#decl-quantumblockencoding-storedgivens-columnsweep-cost-le). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredGivens.stepBudget_fields" (lean := "QuantumBlockEncoding.StoredGivens.stepBudget_fields")
*Plain-English reading.* Lean checks the proposition indexed as “step budget fields”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredGivens.lean:328](../../../../library/modules/storedgivens/#decl-quantumblockencoding-storedgivens-stepbudget-fields). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredGivens.columnSweep_emit" (lean := "QuantumBlockEncoding.StoredGivens.columnSweep_emit")
*Plain-English reading.* Lean checks the proposition indexed as “column sweep emit”; the hypotheses and conclusion in the code panel fix its exact scope. Each iteration emits exactly one record, including a harmless zero-pair rotation.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Each iteration emits exactly one record, including a harmless zero-pair rotation. This is a counter theorem for the producer in addition to its independent list-length refinement.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredGivens.lean:343](../../../../library/modules/storedgivens/#decl-quantumblockencoding-storedgivens-columnsweep-emit). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredGivens.columnSweep_steps_length" (lean := "QuantumBlockEncoding.StoredGivens.columnSweep_steps_length")
*Plain-English reading.* Lean checks the proposition indexed as “column sweep steps length”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredGivens.lean:355](../../../../library/modules/storedgivens/#decl-quantumblockencoding-storedgivens-columnsweep-steps-length). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredGivens.columnSweep_action" (lean := "QuantumBlockEncoding.StoredGivens.columnSweep_action")
*Plain-English reading.* Lean checks the proposition indexed as “column sweep action”; the hypotheses and conclusion in the code panel fix its exact scope. The emitted list and materialized residual are a single coherent run.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The emitted list and materialized residual are a single coherent run.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredGivens.lean:361](../../../../library/modules/storedgivens/#decl-quantumblockencoding-storedgivens-columnsweep-action). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredGivens.materialize_cost" (lean := "QuantumBlockEncoding.StoredGivens.materialize_cost")
*Plain-English reading.* Lean checks the proposition indexed as “materialize cost”; the hypotheses and conclusion in the code panel fix its exact scope. Input generation is charged once per materialized entry, and the specified callback must itself use the counted scalar interface.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Input generation is charged once per materialized entry, and the specified callback must itself use the counted scalar interface.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredGivens.lean:369](../../../../library/modules/storedgivens/#decl-quantumblockencoding-storedgivens-materialize-cost). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/StoredIsometryCompletion.lean

59 explicit public declarations, in source order.

:::definition "QuantumBlockEncoding.StoredIsometryCompletion.Positions" (lean := "QuantumBlockEncoding.StoredIsometryCompletion.Positions")
*Plain-English reading.* This record groups the data and proof fields needed for “positions”. A proposition-valued field is a requirement until a constructor supplies it.

*Formal status.* Data contract in the default import surface; proposition-valued fields are obligations, not automatically established facts.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* structure.

Source: [QuantumBlockEncoding/StoredIsometryCompletion.lean:20](../../../../library/modules/storedisometrycompletion/#decl-quantumblockencoding-storedisometrycompletion-positions). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredIsometryCompletion.Positions.embedding" (lean := "QuantumBlockEncoding.StoredIsometryCompletion.Positions.embedding")
*Plain-English reading.* This definition gives the library's named construction or computation for “embedding”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredIsometryCompletion.lean:24](../../../../library/modules/storedisometrycompletion/#decl-quantumblockencoding-storedisometrycompletion-positions-embedding). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredIsometryCompletion.Positions.materialize" (lean := "QuantumBlockEncoding.StoredIsometryCompletion.Positions.materialize")
*Plain-English reading.* This definition gives the library's named construction or computation for “materialize”. Materialize a counted physical-label generator.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Materialize a counted physical-label generator. Injectivity is an input contract, not a charged search for a proof or a freely evaluated embedding.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredIsometryCompletion.lean:29](../../../../library/modules/storedisometrycompletion/#decl-quantumblockencoding-storedisometrycompletion-positions-materialize). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredIsometryCompletion.Positions.materialize_value" (lean := "QuantumBlockEncoding.StoredIsometryCompletion.Positions.materialize_value")
*Plain-English reading.* Lean checks the proposition indexed as “materialize value”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredIsometryCompletion.lean:37](../../../../library/modules/storedisometrycompletion/#decl-quantumblockencoding-storedisometrycompletion-positions-materialize-value). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredIsometryCompletion.Positions.materialize_cost" (lean := "QuantumBlockEncoding.StoredIsometryCompletion.Positions.materialize_cost")
*Plain-English reading.* Lean checks the proposition indexed as “materialize cost”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredIsometryCompletion.lean:42](../../../../library/modules/storedisometrycompletion/#decl-quantumblockencoding-storedisometrycompletion-positions-materialize-cost). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredIsometryCompletion.transpose" (lean := "QuantumBlockEncoding.StoredIsometryCompletion.transpose")
*Plain-English reading.* This definition gives the library's named construction or computation for “transpose”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredIsometryCompletion.lean:48](../../../../library/modules/storedisometrycompletion/#decl-quantumblockencoding-storedisometrycompletion-transpose). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredIsometryCompletion.transpose_value" (lean := "QuantumBlockEncoding.StoredIsometryCompletion.transpose_value")
*Plain-English reading.* Lean checks the proposition indexed as “transpose value”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredIsometryCompletion.lean:53](../../../../library/modules/storedisometrycompletion/#decl-quantumblockencoding-storedisometrycompletion-transpose-value). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredIsometryCompletion.transposeBudget" (lean := "QuantumBlockEncoding.StoredIsometryCompletion.transposeBudget")
*Plain-English reading.* This definition gives the library's named construction or computation for “transpose budget”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredIsometryCompletion.lean:59](../../../../library/modules/storedisometrycompletion/#decl-quantumblockencoding-storedisometrycompletion-transposebudget). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredIsometryCompletion.transpose_cost" (lean := "QuantumBlockEncoding.StoredIsometryCompletion.transpose_cost")
*Plain-English reading.* Lean checks the proposition indexed as “transpose cost”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredIsometryCompletion.lean:62](../../../../library/modules/storedisometrycompletion/#decl-quantumblockencoding-storedisometrycompletion-transpose-cost). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredIsometryCompletion.prefixCompletion" (lean := "QuantumBlockEncoding.StoredIsometryCompletion.prefixCompletion")
*Plain-English reading.* This definition gives the library's named construction or computation for “prefix completion”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredIsometryCompletion.lean:67](../../../../library/modules/storedisometrycompletion/#decl-quantumblockencoding-storedisometrycompletion-prefixcompletion). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredIsometryCompletion.prefixCompletion_value" (lean := "QuantumBlockEncoding.StoredIsometryCompletion.prefixCompletion_value")
*Plain-English reading.* Lean checks the proposition indexed as “prefix completion value”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredIsometryCompletion.lean:72](../../../../library/modules/storedisometrycompletion/#decl-quantumblockencoding-storedisometrycompletion-prefixcompletion-value). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredIsometryCompletion.prefixBudget" (lean := "QuantumBlockEncoding.StoredIsometryCompletion.prefixBudget")
*Plain-English reading.* This definition gives the library's named construction or computation for “prefix budget”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredIsometryCompletion.lean:79](../../../../library/modules/storedisometrycompletion/#decl-quantumblockencoding-storedisometrycompletion-prefixbudget). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredIsometryCompletion.prefixCompletion_cost_le" (lean := "QuantumBlockEncoding.StoredIsometryCompletion.prefixCompletion_cost_le")
*Plain-English reading.* Lean checks the proposition indexed as “prefix completion cost le”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredIsometryCompletion.lean:82](../../../../library/modules/storedisometrycompletion/#decl-quantumblockencoding-storedisometrycompletion-prefixcompletion-cost-le). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredIsometryCompletion.swapIndex" (lean := "QuantumBlockEncoding.StoredIsometryCompletion.swapIndex")
*Plain-English reading.* This definition gives the library's named construction or computation for “swap index”. Both equality decisions are actual charged operations; the second is skipped if the first comparison succeeds.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Both equality decisions are actual charged operations; the second is skipped if the first comparison succeeds.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredIsometryCompletion.lean:90](../../../../library/modules/storedisometrycompletion/#decl-quantumblockencoding-storedisometrycompletion-swapindex). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredIsometryCompletion.swapIndex_value" (lean := "QuantumBlockEncoding.StoredIsometryCompletion.swapIndex_value")
*Plain-English reading.* Lean checks the proposition indexed as “swap index value”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredIsometryCompletion.lean:96](../../../../library/modules/storedisometrycompletion/#decl-quantumblockencoding-storedisometrycompletion-swapindex-value). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredIsometryCompletion.swapIndex_cost_le" (lean := "QuantumBlockEncoding.StoredIsometryCompletion.swapIndex_cost_le")
*Plain-English reading.* Lean checks the proposition indexed as “swap index cost le”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredIsometryCompletion.lean:102](../../../../library/modules/storedisometrycompletion/#decl-quantumblockencoding-storedisometrycompletion-swapindex-cost-le). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredIsometryCompletion.PermutationTable" (lean := "QuantumBlockEncoding.StoredIsometryCompletion.PermutationTable")
*Plain-English reading.* This record groups the data and proof fields needed for “permutation table”. A proposition-valued field is a requirement until a constructor supplies it.

*Formal status.* Data contract in the default import surface; proposition-valued fields are obligations, not automatically established facts.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* structure.

Source: [QuantumBlockEncoding/StoredIsometryCompletion.lean:107](../../../../library/modules/storedisometrycompletion/#decl-quantumblockencoding-storedisometrycompletion-permutationtable). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredIsometryCompletion.identityPermutation" (lean := "QuantumBlockEncoding.StoredIsometryCompletion.identityPermutation")
*Plain-English reading.* This definition gives the library's named construction or computation for “identity permutation”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredIsometryCompletion.lean:112](../../../../library/modules/storedisometrycompletion/#decl-quantumblockencoding-storedisometrycompletion-identitypermutation). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredIsometryCompletion.swapPermutation" (lean := "QuantumBlockEncoding.StoredIsometryCompletion.swapPermutation")
*Plain-English reading.* This definition gives the library's named construction or computation for “swap permutation”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredIsometryCompletion.lean:117](../../../../library/modules/storedisometrycompletion/#decl-quantumblockencoding-storedisometrycompletion-swappermutation). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredIsometryCompletion.swapPermutation_forward" (lean := "QuantumBlockEncoding.StoredIsometryCompletion.swapPermutation_forward")
*Plain-English reading.* Lean checks the proposition indexed as “swap permutation forward”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredIsometryCompletion.lean:129](../../../../library/modules/storedisometrycompletion/#decl-quantumblockencoding-storedisometrycompletion-swappermutation-forward). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredIsometryCompletion.swapPermutation_inverse" (lean := "QuantumBlockEncoding.StoredIsometryCompletion.swapPermutation_inverse")
*Plain-English reading.* Lean checks the proposition indexed as “swap permutation inverse”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredIsometryCompletion.lean:135](../../../../library/modules/storedisometrycompletion/#decl-quantumblockencoding-storedisometrycompletion-swappermutation-inverse). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredIsometryCompletion.swapPermutation_polarity" (lean := "QuantumBlockEncoding.StoredIsometryCompletion.swapPermutation_polarity")
*Plain-English reading.* Lean checks the proposition indexed as “swap permutation polarity”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredIsometryCompletion.lean:141](../../../../library/modules/storedisometrycompletion/#decl-quantumblockencoding-storedisometrycompletion-swappermutation-polarity). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredIsometryCompletion.realSign" (lean := "QuantumBlockEncoding.StoredIsometryCompletion.realSign")
*Plain-English reading.* This definition gives the library's named construction or computation for “real sign”. A proof-only interpretation of permutation orientation.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* A proof-only interpretation of permutation orientation.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredIsometryCompletion.lean:147](../../../../library/modules/storedisometrycompletion/#decl-quantumblockencoding-storedisometrycompletion-realsign). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredIsometryCompletion.realSign_swap_trans" (lean := "QuantumBlockEncoding.StoredIsometryCompletion.realSign_swap_trans")
*Plain-English reading.* Lean checks the proposition indexed as “real sign swap trans”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredIsometryCompletion.lean:149](../../../../library/modules/storedisometrycompletion/#decl-quantumblockencoding-storedisometrycompletion-realsign-swap-trans). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredIsometryCompletion.realSign_one_iff" (lean := "QuantumBlockEncoding.StoredIsometryCompletion.realSign_one_iff")
*Plain-English reading.* Lean checks the proposition indexed as “real sign one iff”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredIsometryCompletion.lean:154](../../../../library/modules/storedisometrycompletion/#decl-quantumblockencoding-storedisometrycompletion-realsign-one-iff). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredIsometryCompletion.permutation" (lean := "QuantumBlockEncoding.StoredIsometryCompletion.permutation")
*Plain-English reading.* This definition gives the library's named construction or computation for “permutation”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredIsometryCompletion.lean:159](../../../../library/modules/storedisometrycompletion/#decl-quantumblockencoding-storedisometrycompletion-permutation). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredIsometryCompletion.permutation_forward" (lean := "QuantumBlockEncoding.StoredIsometryCompletion.permutation_forward")
*Plain-English reading.* Lean checks the proposition indexed as “permutation forward”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredIsometryCompletion.lean:168](../../../../library/modules/storedisometrycompletion/#decl-quantumblockencoding-storedisometrycompletion-permutation-forward). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredIsometryCompletion.permutation_inverse" (lean := "QuantumBlockEncoding.StoredIsometryCompletion.permutation_inverse")
*Plain-English reading.* Lean checks the proposition indexed as “permutation inverse”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredIsometryCompletion.lean:181](../../../../library/modules/storedisometrycompletion/#decl-quantumblockencoding-storedisometrycompletion-permutation-inverse). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredIsometryCompletion.permutation_polarity" (lean := "QuantumBlockEncoding.StoredIsometryCompletion.permutation_polarity")
*Plain-English reading.* Lean checks the proposition indexed as “permutation polarity”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredIsometryCompletion.lean:194](../../../../library/modules/storedisometrycompletion/#decl-quantumblockencoding-storedisometrycompletion-permutation-polarity). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredIsometryCompletion.collect_cost_le" (lean := "QuantumBlockEncoding.StoredIsometryCompletion.collect_cost_le")
*Plain-English reading.* Lean checks the proposition indexed as “collect cost le”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredIsometryCompletion.lean:207](../../../../library/modules/storedisometrycompletion/#decl-quantumblockencoding-storedisometrycompletion-collect-cost-le). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredIsometryCompletion.swapBudget" (lean := "QuantumBlockEncoding.StoredIsometryCompletion.swapBudget")
*Plain-English reading.* This definition gives the library's named construction or computation for “swap budget”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredIsometryCompletion.lean:215](../../../../library/modules/storedisometrycompletion/#decl-quantumblockencoding-storedisometrycompletion-swapbudget). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredIsometryCompletion.swapPermutation_cost_le" (lean := "QuantumBlockEncoding.StoredIsometryCompletion.swapPermutation_cost_le")
*Plain-English reading.* Lean checks the proposition indexed as “swap permutation cost le”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredIsometryCompletion.lean:219](../../../../library/modules/storedisometrycompletion/#decl-quantumblockencoding-storedisometrycompletion-swappermutation-cost-le). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredIsometryCompletion.permutationBudget" (lean := "QuantumBlockEncoding.StoredIsometryCompletion.permutationBudget")
*Plain-English reading.* This definition gives the library's named construction or computation for “permutation budget”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredIsometryCompletion.lean:242](../../../../library/modules/storedisometrycompletion/#decl-quantumblockencoding-storedisometrycompletion-permutationbudget). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredIsometryCompletion.identityPermutation_cost" (lean := "QuantumBlockEncoding.StoredIsometryCompletion.identityPermutation_cost")
*Plain-English reading.* Lean checks the proposition indexed as “identity permutation cost”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredIsometryCompletion.lean:245](../../../../library/modules/storedisometrycompletion/#decl-quantumblockencoding-storedisometrycompletion-identitypermutation-cost). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredIsometryCompletion.permutation_cost_le" (lean := "QuantumBlockEncoding.StoredIsometryCompletion.permutation_cost_le")
*Plain-English reading.* Lean checks the proposition indexed as “permutation cost le”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredIsometryCompletion.lean:250](../../../../library/modules/storedisometrycompletion/#decl-quantumblockencoding-storedisometrycompletion-permutation-cost-le). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredIsometryCompletion.permuteColumns" (lean := "QuantumBlockEncoding.StoredIsometryCompletion.permuteColumns")
*Plain-English reading.* This definition gives the library's named construction or computation for “permute columns”. Every matrix entry reads its old column from the stored inverse table.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Every matrix entry reads its old column from the stored inverse table.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredIsometryCompletion.lean:267](../../../../library/modules/storedisometrycompletion/#decl-quantumblockencoding-storedisometrycompletion-permutecolumns). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredIsometryCompletion.permuteColumns_value" (lean := "QuantumBlockEncoding.StoredIsometryCompletion.permuteColumns_value")
*Plain-English reading.* Lean checks the proposition indexed as “permute columns value”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredIsometryCompletion.lean:274](../../../../library/modules/storedisometrycompletion/#decl-quantumblockencoding-storedisometrycompletion-permutecolumns-value). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredIsometryCompletion.permuteBudget" (lean := "QuantumBlockEncoding.StoredIsometryCompletion.permuteBudget")
*Plain-English reading.* This definition gives the library's named construction or computation for “permute budget”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredIsometryCompletion.lean:279](../../../../library/modules/storedisometrycompletion/#decl-quantumblockencoding-storedisometrycompletion-permutebudget). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredIsometryCompletion.permuteColumns_cost" (lean := "QuantumBlockEncoding.StoredIsometryCompletion.permuteColumns_cost")
*Plain-English reading.* Lean checks the proposition indexed as “permute columns cost”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredIsometryCompletion.lean:282](../../../../library/modules/storedisometrycompletion/#decl-quantumblockencoding-storedisometrycompletion-permutecolumns-cost). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredIsometryCompletion.signColumn" (lean := "QuantumBlockEncoding.StoredIsometryCompletion.signColumn")
*Plain-English reading.* This definition gives the library's named construction or computation for “sign column”. Negate exactly the spare entry of each stored row.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Negate exactly the spare entry of each stored row. Full row copies are charged, including the untouched entries, so no in-place storage is assumed.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredIsometryCompletion.lean:290](../../../../library/modules/storedisometrycompletion/#decl-quantumblockencoding-storedisometrycompletion-signcolumn). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredIsometryCompletion.signColumn_value" (lean := "QuantumBlockEncoding.StoredIsometryCompletion.signColumn_value")
*Plain-English reading.* Lean checks the proposition indexed as “sign column value”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredIsometryCompletion.lean:298](../../../../library/modules/storedisometrycompletion/#decl-quantumblockencoding-storedisometrycompletion-signcolumn-value). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredIsometryCompletion.signBudget" (lean := "QuantumBlockEncoding.StoredIsometryCompletion.signBudget")
*Plain-English reading.* This definition gives the library's named construction or computation for “sign budget”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredIsometryCompletion.lean:313](../../../../library/modules/storedisometrycompletion/#decl-quantumblockencoding-storedisometrycompletion-signbudget). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredIsometryCompletion.signColumn_cost" (lean := "QuantumBlockEncoding.StoredIsometryCompletion.signColumn_cost")
*Plain-English reading.* Lean checks the proposition indexed as “sign column cost”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredIsometryCompletion.lean:317](../../../../library/modules/storedisometrycompletion/#decl-quantumblockencoding-storedisometrycompletion-signcolumn-cost). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredIsometryCompletion.placeColumns" (lean := "QuantumBlockEncoding.StoredIsometryCompletion.placeColumns")
*Plain-English reading.* This definition gives the library's named construction or computation for “place columns”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredIsometryCompletion.lean:323](../../../../library/modules/storedisometrycompletion/#decl-quantumblockencoding-storedisometrycompletion-placecolumns). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredIsometryCompletion.placeColumns_value" (lean := "QuantumBlockEncoding.StoredIsometryCompletion.placeColumns_value")
*Plain-English reading.* Lean checks the proposition indexed as “place columns value”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredIsometryCompletion.lean:331](../../../../library/modules/storedisometrycompletion/#decl-quantumblockencoding-storedisometrycompletion-placecolumns-value). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredIsometryCompletion.placeBudget" (lean := "QuantumBlockEncoding.StoredIsometryCompletion.placeBudget")
*Plain-English reading.* This definition gives the library's named construction or computation for “place budget”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredIsometryCompletion.lean:363](../../../../library/modules/storedisometrycompletion/#decl-quantumblockencoding-storedisometrycompletion-placebudget). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredIsometryCompletion.placeColumns_cost_le" (lean := "QuantumBlockEncoding.StoredIsometryCompletion.placeColumns_cost_le")
*Plain-English reading.* Lean checks the proposition indexed as “place columns cost le”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredIsometryCompletion.lean:366](../../../../library/modules/storedisometrycompletion/#decl-quantumblockencoding-storedisometrycompletion-placecolumns-cost-le). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredIsometryCompletion.complete" (lean := "QuantumBlockEncoding.StoredIsometryCompletion.complete")
*Plain-English reading.* This definition gives the library's named construction or computation for “complete”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredIsometryCompletion.lean:374](../../../../library/modules/storedisometrycompletion/#decl-quantumblockencoding-storedisometrycompletion-complete). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredIsometryCompletion.complete_value" (lean := "QuantumBlockEncoding.StoredIsometryCompletion.complete_value")
*Plain-English reading.* Lean checks the proposition indexed as “complete value”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredIsometryCompletion.lean:379](../../../../library/modules/storedisometrycompletion/#decl-quantumblockencoding-storedisometrycompletion-complete-value). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredIsometryCompletion.completeBudget" (lean := "QuantumBlockEncoding.StoredIsometryCompletion.completeBudget")
*Plain-English reading.* This definition gives the library's named construction or computation for “complete budget”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredIsometryCompletion.lean:386](../../../../library/modules/storedisometrycompletion/#decl-quantumblockencoding-storedisometrycompletion-completebudget). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredIsometryCompletion.complete_cost_le" (lean := "QuantumBlockEncoding.StoredIsometryCompletion.complete_cost_le")
*Plain-English reading.* Lean checks the proposition indexed as “complete cost le”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredIsometryCompletion.lean:388](../../../../library/modules/storedisometrycompletion/#decl-quantumblockencoding-storedisometrycompletion-complete-cost-le). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredIsometryCompletion.complete_spec" (lean := "QuantumBlockEncoding.StoredIsometryCompletion.complete_spec")
*Plain-English reading.* Lean checks the proposition indexed as “complete spec”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredIsometryCompletion.lean:396](../../../../library/modules/storedisometrycompletion/#decl-quantumblockencoding-storedisometrycompletion-complete-spec). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredIsometryCompletion.polynomialBudget" (lean := "QuantumBlockEncoding.StoredIsometryCompletion.polynomialBudget")
*Plain-English reading.* This definition gives the library's named construction or computation for “polynomial budget”. Expanded bound including the stored prefix matrix, both permutation tables, orientation tracking, column placement, and spare-column correction.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Expanded bound including the stored prefix matrix, both permutation tables, orientation tracking, column placement, and spare-column correction.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredIsometryCompletion.lean:406](../../../../library/modules/storedisometrycompletion/#decl-quantumblockencoding-storedisometrycompletion-polynomialbudget). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredIsometryCompletion.completeBudget_eq" (lean := "QuantumBlockEncoding.StoredIsometryCompletion.completeBudget_eq")
*Plain-English reading.* Lean checks the proposition indexed as “complete budget eq”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredIsometryCompletion.lean:416](../../../../library/modules/storedisometrycompletion/#decl-quantumblockencoding-storedisometrycompletion-completebudget-eq). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredIsometryCompletion.complete_polynomial_cost_le" (lean := "QuantumBlockEncoding.StoredIsometryCompletion.complete_polynomial_cost_le")
*Plain-English reading.* Lean checks the proposition indexed as “complete polynomial cost le”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredIsometryCompletion.lean:423](../../../../library/modules/storedisometrycompletion/#decl-quantumblockencoding-storedisometrycompletion-complete-polynomial-cost-le). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredIsometryCompletion.complete_total_cost_le" (lean := "QuantumBlockEncoding.StoredIsometryCompletion.complete_total_cost_le")
*Plain-English reading.* Lean checks the proposition indexed as “complete total cost le”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredIsometryCompletion.lean:429](../../../../library/modules/storedisometrycompletion/#decl-quantumblockencoding-storedisometrycompletion-complete-total-cost-le). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredIsometryCompletion.completeFrom" (lean := "QuantumBlockEncoding.StoredIsometryCompletion.completeFrom")
*Plain-English reading.* This definition gives the library's named construction or computation for “complete from”. Source construction is composed as counted input producers, never an unpriced callback hidden inside the completion.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Source construction is composed as counted input producers, never an unpriced callback hidden inside the completion.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredIsometryCompletion.lean:447](../../../../library/modules/storedisometrycompletion/#decl-quantumblockencoding-storedisometrycompletion-completefrom). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredIsometryCompletion.completeFrom_value" (lean := "QuantumBlockEncoding.StoredIsometryCompletion.completeFrom_value")
*Plain-English reading.* Lean checks the proposition indexed as “complete from value”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredIsometryCompletion.lean:454](../../../../library/modules/storedisometrycompletion/#decl-quantumblockencoding-storedisometrycompletion-completefrom-value). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredIsometryCompletion.completeFrom_cost_le" (lean := "QuantumBlockEncoding.StoredIsometryCompletion.completeFrom_cost_le")
*Plain-English reading.* Lean checks the proposition indexed as “complete from cost le”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredIsometryCompletion.lean:460](../../../../library/modules/storedisometrycompletion/#decl-quantumblockencoding-storedisometrycompletion-completefrom-cost-le). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/StoredRectangularGivens.lean

30 explicit public declarations, in source order.

:::definition "QuantumBlockEncoding.StoredRectangularGivens.append" (lean := "QuantumBlockEncoding.StoredRectangularGivens.append")
*Plain-English reading.* This definition gives the library's named construction or computation for “append”. Copy the left list spine, sharing the right list.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Copy the left list spine, sharing the right list. Cost-counter traversal is bookkeeping; the data append's reads and writes are charged here.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredRectangularGivens.lean:21](../../../../library/modules/storedrectangulargivens/#decl-quantumblockencoding-storedrectangulargivens-append). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredRectangularGivens.sweep" (lean := "QuantumBlockEncoding.StoredRectangularGivens.sweep")
*Plain-English reading.* This definition gives the library's named construction or computation for “sweep”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredRectangularGivens.lean:24](../../../../library/modules/storedrectangulargivens/#decl-quantumblockencoding-storedrectangulargivens-sweep). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredRectangularGivens.sweep_matrix" (lean := "QuantumBlockEncoding.StoredRectangularGivens.sweep_matrix")
*Plain-English reading.* Lean checks the proposition indexed as “sweep matrix”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredRectangularGivens.lean:36](../../../../library/modules/storedrectangulargivens/#decl-quantumblockencoding-storedrectangulargivens-sweep-matrix). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredRectangularGivens.sweep_steps" (lean := "QuantumBlockEncoding.StoredRectangularGivens.sweep_steps")
*Plain-English reading.* Lean checks the proposition indexed as “sweep steps”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredRectangularGivens.lean:50](../../../../library/modules/storedrectangulargivens/#decl-quantumblockencoding-storedrectangulargivens-sweep-steps). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredRectangularGivens.columnBudget" (lean := "QuantumBlockEncoding.StoredRectangularGivens.columnBudget")
*Plain-English reading.* This definition gives the library's named construction or computation for “column budget”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredRectangularGivens.lean:64](../../../../library/modules/storedrectangulargivens/#decl-quantumblockencoding-storedrectangulargivens-columnbudget). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredRectangularGivens.sweep_cost_le" (lean := "QuantumBlockEncoding.StoredRectangularGivens.sweep_cost_le")
*Plain-English reading.* Lean checks the proposition indexed as “sweep cost le”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredRectangularGivens.lean:67](../../../../library/modules/storedrectangulargivens/#decl-quantumblockencoding-storedrectangulargivens-sweep-cost-le). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredRectangularGivens.identity" (lean := "QuantumBlockEncoding.StoredRectangularGivens.identity")
*Plain-English reading.* This definition gives the library's named construction or computation for “identity”. Materialized identity, including its finite-index equality decisions.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Materialized identity, including its finite-index equality decisions.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredRectangularGivens.lean:92](../../../../library/modules/storedrectangulargivens/#decl-quantumblockencoding-storedrectangulargivens-identity). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredRectangularGivens.identity_value" (lean := "QuantumBlockEncoding.StoredRectangularGivens.identity_value")
*Plain-English reading.* Lean checks the proposition indexed as “identity value”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredRectangularGivens.lean:95](../../../../library/modules/storedrectangulargivens/#decl-quantumblockencoding-storedrectangulargivens-identity-value). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredRectangularGivens.identityBudget" (lean := "QuantumBlockEncoding.StoredRectangularGivens.identityBudget")
*Plain-English reading.* This definition gives the library's named construction or computation for “identity budget”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredRectangularGivens.lean:100](../../../../library/modules/storedrectangulargivens/#decl-quantumblockencoding-storedrectangulargivens-identitybudget). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredRectangularGivens.identity_cost" (lean := "QuantumBlockEncoding.StoredRectangularGivens.identity_cost")
*Plain-English reading.* Lean checks the proposition indexed as “identity cost”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredRectangularGivens.lean:103](../../../../library/modules/storedrectangulargivens/#decl-quantumblockencoding-storedrectangulargivens-identity-cost). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredRectangularGivens.replay" (lean := "QuantumBlockEncoding.StoredRectangularGivens.replay")
*Plain-English reading.* This definition gives the library's named construction or computation for “replay”. Replay uses only a pair of stored row updates.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Replay uses only a pair of stored row updates. Reading the end marker is charged in the empty case; each cons record is charged in the nonempty case.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredRectangularGivens.lean:110](../../../../library/modules/storedrectangulargivens/#decl-quantumblockencoding-storedrectangulargivens-replay). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredRectangularGivens.replay_value" (lean := "QuantumBlockEncoding.StoredRectangularGivens.replay_value")
*Plain-English reading.* Lean checks the proposition indexed as “replay value”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredRectangularGivens.lean:120](../../../../library/modules/storedrectangulargivens/#decl-quantumblockencoding-storedrectangulargivens-replay-value). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredRectangularGivens.replayStepBudget" (lean := "QuantumBlockEncoding.StoredRectangularGivens.replayStepBudget")
*Plain-English reading.* This definition gives the library's named construction or computation for “replay step budget”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredRectangularGivens.lean:130](../../../../library/modules/storedrectangulargivens/#decl-quantumblockencoding-storedrectangulargivens-replaystepbudget). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredRectangularGivens.replay_cost" (lean := "QuantumBlockEncoding.StoredRectangularGivens.replay_cost")
*Plain-English reading.* Lean checks the proposition indexed as “replay cost”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredRectangularGivens.lean:135](../../../../library/modules/storedrectangulargivens/#decl-quantumblockencoding-storedrectangulargivens-replay-cost). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredRectangularGivens.Result" (lean := "QuantumBlockEncoding.StoredRectangularGivens.Result")
*Plain-English reading.* This record groups the data and proof fields needed for “result”. A proposition-valued field is a requirement until a constructor supplies it.

*Formal status.* Data contract in the default import surface; proposition-valued fields are obligations, not automatically established facts.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* structure.

Source: [QuantumBlockEncoding/StoredRectangularGivens.lean:145](../../../../library/modules/storedrectangulargivens/#decl-quantumblockencoding-storedrectangulargivens-result). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredRectangularGivens.compile" (lean := "QuantumBlockEncoding.StoredRectangularGivens.compile")
*Plain-English reading.* This definition gives the library's named construction or computation for “compile”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredRectangularGivens.lean:150](../../../../library/modules/storedrectangulargivens/#decl-quantumblockencoding-storedrectangulargivens-compile). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredRectangularGivens.compile_reduced" (lean := "QuantumBlockEncoding.StoredRectangularGivens.compile_reduced")
*Plain-English reading.* Lean checks the proposition indexed as “compile reduced”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredRectangularGivens.lean:156](../../../../library/modules/storedrectangulargivens/#decl-quantumblockencoding-storedrectangulargivens-compile-reduced). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredRectangularGivens.compile_steps" (lean := "QuantumBlockEncoding.StoredRectangularGivens.compile_steps")
*Plain-English reading.* Lean checks the proposition indexed as “compile steps”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredRectangularGivens.lean:161](../../../../library/modules/storedrectangulargivens/#decl-quantumblockencoding-storedrectangulargivens-compile-steps). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredRectangularGivens.compile_transform" (lean := "QuantumBlockEncoding.StoredRectangularGivens.compile_transform")
*Plain-English reading.* Lean checks the proposition indexed as “compile transform”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredRectangularGivens.lean:166](../../../../library/modules/storedrectangulargivens/#decl-quantumblockencoding-storedrectangulargivens-compile-transform). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredRectangularGivens.compileBudget" (lean := "QuantumBlockEncoding.StoredRectangularGivens.compileBudget")
*Plain-English reading.* This definition gives the library's named construction or computation for “compile budget”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredRectangularGivens.lean:172](../../../../library/modules/storedrectangulargivens/#decl-quantumblockencoding-storedrectangulargivens-compilebudget). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredRectangularGivens.compile_cost_le" (lean := "QuantumBlockEncoding.StoredRectangularGivens.compile_cost_le")
*Plain-English reading.* Lean checks the proposition indexed as “compile cost le”; the hypotheses and conclusion in the code panel fix its exact scope. A componentwise polynomial operation bound for residual, log, and the fully stored accumulated transform produced by this actual algorithm.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* A componentwise polynomial operation bound for residual, log, and the fully stored accumulated transform produced by this actual algorithm.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredRectangularGivens.lean:178](../../../../library/modules/storedrectangulargivens/#decl-quantumblockencoding-storedrectangulargivens-compile-cost-le). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredRectangularGivens.compile_exact_recovery" (lean := "QuantumBlockEncoding.StoredRectangularGivens.compile_exact_recovery")
*Plain-English reading.* Lean checks the proposition indexed as “compile exact recovery”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredRectangularGivens.lean:190](../../../../library/modules/storedrectangulargivens/#decl-quantumblockencoding-storedrectangulargivens-compile-exact-recovery). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredRectangularGivens.compile_transform_orthogonal" (lean := "QuantumBlockEncoding.StoredRectangularGivens.compile_transform_orthogonal")
*Plain-English reading.* Lean checks the proposition indexed as “compile transform orthogonal”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredRectangularGivens.lean:196](../../../../library/modules/storedrectangulargivens/#decl-quantumblockencoding-storedrectangulargivens-compile-transform-orthogonal). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredRectangularGivens.compile_transform_det" (lean := "QuantumBlockEncoding.StoredRectangularGivens.compile_transform_det")
*Plain-English reading.* Lean checks the proposition indexed as “compile transform det”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredRectangularGivens.lean:201](../../../../library/modules/storedrectangulargivens/#decl-quantumblockencoding-storedrectangulargivens-compile-transform-det). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredRectangularGivens.compile_zero_below" (lean := "QuantumBlockEncoding.StoredRectangularGivens.compile_zero_below")
*Plain-English reading.* Lean checks the proposition indexed as “compile zero below”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredRectangularGivens.lean:206](../../../../library/modules/storedrectangulargivens/#decl-quantumblockencoding-storedrectangulargivens-compile-zero-below). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredRectangularGivens.polynomialBudget" (lean := "QuantumBlockEncoding.StoredRectangularGivens.polynomialBudget")
*Plain-English reading.* This definition gives the library's named construction or computation for “polynomial budget”. Expanded polynomial form of every operation category.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Expanded polynomial form of every operation category.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredRectangularGivens.lean:213](../../../../library/modules/storedrectangulargivens/#decl-quantumblockencoding-storedrectangulargivens-polynomialbudget). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredRectangularGivens.compileBudget_eq" (lean := "QuantumBlockEncoding.StoredRectangularGivens.compileBudget_eq")
*Plain-English reading.* Lean checks the proposition indexed as “compile budget eq”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredRectangularGivens.lean:223](../../../../library/modules/storedrectangulargivens/#decl-quantumblockencoding-storedrectangulargivens-compilebudget-eq). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredRectangularGivens.compile_polynomial_cost_le" (lean := "QuantumBlockEncoding.StoredRectangularGivens.compile_polynomial_cost_le")
*Plain-English reading.* Lean checks the proposition indexed as “compile polynomial cost le”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredRectangularGivens.lean:230](../../../../library/modules/storedrectangulargivens/#decl-quantumblockencoding-storedrectangulargivens-compile-polynomial-cost-le). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredRectangularGivens.total" (lean := "QuantumBlockEncoding.StoredRectangularGivens.total")
*Plain-English reading.* This definition gives the library's named construction or computation for “total”. Sum of the eight explicitly separated operation counters.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Sum of the eight explicitly separated operation counters.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredRectangularGivens.lean:236](../../../../library/modules/storedrectangulargivens/#decl-quantumblockencoding-storedrectangulargivens-total). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredRectangularGivens.compile_total_cost_le" (lean := "QuantumBlockEncoding.StoredRectangularGivens.compile_total_cost_le")
*Plain-English reading.* Lean checks the proposition indexed as “compile total cost le”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredRectangularGivens.lean:240](../../../../library/modules/storedrectangulargivens/#decl-quantumblockencoding-storedrectangulargivens-compile-total-cost-le). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/StoredSelectedRyTrace.lean

55 explicit public declarations, in source order.

:::definition "QuantumBlockEncoding.StoredSelectedRyTrace.Coefficients" (lean := "QuantumBlockEncoding.StoredSelectedRyTrace.Coefficients")
*Plain-English reading.* This abbreviation gives a shorter name to the type or expression used for “coefficients”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* abbrev.

Source: [QuantumBlockEncoding/StoredSelectedRyTrace.lean:28](../../../../library/modules/storedselectedrytrace/#decl-quantumblockencoding-storedselectedrytrace-coefficients). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredSelectedRyTrace.basisIndex" (lean := "QuantumBlockEncoding.StoredSelectedRyTrace.basisIndex")
*Plain-English reading.* This definition gives the library's named construction or computation for “basis index”. The first recursive control selects the high half of the stored array.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The first recursive control selects the high half of the stored array.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredSelectedRyTrace.lean:31](../../../../library/modules/storedselectedrytrace/#decl-quantumblockencoding-storedselectedrytrace-basisindex). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredSelectedRyTrace.denote" (lean := "QuantumBlockEncoding.StoredSelectedRyTrace.denote")
*Plain-English reading.* This definition gives the library's named construction or computation for “denote”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredSelectedRyTrace.lean:41](../../../../library/modules/storedselectedrytrace/#decl-quantumblockencoding-storedselectedrytrace-denote). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredSelectedRyTrace.denoteBits" (lean := "QuantumBlockEncoding.StoredSelectedRyTrace.denoteBits")
*Plain-English reading.* This definition gives the library's named construction or computation for “denote bits”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredSelectedRyTrace.lean:44](../../../../library/modules/storedselectedrytrace/#decl-quantumblockencoding-storedselectedrytrace-denotebits). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredSelectedRyTrace.basisIndex_injective" (lean := "QuantumBlockEncoding.StoredSelectedRyTrace.basisIndex_injective")
*Plain-English reading.* Lean checks the proposition indexed as “basis index injective”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredSelectedRyTrace.lean:47](../../../../library/modules/storedselectedrytrace/#decl-quantumblockencoding-storedselectedrytrace-basisindex-injective). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredSelectedRyTrace.tail" (lean := "QuantumBlockEncoding.StoredSelectedRyTrace.tail")
*Plain-English reading.* This definition gives the library's named construction or computation for “tail”. Materialize a tail; persistent storage is not treated as a free view.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Materialize a tail; persistent storage is not treated as a free view.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredSelectedRyTrace.lean:71](../../../../library/modules/storedselectedrytrace/#decl-quantumblockencoding-storedselectedrytrace-tail). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredSelectedRyTrace.tail_value" (lean := "QuantumBlockEncoding.StoredSelectedRyTrace.tail_value")
*Plain-English reading.* Lean checks the proposition indexed as “tail value”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredSelectedRyTrace.lean:74](../../../../library/modules/storedselectedrytrace/#decl-quantumblockencoding-storedselectedrytrace-tail-value). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredSelectedRyTrace.tail_cost" (lean := "QuantumBlockEncoding.StoredSelectedRyTrace.tail_cost")
*Plain-English reading.* Lean checks the proposition indexed as “tail cost”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredSelectedRyTrace.lean:78](../../../../library/modules/storedselectedrytrace/#decl-quantumblockencoding-storedselectedrytrace-tail-cost). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredSelectedRyTrace.halfAdd" (lean := "QuantumBlockEncoding.StoredSelectedRyTrace.halfAdd")
*Plain-English reading.* This definition gives the library's named construction or computation for “half add”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredSelectedRyTrace.lean:83](../../../../library/modules/storedselectedrytrace/#decl-quantumblockencoding-storedselectedrytrace-halfadd). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredSelectedRyTrace.halfSub" (lean := "QuantumBlockEncoding.StoredSelectedRyTrace.halfSub")
*Plain-English reading.* This definition gives the library's named construction or computation for “half sub”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredSelectedRyTrace.lean:87](../../../../library/modules/storedselectedrytrace/#decl-quantumblockencoding-storedselectedrytrace-halfsub). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredSelectedRyTrace.halfAdd_value" (lean := "QuantumBlockEncoding.StoredSelectedRyTrace.halfAdd_value")
*Plain-English reading.* Lean checks the proposition indexed as “half add value”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredSelectedRyTrace.lean:91](../../../../library/modules/storedselectedrytrace/#decl-quantumblockencoding-storedselectedrytrace-halfadd-value). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredSelectedRyTrace.halfSub_value" (lean := "QuantumBlockEncoding.StoredSelectedRyTrace.halfSub_value")
*Plain-English reading.* Lean checks the proposition indexed as “half sub value”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredSelectedRyTrace.lean:92](../../../../library/modules/storedselectedrytrace/#decl-quantumblockencoding-storedselectedrytrace-halfsub-value). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredSelectedRyTrace.halfAdd_cost" (lean := "QuantumBlockEncoding.StoredSelectedRyTrace.halfAdd_cost")
*Plain-English reading.* Lean checks the proposition indexed as “half add cost”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredSelectedRyTrace.lean:94](../../../../library/modules/storedselectedrytrace/#decl-quantumblockencoding-storedselectedrytrace-halfadd-cost). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredSelectedRyTrace.halfSub_cost" (lean := "QuantumBlockEncoding.StoredSelectedRyTrace.halfSub_cost")
*Plain-English reading.* Lean checks the proposition indexed as “half sub cost”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredSelectedRyTrace.lean:98](../../../../library/modules/storedselectedrytrace/#decl-quantumblockencoding-storedselectedrytrace-halfsub-cost). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredSelectedRyTrace.split" (lean := "QuantumBlockEncoding.StoredSelectedRyTrace.split")
*Plain-English reading.* This definition gives the library's named construction or computation for “split”. One read of each input coefficient feeds both charged half operations.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* One read of each input coefficient feeds both charged half operations. The pair vector is stored before its two stored projections are constructed.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredSelectedRyTrace.lean:104](../../../../library/modules/storedselectedrytrace/#decl-quantumblockencoding-storedselectedrytrace-split). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredSelectedRyTrace.split_value" (lean := "QuantumBlockEncoding.StoredSelectedRyTrace.split_value")
*Plain-English reading.* Lean checks the proposition indexed as “split value”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredSelectedRyTrace.lean:120](../../../../library/modules/storedselectedrytrace/#decl-quantumblockencoding-storedselectedrytrace-split-value). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredSelectedRyTrace.split_cost" (lean := "QuantumBlockEncoding.StoredSelectedRyTrace.split_cost")
*Plain-English reading.* Lean checks the proposition indexed as “split cost”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredSelectedRyTrace.lean:128](../../../../library/modules/storedselectedrytrace/#decl-quantumblockencoding-storedselectedrytrace-split-cost). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredSelectedRyTrace.append" (lean := "QuantumBlockEncoding.StoredSelectedRyTrace.append")
*Plain-English reading.* This definition gives the library's named construction or computation for “append”. A real recursive persistent append: inspect each node and copy each nonempty prefix node.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* A real recursive persistent append: inspect each node and copy each nonempty prefix node. The right list is shared, never traversed here.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredSelectedRyTrace.lean:137](../../../../library/modules/storedselectedrytrace/#decl-quantumblockencoding-storedselectedrytrace-append). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredSelectedRyTrace.append_value" (lean := "QuantumBlockEncoding.StoredSelectedRyTrace.append_value")
*Plain-English reading.* Lean checks the proposition indexed as “append value”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredSelectedRyTrace.lean:143](../../../../library/modules/storedselectedrytrace/#decl-quantumblockencoding-storedselectedrytrace-append-value). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredSelectedRyTrace.append_cost" (lean := "QuantumBlockEncoding.StoredSelectedRyTrace.append_cost")
*Plain-English reading.* Lean checks the proposition indexed as “append cost”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredSelectedRyTrace.lean:148](../../../../library/modules/storedselectedrytrace/#decl-quantumblockencoding-storedselectedrytrace-append-cost). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredSelectedRyTrace.emit" (lean := "QuantumBlockEncoding.StoredSelectedRyTrace.emit")
*Plain-English reading.* This definition gives the library's named construction or computation for “emit”. Emission and the list-cell write are both charged.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Emission and the list-cell write are both charged.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredSelectedRyTrace.lean:158](../../../../library/modules/storedselectedrytrace/#decl-quantumblockencoding-storedselectedrytrace-emit). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredSelectedRyTrace.emit_value" (lean := "QuantumBlockEncoding.StoredSelectedRyTrace.emit_value")
*Plain-English reading.* Lean checks the proposition indexed as “emit value”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredSelectedRyTrace.lean:163](../../../../library/modules/storedselectedrytrace/#decl-quantumblockencoding-storedselectedrytrace-emit-value). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredSelectedRyTrace.emit_cost" (lean := "QuantumBlockEncoding.StoredSelectedRyTrace.emit_cost")
*Plain-English reading.* Lean checks the proposition indexed as “emit cost”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredSelectedRyTrace.lean:166](../../../../library/modules/storedselectedrytrace/#decl-quantumblockencoding-storedselectedrytrace-emit-cost). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredSelectedRyTrace.compile" (lean := "QuantumBlockEncoding.StoredSelectedRyTrace.compile")
*Plain-English reading.* This definition gives the library's named construction or computation for “compile”. The actual recursive stored producer.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The actual recursive stored producer. The two append traversals copy only the two recursively emitted prefixes, not an already assembled trace.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredSelectedRyTrace.lean:171](../../../../library/modules/storedselectedrytrace/#decl-quantumblockencoding-storedselectedrytrace-compile). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredSelectedRyTrace.compile_value" (lean := "QuantumBlockEncoding.StoredSelectedRyTrace.compile_value")
*Plain-English reading.* Lean checks the proposition indexed as “compile value”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredSelectedRyTrace.lean:195](../../../../library/modules/storedselectedrytrace/#decl-quantumblockencoding-storedselectedrytrace-compile-value). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredSelectedRyTrace.compile_length" (lean := "QuantumBlockEncoding.StoredSelectedRyTrace.compile_length")
*Plain-English reading.* Lean checks the proposition indexed as “compile length”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredSelectedRyTrace.lean:208](../../../../library/modules/storedselectedrytrace/#decl-quantumblockencoding-storedselectedrytrace-compile-length). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredSelectedRyTrace.compile_refines" (lean := "QuantumBlockEncoding.StoredSelectedRyTrace.compile_refines")
*Plain-English reading.* Lean checks the proposition indexed as “compile refines”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredSelectedRyTrace.lean:215](../../../../library/modules/storedselectedrytrace/#decl-quantumblockencoding-storedselectedrytrace-compile-refines). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredSelectedRyTrace.traceCost" (lean := "QuantumBlockEncoding.StoredSelectedRyTrace.traceCost")
*Plain-English reading.* This definition gives the library's named construction or computation for “trace cost”. This recurrence describes the charged algorithm, including both append traversals and materialization of the control-wire tail at every node.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* This recurrence describes the charged algorithm, including both append traversals and materialization of the control-wire tail at every node.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredSelectedRyTrace.lean:225](../../../../library/modules/storedselectedrytrace/#decl-quantumblockencoding-storedselectedrytrace-tracecost). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredSelectedRyTrace.compile_cost" (lean := "QuantumBlockEncoding.StoredSelectedRyTrace.compile_cost")
*Plain-English reading.* Lean checks the proposition indexed as “compile cost”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredSelectedRyTrace.lean:235](../../../../library/modules/storedselectedrytrace/#decl-quantumblockencoding-storedselectedrytrace-compile-cost). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredSelectedRyTrace.traceCost_fields" (lean := "QuantumBlockEncoding.StoredSelectedRyTrace.traceCost_fields")
*Plain-English reading.* Lean checks the proposition indexed as “trace cost fields”; the hypotheses and conclusion in the code panel fix its exact scope. Exact component counts, written additively to avoid truncated subtraction.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Exact component counts, written additively to avoid truncated subtraction.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredSelectedRyTrace.lean:249](../../../../library/modules/storedselectedrytrace/#decl-quantumblockencoding-storedselectedrytrace-tracecost-fields). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredSelectedRyTrace.traceCost_unused" (lean := "QuantumBlockEncoding.StoredSelectedRyTrace.traceCost_unused")
*Plain-English reading.* Lean checks the proposition indexed as “trace cost unused”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredSelectedRyTrace.lean:267](../../../../library/modules/storedselectedrytrace/#decl-quantumblockencoding-storedselectedrytrace-tracecost-unused). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredSelectedRyTrace.compile_emit" (lean := "QuantumBlockEncoding.StoredSelectedRyTrace.compile_emit")
*Plain-English reading.* Lean checks the proposition indexed as “compile emit”; the hypotheses and conclusion in the code panel fix its exact scope. All instructions, including zero-coefficient rotations, are emitted.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* All instructions, including zero-coefficient rotations, are emitted.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredSelectedRyTrace.lean:275](../../../../library/modules/storedselectedrytrace/#decl-quantumblockencoding-storedselectedrytrace-compile-emit). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredSelectedRyTrace.traceCost_total" (lean := "QuantumBlockEncoding.StoredSelectedRyTrace.traceCost_total")
*Plain-English reading.* Lean checks the proposition indexed as “trace cost total”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredSelectedRyTrace.lean:285](../../../../library/modules/storedselectedrytrace/#decl-quantumblockencoding-storedselectedrytrace-tracecost-total). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredSelectedRyTrace.compile_total_cost_le" (lean := "QuantumBlockEncoding.StoredSelectedRyTrace.compile_total_cost_le")
*Plain-English reading.* Lean checks the proposition indexed as “compile total cost le”; the hypotheses and conclusion in the code panel fix its exact scope. Polynomial in the local table size 'S = 2^q' and control count 'q'.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Polynomial in the local table size 'S = 2^q' and control count 'q'.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredSelectedRyTrace.lean:293](../../../../library/modules/storedselectedrytrace/#decl-quantumblockencoding-storedselectedrytrace-compile-total-cost-le). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredSelectedRyTrace.joinIndex" (lean := "QuantumBlockEncoding.StoredSelectedRyTrace.joinIndex")
*Plain-English reading.* This definition gives the library's named construction or computation for “join index”. Two explicit index-word operations, locally charged under the field tag.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Two explicit index-word operations, locally charged under the field tag. This helper makes no claim about the cost of integer multiplication in bits.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredSelectedRyTrace.lean:304](../../../../library/modules/storedselectedrytrace/#decl-quantumblockencoding-storedselectedrytrace-joinindex). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredSelectedRyTrace.encode" (lean := "QuantumBlockEncoding.StoredSelectedRyTrace.encode")
*Plain-English reading.* This definition gives the library's named construction or computation for “encode”. Convert a stored bit pattern to its array address, with charged tail copies.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Convert a stored bit pattern to its array address, with charged tail copies.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredSelectedRyTrace.lean:315](../../../../library/modules/storedselectedrytrace/#decl-quantumblockencoding-storedselectedrytrace-encode). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredSelectedRyTrace.encode_value" (lean := "QuantumBlockEncoding.StoredSelectedRyTrace.encode_value")
*Plain-English reading.* Lean checks the proposition indexed as “encode value”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredSelectedRyTrace.lean:324](../../../../library/modules/storedselectedrytrace/#decl-quantumblockencoding-storedselectedrytrace-encode-value). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredSelectedRyTrace.encodingIndexOperations" (lean := "QuantumBlockEncoding.StoredSelectedRyTrace.encodingIndexOperations")
*Plain-English reading.* This definition gives the library's named construction or computation for “encoding index operations”. Independent index-operation count; these are the extra local field-tag charges and do not change any existing real/rational field-cost theorem.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Independent index-operation count; these are the extra local field-tag charges and do not change any existing real/rational field-cost theorem.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredSelectedRyTrace.lean:338](../../../../library/modules/storedselectedrytrace/#decl-quantumblockencoding-storedselectedrytrace-encodingindexoperations). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredSelectedRyTrace.encodingCost" (lean := "QuantumBlockEncoding.StoredSelectedRyTrace.encodingCost")
*Plain-English reading.* This definition gives the library's named construction or computation for “encoding cost”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredSelectedRyTrace.lean:340](../../../../library/modules/storedselectedrytrace/#decl-quantumblockencoding-storedselectedrytrace-encodingcost). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredSelectedRyTrace.encode_cost" (lean := "QuantumBlockEncoding.StoredSelectedRyTrace.encode_cost")
*Plain-English reading.* Lean checks the proposition indexed as “encode cost”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredSelectedRyTrace.lean:345](../../../../library/modules/storedselectedrytrace/#decl-quantumblockencoding-storedselectedrytrace-encode-cost). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredSelectedRyTrace.encode_index_operations" (lean := "QuantumBlockEncoding.StoredSelectedRyTrace.encode_index_operations")
*Plain-English reading.* Lean checks the proposition indexed as “encode index operations”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredSelectedRyTrace.lean:353](../../../../library/modules/storedselectedrytrace/#decl-quantumblockencoding-storedselectedrytrace-encode-index-operations). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredSelectedRyTrace.encodingCost_bound" (lean := "QuantumBlockEncoding.StoredSelectedRyTrace.encodingCost_bound")
*Plain-English reading.* Lean checks the proposition indexed as “encoding cost bound”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredSelectedRyTrace.lean:360](../../../../library/modules/storedselectedrytrace/#decl-quantumblockencoding-storedselectedrytrace-encodingcost-bound). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredSelectedRyTrace.oneHot" (lean := "QuantumBlockEncoding.StoredSelectedRyTrace.oneHot")
*Plain-English reading.* This definition gives the library's named construction or computation for “one hot”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredSelectedRyTrace.lean:367](../../../../library/modules/storedselectedrytrace/#decl-quantumblockencoding-storedselectedrytrace-onehot). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredSelectedRyTrace.oneHot_value" (lean := "QuantumBlockEncoding.StoredSelectedRyTrace.oneHot_value")
*Plain-English reading.* Lean checks the proposition indexed as “one hot value”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredSelectedRyTrace.lean:370](../../../../library/modules/storedselectedrytrace/#decl-quantumblockencoding-storedselectedrytrace-onehot-value). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredSelectedRyTrace.oneHot_cost" (lean := "QuantumBlockEncoding.StoredSelectedRyTrace.oneHot_cost")
*Plain-English reading.* Lean checks the proposition indexed as “one hot cost”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredSelectedRyTrace.lean:375](../../../../library/modules/storedselectedrytrace/#decl-quantumblockencoding-storedselectedrytrace-onehot-cost). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredSelectedRyTrace.selectedCoefficients" (lean := "QuantumBlockEncoding.StoredSelectedRyTrace.selectedCoefficients")
*Plain-English reading.* This definition gives the library's named construction or computation for “selected coefficients”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredSelectedRyTrace.lean:381](../../../../library/modules/storedselectedrytrace/#decl-quantumblockencoding-storedselectedrytrace-selectedcoefficients). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredSelectedRyTrace.selectedCoefficients_value" (lean := "QuantumBlockEncoding.StoredSelectedRyTrace.selectedCoefficients_value")
*Plain-English reading.* Lean checks the proposition indexed as “selected coefficients value”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredSelectedRyTrace.lean:385](../../../../library/modules/storedselectedrytrace/#decl-quantumblockencoding-storedselectedrytrace-selectedcoefficients-value). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredSelectedRyTrace.selected" (lean := "QuantumBlockEncoding.StoredSelectedRyTrace.selected")
*Plain-English reading.* This definition gives the library's named construction or computation for “selected”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredSelectedRyTrace.lean:391](../../../../library/modules/storedselectedrytrace/#decl-quantumblockencoding-storedselectedrytrace-selected). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredSelectedRyTrace.selected_value" (lean := "QuantumBlockEncoding.StoredSelectedRyTrace.selected_value")
*Plain-English reading.* Lean checks the proposition indexed as “selected value”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredSelectedRyTrace.lean:397](../../../../library/modules/storedselectedrytrace/#decl-quantumblockencoding-storedselectedrytrace-selected-value). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredSelectedRyTrace.selected_refines" (lean := "QuantumBlockEncoding.StoredSelectedRyTrace.selected_refines")
*Plain-English reading.* Lean checks the proposition indexed as “selected refines”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredSelectedRyTrace.lean:404](../../../../library/modules/storedselectedrytrace/#decl-quantumblockencoding-storedselectedrytrace-selected-refines). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredSelectedRyTrace.selected_cost" (lean := "QuantumBlockEncoding.StoredSelectedRyTrace.selected_cost")
*Plain-English reading.* Lean checks the proposition indexed as “selected cost”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredSelectedRyTrace.lean:411](../../../../library/modules/storedselectedrytrace/#decl-quantumblockencoding-storedselectedrytrace-selected-cost). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredSelectedRyTrace.selected_field_tag_split" (lean := "QuantumBlockEncoding.StoredSelectedRyTrace.selected_field_tag_split")
*Plain-English reading.* Lean checks the proposition indexed as “selected field tag split”; the hypotheses and conclusion in the code panel fix its exact scope. The local field-tag overcount is exposed separately from rational work.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The local field-tag overcount is exposed separately from rational work.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredSelectedRyTrace.lean:418](../../../../library/modules/storedselectedrytrace/#decl-quantumblockencoding-storedselectedrytrace-selected-field-tag-split). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredSelectedRyTrace.selected_length" (lean := "QuantumBlockEncoding.StoredSelectedRyTrace.selected_length")
*Plain-English reading.* Lean checks the proposition indexed as “selected length”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredSelectedRyTrace.lean:427](../../../../library/modules/storedselectedrytrace/#decl-quantumblockencoding-storedselectedrytrace-selected-length). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredSelectedRyTrace.selected_emit" (lean := "QuantumBlockEncoding.StoredSelectedRyTrace.selected_emit")
*Plain-English reading.* Lean checks the proposition indexed as “selected emit”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredSelectedRyTrace.lean:434](../../../../library/modules/storedselectedrytrace/#decl-quantumblockencoding-storedselectedrytrace-selected-emit). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredSelectedRyTrace.selected_total_cost_le" (lean := "QuantumBlockEncoding.StoredSelectedRyTrace.selected_total_cost_le")
*Plain-English reading.* Lean checks the proposition indexed as “selected total cost le”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredSelectedRyTrace.lean:445](../../../../library/modules/storedselectedrytrace/#decl-quantumblockencoding-storedselectedrytrace-selected-total-cost-le). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/StoredTensorTrain.lean

30 explicit public declarations, in source order.

:::definition "QuantumBlockEncoding.StoredTensorTrain.StoredCore" (lean := "QuantumBlockEncoding.StoredTensorTrain.StoredCore")
*Plain-English reading.* This abbreviation gives a shorter name to the type or expression used for “stored core”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* abbrev.

Source: [QuantumBlockEncoding/StoredTensorTrain.lean:19](../../../../library/modules/storedtensortrain/#decl-quantumblockencoding-storedtensortrain-storedcore). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredTensorTrain.denoteCore" (lean := "QuantumBlockEncoding.StoredTensorTrain.denoteCore")
*Plain-English reading.* This definition gives the library's named construction or computation for “denote core”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredTensorTrain.lean:21](../../../../library/modules/storedtensortrain/#decl-quantumblockencoding-storedtensortrain-denotecore). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredTensorTrain.StoredChain" (lean := "QuantumBlockEncoding.StoredTensorTrain.StoredChain")
*Plain-English reading.* This type lists the allowed alternatives for “stored chain”; its constructors are the cases that downstream code must handle.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* inductive.

Source: [QuantumBlockEncoding/StoredTensorTrain.lean:24](../../../../library/modules/storedtensortrain/#decl-quantumblockencoding-storedtensortrain-storedchain). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredTensorTrain.denoteChain" (lean := "QuantumBlockEncoding.StoredTensorTrain.denoteChain")
*Plain-English reading.* This definition gives the library's named construction or computation for “denote chain”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredTensorTrain.lean:29](../../../../library/modules/storedtensortrain/#decl-quantumblockencoding-storedtensortrain-denotechain). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredTensorTrain.sumEntries" (lean := "QuantumBlockEncoding.StoredTensorTrain.sumEntries")
*Plain-English reading.* This definition gives the library's named construction or computation for “sum entries”. Each callback is invoked once; its arithmetic cost remains charged.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Each callback is invoked once; its arithmetic cost remains charged.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredTensorTrain.lean:34](../../../../library/modules/storedtensortrain/#decl-quantumblockencoding-storedtensortrain-sumentries). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredTensorTrain.sumEntries_value" (lean := "QuantumBlockEncoding.StoredTensorTrain.sumEntries_value")
*Plain-English reading.* Lean checks the proposition indexed as “sum entries value”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredTensorTrain.lean:41](../../../../library/modules/storedtensortrain/#decl-quantumblockencoding-storedtensortrain-sumentries-value). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredTensorTrain.sumEntries_cost_le" (lean := "QuantumBlockEncoding.StoredTensorTrain.sumEntries_cost_le")
*Plain-English reading.* Lean checks the proposition indexed as “sum entries cost le”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredTensorTrain.lean:49](../../../../library/modules/storedtensortrain/#decl-quantumblockencoding-storedtensortrain-sumentries-cost-le). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredTensorTrain.absorptionEntry" (lean := "QuantumBlockEncoding.StoredTensorTrain.absorptionEntry")
*Plain-English reading.* This definition gives the library's named construction or computation for “absorption entry”. Multiply the residual into a single bit-preserving output entry.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Multiply the residual into a single bit-preserving output entry.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredTensorTrain.lean:62](../../../../library/modules/storedtensortrain/#decl-quantumblockencoding-storedtensortrain-absorptionentry). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredTensorTrain.absorptionEntry_value" (lean := "QuantumBlockEncoding.StoredTensorTrain.absorptionEntry_value")
*Plain-English reading.* Lean checks the proposition indexed as “absorption entry value”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredTensorTrain.lean:69](../../../../library/modules/storedtensortrain/#decl-quantumblockencoding-storedtensortrain-absorptionentry-value). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredTensorTrain.absorptionEntry_cost_le" (lean := "QuantumBlockEncoding.StoredTensorTrain.absorptionEntry_cost_le")
*Plain-English reading.* Lean checks the proposition indexed as “absorption entry cost le”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredTensorTrain.lean:75](../../../../library/modules/storedtensortrain/#decl-quantumblockencoding-storedtensortrain-absorptionentry-cost-le). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredTensorTrain.absorption" (lean := "QuantumBlockEncoding.StoredTensorTrain.absorption")
*Plain-English reading.* This definition gives the library's named construction or computation for “absorption”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredTensorTrain.lean:87](../../../../library/modules/storedtensortrain/#decl-quantumblockencoding-storedtensortrain-absorption). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredTensorTrain.absorption_value" (lean := "QuantumBlockEncoding.StoredTensorTrain.absorption_value")
*Plain-English reading.* Lean checks the proposition indexed as “absorption value”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredTensorTrain.lean:91](../../../../library/modules/storedtensortrain/#decl-quantumblockencoding-storedtensortrain-absorption-value). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredTensorTrain.absorptionBudget" (lean := "QuantumBlockEncoding.StoredTensorTrain.absorptionBudget")
*Plain-English reading.* This definition gives the library's named construction or computation for “absorption budget”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredTensorTrain.lean:99](../../../../library/modules/storedtensortrain/#decl-quantumblockencoding-storedtensortrain-absorptionbudget). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredTensorTrain.absorption_cost_le" (lean := "QuantumBlockEncoding.StoredTensorTrain.absorption_cost_le")
*Plain-English reading.* Lean checks the proposition indexed as “absorption cost le”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredTensorTrain.lean:103](../../../../library/modules/storedtensortrain/#decl-quantumblockencoding-storedtensortrain-absorption-cost-le). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredTensorTrain.CoreResult" (lean := "QuantumBlockEncoding.StoredTensorTrain.CoreResult")
*Plain-English reading.* This record groups the data and proof fields needed for “core result”. A proposition-valued field is a requirement until a constructor supplies it.

*Formal status.* Data contract in the default import surface; proposition-valued fields are obligations, not automatically established facts.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* structure.

Source: [QuantumBlockEncoding/StoredTensorTrain.lean:122](../../../../library/modules/storedtensortrain/#decl-quantumblockencoding-storedtensortrain-coreresult). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredTensorTrain.factorCore" (lean := "QuantumBlockEncoding.StoredTensorTrain.factorCore")
*Plain-English reading.* This definition gives the library's named construction or computation for “factor core”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredTensorTrain.lean:128](../../../../library/modules/storedtensortrain/#decl-quantumblockencoding-storedtensortrain-factorcore). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredTensorTrain.Result" (lean := "QuantumBlockEncoding.StoredTensorTrain.Result")
*Plain-English reading.* This record groups the data and proof fields needed for “result”. A proposition-valued field is a requirement until a constructor supplies it.

*Formal status.* Data contract in the default import surface; proposition-valued fields are obligations, not automatically established facts.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* structure.

Source: [QuantumBlockEncoding/StoredTensorTrain.lean:141](../../../../library/modules/storedtensortrain/#decl-quantumblockencoding-storedtensortrain-result). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredTensorTrain.nodeBudget" (lean := "QuantumBlockEncoding.StoredTensorTrain.nodeBudget")
*Plain-English reading.* This definition gives the library's named construction or computation for “node budget”. Read the input tag/payload and allocate the output chain node.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Read the input tag/payload and allocate the output chain node.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredTensorTrain.lean:151](../../../../library/modules/storedtensortrain/#decl-quantumblockencoding-storedtensortrain-nodebudget). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredTensorTrain.canonicalize" (lean := "QuantumBlockEncoding.StoredTensorTrain.canonicalize")
*Plain-English reading.* This definition gives the library's named construction or computation for “canonicalize”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredTensorTrain.lean:153](../../../../library/modules/storedtensortrain/#decl-quantumblockencoding-storedtensortrain-canonicalize). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredTensorTrain.canonicalize_maxBond_le" (lean := "QuantumBlockEncoding.StoredTensorTrain.canonicalize_maxBond_le")
*Plain-English reading.* Lean checks the proposition indexed as “canonicalize max bond le”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredTensorTrain.lean:184](../../../../library/modules/storedtensortrain/#decl-quantumblockencoding-storedtensortrain-canonicalize-maxbond-le). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredTensorTrain.absorption_total_cost_le" (lean := "QuantumBlockEncoding.StoredTensorTrain.absorption_total_cost_le")
*Plain-English reading.* Lean checks the proposition indexed as “absorption total cost le”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredTensorTrain.lean:194](../../../../library/modules/storedtensortrain/#decl-quantumblockencoding-storedtensortrain-absorption-total-cost-le). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredTensorTrain.factorCore_total_cost_le" (lean := "QuantumBlockEncoding.StoredTensorTrain.factorCore_total_cost_le")
*Plain-English reading.* Lean checks the proposition indexed as “factor core total cost le”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredTensorTrain.lean:210](../../../../library/modules/storedtensortrain/#decl-quantumblockencoding-storedtensortrain-factorcore-total-cost-le). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredTensorTrain.canonicalize_total_cost_le" (lean := "QuantumBlockEncoding.StoredTensorTrain.canonicalize_total_cost_le")
*Plain-English reading.* Lean checks the proposition indexed as “canonicalize total cost le”; the hypotheses and conclusion in the code panel fix its exact scope. The actual stored producer uses linear-in-length, cubic-in-bond work in the declared exact-real model, including absorption, storage, and node costs.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The actual stored producer uses linear-in-length, cubic-in-bond work in the declared exact-real model, including absorption, storage, and node costs.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredTensorTrain.lean:232](../../../../library/modules/storedtensortrain/#decl-quantumblockencoding-storedtensortrain-canonicalize-total-cost-le). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredTensorTrain.boundary" (lean := "QuantumBlockEncoding.StoredTensorTrain.boundary")
*Plain-English reading.* This definition gives the library's named construction or computation for “boundary”. The output's semantic boundary is obtained from its stored residual.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The output's semantic boundary is obtained from its stored residual.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredTensorTrain.lean:269](../../../../library/modules/storedtensortrain/#decl-quantumblockencoding-storedtensortrain-boundary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredTensorTrain.boundary_mass" (lean := "QuantumBlockEncoding.StoredTensorTrain.boundary_mass")
*Plain-English reading.* Lean checks the proposition indexed as “boundary mass”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredTensorTrain.lean:272](../../../../library/modules/storedtensortrain/#decl-quantumblockencoding-storedtensortrain-boundary-mass). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredTensorTrain.boundary_normalized" (lean := "QuantumBlockEncoding.StoredTensorTrain.boundary_normalized")
*Plain-English reading.* Lean checks the proposition indexed as “boundary normalized”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredTensorTrain.lean:278](../../../../library/modules/storedtensortrain/#decl-quantumblockencoding-storedtensortrain-boundary-normalized). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredTensorTrain.Result.toSemantic" (lean := "QuantumBlockEncoding.StoredTensorTrain.Result.toSemantic")
*Plain-English reading.* This definition gives the library's named construction or computation for “to semantic”. Forget storage, retaining the actual concrete canonicalization data.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Forget storage, retaining the actual concrete canonicalization data.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredTensorTrain.lean:283](../../../../library/modules/storedtensortrain/#decl-quantumblockencoding-storedtensortrain-result-tosemantic). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredTensorTrain.factorCore_R" (lean := "QuantumBlockEncoding.StoredTensorTrain.factorCore_R")
*Plain-English reading.* Lean checks the proposition indexed as “factor core r”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredTensorTrain.lean:292](../../../../library/modules/storedtensortrain/#decl-quantumblockencoding-storedtensortrain-factorcore-r). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredTensorTrain.factorCore_Q" (lean := "QuantumBlockEncoding.StoredTensorTrain.factorCore_Q")
*Plain-English reading.* Lean checks the proposition indexed as “factor core q”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredTensorTrain.lean:303](../../../../library/modules/storedtensortrain/#decl-quantumblockencoding-storedtensortrain-factorcore-q). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredTensorTrain.canonicalize_refines" (lean := "QuantumBlockEncoding.StoredTensorTrain.canonicalize_refines")
*Plain-English reading.* Lean checks the proposition indexed as “canonicalize refines”; the hypotheses and conclusion in the code panel fix its exact scope. Exact data refinement, not just another witness of the same contract.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Exact data refinement, not just another witness of the same contract.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredTensorTrain.lean:336](../../../../library/modules/storedtensortrain/#decl-quantumblockencoding-storedtensortrain-canonicalize-refines). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/StoredTensorTrainNorm.lean

29 explicit public declarations, in source order.

:::definition "QuantumBlockEncoding.StoredTensorTrainNorm.firstEntry" (lean := "QuantumBlockEncoding.StoredTensorTrainNorm.firstEntry")
*Plain-English reading.* This definition gives the library's named construction or computation for “first entry”. The first pass computes one entry of 'A\_bit \* E'.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The first pass computes one entry of 'A\_bit \* E'.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredTensorTrainNorm.lean:27](../../../../library/modules/storedtensortrainnorm/#decl-quantumblockencoding-storedtensortrainnorm-firstentry). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredTensorTrainNorm.firstPass" (lean := "QuantumBlockEncoding.StoredTensorTrainNorm.firstPass")
*Plain-English reading.* This definition gives the library's named construction or computation for “first pass”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredTensorTrainNorm.lean:34](../../../../library/modules/storedtensortrainnorm/#decl-quantumblockencoding-storedtensortrainnorm-firstpass). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredTensorTrainNorm.firstPass_value" (lean := "QuantumBlockEncoding.StoredTensorTrainNorm.firstPass_value")
*Plain-English reading.* Lean checks the proposition indexed as “first pass value”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredTensorTrainNorm.lean:38](../../../../library/modules/storedtensortrainnorm/#decl-quantumblockencoding-storedtensortrainnorm-firstpass-value). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredTensorTrainNorm.secondEntry" (lean := "QuantumBlockEncoding.StoredTensorTrainNorm.secondEntry")
*Plain-English reading.* This definition gives the library's named construction or computation for “second entry”. The second pass reads the stored first pass and the original core.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The second pass reads the stored first pass and the original core. The transpose is an index exchange, not an uncharged matrix constructor.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredTensorTrainNorm.lean:47](../../../../library/modules/storedtensortrainnorm/#decl-quantumblockencoding-storedtensortrainnorm-secondentry). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredTensorTrainNorm.secondPass" (lean := "QuantumBlockEncoding.StoredTensorTrainNorm.secondPass")
*Plain-English reading.* This definition gives the library's named construction or computation for “second pass”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredTensorTrainNorm.lean:54](../../../../library/modules/storedtensortrainnorm/#decl-quantumblockencoding-storedtensortrainnorm-secondpass). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredTensorTrainNorm.secondPass_value" (lean := "QuantumBlockEncoding.StoredTensorTrainNorm.secondPass_value")
*Plain-English reading.* Lean checks the proposition indexed as “second pass value”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredTensorTrainNorm.lean:58](../../../../library/modules/storedtensortrainnorm/#decl-quantumblockencoding-storedtensortrainnorm-secondpass-value). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredTensorTrainNorm.addMatrices" (lean := "QuantumBlockEncoding.StoredTensorTrainNorm.addMatrices")
*Plain-English reading.* This definition gives the library's named construction or computation for “add matrices”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredTensorTrainNorm.lean:65](../../../../library/modules/storedtensortrainnorm/#decl-quantumblockencoding-storedtensortrainnorm-addmatrices). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredTensorTrainNorm.addMatrices_value" (lean := "QuantumBlockEncoding.StoredTensorTrainNorm.addMatrices_value")
*Plain-English reading.* Lean checks the proposition indexed as “add matrices value”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredTensorTrainNorm.lean:72](../../../../library/modules/storedtensortrainnorm/#decl-quantumblockencoding-storedtensortrainnorm-addmatrices-value). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredTensorTrainNorm.update" (lean := "QuantumBlockEncoding.StoredTensorTrainNorm.update")
*Plain-English reading.* This definition gives the library's named construction or computation for “update”. All four contraction outputs and the sum are materialized.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* All four contraction outputs and the sum are materialized. The two uses of 'E' are stored reads of the same cached environment.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredTensorTrainNorm.lean:80](../../../../library/modules/storedtensortrainnorm/#decl-quantumblockencoding-storedtensortrainnorm-update). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredTensorTrainNorm.update_value" (lean := "QuantumBlockEncoding.StoredTensorTrainNorm.update_value")
*Plain-English reading.* Lean checks the proposition indexed as “update value”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredTensorTrainNorm.lean:88](../../../../library/modules/storedtensortrainnorm/#decl-quantumblockencoding-storedtensortrainnorm-update-value). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredTensorTrainNorm.cacheNode" (lean := "QuantumBlockEncoding.StoredTensorTrainNorm.cacheNode")
*Plain-English reading.* This definition gives the library's named construction or computation for “cache node”. A fixed six-word traversal/cache-record allowance per chain node, as in the stored canonicalizer.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* A fixed six-word traversal/cache-record allowance per chain node, as in the stored canonicalizer. Matrix copying is charged separately by each pass.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredTensorTrainNorm.lean:97](../../../../library/modules/storedtensortrainnorm/#decl-quantumblockencoding-storedtensortrainnorm-cachenode). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredTensorTrainNorm.gram" (lean := "QuantumBlockEncoding.StoredTensorTrainNorm.gram")
*Plain-English reading.* This definition gives the library's named construction or computation for “gram”. Streaming cached Gram environments.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Streaming cached Gram environments. The recursive result is bound once and remains a stored matrix throughout the two physical-bit updates.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredTensorTrainNorm.lean:103](../../../../library/modules/storedtensortrainnorm/#decl-quantumblockencoding-storedtensortrainnorm-gram). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredTensorTrainNorm.gram_value" (lean := "QuantumBlockEncoding.StoredTensorTrainNorm.gram_value")
*Plain-English reading.* Lean checks the proposition indexed as “gram value”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredTensorTrainNorm.lean:109](../../../../library/modules/storedtensortrainnorm/#decl-quantumblockencoding-storedtensortrainnorm-gram-value). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredTensorTrainNorm.norm" (lean := "QuantumBlockEncoding.StoredTensorTrainNorm.norm")
*Plain-English reading.* This definition gives the library's named construction or computation for “norm”. Scalar-boundary norm with the final table lookup and square root charged.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Scalar-boundary norm with the final table lookup and square root charged.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredTensorTrainNorm.lean:118](../../../../library/modules/storedtensortrainnorm/#decl-quantumblockencoding-storedtensortrainnorm-norm). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredTensorTrainNorm.norm_value" (lean := "QuantumBlockEncoding.StoredTensorTrainNorm.norm_value")
*Plain-English reading.* Lean checks the proposition indexed as “norm value”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredTensorTrainNorm.lean:123](../../../../library/modules/storedtensortrainnorm/#decl-quantumblockencoding-storedtensortrainnorm-norm-value). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredTensorTrainNorm.norm_eq_sum" (lean := "QuantumBlockEncoding.StoredTensorTrainNorm.norm_eq_sum")
*Plain-English reading.* Lean checks the proposition indexed as “norm eq sum”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredTensorTrainNorm.lean:129](../../../../library/modules/storedtensortrainnorm/#decl-quantumblockencoding-storedtensortrainnorm-norm-eq-sum). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredTensorTrainNorm.productBudget" (lean := "QuantumBlockEncoding.StoredTensorTrainNorm.productBudget")
*Plain-English reading.* This definition gives the library's named construction or computation for “product budget”. Scalar-boundary norm with the final table lookup and square root charged.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Scalar-boundary norm with the final table lookup and square root charged. -/ noncomputable def norm \{n : ℕ\} (C : StoredChain n 1 1) : Run ℝ := do let E ← gram C let mass ← StoredThinLQ.entry E 0 0 StoredGivens.sqrt mass theorem norm\_value \{n : ℕ\} (C : StoredChain n 1 1) : (norm C).value = TensorTrainNormEnvironment.norm (denoteChain C) := by change Real.sqrt (denote (gram C).value 0 0) = \_ rw \[gram\_value\] rfl theorem norm\_eq\_sum \{n : ℕ\} (C : StoredChain n 1 1) : (norm C).value = Real.sqrt (∑ x : Word n, contract (denoteChain C) x 0 0 ^ 2) := (norm\_value C).trans (TensorTrainNormEnvironment.norm\_eq (denoteChain C)) /-! number number  Operation counts for the same producer

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredTensorTrainNorm.lean:135](../../../../library/modules/storedtensortrainnorm/#decl-quantumblockencoding-storedtensortrainnorm-productbudget). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredTensorTrainNorm.additionBudget" (lean := "QuantumBlockEncoding.StoredTensorTrainNorm.additionBudget")
*Plain-English reading.* This definition gives the library's named construction or computation for “addition budget”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredTensorTrainNorm.lean:139](../../../../library/modules/storedtensortrainnorm/#decl-quantumblockencoding-storedtensortrainnorm-additionbudget). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredTensorTrainNorm.updateBudget" (lean := "QuantumBlockEncoding.StoredTensorTrainNorm.updateBudget")
*Plain-English reading.* This definition gives the library's named construction or computation for “update budget”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredTensorTrainNorm.lean:143](../../../../library/modules/storedtensortrainnorm/#decl-quantumblockencoding-storedtensortrainnorm-updatebudget). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredTensorTrainNorm.firstEntry_cost_le" (lean := "QuantumBlockEncoding.StoredTensorTrainNorm.firstEntry_cost_le")
*Plain-English reading.* Lean checks the proposition indexed as “first entry cost le”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredTensorTrainNorm.lean:163](../../../../library/modules/storedtensortrainnorm/#decl-quantumblockencoding-storedtensortrainnorm-firstentry-cost-le). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredTensorTrainNorm.secondEntry_cost_le" (lean := "QuantumBlockEncoding.StoredTensorTrainNorm.secondEntry_cost_le")
*Plain-English reading.* Lean checks the proposition indexed as “second entry cost le”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredTensorTrainNorm.lean:175](../../../../library/modules/storedtensortrainnorm/#decl-quantumblockencoding-storedtensortrainnorm-secondentry-cost-le). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredTensorTrainNorm.firstPass_cost_le" (lean := "QuantumBlockEncoding.StoredTensorTrainNorm.firstPass_cost_le")
*Plain-English reading.* Lean checks the proposition indexed as “first pass cost le”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredTensorTrainNorm.lean:187](../../../../library/modules/storedtensortrainnorm/#decl-quantumblockencoding-storedtensortrainnorm-firstpass-cost-le). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredTensorTrainNorm.secondPass_cost_le" (lean := "QuantumBlockEncoding.StoredTensorTrainNorm.secondPass_cost_le")
*Plain-English reading.* Lean checks the proposition indexed as “second pass cost le”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredTensorTrainNorm.lean:195](../../../../library/modules/storedtensortrainnorm/#decl-quantumblockencoding-storedtensortrainnorm-secondpass-cost-le). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredTensorTrainNorm.addMatrices_cost_le" (lean := "QuantumBlockEncoding.StoredTensorTrainNorm.addMatrices_cost_le")
*Plain-English reading.* Lean checks the proposition indexed as “add matrices cost le”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredTensorTrainNorm.lean:203](../../../../library/modules/storedtensortrainnorm/#decl-quantumblockencoding-storedtensortrainnorm-addmatrices-cost-le). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredTensorTrainNorm.update_cost_le" (lean := "QuantumBlockEncoding.StoredTensorTrainNorm.update_cost_le")
*Plain-English reading.* Lean checks the proposition indexed as “update cost le”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredTensorTrainNorm.lean:210](../../../../library/modules/storedtensortrainnorm/#decl-quantumblockencoding-storedtensortrainnorm-update-cost-le). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredTensorTrainNorm.update_total_cost_le" (lean := "QuantumBlockEncoding.StoredTensorTrainNorm.update_total_cost_le")
*Plain-English reading.* Lean checks the proposition indexed as “update total cost le”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredTensorTrainNorm.lean:234](../../../../library/modules/storedtensortrainnorm/#decl-quantumblockencoding-storedtensortrainnorm-update-total-cost-le). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredTensorTrainNorm.gram_total_cost_le" (lean := "QuantumBlockEncoding.StoredTensorTrainNorm.gram_total_cost_le")
*Plain-English reading.* Lean checks the proposition indexed as “gram total cost le”; the hypotheses and conclusion in the code panel fix its exact scope. Total of all eight counters, including every materialization pass and the fixed node records.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Total of all eight counters, including every materialization pass and the fixed node records. The bond bound concerns the already stored input chain.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredTensorTrainNorm.lean:256](../../../../library/modules/storedtensortrainnorm/#decl-quantumblockencoding-storedtensortrainnorm-gram-total-cost-le). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredTensorTrainNorm.norm_cost" (lean := "QuantumBlockEncoding.StoredTensorTrainNorm.norm_cost")
*Plain-English reading.* Lean checks the proposition indexed as “norm cost”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredTensorTrainNorm.lean:286](../../../../library/modules/storedtensortrainnorm/#decl-quantumblockencoding-storedtensortrainnorm-norm-cost). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredTensorTrainNorm.norm_total_cost_le" (lean := "QuantumBlockEncoding.StoredTensorTrainNorm.norm_total_cost_le")
*Plain-English reading.* Lean checks the proposition indexed as “norm total cost le”; the hypotheses and conclusion in the code panel fix its exact scope. The scalar supplier adds precisely two stored reads and one square root.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The scalar supplier adds precisely two stored reads and one square root.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredTensorTrainNorm.lean:293](../../../../library/modules/storedtensortrainnorm/#decl-quantumblockencoding-storedtensortrainnorm-norm-total-cost-le). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/StoredThinLQ.lean

28 explicit public declarations, in source order.

:::definition "QuantumBlockEncoding.StoredThinLQ.Factors" (lean := "QuantumBlockEncoding.StoredThinLQ.Factors")
*Plain-English reading.* This record groups the data and proof fields needed for “factors”. A proposition-valued field is a requirement until a constructor supplies it.

*Formal status.* Data contract in the default import surface; proposition-valued fields are obligations, not automatically established facts.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* structure.

Source: [QuantumBlockEncoding/StoredThinLQ.lean:16](../../../../library/modules/storedthinlq/#decl-quantumblockencoding-storedthinlq-factors). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredThinLQ.entry" (lean := "QuantumBlockEncoding.StoredThinLQ.entry")
*Plain-English reading.* This definition gives the library's named construction or computation for “entry”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredThinLQ.lean:20](../../../../library/modules/storedthinlq/#decl-quantumblockencoding-storedthinlq-entry). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredThinLQ.entry_value" (lean := "QuantumBlockEncoding.StoredThinLQ.entry_value")
*Plain-English reading.* Lean checks the proposition indexed as “entry value”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredThinLQ.lean:24](../../../../library/modules/storedthinlq/#decl-quantumblockencoding-storedthinlq-entry-value). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredThinLQ.entry_cost" (lean := "QuantumBlockEncoding.StoredThinLQ.entry_cost")
*Plain-English reading.* Lean checks the proposition indexed as “entry cost”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredThinLQ.lean:27](../../../../library/modules/storedthinlq/#decl-quantumblockencoding-storedthinlq-entry-cost). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredThinLQ.transpose" (lean := "QuantumBlockEncoding.StoredThinLQ.transpose")
*Plain-English reading.* This definition gives the library's named construction or computation for “transpose”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredThinLQ.lean:32](../../../../library/modules/storedthinlq/#decl-quantumblockencoding-storedthinlq-transpose). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredThinLQ.transpose_value" (lean := "QuantumBlockEncoding.StoredThinLQ.transpose_value")
*Plain-English reading.* Lean checks the proposition indexed as “transpose value”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredThinLQ.lean:35](../../../../library/modules/storedthinlq/#decl-quantumblockencoding-storedthinlq-transpose-value). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredThinLQ.extractionBudget" (lean := "QuantumBlockEncoding.StoredThinLQ.extractionBudget")
*Plain-English reading.* This definition gives the library's named construction or computation for “extraction budget”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredThinLQ.lean:40](../../../../library/modules/storedthinlq/#decl-quantumblockencoding-storedthinlq-extractionbudget). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredThinLQ.transpose_cost" (lean := "QuantumBlockEncoding.StoredThinLQ.transpose_cost")
*Plain-English reading.* Lean checks the proposition indexed as “transpose cost”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredThinLQ.lean:43](../../../../library/modules/storedthinlq/#decl-quantumblockencoding-storedthinlq-transpose-cost). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredThinLQ.extractR" (lean := "QuantumBlockEncoding.StoredThinLQ.extractR")
*Plain-English reading.* This definition gives the library's named construction or computation for “extract r”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredThinLQ.lean:48](../../../../library/modules/storedthinlq/#decl-quantumblockencoding-storedthinlq-extractr). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredThinLQ.extractQ" (lean := "QuantumBlockEncoding.StoredThinLQ.extractQ")
*Plain-English reading.* This definition gives the library's named construction or computation for “extract q”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredThinLQ.lean:51](../../../../library/modules/storedthinlq/#decl-quantumblockencoding-storedthinlq-extractq). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredThinLQ.extractR_value" (lean := "QuantumBlockEncoding.StoredThinLQ.extractR_value")
*Plain-English reading.* Lean checks the proposition indexed as “extract r value”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredThinLQ.lean:54](../../../../library/modules/storedthinlq/#decl-quantumblockencoding-storedthinlq-extractr-value). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredThinLQ.extractQ_value" (lean := "QuantumBlockEncoding.StoredThinLQ.extractQ_value")
*Plain-English reading.* Lean checks the proposition indexed as “extract q value”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredThinLQ.lean:59](../../../../library/modules/storedthinlq/#decl-quantumblockencoding-storedthinlq-extractq-value). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredThinLQ.extractR_cost" (lean := "QuantumBlockEncoding.StoredThinLQ.extractR_cost")
*Plain-English reading.* Lean checks the proposition indexed as “extract r cost”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredThinLQ.lean:64](../../../../library/modules/storedthinlq/#decl-quantumblockencoding-storedthinlq-extractr-cost). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredThinLQ.extractQ_cost" (lean := "QuantumBlockEncoding.StoredThinLQ.extractQ_cost")
*Plain-English reading.* Lean checks the proposition indexed as “extract q cost”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredThinLQ.lean:69](../../../../library/modules/storedthinlq/#decl-quantumblockencoding-storedthinlq-extractq-cost). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredThinLQ.wide" (lean := "QuantumBlockEncoding.StoredThinLQ.wide")
*Plain-English reading.* This definition gives the library's named construction or computation for “wide”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredThinLQ.lean:74](../../../../library/modules/storedthinlq/#decl-quantumblockencoding-storedthinlq-wide). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredThinLQ.wide_R" (lean := "QuantumBlockEncoding.StoredThinLQ.wide_R")
*Plain-English reading.* Lean checks the proposition indexed as “wide r”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredThinLQ.lean:82](../../../../library/modules/storedthinlq/#decl-quantumblockencoding-storedthinlq-wide-r). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredThinLQ.wide_Q" (lean := "QuantumBlockEncoding.StoredThinLQ.wide_Q")
*Plain-English reading.* Lean checks the proposition indexed as “wide q”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredThinLQ.lean:88](../../../../library/modules/storedthinlq/#decl-quantumblockencoding-storedthinlq-wide-q). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredThinLQ.wide_correct" (lean := "QuantumBlockEncoding.StoredThinLQ.wide_correct")
*Plain-English reading.* Lean checks the proposition indexed as “wide correct”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredThinLQ.lean:94](../../../../library/modules/storedthinlq/#decl-quantumblockencoding-storedthinlq-wide-correct). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredThinLQ.wideBudget" (lean := "QuantumBlockEncoding.StoredThinLQ.wideBudget")
*Plain-English reading.* This definition gives the library's named construction or computation for “wide budget”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredThinLQ.lean:100](../../../../library/modules/storedthinlq/#decl-quantumblockencoding-storedthinlq-widebudget). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredThinLQ.wide_cost_le" (lean := "QuantumBlockEncoding.StoredThinLQ.wide_cost_le")
*Plain-English reading.* Lean checks the proposition indexed as “wide cost le”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredThinLQ.lean:104](../../../../library/modules/storedthinlq/#decl-quantumblockencoding-storedthinlq-wide-cost-le). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredThinLQ.compileBody" (lean := "QuantumBlockEncoding.StoredThinLQ.compileBody")
*Plain-English reading.* This definition gives the library's named construction or computation for “compile body”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredThinLQ.lean:111](../../../../library/modules/storedthinlq/#decl-quantumblockencoding-storedthinlq-compilebody). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredThinLQ.compile" (lean := "QuantumBlockEncoding.StoredThinLQ.compile")
*Plain-English reading.* This definition gives the library's named construction or computation for “compile”. The all-shape dispatcher additionally charges its dimension comparison.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The all-shape dispatcher additionally charges its dimension comparison.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredThinLQ.lean:122](../../../../library/modules/storedthinlq/#decl-quantumblockencoding-storedthinlq-compile). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredThinLQ.compile_R" (lean := "QuantumBlockEncoding.StoredThinLQ.compile_R")
*Plain-English reading.* Lean checks the proposition indexed as “compile r”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredThinLQ.lean:145](../../../../library/modules/storedthinlq/#decl-quantumblockencoding-storedthinlq-compile-r). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredThinLQ.compile_Q" (lean := "QuantumBlockEncoding.StoredThinLQ.compile_Q")
*Plain-English reading.* Lean checks the proposition indexed as “compile q”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredThinLQ.lean:153](../../../../library/modules/storedthinlq/#decl-quantumblockencoding-storedthinlq-compile-q). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredThinLQ.compile_correct" (lean := "QuantumBlockEncoding.StoredThinLQ.compile_correct")
*Plain-English reading.* Lean checks the proposition indexed as “compile correct”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredThinLQ.lean:162](../../../../library/modules/storedthinlq/#decl-quantumblockencoding-storedthinlq-compile-correct). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StoredThinLQ.compileBudget" (lean := "QuantumBlockEncoding.StoredThinLQ.compileBudget")
*Plain-English reading.* This definition gives the library's named construction or computation for “compile budget”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/StoredThinLQ.lean:174](../../../../library/modules/storedthinlq/#decl-quantumblockencoding-storedthinlq-compilebudget). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredThinLQ.compile_cost_le" (lean := "QuantumBlockEncoding.StoredThinLQ.compile_cost_le")
*Plain-English reading.* Lean checks the proposition indexed as “compile cost le”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredThinLQ.lean:177](../../../../library/modules/storedthinlq/#decl-quantumblockencoding-storedthinlq-compile-cost-le). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StoredThinLQ.compile_total_cost_le" (lean := "QuantumBlockEncoding.StoredThinLQ.compile_total_cost_le")
*Plain-English reading.* Lean checks the proposition indexed as “compile total cost le”; the hypotheses and conclusion in the code panel fix its exact scope. Uniform cubic polynomial, including transpose/extraction and dispatch.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Uniform cubic polynomial, including transpose/extraction and dispatch.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/StoredThinLQ.lean:190](../../../../library/modules/storedthinlq/#decl-quantumblockencoding-storedthinlq-compile-total-cost-le). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/TeachingRouteClosures.lean

35 explicit public declarations, in source order.

:::definition "QuantumBlockEncoding.ExecutableResourceCertificate" (lean := "QuantumBlockEncoding.ExecutableResourceCertificate")
*Plain-English reading.* This record groups the data and proof fields needed for “executable resource certificate”. A proposition-valued field is a requirement until a constructor supplies it. Backend-neutral cost evidence computed from the canonical primitive IR.

*Formal status.* Data contract in the default import surface; proposition-valued fields are obligations, not automatically established facts.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Backend-neutral cost evidence computed from the canonical primitive IR.

*Declaration kind.* structure.

Source: [QuantumBlockEncoding/TeachingRouteClosures.lean:18](../../../../library/modules/teachingrouteclosures/#decl-quantumblockencoding-executableresourcecertificate). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.ExecutableResourceCertificate.resource" (lean := "QuantumBlockEncoding.ExecutableResourceCertificate.resource")
*Plain-English reading.* This definition gives the library's named construction or computation for “resource”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/TeachingRouteClosures.lean:24](../../../../library/modules/teachingrouteclosures/#decl-quantumblockencoding-executableresourcecertificate-resource). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.ExecutableResourceCertificate.cost" (lean := "QuantumBlockEncoding.ExecutableResourceCertificate.cost")
*Plain-English reading.* This definition gives the library's named construction or computation for “cost”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/TeachingRouteClosures.lean:27](../../../../library/modules/teachingrouteclosures/#decl-quantumblockencoding-executableresourcecertificate-cost). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ExecutableResourceCertificate.resource_eq_program_resource" (lean := "QuantumBlockEncoding.ExecutableResourceCertificate.resource_eq_program_resource")
*Plain-English reading.* Lean checks the proposition indexed as “resource eq program resource”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TeachingRouteClosures.lean:33](../../../../library/modules/teachingrouteclosures/#decl-quantumblockencoding-executableresourcecertificate-resource-eq-program-resource). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ExecutableResourceCertificate.cost_auxiliaryQubits" (lean := "QuantumBlockEncoding.ExecutableResourceCertificate.cost_auxiliaryQubits")
*Plain-English reading.* Lean checks the proposition indexed as “cost auxiliary qubits”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TeachingRouteClosures.lean:37](../../../../library/modules/teachingrouteclosures/#decl-quantumblockencoding-executableresourcecertificate-cost-auxiliaryqubits). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ExecutableResourceCertificate.cost_gateCount" (lean := "QuantumBlockEncoding.ExecutableResourceCertificate.cost_gateCount")
*Plain-English reading.* Lean checks the proposition indexed as “cost gate count”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TeachingRouteClosures.lean:41](../../../../library/modules/teachingrouteclosures/#decl-quantumblockencoding-executableresourcecertificate-cost-gatecount). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ExecutableResourceCertificate.cost_depth" (lean := "QuantumBlockEncoding.ExecutableResourceCertificate.cost_depth")
*Plain-English reading.* Lean checks the proposition indexed as “cost depth”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TeachingRouteClosures.lean:45](../../../../library/modules/teachingrouteclosures/#decl-quantumblockencoding-executableresourcecertificate-cost-depth). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ExecutableResourceCertificate.cost_oracleCalls" (lean := "QuantumBlockEncoding.ExecutableResourceCertificate.cost_oracleCalls")
*Plain-English reading.* Lean checks the proposition indexed as “cost oracle calls”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TeachingRouteClosures.lean:49](../../../../library/modules/teachingrouteclosures/#decl-quantumblockencoding-executableresourcecertificate-cost-oraclecalls). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.StatePreparationCandidate.certify" (lean := "QuantumBlockEncoding.StatePreparationCandidate.certify")
*Plain-English reading.* This definition gives the library's named construction or computation for “certify”. Promote a candidate only after all three state-preparation obligations are supplied.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Promote a candidate only after all three state-preparation obligations are supplied.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/TeachingRouteClosures.lean:58](../../../../library/modules/teachingrouteclosures/#decl-quantumblockencoding-statepreparationcandidate-certify). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StatePreparationCandidate.certify_firstColumn" (lean := "QuantumBlockEncoding.StatePreparationCandidate.certify_firstColumn")
*Plain-English reading.* Lean checks the proposition indexed as “certify first column”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TeachingRouteClosures.lean:68](../../../../library/modules/teachingrouteclosures/#decl-quantumblockencoding-statepreparationcandidate-certify-firstcolumn). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StatePreparationCandidate.certify_unitary" (lean := "QuantumBlockEncoding.StatePreparationCandidate.certify_unitary")
*Plain-English reading.* Lean checks the proposition indexed as “certify unitary”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TeachingRouteClosures.lean:75](../../../../library/modules/teachingrouteclosures/#decl-quantumblockencoding-statepreparationcandidate-certify-unitary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.StatePreparationCandidate.certify_normalization" (lean := "QuantumBlockEncoding.StatePreparationCandidate.certify_normalization")
*Plain-English reading.* Lean checks the proposition indexed as “certify normalization”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TeachingRouteClosures.lean:82](../../../../library/modules/teachingrouteclosures/#decl-quantumblockencoding-statepreparationcandidate-certify-normalization). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.ConcreteSemantics.ComplexStatePreparationCertificate.ofFirstColumn" (lean := "QuantumBlockEncoding.ConcreteSemantics.ComplexStatePreparationCertificate.ofFirstColumn")
*Plain-English reading.* This definition gives the library's named construction or computation for “of first column”. Build concrete state-preparation evidence from the equivalent first-column statement.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Build concrete state-preparation evidence from the equivalent first-column statement.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/TeachingRouteClosures.lean:94](../../../../library/modules/teachingrouteclosures/#decl-quantumblockencoding-concretesemantics-complexstatepreparationcertificate-offirstcolumn). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.ConcreteSemantics.ComplexStatePreparationCertificate.verifiedOfFirstColumn" (lean := "QuantumBlockEncoding.ConcreteSemantics.ComplexStatePreparationCertificate.verifiedOfFirstColumn")
*Plain-English reading.* This definition gives the library's named construction or computation for “verified of first column”. Promote first-column evidence through the concrete finite-matrix backend.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Promote first-column evidence through the concrete finite-matrix backend.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/TeachingRouteClosures.lean:107](../../../../library/modules/teachingrouteclosures/#decl-quantumblockencoding-concretesemantics-complexstatepreparationcertificate-verifiedoffirstcolumn). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ConcreteSemantics.ComplexStatePreparationCertificate.verifiedOfFirstColumn_preparesTarget" (lean := "QuantumBlockEncoding.ConcreteSemantics.ComplexStatePreparationCertificate.verifiedOfFirstColumn_preparesTarget")
*Plain-English reading.* Lean checks the proposition indexed as “verified of first column prepares target”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TeachingRouteClosures.lean:116](../../../../library/modules/teachingrouteclosures/#decl-quantumblockencoding-concretesemantics-complexstatepreparationcertificate-verifiedoffirstcolumn-preparestarget). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.textbookPauliXVerifiedOfFirstColumn" (lean := "QuantumBlockEncoding.textbookPauliXVerifiedOfFirstColumn")
*Plain-English reading.* This definition gives the library's named construction or computation for “textbook pauli x verified of first column”. Finite witness that the generic first-column route reuses the Pauli-X proof.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Finite witness that the generic first-column route reuses the Pauli-X proof.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/TeachingRouteClosures.lean:131](../../../../library/modules/teachingrouteclosures/#decl-quantumblockencoding-textbookpaulixverifiedoffirstcolumn). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.textbookPauliXVerifiedOfFirstColumn_preparesTarget" (lean := "QuantumBlockEncoding.textbookPauliXVerifiedOfFirstColumn_preparesTarget")
*Plain-English reading.* Lean checks the proposition indexed as “textbook pauli x verified of first column prepares target”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TeachingRouteClosures.lean:141](../../../../library/modules/teachingrouteclosures/#decl-quantumblockencoding-textbookpaulixverifiedoffirstcolumn-preparestarget). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.ConcreteSemantics.exactCleanBlockOfSignalProjection" (lean := "QuantumBlockEncoding.ConcreteSemantics.exactCleanBlockOfSignalProjection")
*Plain-English reading.* This definition gives the library's named construction or computation for “exact clean block of signal projection”. Convert a signal-register projection proof into an exact clean-block certificate.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Convert a signal-register projection proof into an exact clean-block certificate.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/TeachingRouteClosures.lean:148](../../../../library/modules/teachingrouteclosures/#decl-quantumblockencoding-concretesemantics-exactcleanblockofsignalprojection). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ConcreteSemantics.exactCleanBlockOfSignalProjection_correct" (lean := "QuantumBlockEncoding.ConcreteSemantics.exactCleanBlockOfSignalProjection_correct")
*Plain-English reading.* Lean checks the proposition indexed as “exact clean block of signal projection correct”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TeachingRouteClosures.lean:166](../../../../library/modules/teachingrouteclosures/#decl-quantumblockencoding-concretesemantics-exactcleanblockofsignalprojection-correct). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.CertifiedCircuitBlockExtraction" (lean := "QuantumBlockEncoding.CertifiedCircuitBlockExtraction")
*Plain-English reading.* This record groups the data and proof fields needed for “certified circuit block extraction”. A proposition-valued field is a requirement until a constructor supplies it. A circuit block extraction whose selected block equality is carried as a proof.

*Formal status.* Data contract in the default import surface; proposition-valued fields are obligations, not automatically established facts.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* A circuit block extraction whose selected block equality is carried as a proof.

*Declaration kind.* structure.

Source: [QuantumBlockEncoding/TeachingRouteClosures.lean:183](../../../../library/modules/teachingrouteclosures/#decl-quantumblockencoding-certifiedcircuitblockextraction). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.CertifiedCircuitBlockExtraction.extractionTarget" (lean := "QuantumBlockEncoding.CertifiedCircuitBlockExtraction.extractionTarget")
*Plain-English reading.* This definition gives the library's named construction or computation for “extraction target”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/TeachingRouteClosures.lean:198](../../../../library/modules/teachingrouteclosures/#decl-quantumblockencoding-certifiedcircuitblockextraction-extractiontarget). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.CertifiedCircuitBlockExtraction.normalizedTarget" (lean := "QuantumBlockEncoding.CertifiedCircuitBlockExtraction.normalizedTarget")
*Plain-English reading.* This definition gives the library's named construction or computation for “normalized target”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/TeachingRouteClosures.lean:218](../../../../library/modules/teachingrouteclosures/#decl-quantumblockencoding-certifiedcircuitblockextraction-normalizedtarget). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.CertifiedCircuitBlockExtraction.exactCleanBlock" (lean := "QuantumBlockEncoding.CertifiedCircuitBlockExtraction.exactCleanBlock")
*Plain-English reading.* This definition gives the library's named construction or computation for “exact clean block”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/TeachingRouteClosures.lean:222](../../../../library/modules/teachingrouteclosures/#decl-quantumblockencoding-certifiedcircuitblockextraction-exactcleanblock). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.CertifiedCircuitBlockExtraction.extractionTarget_blockProjection_proved" (lean := "QuantumBlockEncoding.CertifiedCircuitBlockExtraction.extractionTarget_blockProjection_proved")
*Plain-English reading.* Lean checks the proposition indexed as “extraction target block projection proved”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TeachingRouteClosures.lean:230](../../../../library/modules/teachingrouteclosures/#decl-quantumblockencoding-certifiedcircuitblockextraction-extractiontarget-blockprojection-proved). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.CertifiedCircuitBlockExtraction.extractionTarget_blockCorrect_proved" (lean := "QuantumBlockEncoding.CertifiedCircuitBlockExtraction.extractionTarget_blockCorrect_proved")
*Plain-English reading.* Lean checks the proposition indexed as “extraction target block correct proved”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TeachingRouteClosures.lean:234](../../../../library/modules/teachingrouteclosures/#decl-quantumblockencoding-certifiedcircuitblockextraction-extractiontarget-blockcorrect-proved). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.CertifiedCircuitBlockExtraction.exactCleanBlock_correct" (lean := "QuantumBlockEncoding.CertifiedCircuitBlockExtraction.exactCleanBlock_correct")
*Plain-English reading.* Lean checks the proposition indexed as “exact clean block correct”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TeachingRouteClosures.lean:238](../../../../library/modules/teachingrouteclosures/#decl-quantumblockencoding-certifiedcircuitblockextraction-exactcleanblock-correct). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.teachingIdentityCircuitSemantics" (lean := "QuantumBlockEncoding.teachingIdentityCircuitSemantics")
*Plain-English reading.* This definition gives the library's named construction or computation for “teaching identity circuit semantics”. Empty-circuit semantics on the one-dimensional zero-qubit space.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Empty-circuit semantics on the one-dimensional zero-qubit space.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/TeachingRouteClosures.lean:246](../../../../library/modules/teachingrouteclosures/#decl-quantumblockencoding-teachingidentitycircuitsemantics). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.teachingIdentityBlockExtraction" (lean := "QuantumBlockEncoding.teachingIdentityBlockExtraction")
*Plain-English reading.* This definition gives the library's named construction or computation for “teaching identity block extraction”. Finite witness for certified circuit block extraction.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Finite witness for certified circuit block extraction.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/TeachingRouteClosures.lean:250](../../../../library/modules/teachingrouteclosures/#decl-quantumblockencoding-teachingidentityblockextraction). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.teachingIdentityBlockExtraction_correct" (lean := "QuantumBlockEncoding.teachingIdentityBlockExtraction_correct")
*Plain-English reading.* Lean checks the proposition indexed as “teaching identity block extraction correct”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TeachingRouteClosures.lean:263](../../../../library/modules/teachingrouteclosures/#decl-quantumblockencoding-teachingidentityblockextraction-correct). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.OperatorBlockEncodingCandidate.certify" (lean := "QuantumBlockEncoding.OperatorBlockEncodingCandidate.certify")
*Plain-English reading.* This definition gives the library's named construction or computation for “certify”. Promote an operator candidate only after its unitary and block proofs are supplied.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Promote an operator candidate only after its unitary and block proofs are supplied.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/TeachingRouteClosures.lean:271](../../../../library/modules/teachingrouteclosures/#decl-quantumblockencoding-operatorblockencodingcandidate-certify). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.OperatorBlockEncodingCandidate.cost_eq_fromLayoutAndResource" (lean := "QuantumBlockEncoding.OperatorBlockEncodingCandidate.cost_eq_fromLayoutAndResource")
*Plain-English reading.* Lean checks the proposition indexed as “cost eq from layout and resource”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TeachingRouteClosures.lean:279](../../../../library/modules/teachingrouteclosures/#decl-quantumblockencoding-operatorblockencodingcandidate-cost-eq-fromlayoutandresource). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.BlockEncodingClassics.QSVTConsumerContract.identity" (lean := "QuantumBlockEncoding.BlockEncodingClassics.QSVTConsumerContract.identity")
*Plain-English reading.* This definition gives the library's named construction or computation for “identity”. Degree-one identity consumer: a proved clean block is returned unchanged.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Degree-one identity consumer: a proved clean block is returned unchanged.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/TeachingRouteClosures.lean:291](../../../../library/modules/teachingrouteclosures/#decl-quantumblockencoding-blockencodingclassics-qsvtconsumercontract-identity). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.BlockEncodingClassics.QSVTConsumerContract.identity_sideConditions" (lean := "QuantumBlockEncoding.BlockEncodingClassics.QSVTConsumerContract.identity_sideConditions")
*Plain-English reading.* Lean checks the proposition indexed as “identity side conditions”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TeachingRouteClosures.lean:300](../../../../library/modules/teachingrouteclosures/#decl-quantumblockencoding-blockencodingclassics-qsvtconsumercontract-identity-sideconditions). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.BlockEncodingClassics.QSVTConsumerContract.identity_output" (lean := "QuantumBlockEncoding.BlockEncodingClassics.QSVTConsumerContract.identity_output")
*Plain-English reading.* Lean checks the proposition indexed as “identity output”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TeachingRouteClosures.lean:304](../../../../library/modules/teachingrouteclosures/#decl-quantumblockencoding-blockencodingclassics-qsvtconsumercontract-identity-output). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.teachingIdentityQSVTConsumer" (lean := "QuantumBlockEncoding.teachingIdentityQSVTConsumer")
*Plain-English reading.* This definition gives the library's named construction or computation for “teaching identity qsvt consumer”. Finite witness that a certified clean block crosses the typed QSVT boundary.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Finite witness that a certified clean block crosses the typed QSVT boundary.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/TeachingRouteClosures.lean:311](../../../../library/modules/teachingrouteclosures/#decl-quantumblockencoding-teachingidentityqsvtconsumer). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/TensorTrainCanonical.lean

33 explicit public declarations, in source order.

:::definition "QuantumBlockEncoding.TensorTrainCanonical.Core" (lean := "QuantumBlockEncoding.TensorTrainCanonical.Core")
*Plain-English reading.* This abbreviation gives a shorter name to the type or expression used for “core”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* abbrev.

Source: [QuantumBlockEncoding/TensorTrainCanonical.lean:18](../../../../library/modules/tensortraincanonical/#decl-quantumblockencoding-tensortraincanonical-core). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.TensorTrainCanonical.slice" (lean := "QuantumBlockEncoding.TensorTrainCanonical.slice")
*Plain-English reading.* This definition gives the library's named construction or computation for “slice”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/TensorTrainCanonical.lean:20](../../../../library/modules/tensortraincanonical/#decl-quantumblockencoding-tensortraincanonical-slice). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.TensorTrainCanonical.Chain" (lean := "QuantumBlockEncoding.TensorTrainCanonical.Chain")
*Plain-English reading.* This type lists the allowed alternatives for “chain”; its constructors are the cases that downstream code must handle. The first core emits the first bit.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The first core emits the first bit. The terminal bond is explicit.

*Declaration kind.* inductive.

Source: [QuantumBlockEncoding/TensorTrainCanonical.lean:24](../../../../library/modules/tensortraincanonical/#decl-quantumblockencoding-tensortraincanonical-chain). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.TensorTrainCanonical.Word" (lean := "QuantumBlockEncoding.TensorTrainCanonical.Word")
*Plain-English reading.* This definition gives the library's named construction or computation for “word”. Bit words indexed recursively in the same order as the cores.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Bit words indexed recursively in the same order as the cores.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/TensorTrainCanonical.lean:29](../../../../library/modules/tensortraincanonical/#decl-quantumblockencoding-tensortraincanonical-word). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.TensorTrainCanonical.contract" (lean := "QuantumBlockEncoding.TensorTrainCanonical.contract")
*Plain-English reading.* This definition gives the library's named construction or computation for “contract”. Matrix of bond-to-bond amplitudes for one fixed emitted word.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Matrix of bond-to-bond amplitudes for one fixed emitted word.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/TensorTrainCanonical.lean:39](../../../../library/modules/tensortraincanonical/#decl-quantumblockencoding-tensortraincanonical-contract). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.TensorTrainCanonical.RightCanonical" (lean := "QuantumBlockEncoding.TensorTrainCanonical.RightCanonical")
*Plain-English reading.* This definition gives the library's named construction or computation for “right canonical”. Right-canonical means orthonormal rows at every individual core.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Right-canonical means orthonormal rows at every individual core.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/TensorTrainCanonical.lean:45](../../../../library/modules/tensortraincanonical/#decl-quantumblockencoding-tensortraincanonical-rightcanonical). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.TensorTrainCanonical.RankReduced" (lean := "QuantumBlockEncoding.TensorTrainCanonical.RankReduced")
*Plain-English reading.* This type lists the allowed alternatives for “rank reduced”; its constructors are the cases that downstream code must handle. The active ranks satisfy the exact backward 'min' recurrence, with an unchanged terminal bond.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The active ranks satisfy the exact backward 'min' recurrence, with an unchanged terminal bond. This is a relation on actual core chains.

*Declaration kind.* inductive.

Source: [QuantumBlockEncoding/TensorTrainCanonical.lean:51](../../../../library/modules/tensortraincanonical/#decl-quantumblockencoding-tensortraincanonical-rankreduced). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.TensorTrainCanonical.absorb" (lean := "QuantumBlockEncoding.TensorTrainCanonical.absorb")
*Plain-English reading.* This definition gives the library's named construction or computation for “absorb”. Multiply a residual into the right bond without mixing the emitted bit.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Multiply a residual into the right bond without mixing the emitted bit.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/TensorTrainCanonical.lean:58](../../../../library/modules/tensortraincanonical/#decl-quantumblockencoding-tensortraincanonical-absorb). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TensorTrainCanonical.absorb_slice" (lean := "QuantumBlockEncoding.TensorTrainCanonical.absorb_slice")
*Plain-English reading.* Lean checks the proposition indexed as “absorb slice”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TensorTrainCanonical.lean:62](../../../../library/modules/tensortraincanonical/#decl-quantumblockencoding-tensortraincanonical-absorb-slice). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TensorTrainCanonical.exists_core_lq" (lean := "QuantumBlockEncoding.TensorTrainCanonical.exists_core_lq")
*Plain-English reading.* Lean checks the proposition indexed as “exists core lq”; the hypotheses and conclusion in the code panel fix its exact scope. Thin LQ with the physical bit/right-bond product index made explicit.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Thin LQ with the physical bit/right-bond product index made explicit.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TensorTrainCanonical.lean:67](../../../../library/modules/tensortraincanonical/#decl-quantumblockencoding-tensortraincanonical-exists-core-lq). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TensorTrainCanonical.exists_rightCanonical" (lean := "QuantumBlockEncoding.TensorTrainCanonical.exists_rightCanonical")
*Plain-English reading.* Lean checks the proposition indexed as “exists right canonical”; the hypotheses and conclusion in the code panel fix its exact scope. Exact all-length right-canonicalization, preserving every amplitude.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Exact all-length right-canonicalization, preserving every amplitude. No full-rank assumption or global state-action hypothesis is used.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TensorTrainCanonical.lean:84](../../../../library/modules/tensortraincanonical/#decl-quantumblockencoding-tensortraincanonical-exists-rightcanonical). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TensorTrainCanonical.RankReduced.head_bound" (lean := "QuantumBlockEncoding.TensorTrainCanonical.RankReduced.head_bound")
*Plain-English reading.* Lean checks the proposition indexed as “head bound”; the hypotheses and conclusion in the code panel fix its exact scope. Every nonempty canonicalized train has a left rank bounded by the original left rank and by twice its next active rank.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Every nonempty canonicalized train has a left rank bounded by the original left rank and by twice its next active rank.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TensorTrainCanonical.lean:104](../../../../library/modules/tensortraincanonical/#decl-quantumblockencoding-tensortraincanonical-rankreduced-head-bound). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TensorTrainCanonical.RankReduced.last_bond_le_two" (lean := "QuantumBlockEncoding.TensorTrainCanonical.RankReduced.last_bond_le_two")
*Plain-English reading.* Lean checks the proposition indexed as “last bond le two”; the hypotheses and conclusion in the code panel fix its exact scope. In particular, the penultimate active bond has dimension at most two.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* In particular, the penultimate active bond has dimension at most two.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TensorTrainCanonical.lean:113](../../../../library/modules/tensortraincanonical/#decl-quantumblockencoding-tensortraincanonical-rankreduced-last-bond-le-two). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.TensorTrainCanonical.mass" (lean := "QuantumBlockEncoding.TensorTrainCanonical.mass")
*Plain-English reading.* This definition gives the library's named construction or computation for “mass”. Squared Euclidean mass for an arbitrary finite real boundary.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Squared Euclidean mass for an arbitrary finite real boundary.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/TensorTrainCanonical.lean:120](../../../../library/modules/tensortraincanonical/#decl-quantumblockencoding-tensortraincanonical-mass). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TensorTrainCanonical.mass_vecMul" (lean := "QuantumBlockEncoding.TensorTrainCanonical.mass_vecMul")
*Plain-English reading.* Lean checks the proposition indexed as “mass vec mul”; the hypotheses and conclusion in the code panel fix its exact scope. An orthonormal-row matrix acts isometrically on row-vector boundaries.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* An orthonormal-row matrix acts isometrically on row-vector boundaries.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TensorTrainCanonical.lean:123](../../../../library/modules/tensortraincanonical/#decl-quantumblockencoding-tensortraincanonical-mass-vecmul). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.TensorTrainCanonical.chainMass" (lean := "QuantumBlockEncoding.TensorTrainCanonical.chainMass")
*Plain-English reading.* This definition gives the library's named construction or computation for “chain mass”. Total mass of all emitted amplitudes, including the terminal bond.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Total mass of all emitted amplitudes, including the terminal bond.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/TensorTrainCanonical.lean:138](../../../../library/modules/tensortraincanonical/#decl-quantumblockencoding-tensortraincanonical-chainmass). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TensorTrainCanonical.chainMass_eq" (lean := "QuantumBlockEncoding.TensorTrainCanonical.chainMass_eq")
*Plain-English reading.* Lean checks the proposition indexed as “chain mass eq”; the hypotheses and conclusion in the code panel fix its exact scope. Local row-isometries compose to an all-length mass-preserving state map.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Local row-isometries compose to an all-length mass-preserving state map.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TensorTrainCanonical.lean:142](../../../../library/modules/tensortraincanonical/#decl-quantumblockencoding-tensortraincanonical-chainmass-eq). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TensorTrainCanonical.residual_mass" (lean := "QuantumBlockEncoding.TensorTrainCanonical.residual_mass")
*Plain-English reading.* Lean checks the proposition indexed as “residual mass”; the hypotheses and conclusion in the code panel fix its exact scope. Factorization preserves total mass, and canonicality identifies it with the mass of the new initial boundary.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Factorization preserves total mass, and canonicality identifies it with the mass of the new initial boundary.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TensorTrainCanonical.lean:161](../../../../library/modules/tensortraincanonical/#decl-quantumblockencoding-tensortraincanonical-residual-mass). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TensorTrainCanonical.exists_rightCanonical_normalized" (lean := "QuantumBlockEncoding.TensorTrainCanonical.exists_rightCanonical_normalized")
*Plain-English reading.* Lean checks the proposition indexed as “exists right canonical normalized”; the hypotheses and conclusion in the code panel fix its exact scope. A normalized input train has a normalized residual initial boundary.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* A normalized input train has a normalized residual initial boundary. The terminal bond remains exactly dimension one.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TensorTrainCanonical.lean:172](../../../../library/modules/tensortraincanonical/#decl-quantumblockencoding-tensortraincanonical-exists-rightcanonical-normalized). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TensorTrainCanonical.exists_normalized_state" (lean := "QuantumBlockEncoding.TensorTrainCanonical.exists_normalized_state")
*Plain-English reading.* Lean checks the proposition indexed as “exists normalized state”; the hypotheses and conclusion in the code panel fix its exact scope. Scalar-boundary state version: the new initial vector is normalized and every individual target amplitude is recovered by contracting it with the right-canonical train.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Scalar-boundary state version: the new initial vector is normalized and every individual target amplitude is recovered by contracting it with the right-canonical train. No target-state equality is a hypothesis.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TensorTrainCanonical.lean:183](../../../../library/modules/tensortraincanonical/#decl-quantumblockencoding-tensortraincanonical-exists-normalized-state). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.TensorTrainCanonical.maxBond" (lean := "QuantumBlockEncoding.TensorTrainCanonical.maxBond")
*Plain-English reading.* This definition gives the library's named construction or computation for “max bond”. Largest actual bond in a chain, including both boundaries.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Largest actual bond in a chain, including both boundaries.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/TensorTrainCanonical.lean:198](../../../../library/modules/tensortraincanonical/#decl-quantumblockencoding-tensortraincanonical-maxbond). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TensorTrainCanonical.RankReduced.maxBond_le" (lean := "QuantumBlockEncoding.TensorTrainCanonical.RankReduced.maxBond_le")
*Plain-English reading.* Lean checks the proposition indexed as “max bond le”; the hypotheses and conclusion in the code panel fix its exact scope. Backward canonicalization never enlarges any maximal bond dimension.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Backward canonicalization never enlarges any maximal bond dimension.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TensorTrainCanonical.lean:203](../../../../library/modules/tensortraincanonical/#decl-quantumblockencoding-tensortraincanonical-rankreduced-maxbond-le). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.TensorTrainCanonical.complexCore" (lean := "QuantumBlockEncoding.TensorTrainCanonical.complexCore")
*Plain-English reading.* This definition gives the library's named construction or computation for “complex core”. In circuit convention the emitted bit/new bond are output rows, and the old bond is the input column.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* In circuit convention the emitted bit/new bond are output rows, and the old bond is the input column. Real amplitudes embed into complex ones.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/TensorTrainCanonical.lean:211](../../../../library/modules/tensortraincanonical/#decl-quantumblockencoding-tensortraincanonical-complexcore). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TensorTrainCanonical.complexCore_isometry" (lean := "QuantumBlockEncoding.TensorTrainCanonical.complexCore_isometry")
*Plain-English reading.* Lean checks the proposition indexed as “complex core isometry”; the hypotheses and conclusion in the code panel fix its exact scope. Right-canonical rows are exactly orthonormal circuit input columns.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Right-canonical rows are exactly orthonormal circuit input columns.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TensorTrainCanonical.lean:215](../../../../library/modules/tensortraincanonical/#decl-quantumblockencoding-tensortraincanonical-complexcore-isometry). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.TensorTrainCanonical.sequentialCore" (lean := "QuantumBlockEncoding.TensorTrainCanonical.sequentialCore")
*Plain-English reading.* This definition gives the library's named construction or computation for “sequential core”. Equal-rank specialization lands literally in the existing sequential preparation core type; padding varying ranks is a separate register embedding.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Equal-rank specialization lands literally in the existing sequential preparation core type; padding varying ranks is a separate register embedding.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/TensorTrainCanonical.lean:230](../../../../library/modules/tensortraincanonical/#decl-quantumblockencoding-tensortraincanonical-sequentialcore). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TensorTrainCanonical.sequential_step" (lean := "QuantumBlockEncoding.TensorTrainCanonical.sequential_step")
*Plain-English reading.* Lean checks the proposition indexed as “sequential step”; the hypotheses and conclusion in the code panel fix its exact scope. Exact clean-column semantic adapter, in output-row/input-column order.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Exact clean-column semantic adapter, in output-row/input-column order. This premise concerns one local stage, not the target state or full run.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TensorTrainCanonical.lean:235](../../../../library/modules/tensortraincanonical/#decl-quantumblockencoding-tensortraincanonical-sequential-step). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.TensorTrainCanonical.padVector" (lean := "QuantumBlockEncoding.TensorTrainCanonical.padVector")
*Plain-English reading.* This definition gives the library's named construction or computation for “pad vector”. Embed a varying active bond into a fixed physical register by zero fill.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Embed a varying active bond into a fixed physical register by zero fill.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/TensorTrainCanonical.lean:246](../../../../library/modules/tensortraincanonical/#decl-quantumblockencoding-tensortraincanonical-padvector). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TensorTrainCanonical.padVector_active" (lean := "QuantumBlockEncoding.TensorTrainCanonical.padVector_active")
*Plain-English reading.* Lean checks the proposition indexed as “pad vector active”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TensorTrainCanonical.lean:249](../../../../library/modules/tensortraincanonical/#decl-quantumblockencoding-tensortraincanonical-padvector-active). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.TensorTrainCanonical.paddedCore" (lean := "QuantumBlockEncoding.TensorTrainCanonical.paddedCore")
*Plain-English reading.* This definition gives the library's named construction or computation for “padded core”. Padded matrix has zero output outside the next active rank and specifies only the active clean-input columns; other completion columns stay free.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Padded matrix has zero output outside the next active rank and specifies only the active clean-input columns; other completion columns stay free.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/TensorTrainCanonical.lean:255](../../../../library/modules/tensortraincanonical/#decl-quantumblockencoding-tensortraincanonical-paddedcore). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TensorTrainCanonical.paddedCore_inactive_output" (lean := "QuantumBlockEncoding.TensorTrainCanonical.paddedCore_inactive_output")
*Plain-English reading.* Lean checks the proposition indexed as “padded core inactive output”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TensorTrainCanonical.lean:260](../../../../library/modules/tensortraincanonical/#decl-quantumblockencoding-tensortraincanonical-paddedcore-inactive-output). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TensorTrainCanonical.sum_padVector" (lean := "QuantumBlockEncoding.TensorTrainCanonical.sum_padVector")
*Plain-English reading.* Lean checks the proposition indexed as “sum pad vector”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TensorTrainCanonical.lean:264](../../../../library/modules/tensortraincanonical/#decl-quantumblockencoding-tensortraincanonical-sum-padvector). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TensorTrainCanonical.sequential_step_padded" (lean := "QuantumBlockEncoding.TensorTrainCanonical.sequential_step_padded")
*Plain-English reading.* Lean checks the proposition indexed as “sequential step padded”; the hypotheses and conclusion in the code panel fix its exact scope. Rank-changing local action in the existing sequential semantics.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Rank-changing local action in the existing sequential semantics. The only circuit premise is equality on active clean-input columns. The output is the zero-padded exact core contraction, including every inactive label.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TensorTrainCanonical.lean:275](../../../../library/modules/tensortraincanonical/#decl-quantumblockencoding-tensortraincanonical-sequential-step-padded). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TensorTrainCanonical.paddedCore_active_isometry" (lean := "QuantumBlockEncoding.TensorTrainCanonical.paddedCore_active_isometry")
*Plain-English reading.* Lean checks the proposition indexed as “padded core active isometry”; the hypotheses and conclusion in the code panel fix its exact scope. Active columns of a right-canonical core remain orthonormal after embedding the output into a larger physical register.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Active columns of a right-canonical core remain orthonormal after embedding the output into a larger physical register.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TensorTrainCanonical.lean:295](../../../../library/modules/tensortraincanonical/#decl-quantumblockencoding-tensortraincanonical-paddedcore-active-isometry). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/TensorTrainLocalCompiler.lean

10 explicit public declarations, in source order.

:::theorem "QuantumBlockEncoding.TensorTrainLocalCompiler.exists_SO_named" (lean := "QuantumBlockEncoding.TensorTrainLocalCompiler.exists_SO_named")
*Plain-English reading.* Lean checks the proposition indexed as “exists so named”; the hypotheses and conclusion in the code panel fix its exact scope. Coordinate adapter for special-orthogonal completion on any finite named basis, with prescribed columns at arbitrary physical labels.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Coordinate adapter for special-orthogonal completion on any finite named basis, with prescribed columns at arbitrary physical labels.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TensorTrainLocalCompiler.lean:12](../../../../library/modules/tensortrainlocalcompiler/#decl-quantumblockencoding-tensortrainlocalcompiler-exists-so-named). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.TensorTrainLocalCompiler.localIndex" (lean := "QuantumBlockEncoding.TensorTrainLocalCompiler.localIndex")
*Plain-English reading.* This definition gives the library's named construction or computation for “local index”. The bit is the highest local physical wire; the bond uses low wires.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The bit is the highest local physical wire; the bond uses low wires.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/TensorTrainLocalCompiler.lean:36](../../../../library/modules/tensortrainlocalcompiler/#decl-quantumblockencoding-tensortrainlocalcompiler-localindex). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TensorTrainLocalCompiler.localIndex_snoc" (lean := "QuantumBlockEncoding.TensorTrainLocalCompiler.localIndex_snoc")
*Plain-English reading.* Lean checks the proposition indexed as “local index snoc”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TensorTrainLocalCompiler.lean:39](../../../../library/modules/tensortrainlocalcompiler/#decl-quantumblockencoding-tensortrainlocalcompiler-localindex-snoc). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TensorTrainLocalCompiler.paddedAt_real" (lean := "QuantumBlockEncoding.TensorTrainLocalCompiler.paddedAt_real")
*Plain-English reading.* Lean checks the proposition indexed as “padded at real”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TensorTrainLocalCompiler.lean:44](../../../../library/modules/tensortrainlocalcompiler/#decl-quantumblockencoding-tensortrainlocalcompiler-paddedat-real). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TensorTrainLocalCompiler.paddedAt_im_zero" (lean := "QuantumBlockEncoding.TensorTrainLocalCompiler.paddedAt_im_zero")
*Plain-English reading.* Lean checks the proposition indexed as “padded at im zero”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TensorTrainLocalCompiler.lean:56](../../../../library/modules/tensortrainlocalcompiler/#decl-quantumblockencoding-tensortrainlocalcompiler-paddedat-im-zero). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.TensorTrainLocalCompiler.activePositions" (lean := "QuantumBlockEncoding.TensorTrainLocalCompiler.activePositions")
*Plain-English reading.* This definition gives the library's named construction or computation for “active positions”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/TensorTrainLocalCompiler.lean:61](../../../../library/modules/tensortrainlocalcompiler/#decl-quantumblockencoding-tensortrainlocalcompiler-activepositions). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.TensorTrainLocalCompiler.activeColumns" (lean := "QuantumBlockEncoding.TensorTrainLocalCompiler.activeColumns")
*Plain-English reading.* This definition gives the library's named construction or computation for “active columns”. The occupied real columns of a padded core, in physical named-wire order.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The occupied real columns of a padded core, in physical named-wire order.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/TensorTrainLocalCompiler.lean:69](../../../../library/modules/tensortrainlocalcompiler/#decl-quantumblockencoding-tensortrainlocalcompiler-activecolumns). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TensorTrainLocalCompiler.activeColumns_isometry" (lean := "QuantumBlockEncoding.TensorTrainLocalCompiler.activeColumns_isometry")
*Plain-English reading.* Lean checks the proposition indexed as “active columns isometry”; the hypotheses and conclusion in the code panel fix its exact scope. Exact real orthonormality is extracted from the proved complex padded-core semantics; no ambient matrix or desired circuit action is assumed.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Exact real orthonormality is extracted from the proved complex padded-core semantics; no ambient matrix or desired circuit action is assumed.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TensorTrainLocalCompiler.lean:77](../../../../library/modules/tensortrainlocalcompiler/#decl-quantumblockencoding-tensortrainlocalcompiler-activecolumns-isometry). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TensorTrainLocalCompiler.exists_local_circuit_with_resources" (lean := "QuantumBlockEncoding.TensorTrainLocalCompiler.exists_local_circuit_with_resources")
*Plain-English reading.* Lean checks the proposition indexed as “exists local circuit with resources”; the hypotheses and conclusion in the code panel fix its exact scope. Every actual canonical stage has an exact primitive implementation on the 'q' low bond wires and one highest output wire.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Every actual canonical stage has an exact primitive implementation on the 'q' low bond wires and one highest output wire. Completion has a spare column because the active rank is at most half the physical dimension. The existence proof uses exact real choices and is not a classical preprocessing-cost bound.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TensorTrainLocalCompiler.lean:99](../../../../library/modules/tensortrainlocalcompiler/#decl-quantumblockencoding-tensortrainlocalcompiler-exists-local-circuit-with-resources). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TensorTrainLocalCompiler.exists_local_circuit" (lean := "QuantumBlockEncoding.TensorTrainLocalCompiler.exists_local_circuit")
*Plain-English reading.* Lean checks the proposition indexed as “exists local circuit”; the hypotheses and conclusion in the code panel fix its exact scope. Minimal local-column interface for sequential tensor-train assembly.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Minimal local-column interface for sequential tensor-train assembly.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TensorTrainLocalCompiler.lean:136](../../../../library/modules/tensortrainlocalcompiler/#decl-quantumblockencoding-tensortrainlocalcompiler-exists-local-circuit). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/TensorTrainNormEnvironment.lean

15 explicit public declarations, in source order.

:::definition "QuantumBlockEncoding.TensorTrainNormEnvironment.gram" (lean := "QuantumBlockEncoding.TensorTrainNormEnvironment.gram")
*Plain-English reading.* This definition gives the library's named construction or computation for “gram”. Right Gram environment.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Right Gram environment. Each update uses only two local matrix products per physical bit, with the previous environment as its right factor.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/TensorTrainNormEnvironment.lean:19](../../../../library/modules/tensortrainnormenvironment/#decl-quantumblockencoding-tensortrainnormenvironment-gram). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TensorTrainNormEnvironment.gram_eq_sum" (lean := "QuantumBlockEncoding.TensorTrainNormEnvironment.gram_eq_sum")
*Plain-English reading.* Lean checks the proposition indexed as “gram eq sum”; the hypotheses and conclusion in the code panel fix its exact scope. Exact semantics of the small-matrix recursion, including every terminal bond label.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Exact semantics of the small-matrix recursion, including every terminal bond label. The full word sum appears only in the specification.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TensorTrainNormEnvironment.lean:27](../../../../library/modules/tensortrainnormenvironment/#decl-quantumblockencoding-tensortrainnormenvironment-gram-eq-sum). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TensorTrainNormEnvironment.gram_scalar" (lean := "QuantumBlockEncoding.TensorTrainNormEnvironment.gram_scalar")
*Plain-English reading.* Lean checks the proposition indexed as “gram scalar”; the hypotheses and conclusion in the code panel fix its exact scope. A scalar-boundary train's environment entry is its complete squared norm.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* A scalar-boundary train's environment entry is its complete squared norm.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TensorTrainNormEnvironment.lean:40](../../../../library/modules/tensortrainnormenvironment/#decl-quantumblockencoding-tensortrainnormenvironment-gram-scalar). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TensorTrainNormEnvironment.gram_scalar_nonneg" (lean := "QuantumBlockEncoding.TensorTrainNormEnvironment.gram_scalar_nonneg")
*Plain-English reading.* Lean checks the proposition indexed as “gram scalar nonneg”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TensorTrainNormEnvironment.lean:45](../../../../library/modules/tensortrainnormenvironment/#decl-quantumblockencoding-tensortrainnormenvironment-gram-scalar-nonneg). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.TensorTrainNormEnvironment.norm" (lean := "QuantumBlockEncoding.TensorTrainNormEnvironment.norm")
*Plain-English reading.* This definition gives the library's named construction or computation for “norm”. Local-core norm supplier; only one square root is performed after the Gram recursion.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Local-core norm supplier; only one square root is performed after the Gram recursion. This is meaningful for zero trains as well as normalized trains.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/TensorTrainNormEnvironment.lean:51](../../../../library/modules/tensortrainnormenvironment/#decl-quantumblockencoding-tensortrainnormenvironment-norm). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TensorTrainNormEnvironment.norm_eq" (lean := "QuantumBlockEncoding.TensorTrainNormEnvironment.norm_eq")
*Plain-English reading.* Lean checks the proposition indexed as “norm eq”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TensorTrainNormEnvironment.lean:53](../../../../library/modules/tensortrainnormenvironment/#decl-quantumblockencoding-tensortrainnormenvironment-norm-eq). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TensorTrainNormEnvironment.norm_sq" (lean := "QuantumBlockEncoding.TensorTrainNormEnvironment.norm_sq")
*Plain-English reading.* Lean checks the proposition indexed as “norm sq”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TensorTrainNormEnvironment.lean:57](../../../../library/modules/tensortrainnormenvironment/#decl-quantumblockencoding-tensortrainnormenvironment-norm-sq). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TensorTrainNormEnvironment.norm_pos_of_nonzero" (lean := "QuantumBlockEncoding.TensorTrainNormEnvironment.norm_pos_of_nonzero")
*Plain-English reading.* Lean checks the proposition indexed as “norm pos of nonzero”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TensorTrainNormEnvironment.lean:61](../../../../library/modules/tensortrainnormenvironment/#decl-quantumblockencoding-tensortrainnormenvironment-norm-pos-of-nonzero). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TensorTrainNormEnvironment.norm_eq_of_contract" (lean := "QuantumBlockEncoding.TensorTrainNormEnvironment.norm_eq_of_contract")
*Plain-English reading.* Lean checks the proposition indexed as “norm eq of contract”; the hypotheses and conclusion in the code panel fix its exact scope. A reusable target adapter.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* A reusable target adapter. This identifies a proven core action with the target norm; it does not assume the norm supplier is already correct.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TensorTrainNormEnvironment.lean:69](../../../../library/modules/tensortrainnormenvironment/#decl-quantumblockencoding-tensortrainnormenvironment-norm-eq-of-contract). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.TensorTrainNormEnvironment.environmentScalars" (lean := "QuantumBlockEncoding.TensorTrainNormEnvironment.environmentScalars")
*Plain-English reading.* This definition gives the library's named construction or computation for “environment scalars”. Storage if every intermediate environment is retained.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Storage if every intermediate environment is retained. Streaming can use less; no exponential word address occurs in this definition.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/TensorTrainNormEnvironment.lean:79](../../../../library/modules/tensortrainnormenvironment/#decl-quantumblockencoding-tensortrainnormenvironment-environmentscalars). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TensorTrainNormEnvironment.environmentScalars_le" (lean := "QuantumBlockEncoding.TensorTrainNormEnvironment.environmentScalars_le")
*Plain-English reading.* Lean checks the proposition indexed as “environment scalars le”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TensorTrainNormEnvironment.lean:83](../../../../library/modules/tensortrainnormenvironment/#decl-quantumblockencoding-tensortrainnormenvironment-environmentscalars-le). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.TensorTrainNormEnvironment.updateArithmeticBudget" (lean := "QuantumBlockEncoding.TensorTrainNormEnvironment.updateArithmeticBudget")
*Plain-English reading.* This definition gives the library's named construction or computation for “update arithmetic budget”. A conservative count for direct dense real arithmetic at one update: two bits, products (l by m)\*(m by m) and (l by m)\*(m by l), charging one multiplication and at most one addition per inner-product term, then l^2 additions to combine the two bits.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* A conservative count for direct dense real arithmetic at one update: two bits, products (l by m)\*(m by m) and (l by m)\*(m by l), charging one multiplication and at most one addition per inner-product term, then l^2 additions to combine the two bits. Copies/transposes are index views.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/TensorTrainNormEnvironment.lean:101](../../../../library/modules/tensortrainnormenvironment/#decl-quantumblockencoding-tensortrainnormenvironment-updatearithmeticbudget). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.TensorTrainNormEnvironment.arithmeticBudget" (lean := "QuantumBlockEncoding.TensorTrainNormEnvironment.arithmeticBudget")
*Plain-English reading.* This definition gives the library's named construction or computation for “arithmetic budget”. Syntactic real-operation budget of the stated local evaluation schedule.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Syntactic real-operation budget of the stated local evaluation schedule. This is not a cost semantics for an external runtime or finite-bit arithmetic.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/TensorTrainNormEnvironment.lean:105](../../../../library/modules/tensortrainnormenvironment/#decl-quantumblockencoding-tensortrainnormenvironment-arithmeticbudget). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TensorTrainNormEnvironment.updateArithmeticBudget_le" (lean := "QuantumBlockEncoding.TensorTrainNormEnvironment.updateArithmeticBudget_le")
*Plain-English reading.* Lean checks the proposition indexed as “update arithmetic budget le”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TensorTrainNormEnvironment.lean:109](../../../../library/modules/tensortrainnormenvironment/#decl-quantumblockencoding-tensortrainnormenvironment-updatearithmeticbudget-le). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TensorTrainNormEnvironment.arithmeticBudget_le" (lean := "QuantumBlockEncoding.TensorTrainNormEnvironment.arithmeticBudget_le")
*Plain-English reading.* Lean checks the proposition indexed as “arithmetic budget le”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TensorTrainNormEnvironment.lean:124](../../../../library/modules/tensortrainnormenvironment/#decl-quantumblockencoding-tensortrainnormenvironment-arithmeticbudget-le). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/TensorTrainPrimitivePreparation.lean

15 explicit public declarations, in source order.

:::definition "QuantumBlockEncoding.TensorTrainPrimitivePreparation.boundaryRow" (lean := "QuantumBlockEncoding.TensorTrainPrimitivePreparation.boundaryRow")
*Plain-English reading.* This definition gives the library's named construction or computation for “boundary row”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/TensorTrainPrimitivePreparation.lean:22](../../../../library/modules/tensortrainprimitivepreparation/#decl-quantumblockencoding-tensortrainprimitivepreparation-boundaryrow). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TensorTrainPrimitivePreparation.boundaryRow_isometry" (lean := "QuantumBlockEncoding.TensorTrainPrimitivePreparation.boundaryRow_isometry")
*Plain-English reading.* Lean checks the proposition indexed as “boundary row isometry”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TensorTrainPrimitivePreparation.lean:24](../../../../library/modules/tensortrainprimitivepreparation/#decl-quantumblockencoding-tensortrainprimitivepreparation-boundaryrow-isometry). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TensorTrainPrimitivePreparation.boundaryCore_isometry" (lean := "QuantumBlockEncoding.TensorTrainPrimitivePreparation.boundaryCore_isometry")
*Plain-English reading.* Lean checks the proposition indexed as “boundary core isometry”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TensorTrainPrimitivePreparation.lean:32](../../../../library/modules/tensortrainprimitivepreparation/#decl-quantumblockencoding-tensortrainprimitivepreparation-boundarycore-isometry). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TensorTrainPrimitivePreparation.boundaryCore_contract" (lean := "QuantumBlockEncoding.TensorTrainPrimitivePreparation.boundaryCore_contract")
*Plain-English reading.* Lean checks the proposition indexed as “boundary core contract”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TensorTrainPrimitivePreparation.lean:41](../../../../library/modules/tensortrainprimitivepreparation/#decl-quantumblockencoding-tensortrainprimitivepreparation-boundarycore-contract). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TensorTrainPrimitivePreparation.exists_unitBoundary_canonical" (lean := "QuantumBlockEncoding.TensorTrainPrimitivePreparation.exists_unitBoundary_canonical")
*Plain-English reading.* Lean checks the proposition indexed as “exists unit boundary canonical”; the hypotheses and conclusion in the code panel fix its exact scope. Eliminate the signed initial residual by absorbing it into the first row-isometric core.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Eliminate the signed initial residual by absorbing it into the first row-isometric core. This is not a free state-initialization assumption.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TensorTrainPrimitivePreparation.lean:51](../../../../library/modules/tensortrainprimitivepreparation/#decl-quantumblockencoding-tensortrainprimitivepreparation-exists-unitboundary-canonical). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.TensorTrainPrimitivePreparation.transportStage" (lean := "QuantumBlockEncoding.TensorTrainPrimitivePreparation.transportStage")
*Plain-English reading.* This definition gives the library's named construction or computation for “transport stage”. Reindex only the finite bond labels of a local stage.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Reindex only the finite bond labels of a local stage.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/TensorTrainPrimitivePreparation.lean:70](../../../../library/modules/tensortrainprimitivepreparation/#decl-quantumblockencoding-tensortrainprimitivepreparation-transportstage). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TensorTrainPrimitivePreparation.run_transport" (lean := "QuantumBlockEncoding.TensorTrainPrimitivePreparation.run_transport")
*Plain-English reading.* Lean checks the proposition indexed as “run transport”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TensorTrainPrimitivePreparation.lean:73](../../../../library/modules/tensortrainprimitivepreparation/#decl-quantumblockencoding-tensortrainprimitivepreparation-run-transport). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TensorTrainPrimitivePreparation.bondIndex_zero" (lean := "QuantumBlockEncoding.TensorTrainPrimitivePreparation.bondIndex_zero")
*Plain-English reading.* Lean checks the proposition indexed as “bond index zero”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TensorTrainPrimitivePreparation.lean:87](../../../../library/modules/tensortrainprimitivepreparation/#decl-quantumblockencoding-tensortrainprimitivepreparation-bondindex-zero). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TensorTrainPrimitivePreparation.bondIndex_zero_iff" (lean := "QuantumBlockEncoding.TensorTrainPrimitivePreparation.bondIndex_zero_iff")
*Plain-English reading.* Lean checks the proposition indexed as “bond index zero iff”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TensorTrainPrimitivePreparation.lean:94](../../../../library/modules/tensortrainprimitivepreparation/#decl-quantumblockencoding-tensortrainprimitivepreparation-bondindex-zero-iff). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TensorTrainPrimitivePreparation.initial_padding" (lean := "QuantumBlockEncoding.TensorTrainPrimitivePreparation.initial_padding")
*Plain-English reading.* Lean checks the proposition indexed as “initial padding”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TensorTrainPrimitivePreparation.lean:104](../../../../library/modules/tensortrainprimitivepreparation/#decl-quantumblockencoding-tensortrainprimitivepreparation-initial-padding). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TensorTrainPrimitivePreparation.run_circuits_clean" (lean := "QuantumBlockEncoding.TensorTrainPrimitivePreparation.run_circuits_clean")
*Plain-English reading.* Lean checks the proposition indexed as “run circuits clean”; the hypotheses and conclusion in the code panel fix its exact scope. Actual local circuit columns imply the complete sequential source state from an empty initial circuit, including terminal cleanup.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Actual local circuit columns imply the complete sequential source state from an empty initial circuit, including terminal cleanup.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TensorTrainPrimitivePreparation.lean:117](../../../../library/modules/tensortrainprimitivepreparation/#decl-quantumblockencoding-tensortrainprimitivepreparation-run-circuits-clean). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TensorTrainPrimitivePreparation.publicCircuit_clean" (lean := "QuantumBlockEncoding.TensorTrainPrimitivePreparation.publicCircuit_clean")
*Plain-English reading.* Lean checks the proposition indexed as “public circuit clean”; the hypotheses and conclusion in the code panel fix its exact scope. The published data-low/bond-high circuit realizes the chain in the corresponding most-significant-bit-first word order.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The published data-low/bond-high circuit realizes the chain in the corresponding most-significant-bit-first word order.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TensorTrainPrimitivePreparation.lean:150](../../../../library/modules/tensortrainprimitivepreparation/#decl-quantumblockencoding-tensortrainprimitivepreparation-publiccircuit-clean). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TensorTrainPrimitivePreparation.assemble_gateCount" (lean := "QuantumBlockEncoding.TensorTrainPrimitivePreparation.assemble_gateCount")
*Plain-English reading.* Lean checks the proposition indexed as “assemble gate count”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TensorTrainPrimitivePreparation.lean:166](../../../../library/modules/tensortrainprimitivepreparation/#decl-quantumblockencoding-tensortrainprimitivepreparation-assemble-gatecount). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TensorTrainPrimitivePreparation.publicCircuit_gateCount_bound" (lean := "QuantumBlockEncoding.TensorTrainPrimitivePreparation.publicCircuit_gateCount_bound")
*Plain-English reading.* Lean checks the proposition indexed as “public circuit gate count bound”; the hypotheses and conclusion in the code panel fix its exact scope. Gate-list length, not only an arithmetic count proxy, is bounded.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Gate-list length, not only an arithmetic count proxy, is bounded.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TensorTrainPrimitivePreparation.lean:180](../../../../library/modules/tensortrainprimitivepreparation/#decl-quantumblockencoding-tensortrainprimitivepreparation-publiccircuit-gatecount-bound). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TensorTrainPrimitivePreparation.exists_primitive_preparation" (lean := "QuantumBlockEncoding.TensorTrainPrimitivePreparation.exists_primitive_preparation")
*Plain-English reading.* Lean checks the proposition indexed as “exists primitive preparation”; the hypotheses and conclusion in the code panel fix its exact scope. Any bounded normalized nonempty real scalar-boundary tensor train has an actual clean primitive preparation.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Any bounded normalized nonempty real scalar-boundary tensor train has an actual clean primitive preparation. The bound counts every instruction in the circuit list; initialization uses zero gates after exact boundary absorption. This theorem makes no claim about classical computation of its exact angles.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TensorTrainPrimitivePreparation.lean:202](../../../../library/modules/tensortrainprimitivepreparation/#decl-quantumblockencoding-tensortrainprimitivepreparation-exists-primitive-preparation). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/TensorTrainSchedule.lean

15 explicit public declarations, in source order.

:::definition "QuantumBlockEncoding.TensorTrainSchedule.wordOfBasis" (lean := "QuantumBlockEncoding.TensorTrainSchedule.wordOfBasis")
*Plain-English reading.* This definition gives the library's named construction or computation for “word of basis”. Convert increasing-wire basis labels to the head-first chain word.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Convert increasing-wire basis labels to the head-first chain word.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/TensorTrainSchedule.lean:18](../../../../library/modules/tensortrainschedule/#decl-quantumblockencoding-tensortrainschedule-wordofbasis). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.TensorTrainSchedule.rankAt" (lean := "QuantumBlockEncoding.TensorTrainSchedule.rankAt")
*Plain-English reading.* This definition gives the library's named construction or computation for “rank at”. Active rank before stage 't'; after the chain it is the terminal rank.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Active rank before stage 't'; after the chain it is the terminal rank.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/TensorTrainSchedule.lean:23](../../../../library/modules/tensortrainschedule/#decl-quantumblockencoding-tensortrainschedule-rankat). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TensorTrainSchedule.rankAt_zero" (lean := "QuantumBlockEncoding.TensorTrainSchedule.rankAt_zero")
*Plain-English reading.* Lean checks the proposition indexed as “rank at zero”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TensorTrainSchedule.lean:28](../../../../library/modules/tensortrainschedule/#decl-quantumblockencoding-tensortrainschedule-rankat-zero). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TensorTrainSchedule.rankAt_length" (lean := "QuantumBlockEncoding.TensorTrainSchedule.rankAt_length")
*Plain-English reading.* Lean checks the proposition indexed as “rank at length”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TensorTrainSchedule.lean:31](../../../../library/modules/tensortrainschedule/#decl-quantumblockencoding-tensortrainschedule-rankat-length). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.TensorTrainSchedule.paddedAt" (lean := "QuantumBlockEncoding.TensorTrainSchedule.paddedAt")
*Plain-English reading.* This definition gives the library's named construction or computation for “padded at”. The finite schedule, zero after the final core.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The finite schedule, zero after the final core. Later stages are never used by the bounded run theorem.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/TensorTrainSchedule.lean:38](../../../../library/modules/tensortrainschedule/#decl-quantumblockencoding-tensortrainschedule-paddedat). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TensorTrainSchedule.rankAt_le_maxBond" (lean := "QuantumBlockEncoding.TensorTrainSchedule.rankAt_le_maxBond")
*Plain-English reading.* Lean checks the proposition indexed as “rank at le max bond”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TensorTrainSchedule.lean:44](../../../../library/modules/tensortrainschedule/#decl-quantumblockencoding-tensortrainschedule-rankat-le-maxbond). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TensorTrainSchedule.paddedAt_supported" (lean := "QuantumBlockEncoding.TensorTrainSchedule.paddedAt_supported")
*Plain-English reading.* Lean checks the proposition indexed as “padded at supported”; the hypotheses and conclusion in the code panel fix its exact scope. Zero amplitude outside the next actual rank, for the extracted schedule.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Zero amplitude outside the next actual rank, for the extracted schedule.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TensorTrainSchedule.lean:54](../../../../library/modules/tensortrainschedule/#decl-quantumblockencoding-tensortrainschedule-paddedat-supported). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TensorTrainSchedule.transfer_shift" (lean := "QuantumBlockEncoding.TensorTrainSchedule.transfer_shift")
*Plain-English reading.* Lean checks the proposition indexed as “transfer shift”; the hypotheses and conclusion in the code panel fix its exact scope. Rewrite chronological transfer in first-bit order, matching 'Chain.cons'.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Rewrite chronological transfer in first-bit order, matching 'Chain.cons'. The algebra still multiplies the last emitted matrix on the left.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TensorTrainSchedule.lean:68](../../../../library/modules/tensortrainschedule/#decl-quantumblockencoding-tensortrainschedule-transfer-shift). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TensorTrainSchedule.paddedSlice_mulVec" (lean := "QuantumBlockEncoding.TensorTrainSchedule.paddedSlice_mulVec")
*Plain-English reading.* Lean checks the proposition indexed as “padded slice mul vec”; the hypotheses and conclusion in the code panel fix its exact scope. One extracted core slice acts as its exact real row-vector contraction, with zero padding on the old and new bond labels.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* One extracted core slice acts as its exact real row-vector contraction, with zero padding on the old and new bond labels.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TensorTrainSchedule.lean:81](../../../../library/modules/tensortrainschedule/#decl-quantumblockencoding-tensortrainschedule-paddedslice-mulvec). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TensorTrainSchedule.transfer_padded" (lean := "QuantumBlockEncoding.TensorTrainSchedule.transfer_padded")
*Plain-English reading.* Lean checks the proposition indexed as “transfer padded”; the hypotheses and conclusion in the code panel fix its exact scope. All-length transfer equals the original chain contraction at every padded output label, not just after projection onto its active subspace.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* All-length transfer equals the original chain contraction at every padded output label, not just after projection onto its active subspace.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TensorTrainSchedule.lean:96](../../../../library/modules/tensortrainschedule/#decl-quantumblockencoding-tensortrainschedule-transfer-padded). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TensorTrainSchedule.run_eq_transfer_bounded" (lean := "QuantumBlockEncoding.TensorTrainSchedule.run_eq_transfer_bounded")
*Plain-English reading.* Lean checks the proposition indexed as “run eq transfer bounded”; the hypotheses and conclusion in the code panel fix its exact scope. A bounded version of the sequential local-column theorem: unused later stages need not implement the zero cores after the end of the schedule.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* A bounded version of the sequential local-column theorem: unused later stages need not implement the zero cores after the end of the schedule.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TensorTrainSchedule.lean:113](../../../../library/modules/tensortrainschedule/#decl-quantumblockencoding-tensortrainschedule-run-eq-transfer-bounded). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TensorTrainSchedule.run_padded" (lean := "QuantumBlockEncoding.TensorTrainSchedule.run_padded")
*Plain-English reading.* Lean checks the proposition indexed as “run padded”; the hypotheses and conclusion in the code panel fix its exact scope. Exact complete sequential action of the schedule extracted from an actual dependent train.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Exact complete sequential action of the schedule extracted from an actual dependent train. Only its first 'n' local active columns are premises.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TensorTrainSchedule.lean:145](../../../../library/modules/tensortrainschedule/#decl-quantumblockencoding-tensortrainschedule-run-padded). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TensorTrainSchedule.run_terminal_clean" (lean := "QuantumBlockEncoding.TensorTrainSchedule.run_terminal_clean")
*Plain-English reading.* Lean checks the proposition indexed as “run terminal clean”; the hypotheses and conclusion in the code panel fix its exact scope. Terminal dimension one gives whole-state cleanup at physical label zero.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Terminal dimension one gives whole-state cleanup at physical label zero. The amplitude at every other padded label is exactly zero.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TensorTrainSchedule.lean:163](../../../../library/modules/tensortrainschedule/#decl-quantumblockencoding-tensortrainschedule-run-terminal-clean). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TensorTrainSchedule.paddedAt_active_isometry" (lean := "QuantumBlockEncoding.TensorTrainSchedule.paddedAt_active_isometry")
*Plain-English reading.* Lean checks the proposition indexed as “padded at active isometry”; the hypotheses and conclusion in the code panel fix its exact scope. Every extracted stage of a canonical chain has orthonormal occupied columns in the one fixed physical register.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Every extracted stage of a canonical chain has orthonormal occupied columns in the one fixed physical register.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TensorTrainSchedule.lean:179](../../../../library/modules/tensortrainschedule/#decl-quantumblockencoding-tensortrainschedule-paddedat-active-isometry). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TensorTrainSchedule.exists_normalized_preparation_schedule" (lean := "QuantumBlockEncoding.TensorTrainSchedule.exists_normalized_preparation_schedule")
*Plain-English reading.* Lean checks the proposition indexed as “exists normalized preparation schedule”; the hypotheses and conclusion in the code panel fix its exact scope. A normalized scalar-boundary source train has a bounded canonical schedule with exact whole-state source action whenever its \*local occupied columns\* are implemented.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* A normalized scalar-boundary source train has a bounded canonical schedule with exact whole-state source action whenever its \*local occupied columns\* are implemented. No source-state oracle or global-action hypothesis occurs in the premises.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TensorTrainSchedule.lean:200](../../../../library/modules/tensortrainschedule/#decl-quantumblockencoding-tensortrainschedule-exists-normalized-preparation-schedule). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/TensorTrainWord.lean

12 explicit public declarations, in source order.

:::definition "QuantumBlockEncoding.TensorTrainWord.toBasis" (lean := "QuantumBlockEncoding.TensorTrainWord.toBasis")
*Plain-English reading.* This definition gives the library's named construction or computation for “to basis”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/TensorTrainWord.lean:10](../../../../library/modules/tensortrainword/#decl-quantumblockencoding-tensortrainword-tobasis). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TensorTrainWord.toBasis_wordOfBasis" (lean := "QuantumBlockEncoding.TensorTrainWord.toBasis_wordOfBasis")
*Plain-English reading.* Lean checks the proposition indexed as “to basis word of basis”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TensorTrainWord.lean:14](../../../../library/modules/tensortrainword/#decl-quantumblockencoding-tensortrainword-tobasis-wordofbasis). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TensorTrainWord.wordOfBasis_toBasis" (lean := "QuantumBlockEncoding.TensorTrainWord.wordOfBasis_toBasis")
*Plain-English reading.* Lean checks the proposition indexed as “word of basis to basis”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TensorTrainWord.lean:20](../../../../library/modules/tensortrainword/#decl-quantumblockencoding-tensortrainword-wordofbasis-tobasis). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.TensorTrainWord.basisEquiv" (lean := "QuantumBlockEncoding.TensorTrainWord.basisEquiv")
*Plain-English reading.* This definition gives the library's named construction or computation for “basis equiv”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/TensorTrainWord.lean:26](../../../../library/modules/tensortrainword/#decl-quantumblockencoding-tensortrainword-basisequiv). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.TensorTrainWord.reverseBasis" (lean := "QuantumBlockEncoding.TensorTrainWord.reverseBasis")
*Plain-English reading.* This definition gives the library's named construction or computation for “reverse basis”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/TensorTrainWord.lean:32](../../../../library/modules/tensortrainword/#decl-quantumblockencoding-tensortrainword-reversebasis). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.TensorTrainWord.sampleEquiv" (lean := "QuantumBlockEncoding.TensorTrainWord.sampleEquiv")
*Plain-English reading.* This definition gives the library's named construction or computation for “sample equiv”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/TensorTrainWord.lean:38](../../../../library/modules/tensortrainword/#decl-quantumblockencoding-tensortrainword-sampleequiv). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.TensorTrainWord.toBits" (lean := "QuantumBlockEncoding.TensorTrainWord.toBits")
*Plain-English reading.* This definition gives the library's named construction or computation for “to bits”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/TensorTrainWord.lean:41](../../../../library/modules/tensortrainword/#decl-quantumblockencoding-tensortrainword-tobits). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TensorTrainWord.toBits_length" (lean := "QuantumBlockEncoding.TensorTrainWord.toBits_length")
*Plain-English reading.* Lean checks the proposition indexed as “to bits length”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TensorTrainWord.lean:45](../../../../library/modules/tensortrainword/#decl-quantumblockencoding-tensortrainword-tobits-length). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TensorTrainWord.primitive_snoc_value" (lean := "QuantumBlockEncoding.TensorTrainWord.primitive_snoc_value")
*Plain-English reading.* Lean checks the proposition indexed as “primitive snoc value”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TensorTrainWord.lean:50](../../../../library/modules/tensortrainword/#decl-quantumblockencoding-tensortrainword-primitive-snoc-value). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TensorTrainWord.sampleEquiv_value" (lean := "QuantumBlockEncoding.TensorTrainWord.sampleEquiv_value")
*Plain-English reading.* Lean checks the proposition indexed as “sample equiv value”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TensorTrainWord.lean:73](../../../../library/modules/tensortrainword/#decl-quantumblockencoding-tensortrainword-sampleequiv-value). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TensorTrainWord.wordSampleIndex_eq" (lean := "QuantumBlockEncoding.TensorTrainWord.wordSampleIndex_eq")
*Plain-English reading.* Lean checks the proposition indexed as “word sample index eq”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TensorTrainWord.lean:90](../../../../library/modules/tensortrainword/#decl-quantumblockencoding-tensortrainword-wordsampleindex-eq). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TensorTrainWord.sampleEquiv_public" (lean := "QuantumBlockEncoding.TensorTrainWord.sampleEquiv_public")
*Plain-English reading.* Lean checks the proposition indexed as “sample equiv public”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TensorTrainWord.lean:96](../../../../library/modules/tensortrainword/#decl-quantumblockencoding-tensortrainword-sampleequiv-public). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/TextbookStatePreparation.lean

29 explicit public declarations, in source order.

:::definition "QuantumBlockEncoding.TextbookStatePreparation.zeroIndex" (lean := "QuantumBlockEncoding.TextbookStatePreparation.zeroIndex")
*Plain-English reading.* This definition gives the library's named construction or computation for “zero index”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/TextbookStatePreparation.lean:20](../../../../library/modules/textbookstatepreparation/#decl-quantumblockencoding-textbookstatepreparation-zeroindex). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.TextbookStatePreparation.oneIndex" (lean := "QuantumBlockEncoding.TextbookStatePreparation.oneIndex")
*Plain-English reading.* This definition gives the library's named construction or computation for “one index”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/TextbookStatePreparation.lean:22](../../../../library/modules/textbookstatepreparation/#decl-quantumblockencoding-textbookstatepreparation-oneindex). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.TextbookStatePreparation.pauliX" (lean := "QuantumBlockEncoding.TextbookStatePreparation.pauliX")
*Plain-English reading.* This definition gives the library's named construction or computation for “pauli x”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/TextbookStatePreparation.lean:24](../../../../library/modules/textbookstatepreparation/#decl-quantumblockencoding-textbookstatepreparation-paulix). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TextbookStatePreparation.pauliX_unitary" (lean := "QuantumBlockEncoding.TextbookStatePreparation.pauliX_unitary")
*Plain-English reading.* Lean checks the proposition indexed as “pauli x unitary”; the hypotheses and conclusion in the code panel fix its exact scope. The Pauli X matrix is unitary in Mathlib's standard unitary group.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The Pauli X matrix is unitary in Mathlib's standard unitary group.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TextbookStatePreparation.lean:28](../../../../library/modules/textbookstatepreparation/#decl-quantumblockencoding-textbookstatepreparation-paulix-unitary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.TextbookStatePreparation.oneState" (lean := "QuantumBlockEncoding.TextbookStatePreparation.oneState")
*Plain-English reading.* This definition gives the library's named construction or computation for “one state”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/TextbookStatePreparation.lean:35](../../../../library/modules/textbookstatepreparation/#decl-quantumblockencoding-textbookstatepreparation-onestate). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.TextbookStatePreparation.oneTarget" (lean := "QuantumBlockEncoding.TextbookStatePreparation.oneTarget")
*Plain-English reading.* This definition gives the library's named construction or computation for “one target”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/TextbookStatePreparation.lean:38](../../../../library/modules/textbookstatepreparation/#decl-quantumblockencoding-textbookstatepreparation-onetarget). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TextbookStatePreparation.oneTarget_normalized" (lean := "QuantumBlockEncoding.TextbookStatePreparation.oneTarget_normalized")
*Plain-English reading.* Lean checks the proposition indexed as “one target normalized”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TextbookStatePreparation.lean:43](../../../../library/modules/textbookstatepreparation/#decl-quantumblockencoding-textbookstatepreparation-onetarget-normalized). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TextbookStatePreparation.pauliX_prepares_one" (lean := "QuantumBlockEncoding.TextbookStatePreparation.pauliX_prepares_one")
*Plain-English reading.* Lean checks the proposition indexed as “pauli x prepares one”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TextbookStatePreparation.lean:49](../../../../library/modules/textbookstatepreparation/#decl-quantumblockencoding-textbookstatepreparation-paulix-prepares-one). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.TextbookStatePreparation.pauliXGate" (lean := "QuantumBlockEncoding.TextbookStatePreparation.pauliXGate")
*Plain-English reading.* This definition gives the library's named construction or computation for “pauli x gate”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/TextbookStatePreparation.lean:62](../../../../library/modules/textbookstatepreparation/#decl-quantumblockencoding-textbookstatepreparation-paulixgate). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.TextbookStatePreparation.pauliXCertificate" (lean := "QuantumBlockEncoding.TextbookStatePreparation.pauliXCertificate")
*Plain-English reading.* This definition gives the library's named construction or computation for “pauli x certificate”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/TextbookStatePreparation.lean:66](../../../../library/modules/textbookstatepreparation/#decl-quantumblockencoding-textbookstatepreparation-paulixcertificate). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.TextbookStatePreparation.pauliXCircuit" (lean := "QuantumBlockEncoding.TextbookStatePreparation.pauliXCircuit")
*Plain-English reading.* This definition gives the library's named construction or computation for “pauli x circuit”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/TextbookStatePreparation.lean:72](../../../../library/modules/textbookstatepreparation/#decl-quantumblockencoding-textbookstatepreparation-paulixcircuit). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.TextbookStatePreparation.pauliXVerified" (lean := "QuantumBlockEncoding.TextbookStatePreparation.pauliXVerified")
*Plain-English reading.* This definition gives the library's named construction or computation for “pauli x verified”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/TextbookStatePreparation.lean:74](../../../../library/modules/textbookstatepreparation/#decl-quantumblockencoding-textbookstatepreparation-paulixverified). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TextbookStatePreparation.pauliXVerified_cost" (lean := "QuantumBlockEncoding.TextbookStatePreparation.pauliXVerified_cost")
*Plain-English reading.* Lean checks the proposition indexed as “pauli x verified cost”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TextbookStatePreparation.lean:78](../../../../library/modules/textbookstatepreparation/#decl-quantumblockencoding-textbookstatepreparation-paulixverified-cost). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.TextbookStatePreparation.invSqrtTwo" (lean := "QuantumBlockEncoding.TextbookStatePreparation.invSqrtTwo")
*Plain-English reading.* This definition gives the library's named construction or computation for “inv sqrt two”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/TextbookStatePreparation.lean:83](../../../../library/modules/textbookstatepreparation/#decl-quantumblockencoding-textbookstatepreparation-invsqrttwo). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TextbookStatePreparation.invSqrtTwo_mul_self" (lean := "QuantumBlockEncoding.TextbookStatePreparation.invSqrtTwo_mul_self")
*Plain-English reading.* Lean checks the proposition indexed as “inv sqrt two mul self”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TextbookStatePreparation.lean:86](../../../../library/modules/textbookstatepreparation/#decl-quantumblockencoding-textbookstatepreparation-invsqrttwo-mul-self). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.TextbookStatePreparation.hadamard" (lean := "QuantumBlockEncoding.TextbookStatePreparation.hadamard")
*Plain-English reading.* This definition gives the library's named construction or computation for “hadamard”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/TextbookStatePreparation.lean:94](../../../../library/modules/textbookstatepreparation/#decl-quantumblockencoding-textbookstatepreparation-hadamard). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TextbookStatePreparation.star_hadamard" (lean := "QuantumBlockEncoding.TextbookStatePreparation.star_hadamard")
*Plain-English reading.* Lean checks the proposition indexed as “star hadamard”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TextbookStatePreparation.lean:99](../../../../library/modules/textbookstatepreparation/#decl-quantumblockencoding-textbookstatepreparation-star-hadamard). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TextbookStatePreparation.hadamard_unitary" (lean := "QuantumBlockEncoding.TextbookStatePreparation.hadamard_unitary")
*Plain-English reading.* Lean checks the proposition indexed as “hadamard unitary”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TextbookStatePreparation.lean:104](../../../../library/modules/textbookstatepreparation/#decl-quantumblockencoding-textbookstatepreparation-hadamard-unitary). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.TextbookStatePreparation.plusState" (lean := "QuantumBlockEncoding.TextbookStatePreparation.plusState")
*Plain-English reading.* This definition gives the library's named construction or computation for “plus state”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/TextbookStatePreparation.lean:113](../../../../library/modules/textbookstatepreparation/#decl-quantumblockencoding-textbookstatepreparation-plusstate). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.TextbookStatePreparation.plusTarget" (lean := "QuantumBlockEncoding.TextbookStatePreparation.plusTarget")
*Plain-English reading.* This definition gives the library's named construction or computation for “plus target”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/TextbookStatePreparation.lean:116](../../../../library/modules/textbookstatepreparation/#decl-quantumblockencoding-textbookstatepreparation-plustarget). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TextbookStatePreparation.plusTarget_normalized" (lean := "QuantumBlockEncoding.TextbookStatePreparation.plusTarget_normalized")
*Plain-English reading.* Lean checks the proposition indexed as “plus target normalized”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TextbookStatePreparation.lean:121](../../../../library/modules/textbookstatepreparation/#decl-quantumblockencoding-textbookstatepreparation-plustarget-normalized). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TextbookStatePreparation.hadamard_prepares_plus" (lean := "QuantumBlockEncoding.TextbookStatePreparation.hadamard_prepares_plus")
*Plain-English reading.* Lean checks the proposition indexed as “hadamard prepares plus”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TextbookStatePreparation.lean:128](../../../../library/modules/textbookstatepreparation/#decl-quantumblockencoding-textbookstatepreparation-hadamard-prepares-plus). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.TextbookStatePreparation.hadamardGate" (lean := "QuantumBlockEncoding.TextbookStatePreparation.hadamardGate")
*Plain-English reading.* This definition gives the library's named construction or computation for “hadamard gate”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/TextbookStatePreparation.lean:139](../../../../library/modules/textbookstatepreparation/#decl-quantumblockencoding-textbookstatepreparation-hadamardgate). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.TextbookStatePreparation.hadamardCertificate" (lean := "QuantumBlockEncoding.TextbookStatePreparation.hadamardCertificate")
*Plain-English reading.* This definition gives the library's named construction or computation for “hadamard certificate”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/TextbookStatePreparation.lean:143](../../../../library/modules/textbookstatepreparation/#decl-quantumblockencoding-textbookstatepreparation-hadamardcertificate). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.TextbookStatePreparation.hadamardCircuit" (lean := "QuantumBlockEncoding.TextbookStatePreparation.hadamardCircuit")
*Plain-English reading.* This definition gives the library's named construction or computation for “hadamard circuit”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/TextbookStatePreparation.lean:150](../../../../library/modules/textbookstatepreparation/#decl-quantumblockencoding-textbookstatepreparation-hadamardcircuit). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.TextbookStatePreparation.hadamardVerified" (lean := "QuantumBlockEncoding.TextbookStatePreparation.hadamardVerified")
*Plain-English reading.* This definition gives the library's named construction or computation for “hadamard verified”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/TextbookStatePreparation.lean:152](../../../../library/modules/textbookstatepreparation/#decl-quantumblockencoding-textbookstatepreparation-hadamardverified). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TextbookStatePreparation.hadamardVerified_cost" (lean := "QuantumBlockEncoding.TextbookStatePreparation.hadamardVerified_cost")
*Plain-English reading.* Lean checks the proposition indexed as “hadamard verified cost”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TextbookStatePreparation.lean:156](../../../../library/modules/textbookstatepreparation/#decl-quantumblockencoding-textbookstatepreparation-hadamardverified-cost). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TextbookStatePreparation.pauliXCertificate_prepares_one" (lean := "QuantumBlockEncoding.TextbookStatePreparation.pauliXCertificate_prepares_one")
*Plain-English reading.* Lean checks the proposition indexed as “pauli x certificate prepares one”; the hypotheses and conclusion in the code panel fix its exact scope. The certified Pauli X example states the familiar textbook equation.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The certified Pauli X example states the familiar textbook equation.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TextbookStatePreparation.lean:162](../../../../library/modules/textbookstatepreparation/#decl-quantumblockencoding-textbookstatepreparation-paulixcertificate-prepares-one). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.TextbookStatePreparation.hadamardCertificate_prepares_plus" (lean := "QuantumBlockEncoding.TextbookStatePreparation.hadamardCertificate_prepares_plus")
*Plain-English reading.* Lean checks the proposition indexed as “hadamard certificate prepares plus”; the hypotheses and conclusion in the code panel fix its exact scope. The certified Hadamard example prepares the equal superposition.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The certified Hadamard example prepares the equal superposition.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/TextbookStatePreparation.lean:168](../../../../library/modules/textbookstatepreparation/#decl-quantumblockencoding-textbookstatepreparation-hadamardcertificate-prepares-plus). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/ThinLQ.lean

3 explicit public declarations, in source order.

:::theorem "QuantumBlockEncoding.ThinLQ.sum_prefix_of_zero" (lean := "QuantumBlockEncoding.ThinLQ.sum_prefix_of_zero")
*Plain-English reading.* Lean checks the proposition indexed as “sum prefix of zero”; the hypotheses and conclusion in the code panel fix its exact scope. Restrict a finite sum to a prefix when all remaining summands vanish.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Restrict a finite sum to a prefix when all remaining summands vanish.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ThinLQ.lean:18](../../../../library/modules/thinlq/#decl-quantumblockencoding-thinlq-sum-prefix-of-zero). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ThinLQ.exists_factor_of_le" (lean := "QuantumBlockEncoding.ThinLQ.exists_factor_of_le")
*Plain-English reading.* Lean checks the proposition indexed as “exists factor of le”; the hypotheses and conclusion in the code panel fix its exact scope. A wide real matrix has an exact factorization with orthonormal rows.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* A wide real matrix has an exact factorization with orthonormal rows. No linear-independence or nonzero-row assumption is required.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ThinLQ.lean:38](../../../../library/modules/thinlq/#decl-quantumblockencoding-thinlq-exists-factor-of-le). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.ThinLQ.exists_thin_lq" (lean := "QuantumBlockEncoding.ThinLQ.exists_thin_lq")
*Plain-English reading.* Lean checks the proposition indexed as “exists thin lq”; the hypotheses and conclusion in the code panel fix its exact scope. Every finite real matrix admits a thin factorization with exactly 'min m n' orthonormal rows.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Every finite real matrix admits a thin factorization with exactly 'min m n' orthonormal rows. This includes rank-deficient and empty matrices.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/ThinLQ.lean:81](../../../../library/modules/thinlq/#decl-quantumblockencoding-thinlq-exists-thin-lq). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

# QuantumBlockEncoding/UniformlyControlledRy.lean

11 explicit public declarations, in source order.

:::definition "QuantumBlockEncoding.primitiveControlAssignment" (lean := "QuantumBlockEncoding.primitiveControlAssignment")
*Plain-English reading.* This definition gives the library's named construction or computation for “primitive control assignment”.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/UniformlyControlledRy.lean:34](../../../../library/modules/uniformlycontrolledry/#decl-quantumblockencoding-primitivecontrolassignment). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.controlledRyBlockMatrix" (lean := "QuantumBlockEncoding.controlledRyBlockMatrix")
*Plain-English reading.* This definition gives the library's named construction or computation for “controlled ry block matrix”. Backend-independent specification: each fixed assignment of the non-target wires owns one exact two-dimensional RY block selected by the control bits.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Backend-independent specification: each fixed assignment of the non-target wires owns one exact two-dimensional RY block selected by the control bits.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/UniformlyControlledRy.lean:42](../../../../library/modules/uniformlycontrolledry/#decl-quantumblockencoding-controlledryblockmatrix). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.controlledRyBlockMatrix_apply" (lean := "QuantumBlockEncoding.controlledRyBlockMatrix_apply")
*Plain-English reading.* Lean checks the proposition indexed as “controlled ry block matrix apply”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/UniformlyControlledRy.lean:51](../../../../library/modules/uniformlycontrolledry/#decl-quantumblockencoding-controlledryblockmatrix-apply). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.compileUniformlyControlledRy" (lean := "QuantumBlockEncoding.compileUniformlyControlledRy")
*Plain-English reading.* This definition gives the library's named construction or computation for “compile uniformly controlled ry”. Reference recursive compiler.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Reference recursive compiler. Controls are consumed from low to high in the supplied control tuple; circuit execution remains chronological.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/UniformlyControlledRy.lean:192](../../../../library/modules/uniformlycontrolledry/#decl-quantumblockencoding-compileuniformlycontrolledry). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::definition "QuantumBlockEncoding.uniformlyControlledRyMatrix" (lean := "QuantumBlockEncoding.uniformlyControlledRyMatrix")
*Plain-English reading.* This definition gives the library's named construction or computation for “uniformly controlled ry matrix”. Recursive matrix specification corresponding to the standard multiplexor identity.

*Formal status.* Compiled declaration in the default ASPBE import surface; its kind and displayed Lean type determine how it may be used.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Recursive matrix specification corresponding to the standard multiplexor identity. This definition is backend-independent and mentions only exact primitive matrix semantics.

*Declaration kind.* def.

Source: [QuantumBlockEncoding/UniformlyControlledRy.lean:217](../../../../library/modules/uniformlycontrolledry/#decl-quantumblockencoding-uniformlycontrolledrymatrix). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.compileUniformlyControlledRy_eval" (lean := "QuantumBlockEncoding.compileUniformlyControlledRy_eval")
*Plain-English reading.* Lean checks the proposition indexed as “compile uniformly controlled ry eval”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/UniformlyControlledRy.lean:240](../../../../library/modules/uniformlycontrolledry/#decl-quantumblockencoding-compileuniformlycontrolledry-eval). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.compileUniformlyControlledRy_eval_controlledRyBlockMatrix" (lean := "QuantumBlockEncoding.compileUniformlyControlledRy_eval_controlledRyBlockMatrix")
*Plain-English reading.* Lean checks the proposition indexed as “compile uniformly controlled ry eval controlled ry block matrix”; the hypotheses and conclusion in the code panel fix its exact scope. The recursive compiler satisfies the independent block-diagonal specification selected by the computational-basis controls.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The recursive compiler satisfies the independent block-diagonal specification selected by the computational-basis controls.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/UniformlyControlledRy.lean:259](../../../../library/modules/uniformlycontrolledry/#decl-quantumblockencoding-compileuniformlycontrolledry-eval-controlledryblockmatrix). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.compileUniformlyControlledRy_ryCount" (lean := "QuantumBlockEncoding.compileUniformlyControlledRy_ryCount")
*Plain-English reading.* Lean checks the proposition indexed as “compile uniformly controlled ry ry count”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/UniformlyControlledRy.lean:370](../../../../library/modules/uniformlycontrolledry/#decl-quantumblockencoding-compileuniformlycontrolledry-rycount). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.compileUniformlyControlledRy_cxCount" (lean := "QuantumBlockEncoding.compileUniformlyControlledRy_cxCount")
*Plain-English reading.* Lean checks the proposition indexed as “compile uniformly controlled ry cx count”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/UniformlyControlledRy.lean:386](../../../../library/modules/uniformlycontrolledry/#decl-quantumblockencoding-compileuniformlycontrolledry-cxcount). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.compileUniformlyControlledRy_oracleCalls_eq_zero" (lean := "QuantumBlockEncoding.compileUniformlyControlledRy_oracleCalls_eq_zero")
*Plain-English reading.* Lean checks the proposition indexed as “compile uniformly controlled ry oracle calls eq zero”; the hypotheses and conclusion in the code panel fix its exact scope.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* The source declaration has no docstring. The reader cue above is generated from its kind and name and does not replace the Lean signature.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/UniformlyControlledRy.lean:403](../../../../library/modules/uniformlycontrolledry/#decl-quantumblockencoding-compileuniformlycontrolledry-oraclecalls-eq-zero). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::

:::theorem "QuantumBlockEncoding.compileUniformlyControlledRy_five_control_counts" (lean := "QuantumBlockEncoding.compileUniformlyControlledRy_five_control_counts")
*Plain-English reading.* Lean checks the proposition indexed as “compile uniformly controlled ry five control counts”; the hypotheses and conclusion in the code panel fix its exact scope. Frozen Robin reference count: five controls require 32 RY and 62 CX.

*Formal status.* Compiled theorem in the default ASPBE import surface; the displayed Lean signature is the authoritative claim.

*Why it is in this chapter.* Circuit and register semantics, reusable tensor-train and matrix constructions, and explicit exact-real storage-cost refinements. Each declaration's hypotheses and conclusion fix its certified scope.

*Technical source note.* Frozen Robin reference count: five controls require 32 RY and 62 CX.

*Declaration kind.* theorem.

Source: [QuantumBlockEncoding/UniformlyControlledRy.lean:411](../../../../library/modules/uniformlycontrolledry/#decl-quantumblockencoding-compileuniformlycontrolledry-five-control-counts). A commit-pinned external link is added by the publication build when the source exists at the published ref.
:::
