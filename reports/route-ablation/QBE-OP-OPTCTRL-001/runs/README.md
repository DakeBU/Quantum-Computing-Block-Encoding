# Raw Route-Ablation Runs

Raw agent traces are local-only artifacts and are ignored by git because they
can be large and can contain machine-specific paths.  Public evidence is kept
in the curated summary files one directory up:

- `latest_results.md`
- `latest_results.json`
- `comparison_status.md`
