\section{Status}

The source paper is Nikita Guseynov, Xiajie Huang, and Nana Liu,
\emph{Quantum framework for simulating linear PDEs with Robin boundary
conditions}, arXiv:2506.20478.  The Lean source of truth is
\Lean{QuantumBlockEncoding/GHL2025.lean}.

The build gate after this export is
\[
  \Lean{python3 tools/qbe.py check}.
\]
It runs \Lean{lake build} and \Lean{lake build Tests}.

\begin{longtable}{p{0.25\textwidth}p{0.42\textwidth}p{0.22\textwidth}}
\hline
Block & Main Lean declarations & Status \\
\hline
Circuit gate-list alignment &
\Lean{oneTermRobinPlaceholdersMatch} &
proved \\
Dimension compatibility &
\Lean{Examples.RobinHeat.oneTermRobinCircuitDimCompat} &
proved \\
Indicator oracle permutation route &
\Lean{indicatorOracleImage\_self\_inverse},
\Lean{indicatorOracleImage\_bijective},
\Lean{indicatorOracleMatrix\_is\_permutation} &
proved \\
SWAP permutation route &
\Lean{swapOracleImage\_self\_inverse},
\Lean{swapOracleImage\_bijective},
\Lean{swapOracleMatrix\_is\_permutation} &
proved; SWAP \Lean{unitary.proved := true} \\
\(\ODBS\) register-safety route &
\Lean{bandedSparseAccessPaperImage\_lt\_qubitDim\_of\_address\_lt},
\Lean{bandedSparseAccessPaperImage\_rowValue\_eq},
\Lean{bandedSparseAccessPaperImage\_odRegisterValue\_eq} &
proved executable blocks \\
\(\Uf\) clean-branch skeleton &
\Lean{functionOraclePaperMatrix\_cleanBranch\_entry},
\Lean{functionOraclePaperMatrix\_cleanWorkspace\_offBranch\_zero} &
proved skeleton entries \\
\(\Uf\) cited amplitude source transcript &
\Lean{FunctionOracleExternalAmplitudeSourceContract},
\Lean{functionOracleExternalAmplitudeSourceContract},
\Lean{functionOracleExternalAmplitudeSourceContract\_flags\_false} &
typed transcript; source-side flags false \\
\(\Uf\) \(N_f\) amplitude route &
\Lean{FunctionOracleAmplitudeProofRoute},
\Lean{functionOracleAmplitudeProofRoute},
\Lean{functionOracleAmplitudeProofRoute\_sourceAnchor},
\Lean{functionOracleAmplitudeProofRoute\_externalSourceContract},
\Lean{functionOracleAmplitudeProofRoute\_flags\_false} &
typed contract route; analytic flags false \\
\(\Uf\) cited amplitude theorem &
\Lean{research-wiki/cited-results/GHL2025.md} row
\Lean{GL2024.Thm5.AmplitudeOracle} &
external cited-result recorded as an obligation \\
\(\ODBS\) cited prior primitive &
\Lean{research-wiki/cited-results/GHL2025.md} row
\Lean{GHL2024.PDE.Def6Lemma1.ODBS} &
external cited-result recorded; cleanup, unitarity, and final block
obligations remain open \\
\(\ODBS\) global-slot source predicate &
\Lean{bandedSparseAccessPaperSparseIndexInKappa},
\Lean{bandedSparseAccessPaperGlobalSlotSource},
\Lean{bandedSparseAccessPaperGlobalSlotSource\_boundaryColumns\_n3},
\Lean{bandedSparseAccessPaperGlobalSlotSource\_encodedOutOfRange\_n3} &
active source is padded clean input with \(s<\kappa\); old columns \(0\) and
\(48\) are both active global sources, while encoded \(s=7\) is out of range
when \(\kappa=7\) \\
\(\ODBS\) active global-slot route guards &
\Lean{oneTermRobinBlockEncodingProofRoute\_odbsActiveGlobalSlotBlockers},
\Lean{oneTermRobinBlockEncodingProofRoute\_odbsActiveGlobalSlotGateFreeze},
\Lean{oneTermRobinBlockEncodingProofRoute\_activeOdbsGatePairBlocked} &
active paper-image wiring is pinned to
\Lean{bandedSparseAccessPaperGlobalSlotSource}; full clean-domain cleanup,
full-space unitarity, and semantic promotion remain false \\
\(\ODBS\) rejected row-dependent regression &
\Lean{oneTermRobinBlockEncodingProofRoute\_rejectedRowDependentCollisionRegression\_n3} &
old row-dependent collision is retained only as regression memory; it is not an
active blocker \\
\(\ODBS\) encoded out-of-range guard &
\Lean{oneTermRobinBlockEncodingProofRoute\_encodedOutOfRangeSparseSlot\_n3} &
clean encoded sparse value \(s=7\) is outside the active source range when
\(\kappa=7\); cleanup and unitarity remain false \\
Seven-gate flag freeze guard &
\Lean{oneTermRobinGateMatrixPlaceholders\_unitaryFlags},
\Lean{oneTermRobinBlockEncodingProofRoute\_gateUnitaryFlags} &
active gate flags remain
\([\Lean{true},\Lean{false},\Lean{false},\Lean{false},\Lean{false},\Lean{true},\Lean{false}]\);
only \(U_{\mathrm{indic}}\) and SWAP are locally proved, and theorem-level
circuit-unitary and block flags remain false \\
Seven-gate order-and-flag freeze guard &
\Lean{oneTermRobinGateMatrixPlaceholders\_gateList},
\Lean{oneTermRobinBlockEncodingProofRoute\_gateListAndFlags} &
active matrix labels match Fig.~1-term Robin order while preserving the same
flag vector and active global-source cleanup scope \\
Derivative and boundary route bridge &
\Lean{oneTermRobinBlockEncodingProofRoute\_derivativeBoundaryContractMap} &
\(\ODT\) and boundary \(R_y\) share the same \(D_j^{(s)}/N_D\) source route
in gate slots 1 and 2; analytic and gate-unitarity flags remain false \\
One-term theorem route &
\Lean{Examples.RobinHeat.oneTermRobinBlockEncodingProofRoute},
\Lean{oneTermRobinBlockEncodingProofRoute\_flags\_false},
\Lean{oneTermRobinBlockEncodingProofRoute\_theoremTranscriptDependencies},
\Lean{oneTermRobinBlockEncodingProofRoute\_theoremTranscriptActiveCleanupMap} &
theorem-level transcript and active-source cleanup evidence are compiled;
semantic blockers remain false \\
Theorem layout/projection audit &
\Lean{defaultOneTermRobinTheoremData\_signalQubits\_eq\_layout},
\Lean{defaultOneTermRobinTheoremData\_pureAncillas\_eq\_resource},
\Lean{effectiveRobinSignalQubits\_eq\_layout\_signal\_plus\_visibleWorkspace},
\Lean{oneTermRobinBlockEncodingProofRoute\_layoutProjectionAudit} &
theorem signal and \(2n\) pure-ancilla counts are separated from the full
non-system projection dimension; cleanup and block flags remain false \\
Block-projection claim guard &
\Lean{oneTermRobinBlockEncodingProofRoute\_claimBlockCorrectFalse},
\Lean{oneTermRobinBlockEncodingProofRoute\_signalZeroBlockIndices} &
circuit-claim block correctness, target projection, and target block equation
remain false; signal-zero row and column offsets are pinned \\
Ak target matrix &
\Lean{Examples.RobinHeat.oneTermRobinAkMatrix},
\Lean{oneTermRobinAkMatrix\_apply} &
theorem target entries are now \(f(x_i)D_{ij}\); the derivative matrix remains
the derivative-oracle factor \\
Finite composition contract &
\Lean{FiniteBlockCompositionContract},
\Lean{Examples.RobinHeat.oneTermRobinFiniteBlockCompositionContract},
\Lean{oneTermRobinBlockEncodingProofRoute\_finiteBlockCompositionContractMap} &
theorem target, \Lean{oneTermRobinAkMatrix}, and normalizer are wired; circuit
unitarity, LCU, projection, normalized block equality, and final extraction
remain false \\
Gamma3 factor-entry ledger &
\Lean{oneTermRobinBlockEncodingProofRoute\_gamma3FactorEntryLedger} &
target entry, clean \(\Uf\) entry, \(\ODT\) ket-zero entry, boundary \(R_y\)
ket-zero entry, and active global-source \(\ODBS\) dagger entry are packaged
for the future signal-block theorem; semantic flags remain false \\
Gamma3 product-entry bridge &
\Lean{oneTermRobinBlockEncodingProofRoute\_gamma3SignalBlockProductEntry} &
the signal-zero block entry is identified with the seven-gate
\Lean{evalGateMatrices} product entry; coefficient equality to the Ak target
remains false \\
Gamma3 Ak coefficient-entry contract &
\Lean{oneTermRobinBlockEncodingProofRoute\_gamma3AkCoefficientEntryContract} &
the product entry, Ak target entry, expanded \(f(x_i)D_{ij}\) entry, and
factor-entry ledger are synchronized; semantic flags remain false \\
Gamma3 product-to-coefficient interface &
\Lean{oneTermRobinBlockEncodingProofRoute\_gamma3ProductToCoefficientInterface},
\Lean{oneTermRobinGamma3ProductToCoefficientObligation} &
the remaining exact entry theorem is named; the obligation is compiled with
\Lean{proved = false} \\
Gamma3 projection path audit &
\Lean{oneTermRobinBlockEncodingProofRoute\_gamma3ProjectionPathAudit\_n3} &
the current signal-zero helper maps \((2,5)\) to full \((2,5)\), but the
executable path does not match the clean gamma3 ledger; no semantic flag is
promoted \\
Gamma3 paper-basis layout &
\Lean{oneTermRobinGamma3PaperBasisIndex},
\Lean{oneTermRobinBlockEncodingProofRoute\_gamma3PaperBasisLayout\_n3} &
Eq.~\texttt{ROBIN clarified} clean ket order is mapped to full endpoints
\((4,10)\) for the focused sparse-slot-zero branch; no semantic flag is
promoted \\
Gamma3 paper-basis path audit &
\Lean{oneTermRobinBlockEncodingProofRoute\_gamma3PaperBasisPathAudit\_n3} &
the slot-zero path from column \(10\) reaches row \(198\), not row \(4\);
the first mismatch is the \(\ODBS\) address writing \(3\) for row \(5\), slot
\(0\) \\
Gamma3 sparse-slot alignment &
\Lean{oneTermRobinBlockEncodingProofRoute\_gamma3SparseSlotAlignment\_n3} &
the coefficient \(D_{2,5}\) uses global sparse slot \(5\), with clean endpoints
\(90\) and \(84\); semantic flags remain false \\
Gamma3 projection-slot convention map &
\Lean{oneTermRobinGamma3ProjectionSlotConventionObligation},
\Lean{oneTermRobinBlockEncodingProofRoute\_gamma3ProjectionSlotConventionMap\_n3} &
the source-contract gap is named with \Lean{proved = false}; generic projection
endpoints remain \((2,5)\), while slot-\(5\) clean endpoints are \((84,90)\) \\
Gamma3 slot-\(5\) path audit &
\Lean{oneTermRobinBlockEncodingProofRoute\_gamma3Slot5PathAudit\_n3} &
the full circuit path from column \(90\) reaches row \(228\), not clean row
\(84\); the next blocker is a projection/register convention \\
Gamma3 projection/register convention decision &
\Lean{oneTermRobinBlockEncodingProofRoute\_gamma3Slot5ProjectionRegisterAudit\_n3},
\Lean{oneTermRobinGamma3ProjectionRegisterConventionDecision\_n3},
\Lean{oneTermRobinGamma3ProjectionRegisterConventionDecision\_n3\_transcript} &
endpoint \(228\) differs from clean endpoint \(84\) first in the indicator bit
and also in sparse index; product search is blocked until a convention is
stated \\
\hline
\end{longtable}

The following remain obligations: \(\ODBS\) injectivity and dagger cleanup,
\(\ODT\) coefficient-normalizer and rotation unitarity,
boundary-rotation analytic identities, \(\Uf\) nonzero \(N_f\), division
semantics, normalizer bound, orthogonal completion, theorem-level amplitude
correctness, unitary completion, and final block extraction with normalizer
\(N_DN_f\kappa\).  The finite matrix composition contract for
Theorem~\texttt{1 term robin} is also still false: the paper gives the theorem
statement, Eq.~\texttt{ROBIN clarified}, Fig.~\texttt{1 term ROBIN}, and the
block-encoding definition, but QBE still needs an exact finite theorem before
any LCU or final-extraction flag can be promoted.  The gamma3 factor-entry
ledger, product-entry bridge, Ak coefficient-entry contract, and
product-to-coefficient interface are synchronized dependency maps: they do not
prove the coefficient equality to \Lean{oneTermRobinAkMatrix} divided by
\(N_DN_f\kappa\) or close normalized block equality.  The circuit-claim and
target-level block flags are both explicitly false.  The current active
\(\ODBS\) source domain is
\Lean{bandedSparseAccessPaperGlobalSlotSource};
\Lean{QBE.ODBS.UnusedZeroBranchExtension} is retained only as rejected-model
memory for the old row-dependent helper.

Target-matrix drift has been corrected at the transcript level.
Theorem~\texttt{1 term robin} and Eq.~\texttt{ROBIN clarified} target
\(A_k\), whose entries are \(f(x_i)D_{ij}\), and the active Lean block target
now uses \Lean{oneTermRobinAkMatrix n}.  The Robin-specific projection/layout
bridge is now compiled as
\Lean{oneTermRobinBlockEncodingProofRoute\_gamma3PaperBasisLayout\_n3}.
Eq.~\texttt{ROBIN clarified} places the trailing ancilla at Lean bit \(0\) and
the system register in bits \([1,1+n)\), so the clean paper-basis index is
\((s \ll (1+n+odPure)) + (j \ll 1)\).  For \(n=3,\kappa=7\), this is
\((s \ll 4) + (j \ll 1)\), giving full endpoints \((4,10)\) for the focused
sparse-slot-zero system entry \((2,5)\).  The paper-basis path audit now
compiles as
\Lean{oneTermRobinBlockEncodingProofRoute\_gamma3PaperBasisPathAudit\_n3}.
The slot-zero path from column \(10\) reaches row \(198\), not row \(4\),
because \(\ODBS\) writes address \(3\) for row \(5\), slot \(0\).  The
sparse-slot alignment and projection-slot convention map now compile as
\Lean{oneTermRobinBlockEncodingProofRoute\_gamma3SparseSlotAlignment\_n3}
and
\Lean{oneTermRobinBlockEncodingProofRoute\_gamma3ProjectionSlotConventionMap\_n3}.
They record that \(D_{2,5}\) uses slot \(5\), with clean endpoints \(90\) and
\(84\), while the generic signal-zero projection endpoints remain \(5\) and
\(2\).  The slot-\(5\) path audit now compiles as
\Lean{oneTermRobinBlockEncodingProofRoute\_gamma3Slot5PathAudit\_n3}.  The
full Fig.~\texttt{1 term ROBIN} path from column \(90\) reaches row \(228\),
not row \(84\).  The field-level audit and middle decision now compile as
\Lean{oneTermRobinBlockEncodingProofRoute\_gamma3Slot5ProjectionRegisterAudit\_n3}
and
\Lean{oneTermRobinGamma3ProjectionRegisterConventionDecision\_n3}.  The first
endpoint mismatch is the indicator bit, and the sparse index also differs.
Product-to-coefficient proof search is blocked until a precise
projection/register convention is stated.  Normalized-block equality, LCU
correctness, cleanup, unitarity, block correctness, and final extraction
remain false.

The latest \(\ODBS\) guards are regression checks, not semantic closure.
The active theorem route no longer carries unused-zero-branch source fields;
it uses \Lean{cleanupScopeDecision} with selected predicate
\Lean{bandedSparseAccessPaperGlobalSlotSource}.  Source column \(8\) separates
the active Lemma 1 paper-image matrix from the legacy helper, source columns
\(0\) and \(48\) record the rejected row-dependent collision only as
regression memory, and
\Lean{oneTermRobinBlockEncodingProofRoute\_encodedOutOfRangeSparseSlot\_n3}
records the encoded \(s=7\) out-of-range guard for \(\kappa=7\).
The guard
\Lean{oneTermRobinBlockEncodingProofRoute\_gateListAndFlags} also pins the
active matrix order to \Lean{oneTermRobinCircuit}, so future source-contract
packets cannot silently reorder the seven gates while preserving the same
false-flag vector.

The 2026-05-25 middle source-dependency audit classifies the remaining
full-domain \(\ODBS\) cleanup gap as a source-contract gap, not as a Lean
tactic target.  The compiled active-source cleanup map may be reused for
columns satisfying \Lean{bandedSparseAccessPaperGlobalSlotSource}, but clean
encoded sparse values outside \(s<\kappa\) still need an exact image rule, and
full-space unitarity still needs a precise reversible-extension theorem or
cited-results entry.

The 2026-05-25 middle gamma3 equality audit classifies the remaining focused
product-to-coefficient step as a QBE-local finite matrix/projection attempt.
The source paper gives the theorem, Eq.~\texttt{ROBIN clarified},
Fig.~\texttt{1 term ROBIN}, and the block-encoding definition, but no separate
finite entry proof.  Cited oracle and LCU rows remain contract-only.

The 2026-05-25 projection path audit refines that blocker: the current generic
signal-zero helper treats the system as the low-order \(n\) bits, while the
paper ket order and Lean Robin extractors place a clean trailing ancilla below
the system register.  This is projection contract drift, not an oracle gap.
The compiled paper-basis bridge fixes the slot-zero endpoints to \((4,10)\),
and the compiled path audit shows that this slot-zero path reaches row \(198\),
not row \(4\).  The next finite obligation is sparse-slot alignment for the
target coefficient, not unique-path multiplication.

The 2026-05-25 projection-slot convention map now compiles as
\Lean{oneTermRobinBlockEncodingProofRoute\_gamma3ProjectionSlotConventionMap\_n3}.
It records that the coefficient \(D_{2,5}\) uses global sparse slot \(5\), with
slot-specific clean endpoints \(90\) and \(84\), while the generic
signal-zero projection endpoints remain \(5\) and \(2\).  The source-contract
obligation
\Lean{oneTermRobinGamma3ProjectionSlotConventionObligation} remains false.

The 2026-05-25 slot-\(5\) path audit now compiles as
\Lean{oneTermRobinBlockEncodingProofRoute\_gamma3Slot5PathAudit\_n3}.  It
records that the clean subchain \(90 \to 42 \to 84\) is not the full seven-gate
path; the actual path is
\[
  90 \to 218 \to 218 \to 218 \to 170 \to 170 \to 212 \to 228.
\]
Row \(228\) has the final dagger entry, while row \(84\) has zero at that
column.  The next finite target is a projection/register field audit, not
product multiplication.  No oracle, LCU, projection, block-correctness, or
final-extraction flag has been promoted.

The 2026-05-25 projection/register decision now compiles as
\Lean{oneTermRobinGamma3ProjectionRegisterConventionDecision\_n3}.  It records
that row \(228\) and row \(84\) agree on the system row, padded-zero field,
trailing ancilla bit, \(m_f\) cleanliness, and active-source predicate, but the
indicator bit is \(1\) versus \(0\) and the sparse index is \(6\) versus \(5\).
The required convention remains a source-contract gap with
\Lean{proved = false}, and \Lean{productSearchBlocked = true}.

Middle selected sparse-register summation as the next convention target,
because Eq.~\texttt{ROBIN clarified} writes the \(\gamma_3\) branch as a sum
over \(s=0,\dots,\kappa-1\).  The planned Lean target is
\Lean{oneTermRobinGamma3SparseRegisterSummationConvention\_n3}.  It must reuse
the compiled endpoint audit, keep the indicator mismatch explicit, and must not
promote product-to-coefficient, LCU, cleanup, unitarity, projection,
block-correctness, normalized equality, or final-extraction flags.

The sparse-register summation convention now compiles as
\Lean{oneTermRobinGamma3SparseRegisterSummationConvention\_n3}, with transcript
theorem
\Lean{oneTermRobinGamma3SparseRegisterSummationConvention\_n3\_transcript}.
It records sparse-register summation as the chosen source-backed convention for
the focused \(n=3\), system-entry \((2,5)\) gamma3 audit.  The record keeps
the clean Eq.~\texttt{ROBIN clarified} endpoint \(84\) and the full
Fig.~\texttt{1 term ROBIN} endpoint \(228\) separate: both have system row
\(2\), but the clean endpoint has indicator \(0\) and sparse index \(5\),
while the full endpoint has indicator \(1\) and sparse index \(6\).

This compiled convention is still a contract map.  It does not prove that the
current block projection implements sparse-register summation, and it does not
handle the indicator mismatch.  The field
\Lean{indicatorMismatchObligation.proved = false} remains the next explicit
projection/register obligation before any gamma3 product-to-coefficient proof
search resumes.

\subsection{2026-05-25 Gamma3 Indicator-Field Gap}

The lower indicator-field check now compiles as
\Lean{oneTermRobinGamma3SparseRegisterSummation\_indicatorGap\_n3}.  It reuses
the sparse-register summation convention and records that this convention does
not explain the indicator bit: the clean endpoint \(84\) has indicator \(0\),
while the full endpoint \(228\) has indicator \(1\).  The system row still
agrees, and the sparse-index mismatch remains secondary evidence.

The next proof-map target is an exact indicator projection/register convention
for the same endpoint pair.  It must be tied to Eq.~\texttt{ROBIN clarified},
Fig.~\texttt{1 term ROBIN}, and Definition~\texttt{def:block-encoding}.  Until
that convention is stated, product-to-coefficient search, LCU correctness,
block projection, block correctness, normalized block equality, and final
extraction remain false.

\subsection{2026-05-25 Gamma3 Indicator Projection Freeze}

The indicator projection convention now compiles as
\Lean{oneTermRobinGamma3IndicatorProjectionConvention\_n3}, with transcript
theorem
\Lean{oneTermRobinGamma3IndicatorProjectionConvention\_n3\_transcript}.  The
source audit around Eq.~\texttt{ROBIN clarified},
Fig.~\texttt{1 term ROBIN}, Definition~\texttt{def:block-encoding}, and the
\(U_{\mathrm{indic}}\) paragraph found no stated rule that resets, ignores,
sums over, or permutes the indicator field after the seven-gate path.  The
Lean record therefore keeps
\Lean{indicatorRelationSpecifiedBySource = false},
\Lean{humanInputRequired = true}, and
\Lean{conventionObligation.proved = false}.

This is the active theorem-facing blocker for the focused \(n=3\),
system-entry \((2,5)\) gamma3 route.  Product-to-coefficient search must remain
parked until a source-backed indicator projection/register rule replaces that
false obligation.  The LCU, cleanup, unitarity, block projection, block
correctness, normalized equality, and final-extraction flags remain false.

The bulk-indicator source audit now compiles as
\Lean{oneTermRobinGamma3BulkIndicatorSourceAudit\_n3}, with transcript theorem
\Lean{oneTermRobinGamma3BulkIndicatorSourceAudit\_n3\_transcript}.  For the
focused column \(j=5\), the paper's \(U_{\mathrm{indic}}\) rule applies
because \(K_1=2\) and \(K_2=5\), so the source-backed indicator value is \(1\).
Lean records that endpoint \(228\) matches this source value, while clean
endpoint \(84\) has indicator \(0\).

This does not close the projection/register gap.  It makes the gap sharper:
a future convention must explain how the theorem-level block treats the
source-backed indicator \(1\) at endpoint \(228\); it must not silently reset
that field to \(0\).  Product-to-coefficient equality, LCU correctness,
cleanup, unitarity, block projection, block correctness, normalized equality,
and final extraction remain false.

\subsection{2026-05-26 Boundary \(R_y\) Corrected-Angle Decision}

Upper and middle re-audited the boundary \(R_y\) blocker against the GHL2025
source, the prior PDE block-encoding construction, and the companion Robin heat
implementation.  The local paper line Eq.~\texttt{eq:angles for Ry} writes
\(\theta_j^s=\arccos(D_j^{(s)}/\mathcal{N}_D)\), while the standard
\(R_y(\theta)\) gate used by the companion implementation has clean entry
\(\cos(\theta/2)\).  The companion Robin heat code computes boundary correction
angles with \Lean{2 * np.arccos(...)}, and the prior sparse-amplitude
construction in arXiv:2405.12855 uses the same factor-two pattern.

The faithful continuation is now recorded as a source-backed correction route:
\[
  \theta_j^s = 2\arccos(D_j^{(s)}/\mathcal{N}_D).
\]

The compiled Lean declaration is
\Lean{Examples.RobinHeat.oneTermRobinGamma3BoundaryRyCorrectedAngleSourceDecision\_n3}.
Its transcript theorem records that lower product search may resume through
this corrected-angle route.  No semantic theorem has been promoted:
product-to-coefficient equality, LCU correctness, cleanup, unitarity, block
projection, block correctness, normalized equality, circuit unitarity, and final
extraction remain false until Lean proves them.

The corrected route now has a conditional coefficient interface,
\Lean{Examples.RobinHeat.oneTermRobinGamma3BoundaryCorrectedCoefficientInterface\_n3},
and the evaluated-product rewrite
\Lean{Examples.RobinHeat.oneTermRobinBlockEncodingProofRoute\_gamma3BoundaryProductEntryEval\_correctedAngle\_n3}.
These declarations replace the free boundary factor by
\Lean{boundaryRotationNormalizedCoefficient (oneTermParameters 3) 0 2} only
under the corrected-entry hypothesis.  That hypothesis and the focused
\Lean{oneTermRobinGamma3ProductToCoefficientObligation 3 0 0} remain false.

\subsection{2026-05-27 Boundary Normalizer/Projection Audit}

Middle re-read GHL2025 Theorem~\texttt{theorem: 1 term robin},
Eq.~\texttt{ROBIN clarified}, Fig.~\texttt{1 term ROBIN}, and
Definition~\texttt{def:block-encoding}.  The corrected boundary branch now has
two compiled local facts:
\[
  \mathrm{product}_{32,32}
  =
  (f_3(0)N_f^{-1})(D_0^{(2)}N_D^{-1}),
\]
and
\[
  (A_k)_{0,0}=f_3(0)D_0^{(2)}.
\]

The remaining blocker is the theorem-level normalization convention.
Eq.~\texttt{ROBIN clarified} gives the boundary gamma3 coefficient with
denominator \(N_DN_f\kappa\), while the current Lean product uses symbolic
factors \Lean{N\_D\_inv} and \Lean{N\_f\_inv} and still needs a typed
sparse-register projection/summation convention for the \(\kappa\) factor.

The active obstruction is
\Lean{oneTermRobinGamma3BoundaryProductToCoefficientObstruction\_n3}.  Its
fields \Lean{normalizedQuotientConvention} and
\Lean{sparseRegisterProjectionConvention} remain \Lean{proved = false}.  The
next lower packet should add a narrow convention record or lemma, suggested as
\Lean{oneTermRobinGamma3BoundaryNormalizerProjectionConvention\_n3}, reusing
the existing obstruction packet.  It must not promote product-to-coefficient,
LCU, cleanup, unitarity, block projection, block correctness, normalized
equality, circuit unitarity, or final extraction.

\subsection{2026-05-27 Boundary Normalizer Split Target}

The lower convention packet now compiles as
\Lean{oneTermRobinGamma3BoundaryNormalizerProjectionConvention\_n3}.  Middle
split this packet into the next proof target
\Lean{oneTermRobinGamma3BoundaryNormalizerSplitTarget\_n3}, with transcript
theorem
\Lean{oneTermRobinGamma3BoundaryNormalizerSplitTarget\_n3\_transcript}.

The fixed theorem-facing target is still
\Lean{oneTermRobinGamma3ProductToCoefficientObligation 3 0 0}.  The split
target separates two remaining obligations:
\[
  N_D^{-1}N_f^{-1}
  \quad\text{and}\quad
  \kappa^{-1}.
\]
The first is the symbolic inverse convention for \Lean{N\_D\_inv} and
\Lean{N\_f\_inv}.  The second is the sparse-register projection factor from
the sum over \(s=0,\dots,\kappa-1\) in Eq.~\texttt{eq: ROBIN clarified}.

\[
\begin{array}{lll}
\text{Sub-obligation} & \text{Lean field} & \text{Status}\\
N_D^{-1},N_f^{-1}\text{ convention} &
\Lean{symbolicInverseObligation.proved} & \text{false}\\
\kappa^{-1}\text{ projection factor} &
\Lean{kappaProjectionObligation.proved} & \text{false}\\
\text{finite normalized block equality} &
\Lean{finiteCompositionNormalizedEquality.proved} & \text{false}\\
\text{focused product theorem} &
\Lean{productObligation.proved} & \text{false}
\end{array}
\]

No LCU, block projection, block correctness, normalized equality, circuit
unitarity, or final extraction flag is promoted by this packet.

\subsection{2026-05-27 Boundary Kappa-Projection Conditional Lemma}

The lower algebra lemma
\Lean{Examples.RobinHeat.oneTermRobinGamma3BoundaryKappaProjectionEval\_n3}
compiles.  For any coefficient environment satisfying
\[
  \mathrm{env}(N_D^{-1})\mathrm{env}(N_D)=1,\qquad
  \mathrm{env}(N_f^{-1})\mathrm{env}(N_f)=1,\qquad
  \mathrm{env}(\kappa^{-1})\mathrm{env}(\kappa)=1,
\]
the projected branch product cancels against the theorem normalizer:
\[
  \mathrm{eval}(\text{branchLocalProduct}\cdot\kappa^{-1})
  \mathrm{eval}(N_DN_f\kappa)
  =
  \mathrm{eval}(\text{targetEntry}).
\]

The packet
\Lean{Examples.RobinHeat.oneTermRobinGamma3BoundaryKappaProjectionSemantics\_n3}
records this conditional algebra as part of the boundary route.  The source
meaning of the inserted \Lean{Coeff.symbol "kappa\_inv"} remains unproved.  It
must come from the \(H_W^{(\kappa)}\) sparse-register preparation in GHL2025
Eq.~\texttt{arbitrary sparcity}, the cited Shukla--Vedula
uniform-superposition contract, and the finite block-projection convention for
the focused sparse slot.

\[
\begin{array}{lll}
\text{Proof block} & \text{Lean declaration} & \text{Status}\\
\text{symbolic } \kappa^{-1}\text{ cancellation} &
\Lean{oneTermRobinGamma3BoundaryKappaProjectionEval\_n3} &
\text{compiled under explicit hypotheses}\\
\text{projected branch packet} &
\Lean{oneTermRobinGamma3BoundaryKappaProjectionSemantics\_n3} &
\text{compiled; semantic fields false}\\
\text{uniform sparse-register preparation} &
\Lean{oneTermRobinGamma3BoundaryUniformSparseRegisterPreparationObligation\_n3} &
\text{false}\\
\text{matching sparse-register projection} &
\Lean{kappaProjectionObligation} &
\text{false}\\
\text{final normalized block equality} &
\Lean{oneTermRobinFiniteBlockCompositionContract.normalizedBlockEquality} &
\text{false}
\end{array}
\]

The next Lean-facing target is a projection/source-contract packet that
connects the compiled \Lean{kappa\_inv} algebra to the paper's
\(H_W^{(\kappa)}\) preparation and block projection.  It must not promote
product-to-coefficient, LCU, cleanup, unitarity, block projection, block
correctness, normalized equality, circuit unitarity, or final extraction.

\subsection{2026-05-27 Boundary Projection Source Contract}

The projection/source contract now compiles as
\Lean{Examples.RobinHeat.oneTermRobinGamma3BoundaryProjectionSourceContract\_n3}.
It records that the intended source of \Lean{Coeff.symbol "kappa\_inv"} is the
product of the \(H_W^{(\kappa)}\) preparation amplitude
\(\kappa^{-1/2}\) and the matching sparse-register projection amplitude
\(\kappa^{-1/2}\) for focused slot \(2\).

The transcript theorem
\Lean{Examples.RobinHeat.oneTermRobinGamma3BoundaryProjectionSourceContract\_n3\_transcript}
checks the focused data \(\kappa=7\), slot \(2\), and basis index \(32\).
It leaves the uniform-preparation obligation, matching-projection obligation,
projection-factor semantics, finite normalized equality, and focused
product-to-coefficient obligation false.

\subsection{2026-05-27 Middle Post-Projection Packet}

The next fixed Lean target is the projection-factor semantics field in
\Lean{Examples.RobinHeat.oneTermRobinGamma3BoundaryProjectionSourceContract\_n3}.
The source of the factor is now documented, but the finite projection theorem
is not proved.
\[
\begin{array}{lll}
\text{Item} & \text{Lean declaration} & \text{Status}\\
\text{source contract for }\Lean{kappa\_inv} &
\Lean{oneTermRobinGamma3BoundaryProjectionSourceContract\_n3} &
\text{compiled}\\
\text{uniform sparse-register preparation} &
\Lean{uniformPreparationObligation.proved} & \text{false}\\
\text{matching projection onto slot }2 &
\Lean{matchingProjectionObligation.proved} & \text{false}\\
\text{product represented as }\Lean{Coeff.symbol "kappa\_inv"} &
\Lean{projectionFactorSemantics.proved} & \text{false}\\
\text{focused product theorem} &
\Lean{productObligation.proved} & \text{false}.
\end{array}
\]
The lower packet should state a narrow finite projection-factor interface for
focused slot \(2\), reusing the compiled source contract and
\Lean{oneTermRobinGamma3BoundaryKappaProjectionEval\_n3}.  It must not promote
LCU, block projection, block correctness, normalized equality, circuit
unitarity, or final extraction.

\subsection{2026-05-27 Boundary Projection Factor Interface}

The finite projection-factor interface now compiles as
\Lean{Examples.RobinHeat.oneTermRobinGamma3BoundaryProjectionFactorSemantics\_n3},
with index lemma
\Lean{Examples.RobinHeat.oneTermRobinGamma3BoundaryProjectionFactorIndex\_n3}.
The index lemma checks that the prepared sparse slot and projected sparse slot
are both slot \(2\), and both sides use clean basis index \(32\).

This is not yet the projection theorem.  The remaining source-facing statement
is that the \(H_W^{(\kappa)}\) amplitude \(1/\sqrt{\kappa}\) and the matching
sparse-slot projection amplitude \(1/\sqrt{\kappa}\) multiply to the symbolic
factor \Lean{Coeff.symbol "kappa\_inv"} used by the boundary product route.
\[
\begin{array}{lll}
\text{Item} & \text{Lean declaration} & \text{Status}\\
\text{finite slot and basis agreement} &
\Lean{oneTermRobinGamma3BoundaryProjectionFactorIndex\_n3} &
\text{compiled}\\
\text{projection-factor interface} &
\Lean{oneTermRobinGamma3BoundaryProjectionFactorSemantics\_n3} &
\text{compiled; semantic fields false}\\
\text{uniform sparse-register preparation} &
\Lean{uniformPreparationObligation.proved} & \text{false}\\
\text{matching projection onto slot }2 &
\Lean{matchingProjectionObligation.proved} & \text{false}\\
\text{product represented as }\Lean{Coeff.symbol "kappa\_inv"} &
\Lean{factorSemanticsObligation.proved} & \text{false}\\
\text{focused product theorem} &
\Lean{productObligation.proved} & \text{false}.
\end{array}
\]
The next lower target remains
\Lean{oneTermRobinGamma3ProductToCoefficientObligation 3 0 0}, routed through
this projection-factor interface.  No LCU, block projection, block correctness,
normalized equality, circuit unitarity, or final extraction flag has been
promoted.

\subsection{2026-05-27 Projection-Factor Obstruction Split}

The obstruction packet now compiles as
\Lean{Examples.RobinHeat.oneTermRobinGamma3BoundaryProjectionFactorObstruction\_n3}.
It records that the remaining \Lean{kappa\_inv} theorem has two inputs: the
cited \(H_W^{(\kappa)}\) per-slot amplitude and QBE's matching sparse-slot
projection convention for slot \(2\).
\[
\begin{array}{lll}
\text{Item} & \text{Lean declaration} & \text{Status}\\
\text{obstruction packet} &
\Lean{oneTermRobinGamma3BoundaryProjectionFactorObstruction\_n3} &
\text{compiled; semantic fields false}\\
\text{cited uniform sparse-register amplitude} &
\Lean{uniformPreparationObligation.proved} &
\text{false; contract-only via }\Lean{ShuklaVedula2024.HWkappaUniformSuperposition}\\
\text{matching projection onto slot }2 &
\Lean{matchingProjectionObligation.proved} & \text{false}\\
\text{product represented as }\Lean{Coeff.symbol "kappa\_inv"} &
\Lean{factorSemanticsObligation.proved} & \text{false}\\
\text{focused product theorem} &
\Lean{productObligation.proved} & \text{false}.
\end{array}
\]
Middle's next lower packet targets the local matching-projection convention,
not the Shukla--Vedula state-preparation implementation.  It should reuse the
obstruction packet, the finite slot-index lemma, and
\Lean{oneTermRobinGamma3BoundaryKappaProjectionEval\_n3}, while leaving LCU,
block projection, block correctness, normalized equality, circuit unitarity,
and final extraction false.

\subsection{2026-05-27 Matching Projection Convention}

The matching-projection convention now compiles as
\Lean{Examples.RobinHeat.oneTermRobinGamma3BoundaryMatchingProjectionConvention\_n3}.
It states the local projection side of the focused boundary route: the bra
selects sparse slot \(2\) at clean basis index \(32\), matching the prepared
branch recorded by the finite index lemma.

This is still not the projection-amplitude theorem.  The matching projection
obligation, the cited uniform-preparation obligation, the \Lean{kappa\_inv}
factor semantics, finite normalized equality, and the focused product theorem
all remain false.
\[
\begin{array}{lll}
\text{Item} & \text{Lean declaration} & \text{Status}\\
\text{matching projection convention} &
\Lean{oneTermRobinGamma3BoundaryMatchingProjectionConvention\_n3} &
\text{compiled; semantic fields false}\\
\text{transcript check} &
\Lean{oneTermRobinGamma3BoundaryMatchingProjectionConvention\_n3\_transcript} &
\text{compiled}\\
\text{matching projection amplitude} &
\Lean{matchingProjectionObligation.proved} & \text{false}\\
\text{uniform sparse-register preparation} &
\Lean{uniformPreparationObligation.proved} &
\text{false; contract-only via }\Lean{ShuklaVedula2024.HWkappaUniformSuperposition}\\
\text{product represented as }\Lean{Coeff.symbol "kappa\_inv"} &
\Lean{factorSemanticsObligation.proved} & \text{false}\\
\text{focused product theorem} &
\Lean{productObligation.proved} & \text{false}.
\end{array}
\]
The next lower target should prove or further classify the matching projection
amplitude for slot \(2\); it should not promote product-to-coefficient, LCU,
block projection, block correctness, normalized equality, circuit unitarity, or
final extraction.

\subsection{2026-05-27 Middle Post-Matching Projection Sync}

The fixed Lean-facing convention for the matching projection is
\Lean{Examples.RobinHeat.oneTermRobinGamma3BoundaryMatchingProjectionConvention\_n3}.
It states that the projection bra selects sparse slot \(2\) at clean basis
index \(32\), the same slot and basis index used by the prepared branch.  This
is a convention packet, not the projection-amplitude theorem.

\[
\begin{array}{lll}
\text{Item} & \text{Lean declaration} & \text{Status}\\
\text{matching projection convention} &
\Lean{oneTermRobinGamma3BoundaryMatchingProjectionConvention\_n3} &
\text{compiled; semantic fields false}\\
\text{transcript check} &
\Lean{oneTermRobinGamma3BoundaryMatchingProjectionConvention\_n3\_transcript} &
\text{compiled}\\
\text{ket-side sparse-register amplitude} &
\Lean{uniformPreparationObligation.proved} &
\text{false; contract-only via }\Lean{ShuklaVedula2024.HWkappaUniformSuperposition}\\
\text{bra-side matching projection amplitude} &
\Lean{matchingProjectionObligation.proved} & \text{false}\\
\text{product represented as }\Lean{Coeff.symbol "kappa\_inv"} &
\Lean{factorSemanticsObligation.proved} & \text{false}\\
\text{focused product theorem} &
\Lean{productObligation.proved} & \text{false}.
\end{array}
\]

The next lower packet should prove or further classify the bra-side matching
projection amplitude for slot \(2\), reusing
\Lean{oneTermRobinGamma3BoundaryMatchingProjectionConvention\_n3\_transcript},
\Lean{oneTermRobinGamma3BoundaryProjectionFactorIndex\_n3},
\Lean{oneTermRobinGamma3BoundaryProjectionSourceContract\_n3\_transcript}, and
\Lean{oneTermRobinGamma3BoundaryKappaProjectionEval\_n3}.  It must not
recursively formalize Shukla--Vedula or promote product-to-coefficient, LCU,
block projection, normalized equality, block correctness, circuit unitarity, or
final extraction.

\subsection{2026-05-27 Matching Projection Amplitude Contract}

The focused bra-side projection amplitude now has a compiled contract:
\[
  \texttt{oneTermRobinGamma3BoundaryMatchingProjectionAmplitudeContract\_n3}.
\]
It fixes the matching block-projection bra to sparse slot \(2\), clean basis
index \(32\), and expected factor
\(\texttt{Coeff.symbol "sqrt\_kappa\_inv"}\).

This is still a contract.  GHL2025 Eq.~\texttt{arbitrary sparcity} and the
Shukla--Vedula cited result provide only the ket-side
\(1/\sqrt{\kappa}\) contract.  Definition~\texttt{def:block-encoding} and
Eq.~\texttt{eq: ROBIN clarified} require QBE to justify the matching
bra-side \(1/\sqrt{\kappa}\) projection factor.

\[
\begin{array}{lll}
\text{Item} & \text{Lean declaration} & \text{Status}\\
\text{bra-side amplitude contract} &
\Lean{oneTermRobinGamma3BoundaryMatchingProjectionAmplitudeContract\_n3} &
\text{compiled; semantic fields false}\\
\text{transcript check} &
\Lean{oneTermRobinGamma3BoundaryMatchingProjectionAmplitudeContract\_n3\_transcript} &
\text{compiled}\\
\text{ket-side uniform preparation} &
\Lean{uniformPreparationProved} &
\text{false; contract-only via }\Lean{ShuklaVedula2024.HWkappaUniformSuperposition}\\
\text{bra-side matching projection amplitude} &
\Lean{amplitudeContractObligation.proved} & \text{false}\\
\text{product represented as }\Lean{Coeff.symbol "kappa\_inv"} &
\Lean{factorSemanticsObligation.proved} & \text{false}\\
\text{symbolic product algebra} &
\Lean{oneTermRobinGamma3BoundaryProjectionFactorProductEval\_n3} &
\text{compiled under an explicit coefficient-environment hypothesis}\\
\text{focused product theorem} &
\Lean{productObligation.proved} & \text{false}.
\end{array}
\]

The next lower packet should start from
\Lean{oneTermRobinGamma3BoundaryMatchingProjectionAmplitudeContract\_n3\_transcript}
and either prove the finite bra-side projection amplitude for sparse slot \(2\)
or state the exact local QBE projection contract needed to accept it.  It must
not recursively formalize Shukla--Vedula or promote product-to-coefficient,
LCU composition, block projection, normalized equality, block correctness,
circuit unitarity, or final extraction.

\subsection{2026-05-27 Projection-Amplitude Semantics Contract}

The focused boundary route now has a compiled Phase-1 semantics packet:
\[
  \Lean{Examples.RobinHeat.oneTermRobinGamma3BoundaryProjectionAmplitudeSemantics\_n3}.
\]
It accepts the ket-side \(H_W^{(\kappa)}\) amplitude and the bra-side matching
projection amplitude only as contracts.  Both are represented by
\(\Lean{Coeff.symbol "sqrt\_kappa\_inv"}\).

The compiled theorem
\[
  \Lean{oneTermRobinGamma3BoundaryProjectionAmplitudeContractProductEval\_n3}
\]
proves only the symbolic product step under the explicit coefficient
hypothesis
\[
  \Lean{env "sqrt\_kappa\_inv" * env "sqrt\_kappa\_inv" = env "kappa\_inv"}.
\]
It does not prove the cited state-preparation circuit, the matching
block-projection amplitude, or the product-to-coefficient theorem.

\[
\begin{array}{lll}
\text{Item} & \text{Lean declaration} & \text{Status}\\
\text{projection-amplitude semantics packet} &
\Lean{oneTermRobinGamma3BoundaryProjectionAmplitudeSemantics\_n3} &
\text{compiled; semantic fields false}\\
\text{transcript check} &
\Lean{oneTermRobinGamma3BoundaryProjectionAmplitudeSemantics\_n3\_transcript} &
\text{compiled}\\
\text{conditional product lemma} &
\Lean{oneTermRobinGamma3BoundaryProjectionAmplitudeContractProductEval\_n3} &
\text{compiled under explicit product hypothesis}\\
\text{ket-side uniform preparation} &
\Lean{uniformPreparationObligation.proved} &
\text{false; contract-only via }\Lean{ShuklaVedula2024.HWkappaUniformSuperposition}\\
\text{bra-side matching projection amplitude} &
\Lean{braAmplitudeObligation.proved} & \text{false}\\
\text{product represented as }\Lean{Coeff.symbol "kappa\_inv"} &
\Lean{factorSemanticsObligation.proved} & \text{false}\\
\text{finite normalized block equality} &
\Lean{finiteCompositionNormalizedEquality.proved} & \text{false}\\
\text{focused product theorem} &
\Lean{productObligation.proved} & \text{false}.
\end{array}
\]

The next lower packet should target \Lean{factorSemanticsObligation} for the
fixed slot \(2\) branch, starting from
\Lean{oneTermRobinGamma3BoundaryProjectionAmplitudeSemantics\_n3\_transcript},
\Lean{oneTermRobinGamma3BoundaryProjectionAmplitudeContractProductEval\_n3},
and \Lean{oneTermRobinGamma3BoundaryKappaProjectionEval\_n3}.  It must not
recursively formalize Shukla--Vedula or promote product-to-coefficient, LCU
composition, block projection, normalized equality, block correctness, circuit
unitarity, or final extraction.

\subsection{2026-05-27 Projection-Amplitude Factor Bridge}

The focused boundary route now has a compiled conditional bridge:
\[
  \Lean{Examples.RobinHeat.oneTermRobinGamma3BoundaryProjectionAmplitudeFactorSemantics\_n3}.
\]
It records that the accepted ket and bra amplitude contracts can replace the
inserted \Lean{kappa\_inv} factor only under explicit coefficient-environment
hypotheses.

The theorem
\[
  \Lean{oneTermRobinGamma3BoundaryProjectionAmplitudeFactorEval\_n3}
\]
combines
\[
  \Lean{oneTermRobinGamma3BoundaryProjectionAmplitudeContractProductEval\_n3}
\]
with
\[
  \Lean{oneTermRobinGamma3BoundaryKappaProjectionEval\_n3}.
\]
It does not prove the \(H_W^{(\kappa)}\) amplitude, the matching projection
amplitude, or the product-to-coefficient statement.

\[
\begin{array}{lll}
\text{Item} & \text{Lean declaration} & \text{Status}\\
\text{conditional factor bridge} &
\Lean{oneTermRobinGamma3BoundaryProjectionAmplitudeFactorEval\_n3} &
\text{compiled under explicit inverse and product hypotheses}\\
\text{factor-semantics packet} &
\Lean{oneTermRobinGamma3BoundaryProjectionAmplitudeFactorSemantics\_n3} &
\text{compiled; semantic fields false}\\
\text{transcript check} &
\Lean{oneTermRobinGamma3BoundaryProjectionAmplitudeFactorSemantics\_n3\_transcript} &
\text{compiled}\\
\text{ket-side uniform preparation} &
\Lean{uniformPreparationObligation.proved} &
\text{false; contract-only via }\Lean{ShuklaVedula2024.HWkappaUniformSuperposition}\\
\text{bra-side matching projection amplitude} &
\Lean{braAmplitudeObligation.proved} & \text{false}\\
\text{product represented as }\Lean{Coeff.symbol "kappa\_inv"} &
\Lean{factorSemanticsObligation.proved} & \text{false}\\
\text{finite normalized block equality} &
\Lean{finiteCompositionNormalizedEquality.proved} & \text{false}\\
\text{focused product theorem} &
\Lean{productObligation.proved} & \text{false}.
\end{array}
\]

No product-to-coefficient, LCU composition, block projection, normalized
equality, block correctness, circuit unitarity, or final extraction was
promoted.

\subsection{2026-05-27 HW-Dagger Adjoint Convention}

The focused boundary route now has a compiled local adjoint convention for the
symbolic sparse-register preparation matrix:
\[
  \Lean{Examples.RobinHeat.oneTermRobinGamma3BoundaryHWKappaDaggerAdjointEntryConvention\_n3}.
\]
The local dagger is represented by the transpose-style matrix
\[
  \Lean{oneTermRobinGamma3BoundaryHWKappaDaggerTransposeMatrix\_n3},
\]
so the focused entry satisfies
\[
  H_W^{(\kappa)\dagger}[0,2] = H_W^{(\kappa)}[2,0].
\]

The theorem
\[
  \Lean{oneTermRobinGamma3BoundaryHWKappaDaggerEntryFromTransposeUniformColumn\_n3}
\]
still assumes the external clean-column source
\[
  H_W^{(\kappa)}[2,0] = 1/\sqrt{\kappa}.
\]
That source remains the Shukla--Vedula contract used by GHL2025
Eq.~\texttt{arbitrary sparcity}; it is not proved in this packet.

\[
\begin{array}{lll}
\text{Item} & \text{Lean declaration or field} & \text{Status}\\
\text{transpose-style local dagger} &
\Lean{oneTermRobinGamma3BoundaryHWKappaDaggerTransposeMatrix\_n3} &
\text{compiled}\\
\text{focused adjoint convention} &
\Lean{oneTermRobinGamma3BoundaryHWKappaDaggerTransposeEntryConvention\_n3} &
\text{compiled}\\
\text{conditional dagger entry from clean column} &
\Lean{oneTermRobinGamma3BoundaryHWKappaDaggerEntryFromTransposeUniformColumn\_n3} &
\text{compiled under the uniform-column hypothesis}\\
\text{adjoint convention packet} &
\Lean{oneTermRobinGamma3BoundaryHWKappaDaggerAdjointEntryConvention\_n3} &
\text{compiled}\\
\text{local adjoint convention flag} &
\Lean{adjointEntryConventionObligation.proved} & \text{true}\\
\text{clean-column uniform source} &
\Lean{uniformColumnObligation.proved} & \text{false}\\
\text{full dagger entry and bra amplitude} &
\Lean{daggerEntryProved}, \Lean{braAmplitudeProved} & \text{false}\\
\text{focused product theorem} &
\Lean{productObligation.proved} & \text{false}.
\end{array}
\]

No product-to-coefficient, LCU composition, cleanup, unitarity, block
projection, normalized equality, block correctness, circuit unitarity, or
final extraction was promoted.

\subsection{2026-05-27 Factor-Semantics Contract Map}

The focused boundary route now has a compiled contract map:
\[
  \Lean{Examples.RobinHeat.oneTermRobinGamma3BoundaryFactorSemanticsContractMap\_n3}.
\]
It is downstream of
\[
  \Lean{oneTermRobinGamma3BoundaryProjectionAmplitudeFactorSemantics\_n3}
\]
and maps the remaining factor-semantics obligation to four explicit inputs:
ket amplitude, bra amplitude, square-root product identity, and finite
normalized block composition.

The theorem
\[
  \Lean{oneTermRobinGamma3BoundaryFactorSemanticsContractMapEval\_n3}
\]
proves only the conditional coefficient step through the contract-map fields.
It assumes \(N_D^{-1}N_D=1\), \(N_f^{-1}N_f=1\),
\(\kappa^{-1}\kappa=1\), and
\(\kappa^{-1/2}\kappa^{-1/2}=\kappa^{-1}\).

\[
\begin{array}{lll}
\text{Item} & \text{Lean declaration or field} & \text{Status}\\
\text{contract map} &
\Lean{oneTermRobinGamma3BoundaryFactorSemanticsContractMap\_n3} &
\text{compiled; semantic fields false}\\
\text{conditional eval} &
\Lean{oneTermRobinGamma3BoundaryFactorSemanticsContractMapEval\_n3} &
\text{compiled under explicit environment hypotheses}\\
\text{transcript check} &
\Lean{oneTermRobinGamma3BoundaryFactorSemanticsContractMap\_n3\_transcript} &
\text{compiled}\\
\text{ket-side uniform preparation} &
\Lean{ketAmplitudeObligation.proved} & \text{false}\\
\text{bra-side matching projection amplitude} &
\Lean{braAmplitudeObligation.proved} & \text{false}\\
\text{square-root product identity} &
\Lean{productHypothesisObligation.proved} & \text{false}\\
\text{factor semantics} &
\Lean{factorSemanticsObligation.proved} & \text{false}\\
\text{finite normalized block equality} &
\Lean{finiteCompositionNormalizedEquality.proved} & \text{false}\\
\text{focused product theorem} &
\Lean{productObligation.proved} & \text{false}.
\end{array}
\]

No product-to-coefficient, LCU composition, block projection, normalized
equality, block correctness, circuit unitarity, or final extraction was
promoted.

\subsection{2026-05-27 Clean-Column To Bra-Route Handoff}

The current compiled input is
\[
  \Lean{Examples.RobinHeat.oneTermRobinGamma3BoundaryHWKappaCleanColumnContract\_n3}.
\]
It records
\[
  H_W^{(\kappa)}[2,0] = 1/\sqrt{\kappa}
\]
as a contract-only input from GHL2025 Eq.~\texttt{arbitrary sparcity} and the
Shukla--Vedula cited row.  The conditional theorem
\[
  \Lean{oneTermRobinGamma3BoundaryHWKappaCleanColumnContract\_feedsTransposeBridge\_n3}
\]
shows that this input feeds the local transpose-style dagger convention.

The next Lean task is to connect that bridge to
\[
  \Lean{oneTermRobinGamma3BoundaryBraProjectionAmplitudeSourceMap\_n3}
  \quad\text{and}\quad
  \Lean{oneTermRobinGamma3BoundaryFactorSemanticsContractMap\_n3}.
\]
Until that route map compiles, the bra amplitude, factor semantics, normalized
equality, and
\[
  \Lean{oneTermRobinGamma3ProductToCoefficientObligation\ 3\ 0\ 0}
\]
remain false.

\subsection{2026-05-27 Clean-Column To Bra-Route Contract}

The route map now compiles as
\[
  \Lean{Examples.RobinHeat.oneTermRobinGamma3BoundaryCleanColumnBraRouteContract\_n3}.
\]
It connects the contract-only clean-column bridge to the same bra-amplitude
obligation used by
\[
  \Lean{oneTermRobinGamma3BoundaryBraProjectionAmplitudeSourceMap\_n3}
  \quad\text{and}\quad
  \Lean{oneTermRobinGamma3BoundaryFactorSemanticsContractMap\_n3}.
\]
The conditional theorem
\[
  \Lean{oneTermRobinGamma3BoundaryCleanColumnBraRouteContract\_feedsBraAmplitude\_n3}
\]
rewrites a supplied clean-column entry
\[
  H_W^{(\kappa)}[2,0] = 1/\sqrt{\kappa}
\]
through the compiled transpose bridge to the expected bra factor
\(\Lean{Coeff.symbol\ "sqrt\_kappa\_inv"}\).

\[
\begin{array}{lll}
\text{Item} & \text{Lean declaration or field} & \text{Status}\\
\text{route contract} &
\Lean{oneTermRobinGamma3BoundaryCleanColumnBraRouteContract\_n3} &
\text{compiled; semantic fields false}\\
\text{conditional feed theorem} &
\Lean{oneTermRobinGamma3BoundaryCleanColumnBraRouteContract\_feedsBraAmplitude\_n3} &
\text{compiled under the external clean-column hypothesis}\\
\text{source-map bra obligation} &
\Lean{sourceMapBraAmplitudeObligation.proved} & \text{false}\\
\text{factor-map bra obligation} &
\Lean{factorMapBraAmplitudeObligation.proved} & \text{false}\\
\text{factor semantics} &
\Lean{factorSemanticsObligation.proved} & \text{false}\\
\text{focused product theorem} &
\Lean{productObligation.proved} & \text{false}.
\end{array}
\]

No product-to-coefficient, LCU composition, block projection, normalized
equality, block correctness, circuit unitarity, or final extraction was
promoted.

\subsection{2026-05-27 Current Factor-Semantics Route Packet}

The current lower-facing target is the under-contract factor-semantics bridge.
It should use
\[
  \Lean{oneTermRobinGamma3BoundaryCleanColumnBraRouteContract\_n3}
\]
as the bra-factor input to
\[
  \Lean{oneTermRobinGamma3BoundaryFactorSemanticsContractMapEval\_n3}.
\]
The bridge may assume the external clean-column amplitude, the ket-side
sparse-register amplitude, the square-root product identity, and the finite
normalized block equality as explicit contracts.  It must keep
\[
  \Lean{oneTermRobinGamma3ProductToCoefficientObligation\ 3\ 0\ 0}
\]
and all downstream block-encoding flags false.

\subsection{2026-05-27 Clean-Column To Factor-Semantics Route}

The current route now compiles as
\[
  \Lean{Examples.RobinHeat.oneTermRobinGamma3BoundaryCleanColumnFactorSemanticsRoute\_n3}.
\]
It uses
\[
  \Lean{oneTermRobinGamma3BoundaryCleanColumnBraRouteContract\_n3}
\]
as the bra-factor input to
\[
  \Lean{oneTermRobinGamma3BoundaryFactorSemanticsContractMapEval\_n3}.
\]
The theorem
\[
  \Lean{oneTermRobinGamma3BoundaryCleanColumnFactorSemanticsRouteEval\_n3}
\]
is conditional on the external clean-column amplitude
\[
  H_W^{(\kappa)}[2,0] = 1/\sqrt{\kappa}
\]
and on coefficient-environment identities for
\(\Lean{N\_D\_inv}\), \(\Lean{N\_f\_inv}\),
\(\Lean{kappa\_inv}\), and \(\Lean{sqrt\_kappa\_inv}\).  It compiles the
route into the factor-semantics calculation, but it does not prove the cited
state-preparation theorem, the ket-side amplitude, the square-root product
convention, finite LCU composition, or the focused product-to-coefficient
theorem.

\[
\begin{array}{lll}
\text{Item} & \text{Lean declaration or field} & \text{Status}\\
\text{clean-column factor route} &
\Lean{oneTermRobinGamma3BoundaryCleanColumnFactorSemanticsRoute\_n3} &
\text{compiled; semantic fields false}\\
\text{conditional route eval} &
\Lean{oneTermRobinGamma3BoundaryCleanColumnFactorSemanticsRouteEval\_n3} &
\text{compiled under external and coefficient hypotheses}\\
\text{transcript check} &
\Lean{oneTermRobinGamma3BoundaryCleanColumnFactorSemanticsRoute\_n3\_transcript} &
\text{compiled}\\
\text{clean-column source contract} &
\Lean{uniformColumnObligation.proved} & \text{false}\\
\text{ket-side sparse-register amplitude} &
\Lean{ketAmplitudeObligation.proved} & \text{false}\\
\text{square-root product identity} &
\Lean{productHypothesisObligation.proved} & \text{false}\\
\text{finite normalized block equality} &
\Lean{finiteCompositionNormalizedEquality.proved} & \text{false}\\
\text{fixed product theorem} &
\Lean{productObligation.proved} & \text{false}.
\end{array}
\]

The next lower packet should keep
\[
  \Lean{oneTermRobinGamma3ProductToCoefficientObligation\ 3\ 0\ 0}
\]
fixed and use this route to state a product-under-contracts interface, or
record the smallest missing field among ket amplitude, coefficient product
convention, finite normalized equality, and projection/product bridge.  No
product-to-coefficient, LCU composition, block projection, normalized equality,
block correctness, circuit unitarity, or final extraction flag has been
promoted.

\subsection{2026-05-27 Product Under Contracts Route}

The current route now compiles as
\[
  \Lean{Examples.RobinHeat.oneTermRobinGamma3BoundaryProductUnderContractsRoute\_n3}.
\]
It uses
\[
  \Lean{oneTermRobinGamma3BoundaryCleanColumnFactorSemanticsRoute\_n3}
\]
as the local coefficient engine and points to the fixed boundary obligation
\[
  \Lean{oneTermRobinGamma3ProductToCoefficientObligation\ 3\ 0\ 0}.
\]
The theorem
\[
  \Lean{oneTermRobinGamma3BoundaryProductUnderContractsEval\_n3}
\]
is conditional on the external clean-column amplitude
\[
  H_W^{(\kappa)}[2,0] = 1/\sqrt{\kappa}
\]
and on coefficient-environment identities for \(\Lean{N\_D\_inv}\),
\(\Lean{N\_f\_inv}\), \(\Lean{kappa\_inv}\), and
\(\Lean{sqrt\_kappa\_inv}\).  It compiles only the local coefficient
calculation.  It does not prove the Shukla--Vedula uniform-preparation
theorem, LCU composition, the finite projection/product bridge, or the focused
product-to-coefficient theorem.

\[
\begin{array}{lll}
\text{Item} & \text{Lean declaration or field} & \text{Status}\\
\text{product-under-contracts route} &
\Lean{oneTermRobinGamma3BoundaryProductUnderContractsRoute\_n3} &
\text{compiled; semantic fields false}\\
\text{conditional route eval} &
\Lean{oneTermRobinGamma3BoundaryProductUnderContractsEval\_n3} &
\text{compiled under external and coefficient hypotheses}\\
\text{transcript check} &
\Lean{oneTermRobinGamma3BoundaryProductUnderContractsRoute\_n3\_transcript} &
\text{compiled}\\
\text{clean-column source contract} &
\Lean{uniformColumnObligation.proved} & \text{false}\\
\text{ket-side sparse-register amplitude} &
\Lean{ketAmplitudeObligation.proved} & \text{false}\\
\text{square-root product identity} &
\Lean{productHypothesisObligation.proved} & \text{false}\\
\text{projection/product bridge} &
\Lean{productBridgeObligation.proved} & \text{false}\\
\text{finite normalized block equality} &
\Lean{finiteCompositionNormalizedEquality.proved} & \text{false}\\
\text{fixed product theorem} &
\Lean{productObligation.proved} & \text{false}.
\end{array}
\]

The next lower packet should keep
\[
  \Lean{oneTermRobinGamma3ProductToCoefficientObligation\ 3\ 0\ 0}
\]
fixed and target the finite projection/product bridge exposed by
\(\Lean{productBridgeObligation}\).  The finite normalized equality remains
contract-only through \(\Lean{LCU.StandardBlockEncoding}\) unless an exact
finite theorem is added.  No product-to-coefficient, LCU composition, block
projection, normalized equality, block correctness, circuit unitarity, or
final extraction flag has been promoted.

\subsection{2026-05-27 Finite Projection/Product Bridge}

The current bridge compiles as
\[
  \Lean{Examples.RobinHeat.oneTermRobinGamma3BoundaryFiniteProjectionProductBridge\_n3}.
\]
It consumes the product-under-contracts route and records the finite
block-entry indices for the focused boundary case.  Definition
\texttt{def:block-encoding} selects the signal-zero block entry for system
\((0,0)\), which is the full matrix entry \([0,0]\).  The branch-correct
sparse-slot route from Eq.~\texttt{ROBIN clarified} still lives at slot \(2\),
whose embedded basis index is \(32\).

\[
\begin{array}{lll}
\text{Item} & \text{Lean declaration or field} & \text{Status}\\
\text{finite block-entry index} &
\Lean{oneTermRobinGamma3BoundaryFiniteProjectionBlockEntryIndex\_n3} &
\text{compiled}\\
\text{finite bridge record} &
\Lean{oneTermRobinGamma3BoundaryFiniteProjectionProductBridge\_n3} &
\text{compiled; semantic fields false}\\
\text{transcript check} &
\Lean{oneTermRobinGamma3BoundaryFiniteProjectionProductBridge\_n3\_transcript} &
\text{compiled}\\
\text{branch basis equals signal block index} &
\Lean{branchBasisMatchesSignalBlockIndex} &
\text{false; }[32,32]\neq[0,0]\\
\text{branch-decomposition/projection theorem} &
\Lean{branchDecompositionObligation},\ \Lean{productBridgeObligation} &
\text{false}\\
\text{finite normalized block equality} &
\Lean{finiteCompositionNormalizedEquality} &
\text{false; contract-only through }\Lean{LCU.StandardBlockEncoding}\\
\text{fixed product theorem} &
\Lean{oneTermRobinGamma3ProductToCoefficientObligation\ 3\ 0\ 0} &
\text{false}.
\end{array}
\]

The next lower packet should define or prove the branch-decomposition interface
from the slot-\(2\) projected branch product to the signal-zero block entry.
It must keep Shukla--Vedula and LCU as contract-only dependencies and must not
promote product-to-coefficient, block projection, normalized equality, block
correctness, circuit unitarity, or final extraction.

\subsection{2026-05-28 Branch-Decomposition Packet}

The active theorem-map object remains
\[
  \Lean{Examples.RobinHeat.oneTermRobinGamma3BoundaryFiniteProjectionProductBridge\_n3}.
\]
Middle re-audited GHL2025 Theorem~\texttt{1 term robin},
Eq.~\texttt{ROBIN clarified}, Fig.~\texttt{1 term ROBIN}, and
Definition~\texttt{def:block-encoding}.  The source fixes the boundary branch
data for system \((0,0)\) and sparse slot \(2\), while the block-encoding
definition fixes the signal-zero block entry.  It does not state the finite
QBE matrix theorem that expands the signal-zero block entry as a branch sum
and selects the slot-\(2\) product.

The next Lean packet should introduce a typed interface such as
\[
  \Lean{OneTermRobinGamma3BoundaryBranchDecompositionSlot2}
  \quad\text{and}\quad
  \Lean{oneTermRobinGamma3BoundaryBranchDecompositionSlot2\_n3},
\]
or a smaller obstruction record.  The interface must consume
\[
  \Lean{oneTermRobinGamma3BoundaryFiniteProjectionProductBridge\_n3},
\]
preserve the compiled facts \([0,0]\) and \([32,32]\), and keep
\(\Lean{branchDecompositionObligation}\),
\(\Lean{productBridgeObligation}\),
\(\Lean{finiteCompositionNormalizedEquality}\), and
\[
  \Lean{oneTermRobinGamma3ProductToCoefficientObligation\ 3\ 0\ 0}
\]
false unless a build-tested theorem proves them.

\subsection{2026-05-28 Branch-Decomposition Interface}

The typed interface now compiles as
\[
  \Lean{Examples.RobinHeat.oneTermRobinGamma3BoundaryBranchDecompositionSlot2\_n3}.
\]
It consumes
\[
  \Lean{oneTermRobinGamma3BoundaryFiniteProjectionProductBridge\_n3}
\]
and records the focused index data: system entry \((0,0)\), sparse slot \(2\),
signal-zero block entry \([0,0]\), and branch-local product entry \([32,32]\).

\[
\begin{array}{lll}
\text{Item} & \text{Lean declaration or field} & \text{Status}\\
\text{interface structure} &
\Lean{OneTermRobinGamma3BoundaryBranchDecompositionSlot2} &
\text{compiled}\\
\text{focused packet} &
\Lean{oneTermRobinGamma3BoundaryBranchDecompositionSlot2\_n3} &
\text{compiled; semantic fields false}\\
\text{transcript check} &
\Lean{oneTermRobinGamma3BoundaryBranchDecompositionSlot2\_n3\_transcript} &
\text{compiled}\\
\text{signal block and branch entry equality} &
\Lean{signalBlockEntryMatchesBranchEntry} &
\text{false}\\
\text{projection-summation theorem} &
\Lean{projectionSummationObligation.proved} &
\text{false}\\
\text{product bridge} &
\Lean{productBridgeObligation.proved} &
\text{false}\\
\text{finite normalized block equality} &
\Lean{finiteCompositionNormalizedEquality.proved} &
\text{false}\\
\text{fixed product theorem} &
\Lean{oneTermRobinGamma3ProductToCoefficientObligation\ 3\ 0\ 0} &
\text{false}.
\end{array}
\]

The remaining theorem is QBE-local finite matrix bookkeeping: expand the
signal-zero block entry as a sparse-branch projection/summation and select the
slot-\(2\) contribution.  This is not supplied by Shukla--Vedula and is not
the standard LCU normalized block equality.

\subsection{2026-05-28 Branch-Entry Selection Packet}

The branch-entry side now compiles as
\[
  \Lean{Examples.RobinHeat.oneTermRobinGamma3BoundaryBranchEntrySelection\_n3},
\]
with the conditional theorem
\[
  \Lean{oneTermRobinGamma3BoundaryBranchEntrySelectionEval\_n3}.
\]
For an environment satisfying the corrected boundary \(R_y\) entry hypothesis,
the theorem evaluates the selected seven-gate entry at \([32,32]\) after
multiplying by
\(\Lean{sqrt\_kappa\_inv * sqrt\_kappa\_inv}\); the result is the route's
\(\Lean{projectedBranchProduct}\).

\[
\begin{array}{lll}
\text{Item} & \text{Lean declaration or field} & \text{Status}\\
\text{branch-entry selection packet} &
\Lean{oneTermRobinGamma3BoundaryBranchEntrySelection\_n3} &
\text{compiled; flags false}\\
\text{conditional coefficient theorem} &
\Lean{oneTermRobinGamma3BoundaryBranchEntrySelectionEval\_n3} &
\text{compiled under corrected }R_y\text{ hypothesis}\\
\text{transcript check} &
\Lean{oneTermRobinGamma3BoundaryBranchEntrySelection\_n3\_transcript} &
\text{compiled}\\
\text{corrected }R_y\text{ source entry} &
\Lean{correctedEntryHypothesis} &
\text{false}\\
\text{sparse-register amplitudes} &
\Lean{ketAmplitudeObligation},\ \Lean{braAmplitudeObligation} &
\text{false}\\
\text{signal-block projection/summation} &
\Lean{projectionSummationObligation},\ \Lean{productBridgeObligation} &
\text{false}\\
\text{fixed product theorem} &
\Lean{oneTermRobinGamma3ProductToCoefficientObligation\ 3\ 0\ 0} &
\text{false}.
\end{array}
\]

The next finite theorem must expand the signal-zero block entry \([0,0]\) as
the sparse-branch projection/summation and select the slot-\(2\) contribution.
This step is QBE-local finite matrix bookkeeping; it is not the
Shukla--Vedula state-preparation theorem and not the standard LCU normalized
block equality.

\subsection{2026-05-28 Branch-Contribution Family Interface}

The branch-sum interface now compiles as
\[
  \Lean{Examples.RobinHeat.oneTermRobinGamma3BoundaryBranchContributionFamily\_n3}.
\]
It introduces \(\Lean{branchContribution : Fin 7 -> Coeff}\), the focused
slot \(2\), and the project-local fold
\[
  \Lean{oneTermRobinGamma3BoundaryBranchContributionSum}.
\]
Lean proves only that the slot-\(2\) contribution is the selected branch
product already accepted by the projection-summation obstruction.

\[
\begin{array}{lll}
\text{Item} & \text{Lean declaration or field} & \text{Status}\\
\text{branch contribution family} &
\Lean{oneTermRobinGamma3BoundaryBranchContributionFamily\_n3} &
\text{compiled; theorem-facing fields false}\\
\text{selected slot theorem} &
\Lean{oneTermRobinGamma3BoundaryBranchContribution\_selectedSlot\_n3} &
\text{compiled}\\
\text{obstruction transcript} &
\Lean{oneTermRobinGamma3BoundaryBranchContributionObstruction\_n3\_transcript} &
\text{compiled}\\
\text{signal-zero block entry as branch sum} &
\Lean{oneTermRobinGamma3BoundaryBranchContribution\_sum\_n3} &
\text{absent}\\
\text{fixed product theorem} &
\Lean{oneTermRobinGamma3ProductToCoefficientObligation\ 3\ 0\ 0} &
\text{false}.
\end{array}
\]

The remaining local theorem is
\[
  \Lean{oneTermRobinGamma3BoundaryBranchContribution\_sum\_n3},
\]
whose target is
\[
  \Lean{oneTermRobinGamma3BoundaryBranchContributionFamily\_n3.projectionSummationStatement}.
\]
No product-to-coefficient, LCU composition, block projection, normalized
equality, block correctness, circuit unitarity, or final extraction flag has
been promoted.

\subsection{2026-05-28 Backend Projection-Summation Field Target}

The backend target now compiles as
\[
  \Lean{Examples.RobinHeat.oneTermRobinGamma3BoundaryBackendProjectionSummationFieldTarget\_n3}.
\]
It records that the existing placeholder family is only a typed convenience:
it is not a branch expansion produced by the finite projection backend for
\(\Lean{contract.expectedTarget.blockMatrix[0,0]}\).

\[
\begin{array}{lll}
\text{Item} & \text{Lean declaration or field} & \text{Status}\\
\text{acceptance predicate} &
\Lean{oneTermRobinGamma3BoundaryBackendBranchContributionPredicate\_n3} &
\text{typed}\\
\text{backend target} &
\Lean{oneTermRobinGamma3BoundaryBackendProjectionSummationFieldTarget\_n3} &
\text{compiled; theorem-facing fields false}\\
\text{transcript check} &
\Lean{oneTermRobinGamma3BoundaryBackendProjectionSummationFieldTarget\_n3\_transcript} &
\text{compiled}\\
\text{placeholder guard} &
\Lean{placeholderFamilyIsBackendSourced = false},\quad
\Lean{placeholderMayCloseProjectionSummation = false} &
\text{compiled}\\
\text{backend branch family} &
\Lean{branchContribution : Fin 7 -> Coeff}\ \text{sourced from the signal-zero block entry} &
\text{absent}\\
\text{fixed product theorem} &
\Lean{oneTermRobinGamma3ProductToCoefficientObligation\ 3\ 0\ 0} &
\text{false}.
\end{array}
\]

Middle classifies the remaining ingredient as QBE-local finite projection
bookkeeping.  It is not a Shukla--Vedula theorem and not standard LCU
composition.  The next lower target is a backend-sourced branch family
satisfying
\[
  \Lean{oneTermRobinGamma3BoundaryBackendBranchContributionPredicate\_n3},
\]
or a smaller typed obstruction explaining why
\(\Lean{BlockExtractionTarget}\) cannot yet expose that field.

\subsection{2026-05-28 Block-Extraction Backend Gap}

The narrow backend gap now compiles as
\[
  \Lean{Examples.RobinHeat.oneTermRobinGamma3BoundaryBlockExtractionBackendGap\_n3}.
\]
It ties the obstruction to the actual \(\Lean{BlockExtractionTarget}\) fields:
the signal-zero block entry and its full-unitary entry are available and equal
for the focused entry, but the backend does not expose a sparse-slot summand
family for that entry.

\[
\begin{array}{lll}
\text{Item} & \text{Lean declaration or field} & \text{Status}\\
\text{backend gap} &
\Lean{oneTermRobinGamma3BoundaryBlockExtractionBackendGap\_n3} &
\text{compiled; theorem-facing fields false}\\
\text{transcript check} &
\Lean{oneTermRobinGamma3BoundaryBlockExtractionBackendGap\_n3\_transcript} &
\text{compiled}\\
\text{block-entry bridge} &
\Lean{gap.signalBlockEntry = gap.unitaryEntry} &
\text{compiled}\\
\text{available backend fields} &
\Lean{exposesUnitaryMatrix},\ \Lean{exposesBlockMatrix},\ \Lean{exposesTargetMatrix},\ \Lean{exposesSignalIndex} &
\text{true}\\
\text{sparse-slot summand family} &
\Lean{exposesBranchContributionField} &
\text{false}\\
\text{backend branch predicate} &
\Lean{oneTermRobinGamma3BoundaryBackendBranchContributionPredicate\_n3} &
\text{typed}\\
\text{fixed product theorem} &
\Lean{oneTermRobinGamma3ProductToCoefficientObligation\ 3\ 0\ 0} &
\text{false}.
\end{array}
\]

The next local interface is a backend-sourced
\(\Lean{branchContribution : Fin 7 -> Coeff}\) for
\(\Lean{contract.expectedTarget.blockMatrix[0,0]}\) satisfying
\[
  \Lean{oneTermRobinGamma3BoundaryBackendBranchContributionPredicate\_n3}.
\]
This remains QBE-local projection bookkeeping, not a Shukla--Vedula theorem and
not standard LCU composition.

\subsection{2026-05-28 Branch-Index Map Obstruction}

The focused branch-index map now compiles as
\[
  \Lean{Examples.RobinHeat.oneTermRobinGamma3BoundaryBackendBranchFullIndex\_n3}.
\]
For the boundary system entry \((0,0)\), it maps each sparse slot
\(\Lean{s : Fin 7}\) to
\[
  \Lean{oneTermRobinGamma3PaperBasisIndex (oneTermParameters 3) s 0}.
\]
Lean proves that slot \(2\) maps to full basis index \(32\).

\[
\begin{array}{lll}
\text{Item} & \text{Lean declaration or field} & \text{Status}\\
\text{branch-index map} &
\Lean{oneTermRobinGamma3BoundaryBackendBranchFullIndex\_n3} &
\text{compiled}\\
\text{selected slot index} &
\Lean{oneTermRobinGamma3BoundaryBackendBranchFullIndex\_selected\_n3} &
\text{compiled; slot }2\text{ maps to }32\\
\text{selected summand formula} &
\Lean{oneTermRobinGamma3BoundaryBackendSelectedBranchSummandFormula\_n3} &
\text{compiled}\\
\text{obstruction packet} &
\Lean{oneTermRobinGamma3BoundaryBackendBranchIndexMapObstruction\_n3} &
\text{compiled; theorem-facing fields false}\\
\text{all-slot summand formula} &
\text{planned theorem computing }\Lean{branchContribution s}\text{ for every }\Lean{s : Fin 7} &
\text{absent}\\
\text{backend predicate closure} &
\Lean{oneTermRobinGamma3BoundaryBackendBranchContributionPredicate\_n3 backendBranchContribution} &
\text{absent}\\
\text{fixed product theorem} &
\Lean{oneTermRobinGamma3ProductToCoefficientObligation\ 3\ 0\ 0} &
\text{false}.
\end{array}
\]

The next local theorem must compute every sparse-slot summand from the finite
backend and then prove that the signal-zero block entry equals the folded
backend branch family.  This remains QBE-local projection bookkeeping; the
Shukla--Vedula and LCU rows stay contract-only.

\subsection{2026-05-28 All-Slot Backend Summand Formula}

The all-slot backend family now compiles as
\[
  \Lean{Examples.RobinHeat.oneTermRobinGamma3BoundaryBackendBranchContribution\_n3}
  \colon \Lean{Fin 7 -> Coeff}.
\]
For each sparse slot it reads the focused seven-gate matrix at the clean
full-basis branch index and attaches the two sparse-register projection
amplitudes.

\[
\begin{array}{lll}
\text{Item} & \text{Lean declaration or field} & \text{Status}\\
\text{all-slot family} &
\Lean{oneTermRobinGamma3BoundaryBackendBranchContribution\_n3} &
\text{compiled}\\
\text{selected slot theorem} &
\Lean{oneTermRobinGamma3BoundaryBackendBranchContribution\_selected\_n3} &
\text{compiled; slot }2\text{ selected}\\
\text{backend target over this family} &
\Lean{oneTermRobinGamma3BoundaryBackendBranchContributionTarget\_n3} &
\text{compiled; semantic flags false}\\
\text{selected contribution bridge} &
\Lean{oneTermRobinGamma3BoundaryBackendBranchContributionTarget\_selectedContribution\_eq\_n3} &
\text{compiled}\\
\text{branch-sum theorem} &
\Lean{BlockExtractionBranchContributionTarget.projectionSummationStatement\
oneTermRobinGamma3BoundaryBackendBranchContributionTarget\_n3} &
\text{absent}\\
\text{backend predicate closure} &
\text{second conjunct of }\Lean{oneTermRobinGamma3BoundaryBackendBranchContributionPredicate\_n3\
oneTermRobinGamma3BoundaryBackendBranchContribution\_n3} &
\text{absent}\\
\text{fixed product theorem} &
\Lean{oneTermRobinGamma3ProductToCoefficientObligation\ 3\ 0\ 0} &
\text{false}.
\end{array}
\]

The next lower target is the signal-zero branch-sum theorem.  This remains
QBE-local finite projection bookkeeping; it is not supplied by Shukla--Vedula
state preparation and not by standard LCU composition.  No product equality,
LCU composition, block projection, normalized equality, block correctness,
circuit unitarity, or final extraction flag has been promoted.

\subsection{2026-05-28 Branch-Sum Closure Packet}

The current closure packet compiles the selected slot-\(2\) predicate clause
and keeps the remaining branch sum as a named obligation.

\[
\begin{array}{lll}
\text{Item} & \text{Lean declaration or field} & \text{Status}\\
\text{selected predicate clause} &
\Lean{oneTermRobinGamma3BoundaryBackendBranchContributionPredicate\_selectedClause\_n3} &
\text{compiled}\\
\text{conditional predicate closure} &
\Lean{oneTermRobinGamma3BoundaryBackendBranchContributionPredicate\_of\_branchSum\_n3} &
\text{compiled}\\
\text{branch-sum closure packet} &
\Lean{oneTermRobinGamma3BoundaryBackendBranchSumClosure\_n3} &
\text{compiled; semantic flags false}\\
\text{transcript check} &
\Lean{oneTermRobinGamma3BoundaryBackendBranchSumClosure\_n3\_transcript} &
\text{compiled}\\
\text{signal-zero branch-sum theorem} &
\Lean{signalBlockEntry = oneTermRobinGamma3BoundaryBranchContributionSum\
oneTermRobinGamma3BoundaryBackendBranchContribution\_n3} &
\text{absent}\\
\text{block-extraction projection statement} &
\Lean{BlockExtractionBranchContributionTarget.projectionSummationStatement\
oneTermRobinGamma3BoundaryBackendBranchContributionTarget\_n3} &
\text{typed but unproved}\\
\text{fixed product theorem} &
\Lean{oneTermRobinGamma3ProductToCoefficientObligation\ 3\ 0\ 0} &
\text{false}.
\end{array}
\]

This remains QBE-local finite projection bookkeeping.  The next lower target is
the signal-zero branch-sum equality, or a smaller typed obstruction identifying
the missing projection-backend field.  Shukla--Vedula and LCU stay
contract-only.

\subsection{2026-06-05 Prepared Projection Route}

The source-correct \(\gamma_3\) route now selects the prepared singleton clean
entry as the theorem-facing projection entry.  GHL2025 Eq.~\texttt{arbitrary
sparcity} provides only the contract for \(H_W^{(\kappa)}\) preparing the
sparse register; Fig.~\texttt{fig:1 term ROBIN} acts after that preparation;
Definition~\texttt{def:block-encoding} then projects the prepared clean output.

\[
\begin{array}{lll}
\text{Item} & \text{Lean declaration or field} & \text{Status}\\
\text{prepared singleton backend bridge} &
\Lean{oneTermRobinGamma3BoundaryPreparedCompositeCleanEntryEval\_eq\_backend\_n3} &
\text{compiled under }\Lean{oneTermRobinGamma3BoundaryHWKappaUniformColumnAllSlotsStatement\_n3 H}\\
\text{theorem-facing prepared target field} &
\Lean{oneTermRobinGamma3BoundarySourcePreparedProjectionTarget\_preparedProjectionEntryEval\_eq\_backend\_n3} &
\text{compiled; theorem-facing flags false}\\
\text{product-route wiring} &
\Lean{oneTermRobinGamma3BoundaryProductUnderContractsRoute\_preparedProjectionBackendEval\_n3} &
\text{compiled; product, block, normalized equality, and final-extraction flags false}\\
\text{active/prepared selected-entry equality} &
\Lean{(oneTermRobinGamma3BoundarySourcePreparedProjectionTarget\_n3 H env).activeToPreparedSingletonEvalStatement} &
\text{absent; next finite CircuitMatrixSemantics target}\\
\text{H-free active fold} &
\Lean{oneTermRobinGamma3BoundaryBackendExpansionStatement\_iff\_uncastActiveEntryExpandedFold\_n3} &
\text{diagnostic/backlog unless recovered through the prepared route}.
\end{array}
\]

The Shukla--Vedula dependency stays contract-only.  No LCU, block-projection,
block-correctness, circuit-unitarity, normalized-equality, or final-extraction
claim is promoted by this route.

\subsection{2026-06-05 Focused Product-Obligation Prepared Bridge}

The fixed theorem-facing product obligation remains
\[
  \Lean{oneTermRobinGamma3ProductToCoefficientObligation\ 3\ 0\ 0}.
\]
The active source-correct projection entry is still the clean-clean entry
\[
  \Lean{(oneTermRobinGamma3BoundarySourcePreparedProjectionTarget\_n3\ H\ env).preparedProjectionEntry}.
\]
It is not the standalone \(H\)-free active entry.

Lean now routes the prepared projection backend equality through the finite
projection/product map by
\[
  \Lean{oneTermRobinGamma3BoundaryFocusedProductObligation\_preparedProjectionBackendEval\_n3}.
\]
This theorem consumes the all-slot clean-column contract
\[
  \Lean{oneTermRobinGamma3BoundaryHWKappaUniformColumnAllSlotsStatement\_n3\ H}
\]
only to evaluate the prepared singleton clean entry as the backend branch fold.
It does not prove the product-to-coefficient theorem.

\[
\begin{array}{lll}
\text{Item} & \text{Lean declaration or field} & \text{Status}\\
\text{focused prepared bridge} &
\Lean{oneTermRobinGamma3BoundaryFocusedProductObligation\_preparedProjectionBackendEval\_n3} &
\text{compiled; theorem-facing flags false}\\
\text{fixed product obligation} &
\Lean{oneTermRobinGamma3ProductToCoefficientObligation\ 3\ 0\ 0} &
\text{false}\\
\text{active/prepared selected-entry equality} &
\Lean{(oneTermRobinGamma3BoundarySourcePreparedProjectionTarget\_n3\ H\ env).activeToPreparedSingletonEvalStatement} &
\text{next local finite matrix target}\\
\text{raw \(H\)-free fold} &
\Lean{oneTermRobinGamma3BoundaryBackendBranchContributionTarget\_n3.backendExpansionStatement} &
\text{diagnostic/recovery only}.
\end{array}
\]

The next lower packet should prove or strictly reduce the active/prepared
selected-entry equality, equivalently the field
\[
  \Lean{(oneTermRobinGamma3BoundaryActivePreparedCircuitFieldTarget\_n3\ H\ env).activePreparedCompositeEvalStatement}.
\]
The \(H\)-free fold may be used only as a recovery lemma for this prepared
route.  Shukla--Vedula recursion, standard LCU proof search, new source
assumptions, \(O_D^{BS}\), \(O_f\), corrected \(R_y\), bulk \(j=5\), and
standalone \(H\)-free closure remain out of scope for this packet.

\subsection{2026-06-05 Direct Prepared-Clean Product Route}

The selected entry is the clean-clean entry of
\[
  \Lean{oneTermRobinGamma3BoundaryPreparedCompositeCircuitSemantics\_n3\ H}
\]
at \(\Lean{oneTermRobinGamma3BoundarySparseCleanIndex\_n3}\).  Under the
contract
\[
  \Lean{oneTermRobinGamma3BoundaryHWKappaUniformColumnAllSlotsStatement\_n3\ H},
\]
Lean routes this exact prepared entry to the fixed product-obligation map.

\[
\begin{array}{lll}
\text{Item} & \text{Lean declaration or field} & \text{Status}\\
\text{focused prepared-clean bridge} &
\Lean{oneTermRobinGamma3BoundaryFocusedProductObligation\_preparedCompositeCleanEntryBackendEval\_n3} &
\text{compiled; theorem-facing flags false}\\
\text{product-obligation prepared-clean wrapper} &
\Lean{oneTermRobinGamma3BoundaryProductToCoefficientObligation\_preparedCompositeCleanEntryBackendEval\_n3} &
\text{compiled; product theorem false}\\
\text{remaining finite matrix target} &
\Lean{(oneTermRobinGamma3BoundarySourcePreparedProjectionTarget\_n3\ H\ env).activeToPreparedSingletonEvalStatement} &
\text{absent}\\
\text{\(H\)-free active fold} &
\Lean{oneTermRobinGamma3BoundaryBackendBranchContributionTarget\_n3.backendExpansionStatement} &
\text{diagnostic/recovery only}.
\end{array}
\]

The next Lean target is the active/prepared selected-entry equality, or the
equivalent active/prepared circuit-field statement.  The external
Shukla--Vedula dependency remains contract-only.

\subsection{2026-06-05 Source-Prepared Target Product Map}

The reusable prepared backend field is
\[
  \Lean{(oneTermRobinGamma3BoundarySourcePreparedProjectionTarget\_n3\ H\ env).preparedSingletonToBackendEvalStatement}.
\]
It evaluates the prepared singleton clean entry selected by
Definition~\texttt{def:block-encoding} against the backend branch fold under
the contract
\[
  \Lean{oneTermRobinGamma3BoundaryHWKappaUniformColumnAllSlotsStatement\_n3\ H}.
\]

\[
\begin{array}{lll}
\text{Item} & \text{Lean declaration or field} & \text{Status}\\
\text{source-prepared product-map witness} &
\Lean{oneTermRobinGamma3BoundaryProductToCoefficientObligation\_sourcePreparedTargetBackendEval\_n3} &
\text{compiled; theorem-facing flags false}\\
\text{fixed product obligation} &
\Lean{oneTermRobinGamma3ProductToCoefficientObligation\ 3\ 0\ 0} &
\text{false}\\
\text{remaining active/prepared target} &
\Lean{(oneTermRobinGamma3BoundarySourcePreparedProjectionTarget\_n3\ H\ env).activeToPreparedSingletonEvalStatement} &
\text{absent}\\
\text{preferred reduced target} &
\Lean{oneTermRobinGamma3BoundaryUncastActivePreparedCompositeEvalStatement\_n3\ H\ env} &
\text{absent}\\
\text{\(H\)-free active fold} &
\Lean{oneTermRobinGamma3BoundaryBackendBranchContributionTarget\_n3.backendExpansionStatement} &
\text{diagnostic/recovery only}.
\end{array}
\]

The next lower packet should prove or strictly reduce the uncast
active/prepared selected-entry equality.  The Shukla--Vedula row stays
contract-only, and no product-to-coefficient, LCU, block-projection,
normalized-equality, block-correctness, circuit-unitarity, or final-extraction
claim is promoted.

\subsection{2026-06-05 Raw-Field Active Route}

The raw-entry prepared-sandwich field is
\[
  \Lean{(oneTermRobinGamma3BoundaryRawEntryPreparedSandwichCircuitField\_n3\ H).rawEntryPreparedSandwichStatement}.
\]
It is the remaining finite projection theorem comparing the active signal-zero
entry with the prepared \(H_W^{(\kappa)\dagger} U H_W^{(\kappa)}\) sandwich
fold.  The latest Lean route is conditional on that field.

\[
\begin{array}{lll}
\text{Item} & \text{Lean declaration or field} & \text{Status}\\
\text{raw field feeds uncast target} &
\Lean{oneTermRobinGamma3BoundaryUncastActivePreparedCompositeEval\_of\_rawEntryPreparedSandwichField\_n3} &
\text{compiled conditional route}\\
\text{raw field feeds source-prepared field} &
\Lean{oneTermRobinGamma3BoundarySourcePreparedProjectionTarget\_activeEval\_of\_rawEntryPreparedSandwichField\_n3} &
\text{compiled conditional route}\\
\text{raw prepared-sandwich field} &
\Lean{(oneTermRobinGamma3BoundaryRawEntryPreparedSandwichCircuitField\_n3\ H).rawEntryPreparedSandwichStatement} &
\text{absent}\\
\text{preferred reduced target} &
\Lean{oneTermRobinGamma3BoundaryUncastActivePreparedCompositeEvalStatement\_n3\ H\ env} &
\text{absent unconditionally}.
\end{array}
\]

The next lower packet should prove or strictly reduce the raw field or the
equivalent uncast active/prepared selected-entry equality.  The \(H\)-free
backend expansion remains diagnostic recovery only, the Shukla--Vedula row
remains contract-only, and no theorem-facing flag is promoted.

\subsection{2026-06-05 Evaluated-Fold Active Route}

The reduced local theorem is
\[
  \Lean{oneTermRobinGamma3BoundaryEvaluatedBackendFoldStatement\_n3\ env}.
\]
Under the all-slot \(H_W^{(\kappa)}\) clean-column contract, this statement now
feeds the preferred uncast active/prepared target, the source-prepared active
field, and the active/prepared circuit-field record.

\[
\begin{array}{lll}
\text{Item} & \text{Lean declaration or field} & \text{Status}\\
\text{evaluated fold feeds uncast target} &
\Lean{oneTermRobinGamma3BoundaryUncastActivePreparedCompositeEval\_of\_evaluatedBackendFold\_n3} &
\text{compiled conditional route}\\
\text{evaluated fold feeds source-prepared field} &
\Lean{oneTermRobinGamma3BoundarySourcePreparedProjectionTarget\_activeEval\_of\_evaluatedBackendFold\_n3} &
\text{compiled conditional route}\\
\text{evaluated fold feeds active/prepared circuit field} &
\Lean{oneTermRobinGamma3BoundaryActivePreparedCircuitFieldTarget\_activeEval\_of\_evaluatedBackendFold\_n3} &
\text{compiled conditional route}\\
\text{evaluated finite projection fold} &
\Lean{oneTermRobinGamma3BoundaryEvaluatedBackendFoldStatement\_n3\ env} &
\text{absent}.
\end{array}
\]

The next lower packet should prove or strictly reduce the evaluated finite
projection fold.  The standalone \(H\)-free backend expansion remains
diagnostic/recovery only through the prepared route, and no product, LCU,
block, normalized-equality, unitarity, or final-extraction flag is promoted.

\subsection{2026-06-09 Fig. 4 Transcript Correction}

The theorem-facing transcript now has a compiled Lean label list,
\[
  \Lean{GHL2025.oneTermRobinTheoremFacingFig4Circuit}.
\]
This is the object to compare with GHL2025 Theorem
\(\texttt{theorem: 1 term robin}\), Eq.~\(\texttt{ROBIN clarified}\),
Eq.~\(\texttt{arbitrary sparcity}\), Fig.~\(\texttt{fig:1 term ROBIN}\), and
Definition~\(\texttt{def:block-encoding}\).

\[
\begin{array}{lll}
\text{Source-facing slot} & \text{Lean name or label} & \text{Status}\\
H_W^{(\kappa)}\ \text{preparation} &
\Lean{Gate.oracleCall\ "H\_W\^{}(kappa)"} &
\text{visible transcript slot; contract-only}\\
U_{\mathrm{indic}}^\dagger\ \text{cleanup} &
\Lean{GHL2025.oneTermRobinGate\_U\_indic\_dagger\_selfInverseBridge} &
\text{compiled local bridge}\\
\text{pre-SWAP }O_{D^T}^{BS} &
\Lean{Gate.oracleCall\ "O\_DT\^{}BS"} &
\text{distinct theorem-facing label}\\
\text{post-SWAP }(O_D^{BS})^\dagger &
\Lean{Gate.oracleCall\ "(O\_D\^{}BS)\^{}dagger"} &
\text{cleanup semantics still unproved}\\
(H_W^{(\kappa)})^\dagger\ \text{projection side} &
\Lean{Gate.oracleCall\ "(H\_W\^{}(kappa))\^{}dagger"} &
\text{visible transcript slot; contract-only}.
\end{array}
\]

The active finite backend remains the seven-gate product guarded by
\[
  \Lean{GHL2025.oneTermRobinActiveBackendCircuit\_gateList}.
\]
It must not be called the full Fig.~\(\texttt{1 term ROBIN}\) transcript.
The next lower target remains
\[
  \Lean{oneTermRobinGamma3BoundaryUncastActivePreparedCompositeEvalStatement\_n3\ H\ env},
\]
or a strictly smaller \(\Lean{Coeff.evalWith}\) selected-entry bridge feeding
the evaluated backend fold route.  The raw symbolic \(\Lean{Coeff}\)
constructor-equality route is diagnostic/backlog, and no theorem-facing flag is
promoted by this transcript sync.

\subsection{2026-06-09 Prefix Diagnostics and Branch-Correct Frontier}

The accepted column-\(0\) prefix facts are finite
\(\Lean{Coeff.evalWith}\) facts for the active seven-gate backend entry
\((0,0)\).  They are not the displayed \(\gamma_3\) slot-\(2\) branch in
Eq.~\(\texttt{ROBIN clarified}\).

\[
\begin{array}{lll}
\text{Item} & \text{Lean declaration or target} & \text{Status}\\
\text{column-0 two-gate prefix entry} &
\Lean{oneTermRobinGamma3BoundaryDUPrefixCol0EntryEval\_n3} &
\text{compiled}\\
\text{column-0 }R_y\text{ prefix entries} &
\Lean{oneTermRobinGamma3BoundaryRDUPrefixRow0Col0\_eval\_n3},
\Lean{oneTermRobinGamma3BoundaryRDUPrefixRow1Col0\_eval\_n3} &
\text{compiled slot-0 diagnostics}\\
\text{column-0 four-gate prefix entries} &
\Lean{oneTermRobinGamma3BoundaryPrefixRow96Col0\_eval\_n3},
\Lean{oneTermRobinGamma3BoundaryPrefixRow97Col0\_eval\_n3} &
\text{compiled slot-0 diagnostics}\\
\text{displayed }\gamma_3\text{ slot-2 path map} &
\text{next proof-DAG packet} &
\text{active natural-language leaf}\\
\text{raw-entry prepared-sandwich bridge} &
\Lean{(oneTermRobinGamma3BoundaryRawEntryPreparedSandwichCircuitField\_n3\ H).rawEntryPreparedSandwichStatement} &
\text{active Lean leaf; absent}.
\end{array}
\]

The next Lean worker should prove a branch-correct raw-entry/prepared-sandwich
bridge, or a smaller \(\Lean{Coeff.evalWith}\) lemma that directly feeds it.
The column-\(0\) slot-\(0\) facts may be reused for active \((0,0)\)
diagnostics, but they must not be used as a proof of the source displayed
slot-\(2\) coefficient.  The raw symbolic \(\Lean{Coeff}\)
constructor-equality and \(\Lean{Matrix.mul_assoc}\) routes remain
diagnostic/backlog.

\subsection{2026-06-09 Source-Prepared Field Selection}

The theorem-facing prepared signal-entry target is
\[
  \Lean{oneTermRobinGamma3BoundarySourcePreparedProjectionTarget\_n3\ H\ env}.
\]
Its prepared entry is the clean entry of the local
\(H_W^{(\kappa)\dagger} U_{\gamma_3} H_W^{(\kappa)}\) singleton semantics.
The smaller missing field is
\[
  \Lean{(oneTermRobinGamma3BoundaryRawEntryPreparedSandwichCircuitField\_n3\ H).rawEntryPreparedSandwichStatement},
\]
which would connect the active signal-zero entry to the prepared sandwich
fold.

\[
\begin{array}{lll}
\text{Item} & \text{Lean declaration or target} & \text{Status}\\
\text{selected prepared projection field} &
\Lean{oneTermRobinGamma3BoundarySourcePreparedProjectionTarget\_n3\ H\ env} &
\text{compiled record; selected frontier}\\
\text{conditional backend bridge} &
\Lean{oneTermRobinGamma3BoundaryPreparedCompositeCleanEntryEval\_eq\_backend\_n3} &
\text{compiled under the external }H_W^{(\kappa)}\text{ contract}\\
\text{raw-entry prepared-sandwich field} &
\Lean{(oneTermRobinGamma3BoundaryRawEntryPreparedSandwichCircuitField\_n3\ H).rawEntryPreparedSandwichStatement} &
\text{open active Lean leaf}\\
\text{column-0 seven-gate expansion} &
\Lean{oneTermRobinGamma3BoundarySevenGateColumn0TwoPathEval\_n3} &
\text{proved diagnostic; retired from active lower work}.
\end{array}
\]

The next lower packet should not prove the seven-gate slot-\(0\) entry equal
to the all-slot prepared sandwich.  It should either feed the raw-entry
prepared-sandwich field through the selected source-prepared target, or record
a typed mismatch if the current active left-hand side is not theorem-facing
prepared semantics.  No oracle, \(H_W\), LCU, block, normalized-equality,
unitarity, or final-extraction flag is promoted.

\subsection{2026-06-09 Middle Target-Repair Frontier}

The latest compiled mismatch witness is
\[
  \Lean{oneTermRobinGamma3BoundarySourcePreparedProjectionTarget\_activeEval\_exposesUncastSevenGate\_n3}.
\]
It records that the selected source-prepared active field still has the
seven-gate \((0,0)\) entry on the left, while the active gate list omits both
\(H_W^{(\kappa)}\) and \(H_W^{(\kappa)\dagger}\).

The next theorem-facing target must expose the prepared clean entry itself:
\[
  \Lean{(oneTermRobinGamma3BoundarySourcePreparedProjectionTarget\_n3\ H\ env).preparedProjectionEntry},
\]
or the equivalent matrix entry of
\(\Lean{oneTermRobinGamma3BoundaryPreparedCompositeCircuitSemantics\_n3\ H}\).
The compiled bridge
\[
  \Lean{oneTermRobinGamma3BoundarySourcePreparedProjectionTarget\_preparedProjectionEntryEval\_eq\_backend\_n3}
\]
already evaluates that prepared entry to the backend fold under the external
\(H_W^{(\kappa)}\) clean-column contract.

\[
\begin{array}{lll}
\text{Item} & \text{Lean declaration or target} & \text{Status}\\
\text{active field mismatch} &
\Lean{oneTermRobinGamma3BoundarySourcePreparedProjectionTarget\_activeEval\_exposesUncastSevenGate\_n3} &
\text{compiled mismatch witness}\\
\text{prepared entry backend bridge} &
\Lean{oneTermRobinGamma3BoundarySourcePreparedProjectionTarget\_preparedProjectionEntryEval\_eq\_backend\_n3} &
\text{compiled conditional}\\
\text{repaired prepared signal-entry LHS} &
\text{new target-shape theorem or record consuming }\Lean{preparedProjectionEntry} &
\text{next Lean leaf}\\
\text{finite active-to-prepared composition} &
\Lean{(oneTermRobinGamma3BoundaryActivePreparedEntryTarget\_n3\ H).entryEqualityStatement} &
\text{open}\\
\text{column-0 seven-gate expansion} &
\Lean{oneTermRobinGamma3BoundarySevenGateColumn0TwoPathEval\_n3} &
\text{diagnostic only; retired}.
\end{array}
\]

This is a QBE-local finite projection/composition issue, not a new cited
subroutine.  No product, LCU, block-projection, block-correctness,
normalized-equality, unitarity, or final-extraction flag is promoted.

\subsection{2026-06-09 Middle Lower-Packet Freeze After Prepared-Entry DAG}

Define \(\mathsf{PreparedEntry}(H,\mathsf{env})\) to be
\[
  \Lean{(oneTermRobinGamma3BoundarySourcePreparedProjectionTarget\_n3\ H\ env).preparedProjectionEntry}.
\]
This is the theorem-facing clean entry of
\(H_W^{(\kappa)\dagger} U_{\gamma_3} H_W^{(\kappa)}\).  It is not the active
seven-gate \((0,0)\) entry exposed by the current active field.

Lower 1 has already supplied the source-to-Lean proof map in
\Lean{proof-attempts/QBE-AUTO-002/prepared-signal-entry-source-dag-20260609-lower1.md}.
It maps source slot \(2\) to full basis \([32,32]\) and routes the all-slot
prepared entry to the backend fold under the external \(H_W^{(\kappa)}\)
clean-column contract.

\[
\begin{array}{lll}
\text{Item} & \text{Lean declaration or target} & \text{Status}\\
\text{lower-1 source proof map} &
\Lean{prepared-signal-entry-source-dag-20260609-lower1.md} &
\text{accepted proof map}\\
\text{prepared entry backend bridge} &
\Lean{oneTermRobinGamma3BoundarySourcePreparedProjectionTarget\_preparedProjectionEntryEval\_eq\_backend\_n3} &
\text{compiled conditional}\\
\text{active-field mismatch guard} &
\Lean{oneTermRobinGamma3BoundarySourcePreparedProjectionTarget\_activeEval\_exposesUncastSevenGate\_n3} &
\text{compiled; active side remains H-free }(0,0)\\
\text{next target-shape leaf} &
\text{theorem or record consuming }\Lean{preparedProjectionEntry} &
\text{assign lower 2}\\
\text{alternate finite-composition leaf} &
\Lean{(oneTermRobinGamma3BoundaryRawEntryPreparedSandwichCircuitField\_n3\ H).rawEntryPreparedSandwichStatement} &
\text{open}\\
\text{retired routes} &
\Lean{oneTermRobinGamma3BoundarySevenGateColumn0TwoPathEval\_n3}\text{ and raw }\Lean{Coeff}\text{ equality} &
\text{diagnostic/backlog only}.
\end{array}
\]

The next Lean worker should edit only
\Lean{QuantumBlockEncoding/RobinMatrix.lean} and compile one of the active
leaves above.  The worker must not use the slot-\(0\) diagnostic as the
displayed slot-\(2\) proof and must not promote oracle, \(H_W\), \(R_y\), LCU,
block-projection, block-correctness, unitarity, normalized-equality, or
final-extraction flags.

\subsection{2026-06-09 Middle Frontier Sync After Prepared-Entry LHS Repair}

Define \(\mathsf{PreparedEntry}(H,\mathsf{env})\) to be
\[
  \Lean{(oneTermRobinGamma3BoundarySourcePreparedProjectionTarget\_n3\ H\ env).preparedProjectionEntry}.
\]
It is the clean entry of the prepared singleton semantics for
\(H_W^{(\kappa)\dagger} U_{\gamma_3} H_W^{(\kappa)}\).

The compiled theorem
\[
  \Lean{oneTermRobinGamma3BoundaryEvaluatedBackendFoldTarget\_preparedProjectionEntryBackendEval\_n3}
\]
now exposes \(\mathsf{PreparedEntry}(H,\mathsf{env})\) as the left side of the
backend evaluator and consumes
\[
  \Lean{oneTermRobinGamma3BoundarySourcePreparedProjectionTarget\_preparedProjectionEntryEval\_eq\_backend\_n3}.
\]
This retires the prepared-entry target-shape leaf.

\[
\begin{array}{lll}
\text{Item} & \text{Lean declaration or target} & \text{Status}\\
\text{prepared-entry LHS repair} &
\Lean{oneTermRobinGamma3BoundaryEvaluatedBackendFoldTarget\_preparedProjectionEntryBackendEval\_n3} &
\text{compiled; theorem-facing flags false}\\
\text{active/prepared composition} &
\Lean{oneTermRobinGamma3BoundaryUncastActivePreparedCompositeEvalStatement\_n3\ H\ env} &
\text{open active Lean leaf}\\
\text{accepted smaller field} &
\Lean{(oneTermRobinGamma3BoundaryActivePreparedEntryTarget\_n3\ H).entryEqualityStatement} &
\text{open}\\
\text{accepted raw prepared-sandwich field} &
\Lean{(oneTermRobinGamma3BoundaryRawEntryPreparedSandwichCircuitField\_n3\ H).rawEntryPreparedSandwichStatement} &
\text{open}\\
\text{next lower packet} &
\Lean{finite-active-prepared-composition-middle-packet-20260609-154329.md} &
\text{active packet}\\
\text{retired diagnostics} &
\Lean{oneTermRobinGamma3BoundarySevenGateColumn0TwoPathEval\_n3}\text{ and raw }\Lean{Coeff}\text{ equality} &
\text{diagnostic/backlog only}.
\end{array}
\]

The remaining blocker is QBE-local finite
\(\Lean{CircuitMatrixSemantics}\) composition.  It is not a new
Shukla--Vedula, Gilyen/LCU, sparse-oracle, function-oracle, or classical
theorem dependency.  No oracle, \(H_W\), \(R_y\), LCU, block-projection,
block-correctness, unitarity, normalized-equality, product-to-coefficient, or
final-extraction flag is promoted by this sync.

\subsection{2026-06-09 Middle Frontier Sync To Uncast Eval Entry}

Define \(\mathsf{ActiveEvalEntry}(\mathsf{env})\) to be the evaluated uncast
entry
\[
  \Lean{Coeff.evalWith\ env\ ((evalGateMatrices\ (GHL2025.oneTermRobinGateMatrixPlaceholders\ (oneTermParameters\ 3)))\ oneTermRobinGamma3BoundaryPrefixRow0\_n3\ oneTermRobinGamma3BoundaryPrefixRow0\_n3)}.
\]
Define \(\mathsf{BackendFold}(\mathsf{env})\) to be
\[
  \Lean{Coeff.evalWith\ env\ (blockExtractionBranchContributionSum\ oneTermRobinGamma3BoundaryBackendBranchContribution\_n3)}.
\]

The next preferred Lean target is
\[
  \Lean{oneTermRobinGamma3BoundaryEvalGateMatricesEntryEval\_eq\_backendFold\_n3\ env},
\]
which states
\(\mathsf{ActiveEvalEntry}(\mathsf{env}) =
\mathsf{BackendFold}(\mathsf{env})\).  The compiled bridge
\[
  \Lean{oneTermRobinGamma3BoundaryEvaluatedBackendFoldStatement\_iff\_uncastActiveCircuitEntryEval\_n3\ env}
\]
turns this leaf into the named evaluated backend-fold statement.

\[
\begin{array}{lll}
\text{Item} & \text{Lean declaration or target} & \text{Status}\\
\text{preferred active leaf} &
\Lean{oneTermRobinGamma3BoundaryEvalGateMatricesEntryEval\_eq\_backendFold\_n3\ env} &
\text{proposed; open}\\
\text{named evaluated fold} &
\Lean{oneTermRobinGamma3BoundaryEvaluatedBackendFoldStatement\_n3\ env} &
\text{active theorem; open}\\
\text{raw prepared-sandwich route bridge} &
\Lean{oneTermRobinGamma3BoundaryEvaluatedBackendFoldStatement\_of\_rawEntryPreparedSandwichField\_n3} &
\text{compiled conditional; raw field still open}\\
\text{backend expansion stronger leaf} &
\Lean{oneTermRobinGamma3BoundaryBackendBranchContributionTarget\_n3.backendExpansionStatement} &
\text{allowed stronger leaf; open}\\
\text{raw prepared-sandwich stronger leaf} &
\Lean{(oneTermRobinGamma3BoundaryRawEntryPreparedSandwichCircuitField\_n3\ H).rawEntryPreparedSandwichStatement} &
\text{allowed stronger leaf; open}\\
\text{retired wrapper} &
\Lean{oneTermRobinGamma3BoundaryFiniteActivePreparedComposition\_reducesToBackendFold\_n3} &
\text{compiled guard; not a smaller target}\\
\text{retired diagnostics} &
\Lean{oneTermRobinGamma3BoundarySevenGateColumn0TwoPathEval\_n3}\text{ and raw }\Lean{Coeff}\text{ equality} &
\text{diagnostic/backlog only}.
\end{array}
\]

This is a QBE-local \(\Lean{evalWith}\) finite matrix-semantics obligation
tied to \(\Lean{main.tex:2027-2035}\) after the Fig.~4 transcript and
gamma3 slot-\(2\) branch map are in place.  It does not prove ODBS, ODTS,
\(\Uf\), \(H_W\), \(R_y\), LCU, block projection, normalized equality, circuit
unitarity, block correctness, or final extraction.

\subsection{2026-06-09 Middle Support-Partition Packet Sync}

The compiled bridge
\[
  \Lean{oneTermRobinGamma3BoundaryEvaluatedBackendFold\_of\_backendExpansion\_n3\ env\ hexpansion}
\]
has one role: a future proof of
\[
  \Lean{oneTermRobinGamma3BoundaryBackendBranchContributionTarget\_n3.backendExpansionStatement}
\]
closes the named evaluated backend fold.  The bridge does not prove the
backend expansion, the raw prepared-sandwich field, or the preferred uncast
\(\Lean{evalWith}\) entry equality.

\[
\begin{array}{lll}
\text{Item} & \text{Lean declaration or target} & \text{Status}\\
\text{conditional backend route} &
\Lean{oneTermRobinGamma3BoundaryEvaluatedBackendFold\_of\_backendExpansion\_n3} &
\text{compiled; not closure}\\
\text{next support subgoal} &
\Lean{support\ partition\ lemma\ in\ RobinMatrix.lean} &
\text{next implementation subgoal}\\
\text{preferred active leaf} &
\Lean{oneTermRobinGamma3BoundaryEvalGateMatricesEntryEval\_eq\_backendFold\_n3\ env} &
\text{proposed; open}\\
\text{stronger backend leaf} &
\Lean{oneTermRobinGamma3BoundaryBackendBranchContributionTarget\_n3.backendExpansionStatement} &
\text{open}\\
\text{stronger prepared-sandwich leaf} &
\Lean{(oneTermRobinGamma3BoundaryRawEntryPreparedSandwichCircuitField\_n3\ H).rawEntryPreparedSandwichStatement} &
\text{open}\\
\text{middle packet} &
\Lean{uncast-eval-entry-support-partition-middle-packet-20260609-170218.md} &
\text{active lower packet}.
\end{array}
\]

This remains a QBE-local finite matrix/projection obligation tied to
\(\Lean{main.tex:2027-2035}\).  It uses the existing source transcript and
branch map from \(\Lean{main.tex:1098-1164}\); it does not promote any external
oracle, \(H_W\), \(R_y\), LCU, block-projection, product-to-coefficient,
normalized-equality, circuit-unitarity, block-correctness, or final-extraction
flag.

\subsection{2026-06-09 Middle Source-Prepared Active Frontier Sync}

Define \(\mathsf{SourcePreparedActive}(H,\mathsf{env})\) to be
\[
  \Lean{oneTermRobinGamma3BoundaryActivePreparedCompositeEvalStatement\_n3\ H\ env}.
\]
Its uncast selected-entry form is
\[
  \Lean{oneTermRobinGamma3BoundaryUncastActivePreparedCompositeEvalStatement\_n3\ H\ env}.
\]

The H-free uncast backend equality is now diagnostic unless it is recovered
through the prepared route.  The compiled obstruction
\[
  \Lean{oneTermRobinGamma3BoundaryEvaluatedBackendFoldTarget\_exposesExpandedSlotZeroFold\_n3\ H\ env}
\]
exposes the active \([0,0]\) entry against a weighted backend fold with
slot \(0\) carrying \(\Lean{projectionAmplitudeFactor}\) and slots \(1\)
through \(6\) still visible.  This does not by itself match the
source-prepared Fig.~\(\Lean{fig:1 term ROBIN}\) route.

\[
\begin{array}{lll}
\text{Item} & \text{Lean declaration or target} & \text{Status}\\
\text{source-prepared backend bridge} &
\Lean{oneTermRobinGamma3BoundarySourcePreparedProjectionTarget\_preparedProjectionEntryEval\_eq\_backend\_n3} &
\text{compiled conditional under the }H_W^{(\kappa)}\text{ contract}\\
\text{H-free expanded-fold obstruction} &
\Lean{oneTermRobinGamma3BoundaryEvaluatedBackendFoldTarget\_exposesExpandedSlotZeroFold\_n3} &
\text{compiled diagnostic; not closure}\\
\text{preferred source-prepared leaf} &
\Lean{oneTermRobinGamma3BoundaryActivePreparedCompositeEvalStatement\_n3\ H\ env} &
\text{open active leaf}\\
\text{preferred uncast leaf} &
\Lean{oneTermRobinGamma3BoundaryUncastActivePreparedCompositeEvalStatement\_n3\ H\ env} &
\text{open smaller leaf}\\
\text{accepted generic entry leaf} &
\Lean{(oneTermRobinGamma3BoundaryActivePreparedEntryTarget\_n3\ H).entryEqualityStatement} &
\text{open smaller leaf}\\
\text{accepted raw prepared-sandwich leaf} &
\Lean{(oneTermRobinGamma3BoundaryRawEntryPreparedSandwichCircuitField\_n3\ H).rawEntryPreparedSandwichStatement} &
\text{open stronger leaf}\\
\text{equivalent backend leaf} &
\Lean{oneTermRobinGamma3BoundaryBackendBranchContributionTarget\_n3.backendExpansionStatement} &
\text{open recovery leaf}\\
\text{retired source-closure leaf} &
\Lean{oneTermRobinGamma3BoundaryEvalGateMatricesEntryEval\_eq\_backendFold\_n3\ env} &
\text{diagnostic/blocked unless all weighted slots are matched or eliminated}.
\end{array}
\]

The next packet is
\(\Lean{proof-attempts/QBE-AUTO-002/source-prepared-active-composition-middle-packet-20260609-1732.md}\).
No oracle, \(H_W\), \(R_y\), LCU, block-projection, normalized-equality,
product-to-coefficient, circuit-unitarity, block-correctness, final-extraction,
ODBS, ODTS, or \(\Uf\) flag is promoted.

\subsection{2026-06-09 Middle Source-Prepared Branch-Sum Frontier Sync}

Define \(\mathsf{SignalBlockEntry}\) to be
\[
  \Lean{oneTermRobinGamma3BoundaryProjectionSummationObstruction\_n3.signalBlockEntry}.
\]
Define \(\mathsf{BranchSum}\) to be
\[
  \Lean{oneTermRobinGamma3BoundaryBranchContributionSum\
  oneTermRobinGamma3BoundaryBackendBranchContribution\_n3}.
\]

The compiled theorem
\[
  \Lean{oneTermRobinGamma3BoundaryBackendExpansionStatement\_equivBranchSum\_n3}
\]
identifies the backend-expansion statement with
\(\mathsf{SignalBlockEntry}=\mathsf{BranchSum}\). The proposed leaf
\[
  \Lean{oneTermRobinGamma3BoundarySignalBlockEntry\_eq\_backendBranchSum\_n3}
\]
is still open.

\[
\begin{array}{lll}
\text{Item} & \text{Lean declaration or target} & \text{Status}\\
\text{Fig. 4 transcript guard} &
\Lean{oneTermRobinTheoremFacingFig4Circuit\_gateList;\ oneTermRobinGate\_U\_indic\_dagger\_selfInverseBridge} &
\text{compiled}\\
\text{prepared clean-entry alias} &
\Lean{oneTermRobinGamma3BoundarySourcePreparedCleanEntryEval\_eq\_backendFold\_n3} &
\text{compiled conditional; stale as lower target}\\
\text{branch-sum leaf} &
\Lean{oneTermRobinGamma3BoundarySignalBlockEntry\_eq\_backendBranchSum\_n3} &
\text{next active leaf}\\
\text{backend-expansion equivalence} &
\Lean{oneTermRobinGamma3BoundaryBackendExpansionStatement\_equivBranchSum\_n3} &
\text{compiled bridge; not closure}\\
\text{backend-expansion statement} &
\Lean{oneTermRobinGamma3BoundaryBackendBranchContributionTarget\_n3.backendExpansionStatement} &
\text{open equivalent recovery leaf}\\
\text{retired H-free and raw routes} &
\Lean{EvalGateMatricesEntryEval\_eq\_backendFold;\ raw\ Coeff\ equalities} &
\text{diagnostic/backlog}.
\end{array}
\]

The next lower packet is
\(\Lean{proof-attempts/QBE-AUTO-002/source-prepared-branch-sum-middle-packet-20260609-1842.md}\).
The one-term theorem remains open. No oracle, \(H_W\), \(R_y\), LCU,
block-projection, normalized-equality, product-to-coefficient,
circuit-unitarity, block-correctness, final-extraction, ODBS, ODTS, or
\(\Uf\) flag is promoted.

\subsection{2026-06-09 Middle Source-Prepared Backend Frontier Sync}

Define \(\mathsf{PreparedCleanBackend}(H,\mathsf{env})\) to be the prepared
clean-entry evaluation theorem
\[
  \Lean{oneTermRobinGamma3BoundaryPreparedCompositeCleanEntryEval\_eq\_backend\_n3\ H\ env\ hUniform},
\]
where \(\Lean{hUniform}\) is the external \(H_W^{(\kappa)}\) clean-column
contract.

The latest compiled bridge
\[
  \Lean{oneTermRobinGamma3BoundaryBackendExpansionStatement\_of\_activePreparedEntryTarget\_n3}
\]
states that a future proof of
\[
  \Lean{(oneTermRobinGamma3BoundaryActivePreparedEntryTarget\_n3\ H).entryEqualityStatement}
\]
implies
\[
  \Lean{oneTermRobinGamma3BoundaryBackendBranchContributionTarget\_n3.backendExpansionStatement}
\]
under \(\Lean{hUniform}\).  It does not prove either statement.

\[
\begin{array}{lll}
\text{Item} & \text{Lean declaration or target} & \text{Status}\\
\text{Fig. 4 transcript guard} &
\Lean{oneTermRobinTheoremFacingFig4Circuit\_gateList;\ oneTermRobinGate\_U\_indic\_dagger\_selfInverseBridge} &
\text{compiled}\\
\text{prepared clean-entry backend bridge} &
\Lean{oneTermRobinGamma3BoundaryPreparedCompositeCleanEntryEval\_eq\_backend\_n3} &
\text{compiled conditional}\\
\text{theorem-facing prepared target bridge} &
\Lean{oneTermRobinGamma3BoundarySourcePreparedProjectionTarget\_preparedProjectionEntryEval\_eq\_backend\_n3} &
\text{compiled conditional}\\
\text{active mathematical leaf} &
\Lean{(oneTermRobinGamma3BoundaryActivePreparedEntryTarget\_n3\ H).entryEqualityStatement} &
\text{open}\\
\text{conditional bridge to backend expansion} &
\Lean{oneTermRobinGamma3BoundaryBackendExpansionStatement\_of\_activePreparedEntryTarget\_n3} &
\text{compiled; not closure}\\
\text{optional alias leaf} &
\Lean{oneTermRobinGamma3BoundarySourcePreparedCleanEntryEval\_eq\_backendFold\_n3} &
\text{proposed safe naming leaf}\\
\text{retired H-free route} &
\Lean{oneTermRobinGamma3BoundaryEvalGateMatricesEntryEval\_eq\_backendFold\_n3} &
\text{diagnostic unless all weighted slots match}\\
\text{raw \Lean{Coeff} route} &
\Lean{UnitaryEntry\_eq\_backendFold;\ EvalGateMatrices\_eq\_sevenGateMatrix} &
\text{backlog}.
\end{array}
\]

The next lower packet is
\(\Lean{proof-attempts/QBE-AUTO-002/source-prepared-clean-entry-middle-packet-20260609-1822.md}\).
No oracle, \(H_W\), \(R_y\), LCU, block-projection, normalized-equality,
product-to-coefficient, circuit-unitarity, block-correctness, final-extraction,
ODBS, ODTS, or \(\Uf\) flag is promoted.

\subsection{2026-06-09 Middle Backend-Expansion/Raw-Sandwich Packet Sync}

Define the active backend-expansion leaf to be
\[
  \Lean{oneTermRobinGamma3BoundaryBackendBranchContributionTarget\_n3.backendExpansionStatement}.
\]
It is the finite branch/projection theorem expanding the clean block entry
into the seven-slot backend branch fold.  The raw prepared-sandwich field
\[
  \Lean{(oneTermRobinGamma3BoundaryRawEntryPreparedSandwichCircuitField\_n3\ H).rawEntryPreparedSandwichStatement}
\]
and the generic prepared-entry field
\[
  \Lean{(oneTermRobinGamma3BoundaryActivePreparedEntryTarget\_n3\ H).entryEqualityStatement}
\]
are accepted route-equivalent leaves under the existing \(H_W^{(\kappa)}\)
clean-column contract.

\[
\begin{array}{lll}
\text{Item} & \text{Lean declaration or target} & \text{Status}\\
\text{active mathematical leaf} &
\Lean{oneTermRobinGamma3BoundaryBackendBranchContributionTarget\_n3.backendExpansionStatement} &
\text{open}\\
\text{raw prepared-sandwich leaf} &
\Lean{(oneTermRobinGamma3BoundaryRawEntryPreparedSandwichCircuitField\_n3\ H).rawEntryPreparedSandwichStatement} &
\text{open}\\
\text{generic prepared-entry leaf} &
\Lean{(oneTermRobinGamma3BoundaryActivePreparedEntryTarget\_n3\ H).entryEqualityStatement} &
\text{open}\\
\text{route equivalence} &
\Lean{oneTermRobinGamma3BoundaryActivePreparedCompositeEval\_iff\_evaluatedBackendFold\_n3\ H\ env\ hUniform} &
\text{compiled conditional; neither side proved}\\
\text{new middle packet} &
\Lean{backend-expansion-raw-sandwich-middle-packet-20260609-180111.md} &
\text{active lower packet}.
\end{array}
\]

The source audit uses \(\Lean{main.tex:948-955}\),
\(\Lean{main.tex:1111-1119}\), \(\Lean{main.tex:1122-1164}\), and
\(\Lean{main.tex:2027-2035}\).  The missing theorem is QBE-local finite
matrix/projection work, not a new external oracle, LCU, \(R_y\), or
block-projection dependency.

No oracle, \(H_W\), \(R_y\), LCU, block-projection, normalized-equality,
product-to-coefficient, circuit-unitarity, block-correctness, final-extraction,
ODBS, ODTS, or \(\Uf\) flag is promoted.

\subsection{2026-06-09 Middle Branch-Sum Frontier Closeout}

The latest frontier is now the local branch-sum equality
\[
  \Lean{oneTermRobinGamma3BoundaryProjectionSummationObstruction\_n3.signalBlockEntry}
  =
  \Lean{oneTermRobinGamma3BoundaryBranchContributionSum\
  oneTermRobinGamma3BoundaryBackendBranchContribution\_n3}.
\]
The source-prepared clean-entry alias is compiled and stale as a lower target.
The branch-sum equality is equivalent to
\[
  \Lean{oneTermRobinGamma3BoundaryBackendBranchContributionTarget\_n3.backendExpansionStatement}
\]
through
\[
  \Lean{oneTermRobinGamma3BoundaryBackendExpansionStatement\_equivBranchSum\_n3}.
\]
The assigned middle packet is
\[
  \Lean{proof-attempts/QBE-AUTO-002/source-prepared-branch-sum-middle-packet-20260609-1842.md}.
\]
The one-term theorem remains open, and no semantic flag is promoted.
