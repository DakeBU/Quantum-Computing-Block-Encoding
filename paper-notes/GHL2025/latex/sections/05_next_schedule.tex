\section{Next Schedule}

The next multi-hour runs should use a cheaper loop:

\begin{itemize}
\item run upper, middle, and one lower agent per cycle;
\item skip reviewer-agent calls for ordinary local proof-DAG continuation;
\item still run \Lean{python3 tools/qbe.py check} after each cycle;
\item run one full reviewer and proof-export pass at the end of the batch.
\end{itemize}

Use a full reviewer cycle when a proof flag changes from false to true, an
oracle contract changes, a cited result changes status, final block extraction
is touched, or this proof export is updated.

\subsection{Current Source-Contract Gate}

The SWAP finite-domain permutation bridge is complete.  Lean proves
\Lean{swapOracleImage_injective}, \Lean{swapOracleImage_bijective}, the row and
column uniqueness lemmas for \Lean{swapOracleMatrix}, and
\Lean{swapOracleMatrix_is_permutation}.  The gate-level field
\Lean{(GHL2025.oneTermRobinGate_SWAP p).unitary.proved} is now true.

The remaining critical-path gate is the corrected global-slot \(O_D^{BS}\)
source contract.  GHL2025 Lemma~1 gives
\[
  \hat O_D^{BS}\ket{0}^{n-l}\ket{s}^{l}\ket{i}^{n}
  =
  \ket{r_{si}}^{n}\ket{i}^{n},
\]
and Fig.~1-term Robin requires \((O_D^{BS})^\dagger\) to restore the padded
zero register after SWAP.  The active Lean address now uses
\Lean{oneTermRobinGlobalSparseAddress}, and the active clean source predicate
is \Lean{bandedSparseAccessPaperGlobalSlotSource}: padded clean input with
sparse slot \(s<\kappa\).  The old row-dependent unused-branch collision is
retained only as rejected-model memory under
\Lean{QBE.ODBS.UnusedZeroBranchExtension}.

The compiled local route now includes
\Lean{oneTermRobinGlobalSparseAddress\_inverseSlot\_unique\_of\_lt\_seven},
\Lean{bandedSparseAccessPaperImage\_injective\_on\_globalSlotSource}, and
\Lean{bandedSparseAccessPaperPostSwapPreimageCandidate\_unique\_on\_globalSlotSource}.
The last theorem proves that any active global-source preimage of the
post-SWAP target is the named candidate.  This is still not a semantic cleanup
proof: \Lean{BandedSparseAccessGlobalSlotInverseOnRangeContract} keeps its
inverse, unique-preimage, image-injectivity, cleanup, and unitary-extension
fields false.

The current cleanup-route bridges also include
\Lean{defaultBandedSparseAccessPaperContract\_cleanupRouteBridge},
\Lean{bandedSparseAccessGlobalSlotInverseOnRangeContract\_restrictedDaggerColumnCleanup},
\Lean{bandedSparseAccessGlobalSlotInverseOnRangeContract\_restrictedDaggerColumnIndicator},
and the theorem-route wrapper
\Lean{oneTermRobinBlockEncodingProofRoute\_odbsRestrictedDaggerColumnIndicator}.
The first bridge exposes the candidate post-SWAP cleanup route through the
default Lemma~1 paper contract.  The restricted column theorem packages the
candidate entry \Lean{Coeff.rat 1} and zero entries for every other active
global-source row into the same contract post-SWAP column.  The indicator
theorem restates that column as a single if-then-else formula, and the route
wrapper exposes it through
\Lean{oneTermRobinBlockEncodingProofRoute}.  This is clean-domain evidence
only; all semantic cleanup, unitarity, LCU, circuit-unitary, projection, and
block-correctness flags remain false.

\subsection{Next Allowed Lower Packets}

Do not assign proof search for \(O_D^{BS}\) injectivity, dagger cleanup,
unitarity, or final block extraction against the row-dependent helper
predicates.  The reviewed bridge
\Lean{bandedSparseAccessGlobalSlotInverseOnRangeContract\_uniquePreimageBridge}
now connects the compiled global-source unique-preimage theorem to the
cleanup-route contract fields while keeping every semantic flag false.

The completed narrow packet is:
\begin{itemize}
\item
\Lean{bandedSparseAccessGlobalSlotInverseOnRangeContract\_restrictedDaggerColumnIndicator}
now states the non-promoting active-domain indicator theorem;
\item
\Lean{oneTermRobinBlockEncodingProofRoute\_odbsRestrictedDaggerColumnIndicator}
exposes the same theorem through the theorem route while keeping
\Lean{blockProjection.proved}, \Lean{blockCorrect.proved}, circuit-unitarity,
block-extraction, and LCU flags false;
\item keep \Lean{bandedSparseAccessPaperValidCleanSource} and
\Lean{bandedSparseAccessPaperUnusedSparseBranch} as rejected-model or audit
helpers only.
\end{itemize}

The completed guard packets are:
\begin{itemize}
\item
\Lean{oneTermRobinBlockEncodingProofRoute\_odbsActiveGlobalSourceCleanupInterface}
packages the restricted indicator with
\Lean{oneTermRobinBlockEncodingProofRoute\_odbsCleanupScopeDecision} and keeps
all semantic flags false;
\item
\Lean{bandedSparseAccessCleanupScopeDecision\_priorPDESourceTranscriptGuard}
records the prior PDE sparse-access source as transcript-only data: the
resource claim is unproved, no Robin-specific reversible-extension image rule
is selected, and lower proof search remains disabled.
\end{itemize}

The next allowed packet is:
\begin{itemize}
\item keep \(O_D^{BS}\) cleanup, injectivity, unitarity, LCU, projection, and
block-correctness proof search blocked until the full clean-domain or
full-space contract is sharpened;
\item if a full-space extension is needed, add a cited-results row with an
exact statement and a matching Lean contract before lower work depends on it;
\item if no such statement is available, move to another fixed Phase~1
transcript block, such as \(O_{D^T}^{S}\) normalizer obligations or \(O_f\)
source contracts.
\end{itemize}

Disallowed packets are \(O_D^{BS}\) permutation or unitarity over the old
row-dependent image, \(O_f\) analytic closure without a formalized cited
theorem, LCU closure, and final block extraction.

\subsection{Source Decision Recorded}

Middle's 2026-05-24 cleanup-scope packet records the next theorem domain as
active global-source only.  This is a scheduling decision, not a semantic
cleanup proof.  The faithful route still needs one of the following before
\(O_D^{BS}\) work can promote a semantic flag:

\begin{center}
\begin{tabular}{p{0.42\linewidth}p{0.46\linewidth}}
\hline
Decision & Effect \\
\hline
completed active global-source interface &
compiled guard; still insufficient for semantic cleanup or unitarity promotion \\
named reversible-extension theorem &
add a cited-results row with exact statement and Lean target before lower uses it \\
approved exploratory work &
create a separate exploratory task; do not change the faithful route \\
no proof &
keep \(O_D^{BS}\) cleanup, unitarity, and block flags false \\
\hline
\end{tabular}
\end{center}

Until then, \Lean{QBE.ODBS.UnusedZeroBranchExtension} remains historical
rejected-model memory, not the active lower target.

The block-projection normalizer audit, source-gate freeze guard,
wrapper-slot freeze guard, global-source inverse interface, seven-slot
reverse-address uniqueness block, active-image injectivity block,
post-SWAP unique-preimage block, restricted dagger-column cleanup block,
active-global-source cleanup interface, and prior-PDE transcript guard are now
compiled.  They are scheduling constraints for future work, not permission to
promote \(O_D^{BS}\) cleanup, unitarity, LCU closure, or final block
extraction.
