name: Vandaele Definition 2.4 semantics frontier

on:
  pull_request:
    branches:
      - "agent/vandaele-comparator-v2-semantics"
    paths:
      - "QuantumBlockEncoding/VandaeleLadderContract.lean"
      - "QuantumBlockEncoding/VandaeleLadderEquationFiveSemantics.lean"
      - "QuantumBlockEncoding/VandaeleLadderInverseSemantics.lean"
      - "QuantumBlockEncoding/VandaeleLadderVSemantics.lean"
      - ".github/workflows/vandaele-definition24-semantics.yml"
  workflow_dispatch:

concurrency:
  group: vandaele-definition24-semantics-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  definition24-semantics:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Set up Lean and caches
        uses: leanprover/lean-action@v1
        with:
          auto-config: false
          use-github-cache: true
          use-mathlib-cache: true

      - name: Compile Definition 2.4 semantics frontier
        run: |
          set -euxo pipefail
          lake build QuantumBlockEncoding.VandaeleLadderVSemantics
