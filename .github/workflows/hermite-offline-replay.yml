name: Retired compiler snapshot (not Hermite acceptance)
on:
  workflow_dispatch:
permissions: {}
jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Explain the retired transport workflow
        run: |
          echo "Archive transport is retired; no sources, logs, compiler or dependency archives are uploaded."
          echo "Run the Hermite state-preparation gate for actual compilation and independent executable replay."
          exit 1
